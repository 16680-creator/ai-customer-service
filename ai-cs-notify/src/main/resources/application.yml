server:
  port: 8086

spring:
  application:
    name: ai-cs-notify
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:localhost:8848}
        namespace: ${NACOS_NAMESPACE:aics}
      config:
        server-addr: ${NACOS_ADDR:localhost:8848}
        namespace: ${NACOS_NAMESPACE:aics}

rocketmq:
  name-server: ${ROCKETMQ_NAMESRV:localhost:9876}
