# 通知服务部署
apiVersion: v1
kind: Service
metadata:
  name: notify-service
  namespace: ai-customer-service
  labels:
    app: notify-service
spec:
  type: ClusterIP
  ports:
    - port: 8086
      targetPort: 8086
      name: http
  selector:
    app: notify-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notify-service
  namespace: ai-customer-service
  labels:
    app: notify-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notify-service
  template:
    metadata:
      labels:
        app: notify-service
    spec:
      containers:
        - name: notify-service
          image: aics/notify-service:1.0.0
          ports:
            - containerPort: 8086
              name: http
          env:
            - name: NACOS_ADDR
              value: "nacos:8848"
            - name: NACOS_NAMESPACE
              value: "aics"
            - name: ROCKETMQ_NAMESRV
              value: "rocketmq-namesrv:9876"
            - name: JAVA_OPTS
              value: "-Xms256m -Xmx256m"
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8086
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8086
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
