# 02-中间件补全开发计划

> 2026-08 制定。来源：全项目中间件扫描（根 `docker-compose.yml`、`deploy/docker-compose/*.yml`、
> `deploy/k8s/`、各服务 `application.yml`、代码连接点）。
> 与 [01-Spring技术补全开发计划](01-Spring技术补全开发计划.md) 平行推进，无前置依赖；
> 其中 P1（部署缺口补齐）越早做越好——它让已有代码真正跑在完整的基础设施上。

## 〇、扫描结果全景

### A. 已使用且部署完整 ✅

| 中间件 | 版本 | 用途 | 接入点 |
|--------|------|------|--------|
| MySQL | 8.0 | 业务库；ShardingSphere 分库分表（user，ID 后四位取模） | 除 gateway 外全部业务模块；另有 `master-slave` 编排（1 主 2 从） |
| Redis | 7 | 缓存、会话、分布式锁（Redisson） | chat/order/product；master-slave 编排含 3 节点 |
| Nacos | 2.3.2 | 注册中心 + 配置中心 | 全部微服务；`tools/nacos-config` 有发布脚本 |
| RocketMQ | 5.1.4 | 异步消息、事务消息、延迟消息 | chat/knowledge/message/notify/order/pay + `ai-cs-mq`（rocketmq-tools 运维） |
| Elasticsearch | 8.12.0 | 全文检索、混合检索 RRF | search（原生 java client）+ chat |
| MinIO | latest | 对象存储（文档/图片） | common 封装 `FileStorageService` |
| Seata Server | 1.7.1 | AT 分布式事务 TC（file 模式） | order/product（`host1`/`master-slave` 编排 + `tools/seata`） |
| Prometheus + Grafana + Tempo + OTel Collector | 2.51 / 10.4 / 2.4.1 / 0.97 | 指标 + 链路追踪 | 全服务 actuator；chat OTLP 导出 |

### B. 缺口一：代码已用、部署缺失 ⚠️（P1 解决）

| 中间件 | 代码锚点 | 部署现状 |
|--------|----------|----------|
| **Chroma 向量库** | chat/knowledge/search 的 `spring-ai-starter-vector-store-chroma`（默认端口 8000） | 所有 compose 均无该服务，靠手工启动，未持久化 |
| **Neo4j** | chat 的 `neo4j-java-driver`（GraphRAG） | 无任何部署物 |
| **XXL-Job Admin** | order 的 `XxlJobConfig`；`application.yml` 注释明确「本地无 admin 也能启动」 | 有 `deploy/mysql/xxl-job-init.sql` 建表脚本，无 admin 容器 → 执行器默认关闭 |
| **Sentinel Dashboard** | chat 的 `spring-cloud-starter-alibaba-sentinel` | 无 dashboard，流控规则只能硬编码 |

### C. 缺口二：能力缺口（代码未用、项目场景需要）

| 中间件 | 场景与现状 | 优先级 |
|--------|------------|--------|
| **MongoDB** | AI 对话记录目前**只写 Redis List**（`ChatHistoryServiceImpl`，易失、无法分页检索）；MongoDB 是会话/半结构化对话数据的教科书场景 | 高 |
| **Canal（CDC）** | MySQL → ES 商品索引同步目前只有离线脚本 `deploy/scripts/sync-kb-to-es.py`；商品改价/上下架后搜索结果不实时 | 高 |
| ClickHouse | 模型用量/对话日志 OLAP 分析（MySQL 已有 `llm-observability-init.sql`，message 有用量配额） | 中（可选） |
| Vault | JWT 密钥、MinIO AK/SK、支付密钥的集中管理 | 中（可选） |
| RabbitMQ / Kafka | **不引入**：RocketMQ 栈已定，再引一套 MQ 是负资产；Debezium 生态绑 Kafka，因此 CDC 选 Canal | 不做 |
| WebRTC / ASR / TTS | 语音客服、音视频人工坐席 | 远期可选 |

---

## 一、P1：部署缺口补齐（4 个中间件进编排）｜2~3 天

### 方案

根 `docker-compose.yml` 与 `deploy/docker-compose/docker-compose-all.yml`、`deploy/k8s/` 三处同步补齐：

| 服务 | 镜像 | 端口 | 要点 |
|------|------|------|------|
| chroma | `chromadb/chroma:0.5` | 8000 | 数据卷持久化（否则重启丢向量库）；与 chat/knowledge/search 的 `CHROMA_PORT` 对齐 |
| neo4j | `neo4j:5-community` | 7687 / 7474 | `NEO4J_AUTH` 环境变量；数据卷 |
| xxl-job-admin | `xuxueli/xxl-job-admin:2.4.1` | 8099 | **版本必须与 xxl-job-core 2.4.1 匹配**；依赖 `xxl-job-init.sql` 建库（排在 mysql 之后，加 depends_on） |
| sentinel-dashboard | `bladex/sentinel-dashboard:1.8.6` | 8858 | 与 SCA 2023.0.1.0 自带的 sentinel-core 版本对齐 |

应用侧改动：

1. `deploy/k8s/` 新增 `chroma.yaml / neo4j.yaml / xxl-job.yaml / sentinel-dashboard.yaml`
2. 连接地址全部走环境变量（XXL_JOB_ADMIN 已有此模式）或进 Nacos 配置
3. XXL-Job 执行器默认值从 `enabled: false` 改为由 `XXL_JOB_ADMIN` 是否配置决定，
   并在 `learning-docs/07-运维部署` 更新一键启动说明
4. Sentinel 规则持久化：dashboard 手改规则重启即丢，落地「push 模式 → Nacos 持久化」
   （chat 监听 Nacos 的 `sentinel-rule` dataId）

### 任务清单（✅ 2026-08 编排落地，技术文档见 [04-中间件/09](../04-中间件/09-AI与治理中间件部署.md)）

- [x] 根 `docker-compose.yml` 补 Chroma/Neo4j/XXL-Job Admin/Sentinel Dashboard（healthcheck、卷、网络）；
      xxl-job-init.sql 加 MySQL 初始化挂载
- [x] K8s 四份 manifest（StatefulSet/PVC 或 Deployment）+ Secret 模板 + k8s apply/rollout 脚本接入
- [ ] `docker compose up -d` 运行验收：本机无 Docker CLI（exit 127）阻塞；验收步骤已写文档
- [ ] Sentinel 规则 Nacos 持久化改造：需 Dashboard/ Nacos 实际联调环境后补
- [x] 部署与选型技术文档产出

### 验收标准

无手工步骤冷启动全部中间件；重启 compose 后向量库/图库数据不丢；XXL-Job 控制台能看到
order 执行器在线且 `OrderTimeoutScanJob` 可在页面手动触发。

### 面试要点

向量库选型（Chroma 轻量原型 vs Milvus 分布式 vs pgvector 复用 PG）；图数据库在 GraphRAG
的角色；调度中心与执行器的 RPC 注册模型；Sentinel 三种规则推送模式（内存/拉/push-Nacos）
及其一致性代价。

---

## 二、P2：MongoDB 对话记录持久化｜3 天

### 现状锚点

- `ai-cs-chat/.../ChatHistoryServiceImpl.java`：会话历史用 `StringRedisTemplate` 的
  List rightPush / range 存取，**重启或过期即丢**，且无法按时间分页检索
- MySQL 只存结构化业务数据；把多轮对话 JSON 塞 MySQL 会遇到 schema 演进和索引难题

### 方案

1. chat 模块引入 `spring-boot-starter-data-mongodb`（遵守依赖方向：repository 放业务模块，
   不进 common）
2. `ChatMessageDocument`：sessionId / userId / role / content / model / tokens /
   createdAt / feedback；`(sessionId, createdAt)` 复合索引
3. 写入链路：对话完成后由 `ChatHistoryMessage` 消费点**双写过渡**——Redis 继续做热窗口
   （最近 N 轮、上下文注入用），Mongo 全量落库；稳定后 Redis 只保留热数据
4. 读取链路：注入上下文仍走 Redis；历史查询接口切 Mongo 分页
5. 清理策略：TTL 索引（90 天）+ 退档说明

### 任务清单（✅ 2026-08 低风险归档模式落地，技术文档见 [04-中间件/10](../04-中间件/10-MongoDB对话审计归档.md)）

- [x] chat 引入 MongoDB document/repository/archive service + compose mongo:7.0（持久化卷）
- [x] 保留 Redis 热窗口 + RocketMQ/message 表事实源；Mongo 仅可选最佳努力审计归档，
      `MONGO_ARCHIVE_ENABLED=false` 默认关闭，归档故障不阻断 AI 回复
- [x] 复合索引 `(sessionKey, createdAt)` 与 archive 服务成功/故障单测（2 tests 全绿）
- [ ] 审计查询 API + TTL 90 天 → 管理端实际需求明确后实施（当前无历史审计 UI）
- [ ] Mongo 运行验收 → 受本机无 Docker CLI 阻塞

### 验收标准

会话跨重启完整可查；按 sessionId 分页 + 按用户检索两个接口上线；Redis 内存占用明显下降
（只存热窗口）。

### 面试要点

文档模型 vs 关系模型选型理由；TTL 索引原理；writeConcern `w:majority` 与会话数据不丢的
关系；为什么不用 MySQL 存对话 JSON。

---

## 三、P3：Canal CDC 实时同步（MySQL → RocketMQ → ES）｜3~4 天

### 现状锚点

- `deploy/scripts/sync-kb-to-es.py`：知识库 → ES 靠**离线 Python 脚本**批量同步
- search 模块只有读路径（`SearchService` / `HybridSearchServiceImpl`），全项目无人写
  商品索引（grep 无 `IndexRequest/BulkRequest`）→ 商品改价、上下架后搜索结果滞后

### 方案

选 **Canal** 而非 Debezium：项目 MQ 是 RocketMQ（Debezium 绑 Kafka Connect），Canal 原生
支持投递 RocketMQ，且伪装 MySQL slave 的架构面试好讲。

```
MySQL binlog(ROW) → canal-server(instance: product) → RocketMQ topic: c-product-sync
→ ai-cs-search 新增消费者 → ES 商品索引幂等 upsert
```

1. MySQL 开 binlog：compose 的 command 加 `--log-bin --binlog-format=ROW --binlog-row-image=FULL`，
   建 canal 账号并授权 REPLICATION SLAVE
2. canal-server 1.1.7（compose + k8s 各一份）；instance 只监听 product 库相关表，过滤字段
3. search 新增 `ProductIndexSyncConsumer`：
   - 幂等：ES document `_id = productId`，upsert 天然幂等
   - 乱序：消息体带 binlog 时间戳/位点，旧版本直接丢弃（比较内存缓存或 ES 侧 version）
   - 删除事件 → 删索引文档
4. 兜底三件套：`sync-kb-to-es.py` 保留为全量初始化；新增定时对账任务（MySQL count vs
   ES count，不一致告警）；消费失败进死信 topic

### 任务清单（⚠️ 2026-08 分阶段落地，技术文档见 [04-中间件/11](../04-中间件/11-CanalCDC商品索引同步.md)）

- [x] MySQL binlog ROW/FULL + canal 最小权限账号初始化 SQL（compose MySQL 挂载）
- [x] search `ProductCdcConsumer` + `c-product-sync` RocketMQ 契约；ES `_id=product.id` upsert/DELETE 幂等；
      默认关闭，2 单测 + search 编译通过
- [ ] Canal Server/Adapter compose + k8s：需 Docker 环境锁定同版本（1.1.7）后按真实 Adapter 消息结构配置，
      不凭空提交可能跑不通的镜像配置
- [ ] 改价/下架/删品端到端集成测试、对账任务与 Grafana 告警：依赖上述运行环境/Canal 联调
- [x] binlog 原理、Canal 架构、幂等/乱序/对账技术文档产出

### 验收标准

商品改价后 **秒级** 在搜索生效；下架商品从搜索消失；重复/乱序 binlog 消息不产生脏数据
（单测证明）。

### 面试要点

binlog 三种格式与 ROW 模式理由；canal 伪装 slave 的握手协议；CDC 链路的幂等/乱序/对账
三板斧；为什么缓存与索引失效不用「删缓存」而用 CDC 订阅变更流。

---

## 四、P4：ClickHouse 模型用量分析（可选）｜3 天

### 现状锚点

- MySQL 已有 `deploy/mysql/llm-observability-init.sql`（模型用量表）；
  chat 的 `ModelUsageRecorder` / `OnlineEvalService` 持续产生指标数据；message 有
  `ModelUsageQuotaController`
- 用量明细是典型追加写 + 聚合分析负载，MySQL 大表聚合会拖垮 OLTP

### 方案

1. `clickhouse/clickhouse-server:24`（单机起步）；compose/k8s 各一份
2. 写入：`ModelUsageRecorder` 产出的事件 → RocketMQ → CH 批量写入（1s/批）；
   或用 Java client 微批直写（先简后繁）
3. 建模：按天分区、`MaterializedView` 预聚合（日 tokens / 成本 / 缓存命中率 / 模型维度）
4. Grafana 自带 ClickHouse 插件建看板：用量趋势、成本 TOP 用户、配额水位

### 任务清单

- [ ] CH 服务 + 建表（分区键/TTL 180 天）；写入链路与批量大小压测
- [ ] 两张物化视图 + Grafana 看板
- [ ] MySQL 侧只留最近 30 天热数据，归档策略文档化

### 面试要点

列存的压缩与向量化执行原理；OLTP/OLAP 分库边界与双写一致性；物化视图在预聚合的角色。

---

## 五、P5：Vault 密钥管理（可选）｜2 天

### 现状锚点

JWT 密钥、MinIO AK/SK、支付渠道密钥目前以明文散落在 `application.yml` / 环境变量 /
Nacos 配置中。

### 方案

1. `hashicorp/vault:1.15` dev 模式起步（生产换 Raft 存储）
2. 引入 `spring-cloud-starter-vault-config`，bootstrap 阶段拉取 KV v2 中的
   `aics/{service}` 密钥集，覆盖本地配置
3. 收编顺序：jwt secret → minio → 支付密钥；Nacos 侧配合 jasypt 做双保险说明

### 面试要点

KV v2 版本化与回滚；动态密钥（数据库账号租约）思想；Vault 与 Nacos/jasypt 加密的职责
边界（传输配置 vs 存放密钥）。

---

## 六、排期与依赖

```
P1 部署补齐（2~3 天）──→ P3 CDC（canal 服务进编排顺路）
        └──→ P2 MongoDB（compose 增补顺路）
P4 / P5 可选，不阻塞主线
```

建议顺序即 P1 → P2 → P3 →（P4/P5 择机）。合计核心链路 8~10 个开发日，全部含可选项 13~15 天。

## 七、风险

| 风险 | 缓解 |
|------|------|
| Chroma 单机、无鉴权，定位是原型级 | 内网隔离 + 数据卷；文档写明上量后迁 Milvus/pgvector 的迁移路径 |
| XXL-Job Admin 版本与执行器不匹配会握手失败 | 锁定 2.4.1 镜像，验收含执行器注册断言 |
| Canal 依赖 MySQL binlog 权限，root 编排 / master-slave / k8s 三套环境配置不同 | 授权 SQL 沉淀到 `deploy/mysql/`，三套编排复用同一脚本 |
| MongoDB 双写期数据不一致 | 双写仅过渡、以 Mongo 为准对账；读切换前跑一致性校验脚本 |
| 新中间件扩大本地资源占用 | compose 给每个服务加 mem_limit；可选服务用 profile 分组（`--profile observability` 模式已有先例） |

## 附：新增镜像/依赖版本清单

| 组件 | 版本 | 说明 |
|------|------|------|
| chromadb/chroma | 0.5.x | 向量库部署 |
| neo4j | 5-community | GraphRAG |
| xuxueli/xxl-job-admin | 2.4.1 | 与 xxl-job-core 严格同版 |
| bladex/sentinel-dashboard | 1.8.6 | 对齐 SCA 内核版本 |
| mongo（镜像） | 7.0 | 对话记录 |
| spring-boot-starter-data-mongodb | 随 Boot 3.2.5 | P2 |
| canal-server | 1.1.7 | CDC |
| clickhouse-server | 24.x | 可选 |
| hashicorp/vault | 1.15 | 可选 |
| spring-cloud-starter-vault-config | 随 Cloud 2023.0.1 | 可选 |
