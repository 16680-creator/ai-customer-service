# Java 17 核心特性

> 本项目使用 **Java 17**（LTS 长期支持版本），它是 Spring Boot 3.x 的最低要求。
> 对应项目配置：根 `pom.xml` 中 `<java.version>17</java.version>`

---

## 一、为什么是 Java 17？

| 版本 | 状态 | 说明 |
|------|------|------|
| Java 8 | 老旧 | 很多企业还在用，但新框架已不支持 |
| Java 11 | LTS | 过渡版本 |
| **Java 17** | **LTS（推荐）** | Spring Boot 3.x 最低要求，本项目使用 |
| Java 21 | LTS（最新） | 虚拟线程、模式匹配增强 |

---

## 二、本项目用到的 Java 17 特性

### 2.1 Record（记录类）

用于定义不可变的数据载体，替代传统的 POJO + getter/setter。

```java
// 传统写法（本项目 ai-cs-common 中 Result 类用的 Lombok @Data）
@Data
public class Result<T> {
    private int code;
    private String message;
    private T data;
}

// Record 写法（适合 DTO、VO 等不需要修改的场景）
public record ChatRequest(String sessionId, String message) {}
public record ChatResponse(int code, String reply) {}

// 使用
var req = new ChatRequest("session-1", "你好");
req.sessionId();  // 自动生成的访问器，注意没有 get 前缀
```

**什么时候用 Record？**
- 接口入参/出参 DTO
- 方法返回多个值
- 配置项封装

**什么时候不用？**
- 需要继承（Record 是 final 的）
- 需要可变字段（Entity 实体类用 Lombok @Data 更合适）

---

### 2.2 Sealed Class（密封类）

限制哪些类可以继承/实现，适合定义有限的类型体系。

```java
// 定义消息类型（本项目 ai-cs-message 可以用）
public sealed interface Notification
    permits SmsNotification, EmailNotification, PushNotification {
    String getContent();
}

public record SmsNotification(String phone, String content) implements Notification {
    public String getContent() { return content; }
}

public record EmailNotification(String email, String content) implements Notification {
    public String getContent() { return content; }
}
```

---

### 2.3 Pattern Matching（模式匹配）

#### instanceof 模式匹配（Java 16+）

```java
// 旧写法
if (obj instanceof String) {
    String s = (String) obj;  // 需要手动强转
    System.out.println(s.length());
}

// 新写法（Java 17）
if (obj instanceof String s) {  // 直接绑定变量
    System.out.println(s.length());
}
```

#### Switch 模式匹配（Java 17 预览，21 正式）

```java
// 处理不同类型的 AI 响应
String result = switch (response) {
    case TextResponse t -> t.getContent();
    case ImageResponse i -> i.getUrl();
    case ErrorResponse e -> "错误: " + e.getMessage();
    default -> "未知类型";
};
```

---

### 2.4 Text Block（文本块）

写 SQL、JSON、Prompt 模板时特别好用：

```java
// 本项目 AI 对话的 System Prompt 可以这样写
String systemPrompt = """
    你是一个专业的AI客服助手。
    
    规则：
    1. 回答要简洁、专业
    2. 如果不确定，引导用户联系人工客服
    3. 不要编造不存在的信息
    
    当前知识库：{knowledgeBase}
    """;

// 对比旧写法
String old = "你是一个专业的AI客服助手。\n\n"
    + "规则：\n"
    + "1. 回答要简洁、专业\n";
```

---

### 2.5 var 局部变量类型推断

```java
// 本项目中常见的用法
var documents = pdfReader.get();           // 推断为 List<Document>
var result = cartService.getCartList(100L); // 推断为 CartVO
var key = Keys.hmacShaKeyFor(secret.getBytes(StandardCharsets.UTF_8)); // SecretKey
```

**注意**：`var` 只能用于局部变量，不能用于方法参数、返回值、字段。

---

### 2.6 增强的集合工厂方法

```java
// 创建不可变集合（Java 9+，17 继续用）
var list = List.of("user", "admin", "vip");
var set = Set.of("READ", "WRITE", "DELETE");
var map = Map.of("code", 200, "message", "success");

// 本项目中初始化配置、常量时常用
private static final Set<String> WHITE_LIST = Set.of(
    "/api/user/login",
    "/api/user/register",
    "/actuator/health"
);
```

---

### 2.7 Stream API 进阶

```java
// 本项目购物车计算总价（参考 CartServiceTest）
BigDecimal total = cartItems.stream()
    .filter(CartItem::getSelected)          // 只算选中的
    .map(item -> item.getProductPrice()
        .multiply(BigDecimal.valueOf(item.getQuantity())))
    .reduce(BigDecimal.ZERO, BigDecimal::add);

// 分组统计
Map<Long, List<Order>> ordersByUser = orders.stream()
    .collect(Collectors.groupingBy(Order::getUserId));

// 链式处理知识库文档
List<String> chunks = documents.stream()
    .map(Document::getContent)
    .flatMap(content -> splitIntoChunks(content).stream())
    .filter(chunk -> chunk.length() > 50)  // 过滤太短的
    .toList();  // Java 16+ 替代 .collect(Collectors.toList())
```

---

### 2.8 Optional 正确使用

```java
// 本项目中查询用户
public UserVO getUser(Long userId) {
    return Optional.ofNullable(userMapper.selectById(userId))
        .map(this::convertToVO)
        .orElseThrow(() -> new BusinessException("用户不存在"));
}

// ❌ 错误用法
if (optional.isPresent()) {
    return optional.get();  // 和 null 判断没区别
}

// ✅ 正确用法
return optional.orElse(defaultValue);
return optional.orElseGet(() -> computeDefault());
optional.ifPresent(user -> log.info("找到用户: {}", user.getName()));
```

---

## 三、并发编程基础（本项目必用）

### 3.1 线程池

```java
// AI 对话是耗时操作，需要异步处理
@Configuration
public class AsyncConfig {
    
    @Bean("chatExecutor")
    public ExecutorService chatExecutor() {
        return Executors.newFixedThreadPool(
            Runtime.getRuntime().availableProcessors() * 2
        );
    }
}

// 使用
@Service
public class ChatServiceImpl implements ChatService {
    
    @Autowired
    @Qualifier("chatExecutor")
    private ExecutorService executor;
    
    public CompletableFuture<String> chatAsync(String message) {
        return CompletableFuture.supplyAsync(() -> {
            // 调用 AI 模型（耗时 2-10 秒）
            return aiClient.call(message);
        }, executor);
    }
}
```

### 3.2 CompletableFuture 组合

```java
// 并行查询多个数据源（RAG 场景）
CompletableFuture<List<Document>> vectorSearch = 
    CompletableFuture.supplyAsync(() -> vectorStore.search(query));
CompletableFuture<List<Document>> keywordSearch = 
    CompletableFuture.supplyAsync(() -> esClient.search(query));

// 等待两个都完成，合并结果
List<Document> merged = vectorSearch
    .thenCombine(keywordSearch, (v, k) -> {
        List<Document> all = new ArrayList<>(v);
        all.addAll(k);
        return all;
    })
    .join();  // 阻塞等待结果
```

---

## 四、泛型（本项目大量使用）

```java
// 本项目的统一返回体 Result<T> 就是泛型类
public class Result<T> implements Serializable {
    private int code;
    private String message;
    private T data;  // T 可以是任何类型
}

// 使用时指定具体类型
Result<String> chatResult = chatService.chat(sessionId, message);
Result<CartVO> cartResult = cartService.getCart(userId);
Result<List<ProductVO>> products = productService.list(query);

// 泛型方法
public static <T> Result<T> success(T data) {
    return new Result<>(200, "success", data);
}
```

---

## 五、异常处理

```java
// 本项目的异常体系（ai-cs-common 模块）
// BusinessException - 业务异常（可预期的错误）
// GlobalExceptionHandler - 全局异常处理器

// 自定义业务异常
public class BusinessException extends RuntimeException {
    private final int code;
    
    public BusinessException(String message) {
        super(message);
        this.code = 500;
    }
    
    public BusinessException(int code, String message) {
        super(message);
        this.code = code;
    }
}

// 使用（参考 CartServiceTest 中的测试）
if (quantity > stock) {
    throw new BusinessException("库存不足，当前库存: " + stock);
}

// 全局捕获（GlobalExceptionHandler）
@RestControllerAdvice
public class GlobalExceptionHandler {
    
    @ExceptionHandler(BusinessException.class)
    public Result<?> handleBusiness(BusinessException e) {
        return Result.fail(e.getCode(), e.getMessage());
    }
    
    @ExceptionHandler(Exception.class)
    public Result<?> handleUnknown(Exception e) {
        log.error("系统异常", e);
        return Result.fail("系统繁忙，请稍后重试");
    }
}
```

---

## 六、注解（Annotation）

本项目中常见的注解：

```java
// Spring 相关
@Component      // 声明为 Spring Bean（如 DocumentLoader）
@Service        // 服务层
@RestController // 控制器
@Autowired      // 依赖注入

// Lombok（减少样板代码）
@Data           // 自动生成 getter/setter/toString/equals/hashCode
@Builder        // 建造者模式
@Slf4j          // 自动生成 log 对象

// 自定义注解（可以扩展）
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface RateLimit {
    int value() default 100;      // 每秒请求数
    String key() default "";      // 限流 key
}
```

---

## 七、学习检查清单

- [ ] 能用 Record 定义 DTO
- [ ] 理解 Text Block 在 Prompt 模板中的应用
- [ ] 熟练使用 Stream API 处理集合
- [ ] 理解泛型 `Result<T>` 的设计
- [ ] 能写自定义异常 + 全局异常处理
- [ ] 理解 CompletableFuture 异步编程
- [ ] 知道 var 的使用场景和限制

---

## 下一步

→ [02-Maven多模块管理](./02-Maven多模块管理.md)
