# 并发编程进阶

> 上接 [01-Java17核心特性 §3](./01-Java17核心特性.md) 的线程池基础，本篇补齐 JMM、锁、JUC 工具
> 与虚拟线程。来源：[00-学习路线总览/04-Java基础补全开发计划](../00-学习路线总览/04-Java基础补全开发计划.md) P2。
> 对应项目：`ai-cs-chat/.../config/ObservabilityExecutorConfig.java`、订单状态机的
> `synchronized`（见 [02-Spring微服务/12](../02-Spring微服务/12-订单状态机治理.md)）、chat SSE 流式接口。

---

## 一、JMM（Java 内存模型）：可见性、有序性、原子性

### 1.1 为什么需要 JMM

```
线程 A（工作内存）          主内存              线程 B（工作内存）
   stop=false  ──写──→   stop=true   ←──读──   stop=false（看不到更新！）
```

每个线程有自己的工作内存（CPU 缓存的抽象），普通变量的修改不一定立即对其他线程可见。
JMM 定义了"什么情况下一个线程的写对另一个线程可见"——核心工具是
**happens-before 规则**（如果 A happens-before B，则 A 的结果对 B 可见）。

### 1.2 happens-before 八条核心规则（面试必背）

1. **程序顺序规则**：单线程内，前面的操作 happens-before 后面的
2. **监视器锁规则**：解锁 happens-before 后续对同一把锁的加锁
3. **volatile 规则**：volatile 写 happens-before 后续对同一变量的读
4. **线程启动规则**：`Thread.start()` 之前的操作 happens-before 新线程里的所有操作
5. **线程终止规则**：线程的所有操作 happens-before `join()` 的返回
6. **线程中断规则**：`interrupt()` 调用 happens-before 被中断线程检测到中断
7. **对象终结规则**：构造函数 happens-before `finalize()` 开始
8. **传递性**：A hb B 且 B hb C → A hb C

### 1.3 volatile：可见 + 有序，但不原子

```java
// 本项目网关限流计数的抽象示例
private volatile boolean closed = false;   // 状态开关：volatile 足够

// ❌ 典型错误：volatile 不保证 i++ 的原子性（读-改-写三步不是原子的）
private volatile int count = 0;
count++;   // 并发下会丢计数！要用 AtomicInteger

// ✅ 正确：volatile 适合"一写多读的状态位"、"双重检查锁定的实例字段"
```

volatile 的两层作用：① **可见性**（写立即刷主存、读失效本地缓存）；
② **有序性**（内存屏障禁止指令重排——这就是双重检查锁定 DCL 里
`instance` 必须加 volatile 的原因：防止"分配内存→赋值引用→初始化"被重排成
"赋值引用→初始化"，别的线程拿到半成品）。

---

## 二、锁：synchronized 与 ReentrantLock

### 2.1 synchronized 的锁升级（注意 JDK 17 的变化）

对象头 Mark Word 里的锁标志决定当前状态：

```
无锁 ──→ 轻量级锁（CAS 自旋）──→ 重量级锁（OS mutex，线程挂起）
  └── 偏向锁：JDK 15 起【已废弃】(JEP 374)，JDK 17 默认关闭！
```

> 面试陷阱：老资料都说"偏向锁→轻量级→重量级"三级，但**偏向锁在 JDK 15 被
> 正式废弃**——维护成本高、收益在现代应用（线程池、并发集合）下不明显。
> JDK 17 回答"无锁→轻量级→重量级"更准确，并主动提 JEP 374 是加分项。

**本项目现场**：订单状态机并发 reset 用的 `synchronized`（
[02-Spring微服务/12](../02-Spring微服务/12-订单状态机治理.md)）属于低竞争保护，
轻量级锁 CAS 自旋基本够用，很少膨胀到重量级——这正是选 synchronized 而非
ReentrantLock 的理由：语法简单、JIT 可做锁消除/锁粗化、无泄漏风险（自动释放）。

### 2.2 synchronized vs ReentrantLock 对比

| 维度 | synchronized | ReentrantLock |
|------|-------------|---------------|
| 实现 | JVM 内建（monitorenter/exit） | AQS（Java 代码） |
| 释放 | 异常自动释放 | 必须 `finally unlock()` |
| 可中断 | 否（等锁时不可中断） | `lockInterruptibly()` 可以 |
| 超时获取 | 否 | `tryLock(timeout)` 可以 |
| 公平锁 | 否 | 可选（构造参数） |
| 条件变量 | 单一 wait/notify | 多个 `Condition` 精准唤醒 |
| 性能 | JDK 6+ 优化后基本无差距 | — |

**AQS 一句话原理**（面试讲这个层级够了）：`volatile int state` 表示锁/资源状态 +
CLH 变体双向队列排队挂起，`acquire/release` 模板方法 + 子类实现 tryAcquire/tryRelease。
ReentrantLock、Semaphore、CountDownLatch 底层全是它，只是 state 语义不同
（重入次数 / 许可数 / 计数器）。

### 2.3 读写锁与 StampedLock（了解即可）

```java
// 读多写少场景：ReentrantReadWriteLock 允许多读并行
// 缺点：读锁持有期间写锁饥饿 → 改用 StampedLock 乐观读
StampedLock sl = new StampedLock();
long stamp = sl.tryOptimisticRead();          // 1. 无锁读快照
int x = data;                                  // 2. 读
if (!sl.validate(stamp)) {                     // 3. 校验期间有无写
    stamp = sl.readLock();                     //    有 → 退化悲观读锁
    try { x = data; } finally { sl.unlockRead(stamp); }
}
```

---

## 三、ThreadLocal：原理与内存泄漏（必考）

### 3.1 结构：Thread → ThreadLocalMap

```
Thread 对象
 └── threadLocals (ThreadLocalMap)
      ├── Entry[0]: key=WeakReference<ThreadLocal tl1>, value=用户上下文
      └── Entry[1]: key=WeakReference<ThreadLocal tl2>, value=traceId
```

**每个 Thread 自己持有 Map**，ThreadLocal 只是 key。读写的都是当前线程自己的
Entry——所以它天生产没有并发竞争，代价是**线程池里线程不销毁，Entry 就一直在**。

### 3.2 内存泄漏链条

1. key 是**弱引用**：外部 `tl = null` 后，下次 GC key 被回收 → Entry 变成 `(null, value)`
2. value 是**强引用**：Map 还在（线程活着），value 回收不掉 → 泄漏
3. ThreadLocalMap 在 get/set 时会顺带清理 stale entry，但**线程池场景长期不调用就永远不清**

```java
// ✅ 标准姿势：try/finally 里 remove（用户上下文、链路 traceId 都这么处理）
try {
    UserContext.set(userId);
    chain.doFilter(request, response);
} finally {
    UserContext.remove();   // 线程池复用线程，不清就串号/泄漏
}
```

> 微服务进阶：`InheritableThreadLocal` 只在**新建线程**时继承，线程池复用线程不触发
> 继承 → 用阿里的 **TransmittableThreadLocal（TTL）**，配合装饰线程池把上下文
> 透传进去（网关透传 `X-User-Id` 之后服务内的用户上下文就是这类需求）。

---

## 四、JUC 工具箱

| 工具 | 底层 | 场景 | 一句话 |
|------|------|------|--------|
| CountDownLatch | AQS 共享模式 | 等全部完成 | 一次性，减到 0 放行 |
| CyclicBarrier | ReentrantLock + Condition | 多方到齐再一起走 | 可重置、可复用 |
| Semaphore | AQS 共享模式 | 限流许可 | 对下游限并发（如 LLM API 限 5 并发） |
| AtomicInteger | CAS + volatile | 计数 | 竞争激烈换 LongAdder |
| LongAdder | Cell 数组分散热点 | 高频统计 | 空间换时间，sum() 是近似值 |

### 4.1 CAS 与 ABA

```java
AtomicInteger i = new AtomicInteger();
i.compareAndSet(0, 1);   // CAS：期望 0 才改成 1，底层 CPU 指令 cmpxchg

// ABA 问题：值经历了 1→2→1，CAS 看不出中间变过
// 解决：AtomicStampedReference 带版本号（乐观锁 version 同理）
// 本项目对照：MySQL 乐观锁 stock - 1 where stock = ? 本质也是 CAS 思想
```

---

## 五、线程池深入（结合本项目配置复盘）

### 5.1 参数与执行顺序（背到能默写）

```
提交任务 → 核心线程未满？建核心线程执行
        → 队列未满？入队等待
        → 最大线程未满？建非核心线程执行
        → 拒绝策略：AbortPolicy(抛异常) | CallerRunsPolicy(调用者跑)
                    | DiscardPolicy(丢弃) | DiscardOldestPolicy(丢最老)
```

**注意顺序**：是"先入队、再扩线程"——很多初学者以为先扩到 max 再入队。
这也是为什么 `FixedThreadPool`（无界队列）永远不会扩线程。

### 5.2 为什么禁用 Executors 快捷方法（阿里巴巴规约）

- `newFixedThreadPool` / `newSingleThreadExecutor`：**无界队列**（LinkedBlockingQueue
  默认 Integer.MAX_VALUE）→ 任务堆积 OOM
- `newCachedThreadPool`：**max = Integer.MAX_VALUE** → 线程无限增长 OOM

### 5.3 本项目复盘：`ObservabilityExecutorConfig`

```java
// usageExecutor：core=1, max=4, queue=1000, AbortPolicy, 优雅停机等待
// 逐参数审视（这就是面试讲的"参数依据"）：
// ① 用量上报是 IO 密集（Feign 落库），但低频轻量 → 不需要按 N_cpu*(1+W/C) 配大池，
//    小池 + 大队列做削峰即可；core=1 保证闲时只占 1 个线程
// ② 队列 1000 兜住瞬时爆发；AbortPolicy 抛出由调用方捕获 → 告警，符合
//    "可观测性不能反过来观测死业务"的隔离原则
// ③ setWaitForTasksToCompleteOnShutdown(true)：停机前把队列里未落库的用量
//    数据跑完，防止停机丢数据
```

**通用估算公式**（面试要说得出思路而不是背数）：
`N_threads ≈ N_cpu × U × (1 + W/C)`，W/C 是等待/计算时间比。
IO 密集（如调用 LLM，等待 2~10s）W/C 很大，线程数偏多合理；
CPU 密集（序列化、加解密）取 N_cpu 或 N_cpu+1。

> Spring 细节：`@Async` 不指定 executor 时用 Boot 自动配置的 `applicationTaskExecutor`
> ——**核心 8 线程 + 无界队列**，max 根本不会生效，业务高峰照样可能堆积。
> 生产必须显式定义自己的 Executor Bean（本项目 chat 就是这么做的）。

### 5.4 SSE 对话为什么适合虚拟线程（Java 21 预研）

```yaml
# Boot 3.2 一键开启（实验分支验证，不默认合主干）
spring:
  threads:
    virtual:
      enabled: true
```

- 平台线程 1:1 映射内核线程，创建/调度昂贵，阻塞即浪费；虚拟线程由 JVM 调度，
  阻塞时**卸载载体线程**去跑别的任务——SSE 长连接 + LLM 调用（等 2~10s）这类
  IO 密集场景，千级并发下不再需要"线程池大小 = 并发数"的权衡
- **Pin 陷阱**：`synchronized` 块内阻塞会把载体线程钉死（JDK 24 才修复，
  JDK 21 需改 `ReentrantLock`）；native 调用也会 pin
- 虚拟线程**不池化、不计数**是常态用法；CPU 密集任务无收益

---

## 六、ConcurrentHashMap（1.8 实现）

- 结构：`Node<K,V>[]` 数组 + 链表/红黑树（8 树化 / 6 退化，且表长 ≥64 才树化，否则先扩容）
- **put**：桶为空 → CAS 放入；不为空 → `synchronized` 锁**桶头节点**再链尾插入
  （锁粒度 = 单个桶，比 1.7 的 Segment 分段锁 16 段更细）
- **size**：`baseCount` + `CounterCell[]` 数组（LongAdder 思想），避免单点 CAS 竞争
- **扩容**：支持多线程协助迁移（ForwardingNode 标记）
- **迭代器弱一致性**：遍历时允许并发修改，不抛 ConcurrentModificationException，
  但不保证读到最新值——这与 HashMap 的 fail-fast 不同

---

## 七、高频面试题（含参考答案）

**Q1：volatile 能保证原子性吗？为什么？**
A：不能。volatile 只保证可见性和有序性（读写各自原子），`i++` 是读-改-写三步，
线程间交错照样丢更新。需要原子性用 `AtomicInteger`（CAS）或加锁。

**Q2：说说 happens-before 是干嘛的，举三条规则。**
A：它是 JMM 判断"一个操作的结果是否对另一个操作可见"的偏序关系，与时间先后无关。
常用三条：程序顺序规则（单线程内天然可见）、volatile 规则（写先于后续读）、
监视器锁规则（解锁先于后续加锁）。没有 happens-before 关系的两个操作，JVM 允许
任意重排，可见性不保证。

**Q3：DCL 单例为什么要 volatile？**
A：`new` 不是原子操作：分配内存 → 初始化 → 引用赋值，② ③ 可能重排。另一个线程
在 ③ 后、② 前拿到引用，会用未初始化完的对象。volatile 禁止该重排并保证可见。

**Q4：synchronized 原理？锁升级讲一下（JDK 17 版本）。**
A：字节码层面 monitorenter/monexit，对象头 Mark Word 存锁状态。JDK 17：无锁 →
轻量级（栈上 Lock Record + CAS 自旋）→ 重量级（ObjectMonitor，OS 互斥量，线程挂起）。
偏向锁已废弃（JEP 374，JDK 15 起默认关闭）。竞争加剧才升级，不降级（重置对象
可能回到无锁）。

**Q5：synchronized 和 ReentrantLock 怎么选？**
A：默认 synchronized（简洁、自动释放、JIT 优化空间大）；需要 tryLock 超时、可中断
获取、多 Condition、公平队列时才用 ReentrantLock。本项目状态机的低竞争保护就是
synchronized 的典型适用场景。

**Q6：ThreadLocal 为什么会内存泄漏？怎么避免？**
A：ThreadLocalMap 的 key 是弱引用，外部引用消失后 key 被 GC，Entry 变成
(null → value)，而 value 被线程强引用着；线程池线程长存 → value 永不回收。
避免：用完必须在 finally 里 `remove()`。原理追问：key 设计成弱引用是"自清理"
的第一道保险（get/set 顺带清 stale entry），但线程池场景必须靠手动 remove。

**Q7：线程池的执行流程？参数怎么定？**
A：核心线程 → 入队 → 扩到 max → 拒绝（注意是先队列后扩容）。参数看负载类型：
CPU 密集 ≈ N_cpu+1；IO 密集按 `N_cpu × (1 + W/C)` 估，再压测校准。队列要有界，
拒绝策略按业务选（用户请求用 CallerRuns 或快速失败，日志类可丢弃）。
可提本项目：usageExecutor 用"小池+大队列+AbortPolicy+优雅停机"组合的理由。

**Q8：为什么阿里规约禁用 Executors 创建线程池？**
A：newFixedThreadPool/newSingleThreadExecutor 无界队列会堆积任务直到 OOM；
newCachedThreadPool 线程数无上限会耗尽线程。必须 `new ThreadPoolExecutor(...)`
显式给出全部 7 个参数。

**Q9：shutdown 和 shutdownNow 的区别？Spring 优雅停机怎么配？**
A：shutdown：不再收新任务，已提交的（含队列）继续执行完；shutdownNow：尝试中断
正在执行的线程，返回队列里未执行的任务列表。Spring 里 `ThreadPoolTaskExecutor`
设 `setWaitForTasksToCompleteOnShutdown(true)` + `setAwaitTerminationMillis`，
配合 server.shutdown: graceful（本项目 03-微服务治理计划 P4 已覆盖）。

**Q10：CAS 是什么？ABA 怎么解决？**
A：Compare-And-Swap，CPU 级原子指令，期望值匹配才更新，失败自旋重试。
ABA：值 A→B→A 期间 CAS 检测不到变化，若状态依赖"历史"就会错（如无锁栈）。
用 `AtomicStampedReference`（版本戳）解决。普通计数场景 ABA 无害。

**Q11：ConcurrentHashMap 1.7 和 1.8 的区别？**
A：1.7 Segment 分段锁（默认 16 段，锁粒度粗，并发度固定）；1.8 改为 CAS（空桶）
+ synchronized（桶头节点），粒度细到单个桶，链表过长树化，size 用 CounterCell
分散热点。并发度不再固定，随表大小伸缩。

**Q12：ConcurrentHashMap 能存 null 吗？为什么？**
A：不能（key/value 都不行）。歧义问题：`get` 返回 null 无法区分"不存在"和
"值就是 null"——并发下无法像 HashMap 那样先 containsKey 再 get（两步之间可能
被改），所以一开始就禁止 null 逼你用 containsKey 的语义想清楚。

**Q13：虚拟线程是什么？什么场景用、什么场景别用？**
A：JVM 调度的轻量线程（JDK 21 正式），阻塞时卸载载体线程，百万级创建成本可控，
适合 IO 密集高并发（SSE、LLM 调用、大量下游 RPC）。别用于 CPU 密集；注意 JDK 21
上 synchronized 块内阻塞会 pin 载体线程（24 修复）；它不是更快的线程池，也不需要
池化——"每任务一线程"回归。

**Q14：多个线程顺序打印 ABC，怎么实现？（手写题）**
A：经典解法三种：① 三个线程各持有一个 Semaphore，`a.acquire(); print; b.release();`
② ReentrantLock + 三个 Condition 精准唤醒 ③ 共享 state + CAS 自旋。写 Semaphore 版
最简洁，顺带说明"这是 Condition 精准唤醒优于 notifyAll 的场景"。

---

## 八、学习检查清单

- [ ] 默写 happens-before 八条里的五条
- [ ] 说清 volatile 不保原子的原因与 DCL 加 volatile 的原因
- [ ] 讲出 ThreadLocal 泄漏链条（弱引用 key → stale entry → 线程池长存）与 remove 姿势
- [ ] 默写线程池执行顺序与 7 参数，能复盘本项目 usageExecutor 每个参数的依据
- [ ] 知道 JDK 17 里偏向锁已废弃、Boot 默认 applicationTaskExecutor 是无界队列
- [ ] 说得出虚拟线程 pin 的原因与适用边界

## 九、动手实践（对照 04 计划 P2 任务）

1. 给 `ObservabilityExecutorConfig` 写一页参数复盘（本文 5.3 节即是底稿）
2. JMeter 压 chat SSE 接口：观察 `usage-report-` / Tomcat `exec-` 线程的排队与拒绝
3. 开实验分支加 `spring.threads.virtual.enabled=true`，记录压测对比，数据说话再定合入

---

## 下一步

→ [05-集合框架与源码](./05-集合框架与源码.md)
