# 集合框架与源码

> 来源：[00-学习路线总览/04-Java基础补全开发计划](../00-学习路线总览/04-Java基础补全开发计划.md) P3。
> 对应项目：`ai-cs-chat/.../agent/tool/AgentToolRegistry.java`（HashMap 注册表 + `Map.copyOf`）、
> `ai-cs-gateway/.../filter/AuthFilter.java`（`List.of` 白名单）、`ai-cs-order/.../CartServiceImpl.java`（购物车 List）。

---

## 一、集合总览与本项目选型现场

```
Collection                Map
├── List                  ├── HashMap（1.8 数组+链表+红黑树）
│   ├── ArrayList         ├── LinkedHashMap（+双向链表，可做 LRU）
│   ├── LinkedList        ├── TreeMap（红黑树，有序导航）
│   └── CopyOnWriteArrayList  ├── ConcurrentHashMap（并发）
├── Set                   └── ...（WeakHashMap 等）
│   ├── HashSet（就是 HashMap 的 key）
│   ├── TreeSet（就是 TreeMap）
│   └── ConcurrentSkipListSet
└── Queue/Deque
    ├── ArrayDeque（官方推荐替代 Stack/LinkedList 当队列）
    └── PriorityBlockingQueue（线程池用的优先队列）
```

**本项目三个选型现场**（读代码时问自己"为什么是这个"）：

```java
// 1. AgentToolRegistry：启动时写、运行时大量读 → HashMap 够了（无并发写）；
//    对外暴露用 Map.copyOf(tools) 返回不可变快照，防止调用方修改内部状态
private final Map<String, AgentTool> tools = new HashMap<>();
public Map<String, AgentTool> all() { return Map.copyOf(tools); }

// 2. AuthFilter 白名单：static final 不可变集合 + stream().anyMatch(startsWith)
private static final List<String> WHITE_LIST = List.of(
    "/api/user/login", "/api/user/register", ...);

// 3. CartServiceImpl：购物车条目列表要频繁按 index/过滤遍历 → ArrayList；
//    汇总用 Stream reduce（见 01-Java17核心特性 §2.7）
```

---

## 二、HashMap 源码走读（1.8，面试重灾区）

### 2.1 三个关键常量

```java
static final int DEFAULT_INITIAL_CAPACITY = 1 << 4;   // 16，且容量恒为 2 的幂
static final float DEFAULT_LOAD_FACTOR = 0.75f;       // 负载因子：size > cap*0.75 就扩容
static final int TREEIFY_THRESHOLD = 8;               // 桶内链表长度 ≥8 尝试树化
static final int UNTREEIFY_THRESHOLD = 6;             // ≤6 退化回链表
static final int MIN_TREEIFY_CAPACITY = 64;           // 表长 <64 时不树化，先扩容
```

### 2.2 hash 扰动与 2 的幂（必考）

```java
static final int hash(Object key) {
    int h;
    // 高 16 位异或低 16 位：让高位也参与 (n-1) & hash 的取模运算
    return (key == null) ? 0 : (h = key.hashCode()) ^ (h >>> 16);
}
// 定位桶：tab[(n - 1) & hash]
```

- **为什么容量是 2 的幂**：`(n-1) & hash` 等价于 `hash % n`，位运算快；
  且 n 为 2 的幂时 `n-1` 的二进制是全 1（如 16-1=1111），与运算能把 hash 的
  低位完整保留、分布均匀。若 n 不是 2 的幂，(n-1) 有 0 位，某些桶永远空。
- **为什么要扰动**：不扰动时只有低位参与，`hashCode` 高位不同、低位相同的
  对象（如两个 Integer 相差 65536）全撞一个桶。

### 2.3 put 流程（能画出来）

```
put(K,V)
 ├─ 表未初始化 → 先 resize
 ├─ (n-1)&hash 定位桶：桶为空 → 直接放新节点
 ├─ 桶头是 TreeNode → 红黑树插入
 ├─ 链表尾插遍历：key 相等（hash 相等且 equals）→ 覆盖
 │    链表长度 ≥8：表长 ≥64 ? 树化 : 先 resize
 └─ 插入后 size > threshold → resize（容量翻倍）
```

### 2.4 resize 的巧思：高低位链表拆分

```java
// JDK 1.8 扩容不再重算 hash：newCap = oldCap << 1
// 节点新位置只有两种：原位置 i 或 原位置 i + oldCap
// 判断依据：e.hash & oldCap 是否为 0（等价于看 hash 在新增的那一位上是 0 还是 1）
if ((e.hash & oldCap) == 0) 低位链表 else 高位链表   // 各自尾插，保持相对顺序
```

### 2.5 1.7 头插死循环 与 1.8 的并发问题（必考）

- 1.7 扩容 transfer 用**头插**，两个线程并发扩容会让链表成环 → 后续 get 死循环 100% CPU
- 1.8 改**尾插**解决了成环，但**仍然线程不安全**：并发 put 可能数据覆盖丢失、
  size 不准。结论：并发场景永远用 ConcurrentHashMap，"1.8 修好了"是谣言

### 2.6 为什么重写 equals 必须重写 hashCode

`get` 的查找逻辑：先比 hash 桶位置，再桶内 `equals`。只重写 equals（两个"内容相同"
的对象 equals 为 true）但不重写 hashCode → 两个对象 hash 不同、落在不同桶 →
**map 里查不到你 equals 相等的 key**，HashSet 去重也失效。
契约：equals 相等 ⇒ hashCode 必相等；反之不要求（hash 冲突允许）。

---

## 三、List 家族

| 实现 | 底层 | get | 中间插删 | 场景 |
|------|------|-----|---------|------|
| ArrayList | Object[] | O(1) | O(n) | 随机读、尾插（默认选择） |
| LinkedList | 双向链表 | O(n) | 头尾 O(1)，中间 O(n) | 很少用（要 Deque 用 ArrayDeque） |
| CopyOnWriteArrayList | volatile 数组 | O(1) | 复制整个数组 O(n) | 读极多写极少（监听器列表） |

```java
// ArrayList 扩容：newCap = oldCap + (oldCap >> 1)  → 1.5 倍，Arrays.copyOf 搬家
// 默认容量 10（首次 add 时才分配，懒加载）

// fail-fast：迭代时结构被改（modCount 不一致）→ ConcurrentModificationException
for (CartItem item : items) {
    if (!item.getSelected()) items.remove(item);   // ❌ 必抛 CME
}
items.removeIf(CartItem::notSelected);             // ✅ 或迭代器 remove / CopyOnWrite
```

> `subList` 是**视图**：改原列表会影响子列表（反之亦然），原列表结构性修改后
> 子列表直接失效抛异常；`Arrays.asList` 定长、不支持 add/remove——两个高频坑。

---

## 四、LinkedHashMap 与手写 LRU

`LinkedHashMap` 在 HashMap 基础上加双向链表串联全部 Entry；`accessOrder=true`
时每次 get/put 都把节点移到尾部——**头部就是最久未使用**，天然的 LRU 骨架。

### 4.1 三行版（面试先给这个）

```java
public class LruCache<K, V> extends LinkedHashMap<K, V> {
    private final int maxEntries;

    public LruCache(int maxEntries) {
        super(16, 0.75f, true);          // accessOrder=true：按访问顺序
        this.maxEntries = maxEntries;
    }

    @Override
    protected boolean removeEldestEntry(Map.Entry<K, V> eldest) {
        return size() > maxEntries;      // 超容量就淘汰头部（最久未访问）
    }
}
```

### 4.2 双向链表 + HashMap 版（说得出 O(1) 两个操作）

```java
public class LruCache2<K, V> {
    private static class Node<K, V> {
        K key; V value; Node<K, V> prev, next;
        Node(K k, V v) { key = k; value = v; }
    }

    private final int capacity;
    private final Map<K, Node<K, V>> map = new HashMap<>();
    private final Node<K, V> head = new Node<>(null, null);  // 哨兵，免判空
    private final Node<K, V> tail = new Node<>(null, null);

    public LruCache2(int capacity) {
        this.capacity = capacity;
        head.next = tail; tail.prev = head;
    }

    public V get(K key) {
        Node<K, V> n = map.get(key);
        if (n == null) return null;
        moveToTail(n);                    // 最近使用 → 尾部
        return n.value;
    }

    public void put(K key, V value) {
        Node<K, V> n = map.get(key);
        if (n != null) { n.value = value; moveToTail(n); return; }
        if (map.size() >= capacity) {
            Node<K, V> eldest = head.next;         // 头部 = 最久未用
            unlink(eldest); map.remove(eldest.key);
        }
        Node<K, V> fresh = new Node<>(key, value);
        map.put(key, fresh); linkToTail(fresh);
    }

    private void moveToTail(Node<K, V> n) { unlink(n); linkToTail(n); }
    private void unlink(Node<K, V> n) { n.prev.next = n.next; n.next.prev = n.prev; }
    private void linkToTail(Node<K, V> n) {
        n.prev = tail.prev; n.next = tail;
        tail.prev.next = n; tail.prev = n;
    }
}
```

> 与本项目呼应：Redis 的淘汰策略（`04-中间件/01-Redis缓存实战`）里
> `allkeys-lru` 是生产级 LRU——但 Redis 用的是**近似 LRU**（随机采样淘汰），
> 精确维护全局链表成本太高；Caffeine 用的是比 LRU 更好的 **W-TinyLFU**。
> 面试能对比"教科书 LRU → 近似 LRU → LFU/TinyLFU 的演进"是高分项。

---

## 五、并发容器

### 5.1 ConcurrentHashMap（详见 [04-并发编程进阶 §六](./04-并发编程进阶.md)）

1.8：CAS（空桶）+ synchronized（桶头）+ 树化 + CounterCell 计数，迭代器弱一致性。

### 5.2 CopyOnWriteArrayList

```java
// 写时复制：add 时 lock + 复制整个数组 + 替换 volatile 引用
// 读无锁（永远读旧快照），迭代器是创建时刻的快照，不抛 CME
// 适用：监听器列表、配置快照——写少读多且允许读到"稍旧"数据
// 禁忌：高频写（每次写 O(n) 复制）、大列表、强一致读取
```

---

## 六、不可变集合：三种写法的区别（易错点）

```java
List.of("/a", "/b")                        // JDK9+ 真不可变：拒绝 null、拒绝 add/remove，
                                           // 内部专用 ImmutableCollections 实现，省内存

Collections.unmodifiableList(mutable)      // 只是"视图"：包了一层抛异常的壳，
                                           // 原列表变了它跟着变！（不是快照）

List.copyOf(mutable)                       // JDK10+：拷贝成不可变快照，
                                           // 若本身已是不可变 List 直接返回原引用

// 防御性拷贝惯用法（DTO 暴露集合字段时）：
public List<CartItem> getItems() { return List.copyOf(items); }   // 外部改不动内部状态
```

**本项目对照**：`AgentToolRegistry.all()` 返回 `Map.copyOf(tools)` 而不是
`Collections.unmodifiableMap(tools)`——前者是快照，调用方拿到的永远不会因注册表
后续变化而变化（审计/展示语义）；如果语义是"实时视图"才该用后者。

---

## 七、高频面试题（含参考答案）

**Q1：HashMap 的 put 流程？（1.8）**
A：① 表未初始化先 resize；② 计算扰动 hash 定位桶 `(n-1)&hash`；③ 空桶直接放；
④ 桶头是树节点走红黑树插入；⑤ 链表尾插，key 已存在（hash 相等且 equals）则覆盖；
⑥ 链长达 8 且表长 ≥64 树化（否则先扩容）；⑦ size 超 threshold 扩容翻倍。

**Q2：为什么容量必须是 2 的幂？**
A：取模 `(n-1)&hash` 要求 n-1 是全 1 的二进制才能让 hash 低位均匀映射到每个桶，
2 的幂减一正好全 1；同时位运算比 `%` 快。扩容翻倍维持该性质。

**Q3：为什么树化阈值是 8、退化是 6？**
A：链表长度服从泊松分布，负载因子 0.75 下长度到 8 的概率约 0.00000006（五千万分之一），
正常几乎不会树化——树化是对 hash 退化（如恶意 key 攻击）的兜底，红黑树把最坏
O(n) 查找降到 O(logn)。退化阈值 6 而不是 7，是为了在 7~8 之间留缓冲，
避免节点数在 8 附近抖动时树↔链表来回转换。

**Q4：HashMap 1.7 的死循环怎么回事？1.8 修复了吗？**
A：1.7 并发扩容 transfer 用头插法，两个线程同时迁移会让链表反向成环，get 落在
环上就 100% CPU 死循环。1.8 改尾插保持相对顺序，解决了成环，但 HashMap 依然
线程不安全（并发 put 覆盖丢数据、size 不准），并发必须用 ConcurrentHashMap。

**Q5：重写 equals 不重写 hashCode 会怎样？**
A：HashMap/HashSet 里两个 equals 相等的对象落进不同桶（hash 不同），导致
"存进去的 key 取不出来"、去重失效。equals 相等必须 hashCode 相等；
hashCode 相等不要求 equals 相等（冲突）。

**Q6：ArrayList 扩容机制？为什么是 1.5 倍？**
A：newCap = oldCap + (oldCap >> 1)，Arrays.copyOf 整体搬家。1.5 倍是空间与
复制次数的折中：太小扩容频繁，太大浪费内存（对比 HashMap 翻倍是因为它必须
维持 2 的幂）。首次 add 才分配容量 10（懒加载）。

**Q7：ArrayList 和 LinkedList 怎么选？**
A：默认 ArrayList：随机访问 O(1)、内存连续缓存友好、尾插均摊 O(1)。
LinkedList 除了头尾 O(1) 几乎全面落后（get 是 O(n)、每节点额外 40 字节前后指针、
缓存不友好），需要 Deque 语义用 ArrayDeque 更好。LinkedList 的"中间插删快"
是误区——定位到中间就要 O(n)。

**Q8：什么是 fail-fast？怎么避免？**
A：迭代器遍历时比较 modCount（结构修改次数），发现不一致立刻抛
ConcurrentModificationException，"快速失败"防止带病运行。单线程用
`removeIf`/`Iterator.remove`；并发场景用 CopyOnWriteArrayList 或
ConcurrentHashMap（弱一致迭代器，不抛 CME）。

**Q9：手写 LRU 要求 O(1)。（现场写）**
A：先给 LinkedHashMap 三行版（accessOrder=true + removeEldestEntry），
再给"HashMap + 双向链表（哨兵节点）"版——get/put 都是 O(1)：
HashMap 定位节点，链表 O(1) 摘除/尾插。能主动提 Redis 近似 LRU（采样）与
Caffeine W-TinyLFU 更佳。完整代码见本文第四节。

**Q10：ConcurrentHashMap 为什么 key 和 value 都不允许 null？**
A：get 返回 null 有歧义："键不存在"还是"值为 null"？HashMap 可以用
containsKey 二次确认，但并发下两步之间值可能被其他线程改掉，二次确认本身
不可靠——所以干脆禁止 null，强制你把语义想清楚。

**Q11：CopyOnWriteArrayList 的原理与代价？**
A：写时复制：加锁复制整个数组、写新数组、volatile 引用替换。读永远无锁，
迭代器是快照不抛 CME。代价：写 O(n) 复制、内存瞬时翻倍、读到的可能是旧值。
只适合监听器/配置这类读多写极少场景。

**Q12：List.of、Collections.unmodifiableList、List.copyOf 的区别？**
A：List.of 真不可变（拒绝 null/修改）；unmodifiableList 是原列表的只读**视图**，
原列表变了它跟着变；List.copyOf 是**快照**拷贝。对外暴露内部集合时：
要快照用 copyOf，要实时只读视图用 unmodifiable（并接受外部间接修改）。

---

## 八、学习检查清单

- [ ] 能默写 put 流程 + 说出扰动函数、2 的幂、8/6/64 三个阈值的原因
- [ ] 讲清 1.7 头插死循环与 1.8 仍线程不安全的原因
- [ ] 现场写出 LRU 两版（LinkedHashMap 版 + HashMap+双向链表版）
- [ ] 说出 ArrayList 1.5 倍扩容、fail-fast、subList/Arrays.asList 两个坑
- [ ] 能复盘 AgentToolRegistry / AuthFilter 白名单的集合选型理由

## 九、动手实践（对照 04 计划 P3 任务）

1. 给 `AgentToolRegistry.all()` 与 `AuthFilter.WHITE_LIST` 的选型写注释级说明
2. 把本文 LRU 两版敲一遍 + 单测（命中/淘汰/更新提升优先级三场景）
3. 全局搜一处 `Arrays.asList` 或 `subList`，确认没有踩"定长/视图"的坑

---

## 下一步

→ [06-反射动态代理与SPI](./06-反射动态代理与SPI.md)
