# 语言级细节与序列化

> 来源：[00-学习路线总览/04-Java基础补全开发计划](../00-学习路线总览/04-Java基础补全开发计划.md) P6。
> 对应项目：`ai-cs-order/.../CartServiceImpl.java` 的金额计算（`productPrice × quantity` 求和）、
> 父 POM `annotationProcessorPaths`（Lombok 1.18.30 + MapStruct 1.5.5）、
> 01-P2 计划采用的 `GenericJackson2JsonRedisSerializer`（Redis 缓存序列化）。

---

## 一、BigDecimal：金额计算的生命线

### 1.1 三个必坑

```java
// 坑 1：double 构造精度陷阱
new BigDecimal(0.1)        // 0.1000000000000000055511151231257827021181583404541015625
new BigDecimal("0.1")      // 0.1 ✅ 字符串构造才是精确值
BigDecimal.valueOf(0.1)    // 0.1 ✅ 内部走 Double.toString，等价字符串构造

// 本项目 CartServiceImpl 的写法是安全的：
item.getProductPrice().multiply(BigDecimal.valueOf(item.getQuantity()))

// 坑 2：divide 不指定精度 → 无限小数直接抛异常
BigDecimal.valueOf(10).divide(BigDecimal.valueOf(3));          // ❌ ArithmeticException
BigDecimal.valueOf(10).divide(BigDecimal.valueOf(3), 2, RoundingMode.HALF_UP);  // 3.33 ✅

// 坑 3：equals 连 scale 一起比
new BigDecimal("1.0").equals(new BigDecimal("1.00"))       // false！（scale 2≠1）
new BigDecimal("1.0").compareTo(new BigDecimal("1.00"))    // 0 ✅ 数值比较用 compareTo
```

### 1.2 金额字段规范（给出本项目结论）

| 项 | 规范 |
|----|------|
| 数据库 | `DECIMAL(12,2)`（或"分为单位的 BIGINT"，二选一团队统一） |
| Java 类型 | `BigDecimal`（禁 double/float——金额用浮点就是资损事故预定） |
| 计算中途 | 不动 scale，保持运算原生精度（乘法 scale 相加） |
| 最终展示/入库 | 最后一步才 `setScale(2, RoundingMode.HALF_UP)` |
| 比较 | 业务判断一律 `compareTo`（如 `total.compareTo(afford) >= 0`） |
| 序列化 | JSON 输出字符串或定点小数（Jackson 默认 OK，别开 `WRITE_BIGDECIMAL_AS_PLAIN` 的科学计数） |

```java
// 边界单测示例（对照 04 计划 P6 任务：先红后绿）
@Test
void 购物车总价_四舍五入到分() {
    // 0.1 × 3 + 0.2 × 3 = 0.900000000...？ 不，BigDecimal 全程精确
    var items = List.of(price("0.10", 3), price("0.20", 3));
    assertEquals(new BigDecimal("0.90"), sum(items));
}

@Test
void 比较金额_用compareTo而非equals() {
    assertNotEquals(new BigDecimal("1.00"), new BigDecimal("1.0")); // scale 不同不等
    assertEquals(0, new BigDecimal("1.00").compareTo(new BigDecimal("1.0")));
}
```

---

## 二、java.time：LocalDateTime / Instant / ZoneId

```java
Instant now = Instant.now();                    // UTC 时间线上的一个点（机器时间）
LocalDateTime local = LocalDateTime.now();      // 无时区概念的"墙钟"（人类时间）
ZonedDateTime zoned = now.atZone(ZoneId.of("Asia/Shanghai"));
```

**三者关系**：`Instant` 是唯一有绝对时刻语义的；`LocalDateTime` 是"某地日历上
看起来的时间"，跨时区无意义。

**一致性清单**（本项目前后端 + DB + MQ 的时区链路）：
1. DB 列用 `DATETIME`（无时区）+ JDBC `serverTimezone=Asia/Shanghai`，团队约定统一
2. Java 实体用 `LocalDateTime`；对外 API 需要"绝对时刻"（如重试时间戳）才用 `Instant`
3. JSON：Spring Boot 已自动注册 `JavaTimeModule`（jackson-datatype-jsr310），
   默认 ISO-8601（`2026-09-03T10:00:00`）；`spring.jackson.date-format` 会影响
   `java.util.Date` 而非 `LocalDateTime` 的默认行为——配置前先想清楚改的是谁
4. 禁用 `SimpleDateFormat`（**非线程安全**，并发下格式错乱）→ 一律
   `DateTimeFormatter`（不可变，可 static final 共享）

---

## 三、String 与编码（快速过）

- **不可变**：内部 `byte[]` final（JDK 9 Compact Strings，Latin-1 单字节省内存）。
  不可变的好处：常量池复用、hashCode 可缓存（HashMap key 的性能基础）、线程安全
- **常量池**：字面量进字符串常量池（JDK 7 起在堆中）；`intern()` 返回池中实例
- **拼接**：`+` 编译为 `invokedynamic` makeConcat（JDK 9+），循环内拼接仍要手动
  `StringBuilder`（每次 `+` 都 new 一个 concat 调用）
- **编码**：UTF-8 变长 1~4 字节；`new String(bytes)` 不指定字符集用平台默认
  （**永远显式 `StandardCharsets.UTF_8`**——本项目 Prompt 拼接、文件上传都涉及）

---

## 四、序列化：为什么禁用 Java 原生序列化

### 4.1 原生序列化的三宗罪

1. **安全**：`readObject` 反序列化即执行代码路径，是历史上 Java 反序列化 RCE 漏洞
   的温床（Apache Commons、WebLogic 都中过招）
2. **体积**：带类元数据，比 JSON 大数倍
3. **兼容**：类结构一变 `serialVersionUID` 不符就 `InvalidClassException`

> 结论：**跨服务/跨进程一律 JSON（或 Protobuf）**；Java 原生序列化只出现在
> 不得不用的老框架内部。本项目 MQ 消息、Redis 缓存、HTTP 接口全是 JSON。

### 4.2 Jackson 定制（本项目序列化基线）

```java
// Redis 缓存场景（01-P2 已落地 GenericJackson2JsonRedisSerializer 的原理）：
// 它开了 default typing——JSON 里带 "@class" 才能反序列化回正确的业务类型
{"@class": "com.aics.product.vo.ProductVO", "id": 1001, "name": "谷子·徽章"}

// ⚠️ default typing 的安全代价：多态反序列化可能被构造恶意类
// Jackson 2.10+ 必须配白名单校验器：
ObjectMapper mapper = JsonMapper.builder()
    .activateDefaultTyping(
        BasicPolymorphicTypeValidator.builder()
            .allowIfSubType("com.aics.")          // 只放行本项目包
            .build(),
        ObjectMapper.DefaultTyping.NON_FINAL)
    .build();
```

| 定制点 | 写法 | 本项目场景 |
|--------|------|-----------|
| LocalDateTime | 注册 `JavaTimeModule`（Boot 自动） | 全部接口/缓存 |
| 时区 | `mapper.setTimeZone(TimeZone.getTimeZone("Asia/Shanghai"))` | 统一基准 |
| 忽略未知字段 | `FAIL_ON_UNKNOWN_PROPERTIES=false` | 前后端字段演进不炸 |
| 多态类型信息 | `@JsonTypeInfo` / default typing + 白名单 | Redis 缓存回读 |
| 不序列化字段 | `transient` 或 `@JsonIgnore` | 密码字段 |

---

## 五、equals / hashCode / clone / 不可变设计

```java
// equals 契约：自反、对称、传递、一致、对 null 返回 false
// hashCode 契约：equals 相等 ⇒ hashCode 相等（反之不要求）
// —— HashMap 全依赖这两条（见 05-集合框架与源码 §2.6）

// clone 的建议：别用（浅拷贝 + Cloneable 是设计缺陷）。
// 深拷贝三选一：① 拷贝构造器 ② 不可变对象直接共享 ③ JSON 往返（性能换便利）
public CartVO(CartVO other) {           // 拷贝构造器，显式、可控
    this.items = List.copyOf(other.items);
}
```

**不可变对象设计**（与 [01-Java17核心特性 §2.1](./01-Java17核心特性.md) 的 Record 呼应）：
字段 final、不提供 setter、集合字段防御性拷贝（入参拷贝进来，`List.copyOf` 拷出去）。
不可变对象天然线程安全——并发优化的第一手段是"消灭共享可变状态"，而不是加锁。

---

## 六、Lombok 与 MapStruct 原理（父 POM 双处理器的恩怨）

### 6.1 两条注解处理器路线

```
javac 编译 → 注解处理器（JSR 269）多轮处理
  ├── Lombok：直接改 AST（语法树）→ getter/setter "本来就在"
  └── MapStruct：扫描 Mapper 接口 → 生成 XxxMapperImpl.java（普通代码，可读可调试）
```

- **Lombok**：`@Data` = `@Getter + @Setter + @ToString + @EqualsAndHashCode +
  @RequiredArgsConstructor`（本项目 `Result<T>` 就是 `@Data`）
- **为什么 annotationProcessorPaths 里 Lombok 要放 MapStruct 前面**：MapStruct
  生成的 Impl 要调用 getter/setter，必须等 Lombok 先把 AST 改完——
  顺序错了 MapStruct 找不到属性，报"unknown property"（02-Maven 文档 Q3 的原理展开）
- **`@Data` 的坑**（见 06 文档 Q10）：继承要 `@EqualsAndHashCode(callSuper=true)`；
  `@Builder` 的默认值要 `@Builder.Default`，否则对象字段被置 null/0

### 6.2 MapStruct 为什么快过 BeanUtils

```java
@Mapper(componentModel = "spring")
public interface ProductConverter {
    ProductVO toVO(Product entity);        // 编译期生成 Impl：逐字段 set/get
}
// Spring BeanUtils.copyProperties 是运行时反射逐字段找 setter——慢、且字段改名
// 编译不报错（类型不匹配也是静默）。MapStruct 编译期就校验类型与字段映射。
```

---

## 七、高频面试题（含参考答案）

**Q1：new BigDecimal(0.1) 和 new BigDecimal("0.1") 的区别？**
A：double 构造把 0.1 的**二进制浮点实际值**（0.1000000000000000055...）原样转入，
精度陷阱；字符串构造按十进制精确解析。`BigDecimal.valueOf(0.1)` 内部走
`Double.toString` 也安全。规约：金额一律字符串构造或 valueOf，杜绝 double 构造。

**Q2：BigDecimal 的 equals 和 compareTo 有什么坑？**
A：equals 同时比较数值与 scale，`1.0` 与 `1.00` 不相等（去数据库 round-trip 后
scale 变化就踩坑）；compareTo 只比数值。业务比较（金额够不够、排序）一律
compareTo；想判"数值相等"用 `compareTo(...) == 0`。

**Q3：divide 不指定精度会怎样？**
A：除不尽（如 10/3）抛 ArithmeticException: Non-terminating decimal expansion。
必须 `divide(x, scale, RoundingMode.HALF_UP)` 显式精度与舍入。金额计算规范：
中途保精度、最终展示前统一 setScale(2) 舍入一次。

**Q4：金额字段为什么不用 double？DB 里怎么存？**
A：double 二进制浮点无法精确表示 0.1 等十进制小数，累加误差在金额上是资损问题。
DB 用 DECIMAL(12,2)（精确十进制）或以"分"为单位的 BIGINT，Java 对应 BigDecimal
或 long，两层类型必须配套。本项目 CartServiceImpl 的 multiply/reduce 链路全程
BigDecimal，是正确示范。

**Q5：LocalDateTime、Instant、ZonedDateTime 怎么选？**
A：Instant=绝对时刻（UTC 时间线，机器用）；LocalDateTime=无时区墙钟（人类日历，
存 DB、返回前端）；ZonedDateTime=带时区的完整语义（跨时区业务才需要）。
本项目约定：实体/API/缓存统一 LocalDateTime + 全链路同一时区，消息里带时间戳
用 Instant（epoch millis）。

**Q6：SimpleDateFormat 有什么问题？**
A：内部共享 Calendar 状态，**非线程安全**，并发格式化会错乱甚至抛异常。
替换：DateTimeFormatter（不可变、可静态共享）。这也是为什么代码规约禁止把
SimpleDateFormat 定义成 static。

**Q7：为什么禁用 Java 原生序列化？**
A：① 安全：readObject 反序列化可触发 gadget 链 RCE（历史漏洞重灾区）；
② 冗余的类元数据体积大；③ 类演进兼容性差（serialVersionUID 不符即炸）。
跨进程通信用 JSON/Protobuf；确需保留时收紧白名单、升级到安全的替代（如
JSON 的多态白名单校验）。

**Q8：Redis 缓存的对象为什么 JSON 里会多一个 @class 字段？有什么风险？**
A：GenericJackson2JsonRedisSerializer 开启了 default typing，写入类型信息才能把
JSON 反序列化回正确的 Java 类型（缓存读出来要还原成 ProductVO 而不是 Map）。
风险：多态反序列化可被注入任意类 → 必须配 `BasicPolymorphicTypeValidator`
白名单（如只允许 `com.aics.` 前缀），Jackson 2.10+ 默认要求显式声明。

**Q9：transient 和 serialVersionUID 的作用？**
A：transient 字段不参与序列化（密码、线程、连接这类不可序列化/敏感字段）；
serialVersionUID 是类版本号，反序列化时两端不一致抛 InvalidClassException。
显式声明 serialVersionUID 可控演进（新增字段向后兼容），不写则由类结构自动
计算，任何小改动都会导致旧数据反序列化失败。

**Q10：怎么实现深拷贝？**
A：不推荐 clone（浅拷贝语义混乱、Cloneable 是设计缺陷）。三选一：拷贝构造器
（显式可控，首选）、序列化往返（JSON，性能换省事）、不可变对象直接共享
（无需拷贝）。集合字段注意防御性拷贝，`List.copyOf` 出入各一次。

**Q11：Lombok 是怎么工作的？@Data 有什么坑？**
A：JSR 269 注解处理器在 javac 编译期直接修改 AST，让 getter/setter 像原生写出。
坑：① `@EqualsAndHashCode` 继承场景要 callSuper=true；② `@Builder` 默认值要
`@Builder.Default`；③ `@Data` 用在 JPA/实体上，可变字段破坏 Set/Map 语义；
④ `@AllArgsConstructor` 按字段声明顺序生成，重排字段会静默改变调用方。

**Q12：MapStruct 和 Spring BeanUtils 的区别？为什么它和 Lombok 有顺序要求？**
A：MapStruct 编译期生成 Impl（普通 get/set 调用，类型不匹配编译报错，零反射）；
BeanUtils 运行时反射拷贝（慢、改名不报错、类型不匹配静默）。顺序：annotation
processor 是多轮处理的，Lombok 必须排在 MapStruct 前面先生成 getter/setter，
否则 MapStruct 编译期分析属性时"看不到"字段——父 POM 的
annotationProcessorPaths 顺序即此原理。

---

## 八、学习检查清单

- [ ] 背出 BigDecimal 三坑（double 构造/divide 精度/equals scale）
- [ ] 说出金额规范：DECIMAL + BigDecimal + compareTo + 最终 setScale
- [ ] 讲清 LocalDateTime/Instant 的选择与全链路时区一致性清单
- [ ] 能画 Redis 缓存 @class 字段的来龙去脉与白名单防御
- [ ] 说得出 Lombok 改 AST、MapStruct 生成 Impl、两者顺序敏感的原因

## 九、后续实践（对照 04 计划 P6 任务）

1. 给 `CartServiceImpl` 补精度边界单测（本文 1.2 的两条即是起点：先红后绿）
2. 在 common 定义统一 `ObjectMapper` Bean（JavaTimeModule + 时区 + 白名单
   default typing + 非空策略），走 01-P4 的 Starter 自动装配收编路径
3. 复盘一个接口的 JSON 时间格式，确认前端解析无时区歧义

---

## 下一步

第一阶段（Java 基础）到此收官 → [02-Spring微服务/01-SpringBoot核心原理](../02-Spring微服务/01-SpringBoot核心原理.md)
