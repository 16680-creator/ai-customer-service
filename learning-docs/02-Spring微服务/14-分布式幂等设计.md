# 14-分布式幂等设计：从散点手写到 @Idempotent 框架化

> 2026-09 落地记录：缺口总账 [00-学习路线总览/05](../00-学习路线总览/05-技术缺口分析与补全计划.md) 的 B 类头号缺口。本批在 `ai-cs-common` 落地了注解式幂等组件（`com.aics.common.idempotent`，9 个单测通过，`mvn -pl ai-cs-common -am test` BUILD SUCCESS），根 POM 补开了 `maven.compiler.parameters`。诚实说：**组件尚未接入任何业务方法**——支付回调等链路的行为本批零改动，接入验证是下一步。
> 前置阅读：[13-支付渠道集成与回调一致性](13-支付渠道集成与回调一致性.md)（DB 唯一键幂等实例）。

---

## 一、幂等问题从哪来

分布式系统里"同一个请求到达两次"不是异常，是常态：

| 成因 | 本项目实例 |
|---|---|
| **调用方重试** | Feign 重试（网关路由注释明写"重试只对幂等 GET"）、渠道回调重试（应答非 2xx 就再打） |
| **消息至少一次投递** | RocketMQ 重投、事务消息回查（`PaySuccessTransactionListener` 注释："回查必须幂等"） |
| **并发重复** | 用户双击支付按钮、两个回调线程同时入账 |

幂等的定义就一句话：**同一业务动作执行一次和执行 N 次，结果一致**。

## 二、本项目幂等现状盘点（grep 实证）

全仓 grep `幂等|idempot|Idempotent`（组件落地前）命中四类散点：

1. **状态检查 + 唯一键**（最完整）：`PayTransactionServiceImpl.markSuccess`（"已成功或不存在 return false"）+ `pay_transaction.uk_order_no` 唯一索引；
2. **upsert 天然幂等**：`ProductCdcConsumer`（"ES document id 固定为 product.id，重放均覆盖"）、`KnowledgeSyncConsumer`（Chroma delete+add）；
3. **业务幂等键**：Agent 写工具 `CreateAfterSaleTool`（"幂等键 = runId + 动作类型，订单服务按唯一键去重"）；
4. **明示未做**：`ChatMessageConsumer`（"实现幂等消费需业务方自行保证"）。

问题不在"有没有"，在**形态**：每处手写，语义靠注释约定，漏一处就是重复入账/重复发货。框架化目标：把"挡并发重放"这一层收敛为一个注解。

## 三、三层幂等全景：组件只是第一层

```text
┌─ 第 1 层 @Idempotent（Redis SET NX）── 挡并发重放：快、便宜，但 Redis 可丢 → 非权威
├─ 第 2 层 业务状态检查 ──────────────── 挡语义重复：PENDING→SUCCESS 才入账（DB 事务内）
└─ 第 3 层 DB 唯一键（uk_order_no）───── 终审：并发窗口漏网之鱼在提交时刻被唯一约束击落
```

为什么第 1 层"非权威"还要做？第 2/3 层已经能保证正确性，但每次重复都要打进 DB；高并发回调下第 1 层用一次 Redis GET 级别的代价把 99% 的重放挡在业务之外。**正确性归 DB，性能归 Redis**——这个定位决定了下面的失败语义。

## 四、组件落地实录：com.aics.common.idempotent

四个主类 + 一个注解，全部在 `ai-cs-common/src/main/java/com/aics/common/idempotent/`：

```text
com.aics.common.idempotent
├── Idempotent.java                 注解：key(SpEL) + ttlSeconds(默认300) + message
├── IdempotentAspect.java           切面：SET NX 占位 → 放行 → 成功保留/失败释放
├── IdempotentRejectException.java  重复拒绝（继承 BusinessException，code 409）
├── IdempotentProperties.java       aics.idempotent.enabled / keyPrefix
└── IdempotentAutoConfiguration.java 自动装配（imports 第 4 行注册）
```

核心切面语义（`IdempotentAspect.around`，完整行为表）：

| 场景 | 行为 | 为什么 |
|---|---|---|
| 首次调用 | `setIfAbsent(key, "1", ttl)` 成功 → 放行；业务成功后 key **保留至 TTL** | TTL 即去重窗口 |
| TTL 内重复 | 占位失败 → 抛 `IdempotentRejectException`（409"重复请求"） | 业务方法零消耗 |
| 业务异常 | `delete(key)` 后原样上抛 | 允许渠道重试/MQ 重投再进来 |
| Redis 故障 | **fail-open 放行**（log.warn + proceed） | 去重层故障不能阻断业务；正确性由第 2/3 层兜底 |

新增公共枚举：`ResultCode.DUPLICATE_REQUEST(409, "重复请求，请勿重复提交")`（通用段，409 未被占用）。异常继承 `BusinessException`，`GlobalExceptionHandler` 无需改动即可统一渲染。

关键实现片段（占位 + fail-open）：

```java
boolean acquired;
try {
    acquired = Boolean.TRUE.equals(ops.setIfAbsent(redisKey, TOKEN, ttl));
} catch (RuntimeException e) {
    log.warn("幂等组件 Redis 异常，fail-open 放行 key={}", redisKey, e);
    return joinPoint.proceed();
}
if (!acquired) {
    throw new IdempotentRejectException(idempotent.message());
}
try {
    return joinPoint.proceed();
} catch (Throwable t) {
    tryRelease(redisKey);
    throw t;
}
```

## 五、两个实现级的坑（都真实踩过）

### 5.1 SpEL 参数名依赖 -parameters 编译标志

key 表达式 `#orderNo` 按参数名取值，而 Spring Framework 6.1（Boot 3.2）**移除了字节码局部变量表推断**（LocalVariableTableParameterNameDiscoverer），参数名必须来自编译期 `-parameters` 标志。本项目的根 POM 是自维护 parent（没有 spring-boot-starter-parent 兜底），默认未开——实测 SpEL 拿到的是 `p0`。修复：根 POM properties 增加

```xml
<maven.compiler.parameters>true</maven.compiler.parameters>
```

对全仓只是多编进一段调试信息，无行为影响；切面里同时保留 `#p0` 位置参数兜底。

### 5.2 Redis 依赖声明为 optional

`ai-cs-common` 此前无 Redis 依赖。幂等组件要 `StringRedisTemplate`，但 common 不该把 Redis 强加给所有引用方（gateway 引 common 时连 web 都要显式排除）。做法：`spring-boot-starter-data-redis` 声明 `<optional>true</optional>` + 自动装配 `@ConditionalOnClass(StringRedisTemplate.class)`——有 Redis 的服务（chat/order/pay…）自动获得切面，没有的跳过整个装配。`spring-boot-starter-aop` 则声明为普通依赖（切面织入需要 aspectjweaver，全仓第一个 `@Aspect` 就在这）。

## 六、怎么接入（示例写法，本批未应用）

```java
// 支付回调入账（ai-cs-pay，下一步的接入对象）
@Idempotent(key = "'pay:callback:' + #paymentMethod + ':' + #context.orderNo()", ttlSeconds = 300)
public void processNotify(String paymentMethod, NotifyContext context) { ... }

// MQ 消费者（ai-cs-message，现状注释自认未做幂等）
@Idempotent(key = "'chat:consume:' + #message.messageId", ttlSeconds = 600)
public void onMessage(ChatMessage message) { ... }
```

注意两个语义细节：

1. **拒绝 ≠ 排队**：占位期间的并发请求直接 409/异常，不做等待合并——适合回调与消费场景；要做"请求合并"是另一个组件。
2. **事务边界**：注解方法内若开事务，占位发生在提交前；失败路径已显式释放，但如果业务"成功提交后进程崩溃"，key 保留至 TTL，期间重复请求被拒——这正是去重窗口的语义，TTL 按业务可容忍的重复间隔设定。

## 七、测试与验证

两个测试类（`ai-cs-common/src/test/java/com/aics/common/idempotent/`），不依赖真实 Redis：

- `IdempotentAspectTest`（6 用例）：Mockito 模拟 `StringRedisTemplate` + `AspectJProxyFactory` 织入真实注解方法——首次放行并校验最终 key（`aics:idem:pay:callback:O1`）、重复拒绝且业务零触达、业务异常释放 key、成功保留 key、Redis 故障 fail-open、自定义 keyPrefix。其中"SpEL 参数名解析"这个用例同时验证了 5.1 的 `-parameters` 生效。
- `IdempotentAutoConfigurationTest`（3 用例）：`ApplicationContextRunner` 契约测试（与 `CommonAutoConfigurationTest` 同套路）——默认装配 / `enabled=false` 不装配 / 自定义 Bean 让位。

```bash
mvn -pl ai-cs-common -am test    # 2026-09 实测：Tests run: 19, Failures: 0, BUILD SUCCESS
```

**待办（下一批）**：把 `processNotify` 按第六节接入，并用双线程并发重放 100 次验证"仅入账 1 次"（需本地 Redis 环境）。

## 八、面试要点总结

> 本项目幂等分三层：Redis SET NX 注解切面挡并发重放（失败语义：成功保留 TTL 去重窗口、业务异常释放允许重试、Redis 故障 fail-open）、业务状态检查挡语义重复、DB 唯一键终审。组件以 @Idempotent(SpEL key) + @Aspect 落在 common，Redis 依赖 optional + @ConditionalOnClass 使无 Redis 服务自动跳过装配，重复请求统一 409；SpEL 参数名依赖根 POM 的 maven.compiler.parameters（Spring 6.1 移除了字节码推断）。定位原则：正确性归 DB，性能归 Redis。

```text
关键词：SET NX EX 占位 · 三层幂等 = 注解加速挡/状态检查/唯一键终审
失败语义 = 成功保留·异常释放·Redis 故障 fail-open · optional 依赖 + ConditionalOnClass
-parameters 编译标志 · 拒绝而非排队（回调/消费语义）
```

## 学习检查清单

- [ ] 能说出三层幂等各自的职责与"谁权威"
- [ ] 能解释为什么 Redis 层 fail-open 是安全的设计
- [ ] 能解释 -parameters 标志与 SpEL 参数名的关系
- [ ] 完成 processNotify 接入与 100 次并发重放验证（有本地 Redis 后）
