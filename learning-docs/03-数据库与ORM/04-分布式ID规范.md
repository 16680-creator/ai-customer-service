# 04-分布式 ID 规范（雪花 ID）

> 2026-08 制定（03-P6 落地）。背景：全项目盘点发现主键策略混用——user 分片表
> `ASSIGN_ID`（正确），order/product/pay/message 全是 `IdType.AUTO`（DB 自增）。
> 本规范统一约束，存量表不强制迁移，新增代码必须遵守。

## 一、为什么分库分表场景禁止数据库自增

- 自增主键的"唯一性"只在**单库内**成立：order 库和 product 库各自从 1 开始，
  跨库合并/迁移/对账时必然冲突
- 分片键路由（如 user 表按 ID 后四位取模）要求 ID 在写入前就已确定，
  自增是 DB 写入时才生成，路由无从谈起
- 扩容迁移（双写 → 切读）时，两套自增序列无法合并

## 二、项目规范（三条）

| # | 规则 | 依据 |
|---|------|------|
| 1 | **新增表实体一律 `@TableId(type = IdType.ASSIGN_ID)`**（MyBatis-Plus 内置雪花） | 应用侧生成，写入前 ID 可知，全局唯一 |
| 2 | **分片表禁止 AUTO**（守护测试锁死：`ai-cs-user`） | 见第一节 |
| 3 | 有业务主键的表用 `IdType.INPUT`（如 message 的 `AgentRun`/`LlmTrace`，主键由上游生成） | 调用方已保证唯一性 |

存量 `AUTO` 表不强制迁移，风险边界：

- 这些表（order/product/pay/message）当前**未分片**，单库内自增自洽
- 一旦某表要分片（订单表是大概率），迁移步骤：先改 ASSIGN_ID → 新数据雪花 →
  存量自增 id 保留（雪花 ID 是 19 位长整数，与存量自增 ID 空间不重叠，无需回填）

## 三、雪花 ID 结构与 MyBatis-Plus 实现

```
0 | 时间戳(41bit, 约69年) | 机器ID(10bit) | 序列号(12bit, 同毫秒4096个)
```

- MyBatis-Plus `ASSIGN_ID` 默认实现 `Sequence`：workerId 取环境变量
  `MP_WORKER_ID`（不配则自动推算），数据中心位参与机器位
- **时钟回拨**：Sequence 内部处理——回拨小于阈值则等待，超过则抛异常
  （生产部署要求 NTP 微调式同步，禁止大幅跳变）
- 列类型必须 `BIGINT`（雪花是 19 位 long）；前端序列化注意 JS 精度丢失
  （超过 2^53），本项目前端统一用 String 接收 id（Jackson 配置已在 common 处理，
  若新增前端字段注意 `@JsonSerialize(using = ToStringSerializer.class)`）

## 四、守护测试

`ai-cs-user/src/test/java/com/aics/user/sharding/ShardedEntityIdTypeTest.java`：
扫描 `com.aics.user.entity` 包所有实体的 `@TableId`，断言分片表（user）不允许
`IdType.AUTO`——防止后续有人给分片表配回自增（这是主键冲突事故的源头）。

```java
// 核心断言：分片表实体必须 ASSIGN_ID 或 INPUT
assertNotEquals(IdType.AUTO, tableId.type());
```

## 五、面试要点速记

- 雪花结构 41+10+12；时钟回拨两种处理（等待/抛异常）与 NTP 部署要求
- 号段模式 vs 雪花：号段（Leaf）DB 压力小、可读性好；雪花无单点、性能高
- 为什么自增在分片场景失效（唯一性域、路由前置要求、扩容合并）
- JS 精度丢失：Java long → JS number 超 2^53 丢精度，接口层转字符串
