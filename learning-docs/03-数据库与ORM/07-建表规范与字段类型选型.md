# 07-建表规范与字段类型选型

> 本项目 `deploy/mysql/` 下有 14 个建表脚本（init.sql、order-init.sql、product-init.sql、after-sales-agent-init.sql……），各自长年演化，规范靠"口口相传"。本篇把散在各脚本里的隐式约定**提炼成显式规范**，并对真实 DDL 做一次"规范审视"——挑出来的问题都是可动手修的。
> 锚定文件：`deploy/mysql/init.sql`（user/knowledge/chat 三库）、`deploy/mysql/order-init.sql`（订单四表）、`deploy/mysql/mysql.cnf`（字符集与 sql_mode）。
> 前置阅读：[01 §三 表设计](01-MySQL核心知识.md)、[04-分布式ID规范](04-分布式ID规范.md)（主键策略的下半篇在这里）。

---

## 一、字段类型选型表

### 1.1 金额：只准 DECIMAL

`order-init.sql` 的订单表是正面教材：

```sql
`total_amount` DECIMAL(10,2) NOT NULL COMMENT '商品总金额（原价）',
`pay_amount`   DECIMAL(10,2) NOT NULL COMMENT '应付金额',
```

| 类型 | 能用吗 | 原因 |
|---|---|---|
| FLOAT / DOUBLE | **禁止** | 二进制浮点，`0.1+0.2≠0.3`，对账必炸（对账场景见 [18-银行金融开发/09-金融级一致性与对账实战](../18-银行金融开发/09-金融级一致性与对账实战.md)） |
| DECIMAL(10,2) | 推荐 | 精确小数；10 位里含 2 位小数 → 单笔上限 99,999,999.99，电商订单够用 |
| BIGINT（分为） | 亦可 | Java 侧 `long` 无精度问题；**但必须全链路统一单位，注释写清"单位：分"** |

Java 侧对应类型是 `BigDecimal`（`new BigDecimal("0.1")` 字符串构造，别用 double 构造），ORM 映射零转换。

### 1.2 时间：本项目统一 DATETIME

```sql
`create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
`update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
```

| | DATETIME（本项目） | TIMESTAMP |
|---|---|---|
| 范围 | 1000~9999 年 | 1970~2038（**2038 问题**） |
| 存储 | 8 字节，与时区无关 | 4 字节，存 UTC 按时区转换 |
| 取舍 | 项目容器已固定 `TZ: Asia/Shanghai`，直接存本地时间 | 跨时区部署自动转换是优点，但 2038 上限是硬伤 |

规范三条：**① 每表必有 create_time/update_time**（`ON UPDATE CURRENT_TIMESTAMP` 让数据库兜底，别只信应用层）；② 不可变记录（如 `chat_message`）可以**只有 create_time**——消息发出去就不改，没有 update 语义；③ 终止时间字段（`orders.expire_time`）建索引，因为它要被定时任务扫描（见 [05 §3.3](05-MySQL锁机制与主从复制读写分离.md)）。

### 1.3 字符串与大字段

| 场景 | 选型 | 本项目例 |
|---|---|---|
| 短定长/枚举型 | `VARCHAR(n)`，n 按真实上限 + 余量 | `payment_method VARCHAR(20)`（WECHAT/ALIPAY/BANK_CARD） |
| 密码/摘要 | `VARCHAR(128)`（存密文不是明文） | `sys_user_0.password`，两层安全模型见 [09-安全与设计模式/03](../09-安全与设计模式/03-SpringSecurity微服务两层安全模型.md) |
| 消息正文等大文本 | `TEXT`（64KB）别用 VARCHAR(65535) | `chat_message.content TEXT` |
| 半结构化扩展字段 | `JSON`（8.0 可建函数索引/多值索引） | `chat_message.metadata JSON` |
| 手机号 | `VARCHAR(20)` 别用 BIGINT | 区号/前导零/脱敏都在字符串上做 |

规范：**TEXT/JSON 不进联合索引、不参与 `SELECT *`**；`chat_message` 查列表时先查 `id/session_id/sender/create_time`（小列），正文按 id 二次取——这就是 [12-性能工程/05](../12-性能工程/05-SQL调优与执行计划.md) 覆盖索引思想的建表侧配合。

### 1.4 状态：数字还是字符串？项目两套并存

| 风格 | 本项目例 | 优点 | 缺点 |
|---|---|---|---|
| `TINYINT` + 枚举注释 | `chat_session.status TINYINT`（0-已结束 1-进行中 2-转人工） | 省 1 字节、索引小 | 肉眼不可读，全靠 COMMENT 纪律 |
| `VARCHAR` + 字面量 | `orders.status VARCHAR(20) DEFAULT 'PENDING_PAY'` | 日志/排障直读 | 占空间，拼写错误数据库不管 |

规范：**新表二选一并全模块统一**；若选 VARCHAR，Java 侧必须用枚举类约束取值（参考 `02-Spring微服务/12-订单状态机治理` 的状态机）；若选 TINYINT，注释里的枚举清单是唯一文档，改值必须同步注释。**禁止**两套风格在同一新模块混用（本项目 order 库与 chat 库并存，属历史债，见 §四审视）。

### 1.5 布尔与字符集

- 布尔用 `TINYINT(1)`：`cart_item.selected TINYINT(1) NOT NULL DEFAULT 1`。MySQL 没有真 BOOLEAN，`(1)` 只是显示宽度约定。
- 库、表、连接三处统一 `utf8mb4`（`mysql.cnf` 的 `character-set-server=utf8mb4` + 每表 `DEFAULT CHARSET=utf8mb4`）。**utf8mb3 存不了 emoji**——客服消息里用户发 emoji 是常态，老库迁移时先查 `information_schema` 有没有 utf8mb3 残留。
- 排序规则：user/chat 库 `utf8mb4_unicode_ci`、order 库 `utf8mb4_general_ci` 并存。**跨库 JOIN 时 collation 不一致会让索引失效**（隐式转换），新表建议统一 `utf8mb4_0900_ai_ci`（8.0 默认）或与主库对齐。

## 二、必备字段与主键策略

每表七件套（项目 DDL 已基本遵守）：

```sql
id          BIGINT NOT NULL COMMENT '主键',
create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
update_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
deleted     TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除 1-已删除',   -- 视语义
PRIMARY KEY (id)
```

主键二选一（决策表，接 [04-分布式ID规范](04-分布式ID规范.md) 的三条规范）：

| 策略 | 本项目例 | 何时用 |
|---|---|---|
| 自增 AUTO_INCREMENT | `orders.id`、`cart_item.id` | 单库、无分片计划、插入密集（自增主键顺序写对 B+ 树最友好） |
| 雪花 ID | `chat_session.id`、`sys_user_*.id` | 已分库分表（ShardingSphere 的分片键）/ 未来可能分 |

对照记忆：**user 库要分片所以雪花，order 库单库所以自增**——这不是不统一，而是**主键策略跟着"分不分片"走**。但 order 库必须有"业务键"兜底：`order_no VARCHAR(32)` + `UNIQUE KEY uk_order_no`，对外只暴露 order_no，将来分片按 order_no/user_id 重构时 id 可弃。

逻辑删除两问：**① 哪些表配 deleted？** 有"回收站/可恢复"语义的（会话、用户）配；有状态机的（订单 PENDING_PAY→CANCELED）**用状态字段表达终止，不再叠 deleted**，否则"已取消又已删除"的组合爆炸。**② 唯一键怎么办？** `deleted` 参与唯一键要写成 `uk(user_id, product_id, deleted)`——但删两次就冲突，规范做法是 deleted 存"0 或主键值"（同表内不重复），或干脆用时间戳。本项目 `uk_user_product (user_id, product_id)` 未含 deleted，物理删除购物车（`deleteBatchIds`）所以没踩坑——**选了物理删除就别配 deleted 字段，两套语义别混**。

## 三、索引设计五条（建表期就定）

1. **唯一键兜底并发**：`uk_order_no` 之外，支付流水表靠 `uk(渠道, 渠道单号)` 挡回调重试——见 [05 §3.1](05-MySQL锁机制与主从复制读写分离.md)，这是"用约束写正确性"的第一原则。
2. **区分度差的列不单独建索引**：`idx_status`（订单状态 5 个枚举值）单独用上不了 EXPLAIN 的好等级，**组合进 `(user_id, status)` 才有意义**——见 §四对真实 DDL 的审视。
3. **最左前缀**：联合索引列序 = 等值列在前、范围列在后（[01 §4.3](01-MySQL核心知识.md)）。
4. **单表索引 ≤ 5~6 个**：每个索引都是一棵要维护的 B+ 树，写多的表（orders、chat_message）尤其克制。
5. **外键不用物理外键**：本项目所有表只有逻辑关联（`order_item.order_id` 无 FOREIGN KEY）。微服务 + 分库分表下物理外键是负资产，一致性由应用层 + Seata 保证（[02-Spring微服务/06](../02-Spring微服务/06-Seata分布式事务AT模式.md)）。

## 四、反范式与快照冗余（项目最值得学的建表技巧）

`order_item` 明明能 JOIN `product` 拿商品名，却冗余了三列：

```sql
`product_name`  VARCHAR(200) NOT NULL COMMENT '商品名称快照',
`product_price` DECIMAL(10,2) NOT NULL COMMENT '成交单价快照',
`order_no`      VARCHAR(32) NOT NULL COMMENT '订单编号（冗余）',
```

这不是浪费，是**业务正确性**：商品会改价、改名甚至下架，而"当时买的那一单"必须永久可复现（发票、售后、审计都要）。判据一句话：**冗余"事实快照"，不冗余"现状引用"**。

| 冗余了什么 | 类型 | 对不对 |
|---|---|---|
| 成交时单价、商品名快照 | 事实快照 | ✅ 必须 |
| 库存数（现状） | 现状引用 | ❌ 该 JOIN 或走缓存 |
| `order_item.order_no` | 查询路径优化（免回表/跨表） | ✅ 典型的"以空间换覆盖索引" |

## 五、命名与脚本规范

- 表名小写下划线复数不定（`cart_item`/`orders`），字段小写下划线；索引 `uk_`（唯一）/`idx_`（普通）前缀 + 字段名——本项目 DDL 已统一，新脚本照抄。
- 每张表 `COMMENT='中文名'`、每个字段 `COMMENT` 必写；枚举字段的取值清单写进 COMMENT。
- 建表脚本幂等：全部用 `CREATE TABLE IF NOT EXISTS`（14 个脚本已遵守），容器重复初始化不炸。
- `sql_mode` 严格模式（`mysql.cnf`：`STRICT_TRANS_TABLES,...`）：**禁止**图省事去掉——非严格模式下超长截断、零日期静默入库，是脏数据之源。

## 六、真实 DDL 规范审视（动手改）

对照 `deploy/mysql/order-init.sql` 逐条挑刺，每条都给修法（改前先看 [08-大表治理](08-大表治理-冷热分离与数据归档.md) 的 Online DDL 小节——DDL 也会锁表）：

| # | 发现 | 风险 | 修法 |
|---|---|---|---|
| 1 | `order_item` 缺 `create_time/update_time` | 无法排障"什么时候生成的订单项" | 补两列，`DEFAULT CURRENT_TIMESTAMP` |
| 2 | `orders.idx_status` 单列索引 | 区分度≈5 个枚举，基本不被选中 | 改 `idx_user_status(user_id, status)`，删单列 |
| 3 | `coupon.idx_status` 同病 | UNUSED 占绝大多数 | 高频查询是"我的可用券"：`idx(user_id, status, expire_time)` |
| 4 | order 库无 `deleted` 字段且购物车物理删除 | 可接受（选了物理删除） | 保持一致即可，**别一半表逻辑删一半物理删还不写文档** |
| 5 | order 库 collation `utf8mb4_general_ci` ≠ user/chat 库 `unicode_ci` | 未来跨库 JOIN/比对隐式转换 | 新表统一 8.0 默认 `utf8mb4_0900_ai_ci`，存量排队迁移 |
| 6 | `orders` 状态 VARCHAR 与 `chat_session` 状态 TINYINT 并存 | 双规范并存心智负担 | 本篇 §1.4 决策后，新表统一，存量发版时顺手改 |

> 审视练习的正确心态：不是"原作者错了"，而是**规范是活的**——上面每条在当年都可能是对的（比如 idx_status 建表时数据量小无所谓）。规范审视的价值在于让"为什么改"有据可查。

## 七、动手练习

1. 给 `order_item` 写出 ALTER 语句补时间两列，并用 `ALGORITHM=INPLACE` 验证不锁写（对照 08 篇 §六）。
2. 把 §六 #2 的索引改造写成"先建新索引 → 观察 EXPLAIN → 删旧索引"的安全三步。
3. 为 `chat_message.metadata JSON` 设计一个"取 metadata 里 model 字段统计用量"的查询，体验 8.0 的 `JSON_EXTRACT` 与生成列索引。

## 八、面试要点

- "DECIMAL 和 DOUBLE 差在哪？" → 定点 vs 二进制浮点；金额必 DECIMAL/分为单位，对账零误差。
- "DATETIME 和 TIMESTAMP 怎么选？" → 范围（2038）、时区行为、8 字节 vs 4 字节；本项目容器固定时区选 DATETIME。
- "为什么订单项要冗余商品名？" → 事实快照 vs 现状引用；历史必须可复现。
- "逻辑删除和唯一键冲突怎么办？" → deleted 参与唯一键 + 非 0 值方案；或明确选物理删除。
- "为什么不用物理外键？" → 分库分表/微服务下跨库不可达，一致性上移到应用层与分布式事务。

## 学习检查清单

- [ ] 能背出金额、时间、状态、大字段四类选型及理由
- [ ] 能说出本项目"自增 vs 雪花"双轨各自的适用表
- [ ] 能解释 `order_item` 三个冗余列分别属于哪类冗余
- [ ] 独立完成 §六 六条审视中至少三条的 ALTER 改造
- [ ] 新建表能一次写出七件套 + 命名规范 + 幂等脚本
