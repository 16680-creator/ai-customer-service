# 08-大表治理：冷热分离与数据归档

> 表是在"没人管"的时候变大的。本项目有三张天然会无限增长的表：`chat_message`（每条 AI 对话一行，TEXT 正文 + JSON 元数据）、`orders`/`pay_transaction`（只增不改的历史单据）、以及 Seata 的 `undo_log`（写多删多的临时表）。
> 本篇回答：表多大了要动手？**归档、分区表、分库分表**三条路怎么选？搬数据怎么搬不丢不锁？大表怎么改 DDL？
> 锚定文件：`deploy/mysql/init.sql`（chat_message DDL）、`deploy/mysql/mysql.cnf`（buffer-pool 256M）、`deploy/docker-compose/docker-compose-master-slave.yml`。
> 前置阅读：[03-ShardingSphere用户表分库分表实战](03-ShardingSphere用户表分库分表实战.md)、[07-建表规范与字段类型选型](07-建表规范与字段类型选型.md)。
> 深分页（扫描式翻页的优化）**不在本篇**，见 [12-性能工程/05 §五](../12-性能工程/05-SQL调优与执行计划.md)。

---

## 一、表是怎么长大的：先算账再动手

`chat_message` 增长模型（按本项目量级估算）：

```
假设：1 万 DAU × 每人每天 20 轮对话 × 每轮 3 条消息（用户/AI/客服） = 60 万行/天
单行：content TEXT 平均 1KB + 索引 ≈ 1.5KB → 0.9 GB/天 → 一年 ≈ 330 GB
```

对照 `mysql.cnf` 的 `innodb-buffer-pool-size=256M`：**一年后的热数据是缓冲区的 1000 倍**，缓存命中率必然塌掉，慢查询只是时间问题。这就是"先算账"的意义——**治理时点由增长率决定，不是由"感觉卡了"决定**。经验阈值（单表，InnoDB，常规硬件）：

| 指标 | 警戒线 | 动作 |
|---|---|---|
| 行数 | 2000 万 ~ 5000 万 | 评估归档/分区（B+ 树层高从 3 变 4 的量级） |
| 表空间 | buffer-pool 的 10~20 倍 | 热数据挤不进内存，命中率下降 |
| 单表 DDL 变更时长 | > 30 分钟 | 必须上 Online DDL 工具（§六） |

## 二、三条路决策表：归档 vs 分区 vs 分库分表

| | 数据归档（冷热分离） | 分区表（PARTITION） | 分库分表（ShardingSphere） |
|---|---|---|---|
| 思路 | 老数据搬走，主表保持小 | 物理上拆文件，逻辑上还是一张表 | 逻辑层拆到多库多表 |
| 改造成本 | **低**（应用基本无感） | 低（DDL 即可，但约束多） | **高**（改配置 + 改 SQL 习惯 + 迁移，见 03 篇） |
| 收益 | 主表小而热；冷数据仍可查（归档库/ES/MongoDB） | 分区裁剪加速范围查询；按分区删除 = 秒删 | 突破单库写上限与容量上限 |
| 风险 | 归档任务 bug 会丢数据（要演练） | 分区键必须进主键/唯一键；查询不带分区键就全分区扫 | 分片键选错是事故（03 篇踩坑记录） |
| 本项目 | **chat_message 首选** | orders 按 expire_time/create_time 可选 | user 表已用；订单量级暂不需要 |

决策口诀：**先问"老数据还要不要高频查"——不要就归档（最便宜）；要但只按时间查就分区；写入量连单库都扛不住才分库分表。** 本项目 user 库分片是"为学而练"（量级远未到），chat_message 归档才是真实需求。

## 三、冷热分离设计

### 3.1 冷热边界怎么定

按**业务访问模式**定，不按"超过 N 个月"拍脑袋：

- `chat_message`：正在进行的会话读最近消息；会话结束（`chat_session.status=0`）后，消息基本只被"审计回查/投诉取证"访问 → **以会话结束时间划冷热**，而不是消息创建时间（否则进行中的老会话被误伤）。
- `orders`：已完成订单 90 天内可能退换货，之后只进报表 → 90 天为界；报表走 14-数据工程与OLAP 的链路，不需要行级点查。

### 3.2 冷数据放哪

| 目标 | 适用 | 本项目现状 |
|---|---|---|
| 同库归档表 `chat_message_archive` | 量级中等、仍要偶尔 SQL 查 | 最小改造首选 |
| 独立归档库（同实例） | 隔离备份策略 | 备份可只备热库（→ [09](09-备份恢复与数据安全.md)） |
| MongoDB | 非结构化、 schema 自由 | **已有先例**：对话审计归档就是这条路（[04-中间件/10](../04-中间件/10-MongoDB对话审计归档.md)） |
| 对象存储（MinIO）+ 索引表 | 只取证不查询，成本最低 | 导出 JSON/CSV 挂 MinIO，主表只留 id 列表 |

> 查重声明：MongoDB 侧的消费、查询、TTL 细节在 [04-中间件/10-MongoDB对话审计归档](../04-中间件/10-MongoDB对话审计归档.md)；本篇只负责"MySQL 侧怎么搬出去"。

## 四、归档任务落地（三步 + 三个坑）

```sql
-- 1. 归档表与主表同构（建议直接 CREATE TABLE ... LIKE chat_message）
CREATE TABLE chat_db.chat_message_archive LIKE chat_db.chat_message;

-- 2. 搬：批内一次事务，主键范围切批，避免大事务
INSERT INTO chat_message_archive
SELECT m.* FROM chat_message m
JOIN chat_session s ON s.id = m.session_id
WHERE s.status = 0 AND s.update_time < NOW() - INTERVAL 90 DAY
LIMIT 2000;

-- 3. 删：同批同条件物理删除（INSERT...SELECT 与 DELETE 分开提交，崩溃可重）
DELETE m FROM chat_message m
JOIN chat_session s ON s.id = m.session_id
WHERE s.status = 0 AND s.update_time < NOW() - INTERVAL 90 DAY
LIMIT 2000;
```

调度用 XXL-Job（部署见 [07-运维部署/05-XXL-Job分布式调度](../07-运维部署/05-XXL-Job分布式调度.md)），低峰执行。**三个坑**：

1. **DELETE 必须带 LIMIT 循环**：一次删 500 万行 = 超长事务 = undo 膨胀（[06 §二](06-事务进阶-MVCC实现与三大日志.md)）+ 从库延迟（[13-稳定性工程/04](../13-稳定性工程/04-高可用架构模式.md) 的复制延迟节）。
2. **先 INSERT 后 DELETE 的窗口内数据是"两边都有"**：查询端要么容忍重复（去重键），要么归档表只作备份不作查询源，直到核对通过。
3. **搬前先 COUNT 校验 + 搬后抽样比对**：`SELECT COUNT(*)` 对不上就停任务；上线首月每天人工核对一次，再转全自动。

`undo_log` 是另一类"清理"：Seata 二阶段提交后会异步删，但全局回滚异常时会残留——运维巡检项：`SELECT COUNT(*) FROM undo_log WHERE log_created < NOW() - INTERVAL 1 DAY`，大于 0 查 Seata 侧异常（机制见 [02-Spring微服务/06](../02-Spring微服务/06-Seata分布式事务AT模式.md)）。

## 五、分区表：什么时候值得用

```sql
-- orders 按月 RANGE 分区（示意）
ALTER TABLE orders PARTITION BY RANGE (TO_DAYS(create_time)) (
    PARTITION p202608 VALUES LESS THAN (TO_DAYS('2026-09-01')),
    PARTITION p202609 VALUES LESS THAN (TO_DAYS('2026-10-01')),
    PARTITION pmax    VALUES LESS THAN MAXVALUE
);
```

- **分区裁剪**：`WHERE create_time >= '2026-09-01'` 只扫 9 月分区；`DROP PARTITION` 一秒删掉整月——比 DELETE 快几个数量级，是"时间序列表"的天然归档方式。
- **代价**：分区键必须包含在所有唯一键里（`uk_order_no` 要扩成 `uk(order_no, create_time)`，唯一性被削弱——同单号跨月可重复入库，必须应用层再兜底）；查询不带分区键则全分区扫。
- 与 ShardingSphere 的关系：分区在**单库内**解决单表问题，分片在**多库间**解决容量与写入问题；**分了片就不用分区**（每片已经够小），本项目 orders 未分片，分区可用。

## 六、Online DDL：表大了以后怎么改结构

小表 `ALTER TABLE` 秒级；百万行以上要先懂 MDL（元数据锁）：任何 DML 都持有 MDL 读锁，DDL 需要 MDL 写锁——**一个长事务挡住 DDL，DDL 挡住后面所有 DML，整表雪崩**（05 §二的 MDL 行有提示，这里给完整方案）。

| 方式 | 原理 | 适用 |
|---|---|---|
| `ALGORITHM=INSTANT`（8.0） | 只改元数据，秒级 | 加列（末尾）、改列名等有限操作，**首选先试** |
| `ALGORITHM=INPLACE, LOCK=NONE` | 原表上重建，允许并发 DML | 加索引、加列（8.0 前位置）等；注意磁盘翻倍 |
| gh-ost / pt-online-schema-change | 影子表 + 增量同步 + 原子改名 | 大表、INSTANT/INPLACE 不支持的变更；**工程未落地**，本项目用 docker 起一套即可 |

操作纪律：任何 DDL 前先 `SHOW PROCESSLIST` 确认无长事务 → 低峰执行 → `ALTER` 语句显式写 `ALGORITHM=INPLACE, LOCK=NONE`，不支持就报错而不是悄悄拷表（默认 `ALGORITHM=DEFAULT` 会静默退化成锁表拷贝）。

## 七、动手练习

1. 估算：按你真实跑的对话量，`chat_message` 多久达到 2000 万行？把结论写进团队 README 作为治理时点依据。
2. 用 §四三段 SQL 在本地库对 `chat_message` 做一次 10 行级的归档演练，故意在 DELETE 前中断，验证"两边都有"窗口的存在。
3. `EXPLAIN SELECT * FROM chat_message WHERE create_time >= '2026-09-01'` 对比分区前后的扫描量（分区裁剪看 EXPLAIN 的 partitions 列）。

## 八、面试要点

- "表多大要分库分表？" → 先反问增长模型与访问模式：多数场景归档/分区更便宜；给出本项目 chat_message 归档 vs user 表分片的不同选择及理由，比背"2000 万行"高分。
- "归档任务怎么保证不丢不重？" → 主键范围切批 + 先插后删分事务 + COUNT 核对 + 抽样比对 + 首月人工复核。
- "分区表有什么坑？" → 唯一键必须含分区键、不带分区键全分区扫、`DROP PARTITION` 秒删是核心收益。
- "大表加字段怎么做？" → 先 INSTANT、不行 INPLACE + LOCK=NONE、再不行 gh-ost；执行前查长事务防 MDL 雪崩。

## 学习检查清单

- [ ] 能按增长模型算出本项目的治理时点，而不是背阈值
- [ ] 能独立写出"切批搬 + 分事务删 + 核对"的归档三段式
- [ ] 能说出归档/分区/分库分表的决策口诀
- [ ] 知道 MDL 雪崩的成因与 DDL 三板斧的适用边界
- [ ] 知道本项目对话归档的 MongoDB 落点（→ 04-中间件/10）
