# 09-备份恢复与数据安全

> 全库会话记录被误删、`DROP TABLE` 手滑、磁盘损坏、机房失火——这些事故的共同点：**主从复制救不了你**。复制是"实时同步"，误删 0.1 秒内同步到两个从库；冗余解决的是"硬件坏了数据还在"，不是"数据被写坏了怎么回来"。
> 本篇讲清**冗余 ≠ 备份**，给出本项目 1 主 2 从拓扑下的备份策略，并补一次完整的"误删恢复演练"。
> 锚定文件：`deploy/docker-compose/docker-compose-master-slave.yml`、`deploy/mysql/mysql.cnf`（`expire-logs-days=7`）、`deploy/mysql/slave.cnf`（**发现一处复制过滤缺口，§四**）。
> 前置阅读：[05-MySQL锁机制与主从复制读写分离](05-MySQL锁机制与主从复制读写分离.md)（复制原理与部署）、[06-事务进阶-MVCC实现与三大日志](06-事务进阶-MVCC实现与三大日志.md)（binlog 角色）。
> 边界声明：主从切换/仲裁/脑裂见 [13-稳定性工程/04-高可用架构模式](../13-稳定性工程/04-高可用架构模式.md)；本篇不重复。

---

## 一、核心认知：为什么 1 主 2 从还要备份

| 事故 | 从库能救吗 | 什么能救 |
|---|---|---|
| 主库磁盘损坏 | ✅ 提升从库（流程见 13-稳定性工程/04） | 冗余即可 |
| `UPDATE` 漏 WHERE，10 万行被改错 | ❌ 已同步 | 备份 + binlog 恢复（§三） |
| `DROP TABLE` / 库被删 | ❌ 已同步 | 备份 + binlog 恢复，或延时从库（§五） |
| 勒索加密 / 恶意删除（有权限的人） | ❌ 全拓扑一起坏 | **离线异地备份**（备份能被同一个账号删掉 = 没有备份） |

记忆框架：**复制保"故障不丢"，备份保"错误可撤"**。RTO/RPO 语言：从库切换 RPO≈0 但撤不了逻辑错误；备份恢复 RPO = 两次备份间隔 + binlog 完整度，本项目 `expire-logs-days=7` 决定 **binlog 可重放窗口只有 7 天**——备份频率必须 ≤ 7 天，否则窗口断档。

## 二、备份工具与类型

| 维度 | 选项 | 对比 |
|---|---|---|
| 物理 vs 逻辑 | xtrabackup（物理拷文件） vs mysqldump（逻辑导 SQL） | 物理快（TB 级小时级）、恢复也快；逻辑慢但跨版本/跨库可读，小库（本项目 GB 级）两者都行 |
| 一致性 | `--single-transaction`（InnoDB MVCC 快照，不锁表） vs `--lock-all-tables` | 业务库**必加** `--single-transaction`（原理正是 06 篇的 ReadView！） |
| 增量 | binlog 即增量（配合全备重放） vs xtrabackup --incremental | 前者是 MySQL 原生免费方案；后者大库专用 |

本项目库均 < 几 GB，**mysqldump 全量 + binlog 增量**就是合理方案（xtrabackup 标注"工程未落地"，量级到了再上）：

```bash
# 每日凌晨全量（在从库上跑更好，见 §四）
mysqldump -h127.0.0.1 -uroot -p \
  --single-transaction \
  --master-data=2 \
  --routines --triggers --events \
  --databases user_db chat_db ai_customer_service product_db knowledge_db \
  > backup/full_$(date +%F).sql
# --master-data=2：把当时 binlog 位点写进注释 → 恢复时知道从哪个 binlog 开始重放
# --databases：显式列出库；不加会连 nacos_config/sys 库一起备（看策略，见 §四）
```

## 三、恢复三板斧：全备 + binlog 点恢复

误删恢复的标准流程（**先止血再恢复**：立即停应用写入/把流量切走，防止新写入污染恢复现场）：

```bash
# 0. 确认误删时刻 T_bad（从应用日志/binlog 找：mysqlbinlog 里搜 DROP TABLE 前的位置）
mysqlbinlog --no-defaults mysql-bin.000012 > /tmp/dump.sql   # 先导出人工定位
#   找到 "#260912 3:14:05 server id 1 ... DROP TABLE" 上方的 END_LOG_POS，例如 8823104

# 1. 重放全量备份
mysql -uroot -p < backup/full_2026-09-11.sql

# 2. 从全备位点重放 binlog，到误删语句【之前】为止
mysqlbinlog --no-defaults \
  --start-position=1569877 \
  --stop-position=8823104 \
  --databases=ai_customer_service \
  mysql-bin.000012 mysql-bin.000013 | mysql -uroot -p

# 3. 核对行数/关键单据 → 恢复制 → 评估丢掉的 (T_bad 之后) 数据怎么补
```

三个纪律：**① 恢复前把现场 binlog 拷走**（防止误操作把最后一线证据覆盖掉）；**② `--stop-position` 精确到误删语句之前**——重放库的事务也是"幂等靠不住"的场景，宁可少不可多；**③ 演练过才算有备份**——未演练的备份在事故现场的失败率超乎想象（文件损坏、字符集、版本差异都会在演练时暴露，这正是把演练做成 XXL-Job 季度任务的原因）。

## 四、本项目备份策略设计与一处真实缺口

拓扑回顾（05 篇已部署）：master（server-id=1）+ slave×2（server-id=2/3，`read-only=1`）。

**该在哪台备？** 在从库上跑 mysqldump（MVCC 快照不锁表，从库也无写压力）；但——

> 🔍 **真实缺口（盘点顺带发现）**：`deploy/mysql/slave.cnf` 的 `replicate-do-db=user_db, knowledge_db, chat_db, nacos_config` **不含订单库 `ai_customer_service` 与 `product_db`**。也就是说：两台从库里**没有订单和商品数据**——
> ① 想在从库备份订单数据 → 没得备；② 05 篇规划的"读走从库"对订单查询同样不生效；③ 若是有意为之（从库只服务用户/知识/对话），也应注释写明，否则就是事故伏笔。

修法二选一：`replicate-do-db` 补上 `ai_customer_service, product_db`（重建复制：`STOP SLAVE` → 改配置 → 重新 `CHANGE MASTER` 全量追平），或删除过滤项复制全库（学习环境推荐，简单）。改完用 `SHOW REPLICA STATUS\G` 核对 `Replicate_Do_DB`，并在主库各库各插一行验证。

完整策略表（可直接照抄执行）：

| 对象 | 频率 | 内容 | 保留 |
|---|---|---|---|
| 全量 mysqldump | 每日 02:00（从库） | 全业务库 | 30 天（滚动） |
| binlog | 实时（本就有）+ 每日拷贝到备份盘 | 全部 | `expire-logs-days=7` 本地，备份盘 ≥ 30 天 |
| 恢复演练 | 季度 | 随机挑一个库做"删表→恢复" | 演练记录归档 |
| 离线副本 | 每周 | 备份文件传 MinIO 另一桶/异地 | 按合规要求 |

最后一行的"离线"就是 §一表里的勒索防线：**备份存储的账号与数据库账号不同源**。

## 五、延时从库：防误删的最后防线

```sql
CHANGE REPLICATION SOURCE TO SOURCE_DELAY=3600;  -- 8.0 语法，从库延迟 1 小时回放
```

延时从库的本质：**把 RPO 从"0 但撤不了错"换成"最多 1 小时，且逻辑错误可撤"**——误删发生后 1 小时内，`STOP SLAVE`、把从库推到误删语句前、导出被删数据、放行追平。它不替代备份，是"备份恢复要小时级"与"实时副本秒级同错"之间的折中。本项目 1 主 2 从中拿一台做 `SOURCE_DELAY=3600` 是零成本高收益的改造（注意：延时从库不可当读库用，数据是旧的）。

## 六、数据安全：备份之外的三件事

1. **最小权限**：应用账号不给 `DROP`；备份账号只读 + `LOCK TABLES`；Canal 账号先例见 [04-中间件/11 §六](../04-中间件/11-CanalCDC商品索引同步.md)。`GRANT SELECT ON aics_order.* TO 'app'@'%'`——按库授权而不是 `*.*`。
2. **备份文件本身要脱敏/加密**：备份里是全量明文（含 `password` 密文列、手机号）。离线副本加密存储；用于开发调试的备份先脱敏（脱敏算法见 [05-AI集成 的 PiiMasker 及 18 模块](../18-银行金融开发/README.md)）。
3. **删除要有留痕**：物理删除走软删/归档（08 篇）优先；确需物理删除的表，删除任务记录"谁、何时、删了多少行、依据"，对账场景见 [18-银行金融开发/09](../18-银行金融开发/09-金融级一致性与对账实战.md)。

## 七、动手练习

1. 在本地对 `chat_db` 做全备 → 删 `chat_feedback` 表 → 按 §三流程完整恢复，记录各步耗时。
2. 给 slave 容器加 `SOURCE_DELAY=60`（1 分钟版），主库建表后观察 `SHOW REPLICA STATUS\G` 的延迟与追平过程。
3. 检查 `replicate-do-db` 缺口修复后的结果：主库往 `ai_customer_service.orders` 插一行，两从库分别验证可见。

## 八、面试要点

- "有主从了还要备份吗？" → 先亮"复制保故障、备份保错误"框架，再给 `UPDATE 漏 WHERE` 反例——这是本篇最常被问的问题。
- "误删了怎么恢复？" → 止血 → 全备回放 → binlog `--stop-position` 重放 → 核对 → 补数，五步说全；提到"备份必须演练过"是加分项。
- "`--single-transaction` 为什么不锁表？" → InnoDB 一致性快照（MVCC），回扣 06 篇 ReadView。
- "RPO/RTO 怎么权衡？" → 实时副本 RPO≈0 撤不了逻辑错；延时从库 RPO=延迟量可撤错；备份 RPO=间隔。按业务分级配置。

## 学习检查清单

- [ ] 能向新人讲清"冗余 ≠ 备份"并各举一个事故
- [ ] 独立完成一次"全备 + 点恢复"演练并记录耗时
- [ ] 知道本项目 binlog 窗口 7 天对备份频率的约束
- [ ] 已确认/修复 `slave.cnf` 复制过滤不含订单库的缺口
- [ ] 能说出延时从库解决的问题与代价
- [ ] 备份文件有离线副本且账号与数据库不同源
