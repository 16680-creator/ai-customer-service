# 10-数据库面试专练

> 本模块 01~09 篇的**验收出口**。写法对齐 [05-AI集成/面试题](../05-AI集成/面试题/04-向量数据库与Embedding.md) 的模式：先给四层答题模板，再按主题分组刷高频题（每题一句话答案 + 展开锚点），然后做三条追问链演练，最后一张考前速览卡。
> 与其他面试资料的关系：[12-性能工程/05 §八](../12-性能工程/05-SQL调优与执行计划.md) 覆盖 SQL 调优向的问答（EXPLAIN/深分页/join），本篇覆盖**原理与架构向**（MVCC/日志/锁/备份/治理），银行方向补充见 [18-银行金融开发/11-银行岗面试题库与简历映射](../18-银行金融开发/11-银行岗面试题库与简历映射.md)。

---

## 一、四层答题模板（先架构、再机制、后本项目、兜底边界）

被问任何 MySQL 原理题，按这四层组织，信息密度最高：

1. **一句话定性**（是什么/解决什么）：如"MVCC 是 InnoDB 用多版本让读写不互相阻塞的机制"。
2. **机制展开**（怎么实现）：版本链 + ReadView，RC/RR 只差拍照时机。
3. **落到本项目**（证明真做过）：支付回调并发场景里客服查询走快照读不被阻塞；`mysql.cnf` 双 1。
4. **边界与代价**（体现工程判断）：快照读解决不了当前读幻读（临键锁上）；长事务让版本链膨胀 undo。

## 二、高频题分组刷（判断 / 权衡 / 排查 / 设计）

### 2.1 索引组

| 题 | 一句话答案 | 展开锚点 |
|---|---|---|
| 为什么用 B+ 树不用 B 树/哈希/红黑树 | B+ 树矮胖（叶子链表利于范围扫描），哈希不支持范围与排序，红黑树太高 IO 多 | [01 §四](01-MySQL核心知识.md) |
| 联合索引 (a,b,c) 哪些条件能走 | a / a,b / a,b,c 走；b、c 单独不走（最左前缀）；a>1 and b=1 只有 a 走 | [01 §4.3](01-MySQL核心知识.md) |
| 索引失效最常见几个 | 隐式转换、函数包裹列、前导 %、OR 混合、不满足最左 | [12-性能工程/05 §三](../12-性能工程/05-SQL调优与执行计划.md) |
| 唯一索引和普通索引怎么选 | 查询性能几乎一样（change buffer）；选唯一索引是用**约束换正确性**（uk_order_no 挡并发重复） | [05 §3.1](05-MySQL锁机制与主从复制读写分离.md)、[07 §三](07-建表规范与字段类型选型.md) |
| `idx_status` 这类低区分度索引有用吗 | 单独没用，组合才有意义：(user_id, status) | [07 §六 审视#2](07-建表规范与字段类型选型.md) |

### 2.2 事务与锁组

| 题 | 一句话答案 | 展开锚点 |
|---|---|---|
| RR 怎么解决幻读 | 快照读靠 MVCC，当前读靠临键锁；两层分开答 | [06 §三](06-事务进阶-MVCC实现与三大日志.md)、[05 §二](05-MySQL锁机制与主从复制读写分离.md) |
| RC 和 RR 的实现差异 | 只差 ReadView 生成时机：每语句一次 vs 事务首次一次 | [06 §三](06-事务进阶-MVCC实现与三大日志.md) |
| 两个事务同时扣库存会怎样 | 行锁串行化写；or 乐观方案：`UPDATE ... SET stock=stock-1 WHERE id=? AND stock>=1` 影响行数判成败 | [05 §3.2](05-MySQL锁机制与主从复制读写分离.md) |
| 死锁了怎么排查 | `SHOW ENGINE INNODB STATUS` 看 LATEST DETECTED DEADLOCK，定位两条 SQL 与持锁/等锁资源 | [05 §四](05-MySQL锁机制与主从复制读写分离.md) |
| 更新丢失用数据库还是 Redis 锁 | 单行扣减用数据库行锁/条件更新最简单可靠；跨资源竞争才上 Redisson | [04-中间件/07](../04-中间件/07-Redisson分布式锁.md) |
| 你们分布式事务怎么做的 | Seata AT：一阶段本地事务 + undo_log 镜像，二阶段异步删/反向补偿；再讲 XID 传播 | [02-Spring微服务/06、07](../02-Spring微服务/06-Seata分布式事务AT模式.md) |

### 2.3 日志与恢复组

| 题 | 一句话答案 | 展开锚点 |
|---|---|---|
| redo/undo/binlog 各干什么 | redo 保崩溃不丢（物理）、undo 保回滚+MVCC（逻辑旧值）、binlog 保复制与归档（Server 层逻辑） | [06 §四](06-事务进阶-MVCC实现与三大日志.md) |
| 为什么 redo 和 binlog 之间要两阶段提交 | 两层日志无法原子写，用 prepare/commit + binlog 作裁判保证主库恢复结果与从库一致 | [06 §四](06-事务进阶-MVCC实现与三大日志.md) |
| 双 1 是什么，能不能关 | trx_commit=1 + sync_binlog=1；关了省 fsync 但崩溃丢秒级数据，含支付的库不动 | [06 §五](06-事务进阶-MVCC实现与三大日志.md) |
| 有主从了为什么还要备份 | 复制保"故障不丢"，备份保"错误可撤"；漏 WHERE 的 UPDATE 会同步到所有从库 | [09 §一](09-备份恢复与数据安全.md) |
| 误删了怎么恢复 | 止血 → 全备回放 → binlog stop-position 重放 → 核对 → 补数 | [09 §三](09-备份恢复与数据安全.md) |

### 2.4 架构与治理组

| 题 | 一句话答案 | 展开锚点 |
|---|---|---|
| 分库分表怎么选分片键 | 按最高频查询路径（user 表按 user_id 哈希）；让 80% 查询单片命中，跨片查询走绑定表/广播 | [03 §一](03-ShardingSphere用户表分库分表实战.md) |
| 分库分表后自增 ID 怎么办 | 不用自增，雪花 ID（时间戳+机器+序列） | [04](04-分布式ID规范.md) |
| 表多大要治理 | 先算增长模型再选路：归档最便宜、分区按时间、分库分表最后 | [08 §二](08-大表治理-冷热分离与数据归档.md) |
| 大表加字段怎么做 | INSTANT → INPLACE+LOCK=NONE → gh-ost；先查长事务防 MDL 雪崩 | [08 §六](08-大表治理-冷热分离与数据归档.md) |
| 读写分离的主从延迟怎么办 | 强一致读走主库、写后立读会话路由、半同步缩小窗口；先量化（Seconds_Behind_Master） | [05 §七](05-MySQL锁机制与主从复制读写分离.md) |

### 2.5 ORM 组（MyBatis-Plus）

| 题 | 一句话答案 | 展开锚点 |
|---|---|---|
| Wrapper 和手写 SQL 怎么选 | 简单动态条件 Wrapper，复杂统计/多表 JOIN 手写 XML——别用 Wrapper 拼 JOIN | [02 §四](02-MyBatisPlus实战.md) |
| 分页插件原理 | 拦截器改写 SQL 加 LIMIT + 先发 COUNT；深分页要换游标/延迟关联 | [02 §五](02-MyBatisPlus实战.md)、[12-性能工程/05 §五](../12-性能工程/05-SQL调优与执行计划.md) |
| 逻辑删除的坑 | 唯一键与 deleted 冲突、`SELECT *` 要过滤、索引设计要含 deleted——先想清楚选没选物理删除 | [07 §二](07-建表规范与字段类型选型.md) |
| 自动填充是干什么的 | MetaObjectHandler 在插入/更新时统一填 create_time/update_time，兜底数据库 `DEFAULT` | [02 §七](02-MyBatisPlus实战.md) |

## 三、追问链演练（面试官的第二刀最致命）

### 链 1："你说你们用了 Seata AT" → 三连追问

1. **undo_log 表里存的是什么？** → SQL 前后镜像（rollback_info LONGBLOB），回滚时生成反向补偿 SQL。（[02/06](../02-Spring微服务/06-Seata分布式事务AT模式.md)）
2. **它和 InnoDB 的 undo log 什么关系？** → 只是同名。InnoDB undo 是引擎机制（回滚+MVCC），Seata undo_log 是业务表。**答混直接出局。**（[06 §二](06-事务进阶-MVCC实现与三大日志.md)）
3. **全局提交时镜像表谁来删？会不会堆积？** → 二阶段异步删；异常残留靠巡检 SQL 发现（08 §四给出巡检语句）。

### 链 2："讲讲 MVCC" → 三连追问

1. **ReadView 里有哪几个字段？** → m_ids / min / max / creator，逐个说含义。（[06 §三](06-事务进阶-MVCC实现与三大日志.md)）
2. **max_trx_id 是最大活跃事务 ID 吗？** → 不是，是"下一个将分配的 ID"——答错说明没理解，只会背图。
3. **本项目的隔离级别是什么，改过吗？** → MySQL 8.0 默认 RR；能说出"改 RC 省间隙锁"的取舍更好（电商高并发常见选择，但要接受不可重复读）。

### 链 3："数据库怎么保证不丢数据" → 三连追问

1. **先反问：丢的哪种？** 硬件坏 vs 逻辑误删——分开答，这一步就赢过多数人。
2. **硬件坏** → redo 双 1 + 主从冗余 + 故障切换（13/04）；**逻辑误删** → 备份 + binlog 点恢复 / 延时从库（09）。
3. **binlog 只留 7 天，从库断了 8 天怎么办？** → 只能全量重建从库（拉新全备重新追）——所以备份频率 ≤ 7 天、binlog 备份盘留 30 天。（[09 §四](09-备份恢复与数据安全.md)）

## 四、考前 30 分钟速览卡

```
索引       B+树矮胖 | 最左前缀 | 失效五宗罪 | 低区分度进组合索引 | 唯一键=并发正确性
事务/锁    快照读=MVCC | 当前读=锁 | RC每句拍 ReadView / RR 只拍一次 | 行锁锁的是索引项
日志       redo物理引擎层 crash-safe | undo旧值回滚+MVCC | binlog逻辑Server层复制
两阶段     redo prepare → binlog → redo commit | binlog 当裁判 | 双1=不丢已提交
恢复       全备 + binlog stop-position | 演练过才算有备份 | 离线副本防勒索
复制       binlog dump → IO thread → relay log → SQL thread | 延迟量化看 Seconds_Behind_Master
治理       增长模型先行 | 归档<分区<分库分表 | DDL: INSTANT→INPLACE→gh-ost | MDL 雪崩
本项目     user 分片雪花 / order 单库自增+order_no | 双1 | 慢日志2s | ROW+FULL | 1主2从
```

## 五、自测记录

每轮复习后在此打分（1~5），低于 3 的回对应锚点重读：

| 主题 | 第一轮 | 第二轮 |
|---|---|---|
| MVCC 手算 | | |
| 两阶段提交顺序 | | |
| 锁场景五连 | | |
| 恢复流程五步 | | |
| 分库分表决策 | | |
| 本项目参数逐行 | | |
