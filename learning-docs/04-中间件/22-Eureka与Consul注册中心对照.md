# 22-Eureka 与 Consul：从零开始理解注册中心的 AP/CP 两派（认知拓展，工程未落地）

> **定位**：本仓注册中心是 **Nacos 2.3**（[02-Spring微服务/03-Nacos注册与配置中心](../02-Spring微服务/03-Nacos注册与配置中心.md)），同时 K8s 侧还有一层**原生服务发现**（Service/DNS，[16-云原生与GitOps/04-K8s网络模型](../16-云原生与GitOps/04-K8s网络模型与Service.md)）。Eureka（AP 派老霸主）与 Consul（CP 派 + 多数据中心）是面试对照题的标准素材——本篇把"注册中心"这个你已经天天在用的东西，放进 **AP/CP、推/拉、雪崩保护、多数据中心**四个轴里收口。工程不引入。
> 前置阅读：[02-Spring微服务/03](../02-Spring微服务/03-Nacos注册与配置中心.md)（必读）、[04-中间件/16-ZooKeeper与etcd](16-ZooKeeper与etcd分布式协调.md)（CP 共识机制）、[16-云原生与GitOps/04](../16-云原生与GitOps/04-K8s网络模型与Service.md)。

---

## ⚡ 30 秒速记卡

```text
① 注册中心三件套：注册(心跳续约) / 发现(拉取+缓存) / 剔除(超时下线)——三家都一样，差别在"故障时保什么"
② Eureka = AP 派：对等复制、客户端缓存、宁可给你"可能过期的名单"也不拒绝服务；
   15 分钟丢 85% 心跳 → 自我保护模式：一个都不剔（宁滥勿缺）
③ Consul = CP 派：Raft 强一致写 + agent 本地健康检查 + DNS 接口 + 多数据中心(WAN gossip)
④ Nacos 一家通吃两派：临时实例 Distro(AP 异步复制) / 持久实例 Raft(CP)——你用的就是它
⑤ 云原生变量：K8s Service+EndpointSlice 本身就是注册中心——同集群内流量可能根本不经过 Eureka/Nacos
```

---

## 一、先用你已经懂的 Nacos 定坐标

回顾你天天在用的流程：服务启动 → 注册到 Nacos → 心跳/长连接保活 → 消费者拉取并**本地缓存**实例列表 → 实例下线被剔除。任何注册中心都是这三件套，**面试的区分点只有一个：网络分区/大规模故障时，它选择"可用性"还是"一致性"**。

```text
分区发生：Nacos 集群与一半实例失联
 ├─ AP 派：名单照发（哪怕略旧）→ 调用可能打到已死实例 → 靠调用方重试/熔断兜底 → 服务总体可用
 └─ CP 派：写入要先过半数确认 → 分区期间可能拒绝新注册/更新 → 名单更"真"，但可用性下降
```

---

## 二、Eureka：AP 派的教科书

| 机制 | 说明 | 记忆点 |
|---|---|---|
| **对等复制（peer-to-peer）** | 无 Leader：每个节点都是平等的 Server，注册信息异步复制给同伴 | 简单、容忍分区；代价：节点间名单短暂不一致 |
| **客户端缓存** | Consumer 拉取后本地缓存 + 定时刷新；即使 Server 全挂，**存量调用照常** | "有缓存的 AP"——本仓 Nacos 客户端同样如此 |
| **自我保护模式** | 15 分钟内心跳丢失比例 ≥85%（网络抖动而非实例真死）→ **停止剔除任何实例** | 宁滥勿缺；代价：真死实例也可能留在名单里，靠调用方超时兜底 |
| 心跳续约 | 默认 30s 心跳 / 90s 判死 | 短平快，无长连接 |
| 生态位 | Spring Cloud Netflix 老全家桶成员；**1.x 仍维护，Netflix 已停进 2.x** | 老 Spring Cloud 项目的标配 |

> 一句话画像：**Eureka 把"可用"刻进骨子里——它的所有"缺点"（名单可能旧、可能不剔死实例）都是为可用性付出的自觉代价。**

---

## 三、Consul：CP 派 + 多数据中心 + 一堆附加能力

| 机制 | 说明 | 记忆点 |
|---|---|---|
| **Raft（CP）** | 写操作（注册/注销）要过 Leader + 过半确认 | 分区期间部分写不可用——[04-16](16-ZooKeeper与etcd分布式协调.md) 的共识知识直接复用 |
| **agent 架构** | 每台机/每 Pod 跑一个 agent：注册、**本地健康检查**（HTTP/TCP/脚本/TTL）都由 agent 做 | 健康检查种类远比 Eureka 丰富（不只是心跳，还能"探活业务语义"） |
| **多数据中心** | 原生支持：数据中心间 WAN gossip 联邦，跨 DC 查询 | Eureka/Nacos 的多机房要自己拼 |
| **DNS 接口** | `service.consul` 域名直接解析服务实例 | 老应用零改造接入 |
| **Connect（服务网格）** | 内建 mTLS sidecar 能力 | 与 Istio 的关系见 [16-05-ServiceMesh与Istio边界](../16-云原生与GitOps/05-ServiceMesh与Istio边界.md) |
| 附加 | KV 存储（配置中心能力）、Watch | 与 etcd 定位部分重叠（[04-16](16-ZooKeeper与etcd分布式协调.md)） |

> 一句话画像：**Consul 是"瑞士军刀"——注册+配置+网格+多 DC 全都带，代价是组件与概念更多，国内落地率低于 Nacos。**

---

## 四、总对照表（把三家的差异钉在四个轴上）

| 维度 | Eureka | Consul | Nacos 2.x（本仓） |
|---|---|---|---|
| 一致性 | **AP**（对等复制） | **CP**（Raft） | 临时实例 AP（Distro）+ 持久实例 CP（Raft） |
| 存活检测 | 客户端心跳（30s） | agent 健康检查（HTTP/TCP/脚本/TTL） | 心跳（1.x）→ **gRPC 长连接**（2.x，连接断即判死，秒级） |
| 变更感知 | 客户端定时拉（30s） | 阻塞查询（blocking query）/ watch | **服务端推送**（gRPC） |
| 故障自保 | **自我保护模式**（不剔除） | 无（CP 决定生死，靠健康检查说话） | 保护阈值（比例健康实例低于阈值时也不剔除） |
| 多数据中心 | ❌（要自建分区方案） | ✅ 原生 WAN 联邦 | ❌（同机房为主，集群间需自建同步） |
| 配置中心 | ❌（老组合用 Spring Cloud Config） | ✅ KV | ✅（一体，你已在用动态刷新） |
| K8s 协同 | 弱 | 中（有 K8s sync 项目） | 中（Nacos Sync / MCP） |
| 适用一句话 | 存量 Netflix 栈维护 | 多 DC / 需要强一致注册 | **国内微服务一体机**（本仓选择） |

---

## 五、云原生变量：K8s Service 也是注册中心（容易被追问的最后一块拼图）

你的服务跑在 K8s 里时其实存在**两层发现**：

```text
集群内：ai-cs-chat 访问 ai-cs-order
   → Service 名解析（CoreDNS）→ EndpointSlice 挑一个 Pod IP
   → 这条路径没经过 Nacos！（kube-proxy/ipvs 负载均衡）

集群间/混合部署：chat（集群外）→ 网关 → 内部服务
   → 走 Nacos 注册发现（你的现状）
```

**由此推出面试金句**：
> "容器化程度越高，注册中心的**发现职能**越可能被 K8s 原生机制吸收（Service/EndpointSlice + 就绪探针），
> 但注册中心的**治理职能**（元数据、灰度权重、优雅上下线联动配置）仍有价值——Nacos 2.x 的 gRPC 推送与保护阈值解决的是'发现质量'问题。"

这也解释了 [16-04](../16-云原生与GitOps/04-K8s网络模型与Service.md) 为什么说"K8s 里 Service 就是四层负载均衡 + 服务发现"。

---

## 六、面试要点总结

```text
关键词：
三件套：注册/发现/剔除 · 分区时选 AP(名单照发)还是 CP(过半写入)
Eureka：对等复制+客户端缓存+自我保护(15min/85% 心跳→不剔除) · Netflix 2.x 停更背景
Consul：Raft CP+agent 健康检查(HTTP/TCP/脚本/TTL)+多数据中心 WAN+DNS+Connect 网格
Nacos：临时=AP(Distro) 持久=CP(Raft) · 2.x gRPC 长连接推送 · 保护阈值
云原生：K8s Service/EndpointSlice 吸收"发现"职能，治理职能仍在
项目锚点：Nacos 2.3 全服务注册 + 动态刷新（02-03）· K8s Service 双层发现现状（16-04）
```

## 学习检查清单

- [ ] 能用"分区时保什么"一句话区分 AP/CP 注册中心
- [ ] 能解释 Eureka 自我保护模式的触发条件与代价
- [ ] 能列举 Consul 相比 Eureka 多出的四类能力
- [ ] 能说清 Nacos 临时/持久实例分别走哪套协议
- [ ] 能画出本仓"集群内 K8s 发现 + 集群外 Nacos 发现"的双层路径

## 下一步

- [04-中间件/16-ZooKeeper与etcd](16-ZooKeeper与etcd分布式协调.md)：CP 共识的底层实现；
- [16-云原生与GitOps/04-K8s网络模型与Service](../16-云原生与GitOps/04-K8s网络模型与Service.md)：另一层服务发现的机制细节。
