# 10-提示词版本管理与灰度发布实战

> 2026-09 补全记录：`ai-cs-chat` 的提示词注册表（`com.aics.chat.prompt`）已在跑 8 个 scenario 的版本管理 + 三种灰度策略 + 热切换 API，但此前只有 [08-AI功能与技术缺口分析](08-AI功能与技术缺口分析.md) 一篇提到过它（L86："Prompt 配置化 ⚠️ 部分实现……缺版本、灰度、回滚和效果关联"）。本篇把这套机制讲全，并对照那份缺口判语做一次"已补齐 / 仍未做"的对账。
> 前置阅读：[01-SpringAI入门](01-SpringAI入门.md)（ChatClient 基础）、[05-LLM可观测性评估与成本治理](05-LLM可观测性评估与成本治理实现文档.md)（Trace 与评估门禁）。

---

## 一、为什么提示词也需要"发版纪律"

改一行提示词 = 改一次系统行为，但它不像改代码那样有编译器、测试、Code Review 拦着。没有版本管理时，典型事故是：改坏了一句 system prompt → 意图分类全乱 → 想回滚发现**旧版本已被覆盖**。

本项目把它当配置管理做：**提示词 = YAML 里的版本化资产**，支持多版本共存、按策略放量、不重启热切换、一键回滚。核心思想一句话：**把 prompt 从代码里拿出来，当作需要灰度的"发布物"**。

## 二、注册表全景

代码落点（`ai-cs-chat/src/main/java/com/aics/chat/prompt/`）：

```text
com.aics.chat.prompt
├── PromptProperties        @ConfigurationProperties(prefix = "aics.prompt")，嵌套 Scenario/Rollout/UserIdMod/VersionConfig
├── PromptRegistry          核心：加载/校验/渲染/版本管理/热回滚（InitializingBean 启动即校验）
├── PromptRouter            灰度策略解析：weights / userId-mod / pinned
├── PromptConfig            @Configuration，声明 promptRouter / promptRegistry Bean
├── PromptRenderException   缺 scenario/版本/变量时抛出
└── controller/PromptController  管理端 API（/api/prompts/**）

资源：src/main/resources/application-prompt.yml（经 application.yml 的
     spring.config.import: optional:classpath:application-prompt.yml 引入）
```

已注册的 8 个 scenario：`default-system`（全局人设，含 `{{dbSchema}}`）、`intent`（意图分类，输出 JSON）、`rewrite`（查询改写）、`rag`、`summary`、`judge`（评估裁判）、`chart`、`vision`。一个真实条目长这样（`rewrite`）：

```yaml
aics:
  prompt:
    enabled: true
    scenarios:
      rewrite:
        activeVersion: v1
        rollout:
          strategy: pinned        # pinned | weights | user_id_mod
          pinned: v1
        versions:
          v1:
            system: "你是检索查询优化专家。只输出指定的 JSON 格式，不要输出多余内容。"
            user: "请把下面的用户问题改写成 {{count}} 个子查询，用于检索知识库。\n保留原意，覆盖不同表述角度。\n用户问题：{{question}}"
```

三个结构要点：**scenario → rollout（怎么放量）→ versions（有哪些版本）** 三层；`{{变量}}` 占位符渲染期替换；YAML 顶部注释直接写明运维流程（"修改 activeVersion / rollout / versions 后调用 POST /api/prompts/{scenario}/active 即可热切换，无需发版"）。

## 三、渲染链路：scenario + userId + 变量 → RenderedPrompt

核心 API（`PromptRegistry`）：

```java
public RenderedPrompt render(String scenario, String userId, Map<String, Object> variables, FallbackSupplier fallback)
public RenderedPrompt renderOrFallback(String scenario, String userId, Map<String, Object> variables, FallbackSupplier fallback)
public void setActiveVersion(String scenario, String version)   // 热切换/热回滚
public Map<String, String> activeSnapshot()                     // 全量 scenario 的当前版本
```

`render` 内部做的事：按 rollout 策略选版本（第四节）→ 取 VersionConfig → 替换 `{{变量}}` → 缺变量抛 `PromptRenderException`。`renderOrFallback` 的区别是渲染失败时退回调用方提供的兜底 prompt——AI 主链路不因配置问题而 500。

## 四、三种灰度策略（PromptRouter.resolveVersion）

| 策略 | 配置 | 语义 | 适用 |
|---|---|---|---|
| `pinned`（默认） | `rollout.pinned: v1` | 固定用某版本 | 日常；发布前把新版本"钉住"预览 |
| `weights` | `rollout.weights: {v1: 0.9, v2: 0.1}` | 按权重随机抽样 | 无用户态的内部链路（改写/摘要/judge） |
| `user_id_mod` | `rollout.userIdMod: {version: v2, mod: 10, remainder: 3}` | `floorMod(userId, mod) == remainder` 的用户进新版本 | **按用户分桶**：同一用户永远同一版本，体验一致、可对比 |

两个实现细节值得学：

1. **确定性注入**：`weights` 用注入的 `java.util.function.DoubleSupplier`（默认 `ThreadLocalRandom`），测试里喂 0.95/0.1 就能精确打到 v2/v1——`PromptConfigTest.router_weights_samplesBothVersions` 就是这么写的（可测性优先于"直接在方法里 random"）。
2. **三级回退链**：选中的版本不存在 → 退 `activeVersion` → 再退 `versions` 的第一个 key。配置写错最多退化，不会让链路瘫痪。

## 五、热切换 API：不重启换提示词

`PromptController`（`/api/prompts`）：

| 端点 | 方法 | 返回 |
|---|---|---|
| `/api/prompts` | GET | `Result<Map<String,String>>`：所有 scenario 的 activeVersion 快照 |
| `/api/prompts/{scenario}` | GET | `{activeVersion, versions: {vX: {systemLength, userLength}}}` —— **只给长度不给正文**，防止提示词全文从管理接口泄漏 |
| `/api/prompts/{scenario}/active?version=v2` | POST | 切换 activeVersion，返回 `{scenario, activeVersion}` |

**诚实说**：热切换写的是内存 `ConcurrentHashMap<String,String> activeOverrides`，**不持久化**——服务重启后回到 YAML 里的 `activeVersion`。这可以当"变更自动随发版收敛"的特性看，也可以当缺陷看；若要真正持久化，下一站是把它写进 Nacos 配置（配合 [02-Spring微服务/03-Nacos注册与配置中心](../../02-Spring微服务/03-Nacos注册与配置中心.md) 的 `@ConfigurationProperties` 动态刷新），"内存热切换"是通往那条路的第一步。

## 六、消费链路：8 个 scenario 都在哪被用

| 调用点 | scenario | 说明 |
|---|---|---|
| `SpringAiConfig.chatClientCustomizer` | `default-system` | 启动装配时渲染（含 `{{dbSchema}}`）注入 `ChatClient.defaultSystem` |
| `ChatServiceImpl` | `rag` / `summary` | RAG 回答与会话摘要（`resilientAiService.callRagChat/callSummary`） |
| `QueryRewriteService.rewrite` | `rewrite` | 查询改写（经 `routedChatClientFactory.chatClientFor(REWRITE)`） |
| `IntentClassifierService` / `LlmJudgeService` / `ChartAnswerGenerator` / `VisionModelClient` | `intent` / `judge` / `chart` / `vision` | 各 AI 子链路 |

所有调用点都会写 `TraceContext.setPrompt(scenario, version)`——**每次 LLM 调用的 trace 都带着"用了哪个提示词版本"**，这是第七节效果关联的基础设施（详见 [05-LLM可观测性评估与成本治理](05-LLM可观测性评估与成本治理实现文档.md)）。

## 七、测试与发布流程

`PromptConfigTest` 六个用例构成发布安全网：`render_replacesPlaceholders`（占位符替换）、`render_missingVariable_throws`、`router_userIdMod_stableBucketing`（同用户稳定分桶）、`router_weights_samplesBothVersions`（注入 DoubleSupplier 驱动两个版本）、`hotRollback_switchesActiveVersion`（切换后渲染 v2，再切回 v1）、`applicationPromptYaml_loadsWithSpringYamlLoader`（用 `YamlPropertySourceLoader` 真加载 YAML 校验字段）。

推荐的发布流程（把版本管理串起来）：

```text
1. YAML 里给目标 scenario 加 v2 → 改 rollout 为 user_id_mod（remainder 只放 1/10 用户）
2. POST /api/prompts/{scenario}/active?version=v2 热切换（或重启生效）
3. 观测：TraceView 按 prompt 版本过滤，比对回答质量/成本
4. 好评 → 全量（remainder 调大或改 activeVersion + pinned）；翻车 → POST 切回 v1（秒级回滚）
5. 稳定一周后删 v1，合并回 YAML
```

## 八、与缺口判语的对账

[08-AI功能与技术缺口分析](08-AI功能与技术缺口分析.md) 当时的判语是"部分实现……缺版本、灰度、回滚和效果关联"。逐项对账：

| 判语中的缺口 | 现状 |
|---|---|
| 缺版本 | ✅ 已补：8 scenario × 多版本共存（versions map） |
| 缺灰度 | ✅ 已补：pinned / weights / user_id_mod 三策略 + 三级回退 |
| 缺回滚 | ✅ 已补：setActiveVersion 热回滚（内存级，重启收敛到 YAML） |
| 缺效果关联 | ⚠️ 半补：Trace 已带 scenario+version（可人工按版本过滤比对），但**评估指标按 prompt 版本自动聚合的报表未做**——这是下一个增量 |
| "部分 Prompt 仍位于代码中" | ⚠️ 部分：8 个核心 scenario 已配置化，零散的局部拼接语句仍在代码里 |

## 九、动手练习

1. 给 `rag` 加一个 v2（改一句 system），配置 `user_id_mod {mod: 10, remainder: 0}`，用 `PromptConfigTest` 的套路写一个分桶断言测试。
2. 用 `DoubleSupplier` 注入写一个 weights 策略的单测：权重 v1=0.99、v2=0.01，喂 0.995 与 0.005，断言各打到对应版本。
3. 思考题：如果要把 activeVersion 持久化到 Nacos，`setActiveVersion` 该改成什么？（提示：热切换 API 变"写 Nacos + 监听刷新"，内存 overrides 只作 Nacos 不可用时的降级。）

## 十、面试要点总结

> 本项目把提示词当版本化资产管理：YAML 注册表（scenario→rollout→versions 三层）+ 启动校验 + {{变量}}渲染，8 个核心 scenario 全覆盖；灰度提供 pinned/weights/user_id_mod 三策略（随机抽样用注入的 DoubleSupplier 保证可测，用户分桶保证同一用户版本稳定），选中版本缺失时按 activeVersion→首个 key 三级回退；管理 API 支持快照查询（只暴露版本号与长度防泄漏）与内存级热切换/秒级回滚，每次 LLM 调用把 scenario+version 写入 Trace 供效果比对。诚实边界：热切换不持久化（重启收敛 YAML），评估指标按版本自动聚合未做。

```text
关键词：prompt = 发布物 · scenario/rollout/versions 三层 · user_id_mod 分桶稳定
DoubleSupplier 注入 = 可测的随机 · 三级回退链 · 热切换内存 overrides（重启收敛）
Trace.setPrompt(scenario, version) = 效果关联的地基
```

## 学习检查清单

- [ ] 能画出 YAML 三层结构与一次 render 的完整解析链
- [ ] 能说出三种灰度策略各自的适用场景
- [ ] 完成练习 1、2
- [ ] 能对"热切换不持久化"给出至少一个辩护和一个改进方案
