# 引用溯源实战：带出处的回答（Citation）

> 本文档讲解 RAG 回答的**引用溯源（Citation）**能力：AI 回答每条结论时，
> 附上"出自哪份文档、哪一页、相关度多少、原文片段"。
> 前置知识：[03-RAG向量检索实战.md](03-RAG向量检索实战.md)。
>
> **核心目标**：回答"有据可查"。客服场景下，用户投诉/纠纷时需要平台能证明
> "这条答复依据的是哪份政策文件"，引用溯源是 RAG 落地到生产的**合规刚需**。

---

## 一、实战背景：引用溯源的必要性

### 1. 客服场景的合规需求

- **审计追踪**：客服答复必须可回溯——用户问"为什么不能退款"，平台要能拿出《退款政策》原文；
- **责任界定**：AI 说错话时，需要快速定位是"检索错"还是"模型编造"，引用列表就是证据链；
- **信任建立**：用户看到"引用来源 3 条 + 原文片段"，会更信任 AI 的答复；
- **质量评估**：开发人员可以用 citations 的分数、命中来源做 RAG 效果量化评估。

### 2. 实现目标

```
后端：检索命中的 Document（含 metadata） ──► buildCitations() ──► List<CitationItemDTO>
前端：SSE done 事件携带 citations ──► 渲染"引用来源"卡片（标题/页码/相关度/原文预览）
```

---

## 二、DTO 设计

### 1. CitationItemDTO（单条引用）

文件：`ai-cs-chat/src/main/java/com/aics/chat/dto/CitationItemDTO.java`

```java
@Data
@Accessors(chain = true)
@NoArgsConstructor
public class CitationItemDTO {

    /** 来源文档 ID */
    private Long documentId;

    /** 文档标题 */
    private String title;

    /** 页码（PDF 分页读取时存在，metadata key 为 page_number） */
    private Integer page;

    /** 相关度分数 */
    private Double score;

    /** 引用原文片段 */
    private String content;
}
```

### 2. ChatRagResponseDTO（回答 + 引用列表）

文件：`ai-cs-chat/src/main/java/com/aics/chat/dto/ChatRagResponseDTO.java`

```java
@Data
@Accessors(chain = true)
@NoArgsConstructor
@AllArgsConstructor
public class ChatRagResponseDTO {

    /** AI 生成的回答内容 */
    private String content;

    /** 回答引用的知识库来源列表（按相关度排序） */
    private List<CitationItemDTO> citations;
}
```

> 设计要点：引用与回答**同包返回**（而非另开接口），保证前端一次拿到完整渲染数据；
> 前端可先渲染回答正文，再在回答下方展示引用卡片。

---

## 三、后端实现：buildCitations

### 1. 数据从哪来？——入库时写入的 metadata

引用所需的字段，全部来自向量化入库时打在每个分块上的 metadata：

| CitationItemDTO 字段 | metadata key | 写入位置 |
|----------------------|--------------|----------|
| documentId | `documentId` | `KnowledgeBaseService.addChunks`：`meta.put("documentId", chunk.getId())` |
| title | `title` | 同上：`meta.put("title", title)` |
| page | `page_number` | `PagePdfDocumentReader` 按页读取 PDF 时自动写入 |
| score | — | 检索时由 VectorStore 计算（`Document.getScore()`） |
| content | — | 分块原文本身（`Document.getText()`） |

### 2. 核心方法（ChatServiceImpl.buildCitations）

文件：`ai-cs-chat/src/main/java/com/aics/chat/service/impl/ChatServiceImpl.java`

```java
/**
 * 从 RAG 检索结果构建引用溯源列表。
 *
 * <p>字段来源：documentId/title 来自入库时写入的 metadata；
 * page 来自 metadata 的 {@code page_number}（PagePdfDocumentReader 写入）；
 * score 为检索相似度；content 为命中片段原文。</p>
 */
private List<CitationItemDTO> buildCitations(List<Document> docs) {
    List<CitationItemDTO> citations = new ArrayList<>(docs.size());
    for (Document doc : docs) {
        Map<String, Object> meta = doc.getMetadata();
        CitationItemDTO item = new CitationItemDTO();

        // documentId：入库时写入的是 chunk.getId() 字符串，可转 Long 时转（UUID 等非数字忽略）
        Object docId = meta.get("documentId");
        if (docId != null) {
            try {
                item.setDocumentId(Long.valueOf(String.valueOf(docId)));
            } catch (NumberFormatException ignore) {
                // 非数字 ID 不设置
            }
        }

        Object title = meta.get("title");
        if (title != null) {
            item.setTitle(String.valueOf(title));
        }

        Object page = meta.get("page_number");
        if (page instanceof Number pageNum) {
            item.setPage(pageNum.intValue());
        }

        if (doc.getScore() != null) {
            item.setScore(doc.getScore().doubleValue());
        }

        item.setContent(doc.getText());
        citations.add(item);
    }
    return citations;
}
```

**关键点解读**：

| 代码片段 | 说明 |
|----------|------|
| `meta.get("documentId")` | 检索结果自带入库时的 metadata，无需二次查库 |
| `Long.valueOf(...)` + try/catch | 兼容两种 id 体系：纯数字（Long）与 UUID（忽略） |
| `page instanceof Number` | 防御性判断：`page_number` 可能是 Integer/Double |
| `doc.getScore()` | Spring AI 检索结果自带相似度分数，直接透传 |

### 3. 同步接口接入（chatWithRag）

```java
public Result<ChatRagResponseDTO> chatWithRag(String sessionId, String message, String knowledgeBase) {
    // 1. 两阶段检索（宽召回 + Rerank）
    List<Document> docs = knowledgeBaseService.search(knowledgeBase, message, 5, 0.5);
    String context = knowledgeBaseService.buildContext(docs);

    // 2. 构建 RAG Prompt（上下文 + 问题）并调用 LLM
    String ragPrompt = """...""".formatted(...);
    String response = resilientAiService.callRagChat(ragPrompt).get();
    response = cleanResponse(response);

    // 3. 从检索结果构建引用溯源列表（documentId/title/page/score/content）
    List<CitationItemDTO> citations = buildCitations(docs);

    log.info("RAG对话完成: sessionId={}, 检索命中{}条, 引用{}条", sessionId, docs.size(), citations.size());
    return Result.success(new ChatRagResponseDTO(response, citations));
}
```

### 4. 流式接口接入（chatStreamSse：done 事件携带引用）

SSE 流式场景下，引用不能在"流式内容"里逐条推送（检索是**一次性**完成的），
正确做法是：**检索阶段先缓存 citations，等流式回答结束后，在 done 事件里一次性携带**。

```java
// ① 检索阶段：缓存引用溯源，完成事件时随 done 一起推送
final List<CitationItemDTO> citations;
if (hasKb) {
    List<Document> docs = knowledgeBaseService.search(knowledgeBase, message, 5, 0.5);
    String context = knowledgeBaseService.buildContext(docs);
    String ragPrompt = """...""".formatted(...);
    futureFlux = resilientAiService.callSseRagStream(ragPrompt);
    citations = buildCitations(docs);          // ← 先构建好
} else {
    futureFlux = resilientAiService.callSseStream(streamHistory);
    citations = List.of();
}

// ② 流式结束后：done 事件携带回答正文 + 引用溯源列表
() -> {
    String response = cleanResponse(full.toString());
    chatHistoryService.append(sessionId, "assistant", response);
    Map<String, Object> doneEvent = new HashMap<>();
    doneEvent.put("done", true);
    doneEvent.put("content", response);        // 完整回答（去思考标签）
    doneEvent.put("citations", citations);     // 引用溯源列表
    emitter.send(SseEmitter.event().data(doneEvent));
    emitter.complete();
};
```

**SSE 事件流结构**：

```
data: {"content":"根",""}                              ← 逐 token 增量
data: {"content":"据",""}
data: {"content":"退货政策，15天内可申请退款。",""}
data: {"done":true,"content":"根据退货政策，15天内可申请退款。",
       "citations":[{"documentId":1001,"title":"退货政策.pdf","page":2,"score":0.87,
                     "content":"我们支持15天无理由退货，运费由买家承担。"}]}
```

---

## 四、前端实现：SSE done 事件解析 + 引用卡片

### 1. SSE 流解析（ChatView.vue）

文件：`ai-cs-frontend/src/views/ChatView.vue`

```js
// 预置空回复气泡，流式填充
const assistant = { role: 'assistant', content: '' }
messages.value.push(assistant)

while (true) {
  const { done, value } = await reader.read()
  if (done) break
  buffer += decoder.decode(value, { stream: true })

  // 按 SSE 事件分隔（空行）逐条解析
  let sep
  while ((sep = buffer.indexOf('\n\n')) >= 0) {
    const rawEvent = buffer.slice(0, sep)
    buffer = buffer.slice(sep + 2)
    for (const line of rawEvent.split('\n')) {
      if (!line.startsWith('data:')) continue
      const data = line.slice(5).trim()
      if (!data) continue
      let obj
      try { obj = JSON.parse(data) } catch { continue }

      if (obj.content) {
        assistant.content += obj.content        // 增量拼装回答正文
        assistant.citations = assistant.citations || []
        await scrollToBottom()
      }
      if (obj.citations) {
        assistant.citations = obj.citations     // done 事件：整体覆盖为完整引用列表
      }
      if (obj.error) streamError = obj.error
      if (obj.done) {
        // done 事件：引用已在 obj.citations 中完整返回，无需额外处理
      }
    }
  }
}
```

**解析要点**：
- 用 `\n\n` 作为 SSE 事件分隔符，逐事件解析 `data:` 行；
- `obj.content` 增量拼接到 `assistant.content`（流式打字机效果）；
- `obj.citations` 是完整列表，直接**整体覆盖**（不能追加，避免重复）；
- done 事件只做收尾，回答与引用都在其中。

### 2. 引用卡片 UI

```html
<!-- 引用溯源卡片：RAG 回答有 citations 时展示 -->
<div v-if="msg.citations && msg.citations.length > 0" class="citation-cards">
  <el-divider content-position="left">
    <el-tag size="small" type="info" effect="plain">引用来源 {{ msg.citations.length }} 条</el-tag>
  </el-divider>
  <div class="citation-list">
    <el-card
      v-for="(cit, ci) in msg.citations"
      :key="ci"
      shadow="never"
      class="citation-card"
    >
      <div class="citation-header">
        <el-icon :size="14"><Document /></el-icon>
        <span class="citation-title">{{ cit.title || '未命名文档' }}</span>
        <span v-if="cit.page" class="citation-page">P.{{ cit.page }}</span>
      </div>
      <!-- 相关度进度条：分数 ≥0.7 绿 / ≥0.5 黄 / 低红 -->
      <div v-if="cit.score != null" class="citation-score">
        <span class="score-label">相关度</span>
        <el-progress
          :percentage="Math.round(cit.score * 100)"
          :stroke-width="6"
          size="small"
          :color="cit.score >= 0.7 ? '#67c23a' : cit.score >= 0.5 ? '#e6a23c' : '#f56c6c'"
        />
      </div>
      <!-- 原文片段预览（3 行截断） -->
      <div class="citation-content-preview">
        <el-text line-clamp="3" size="small" type="info">
          {{ cit.content }}
        </el-text>
      </div>
    </el-card>
  </div>
</div>
```

**UI 设计要点**：
- **标题 + 页码**：一眼定位来源文档（PDF 场景 P.2 直接翻页核对）；
- **相关度进度条**：颜色分级（≥0.7 绿 / ≥0.5 黄 / 低红），用户可自行判断可信度；
- **原文预览**：3 行截断的命中片段，点击可展开（`line-clamp`）；
- **空值兜底**：`cit.title || '未命名文档'`、`v-if="cit.page"` 防止字段缺失时 UI 崩坏。

---

## 五、验证方法

```bash
# 1. 入库带标题/页码的测试数据（PDF 会带 page_number）
curl -X POST "http://localhost:8083/rag/knowledge-base/upload" \
     -F "knowledgeBase=product-manual" \
     -F "file=@退货政策.pdf"

# 2. 同步接口验证：响应中包含 citations
curl -X POST "http://localhost:8083/chat/rag" \
     -H "Content-Type: application/json" \
     -d '{"sessionId":"s1","message":"退货政策是什么?","knowledgeBase":"product-manual"}'
# 期望响应：
# {
#   "code": 200,
#   "data": {
#     "content": "根据我们的退货政策，支持15天无理由退货，运费由买家承担。",
#     "citations": [
#       {"documentId": 1001, "title": "退货政策.pdf", "page": 2, "score": 0.87,
#        "content": "我们支持15天无理由退货，运费由买家承担。"}
#     ]
#   }
# }

# 3. 流式验证：SSE 流结束时收到 done 事件，含 content + citations 两个字段

# 4. 前端验证：RAG 对话完成后，回答下方出现"引用来源 N 条"卡片
#    - 标题、页码（PDF 场景）正确展示
#    - 相关度进度条颜色与分数匹配
#    - 原文片段 3 行截断预览
```

---

## 六、常见问题

**Q1：为什么引用从"检索结果"构建，而不是让 LLM 自己标注？**
LLM 标注来源容易编造（幻觉来源）；检索结果的 metadata 是**真实入库数据**，天然可信。
引用列表 = 检索命中的片段，回答 = 基于这些片段生成，两者天然对应。

**Q2：Rerank 后引用顺序为什么变了？**
citations 按 `buildCitations(docs)` 遍历生成，顺序即 Rerank 精排后的相关度顺序——
分数最高的引用排最前，与回答内容的主次一致。

**Q3：documentId 可能是 UUID 怎么办？**
`buildCitations` 里 `Long.valueOf` 失败会忽略（try/catch），前端此时不显示 documentId，
只显示标题/页码/内容——引用溯源不依赖 ID 也能工作。

**Q4：未命中知识库时 citations 为空？**
是的，`docs` 为空 → `citations` 为 `List.of()`，前端 `v-if` 判断不渲染卡片，
回答也会如实告知"暂无资料"，体验一致。

**Q5：引用和回答内容对不上怎么办？**
这是"检索质量"问题而非"展示"问题。对策链：检查宽召回阈值 → 检查 Rerank min-score →
检查分块粒度（过大导致片段混杂多个主题）。引用溯源的价值之一就是**让这类问题可被审计**。

---

## 七、总结

| 环节 | 实现 | 价值 |
|------|------|------|
| DTO | `CitationItemDTO`（documentId/title/page/score/content）+ `ChatRagResponseDTO` | 回答与引用同包返回 |
| 数据来源 | 入库时写入的 metadata（documentId/title/page_number）+ 检索分数 | 无需二次查库 |
| 后端 | `buildCitations()`：metadata → DTO，兼容 Long/UUID 两种 ID | 防御性转换 |
| 流式 | 检索阶段缓存 → done 事件一次性携带 | 不打断流式体验 |
| 前端 | SSE 解析 `obj.citations` 整体覆盖 + 引用卡片（标题/页码/分数条/原文预览） | 合规 + 可信 + 可审计 |

至此，RAG 回答实现了"**每句话都有出处**"，为客服场景的合规审计与用户信任打下了基础。
