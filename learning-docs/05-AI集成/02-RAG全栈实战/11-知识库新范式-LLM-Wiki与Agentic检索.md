# 知识库新范式：LLM Wiki 与 Agentic 检索

> 本文回答一个近期高频问题：**"为什么现在说 Wiki 要代替 RAG 了？"**
>
> 结论先说：**这不是替代，而是"知识组装的时机"从查询时（query-time）前移到了摄入时（compile-time）。**
> 两者面向不同规模、不同场景，选错方向比选错参数代价大得多。
>
> 前置阅读：[01-RAG检索增强生成](01-RAG检索增强生成.md) · [05-混合检索实战-ES-BM25与向量RRF融合](05-混合检索实战-ES-BM25与向量RRF融合.md) · [10-RAG全栈实战-知识点汇总](10-RAG全栈实战-知识点汇总.md)
> 关联阅读：[05-向量数据库](../05-向量数据库/README.md)（检索底座原理与运维） · [04-SpecKit实战-RAG进阶六件套](../04-规格驱动工具/04-SpecKit实战-RAG进阶六件套.md)（本项目 GraphRAG 的实验实现）

---

## 目录

1. [这个说法从哪来](#一这个说法从哪来)
2. [先分清三件事：RAG / GraphRAG / LLM Wiki](#二先分清三件事rag--graphrag--llm-wiki)
3. [LLM Wiki 到底是什么](#三llm-wiki-到底是什么)
4. [三大核心操作：ingest / query / lint](#四三大核心操作ingest--query--lint)
5. [为什么它在某些场景确实比 RAG 强](#五为什么它在某些场景确实比-rag-强)
6. [它的真正风险：幻觉固化](#六它的真正风险幻觉固化)
7. [硬边界：什么时候绝对不能上 Wiki](#七硬边界什么时候绝对不能上-wiki)
8. [正确姿势：不是替代，是分层](#八正确姿势不是替代是分层)
9. [本项目落地的最小改造路径](#九本项目落地的最小改造路径)
10. [术语速查](#十术语速查)
11. [常见误区速查](#十一常见误区速查)
12. [面试专练与追问链](#十二面试专练与追问链)
13. [动手练习](#十三动手练习)
14. [来源与更新](#十四来源与更新)

---

## 一、这个说法从哪来

### 1.1 一句话版本

2026 年 4 月，Andrej Karpathy 提出 **LLM Wiki** 方案（原文为 Gist，随后在中文技术社区被大量转载解读），
核心一句是"**用 AI 编译知识，彻底告别 RAG 重复推导**"。
标题够劲爆，于是"Wiki 代替 RAG"就成了传播口径。

但把原文读完会发现，作者自己给的边界是：
- 适合**≤100 篇文档 / ≤40 万字**的个人研究与小团队场景；
- **超过 100 万 tokens 应改回 RAG 或 GraphRAG**。

所以准确的说法是：**Wiki 是 RAG 之外的另一条路线，不是 RAG 的下一代。**

### 1.2 RAG 被吐槽的三个点（这些吐槽是真实的）

| 吐槽 | 具体表现 | 典型场景 |
|------|----------|----------|
| **无积累性** | 每次查询都从原始 chunk 重新推导一遍，知识不落地 | 同一个复杂问题今天问过、明天还得重新消耗 token 推一遍 |
| **跨文档合成弱** | 需要综合 5 份以上文档时，片段式召回容易漏 | "对比这 20 份竞品报告，总结行业格局"——RAG 只能给你最相似的几块 |
| **会话间失忆** | 检索结果是上下文，会话一结束什么都没留下 | 换 session 后 AI 完全不记得上次的架构决策 |

这三条本质上是同一件事：**RAG 优化的是"这一次回答的质量"，不生产"沉淀下来的知识资产"。**

### 1.3 除了 Wiki，还有两股力量也在喊"替代 RAG"

看到"RAG 要凉"的标题时，先判断它说的是哪一股：

| 势力 | 主张 | 真实定位 |
|------|------|----------|
| **LLM Wiki** | 把原始资料**一次编译**成结构化知识页，查询直接读页面 | 摄入时组装知识，适合中小规模、重沉淀 |
| **长上下文** | 模型上下文都 100 万 tokens 了，全塞进去不就行了 | 成本随查询次数线性放大，且"塞得进"≠"用得上"（中间信息易被忽略） |
| **Agentic 检索 / Agentic RAG** | 不预先向量化，让 Agent 拿 grep / read / 目录导航**现场找** | 不是替代 RAG，是把"固定检索管线"换成"Agent 自主检索策略" |

三股力量的共同点：**都在质疑"固定 topK 向量召回"这一条管线**。
不同点：Wiki 是把组装前移，长上下文是把检索取消，Agentic 是把策略交给模型。

> 一句话记忆：**"RAG 要凉"的标题，九成是在说"固定管线要凉"。**

---

## 二、先分清三件事：RAG / GraphRAG / LLM Wiki

用"图书馆"做类比最容易记住：

| 方案 | 图书馆比喻 | 检索对象 | 回答什么问题 |
|------|-----------|----------|-------------|
| **普通 RAG** | 全文检索机：按相似度给你抽几页影印件 | chunk + 向量 | "这段资料里怎么说？" |
| **GraphRAG** | 关系索引卡：先查"A 和 B 什么关系"，再顺着卡片找证据 | 实体 + 关系 + 证据 chunk | "A 和 B 是什么关系？链路是什么？" |
| **LLM Wiki** | 馆员已经写好的主题词条：直接翻条目 | 加工后的知识页面 + 来源 | "帮我系统理解某个主题" |

### 2.1 全维度对比

| 维度 | 普通 RAG | GraphRAG | LLM Wiki |
|------|----------|----------|----------|
| **知识组装时机** | 查询时 | 查询时（图在摄入时建） | **摄入时** |
| **数据形态** | chunk + 向量 | 实体 + 关系 + chunk | Wiki 页面 + 向量 + 来源 |
| **状态性** | 无状态，每次从零推导 | 半有状态（图谱） | **有状态，知识持续积累** |
| **基础设施** | 向量库 + Embedding 管线 | 图数据库 + 向量库 | 文件系统 / 关系库 + Markdown |
| **可解释性** | 低（向量空间是黑盒） | 高（可列出命中的实体与关系） | 高（每条知识可追溯来源文件） |
| **跨文档综合** | 弱（受检索精度限制） | 中（多跳扩展） | **强（编译阶段已完成整合）** |
| **构建成本** | 低 | 高（抽取、归一化、清洗） | 中高（入库前要调 LLM 生成页面） |
| **适合规模** | 无上限 | 中等偏大 | **中小（≤10 万 tokens 最优）** |
| **幻觉扩散** | 限于单次回答 | 错误边持续影响检索 | **可能扩散到整个 Wiki** |

### 2.2 选型决策树

```text
你的主要问题是什么？
├─ 简单事实 / 文档问答（"资料里怎么说"）
│     → 普通 RAG，优化切分 / 混合检索 / Rerank / 引用
│       （能满足 80% 的问题，就别上更复杂的东西）
├─ 关系 / 链路 / 依赖 / 多跳（"A 和 B 什么关系"）
│     → GraphRAG：实体关系 + 子图检索 + 原文证据
└─ 学习 / 总结 / 体系化理解 / 长期沉淀（"帮我系统搞懂"）
      → LLM Wiki：主题页面 + 候选学习 + 审核治理
```

### 2.3 能不能组合

**能，而且企业级架构通常是组合的**：

```text
原始文档（不可变）
  ├─ 普通 RAG  → 快速语义检索原文 chunk（保真、可溯源）
  ├─ GraphRAG  → 实体关系、多跳推理、结构化解释
  └─ LLM Wiki  → 主题化总结、知识沉淀、长期维护

用户问题 → 按意图路由：
  事实查询 → RAG 证据
  关系查询 → 图谱结构
  体系理解 → Wiki 页面
  → LLM 综合三类上下文生成答案
```

代价是成本和复杂度更高，收益是更接近真正的"领域专家助手"。

---

## 三、LLM Wiki 到底是什么

### 3.1 一句话定义

> 让大模型在**入库阶段**先把原始文档整理成**结构化、主题化、概念化的知识页面**，
> 之后的检索与问答都基于这些"加工后的页面"，而不是原始 chunk。

### 3.2 核心心智模型：原始文档是源代码，Wiki 是编译产物

```text
旧范式（RAG）：
  原始文档 → 每次查询都临时检索 → 临时片段 → 每次重新推理 → 回答

新范式（LLM Wiki）：
  原始文档 → 一次编译 → 结构化 Wiki → 直接查询 → 回答
                          ↑
                    Wiki 持续自我更新（知识复利）
```

**编译只做一次，之后所有查询都基于已编译的结果。** 这是它和 RAG 最本质的差别，
也是"彻底告别重复推导"这句话的由来。

### 3.3 Karpathy 原方案的三层目录

方案刻意做得极简——让任何 LLM Agent 不需要定制工具就能直接操作（本质是"用文件系统当知识库"）：

| 层级 | 目录 | 谁写 | 谁读 | 核心原则 |
|------|------|------|------|----------|
| 原始资料层 | `raw/` | 人类（只追加） | LLM（只读） | **不可变**，所有追溯链终止于此 |
| 编译知识层 | `wiki/` | **LLM 写** | 人类读 | LLM 拥有写权限，人类不直接编辑 |
| 规则配置层 | `schema.md` | 人类定义 | LLM 遵循 | 规定格式、约定与 Workflow |

`schema.md` 的典型内容：

```markdown
# 文档结构
title / summary / tags / related（[[wiki-links]]）/ sources

# Workflow
## ingest
读 raw → 提取概念实体 → 新建或更新 wiki 页 → 维护 related 双向链接 → 追加 log.md
## query
先读 index.md 定位 → 加载相关 wiki 页（**不查 raw**）→ 基于已编译知识回答
## lint
检测：孤儿页面 / 断链 / 矛盾声明 / 过期信息
```

### 3.4 两种落地形态（别混为一谈）

| 形态 | 检索方式 | 说明 | 适合 |
|------|----------|------|------|
| **A. 纯文件系统 Wiki** | Agent 现场 `grep` / `read` / 读 `index.md` 导航 | Karpathy 原方案；零基础设施 | 个人、小团队、≤几百页 |
| **B. Wiki 页面 + 向量检索** | 把 Wiki 页面当 document 入库，走标准 RAG 检索 | 国内落地最常见；**Wiki 换掉的是"切块内容"，不是"检索手段"** | 企业知识库、客服 |

> 关键认知：**形态 B 里 Wiki 和向量库是协作关系，不是竞争关系。**
> 很多"Wiki 取代 RAG"的文章，实际讲的是形态 B——取代的是"原始 chunk 作为检索单元"，
> 向量库、Embedding、Rerank 一个都没少。

### 3.5 一个 Wiki 页面长什么样

| 字段 | 作用 |
|------|------|
| 标题 / 摘要 | 快速定位，降低检索噪声 |
| 核心概念 | 定义与边界 |
| 关键事实 | 结论性内容（**必须带 sources**） |
| 适用场景 | 什么时候成立、什么前提 |
| 与其他概念的关系 | 双向链接，支撑多跳 |
| 原文引用 / 来源 | 可追溯，防总结偏差 |
| 后续沉淀内容 | 高质量问答反哺回页面 |

### 3.6 效果对比（同一份文档）

假设一份白皮书里关于 "AI Agent" 的内容散落在 5 个章节（定义、工具调用、记忆、安全、部署）：

| 方案 | 提问"AI Agent 核心组件有哪些、什么关系" |
|------|----------------------------------------|
| 普通 RAG | 召回其中 2~3 个相似 chunk，模型自己拼，**可能漏掉"记忆模块"那章** |
| LLM Wiki | 摄入时已生成一张 "AI Agent 主题页"（定义+组件+关系+区别+来源），**直接命中结构完整的页面** |

---

## 四、三大核心操作：ingest / query / lint

### 4.1 Ingest（编译）——和 RAG 差距最大的一步

不是"索引"，而是完整的**理解 → 提炼 → 整合 → 建关联**：

- 一份资料可能触及 **10~15 个 Wiki 页面**；
- 例：一篇"特斯拉固态电池"论文进来 → 更新"特斯拉公司"页、"电池技术"页，**新建**"固态电池"页，
  并在"充电效率"页建立反向链接。

这正是它比 RAG"跨文档综合强"的来源——**整合发生在摄入时，而不是查询时临时拼。**

### 4.2 Query（查询）

- 先读 `index.md` 定位主题，再加载相关 Wiki 页，**不回查 raw**；
- **查询结果可以写回成新的 Wiki 页**（或进候选池），形成"知识复利"；
- 工程上要给写入加门槛：不确定的答案不沉淀、重复内容去重、重要知识人工审核、写入限流。

### 4.3 Lint（自我修复）——最容易被忽略、也最重要的一步

Karpathy 会定期跑健康检查，四类问题自动发现：

| 检查项 | 现象 | 处理 |
|--------|------|------|
| 孤儿页面 | 没有任何页面链接到它 | 补 `related` 或删除 |
| 断链 | 链接指向不存在的页 | 自动建空页或修链接 |
| **矛盾声明** | 两个页面结论冲突 | 标记 ⚠️ 待人工裁定 |
| 待创建页面 | 被引用但还没建立 | 生成建议清单 |

**没有 lint 的 Wiki = 一座会腐烂的图书馆。** 这是 wiki 类方案能否长期运行的分水岭。

---

## 五、为什么它在某些场景确实比 RAG 强

### 5.1 强在"知识组装时机"前移

| 对比项 | RAG | LLM Wiki |
|--------|-----|----------|
| 组装时机 | 查询时 | **摄入时** |
| 组装成本 | 每次查询都付一遍 | 只付一次 |
| 组装质量 | 受限于本次召回是否齐全 | 全局视角，可交叉整合 |
| 结果稳定性 | 同一问题两次问，召回可能不同 | 读同一页面，结论稳定 |

### 5.2 其他真实优势

1. **降低检索噪声**：摄入阶段已过滤页眉、目录、重复描述、格式噪声；
2. **基础设施极简**：形态 A 只需要文件系统 + Markdown，没有向量库、没有 Embedding 管线；
3. **可解释性高**：每条知识都能追到 `sources` 里的原始文件；
4. **适合学习型场景**：接近人类学习方式——读资料 → 提炼概念 → 形成笔记 → 建立主题页 → 持续补充。

### 5.3 一句话总结适用性

> **RAG 擅长"找到某一处答案"；LLM Wiki 擅长"积累并理解一整套知识体系"。**
> 问的是事实 → RAG；问的是体系 → Wiki。

---

## 六、它的真正风险：幻觉固化

这是 wiki 类方案**最重要、也最容易被忽略**的风险，必须在选型时摊开讲。

### 6.1 同一种幻觉，两种扩散范围

| 风险来源 | 在 RAG 中的影响 | 在 LLM Wiki 中的影响 |
|----------|----------------|---------------------|
| LLM 幻觉 | 仅影响这一次回答 | **可能被编译进 Wiki，扩散到 10~15 个关联页面** |
| 错误来源文档 | 下次检索可能不召回，自然稀释 | 编译后**永久**影响概念理解 |
| 信息过期 | 新文档进来后自动稀释旧结论 | 旧结论可能长期保留且无人察觉 |

**RAG 的错误是一次性的，Wiki 的错误是资产化的。** 这句话是本篇最值得记住的一句。

### 6.2 缓解手段（缺一不可）

| 手段 | 做法 |
|------|------|
| **强制留源** | 每个页面的 `sources` 字段必须指向原始文件，禁止无源结论 |
| **定期 lint** | 定时检查矛盾声明、过期信息、孤儿页 |
| **置信度标注** | 关键事实带 confidence 分数 + 时间戳 |
| **置信度衰减** | 置信度随时间衰减、随多来源佐证增强（LLM Wiki v2 的核心改进） |
| **人工审核闸门** | 不确定的答案进候选池，不进正式页面 |
| **可回滚** | wiki 目录纳入 Git，出问题能 diff、能 revert |

> 置信度衰减把 Wiki 从"一堆平等权重的声明"升级为"有可信度层次的动态知识模型"。
> 没有这一层，Wiki 会随着时间推移悄悄腐烂。

---

## 七、硬边界：什么时候绝对不能上 Wiki

对照下面这张表，命中任意一条就别用纯 Wiki 方案：

| 场景 | 为什么不行 | 应该用什么 |
|------|-----------|-----------|
| **超大规模（>100 万 tokens）** | `index.md` 放不进上下文，导航失效 | RAG / GraphRAG |
| **多用户协作 + 权限隔离** | 纯文件系统没有 ACL 与并发写保护 | 企业知识库 + 关系库；或 Git 工作流 |
| **强合规 / 敏感数据** | 纯文件系统缺加密与审计层 | 加密存储 + 访问审计 |
| **毫秒级实时更新** | ingest 需要 LLM 编译时间，天然是批处理节奏 | RAG 增量索引（见 [08 篇](08-知识库增量同步实战-RocketMQ驱动向量化.md)） |
| **高频不变的事实查询** | 为几个固定 FAQ 编译整套 Wiki，成本与收益倒挂 | 普通 RAG 甚至硬编码 FAQ |
| **答案要求逐字保真** | 总结必然有信息损耗与归纳偏差 | 原文检索 + 引用溯源 |

### 7.1 一个容易踩的坑：把 Wiki 当"唯一真相源"

Wiki 页面是**编译产物**，不是原始事实。原始文档才是真相源。
所以工程上必须保留两层：`raw/` 不可变 + `wiki/` 可重建。
**做不到"Wiki 能从 raw 全量重建"，就不要把它当资产。**

---

## 八、正确姿势：不是替代，是分层

### 8.1 推荐分层架构

```text
┌──────────────────────────────────────────────────────────┐
│ L1 原始层（真相源，不可变）                                │
│    对象存储 / MinIO / DB（PDF、Word、网页快照）            │
└───────────────────────┬──────────────────────────────────┘
                        │ ingest（LLM 编译，10~15 页/资料）
                        ▼
┌──────────────────────────────────────────────────────────┐
│ L2 编译层（知识资产，可重建）                              │
│    Wiki 主题页（Markdown，带 sources / related / conf）    │
│    + 图谱三元组（关系型问题）                              │
└───────────────────────┬──────────────────────────────────┘
                        │ 页面即 document，切块/向量化
                        ▼
┌──────────────────────────────────────────────────────────┐
│ L3 检索层（手段，可替换）                                  │
│    向量库（语义）+ ES BM25（精确）+ RRF + Rerank           │
└──────────────────────────────────────────────────────────┘
```

**关键认知：Wiki 在 L2，向量库在 L3。它们不在同一层，谈不上谁取代谁。**
"Wiki 取代 RAG"真正发生的是：**L3 的检索单元从"原始 chunk"换成了"编译后的页面"。**

### 8.2 各层职责与失败模式

| 层 | 出问题时的表现 | 兜底手段 |
|----|----------------|----------|
| L1 原始层 | 来源丢失 → 无法追溯 | 只追加不修改，永久保留 |
| L2 编译层 | 幻觉固化、结论腐烂 | lint + confidence + Git 可回滚 |
| L3 检索层 | 召回不到、排序不准 | 混合检索 + Rerank + 降级（见 [04](04-RAG进阶实战-Rerank重排序.md)/[05](05-混合检索实战-ES-BM25与向量RRF融合.md) 篇） |

---

## 九、本项目落地的最小改造路径

本项目的 RAG 链路（`kb_document` → RocketMQ → `KnowledgeVectorService.vectorize()` → Chroma →
`retrieveRagDocs(...)` 四模式编排 → `citations`）**不需要推翻**，只需在 L1 和 L3 之间插入 L2。
下面是复用现有资产的最小改造清单：

### 9.1 复用现有的部分（零改动）

| 现有资产 | 复用于 Wiki |
|----------|-------------|
| `kb_document` 表 + 文档 CRUD | 直接存 Wiki 页，加一个 `doc_type` 区分 `RAW` / `WIKI_PAGE` |
| `knowledge-doc-sync-topic` + `KnowledgeSyncConsumer` | Wiki 页保存后照旧投递，异步向量化（[08 篇](08-知识库增量同步实战-RocketMQ驱动向量化.md) 链路直接可用） |
| `KnowledgeVectorService.vectorize()` | 页面切块向量化，逻辑不变 |
| `citations` 引用溯源（[06 篇](06-引用溯源实战-带出处回答.md)） | 把 metadata 的 `documentId/title` 指向 Wiki 页，`sources` 字段透传给前端 |
| `retrieveRagDocs(...)` 四模式 | 对上层完全透明，检索单元换掉即可 |

### 9.2 需要新增的部分

```text
1) ingest 编译管线（新增）
   上传原始文档 → 保留 RAW 记录
        → LLM 抽取概念实体（复用 Nl2Sql/LLM 调用的弹性链路）
        → 聚类到主题 → 生成/更新 Wiki 页（带 sources / related）
        → 写 kb_document(doc_type=WIKI_PAGE) → 投递 MQ → 向量化

2) lint 定时任务（新增，建议走 XXL-Job）
   扫描：孤儿页 / 断链（related 指向不存在的页）/ 矛盾声明 / 过期页
   输出：告警 + 待人工裁定清单

3) 置信度字段（新增）
   page.metadata 增加 confidence + updatedAt，检索时可对低置信度页面降权
```

### 9.3 与已有 GRAPH_RAG 的关系

本项目 `GraphRagService`（`aics.rag.graph.enabled=false` 默认关闭，见
[04-SpecKit实战-RAG进阶六件套](../04-规格驱动工具/04-SpecKit实战-RAG进阶六件套.md)）已经提供了 L2 的另一半——
**实体与关系**。Wiki 页负责"主题化总结"，图谱负责"多跳关系"，两者在 L2 是互补的：
Wiki 页可以直接作为图谱实体的落地页（页面 ↔ 实体一对一），点开实体即见总结。

### 9.4 分阶段落地建议

| 阶段 | 做什么 | 验收 |
|------|--------|------|
| P0 | 挑一个**单主题、≤20 篇资料**的场景（如"退款政策"）手工编译几页 Wiki，当作 `doc_type=WIKI_PAGE` 入库 | 对比同一问题：Wiki 页检索 vs 原始 chunk 检索的答案完整度 |
| P1 | 把 ingest 编译步骤半自动化（LLM 生成页面草稿 + 人工审核） | 有审核闸门，页面带 `sources` |
| P2 | 上线 lint 定时任务 + confidence 字段 | 能自动发现矛盾与孤儿页 |
| P3 | 高质量问答反哺候选池 → 审核通过后写回页面 | 形成"知识复利"闭环 |

> **不建议一步到位全自动 ingest 全量文档**——P0 阶段的最大价值是让你**亲手感受到幻觉固化有多贵**。

### 9.5 面试话术（一句话把这件事讲清楚）

> "RAG 和 LLM Wiki 不是替代关系，它们在不同层：Wiki 在编译层，向量库在检索层。
> 所谓 Wiki 取代 RAG，实际是把检索单元从原始 chunk 换成了 LLM 预编译的知识页面。
> 我在项目里做的是分层：原始文档保证保真与溯源，Wiki 页承载跨文档综合，向量库 + BM25 + Rerank 负责召回。
> Wiki 最大的风险是幻觉固化——错误会随交叉引用扩散，所以必须强制留源、定期 lint、带置信度衰减，
> 并且保证 Wiki 能从原始文档全量重建。"

---

## 十、术语速查

| 术语 | 人话解释 |
|------|----------|
| **LLM Wiki** | 让大模型把原始文档预先编译成结构化主题页，检索时直接读页面而不是原始片段 |
| **ingest（摄入 / 编译）** | 把原始资料理解、提炼、整合进 Wiki 页的过程；一份资料可能改动 10~15 个页面 |
| **query（查询）** | 在已编译的 Wiki 里导航取答案，不再回查原始文档 |
| **lint（体检）** | 定期检查孤儿页、断链、矛盾声明、过期信息、待建页面的机制 |
| **raw 层** | 不可变的原始资料层，所有追溯链的终点，人类只追加 |
| **schema.md** | 人类给 LLM 定的"写作规范 + 工作流"，规定页面结构与 ingest/query/lint 流程 |
| **wiki-links / related** | 页面之间的双向链接，支撑多跳导航 |
| **sources 字段** | 页面每个结论指向的原始文件，防总结偏差、支持人工核查 |
| **compile-time / query-time** | 知识组装发生在摄入时 / 查询时——本篇最核心的一组对立概念 |
| **幻觉固化** | LLM 的错误被编译进 Wiki 后，随交叉引用扩散，而非只影响单次回答 |
| **confidence 衰减** | 给关键事实打置信度分数并随时间衰减、随佐证增强，防止知识腐烂 |
| **知识复利** | 查询产生的高质量答案写回 Wiki，使知识库越用越强 |
| **候选学习池** | 未经审核的新知识暂存区，审核通过才进正式页面 |
| **Agentic 检索** | 不预先固定检索管线，让 Agent 用工具（读目录 / grep / read）现场决定怎么找 |
| **GraphRAG** | 抽取实体与关系建图，检索时做多跳扩展并回溯原文证据 |
| **索引放不进上下文** | Wiki 规模太大时 `index.md` 超过窗口长度、导航失效，是纯 Wiki 方案的规模天花板 |

---

## 十一、常见误区速查

| 误区 | 现实 |
|------|------|
| "Wiki 会取代 RAG" | 两者在不同层：Wiki 是编译层，向量库是检索层；形态 B 里 Wiki 页照样走向量检索 |
| "上了 Wiki 就不需要向量库了" | 只有"纯文件系统 Wiki + Agent 现场 grep"才不需要；企业规模几乎都要保留检索层 |
| "Wiki 比 RAG 先进，所以更好" | 规模 >100 万 tokens、多租户、强实时、强合规场景下 Wiki 直接失效 |
| "检索到内容就能答对" | 与 RAG 同理，还取决于上下文拼接、Prompt、模型能力、是否要求引用证据 |
| "LLM 总结的内容一定是可信的" | 总结必然有损耗与归纳偏差，必须留 source、设审核、可回滚 |
| "错误进 Wiki 和错误进 RAG 影响差不多" | 差别巨大：RAG 错一次，Wiki 错一片（可能扩散到 10~15 个页面） |
| "wiki 文件是资产，不能动" | Wiki 是编译产物，必须保证能从 raw 全量重建，否则不可回滚 |
| "GraphRAG 一定比普通 RAG 好" | 它擅长关系密集型问题；简单 FAQ 用普通 RAG 更便宜更稳 |
| "只要 Agent 会 grep，知识库就直接用文件系统" | grep 依赖关键词命中，语义改写、同义词、跨语言会漏——这正是向量的价值 |
| "长上下文能替代检索" | 能塞下 ≠ 用得上（中间信息易被忽略），且成本随查询次数线性放大 |

---

## 十二、面试专练与追问链

### Q1：为什么现在有人说 Wiki 要代替 RAG？

**答题骨架（结论 → 归因 → 边界）**：
1. 结论：不是替代，是知识组装时机从 query-time 前移到 compile-time；
2. 归因：RAG 的三个真实痛点（无积累、跨文档合成弱、会话失忆）+ Karpathy LLM Wiki 方案（2026-04）的传播；
3. 边界：原方案自认适合 ≤10 万 tokens，超百万 tokens 应回到 RAG / GraphRAG。

**追问 1：那"Wiki 页 + 向量检索"里，Wiki 取代了什么？**
→ 取代的是**检索单元**（原始 chunk → 编译后的页面），不是检索手段；Embedding / 向量库 / Rerank 一个没少。

**追问 2：为什么摄入时组装比查询时组装好？**
→ 组装成本只付一次；有全局视角可交叉整合；结果稳定、可复用、可审计。
代价是摄入慢、有信息损耗、错误会被资产化。

### Q2：LLM Wiki 最大的风险是什么？怎么防？

→ **幻觉固化**。RAG 的错误只影响单次回答，Wiki 的错误会随交叉引用扩散到 10~15 个页面并长期存在。
防护四件套：强制 `sources` 留源 + 定期 `lint` 查矛盾/过期 + `confidence` 带时间衰减 + Git 可回滚（能从 raw 重建）。

**追问：lint 具体查什么？**
→ 孤儿页面、断链、矛盾声明、过期信息、待创建页面。

### Q3：什么场景你会坚持用 RAG 而不是 Wiki？

→ 命中任意一条即坚持 RAG：
① 数据量 >100 万 tokens（index 进不了上下文）；
② 多租户 / ACL / 强合规加密；
③ 毫秒级实时更新（ingest 是批处理节奏）；
④ 答案要求逐字保真（总结必有损耗）；
⑤ 高频但固定的事实查询（编译成本收不回来）。

### Q4：Wiki 和 GraphRAG 是什么关系？

→ 同在编译层、互为补充。Wiki 页解决"主题化总结"，图谱解决"多跳关系"。
落地时可以让 Wiki 页成为图谱实体的落地页（一对一），两者都由同一份 raw 编译而来。

### Q5：（进阶）Agentic 检索和 LLM Wiki 是一回事吗？

→ 不是。
- **Agentic 检索**：检索**手段**的变化——把固定管线换成 Agent 自主选策略（读目录、grep、读文件、调向量库）；
- **LLM Wiki**：知识**形态**的变化——把原始 chunk 换成预编译页面。
两者正交，可以叠加：Agent 在一个 Wiki 目录上做自主检索。

### Q6：怎么向上级论证"我们要不要做 Wiki"？

→ 先问三个数：① 文档总量（tokens）；② 用户是不是同一批人（是否需要 ACL）；③ 主要问题是事实型还是体系型。
三个数决定选型，而不是先决定技术再找场景。
**并且要说明：RAG 那套（切分/混合检索/Rerank/引用）不是白做，Wiki 方案照样要用。**

---

## 十三、动手练习

1. **最小复现**：建三个目录 `raw/`、`wiki/`、`schema.md`，放 5 篇同主题 Markdown，
   用任意 LLM 按 `schema.md` 跑一次 ingest，看它生成了几页、有没有维护 `related`。
2. **对比实验**：同一组 10 个问题，分别用"raw 直接切块检索"和"Wiki 页检索"回答，
   记录答案完整度与引用质量差异（注意：这正是 §9.4 的 P0 验收）。
3. **故意注入错误**：在 raw 里放一条错误事实，走完 ingest，观察它污染了几页——体会"幻觉固化"。
4. **跑一次 lint**：手工检查你的 Wiki 有没有孤儿页/断链，写一段脚本自动检测。
5. **接回本项目**：把生成的一页 Wiki 当作 `doc_type=WIKI_PAGE` 写进 `kb_document`，
   走一遍 MQ → 向量化 → `/chat/rag`，确认检索与 `citations` 是否正常。

---

## 十四、来源与更新

| 日期 | 变更 |
|------|------|
| 2026-09-10 | 初版：回应"Wiki 代替 RAG"的讨论，补齐"知识库新范式"这一层文档空白；含三方案对比、ingest/query/lint 三操作、幻觉固化风险、分层架构与本项目最小改造路径 |

**主要参考**（均为社区公开资料，观点按原文转述）：

| 来源 | 内容 |
|------|------|
| Andrej Karpathy《LLM Knowledge Bases》Gist（2026-04） | LLM Wiki 原始方案：三层目录、`schema.md`、ingest/query/lint |
| 社区解读《Karpathy LLM Wiki：用 AI 编译知识，彻底告别 RAG 重复推导》 | 编译隐喻、全维度对比、适用/不适用清单、幻觉固化与 confidence 衰减 |
| 社区解读《从普通 RAG、知识图谱 RAG 到 LLM Wiki：原理、区别与选型》 | 三方案对比表、选型决策树、Wiki 页面字段、组合架构、学习路线 |
| 社区解读《Agentic 知识库：Agent Wiki 不是取代向量数据库》 | 澄清"取代向量库"的说法 |

> 阅读提示：本篇对时间点与出处的表述按公开资料转述，用于技术选型时请交叉核实原始 Gist 与开源实现。
> 与既有篇章的关系：本篇是 [10-知识点汇总](10-RAG全栈实战-知识点汇总.md) 之后的**范式层**补充，
> 不重复讲 RAG 的实现细节；检索底座原理见 [05-向量数据库](../05-向量数据库/README.md)。
