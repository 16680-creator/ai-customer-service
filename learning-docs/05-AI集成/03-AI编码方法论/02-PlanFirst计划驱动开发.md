# Plan-First（计划驱动开发）详解

> 参考来源：`docs/09-AI开发模式全景对比.md` 第4节

---

## 一、是什么

**Plan-First**（计划驱动开发）是一种"先规划、后执行"的 AI 开发模式。先让 AI 输出完整的实施计划（分步骤、分模块、标注文件路径），人审核确认后再逐步执行。

它是 SDD 的轻量级替代方案——保留了 SDD 的"计划先行"理念，但省略了繁重的 spec/clarify 多阶段文档流程。

---

## 二、核心流程

```
需求描述
   ↓
AI 生成完整实施计划
   ├── 步骤1：创建 xxx 文件，实现 xxx 功能
   ├── 步骤2：修改 xxx 文件，添加 xxx 逻辑
   ├── 步骤3：编写测试用例
   └── 步骤4：集成联调
   ↓
人审核计划（调整/确认）
   ↓
逐步执行（每步执行 + 验证）
   ↓
最终验收
```

---

## 三、与 SDD 的对比

| 维度 | Plan-First | SDD |
|------|-----------|-----|
| **流程阶段** | 1-2 步（计划 + 执行） | 6 步（specify → clarify → plan → tasks → implement → analyze） |
| **文档产出** | 一份实施计划 | 完整的规格文档链 |
| **审批节点** | 1 次（审核计划） | 每阶段都有审批 |
| **适合复杂度** | 中等 | 高 |
| **时间成本** | 低 | 高 |
| **门槛** | 低，上手快 | 高，需要学习流程 |

简单说：**SDD 是"正规军"，Plan-First 是"快速反应部队"**。

---

## 四、Plan-First 的 Plan 应该包含什么

一份好的 AI 实施计划应包含：

```markdown
## 实施计划

### 目标
在 xxx 模块中实现 xxx 功能

### 影响范围
- 新增文件：src/xxx/XxxService.java
- 修改文件：src/xxx/XxxController.java
- 涉及模块：user、order、gateway

### 执行步骤

**步骤1：创建数据模型**
- 文件：src/xxx/model/XxxDTO.java
- 内容：定义 xxx 请求/响应字段
- 验证：编译通过

**步骤2：实现 Service 层**
- 文件：src/xxx/XxxService.java
- 内容：核心业务逻辑
- 依赖：步骤1
- 验证：单元测试通过

**步骤3：实现 Controller 层**
- 文件：src/xxx/XxxController.java
- 内容：API 接口暴露
- 依赖：步骤2
- 验证：接口可调用

**步骤4：集成测试**
- 内容：端到端流程验证
- 依赖：步骤3
- 验证：所有测试通过
```

---

## 五、人的角色 vs AI 的角色

| 角色 | 职责 |
|------|------|
| **人** | 需求描述、计划审核、方向把控、关键决策 |
| **AI** | 分析需求、生成计划、逐步执行、验证结果 |

人在 Plan-First 中的核心价值是**审核计划**——在代码写出来之前就发现方向性问题，避免返工。

---

## 六、适用场景

### 适合
- **中等复杂度功能**：涉及 3-10 个文件，有明确的前置后置依赖
- **多文件修改**：需要跨模块协调，不先规划容易遗漏
- **新模块开发**：从零搭建新模块，需要先想清楚架构
- **技术方案评审前**：用 AI 快速生成方案供评审
- **重构任务**：大规模重构前先规划影响范围

### 不适合
- **一行代码的改动**：杀鸡用牛刀
- **需求极度模糊**：连人都不清楚要做什么，AI 更规划不出来
- **探索性开发**：需要边试边改，计划赶不上变化

---

## 七、优缺点

### 优点
| 优点 | 说明 |
|------|------|
| 避免返工 | 方向性错误在计划阶段就发现 |
| 结构清晰 | 执行时知道每一步做什么、依赖什么 |
| 轻量高效 | 比 SDD 快，比对话迭代稳 |
| 可追溯 | 计划文档本身就是一个轻量级的开发记录 |
| 降低遗漏 | 多文件修改时不会漏掉某个文件 |

### 缺点
| 缺点 | 说明 |
|------|------|
| 计划质量依赖 AI 理解 | 如果 AI 理解偏差，计划全错 |
| 灵活性不足 | 执行中如果发现计划不合理，需要重新规划 |
| 有前期成本 | 花时间生成和审核计划 |
| 不适合简单任务 | 对于小改动是过度设计 |

---

## 八、实战示例

### 场景：给用户模块添加"手机号登录"功能

**Step 1：让 AI 生成计划**
```
开发者：用户模块当前只支持邮箱登录，需要新增手机号验证码登录。
请先生成实施计划，列出所有需要修改的文件和步骤。
```

**AI 输出计划：**
```
1. 新增 LoginByPhoneRequest（DTO）
2. 修改 UserController，新增 /login/phone 接口
3. 修改 UserService，新增 phoneLogin() 方法
4. 新增 SmsService（短信发送服务）
5. 修改 SecurityConfig，放行新接口
6. 新增单元测试
7. 新增集成测试
```

**Step 2：人审核**
```
开发者：第4步 SmsService 应该复用已有的 NotifyService，不要新建。
另外第5步不需要，/login/phone 已经匹配了现有的通配规则。
```

**Step 3：按修正后的计划逐步执行**

---

## 九、Plan-First 的 Prompt 模板

```
我有一个需求：[描述需求]

请按以下格式输出实施计划：
1. 目标：一句话概括
2. 影响范围：需要新增/修改的文件列表（带路径）
3. 执行步骤：每步包含文件路径、实现内容、依赖关系、验证方式
4. 风险点：可能出问题的地方

输出计划后等待我确认，不要直接写代码。
```

---

## 十、在本项目中的实践

本项目（ai-customer-service）中，中等复杂度的日常任务默认使用 Plan-First 模式：

```
Rules-Driven：QODER.md 定义规范
       ↓
Plan-First：生成计划 → 审核 → 逐步执行
       ↓
验证：编译 + 测试 + 接口调用
```

实际上，Plan-First 是项目中使用频率最高的开发模式，因为大部分任务都是"中等复杂度"——既不是一行代码的小改动，也不值得启动完整的 SDD 流程。