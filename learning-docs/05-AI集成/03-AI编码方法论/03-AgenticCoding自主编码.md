# Agentic Coding（Agent 自主编码）详解

> 参考来源：`docs/09-AI开发模式全景对比.md` 第5节

---

## 一、是什么

**Agentic Coding**（Agent 自主编码）是目前 AI 自主度最高的开发模式。给 AI Agent 一个高层目标，Agent 自主决定如何完成：搜索代码库、编写代码、运行测试、修复错误，循环直到目标达成。

与 Vibe Coding 的区别在于：Agentic Coding 有**自主规划能力**和**自我纠错循环**，而 Vibe Coding 更多是"一步生成"。

---

## 二、核心流程

```
设定高层目标
      ↓
Agent 自主理解上下文（搜索代码库、阅读现有代码）
      ↓
Agent 自主规划（分解任务、确定执行顺序）
      ↓
Agent 自主执行（编写代码、运行测试、修复错误）
      ↓
Agent 自我验证（检查结果是否符合目标）
      ↓
不符合 → 自动修复 → 重新验证
      ↓
符合 → 交付
```

关键在于**人在循环中的位置**：人只在最开始设定目标，和最后验收结果，中间全程不参与。

---

## 三、典型工具

| 工具 | 特点 |
|------|------|
| **Devin** | 首个"AI 软件工程师"，可独立完成 PR |
| **OpenHands** | 开源 Agent，支持浏览器端操作 |
| **Qoder Agent** | 商汤出品，多模式切换（Agent/Plan/Debug） |
| **Claude Code** | Anthropic 的终端 Agent，自主搜索、编辑、测试 |
| **Cursor Agent** | IDE 内 Agent 模式，自主跨文件操作 |
| **GitHub Copilot Workspace** | 从 Issue 到 PR 的全自动流程 |

---

## 四、人的角色 vs AI 的角色

| 角色 | 职责 |
|------|------|
| **人** | 目标设定者、最终验收者 |
| **AI** | 完全自主的规划者和执行者 |
| **人不需要做的事** | 代码审查、测试编写、调试、部署 |

人的价值从"写代码"转向"定义问题"和"判断结果"。

---

## 五、Agent 的自主循环

Agent 的核心能力在于**自主决策循环**：

```
┌──────────────────────────────────────────┐
│              Agent 决策循环               │
│                                          │
│   观察环境 ──→ 思考 ──→ 行动 ──→ 观察结果 │
│     ↑                              │     │
│     └──────── 反馈修正 ←────────────┘     │
└──────────────────────────────────────────┘
```

具体来说，Agent 在每一步会：

1. **观察**：读取当前代码、运行结果、错误信息
2. **思考**：分析问题原因，决定下一步做什么
3. **行动**：修改代码、运行命令、搜索文件
4. **反馈**：根据行动结果调整策略

---

## 六、适用场景

### 适合
- **明确可验证的独立任务**：有清晰的输入输出，可以自动判断成功/失败
- **Bug 修复**：有明确的错误信息，Agent 可以自主搜索、定位、修复
- **代码迁移**：如升级框架版本、迁移 API 调用
- **文档生成**：扫描代码库自动生成 API 文档
- **测试补充**：自动分析代码覆盖率，补充缺失的测试用例
- **小型 PR**：从 Issue 到 PR 的完整流程

### 不适合
- **需要频繁人工决策**：架构选型、技术方案选择
- **需求模糊**：目标不明确时 Agent 会"瞎猜"
- **高风险操作**：涉及数据库变更、支付逻辑、安全设置
- **跨团队协作**：需要沟通对齐的场景

---

## 七、优缺点

### 优点
| 优点 | 说明 |
|------|------|
| 极大解放人力 | 人可以同时管理多个 Agent 并行工作 |
| 持续工作 | Agent 不会累，可以 24 小时工作 |
| 自我纠错 | 有错误自动修复，不依赖人发现 |
| 上下文完整 | 能搜索整个代码库，理解全貌 |
| 一致性高 | 同一套规则下，输出风格一致 |

### 缺点
| 缺点 | 说明 |
|------|------|
| 信任成本高 | 放手让 AI 自主操作，需要勇气 |
| 可能跑偏 | Agent 可能陷入死循环或错误方向 |
| Token 消耗大 | 自主搜索 + 多轮修复，成本高 |
| 难以审计 | 中间过程不透明，出问题难定位 |
| 不适合创新 | 对真正需要创造力的任务表现一般 |

---

## 八、Agentic 的信任层级

人对 Agent 的信任可以分级：

| 层级 | 描述 | 示例 |
|------|------|------|
| **L1：建议模式** | Agent 只建议，不执行 | "建议在第42行修改xxx" |
| **L2：半自动** | Agent 执行，每步需人确认 | 写代码前询问，运行前询问 |
| **L3：自动+审查** | Agent 自动执行，人事后审查 | 自动完成 PR，人 review 后合并 |
| **L4：全自动** | Agent 自主完成，人只验收结果 | 给 Issue，自动完成并部署 |

目前大多数团队在 **L2-L3** 之间，L4 全自动仅适用于低风险任务。

---

## 九、实战示例

### 场景：用 Claude Code 修复一个 Bug

**Issue 描述：**
```
用户登录后，刷新页面会丢失登录状态，需要重新登录。
预期：刷新后应保持登录状态。
```

**Agent 自主执行过程：**
```
1. [搜索] 搜索 "login" "token" "session" 相关代码
2. [阅读] 读取 AuthService.java、TokenFilter.java
3. [分析] 发现 token 存储在内存中，刷新后丢失
4. [规划] 需要将 token 持久化到 localStorage/Cookie
5. [执行] 修改前端代码，添加 token 持久化逻辑
6. [测试] 运行相关测试用例
7. [修复] 一个测试失败，修复 mock 配置
8. [验证] 所有测试通过
9. [交付] 生成 PR 描述
```

全程开发者只提供了 Issue 描述，其余都是 Agent 自主完成。

---

## 十、Agentic 的未来趋势

| 趋势 | 说明 |
|------|------|
| **多 Agent 协作** | 多个 Agent 分工协作，如前端 Agent + 后端 Agent |
| **Agent 团队** | Agent 模拟完整开发团队角色（PM、Dev、QA） |
| **持续 Agent** | Agent 长期运行，监控项目，自动修复问题 |
| **意图驱动** | 从"给指令"走向"给意图"，Agent 理解人的真实目的 |
| **安全沙箱** | Agent 在受限环境中运行，高风险操作需人确认 |

---

## 十一、在本项目中的实践

本项目使用 Qoder 的 Agent 模式处理以下任务：

- 跨模块的代码搜索和分析
- 重复性代码生成（如批量创建 CRUD 接口）
- 简单的 Bug 修复（有明确错误信息）

对于复杂任务，仍然使用 Plan-First 或 SDD，避免 Agent 在复杂场景下"跑偏"。