# BDD + AI（行为驱动 + AI）详解

> 参考来源：`docs/09-AI开发模式全景对比.md` 第6节

---

## 一、什么是 BDD

**BDD**（Behavior-Driven Development，行为驱动开发）是一种软件开发方法，强调用**自然语言**描述系统行为，让技术团队和非技术团队使用共同的语言沟通。

核心概念：
- **行为描述 > 技术实现**：关注"系统应该做什么"，而不是"怎么实现"
- **通用语言（Ubiquitous Language）**：业务人员、开发、测试使用同一套语言
- **可执行规范**：行为描述直接转化为自动化测试

---

## 二、BDD + AI 的核心流程

```
编写 Gherkin 场景（Given/When/Then）
      ↓
AI 解析场景，理解业务意图
      ↓
AI 生成步骤定义（Step Definitions）
      ↓
AI 生成实现代码（Service/Controller）
      ↓
运行测试验证
      ↓
不通过 → AI 修复代码
      ↓
通过 → 完成
```

---

## 三、Gherkin 语法简介

Gherkin 是 BDD 的场景描述语言，使用 `Given/When/Then` 结构：

```gherkin
Feature: 购物车结算
  作为一个注册用户
  我想要结算购物车中的商品
  以便完成购买

  Scenario: 正常下单
    Given 用户已登录
    And 购物车中有 2 件商品，总价为 199.00
    When 用户点击结算按钮
    Then 系统生成一个订单
    And 订单状态为"待支付"
    And 购物车被清空
    And 商品库存减少 2 件

  Scenario: 库存不足
    Given 用户已登录
    And 购物车中有 1 件商品
    And 该商品库存为 0
    When 用户点击结算按钮
    Then 系统提示"商品库存不足"
    And 订单不会被创建
```

---

## 四、人的角色 vs AI 的角色

| 角色 | 职责 |
|------|------|
| **人** | 业务行为定义者，编写 Gherkin 场景 |
| **AI** | 将行为描述转化为可执行的步骤定义和实现代码 |

**关键价值**：业务人员（产品经理、QA）可以直接参与编写场景，不需要懂代码。AI 负责把"人话"翻译成"代码"。

---

## 五、BDD + AI 的技术栈

| 层级 | Java 生态 | JavaScript 生态 |
|------|-----------|-----------------|
| BDD 框架 | Cucumber-JVM | Cucumber.js / Jest-Cucumber |
| 步骤定义 | Java + JUnit | TypeScript + Jest |
| AI 集成 | 通过 AI 工具生成步骤定义和实现 | 同上 |

### Java + Cucumber 示例

```java
// Gherkin 场景定义在 feature 文件中

// AI 生成的步骤定义
public class CheckoutSteps {

    @Given("用户已登录")
    public void user_is_logged_in() {
        // AI 生成的登录逻辑
    }

    @Given("购物车中有 {int} 件商品，总价为 {double}")
    public void cart_has_items(int count, double total) {
        // AI 生成的购物车填充逻辑
    }

    @When("用户点击结算按钮")
    public void user_clicks_checkout() {
        // AI 生成的结算调用
    }

    @Then("系统生成一个订单")
    public void order_is_created() {
        // AI 生成的断言
    }
}
```

---

## 六、适用场景

### 适合
- **业务逻辑复杂**：结算、退款、审批流程等需要精确描述边界条件
- **需要非技术人员参与**：产品经理、业务专家直接编写验收标准
- **合规要求高**：金融、医疗等需要可追溯的业务验证
- **API 行为定义**：用 BDD 描述 API 的预期行为
- **回归测试**：积累的场景文件本身就是回归测试用例

### 不适合
- **纯技术重构**：不涉及业务行为变化
- **UI 布局调整**：BDD 不适合描述视觉细节
- **性能优化**：BDD 关注行为正确性，不关注性能

---

## 七、优缺点

### 优点
| 优点 | 说明 |
|------|------|
| 沟通桥梁 | 业务人员和技术人员使用同一份文档 |
| 活文档 | 场景文件即文档，且始终保持最新（因为测试会跑） |
| 需求可追溯 | 每个场景对应一个业务需求，出问题可追溯到场景 |
| 质量保障 | 所有场景自动运行，回归测试成本低 |
| AI 友好 | 结构化自然语言，AI 理解准确率高 |

### 缺点
| 缺点 | 说明 |
|------|------|
| 维护成本 | 场景文件也需要维护，业务变更时需同步更新 |
| 学习曲线 | 团队需要学习 Gherkin 语法和 BDD 理念 |
| 场景爆炸 | 条件组合多时，场景数量指数增长 |
| 执行慢 | 端到端 BDD 测试通常比单元测试慢 |

---

## 八、BDD 与 TDD 的关系

```
TDD：先写单元测试 → 再写代码
BDD：先写行为场景 → 再写代码
```

| 维度 | TDD | BDD |
|------|-----|-----|
| 关注点 | 代码正确性 | 业务行为正确性 |
| 描述语言 | 代码（assert/expect） | 自然语言（Given/When/Then） |
| 粒度 | 单元级别 | 功能级别 |
| 可读性 | 技术人员 | 全团队 |
| 测试类型 | 单元测试 | 集成/端到端测试 |

实际上两者互补：**TDD 保证代码写对了，BDD 保证写对了代码**。

---

## 九、实战示例

### 场景：本项目"用户注册"的 BDD 场景

```gherkin
Feature: 用户注册
  作为一个新用户
  我想要注册账号
  以便使用平台服务

  Scenario: 正常注册
    Given 手机号 "13800138000" 未被注册
    When 用户提交注册，手机号 "13800138000"，验证码 "123456"
    Then 注册成功
    And 返回用户信息和 JWT Token

  Scenario: 重复注册
    Given 手机号 "13800138000" 已被注册
    When 用户提交注册，手机号 "13800138000"，验证码 "123456"
    Then 注册失败
    And 返回错误信息 "该手机号已注册"

  Scenario: 验证码错误
    Given 手机号 "13800138000" 未被注册
    When 用户提交注册，手机号 "13800138000"，验证码 "000000"
    Then 注册失败
    And 返回错误信息 "验证码错误"
```

---

## 十、AI 如何辅助 BDD

| AI 辅助能力 | 说明 |
|------------|------|
| **场景生成** | 根据需求描述，AI 自动生成 Gherkin 场景 |
| **步骤定义生成** | 根据 Gherkin 场景，AI 生成对应的步骤定义代码 |
| **实现代码生成** | 根据步骤定义，AI 生成 Service/Controller 代码 |
| **场景补全** | 分析现有场景，AI 建议补充遗漏的边界条件 |
| **场景重构** | 当业务变更时，AI 帮助更新场景文件 |

---

## 十一、最佳实践

1. **场景从业务角度写**：不要写技术细节，写用户视角的行为
2. **一个场景一个行为**：不要在一个 Scenario 中混合多个独立行为
3. **使用 Background 减少重复**：多个场景共用的前置条件放到 Background
4. **场景即文档**：命名要有意义，让非技术人员能看懂
5. **不要替代单元测试**：BDD 做端到端验证，TDD 做单元验证
6. **AI 生成后人工审查**：确保 AI 没有误解业务意图