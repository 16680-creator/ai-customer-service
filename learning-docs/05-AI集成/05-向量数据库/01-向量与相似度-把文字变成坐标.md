# 01-向量与相似度：把文字变成坐标

> 读完你会明白：为什么一串小数能代表一段话的意思，"距离近"为什么就等于"意思相近"，
> 以及余弦相似度那个 0.82 到底该怎么解读。这是整个模块的地基，[02 索引算法](./02-索引算法-向量检索怎么从全表扫描里活下来.md) 全部建立在本文的概念上。

---

## 一、先看传统搜索栽在哪

假设你的客服系统里有这三条知识：

```
A. 手机使用一小时后机身温度达到 45℃，属于正常现象
B. iPhone 发烫是怎么回事
C. 今天天气不错，适合出游
```

用户问：**"我的苹果手机很烫"**，关键词搜索（LIKE / BM25）的表现：

| 方案 | 结果 | 为什么 |
|---|---|---|
| `LIKE '%烫%'` | 命中 B，**漏掉 A** | A 里写的是"温度""发烫"不同字 |
| 分词 + BM25 | 命中 A、B，但 B 排前面 | 靠字面重合度打分，"很烫"和"温度 45℃"没有字面交集 |
| 同义词词典 | 勉强能救 | 你得提前想到"烫=发热=温度高=发烫"，词典维护不完 |

> 本表两个名词：`LIKE` 是 SQL 的模糊匹配语法；**BM25** 是 Elasticsearch 这类搜索引擎默认的
> 关键词打分算法（一个词出现越多、这个词本身越稀有，得分越高）——它们都只看**字面**。

**根本问题**：关键词搜索匹配的是**符号**，不是**意义**。字面不一样但意思一样的东西，它天生抓不到。

向量的思路是完全换个方向：**先把语义翻译成坐标，然后比较坐标。**

---

## 二、向量是什么：从一维走到 1024 维

### 2.1 一维：一个数字就能排序

如果我只关心"食物的甜度"，可以用一个数字表示：

```
柠檬 3 ─── 苹果 6 ──── 西瓜 8 ── 糖 10
```

这就是一个 **1 维向量**。"相似度"就是数值差的大小——西瓜和苹果比糖和苹果更"接近"。

### 2.2 二维：一个点，距离开始有意义

现在加一个维度"酸度"：

```
酸度
 9 │ 柠檬 ●
   │
 5 │      ● 酸奶
   │
 1 │              ● 西瓜        ● 糖
   └────────────────────────────── 甜度
     1    3    5    7    9   10
```

每个食物变成一个**点**。"两个食物像不像"变成"两个点离得近不近"——几何距离 = 语义距离。
**这就是向量检索的全部直觉**，剩下的高维只是这个画面的推广。

### 2.3 高维：维度不是"人类能理解的特征"

关键认知转变来了。上面"甜度/酸度"是我们**手工定义**的维度，所以能理解。
但真实的 Embedding 不是这样工作的：

```
"手机发热" → [0.23, -0.45, 0.67, 0.12, ..., 0.88]   ← 1024 个数
```

**这 1024 个数字里，第 337 维代表什么？答案是：没有人知道。**

- 它不是人工设计的特征（不是"温度""品牌""情感极性"这类可解释标签）
- 它是模型在海量文本上**自己学出来**的一种坐标划分方式
- 训练目标只有一个：**意思接近的文本，坐标也接近；意思不同的，坐标离远**

所以你不需要理解每一维的含义，只需要相信：**在这个 1024 维空间里，距离关系是语义关系的近似映射。**

> 一个常见误解："1024 维空间我们能想象" —— 不能。但数学上"距离""夹角"的定义不依赖你能不能画出来，
> 公式照样成立（本文 §四 会实际算）。高维唯一带来的反直觉影响见 §5.3。

### 2.4 向量的存储大小（这个数字后面会反复用到）

向量通常用 `float32`（4 字节）存每个维度：

```
单条向量字节数 = 维度数 × 4 B

512 维   →  2 KB
1024 维  →  4 KB        ← 本模块大部分算例用这个
1536 维  →  6 KB
3072 维  →  12 KB
```

所以 100 万条 1024 维向量：`1,000,000 × 4 KB ≈ 4 GB`——**这还只是向量的裸数据**，没算索引结构和元数据。
记住这个 4 GB，[02 篇](./02-索引算法-向量检索怎么从全表扫描里活下来.md) 会用它解释为什么暴力检索慢、[03 篇](./03-选型与容量规划.md) 会用它做容量规划。

---

## 三、Embedding：文字是怎么变成那一串数字的

### 3.1 完整流程

```
"我的苹果手机很烫"
      │
      ▼ ① 切 Token（模型自己的词表，中文常见 1 字 1~2 token）
[我] [的] [苹] [果] [手] [机] [很] [烫]
      │
      ▼ ② 查表得到每个 token 的初始向量（Token Embedding）
[[0.12,...], [0.88,...], ..., [0.31,...]]      ← 8 个向量，每个 d 维
      │
      ▼ ③ 送入 Transformer Encoder（多层自注意力）
   ★ 核心步骤：每个 token 都"看一遍"其他所有 token，融合上下文
     于是"苹果"这个 token 的向量被调整成"水果"义还是"手机品牌"义
      │
      ▼ ④ 池化 Pooling：把 8 个 token 向量压成 1 个句子向量
   常见做法：Mean Pooling（对应位置求平均）或取 [CLS] 位置
      │
      ▼ ⑤ L2 归一化（可选但强烈推荐）：把向量长度拉成 1
   [0.23, -0.45, ..., 0.88]  →  长度 = 1 的同方向向量
```

> 本流程的新名词，一次性解释清楚：
> - **Token**：模型处理文本的最小单位，约等于半个到一个汉字；模型的输入上限和 API 计费都按 token 数算
> - **Token Embedding**：模型预训练时学好的那张"token → 向量"对照大表，② 就是查这张表
> - **Transformer Encoder / 自注意力**：当前主流的神经网络结构；"自注意力"就是 ③ 说的"每个 token 都看一遍其他所有 token"
> - **[CLS]**：BERT 类模型在句首额外放的一个特殊 token，它的输出向量常被直接当作整句表示

### 3.2 两个必须理解的点

**① 为什么"上下文"重要？**
"苹果发布会"和"苹果很甜"里的"苹果"，初始向量是同一个，但经过注意力层后会被调整到不同位置。
这就是 Embedding 模型比"查同义词表"强的地方——**它按上下文消歧**。

**② 为什么第 ⑤ 步归一化这么关键？**
归一化后所有向量都落在**单位球面**上（半径为 1 的球的表面——所有长度为 1 的向量都正好分布在这个"壳"上），长度全部为 1。此时：

```
余弦相似度 = 点积          （因为分母 ‖A‖·‖B‖ = 1）
欧氏距离 与 余弦相似度 单调等价   （一个变小另一个就变大，排序结论完全一致）
```

**三种度量的选择分歧直接消失了。** 这就是为什么主流 Embedding 模型（bge、m3e 是开源中文模型，
text-embedding-3 是 OpenAI 的商用模型）都建议归一化后用内积/余弦。很多向量库里的 `IP`（内积）索引，实际配套做的就是归一化。

### 3.3 工程上的三个坑

| 坑 | 现象 | 处理 |
|---|---|---|
| **切块（chunking）** | 一篇 50 页 PDF 不能整体变成一个向量，语义会被"平均"糊掉 | 切成 300~800 token 的块，每块一个向量。见 [`../02-RAG全栈实战/01-RAG检索增强生成.md`](../02-RAG全栈实战/01-RAG检索增强生成.md) |
| **模型/版本不能混** | v1 模型生成的向量和 v2 的**在同一个空间里没有可比性**，距离出来是噪声 | 换模型 = 全量重建。详见 [04 §三](./04-生产运维-向量库跑起来之后才会遇到的事.md) |
| **调用成本** | 每次检索都要先把 query 向量化，高频 FAQ 重复花这笔钱 | 缓存 query 向量与结果。本项目 `CachingEmbeddingModel` 就是装饰器实现 |

> **RAG**（Retrieval-Augmented Generation，检索增强生成）是本模块反复出现的应用场景：
> 先从知识库检索相关资料，再把资料连同问题一起交给大模型，让它"照着资料回答"而不是凭空编。

---

## 四、相似度度量：三个公式与它们的真实差别

### 4.1 定义

给两个向量 A、B：

| 名称 | 公式 | 值域 | 越小/越大说明什么 |
|---|---|---|---|
| **余弦相似度** | \(\dfrac{A \cdot B}{\|A\|\,\|B\|}\) | [-1, 1] | **越大**越相似 |
| **欧氏距离**（L2） | \(\sqrt{\sum_i (a_i-b_i)^2}\) | [0, ∞) | **越小**越相似 |
| **点积 / 内积**（IP） | \(\sum_i a_i b_i\) | 取决于向量 | **越大**越相似 |

`‖A‖` 是向量的**长度**（各维度平方求和再开根号），也叫 L2 范数。

### 4.2 手算一遍（这是本文最重要的一节，别跳）

用极简的 2 维版本，方便你跟着按计算器。假设模型输出（未归一化）：

```
A = "手机发热"   → [0.9, 0.1]
B = "iPhone烫手" → [0.8, 0.3]
C = "今天天气"   → [0.1, 0.95]
```

**第一步：算长度**

```
‖A‖ = √(0.9² + 0.1²) = √(0.81 + 0.01) = √0.82  = 0.9055
‖B‖ = √(0.8² + 0.3²) = √(0.64 + 0.09) = √0.73  = 0.8544
‖C‖ = √(0.1² + 0.95²)= √(0.01 + 0.9025)= √0.9125 = 0.9553
```

**第二步：算点积（对应位置相乘再全部加起来）**

```
A·B = 0.9×0.8 + 0.1×0.3 = 0.72 + 0.03 = 0.75
A·C = 0.9×0.1 + 0.1×0.95 = 0.09 + 0.095 = 0.185
```

**第三步：余弦 = 点积 ÷ 两长度之积**

```
cos(A,B) = 0.75 / (0.9055 × 0.8544) = 0.75 / 0.7737 = 0.969   ← 很相似 ✅
cos(A,C) = 0.185 / (0.9055 × 0.9553) = 0.185 / 0.8651 = 0.214  ← 不相似 ✅
```

**欧氏距离对照：**

```
d(A,B) = √((0.9-0.8)² + (0.1-0.3)²) = √(0.01 + 0.04) = 0.224   ← 小 = 近
d(A,C) = √((0.9-0.1)² + (0.1-0.95)²) = √(0.64 + 0.7225) = 1.167 ← 大 = 远
```

两种度量在这组数据上给出了**一致的结论**（A 与 B 相似）。这在实践中是常态：
只要向量做过归一化，选哪个都不会错。

### 4.3 它们什么时候会分道扬镳

把 B 整体缩小 10 倍，方向不变：

```
A  = [0.9, 0.1]
B' = [0.09, 0.03]      ← 和 B 方向完全相同，只是长度是 B 的 1/10

cos(A,B') = 0.084 / (0.9055 × 0.0949) = 0.978   ← 几乎没变！
d(A,B')   = √(0.6561 + 0.0049) = 0.813           ← 变成 B 的 3.6 倍，"看起来很远"
A·B'      = 0.084                                  ← 从 0.75 掉到 0.084，"看起来不相似"
```

**结论一目了然：**

| 度量 | 在乎方向 | 在乎长度（模） |
|---|---|---|
| 余弦相似度 | ✅ | ❌ 完全忽略 |
| 欧氏距离 | ✅ | ✅ |
| 点积 | ✅ | ✅ |

所以选择规则是：

```
你的向量归一化了吗？
├── 是（绝大多数 Embedding 模型都提供归一化输出）
│      → 三者等价，用余弦或点积都行，选库默认支持的那个
└── 否
       ├── 长度携带信息（如 TF-IDF 向量——一种按"词频 × 词稀有度"加权的老式文本向量、推荐场景的用户活跃度权重）→ 用点积或欧氏
       └── 长度是噪声（纯语义文本向量，长文档向量自然更长）→ 用余弦，或先归一化
```

> **面试加分点**：不要说"文本就用余弦"。要说"文本 Embedding 通常归一化，此时余弦与内积等价，
> 我选哪个取决于向量库对哪个度量支持得更好（比如 HNSW 通常原生支持 IP/L2，要余弦就得先归一化再走 IP）"。

### 4.4 阈值怎么定（新手最容易照抄 0.7 的地方）

Spring AI 之类框架里常见的 `similarityThreshold(0.7)` **不是什么行业标准值**。正确做法：

```
1. 准备一批真实用户问题（50~200 条）
2. 人工标注每条问题在知识库里的"正确来源文档"（也标出"库里确实没有答案"的）
3. 跑检索，画出两组分布直方图：
      命中的相关性分数分布  vs  误召的分数分布（误召 = 把不相关的也捞进来了）
4. 阈值取两个分布的交叉点偏右 —— 让误召率达标
5. 上线后监控空结果率，分布会随语料（你库里存的全部文本数据）变化漂移，定期重标
```

**一个可以直接用的经验起点**（bge 系中文模型、归一化后、余弦）：

| 分数区间 | 大致含义 | 建议动作 |
|---|---|---|
| > 0.85 | 几乎是同一件事的不同说法 | 可当作近似重复用于去重/缓存命中 |
| 0.65 ~ 0.85 | 同一主题，相关 | RAG 召回的正常区间 |
| 0.4 ~ 0.65 | 弱相关，可能引入噪声 | 需要 Rerank 二次判别 |
| < 0.4 | 基本无关 | 丢弃 |

表中的 **Rerank（重排，也叫精排）** 指：用一个小而准的模型，把宽召回的 20 条候选逐条重新打分排序、
只留前 5——它和阈值是配套决策，见下文与 [02 篇](./02-索引算法-向量检索怎么从全表扫描里活下来.md)。

**但请用你自己标定的数字覆盖这张表。** 本项目的 `recall-threshold: 0.3` 就明显低于"通用直觉"，
因为它的策略是**宽召回（top-20）+ Rerank 精排**——阈值只负责挡掉完全无关的，交给精排去排序。
**阈值不是孤立参数，它和你后面有没有 Rerank 是一个整体决策。**

### 4.5 距离 vs 相似度：向量库 API 的坑

两个方向相反的约定，容易在代码里搞混：

```
"距离"型 API（越大越不相似）： L2 距离
"相似度"型 API（越大越相似）： 余弦相似度、内积分数
```

有些库（如 Chroma）返回的是 `distances` 字段，但它内部按集合的 `distance_fn` 配置决定语义——
同一个字段名可能是距离也可能是相似度。**接入任何向量库前，先用两个已知相关的文本探一下返回值的单调性。**

---

## 五、维度：是不是越高越好

### 5.1 成本是线性的，收益不是

```
维度翻倍  →  存储翻倍、每次距离计算翻倍、索引构建变慢
维度翻倍  →  检索质量提升？ 通常只有个位数百分点，甚至下降
```

| 维度 | 单条 | 100 万条裸数据 | 典型模型 |
|---|---|---|---|
| 384 | 1.5 KB | 1.5 GB | all-MiniLM-L6-v2 |
| 768 | 3 KB | 3 GB | m3e-base |
| 1024 | 4 KB | **4 GB** | **bge-m3、bge-large-zh** |
| 1536 | 6 KB | 6 GB | text-embedding-3-small |
| 3072 | 12 KB | 12 GB | text-embedding-3-large |

### 5.2 Matryoshka（套娃）表示学习：一个省钱的现代特性

OpenAI 的 text-embedding-3 系列和部分开源模型支持训练时让**前缀维度也自成有效向量**：

```python
# 3072 维向量，直接截断前 512 维当作 512 维用
vec_3072 = embed(text)                  # [x1, x2, ..., x3072]
vec_512  = vec_3072[:512]               # 存储成本降到 1/6，质量通常只降 1~3 个点
```

工程含义：**用一个大模型生成高质量向量，用截断后的低维版本承担线上成本**，
把完整向量留给精排或离线分析。这是近三年向量成本控制最实用的手段之一。
（是否支持取决于模型，用之前查模型卡，不要假设。）

### 5.3 高维的一个反直觉现象：距离差异会"糊掉"

在低维空间里，"最近邻"和"第 100 名"的距离差得很明显。维度极高时，
所有点对之间的距离会**趋同**（距离集中现象）。后果：

- Top-K 的排序变得不稳定，多一个维度少一个维度结果就翻
- **阈值失效**——你没法用一条绝对分数线区分"相关"和"不相关"

这不是"维度高就一定差"（模型训练时已经在优化这个），而是提醒你：
**判断质量只能靠标注集实测，不能靠"维度高所以应该更好"的直觉。**

---

## 六、收束：向量数据库到底在解决什么问题

现在把问题形式化写出来——你会看到它是一个非常干净的数学问题，[02 篇](./02-索引算法-向量检索怎么从全表扫描里活下来.md) 的全部算法都在攻这一个式子：

> **给定** N 条 d 维向量 \(v_1, v_2, \dots, v_N\)，和一条查询向量 \(q\)；
> **求** 与 \(q\) 距离最近的 K 条向量的 id（及距离）。
> **约束** N 可达 10 亿；单次查询延迟要求 < 50ms；全部数据放不进单机内存。

最朴素的解法（**Flat / 暴力检索**）：

```python
scores = [cosine(q, v) for v in all_vectors]   # 每一条都要算一次
return top_k(scores)
```

复杂度 \(O(N \cdot d)\)（O 记号读作"量级"：计算量正比于 N×d 这个乘积——N 翻倍就慢一倍），代入真实数字：

```
N = 100 万, d = 1024
计算量 = 1e6 × 1024 × 2 ≈ 2×10⁹ 次浮点运算
内存读取 = 必须把 4 GB 向量全部读一遍        ← 真正的瓶颈在这里
按内存带宽 30 GB/s 算 → 单次查询下限 ≈ 130 ms
```

**慢的根源往往不是"算不过来"，而是"数据搬不动"**——CPU 算得再快也得等内存把 4 GB 送过来。
理解这一点，才能理解后面所有索引算法的设计动机：

```
HNSW   →  只访问图中的一小撮向量（搬运量从 4 GB 降到几 MB）
IVF    →  只搜索少数几个簇（先粗筛，减少搬运）
PQ     →  把 4 GB 压成 64 MB 放内存（直接让搬运量小一个数量级）
```

这就是下一篇的全部内容。

---

## 七、动手练习（10~25 分钟，做完才算掌握）

```python
# pip install numpy
import numpy as np

def cosine(a, b):
    return float(np.dot(a, b) / (np.linalg.norm(a) * np.linalg.norm(b)))

A = np.array([0.9, 0.1]); B = np.array([0.8, 0.3]); C = np.array([0.1, 0.95])
print(cosine(A, B), cosine(A, C))     # 应得约 0.969 / 0.214，验证 §4.2 的手算
```

1. **验证等价性**：把 A、B、C 各自 L2 归一化（`v / np.linalg.norm(v)`），
   再分别算余弦、点积、欧氏距离——确认三者对"A 和谁更近"给出**相同答案**。

2. **制造分歧**：构造一个与 B 同方向但长度为 1/10 的向量 `B2`，
   观察余弦几乎不变、欧氏距离大幅变化。用自己的话写下"什么场景下这会坑我"。

3. **感受维度成本**：随机生成 10 万条 1024 维单位向量
   （`np.random.rand(100000, 1024).astype('float32')` 再归一化），
   用 `arr.nbytes` 看实际字节数，和 §5.1 表格对一下。
   然后写一个循环暴力算它与某条查询的 Top-10，**记录耗时**——
   这个数字会在下一篇成为你的比较基准线。

4. **思考题**：为什么"一篇 50 页文档压成一个向量"效果会很差？
   （提示：Mean Pooling 是求平均，两个不同主题的段落平均后得到的是一个"谁都不像"的坐标。）

---

## 八、面试要点速记

1. **一句话定义**：向量检索 = 把语义映射到高维坐标，用几何距离近似语义相似度，求 Top-K 最近邻。
2. **Embedding 五步**：切 token → 查表 → Transformer 注意力融合上下文 → 池化压成句向量 → 归一化。
3. **三度量选择**：归一化后三者等价；未归一化时，长度有意义用点积/欧氏，无意义用余弦。
4. **能手算余弦**：面试官常要求你写一个 cosine 函数，或对两个小向量手算——§4.2 的步骤要能默出来。
5. **阈值不是抄来的**：必须用自己标注的数据标定分布；且要和"有没有 Rerank"联动决策。
6. **换 Embedding 模型必须全量重建**：不同模型的向量空间不可比，这是高频踩坑题。
7. **维度换质量不换比例**：成本线性涨，质量个位数涨；有 Matryoshka 模型可以考虑截断省钱。
8. **暴力检索慢在内存带宽不在计算**：百万条 1024 维查询要读 4 GB，这句答出来就赢过一半候选人。

---

## 九、延伸阅读

- 本模块下一篇：[02-索引算法](./02-索引算法-向量检索怎么从全表扫描里活下来.md)（把 §六 的那个式子拆开讲）
- 复杂度基础：[`../../11-数据结构与算法/01-复杂度分析与算法思维.md`](../../11-数据结构与算法/01-复杂度分析与算法思维.md)
- 应用场景（RAG 全链路）：[`../02-RAG全栈实战/03-RAG向量检索实战.md`](../02-RAG全栈实战/03-RAG向量检索实战.md)
- 速记卡形式的问答：[`../面试题/04-向量数据库与Embedding.md`](../面试题/04-向量数据库与Embedding.md)
