# 02-索引算法：向量检索怎么从全表扫描里活下来

> 本模块核心篇。读完你能在白纸上推导：为什么 HNSW 快、它用什么换的、`M` 和 `efSearch` 分别拧哪个旋钮，
> 以及拿到一个真实需求时怎么选索引而不是抄默认值。

---

## 一、基线：暴力检索，以及它什么时候真的够用

[01 篇 §六](./01-向量与相似度-把文字变成坐标.md) 留下的问题：

```
N = 100 万, d = 1024  →  每次查询要搬 4 GB 内存  →  单次下限 ~130 ms
```

先说清楚一个**新手最容易过度反应**的点：**如果数据量小，暴力检索就是最优解，不要上索引。**

为什么？因为工程实现里的暴力检索不是"老老实实循环"，而是**矩阵乘法 + SIMD**：

```python
import numpy as np
# V: (N, 1024) float32，全部向量；q: (1024,) 查询向量
scores = V @ q                    # 一次 BLAS 调用算完 N 条内积
top_k = np.argpartition(scores, -10)[-10:]
```

`V @ q` 走的是高度优化的 BLAS（AVX-512 / AMX），N=10 万、d=1024 也就是 1×10⁸ 次乘加，
现代 CPU 亚毫秒到几毫秒级——**而且精度是 100%，没有召回损失**。

| 数据规模（1024 维） | 裸内存    | 矩阵化暴力检索量级 | 建议                    |
| ------------ | ------ | --------- | --------------------- |
| 200 条        | 800 KB | **微秒级**   | 暴力。放 L1/L2 缓存，索引反而是负担 |
| 1 万条         | 40 MB  | ~1 ms     | 暴力通常仍是最优选             |
| 10 万条        | 400 MB | 2~10 ms   | 暴力可用，开始比较索引           |
| 100 万条       | 4 GB   | 100 ms+   | **必须上 ANN**           |
| 1 亿条         | 400 GB | 分钟级       | ANN + 分片 + 量化，单机做不到   |

> 本项目的语义缓存就是第一行的情况：缓存条目上限 200 条，对每条做 O(n) 余弦扫描，
> **刻意不引入 HNSW**。这不是"没来得及优化"，而是判断过 n 太小、索引的构建/内存/维护成本
> 换不回任何延迟收益，还可能因近似漏掉命中。这是个能在面试上讲三分钟的好例子，见 §九。

---

## 二、ANN：先把这笔交易说清楚

**ANN = Approximate Nearest Neighbor，近似最近邻。** 与它相对的是精确最近邻（KNN/Flat）。

所有 ANN 算法做的都是同一个交易：

```
用「一点精度」换「数量级的速度 + 内存」
```

### 2.1 精度怎么量化：Recall@k

```
Recall@k = |真实 top-k 与 ANN 返回 top-k 的交集| / k
```

**必须跟"谁"比**：分母那个"真实 top-k"是同数据集上暴力检索算出的精确答案。
所以 Recall@10 = 0.95 的准确含义是：**平均每 10 个返回结果里，有 9.5 个是真的前 10 名，漏了约 0.5 个。**

### 2.2 关键警示：期望值掩盖尾部风险

```
"我们的 Recall@10 有 98%"  ← 这是平均值，听起来很好
"但用户这次问的那条唯一正确答案，恰好是那 2% 被漏掉的"  ← 这是单次事故
```

RAG 场景尤其致命：**如果知识库里只有 1 条文档能回答问题，而它被 ANN 漏掉了，
那么这次问答 100% 会胡说八道**（模型没资料可引用）。

因此实践中的对策：

- **召回阶段宽一点**（top-20 甚至 top-50），把漏检风险摊薄，再用 Rerank 收窄 → 本项目 `recall-top-k: 20`
- **必须用业务标注集测 Recall，不能只看库官方 benchmark** —— 你的向量分布和公开数据集不是一回事
- 对"必须有答案"的关键场景，考虑混合检索（关键词 + 向量）互为兜底，见 [`../02-RAG全栈实战/05-混合检索实战-ES-BM25与向量RRF融合.md`](../02-RAG全栈实战/05-混合检索实战-ES-BM25与向量RRF融合.md)

### 2.3 ANN 为什么能少算 yet 还准

直觉：**相似性是有"邻域传递性"的**——如果 A 像 B、B 像 C，那 A 大概率像 C。
所有索引本质上都在构造某种"可以从近点跳到更远点"的结构，从而**只探测全库的极小一部分**就 confident 地找到最近邻。

三种主流构造方式：

| 思路                              | 代表算法             | 一句话           |
| ------------------------------- | ---------------- | ------------- |
| **图**：把向量连成网络，从入口贪心走向目标         | HNSW、NSG、DiskANN | 靠"跳跃式下降"少访问节点 |
| **聚类分桶**：先粗筛桶，只在少数桶内精算          | IVF              | 靠"不看绝大多数桶"    |
| **压缩编码**：把向量本身变得极小，让全量都能塞进内存快速扫 | PQ、AQ/ScaNN      | 靠"搬运量小几个数量级"  |

下面逐个拆。

---

## 三、HNSW：分层可导航小世界图（面试最常考）

### 3.1 先理解它的两个母体

**母体一：跳表（Skip List）**——你在有序链表上加"快速通道层"：

```
层2:  1 ─────────────► 9               ← 稀疏，步长很大
层1:  1 ──────► 5 ───► 9               ← 中等
层0:  1 ─► 3 ─► 5 ─► 7 ─► 9 ─► 11      ← 完整链表
查 7：从层2 走到 9（发现过头）→ 降层到 1（9 处）→ 降层到 0 → 回退到 5 → 走到 7
```

查找从 O(n) 变成 O(log n)：**先大步近似定位，再小步精确搜索。**

**母体二：小世界图（社交网络里的"六度分隔"）**——图中既有大量短边（熟人），
又有少量长边（跨城朋友），任意两点间跳 5~6 次就能到。

**HNSW = 把跳表的"分层"和小世界图的"短长边"结合起来。**

### 3.2 结构

```
层 2（稀疏，只有很少节点）:      A ──────────────────── F
                                 │                      │
层 1:                            A ───── D ───── F      │
                                 │       │      │      │
层 0（所有节点都在这层）:  A─B─C─D─E─F─G─H─I─J─K─L ...

性质：① 每个节点随机分配一个"最高所在层"（几何分布，层越高节点越稀少）
      ② 节点在它及以上的每一层都有邻居连线
      ③ 上层边长（跨得远），下层边短（搜得细）
```

### 3.3 一次查询怎么走（**面试要能在白板画出来**）

```
1. 从"入口点"（entry point，全图最高层的某个节点）开始
2. 在当前层做贪心搜索：看所有邻居，谁离 q 最近就跳过去，反复直到
   "邻居里没有比当前更近的"（局部最优）
3. 降一层，从刚才那个节点继续第 2 步 —— 此时只是"粗定位"，不返回结果
4. 到达第 0 层后，改用 beam search：不再只走 1 个最优，而是维护一个
   大小为 efSearch 的候选队列，扩大搜索范围
5. 从第 0 层的候选集里返回最近的 top-K
```

**关键理解**：上层是"粗定位"（快速飞到目标区域），第 0 层的 `efSearch` 才决定"精搜多仔细"。

### 3.4 三个参数，各拧什么（背下来不如理解）

| 参数                 | 含义                        | 调大的效果                       | 代价                                    |
| ------------------ | ------------------------- | --------------------------- | ------------------------------------- |
| `M`                | 每个节点的最大邻居数（第 0 层通常是 `2M`） | 图更"密"，连通性好 → 召回↑、可支撑更大 topK | **内存↑、构建变慢**。常用 16/32；高维大 topK 才上 48+ |
| `efConstruction`   | **建索引时**每层的候选队列宽度         | 图质量更高（邻居选得更优）→ 查询召回上限↑      | **构建时间↑**（一次性成本，通常 100~500）           |
| `efSearch`（或 `ef`） | **查询时**第 0 层的候选队列宽度       | 召回↑、延迟同步↑                   | 每次查询的**在线成本**，可调可热改（这是它最实用的地方）        |

**必须记住的一句话**：
`efConstruction` 是**建库时一次性投入**，决定"召回率天花板"；
`efSearch` 是**每次查询的实时开销**，决定"你愿意贴近天花板多少"。
调错对象是最常见的浪费——想提召回，先确认是天花板问题（efConstruction）还是没贴近（efSearch）。

约束：`efSearch ≥ topK`（否则连候选都凑不够），一般 `efSearch = 2~4 × topK` 起步。

### 3.5 优缺点（诚实清单）

| 优点                       | 缺点                                             |
| ------------------------ | ---------------------------------------------- |
| 高维（d 50~1000）下延迟/召回表现顶级  | **原始向量必须常驻内存**，内存墙来得早                          |
| 无需训练（不像 IVF 要先跑 k-means） | 构建慢，大图可能几十分钟到数小时                               |
| 支持增量插入                   | 删除只能"墓碑标记"，图结构会退化，需重建                          |
| 查询延迟稳定                   | 带**过滤条件**时性能可能崩塌（过滤掉的路径不可达），需专门的 filtered-HNSW |
| 参数只有 3 个，容易解释            | 数据分布大变化后，老图质量下降且不可自动修复                         |

### 3.6 内存到底多多少（新手常被这个坑到）

```
总内存 ≈ 原始向量 + 图边表 + ID/元数据 + 分配器碎片

原始向量：1024 × 4 B = 4096 B
图边表  ：每节点平均邻居数 ≈ M，每邻居存 4 B id；第 0 层上限 2M
          M=16 → 约 16 × 2 × 4 = 128 B（含上层则总量略高）
元数据  ：你的标量字段（tenant_id / category / 时间戳...）—— 常常被低估的大头
```

**量级结论**：M=16 的 HNSW，索引结构本身约为向量本体的 **5%~15%**；
但**加上丰富元数据和内存分配开销，实际总占用通常是裸向量的 1.5~2 倍**。
所以"100 万条 1024 维 = 4 GB"这个估算，做容量规划时**请乘以 2**。详见 [03 §三](./03-选型与容量规划.md)。

---

## 四、IVF：倒排文件索引（聚类分桶）

### 4.1 核心思想

思路极其朴素，接近人类找东西的方式：

```
建库阶段：
  1. 用 k-means 把全部 N 条向量聚成 nlist 个簇，得到 nlist 个"质心"
  2. 建一张倒排表：质心1 → [向量id 5, 892, 30115, ...]
                    质心2 → [向量id 7, 44, 8812, ...]

查询阶段：
  1. 先算 q 与 nlist 个质心的距离（只有 nlist 次计算，很便宜）
  2. 挑出最近的 nprobe 个簇
  3. 只在这 nprobe 个簇内部做（相对）精确比较
```

**加速比 ≈ nlist / nprobe**。100 万条、nlist=4096、nprobe=16 → 只扫约 1/256 的数据。

### 4.2 与倒排索引的类比（如果你熟搜索会更理解）

|        | 全文检索倒排       | IVF                 |
| ------ | ------------ | ------------------- |
| 桶是什么   | 一个词 term     | 一个向量簇（质心）           |
| 桶怎么来   | 分词器决定        | k-means **训练**出来    |
| 桶里存什么  | 含该词的文档 id 列表 | 落在该簇的向量 id 列表       |
| 查询怎么剪枝 | 只取含查询词的桶     | 只取离 q 最近的 nprobe 个桶 |

### 4.3 两个参数

| 参数       | 含义                 | 经验值                                                         |
| -------- | ------------------ | ----------------------------------------------------------- |
| `nlist`  | 簇的数量（**建库时定死**）    | 常用区间 `√N` ~ `16√N`；百万级常取 1024~4096。太小则每桶过大剪枝无效，太大则训练不充分、空桶多 |
| `nprobe` | 查询时探测几个簇（**可在线调**） | 从 8/16 起，追召回往上加；它就是 IVF 版的 `efSearch`                       |

**召回损失从哪来（面试重点）**：真正的最近邻可能在**地理上相邻的另一个簇**里，
而你没探那个簇。`nprobe=1` 时这类漏检最严重；这也是为什么质心边界附近的向量最容易出问题。

**改进手段**：

- 多质心分配（redundant assignment）：一个向量同时进多个邻近簇 → 召回↑、内存↑
- 残差量化（见 §五）配合，降低量化误差
- 混合检索：IVF 的漏检由关键词通道兜底

### 4.4 变体：桶里存什么决定精度和内存

```
IVF-Flat  → 桶内保存原始向量，簇内精确计算     召回最高，内存 = 全量原始向量
IVF-PQ    → 桶内只存 PQ 压缩码（如 64 B/条）   内存降 64 倍，精度受量化损失
IVF-SQ8   → 每维压成 1 字节（8-bit 标量量化）  内存降 4 倍，损失较小，最实用
```

**`IVF-SQ8` 常被低估**：只把 `float32` 每维线性映射到 `uint8`，内存直接 4 倍压缩，
在很多中文 Embedding 数据集上 Recall@10 只掉 1~2 个点。**这是性价比最高的第一步优化。**

---

## 五、PQ：乘积量化（把向量本身压小）

### 5.1 名字里的"乘积"是什么意思

不是"乘起来"，而是**笛卡尔积**：把长向量切成若干段，每段各自独立量化，
总码本大小 = 各段码本的**乘积**。

```
原始 1024 维，切成 m = 64 段，每段 1024/64 = 16 维

对第 1 段的全体 16 维子向量跑 k-means → 256 个子中心 → 编号 0~255（8 bit）
对第 2 段 ... 第 64 段，各做一遍

于是每条向量被编码成 64 个字节：
[137, 4, 219, 88, ..., 61]  ← 64 字节 = 512 bit
```

**压缩比**：4096 B → 64 B，**64 倍**。100 万条从 4 GB 降到 **64 MB**。

### 5.2 为什么压缩后还能算距离

两个层面：

1. **近似距离**：用子中心代表子向量，两个编码的距离 = 它们子中心之间的距离（可预存查找表）
2. **ADC（非对称距离计算）查询加速**——这是 PQ 真正精巧的地方：

```
① 把库内向量全部量化成码（丢精度）
② 但查询向量 q 【不量化】——保持精确
③ 预计算：q 的每段 与 该段 256 个子中心 的距离 → 一张 64×256 的查找表
④ 之后对任意一条编码，只需在表里查 64 个数相加
   → 单条距离计算 = 64 次查表 + 64 次加法，且天然 SIMD 友好
```

"非对称"指的就是**库量化、查询不量化**这个不对称设计，它比两边都量化误差更小。

### 5.3 代价与改进

| 问题                              | 改进手段                                                     |
| ------------------------------- | -------------------------------------------------------- |
| 量化误差随维度切分方式变化，"重构误差最小"≠"检索误差最小" | **OPQ**：先学一个旋转矩阵再切段                                      |
| 64 B 精度不够                       | **残差量化**：存"原向量 − 一级近似"再量化一次（2~3 级），精度换内存                 |
| 单靠 PQ 召回不足                      | 组合 IVF 粗筛 + **rerank 用原始向量**（原始向量放 SSD，只对最终 top-100 做重排） |

> **面试高频陷阱**："PQ 压缩了什么？" 不要只答"压缩向量"。要说：
> **切段 + 每段独立码本 + 每段 1 字节编码**，以及查询侧用 ADC 查找表把距离计算变成查表加法——
> 后者才是它既省内存又快的原因。

---

## 六、其他你会遇到的名字（知道定位即可）

| 算法                           | 归属     | 定位                                                |
| ---------------------------- | ------ | ------------------------------------------------- |
| **DiskANN / Vamana / SPANN** | 图 + 磁盘 | 图结构和压缩码放内存，原始向量放 SSD，**单机十亿级**的主流答案               |
| **ScaNN**                    | 量化     | Google；核心洞察是优化**内积误差**而非重构误差（各向异性量化），对 MIPS 场景特别强 |
| **NSG**                      | 图      | 理论更接近"单调图"，搜索质量好但构建极慢，实践中常被 HNSW 取代               |
| **ANNOY**                    | 随机投影树  | Spotify 开源，**只读**（建完不能加），适合离线批量 + 内存映射；新系统少选      |
| **CAGRA**                    | GPU 图  | NVIDIA；GPU 向量检索的高吞吐路线                             |
| **SCANN/IVF-PQ-GPU**         | GPU 量化 | 亿级高吞吐场景，配合 `faiss-gpu`                            |

**一个容易忽略的分类维度**：向量索引的**构建是否可增量**。
ANNOY 完全不行；HNSW 可增量但会退化；IVF 需要先训练质心才能加数据。
你的业务如果是"每天新灌 100 万条"，这一条比召回率更决定选型。

---

## 七、汇总对比表（建议打印贴墙）

以 **100 万条 × 1024 维 float32（裸数据 4 GB）**、topK=10 为基准：

| 方案                                  | 常驻内存（量级）                       | 单次查询延迟（量级）      | Recall@10（量级） | 构建耗时      | 增删         |
| ----------------------------------- | ------------------------------ | --------------- | ------------- | --------- | ---------- |
| **Flat 暴力**                         | ~4 GB                          | 100 ms+（内存带宽瓶颈） | **100%**      | 无         | 随意         |
| **HNSW**（M=16, efC=200, efS=64）     | 6~8 GB                         | **1~5 ms**      | 95~98%        | 数十分钟~小时   | 可增量插，删除留墓碑 |
| **IVF-Flat**（nlist=4096, nprobe=16） | ~4.2 GB                        | 5~15 ms         | 85~95%        | 分钟级（含训练）  | 增删较好       |
| **IVF-SQ8**                         | ~1.2 GB                        | 5~15 ms         | 83~94%        | 分钟级       | 增删较好       |
| **IVF-PQ**（m=64）                    | **~0.3 GB**（码 64 MB + 质心 + 缓冲） | 2~10 ms         | 70~90%        | 十几~几十分钟   | 可增量        |
| **IVF-PQ + 磁盘 rerank**              | 0.3 GB + SSD                   | 10~30 ms        | 90~97%        | 同上 + 重排数据 | 可增量        |
| **DiskANN**                         | 数 GB（图） + SSD（向量）              | 5~20 ms         | 90~96%        | 长         | 支持在线增      |

⚠️ **这些数字是"量级参考"，不是承诺。** 真实结果受数据分布（簇的紧致度、维度相关性）、
CPU 代次、SIMD 指令集、并发压力影响，波动可达数倍。**做任何选型前，用你自己的数据跑一遍基准**（方法见 §十）。

---

## 八、三角权衡：召回率 / 延迟 / 内存（本文真正的决策工具）

三者构成不可能三角——**任意两个好，必然牺牲第三个**：

```
              召回率 ↑
                 /\
                /  \        想要高召回 + 低延迟
               /    \       → 必须堆内存（HNSW 全内存原始向量）
              /      \
             /________\
        低延迟          低内存
        ↑ → 需要少看数据 → 损失召回
        ↑ → 需要压缩编码 → 损失召回
```

**破局方式不是"找到完美算法"，而是三招**：

1. **分层计算**（用便宜手段粗筛，昂贵手段只处理少量候选）
   `IVF 粗筛 → PQ 快速排序 → 原始向量 rerank top-100 → Rerank 模型精排 top-20`
   这是**生产系统最主流的结构**，本项目的 `recall-top-k: 20 → rerank top-5` 是同一思路的应用。

2. **冷热分层**（热数据全精度内存、冷数据压缩码或 SSD）
   按时间/访问频度切：近 30 天知识库 HNSW 全精度，历史归档 IVF-PQ 或落盘。

3. **先降低需求，再优化实现**（很多系统真正的问题是"根本不该有 10 亿条向量"）
   切块策略、去重、按业务域分集合，常比换索引省一个数量级。

### 决策流程

```
① N × d × 4B ≤ 可用内存的 50%，且暴力延迟可接受？
     → 用暴力（矩阵化）。别引入索引复杂度。【n < 10 万的常见答案】

② 内存够放原始向量 + 要低延迟 + 数据量百万~千万？
     → HNSW（M=16~32, efConstruction=200+, 在线调 efSearch）

③ 内存不够（原始向量放不下）？
     ├── 能接受精度损失 → IVF-PQ / IVF-SQ8（先试 SQ8，性价比最高）
     └── 不能接受       → DiskANN（图在内存 + 向量在 SSD）

④ 查询带强过滤条件（多租户/权限/时间）？
     → 优先考虑原生支持 filtered 的库（Qdrant/pgvector/ES），
       或在架构上"按过滤维度分集合"绕开问题。见 [04 §五](./04-生产运维-向量库跑起来之后才会遇到的事.md)

⑤ 要求 100% 精确（法务/合规检索）？
     → 老老实实暴力 + 分片并行，或离线预计算。ANN 不满足需求。
```

---

## 九、把判断讲清楚：为什么本项目 200 条不用 HNSW

通用原理讲完，用本仓库的真实代码演示"如何按 §八 的流程做决策"——这是面试里最能拉开差距的部分。

`ai-cs-chat/.../cache/SemanticCacheService` 做语义缓存匹配：新问题和已缓存的问答对比较，
相似度超过阈值（0.92 量级）就直接返回缓存答案，跳过 LLM 调用。它的实现是**对全部缓存条目做 O(n) 余弦扫描**，
而缓存条目上限只有 **200**（配置 `max-entries: 200`）。

代入 §八 的流程：

```
① 数据量：n = 200, d = 1024（bge-m3）
   内存 = 200 × 4 KB ≈ 800 KB      ← 连 L2 缓存都装得下
   计算 = 200 × 1024 ≈ 2×10⁵ 次乘加 ← 微秒级
   延迟完全可接受 → 决策直接结束：用暴力。
```

如果"为了显得专业"加上 HNSW，会发生什么：

| 引入 HNSW 的成本     | 在本场景的实际后果                                                    |
| --------------- | ------------------------------------------------------------ |
| 依赖一个 C++ 库或额外服务 | 多一个故障点和构建环节                                                  |
| 每次写缓存要更新图       | 缓存是高频写场景（命中率驱动），图持续退化                                        |
| **ANN 有召回损失**   | 🔴 **致命**：语义缓存的价值就是"确定命中就别调 LLM"，漏命中 = 白花一次 LLM 费用，缓存收益被直接侵蚀 |
| 图内存/构建开销        | 800 KB 数据配一套图结构，纯亏                                           |

**最后一行才是重点**：n=200 时暴力是 100% 召回，HNSW 是 ~97% 召回。
**在"每一次漏检都要花真金白银"的场景里，用降低召回换延迟毫无意义——延迟本来就够低。**

> 完整的算法视角分析见 [`../../11-数据结构与算法/09-缓存淘汰-LRU-LFU与两级缓存.md`](../../11-数据结构与算法/09-缓存淘汰-LRU-LFU与两级缓存.md)；
> 性能视角见 [`../../12-性能工程/06-接口性能优化实战.md`](../../12-性能工程/06-接口性能优化实战.md)。

---

## 十、动手实验：亲手调一次三个参数

```python
# pip install numpy hnswlib
import numpy as np, hnswlib, time

rng = np.random.default_rng(0)
dim, n, k = 256, 50_000, 10
data = rng.random((n, dim), dtype='float32')
data /= np.linalg.norm(data, axis=1, keepdims=True)      # 归一化，于是 'ip' 等价余弦
query = data[123] + rng.normal(0, 0.05, dim).astype('float32')

# ---- 基线：暴力（精确答案） ----
t = time.perf_counter()
exact = np.argsort(-(data @ query))[:k]
brute_ms = (time.perf_counter() - t) * 1000

# ---- HNSW：扫 efSearch，看召回与延迟如何互换 ----
idx = hnswlib.Index(space='ip', dim=dim)
idx.init_index(max_elements=n, ef_construction=200, M=16)
idx.add_items(data, np.arange(n))

for ef in (10, 16, 32, 64, 128, 256):
    idx.set_ef(ef)
    t = time.perf_counter()
    labels, _ = idx.knn_query(query, k=k)
    ms = (time.perf_counter() - t) * 1000
    recall = len(set(labels[0]) & set(exact)) / k
    print(f"ef={ef:4d}  {ms:6.2f} ms  recall@{k}={recall:.2f}")

print(f"brute      {brute_ms:6.2f} ms  recall@{k}=1.00")
```

**观察并回答**：

1. `efSearch` 从 10 涨到 256，召回和延迟各自怎么变？曲线是线性还是边际递减？
   （典型答案：召回快速逼近 1 后趋于平，延迟持续线性上涨——**"免费"的召回只在前面几档**）
2. 把 `M` 改成 8 和 48 重跑，`efSearch=32` 时召回有什么变化？内存变化呢？
3. 把 `data` 换成 `rng.random` 的**未归一化**版本，结果排序会变吗？为什么？
   （提示：`space='ip'` 下未归一化会让"长向量"天然占优，这就是 [01 §4.3](./01-向量与相似度-把文字变成坐标.md) 讲的长度问题）
4. 把 `n` 提到 50 万，观察暴力检索耗时何时开始"崩"——那个拐点就是内存带宽墙。

**进阶（faiss，体验 IVF/PQ 的取舍）**：

```python
# pip install faiss-cpu
import faiss
xb = data.astype('float32'); xq = query[None, :].copy()

for desc in ("IDMap,Flat", "IDMap,HNSW16,Flat", "IVF4096,PQ64", "IVF4096,SQ8"):
    index = faiss.index_factory(dim, desc)
    if not index.is_trained:
        index.train(xb[:100_000])
    index.add(xb)
    if "IVF" in desc:
        index.nprobe = 16
    D, I = index.search(xq, k)
    print(f"{desc:24s} recall@{k} = {len(set(I[0]) & set(exact)) / k:.2f}")
```

你会亲眼看到 `IVF4096,PQ64` 的召回明显低于其他三个——**这就是"省 64 倍内存"的账单**。

---

## 十一、动手练习

1. **白板题**：不看文，画出 HNSW 三层结构，并写出一次查询的 5 个步骤。画不出就回 §3.3。
2. **一句话区分**：用你自己的话说清 `efConstruction` 和 `efSearch` 的区别（提示：一个离线一次、一个在线每次）。
3. **口算题**：300 万条 768 维 float32，裸内存多少？HNSW 大概占多少？机器只有 8 GB 内存怎么办？
   （`300万 × 768 × 4 B = 9.2 GB` → 裸数据就超了；答案指向量化（SQ8 → 2.3 GB）或 IVF-PQ 或分片。）
4. **推理题**：IVF 的 `nprobe=1` 为什么会漏掉真正的最近邻？画图说明那个向量在哪儿。
5. **设计题**：面试官说"我们要做 10 亿条人脸向量的 1:1 比对，延迟 20ms 以内"，
   你会问哪三个澄清问题？（提示：先问能不能降维/量化、QPS 多少、是否允许磁盘）

---

## 十二、面试要点速记

1. **先给基线再谈索引**：说出"10 万条以内矩阵化暴力通常就是最优解，且召回 100%"，展示的是判断力不是无知。
2. **HNSW 三参数**：`M` 图稠密度（内存/召回）、`efConstruction` 建库一次性投入决定召回天花板、`efSearch` 在线旋钮决定贴近天花板多少。
3. **IVF 两参数**：`nlist` 建库定死、`nprobe` 在线可调；召回损失源于"最近邻在没探测的相邻簇里"。
4. **PQ 必答三点**：切段 → 每段独立 256 码本 → 每段 1 字节；查询用 ADC 查找表把距离计算降级为查表加法；"库量化、查询不量化"叫非对称。
5. **SQ8 是性价比第一步**：内存 4 倍压缩、召回通常只掉 1~2 点，比直接上 PQ 更常选。
6. **Recall@k 的分母是谁**：同数据集暴力检索的精确 top-k；平均值掩盖单次事故，RAG 场景必须警惕。
7. **三角权衡 + 破局三招**：分层计算 / 冷热分离 / 先降需求。说出"分层计算（粗筛→压缩码排序→原始向量重排）"是生产主流结构，属于加分答案。
8. **删除为什么难**：HNSW 图不能安全摘节点（会断连通），只能打墓碑 + 定期重建——见 [04 §二](./04-生产运维-向量库跑起来之后才会遇到的事.md)。
9. **过滤是 HNSW 的软肋**：强过滤下路径不可达导致召回崩塌；对策是分集合或选原生支持 filtered 的引擎。
10. **数字要带"量级"**：引用任何延迟/召回结论都补一句"取决于数据分布，需自己压测"，这是专业信号不是不自信。

---

## 十三、延伸阅读

- 上承：[01-向量与相似度](./01-向量与相似度-把文字变成坐标.md)（§六 的问题定义）
- 下接：[03-选型与容量规划](./03-选型与容量规划.md)（把本文的内存/延迟数字变成预算和机器数）
- 索引维护：[04-生产运维](./04-生产运维-向量库跑起来之后才会遇到的事.md)（墓碑、重建、分片、监控）
- TopK 数据结构：[`../../11-数据结构与算法/04-树堆与TopK.md`](../../11-数据结构与算法/04-树堆与TopK.md)
- 为什么 O(n) 有时优于 O(log n)：[`../../11-数据结构与算法/01-复杂度分析与算法思维.md`](../../11-数据结构与算法/01-复杂度分析与算法思维.md)
- 上层应用：[`../02-RAG全栈实战/03-RAG向量检索实战.md`](../02-RAG全栈实战/03-RAG向量检索实战.md)
