# 03-选型与容量规划

> [02 篇](./02-索引算法-向量检索怎么从全表扫描里活下来.md) 讲"算法怎么选"，本篇讲"产品和机器怎么选"。
> 读完你能：把需求三个数字问出来 → 算出要多少内存几台机器 → 从一堆向量库里挑出那个不后悔的。

---

## 一、选型前必须先拿到的数字（拿不到就别开始比较产品）

新手一上来就问"Milvus 和 Qdrant 哪个好"——**这个问题没有答案**，因为它取决于下面这张表。
先把表填满，答案通常会自己浮出来。

| # | 必填项 | 为什么决定一切 | 示例值 |
|---|---|---|---|
| 1 | **向量条数 N**（注意是"条"不是"文档数"） | 决定能不能用轻量方案 | 500 万 |
| 2 | **维度 d 与精度** | 代入内存公式 | 1024 / float32 |
| 3 | **QPS 与 P99 延迟目标**（QPS=每秒查询数；P99=99% 的请求都快于这条延迟线） | 决定要不要分片、要不要上 HNSW | 300 QPS，P99 < 50ms |
| 4 | **召回率要求（用业务语言）** | "漏一条会怎样"比"Recall 多少"更重要 | 允许偶发漏检，因为有 Rerank 和关键词兜底 |
| 5 | **写频率**：每天新增/更新/删除多少 | 决定能否接受"重建型"索引 | 每天 5 万更新，1 万删除 |
| 6 | **过滤条件有多复杂** | 🔴 最容易被忽略、又最常是决定性因素 | 必带 `tenant_id`，常带 `category`、时间范围 |
| 7 | **现有技术栈与团队技能** | 多一个组件 = 多一份长期运维账单 | 已有 PG + ES，没人会 K8s |
| 8 | **数据规模增长曲线**（6/18 个月） | 防止半年后推翻重来 | 预计 1 年内 10 倍 |
| 9 | **是否要求 100% 精确** | 若是，ANN 整条路线出局 | 否 |
| 10 | **预算形态**：CapEx 机器 / OpEx 云服务（CapEx=一次性买设备，OpEx=按月付服务费） | 决定自建还是托管 | 只有一台 32 GB 开发机 |

**关于第 6 条的展开**（新手普遍低估）：
过滤条件的复杂度往往比向量检索本身更早成为瓶颈。你的向量库需要回答：

```
① 过滤是先做还是后做？（pre-filter / post-filter，代价见 §二.3）
② 支持哪些谓词？范围、IN、前缀、布尔组合、嵌套
③ 过滤后会不会让图索引"走不通"从而暴跌召回？
④ 高基数标签（tenant_id 有 10 万个值）扛得住吗？
```

**表里的名词速览**（第一次遇到在此解释，后文不再重复）：

- **租户 / `tenant_id` / 多租户**：租户（tenant）= 使用你系统的每一家公司或团队；多租户 = 一套系统同时服务多家，每条数据用 `tenant_id` 标记归属
- **谓词**：过滤条件里的比较表达式（=、IN、范围、AND/OR 组合）
- **高基数**：某个字段的取值种类特别多（基数 = 取值种类数；`tenant_id` 有 10 万个值就是高基数）
- **PG / ES / K8s**：PG=PostgreSQL 数据库，ES=Elasticsearch 搜索引擎，K8s=Kubernetes 容器编排平台
- **ANN / HNSW / IVF / PQ**：见 [02 篇](./02-索引算法-向量检索怎么从全表扫描里活下来.md)，此处只需知道它们是"近似检索的索引算法"

**第 7 条要诚实执行**：一个团队从没运维过 etcd/对象存储/消息队列，就不要选那些"需要这三样才能跑起来"的分布式向量库。
选型里"运维认知成本"经常比"性能差 20%"重要得多。

---

## 二、产品分三类，先认清形态差异

### 2.1 第一类：库 / 嵌入式（Library）

**没有服务进程，作为一个依赖跑在你的应用进程里。**

| 代表 | 特点 |
|---|---|
| FAISS（Meta） | 算法最全（含 GPU），**不是数据库**：没有元数据、没有服务、增删受限，得自己搭一层 |
| hnswlib | 极轻量 HNSW 实现，几行代码上手；删除只有 `markDelete` |
| ANNOY / ScaNN / Voyager | 各自生态内成熟，通用性弱 |
| LanceDB | 嵌入式 + 列式存储格式，本地多模态数据集场景友好 |
| Chroma（本地模式） | 介于两类之间：可嵌入式持久化，也可起 server |

```
✅ 优点：零网络跳数（延迟最低）、零运维、成本几乎为零、开发反馈环快
❌ 缺点：容量受单进程内存限制；多实例部署时每个副本各存一份（数据一致性要自己管）；
        无法独立扩缩容；重启要重新加载
```

**关键判断**：如果你的应用是"多副本无状态 + 向量数据只读且不大"，
嵌入式方案（把向量打包进镜像或启动时加载）常常比维护一个向量数据库服务**更简单也更便宜**。
很多人默认"要用向量库"，其实需要的是一个索引文件。

### 2.2 第二类：专用向量数据库（独立服务）

向量检索是它的主业，围绕它做全套：集合、元数据、过滤、副本、分片、多租户、备份。

| 代表 | 一句话定位 |
|---|---|
| **Milvus** | 分布式全家桶，亿~十亿级；功能最强，运维最重 |
| **Qdrant** | Rust 单机性能强 + **过滤能力最顺手**（payload=挂在向量上的标量元数据字段；payload 索引=给这些字段建过滤索引，配合原生 filtered HNSW）；分布式在后头 |
| **Weaviate** | 模块化，内置向量化器、原生混合检索、GraphQL（按需取字段的查询接口）；多模态友好 |
| **Chroma** | 开发者体验优先，AI 应用原型默认选项；规模化能力有限 |
| **Vespa** | 检索 + 排名 + 张量计算平台，能力天花板高，学习曲线陡峭 |
| Pinecone / Zilliz / Turbopuffer | 托管服务，见 §五成本模型 |

### 2.3 第三类：**已有数据库长出的向量能力**（最常被忽略的一类）

| 代表 | 向量方案 |
|---|---|
| **PostgreSQL + pgvector** | `vector(n)` / `halfvec` / `bit` 类型，**HNSW 与 IVFFlat** 索引 |
| **Elasticsearch 8.x / OpenSearch** | `dense_vector`（支持 `int8`/`bbq` 量化——把每维压成 1 字节甚至更少比特）、k-NN（k 近邻向量检索）与 BM25 原生混合 |
| **Redis Stack / Valkey** | HNSW、FLAT，全内存极低延迟 |
| **MongoDB Atlas / ClickHouse / Snowflake / Databricks** | 各自向量类型与索引 |

**这一类的价值主张是一个问题的答案：**"我能不能不引入新组件？"
如果你的数据量在千万以内、已经在用 PG 或 ES，**答案大概率是"能"**——
而"少一个组件"意味着少一套部署、少一份监控、少一类权限、少一个备份链路、少一次跨库一致性设计。

**pgvector 的隐藏优势**（很多教程没讲）：向量和业务数据在**同一个事务、同一个 JOIN** 里。

```sql
-- 权限过滤 + 向量检索 + 业务条件，一条 SQL 搞定，且是精确 pre-filter
SELECT id, content, 1 - (embedding <=> :q_vec) AS score
FROM   kb_chunk
WHERE  tenant_id = :tid AND status = 'published'          -- 关系型过滤，走普通 B-tree 索引（关系库最常用的经典索引结构）
ORDER  BY embedding <=> :q_vec                            -- <=> 是余弦距离
LIMIT  20;
```

**元数据内存陷阱（三类通用）**：向量库里每条记录的标量字段（payload/metadata）**也常驻内存**，
而且给字段建过滤索引会**再**占一份。一个 1024 维向量本体 4 KB，但你可能给它挂了 1~2 KB 的元数据
——**元数据能让总占用涨 30%~100%**。所以：

```
只把"需要过滤/需要返回"的字段放进向量库；
大文本正文放对象存储或主库，向量库只存 id + 过滤字段。
```

---

## 三、过滤 vs 召回：pre-filter 与 post-filter（选型深坑）

[02 §3.5](./02-索引算法-向量检索怎么从全表扫描里活下来.md) 提到"HNSW 遇到过滤可能崩塌"，这里讲透机制：

```
Post-filtering（先搜后滤）—— 多数库的朴素做法
  向量索引取 top-200 → 按条件过滤 → 剩 3 条 → 返回
  ❌ 问题：明明库里符合条件的有 5 万条，你却只拿到 3 条。过滤越严格，漏得越惨。

Pre-filtering（先滤后搜）—— 理想但难做
  先圈出符合条件的向量子集 → 在这个子集上跑 ANN
  ❌ 难点：HNSW 是一张连通的图，"屏蔽掉一半节点"会让剩下节点之间的路径断开，
         搜索可能到不了真正的最近邻 → 召回率暴跌

Filter-aware / 带过滤的图索引 —— 现代解法
  建图时就把高频过滤维度（如 tenant_id）作为连通性约束，
  或为每个过滤桶维护子图。Qdrant 的 payload 索引 + filtered HNSW 是代表实现。
```

**工程对策，按优先级：**

| 手段 | 适用 | 代价 |
|---|---|---|
| **按过滤维度分集合/分区**（每个租户一个 collection） | 租户数有限且可枚举 | 集合数爆炸时管理成本高；小租户浪费资源 |
| **选原生支持 filtered 的引擎** | 过滤维度多、基数高 | 绑定特定产品 |
| **加大 topK 再过滤**（取 500 过滤后截 20） | 过滤选择性不高时（过滤掉 <80%） | 延迟↑，选择性高时依然失效 |
| **关系库做精确过滤 + 向量做重排**（pgvector 路线） | 过滤是硬需求、数据量中等 | 向量检索性能受关系库引擎限制 |
| **先过滤拿 id 集合，再用 id 集合做向量查询** | 支持 id 白名单查询的库 | 两次查询、id 集合过大时退化 |

> **面试加分**：讲完 pre/post-filter 的定义，补一句"我判断这个坑的深度要看**过滤选择性**"
> ——过滤掉 10% 时 post-filter 几乎无损，过滤掉 99% 时 post-filter 直接返回空。这个"选择性"视角比背方案有用。

---

## 四、决策树

```
第 0 步：需要 100% 精确吗？（合规/审计/去重判定）
  ├── 是 → 暴力检索（可 SIMD / 可分片并行）。所有 ANN 方案出局。
  └── 否 ↓

第 1 步：N × d × 4B 是否 < 你的应用进程可用内存的 30%，且数据只读或变更极少？
  ├── 是 → 嵌入式（hnswlib / FAISS / LanceDB）。不要引入向量数据库服务。
  └── 否 ↓

第 2 步：N 是否 < 10 万，且 QPS 不高？
  ├── 是 → 复用现有库的向量能力（pgvector / ES dense_vector），或直接暴力。
  └── 否 ↓

第 3 步：主力诉求是什么？
  ├── 已有 PG、数据 < 千万、强关系过滤/事务/JOIN  → pgvector（运维成本最低）
  ├── 已有 ES、要 BM25 + 向量混合检索             → ES 8.x / OpenSearch（少一套链路）
  ├── 极低延迟 + 数据可放内存 + 变更频繁          → Redis Stack / Qdrant
  ├── 过滤复杂（多租户 + 多维标签 + 高基数）      → Qdrant（filtered 能力优先）
  ├── 亿~十亿级、要多副本分片高可用、有专职运维   → Milvus / Vespa / 托管服务
  ├── 只要快速做 demo / 验证产品                  → Chroma
  └── 不想碰任何运维                               → 托管（Pinecone / Zilliz Cloud / Turbopuffer）

第 4 步：回到第 7 条现实约束 —— 团队会不会运维它？
  不会 → 降一级：Milvus 换成 Qdrant 单机；Qdrant 换成 pgvector；pgvector 换成嵌入式。
  【选错"运维不起的先进方案"是本项目级以下团队最常见的失败模式】
```

**反向清单（这些理由不该选它）**：

| 不该选的理由 | 为什么 |
|---|---|
| "简历上想写 Milvus" | 为一个组件引入 etcd（分布式键值存储，当"公共记事本"用）+ 对象存储 + 消息队列，运维账单真实存在 |
| "怕以后长到十亿" | 为三年后可能不来的规模付今天的复杂度；向量库迁移成本远低于直觉（见 §七） |
| "向量库都差不多" | 过滤能力、量化方案、删除语义差别巨大，见 §三 |
| "上了专用库效果会更好" | **检索效果主要由 Embedding 模型和切块策略决定，不是索引**。索引只影响速度/内存/召回损失 |

---

## 五、容量规划：把 N 和 d 变成机器数

### 5.1 公式

```
① 裸向量内存   M_raw  = N × d × 4 B                        (float32)
                SQ8/halfvec 时 ×2 分之一，PQ(m=64) 时 N × 64 B

② 索引与结构   M_index = M_raw × (1.2 ~ 1.5)               (HNSW 边表 + 分配器 + 墓碑)

③ 元数据       M_meta  = N × 平均元数据字节数               ← 别漏，常占 20~40%

④ 副本         × (1 + 副本数)                               (1 主 1 备 → ×2)

⑤ 安全水位     ÷ 0.7                                       (留 30% 给建索引峰值、突发、OS page cache)

⑤' 建索引峰值提醒：HNSW 构建期间内存常显著高于稳态，
    有的实现需要 1.5~2 倍瞬时空间；大库建索引前先确认这一点，
    否则"容量算得刚好、一建索引就 OOM"。

⑥ 分片数       = ceil(总需求 / 单节点可分配向量内存)
   节点数      = 分片数 × (1 + 副本数)
```

> 名词：**page cache**=操作系统自动把最近读过的磁盘文件顺手缓存在空闲内存里；
> **OOM**=内存耗尽、进程被操作系统杀掉；**分片（shard）**=把数据横向切成几份、每份放一台机器；
> **副本（replica）**=同一份数据再存一份拷贝，用于容错与分担读压力；"1 主 1 备" = 每个分片带 1 个副本。

### 5.2 完整算例（照着走一遍就会了）

**需求**：2000 万条 × 1024 维 float32，QPS 300，P99 < 50ms，1 主 1 备，过滤字段约 300 B/条。

```
① M_raw   = 2×10⁷ × 1024 × 4 B = 81.9 GB
② M_index = 81.9 × 1.4          ≈ 115 GB
③ M_meta  = 2×10⁷ × 300 B        = 6 GB     → 合计 ≈ 121 GB
④ 副本    × 2                     = 242 GB
⑤ 水位    ÷ 0.7                   ≈ 346 GB  → 集群总内存需求
⑥ 单节点给向量的可用内存设 120 GB（买 160~192 GB 机型）
   分片数 = ceil(346 / 120) = 3 分片；节点数 = 3 × 2 = 6 台
```

**如果预算只给 2 台机器怎么办？** 按代价从小到大依次尝试：

```
方案 A：SQ8 量化（float32 → int8，内存 ÷4）
        81.9 → 20.5 GB 裸；总需求降到 ~90 GB → 2 台 64 GB 可跑
        召回通常只掉 1~2 点      ✅ 【首选，性价比最高】

方案 B：Matryoshka 截断 1024 → 512 维（前提：模型支持）
        内存直接 ÷2，质量损失个位数百分点

方案 C：IVF-PQ(m=64)
        码本 2×10⁷ × 64 B = 1.28 GB → 内存极度宽裕
        召回损失明显，必须配原始向量磁盘 rerank → 延迟上升

方案 D：冷热分层
        近 3 个月热数据（约 400 万条）HNSW 全精度在内存，历史走 IVF-PQ/落盘
        工程复杂度换成本，通常是规模变大后的长期解

方案 E：接受 P99 放宽到 200ms，改暴力 + 分片并行    （多数团队不愿选，但先问过业务）
```

### 5.3 自建 vs 托管：一笔完整的账

**别只比"云服务价格 vs 机器价格"**，自建的真实成本是这样的：

```
自建总成本 =
    服务器（含冗余与水位）
  + 人力：部署、升级、监控、备份演练、故障处理、容量巡检   ← 通常是最大项
  + 试错成本：索引重建、参数调优、版本兼容问题
  + 可用性责任：一次生产事故的代价
托管总成本 =
    存储 + 请求费用（按 QPS/查询量，规模大时增长很快且线性）
  + 数据出网/传输费
  + 厂商锁定与迁移成本
```

**经验规律**：小规模（百万级以内、低 QPS）时托管明显便宜，因为人力账单主导；
大规模（数千万以上、稳定高 QPS）时自建明显便宜，因为云服务费随规模线性涨而人力近似常量。
**拐点通常在"你需要为它配一个专职运维"的那个规模上**——用这个来倒推，比看价格表准。

---

## 六、复盘一个真实案例：本项目为什么用 Chroma

把 §四 的决策树跑一遍，示范"判断怎么落"。（数据规模为**量级假设**，请按实际环境重算）

| 输入项 | 本项目的值 |
|---|---|
| N | 知识库 chunk 十万级（文档切块后），**远小于百万** |
| d / 精度 | `BAAI/bge-m3`，1024 维 |
| 延迟目标 | 对话式应用，LLM 生成本身秒级 → 检索 10~100ms **不在关键路径上** |
| 写频率 | 文档上传驱动，低频；RocketMQ 异步增量同步 |
| 过滤 | 主要按知识库（collection）隔离，条件简单 |
| 已有栈 | Spring Boot + Nacos + ES + Redis + MySQL；**没有 K8s 运维经验用于向量库** |
| 团队 | 学习/单人项目 |

代入决策树：

```
第 1 步：十万级 × 4 KB ≈ 几百 MB < 进程内存 30%？
        → 理论上嵌入式就够了。但需要独立服务给 knowledge/chat 两个服务共享 → 需要一个轻量 server
第 2 步：N < 10 万且 QPS 不高？ 是（接近边界）
        → 候选：pgvector（已有 MySQL，但 PG 更契合向量）/ ES（已有！）/ Chroma
第 3 步：要 BM25 + 向量混合检索 → ES 8.x 其实完全够格【注意，这是个真实可选项】
最终选择 Chroma 的合理性：
   ✅ 部署轻（单容器），符合"没人运维向量库"的约束
   ✅ Spring AI 一等公民支持，`VectorStore` 抽象干净（换库成本被抽象层压低）
   ✅ 与 ES 分工：ES 承担关键词/混合，Chroma 承担向量 → 两边都不做不擅长的事
   ⚠️ 代价：无 HA（高可用：坏一台服务不中断）、无分片、内存型；规模上去后是第一个瓶颈
```

**这个案例真正该学的三点**：

1. **"够用"是选型的终点，不是妥协**。给十万级数据上 Milvus 是负资产。
2. **已有组件往往就是答案**：本项目 ES 其实已有向量能力，选 Chroma 是因为"职责分离 + 部署更轻"，
   而不是"ES 不行"。选型时要能说清**否掉了谁、为什么**——面试官最爱追问这个。
3. **抽象层是后悔药**：代码只依赖 Spring AI 的 `VectorStore` 接口，
   换 Chroma→Milvus 是**换配置 + 导数据**，不是重写。这是下面 §七 的前提。

---

## 七、什么时候该换库，以及怎么换（比想象便宜）

### 7.1 迁移触发信号（出现任一即启动评估）

| 信号 | 说明 |
|---|---|
| 单节点内存稳定 > 70% | 下一次数据增长就会 OOM |
| P99 持续超目标且调 `efSearch` 无效 | 索引选型到极限了 |
| 过滤需求变复杂（出现租户/权限/多维标签组合） | Chroma 这类轻量库会开始漏结果 |
| 需要高可用（不能接受单点） | 需要副本/分片能力 |
| 集合数量爆炸（每租户一集合导致成千上万） | 多数库的元数据管理会开始退化 |
| 构建/重建索引时长超过可接受窗口 | 需要分布式构建能力 |

### 7.2 一个能省掉大量成本的关键认知

> **换向量库 ≠ 重跑 Embedding。**

只要 Embedding 模型不变，向量数值本身就是一样的。迁移是**把向量从 A 库导出、灌进 B 库**，
不需要重新调用模型。省钱省时的关键是：**源文档和它的向量、以及生成它们所用的模型标识，都要能查回来。**

所以建库之初就该设计好这张表：

```
chunk_id | doc_id | text_hash | embedding_model | model_version | （向量本身存库，此处只留 id）
```

**`embedding_model` + `model_version` 两个字段是救命字段**——
没有它们，你将来无法判断"哪些向量是旧模型产的、需要重建"，只能全量重跑（大模型场景下这笔钱很可观）。

### 7.3 安全迁移四步

```
① 双写      新数据同时写旧库 + 新库；旧库仍是唯一读来源（风险为零）
② 全量回灌   从向量导出（或按上表重新向量化）填满新库，核对条数与抽样一致性
③ 影子读     线上查询同时发给两个库，只返回旧库结果，异步比对召回差异
             关键指标：两边 top-k 的 Jaccard 重合度（两集合交集 ÷ 并集，0~1）、新库是否有系统性漏检
④ 灰度切流   按流量百分比 / 按 collection 逐步切到新库（灰度 = 一点点切、随时能回退），保留快速回滚开关
```

**影子读这一招非常值得单独记**：它是唯一能在**零用户风险**下验证"新库在我真实数据上到底行不行"的方法。
只对比延迟是外行做法——**先证明召回不劣化，再谈它更快。**

---

## 八、动手练习

1. **填满 §一 的表**：挑一个你真想做的场景（哪怕是"给自己的笔记做语义搜索"），
   十个格子全部填出具体数字。填不出来的格子，就是你还没想清楚的地方。

2. **口算容量**：800 万条 × 768 维 float32，无副本，元数据 200 B/条，水位 0.7。
   需要多少内存？只有 64 GB 机器时给两个方案。
   （`800万×768×4 ≈ 24.6 GB`，`×1.4 ≈ 34 GB`，元数据 1.6 GB，`÷0.7 ≈ 51 GB` → 64 GB 勉强但无余量；
   方案：SQ8 → 约 14 GB 裸，或降维 768→256，或分 2 片。）

3. **过滤陷阱复现**：用 Chroma 或 Qdrant 建 1 万条向量，其中 `tag='rare'` 只有 5 条。
   请求 `topK=10 + filter tag='rare'`，观察返回几条。再查文档确认该库是 pre 还是 post-filter。
   **这个实验 10 分钟，但会让你对 §三 的理解彻底落地。**

4. **反向论证**：为"本项目为什么不用 Elasticsearch 承担向量检索"写出三条支持理由和两条反对理由。
   写不出反对理由说明你其实没理解这个决策的代价。

5. **面试演练**：把 §六 的案例讲成 3 分钟版本，必须包含"我否掉了哪个方案、为什么"。

---

## 九、面试要点速记

1. **先反问，再回答**：被问"选什么向量库"，正确答案的第一句是问清 **N、d、QPS、过滤复杂度、现有栈**。
   直接报产品名的，多半只会抄默认值。
2. **三类产品要分清**：库/嵌入式 → 专用向量 DB → 已有 DB 的向量能力。
   并且说出"第一类和第三类在多数中小规模场景才是对的"，这是判断力。
3. **"少一个组件"是真实的架构收益**：它省掉部署、监控、权限、备份、跨库一致性五件事。
4. **容量公式背这条链**：`N×d×4B → ×1.4 索引 → + 元数据 → × 副本 → ÷0.7 水位 → 算分片`。
   面试官会看你是否**漏掉元数据**和**忘了水位**。
5. **SQ8 是第一步优化**（内存 ÷4，召回掉 1~2 点），比直接跳 IVF-PQ 更常选。
6. **pre/post-filter 与选择性**：post-filter 在强过滤下结果数骤减；pre-filter 可能破坏图连通性导致召回暴跌。
7. **换库不等于重跑 Embedding**：前提是存了 `embedding_model` + `model_version`。这句话一出口，面试官知道你真迁移过。
8. **影子读**：零风险验证新库，且**先证明召回不劣化再谈性能**。
9. **检索效果 ≠ 索引**：效果主要由 Embedding 模型和切块决定，索引只管速度/内存/召回损失。混淆这两者是最常见的暴露。
10. **讲清你否掉了谁**：只说"我选了 X"没有说服力，"我评估过 Y 和 Z，因为某约束淘汰了它们"才是完整决策。

---

## 十、延伸阅读

- 索引算法原理：[02-索引算法](./02-索引算法-向量检索怎么从全表扫描里活下来.md)
- 换模型/换库的运维细节：[04-生产运维](./04-生产运维-向量库跑起来之后才会遇到的事.md)
- 混合检索（ES BM25 + 向量 RRF）：[`../02-RAG全栈实战/05-混合检索实战-ES-BM25与向量RRF融合.md`](../02-RAG全栈实战/05-混合检索实战-ES-BM25与向量RRF融合.md)
- 本仓库向量库部署：`../../04-中间件/09-AI与治理中间件部署.md`
- 选型速记卡对照：[`../面试题/04-向量数据库与Embedding.md`](../面试题/04-向量数据库与Embedding.md) Q4/Q6
