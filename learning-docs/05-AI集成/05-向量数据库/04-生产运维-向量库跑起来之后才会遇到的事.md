# 04-生产运维：向量库跑起来之后才会遇到的事

> 选型和调参的书都讲"怎么搭起来"，本篇讲"跑起来之后才会暴露的事"——
> 删除为什么内存不降、更新为什么这么贵、换模型为什么要全量重建、监控该看哪五个指标、半夜告警先查什么。

---

## 一、写入链路：从一篇文档到可检索

完整链路每一步都有独立的失败模式：

```
源文档（PDF/Word/网页）
  │ ① 解析         失败面：格式不支持、扫描件无文字层、编码乱码
  ▼
纯文本
  │ ② 切块         失败面：切在句子中间、表格被拆散、块过大稀释语义
  ▼
chunk 列表
  │ ③ 向量化       失败面：模型限流/超时、超长被截断、维度与库不匹配
  ▼
向量 + 元数据
  │ ④ 入库         失败面：批量大小不当、重复写入产生孤儿、部分成功导致不一致
  ▼
可检索
```

### 1.1 批量写入的参数（新手最容易设错）

```python
# 反例：逐条写入 —— 每次都走一遍网络 + 索引插入，慢且放大服务端压力
for v, meta in items:
    collection.add(ids=[...], embeddings=[v], metadatas=[...])
```

正确做法与经验区间：

| 参数 | 建议起点 | 调大的收益 | 调大的风险 |
|---|---|---|---|
| 每批条数 | 100~1000 | 吞吐显著提升（摊薄网络与事务开销） | 单批超时、内存峰值、失败重试代价大 |
| 并发写线程 | 2~8 | 吞吐 | 🔴 HNSW 并发插入会**抢图锁**，超过一定并发后吞吐反而下降 |
| 写入速率上限 | 按 Embedding API 配额 | — | 向量化往往是整条链路最慢的一环，瓶颈不在库 |

**关键顺序认知**：批量灌库时，**Embedding 计算通常比向量写入慢 1~2 个数量级**。
先测"向量化 N 条要多久"，再测"入库 N 条要多久"，你的优化优先级会立刻清晰。

### 1.2 写入要幂等（否则重复消费会造脏数据）

先解释**幂等（idempotent）**：同一个操作执行多次和执行一次，最终效果完全一样。
对向量写入而言，就是"同一条消息哪怕重复投递 10 次，库里也只会有 1 份数据"。

消息队列驱动的向量化（本项目就是 RocketMQ 驱动增量同步）必然遇到**重复投递**
（网络抖动时，消费者没来得及确认，MQ 会重发同一条消息）。
向量库写入的幂等靠**稳定的 id**：

```
✅ 用可推导的业务 id： chunk_id = doc_id + "-" + chunk_index
   → 同一逻辑块永远映射到同一 id，重复写入即覆盖（upsert 语义：有则更新、无则插入）

❌ 用随机 uuid：每次消费都生成新 id → 重复投递 = 库里出现 N 份重复向量
   → 后果不只是浪费内存：检索时 Top-10 被同一篇文档的 5 个副本占满，真正的其他来源进不来
```

> **这个"重复向量挤占 Top-K"的现象值得亲自复现一次**：它是那种"检索质量莫名变差、
> 但没有任何报错"的经典隐性故障，排查成本高，预防成本为零。

---

## 二、删除的真相：墓碑、compact、以及为什么不释放内存

**这是新手最普遍的认知空白**（也是很好的面试区分点）。

### 2.1 为什么不能真删

以 HNSW 为例：它是一张**连通图**，节点之间靠边相连，查询靠"沿着边跳"来定位。
直接物理摘掉一个节点，会让原本经过它的那些路径**断开**，图的可达性被破坏 → 召回率下降。

所以几乎所有图索引的删除都是：

```
① 打个标记（tombstone，墓碑位）—— 查询时遇到就跳过，但【节点和边都还在原地】
② 空间不释放，邻居表不变
③ 定期 compact（整理压缩）/ rebuild（重建）：真正把墓碑节点剔除、把空间归还、重建图
```

### 2.2 后果（三条，都会在生产上撞到）

| 后果 | 说明 | 应对 |
|---|---|---|
| **内存不降** | 删了 80% 数据，内存占用几乎不变；容量规划必须按"**历史累计写入量**"而非当前有效条数 | 监控墓碑率；定期 compact |
| **召回率缓慢退化** | 查询要"穿过"大量墓碑节点，有效路径变长变绕 | 墓碑率超阈值（经验：20%~30%）触发重建 |
| **查询延迟上升** | 取 topK=10 但扫到 40% 是墓碑 → 实际候选不足，需要更大 `efSearch` 补 | 同上；或调大在线 `ef` |

### 2.3 该建立的运维习惯

```
① 监控"有效向量数 / 总占用"这个比值 —— 它是向量库的健康度指标，作用类似 MySQL 的 data_free
   （表里已删除、但空间还没归还给操作系统的部分占比）
② 为 compact/rebuild 排周期任务（低峰期），并把它写进 runbook（故障与维护操作手册，出事照着执行）
③ 大批量删除（下线一整个知识库）之前，先评估是"删"还是"整库重建 + 切换"
   —— 删 500 万条通常应该是"重建剩下的 300 万条然后换索引"，而不是真的去 delete 500 万次
④ 确认你的库的删除语义：有的库 delete 只是标记，有的支持同步硬删（IVF 系删除代价模型不同）
```

> **一句话记忆**：向量库里 **delete ≈ 逻辑删除，物理空间靠 rebuild 归还。**

---

## 三、更新与"换模型"：最贵的两个操作

### 3.1 更新 = 删除 + 新增，且比看起来贵

向量不是能"原地改几个字节"的东西：内容变了 → 整个 1024 维都变 → 它在空间里的位置完全改变。

```
"更新一条向量" 实际发生的事：
  ① 旧节点打墓碑（不释放）
  ② 新节点插入并【重新建立 M 条邻居关系】—— 这一步要跑一次局部搜索，成本远高于普通 append
```

所以：

```
❌ 高频全量更新（每天重写 80% 数据）+ HNSW  →  图迅速退化，墓碑堆积，重建频率高到无法运维
✅ 这种模式的正确解法：离线重建新一代索引 + 原子切换（见 §3.3）
```

**选型阶段的对应结论**（回指 [02 §六](./02-索引算法-向量检索怎么从全表扫描里活下来.md) 的可增量维度）：
如果你的业务是"数据高频变动"，索引的**更新代价**比查询召回率更该主导选型。

### 3.2 换 Embedding 模型 = 必须全量重建（不可绕过）

```
不同模型的向量空间毫无关系：
  bge-m3 的第 7 维 和 text-embedding-3 的第 7 维，不代表任何东西，
  甚至同一模型 v1.5 和 v2 的向量也不共享坐标系。

混用的后果：新数据用新模型、老数据留老向量 → 距离计算得到的是【纯粹的噪声】，
          而且不报错，只是结果"莫名其妙不对"。这是向量系统最难排查的一类故障。
```

**正确做法（也是必须提前埋好的设计）**：

```
① 集合按模型隔离：collection = "kb-bge-m3"（推荐，最省心）
   或 元数据强制带 embedding_model / model_version 字段
② 升级走"新一代索引"：全量重新向量化 → 建 _v2 集合 → 影子验证 → 切换 → 旧集合保留一段时间作回滚
③ 【永远不要】在同一个集合里原地换模型
```

**成本提醒**：全量重新向量化是真实花钱的（商用 Embedding API 按 token 计费）。
用 §3.4 的公式先算一遍，再决定要不要升级模型——很多时候升级带来的质量提升
不如先把切块策略调对，而后者的成本几乎为零。

### 3.3 蓝绿索引切换（大版本升级的标准做法）

**蓝绿切换**：新旧两套索引（"蓝"=现役，"绿"=新兵）同时在线，用一个开关在某一瞬间整体切换——
不存在"切了一半"的中间状态（这种"要么全成、要么不成"的特性叫**原子切换**）。

```
        ┌──────────── 写入双路（或只写新） ────────────┐
        │                                              │
  index_v1 (现役，对外服务)                      index_v2 (后台构建)
        │                                              │
        └────► 线上读 v1 ◄──── 影子比对 ────► 校验读 v2 ┘
                           │
                    重合度/召回/延迟达标
                           ▼
              路由层原子切换：v1 → v2   （保留 v1 待回滚）
```

实现可以很轻：一个配置项 / 一个别名（alias）。**关键是"切换"和"删除旧索引"之间要留窗口**——
切换后立刻删旧索引 = 失去回滚能力。

### 3.4 重新向量化的成本估算

```
token 总量 ≈ 语料字符数（中文近似 1 字 ≈ 1~2 token，用模型的 tokenizer 实测——tokenizer 是模型
的分词器，负责把文本切成 token，算 token 数要用它而不是估算）
费用 = Σ token / 1M × 单价
时间 = token 总量 / 你的吞吐上限（Embedding API 通常有 RPM/TPM 配额——RPM=每分钟请求次数上限、
TPM=每分钟 token 数上限；配额才是瓶颈）

例：3000 万字的语料 → 约 4000 万 token → 若单价 $0.02/1M ≈ $0.8（商用 small 模型）
    但 3000 万字切块后可能是 5 万个 chunk，注意配额：按 100 万 token/min 算需 40 min
```
（价格会变，公式稳定。**真正要提前确认的是配额决定的耗时窗口，而不是钱。**）

---

## 四、索引生命周期：它会"老化"

```
    ┌── ① 抽样训练（IVF/PQ 需要；HNSW 免训练）
    │  ② 全量构建（最耗时，注意内存峰值）
    │  ③ 上线（蓝绿切换）
    │  ④ 在线服务（增量写、调 efSearch/nprobe）
    │  ⑤ 【退化】←── 这一步所有人都忘记它会发生
    └─ ⑥ 重建 → 回到 ①
```

**为什么会退化（三个独立原因）**：

| 原因 | 机制 |
|---|---|
| 墓碑累积 | §二.2，跳过无效节点，路径变差 |
| **数据分布漂移** | 索引是按**建库时**的分布建的。上线后新增的主题（新知识库、新业务线）可能落在原本稀疏的区域，那里的边很少 → 召回下降 |
| 图的贪心特性退化 | 邻居关系是在"当时的候选集"里选的；新节点加入不会回头优化老节点的邻居表 |

**监控退化的办法**：**固定一个"探针查询集"**（比如 200 条带标注答案的查询），
每天低峰自动跑一遍，记录 Recall@10 与 P99。画成时间序列——**这条曲线就是向量索引的健康心电图**。

```
探针集 Recall@10:  0.97 → 0.97 → 0.96 → 0.93 → 0.88  📉
                                        ↑ 分布漂移开始，该重建了
```

这比"等用户投诉说搜不准了再查"领先两周，且实现成本极低（一个定时任务 + 一张表）。

---

## 五、多租户与分区：把过滤变成物理结构

[03 §三](./03-选型与容量规划.md) 讲了 pre/post-filter 的机制，这里给运维层面的落地策略：

| 隔离粒度 | 做法 | 适用 |
|---|---|---|
| **库级** | 每租户一个 collection | 租户数少（几十）、要求强隔离、大客户 |
| **分区级** | 同库内按租户/时间/业务域分区（Milvus partition、ES shard routing——写入时就按规则决定数据进哪个分片，查询直达） | 中等租户数、要共享资源 |
| **元数据过滤** | 全库共用 + `tenant_id` 过滤 | 租户多且小、长尾 |

**分区还有一个常被忽略的收益：缩小搜索空间。**
按时间分区后，"近 7 天"的查询只扫相关分区——这是免费的性能优化，且不影响召回。
这也是 [03 §5.2 方案 D（冷热分层）](./03-选型与容量规划.md) 的落地形式。

**混合策略是实践常态**：大客户独立 collection（可单独调参/扩缩容），
长尾小租户共用一个 collection 走元数据过滤。

---

## 六、监控：至少盯这五个

新手常见的错误是把向量库当成"黑盒数据库"，只看 CPU 和内存。向量库必须监控**检索质量**，
因为它的质量退化**不会产生任何错误日志**。

| # | 指标 | 为什么是它 | 告警思路 |
|---|---|---|---|
| 1 | **空结果率 / 低分结果率**（返回 0 条 或 top1 分数 < 阈值的查询占比） | 最直接的"用户拿不到答案"信号；突增通常意味着模型/向量空间错乱 | 环比突增 3 倍告警 |
| 2 | **探针 Recall@10**（定时任务算出来的，非内生指标） | §四，唯一能发现"索引老化"的指标 | 跌破基线 5 个点 |
| 3 | **检索 P50 / P99 延迟**（分阶段拆开：向量化耗时 vs 检索耗时） | 一定要拆开——向量化（Embedding 调用）和检索是两个独立故障域 | P99 超 SLO（SLO=服务可靠性目标，如"P99 < 50ms"） |
| 4 | **内存占用 + 墓碑率 + 有效向量数** | §二；容量与老化的复合信号 | 内存 >75%；墓碑率 >25% |
| 5 | **源数据与向量条数差异**（对账） | §七，数据"进了库没进索引"是静默失败 | 差异超阈值 |

辅助：QPS、写入速率、写入失败重试数、缓存命中率（如果你做了 query 向量/结果缓存）、
以及 Embedding API 的**配额余量与限流次数**（这一项在生产上经常被遗漏，而它是最容易先触顶的）。

**仪表盘布局建议**：一行放"质量"（1、2、5），一行放"性能与容量"（3、4）。
出问题时先看质量行——**质量掉了但性能正常 = 分布漂移或向量空间错乱；性能掉了但质量正常 = 资源问题。** 这个二分能省掉一半排查时间。

---

## 七、一致性与备份：向量丢了怎么办

### 7.1 备份的第一性问题：**向量能不能重建？**

```
向量 = f(源文档, embedding_model, model_version, 切块策略)

只要右边四个都能拿到，向量就是【可再生的派生数据】，备份优先级可以很低。
源文档丢了 → 才是真灾难（且不可恢复）。
```

所以正确的备份策略是：

| 数据 | 是否必须备份 | 理由 |
|---|---|---|
| 源文档（对象存储/DB） | 🔴 **必须**，且是备份的重心 | 不可再生 |
| 切块记录 + `embedding_model`/`version` | 🔴 必须（体积极小） | 决定"能不能只重建受影响部分" |
| 向量与索引本体 | 🟡 可选（快照方便快速恢复，但可由①②重算） | 可再生，代价是时间和钱 |

> 这里有个反直觉结论值得记住：**给向量库做全量快照，往往不如把源文档和切块元数据备份好。**
> 因为快照只能救"库挂了"，救不了"模型换错了/切块策略变了"——后者要重算，而重算依赖源。

### 7.2 源改了、向量没跟上（最终一致要显式设计）

**故障表现**：客服改了退款政策文档，用户问起来 AI 还在按旧政策回答。
知识库条数、向量数都对，没有报错——因为旧向量还在库里，而且它和查询的相似度甚至更高。

**正确的同步链路**（本项目用 MQ，是通用模式的实例）：

```
文档更新（DB 落库）
  └─ 同一事务内发【事务消息】/ 记 outbox
        └─ MQ（携带 docId + 事件类型 + 版本号）
              └─ 消费者：删旧 chunk 向量 → 重新切块向量化 → 写新向量 → ACK
                    └─ 失败进重试队列 → 超限进死信 + 告警
```

> 名词：**事务消息 / outbox**=保证"数据库改成功"与"消息发出去"要么都成、要么都不成的机制
> （否则 DB 改了、消息丢了，下游永远不知道要更新）；**ACK**=消费者回复"这条我处理完了"，
> 没确认的消息会被重新投递；**死信队列**=重试用尽仍失败的消息的去处，攒着告警、等人处理。

三个必须有的保障：

```
① 幂等：用 §1.2 的可推导 chunk_id，重复消费不产生副本
② 可见性兜底：向量元数据带 doc_version；检索结果按版本过滤，
   旧版本立即被"过滤"而不是"返回"（宁可空结果也不能答错的）
③ 对账任务：定时扫描【源文档最新更新时间】vs【向量 last_indexed_at】，
   差异超阈值（如 > 5 分钟）告警。这一条是唯一的"最后防线"——
   MQ 会丢、消费者会崩、重试用尽会进死信，只有对账能兜住。
```

> **认知升级点**：把"向量索引"理解成**下游物化视图**（物化视图=把上游数据算好存下来的"缓存表"，
> 随时可以从源头重算重建），而不是"另一个数据库"。
> 于是所有你熟悉的问题都自动适用：需要幂等消费、需要版本、需要对账、需要能重跑。

---

## 八、故障排查速查表

| 现象 | 先查这个 | 常见根因 |
|---|---|---|
| **搜索结果莫名变差，无报错** | ① 最近有没有换 Embedding 模型/改切块策略 ② 探针 Recall 曲线 ③ 库里是否有同 id 重复向量 | 混用了不同模型的向量（最高频）；索引老化；重复数据挤占 topK |
| 内存持续上涨，删数据不降 | 墓碑率、有效条数 vs 总条数 | §二 逻辑删除；需要 compact |
| 写入吞吐上不去 | 分别测 Embedding 耗时 与 入库耗时 | 99% 是 Embedding API 配额限流，不是库的锅 |
| 带过滤的结果数远少于 topK | 该库是 pre 还是 post-filter；过滤选择性 | §三(03)：post-filter 在强过滤下必然发生 |
| P99 抖但 P50 正常 | GC（垃圾回收停顿）/ 是否发生后台 compact / 热点分片 / 邻居分布不均 | 维护任务未错峰；单分片承载全部查询 |
| 重启后前几分钟特别慢 | 索引是否在内存里懒加载（用到才加载）/ mmap（把磁盘文件映射成内存地址、首次访问才真正读盘）冷启动 | 缺预热：启动时跑一批探针查询 |
| 同样的查询，不同副本结果不一致 | 副本间数据/版本是否同步、是否各自独立建过索引 | 副本只同步数据不同步"索引重建"这件事 |
| 换库/升版本后延迟变高但召回也变高 | 这是正常权衡，不是故障 | 见 [02 §八](./02-索引算法-向量检索怎么从全表扫描里活下来.md) 不可能三角 |

**通用排查手法（值得单独记）**：拿一条**已知应该命中**的查询，把它的向量取出来，
直接在原始向量集上做暴力精确检索，看真实 top-10 是谁。

```
暴力精确检索能命中、线上索引检索不到  →  索引层问题（墓碑/过滤/ef 太小/图退化）
暴力精确检索也命不中                  →  表示层问题（Embedding 不对、切块切坏了、内容本身不相关）
```

这一步把"搜索不准"这个模糊问题**一分为二**，是所有向量检索故障排查的第二步（第一步永远是确认数据和配置没变）。

---

## 九、动手练习

1. **制造一次墓碑问题**：本地 Chroma/Qdrant 灌 10 万条，记录内存；删掉 8 万条，再看内存。
   然后跑一次 compact/rebuild，观察变化。写下你的库的删除语义。

2. **建一条探针曲线**：给你的场景（或任意 50 条你能标注正确来源的查询）写一个脚本，
   输出 `Recall@10`，接上定时任务跑三天。**你会立刻拥有一张别人都没有的健康图。**

3. **复现"重复向量挤占 Top-K"**：把同一段文本用同一 id 规则以外的方式（随机 id）写 5 遍，
   查一个相关 query，观察结果里同一内容占了几名。

4. **设计题（面试常考）**："知识库支持租户隔离 + 文档每日更新 + 每半年可能换 Embedding 模型"，
   画出你的 collection/分区/版本与切换方案。要求明确回答：
   ① 换模型时用户查询会经历什么 ② 旧模型集合何时可以安全删除 ③ 对账任务查什么。

5. **写一页 runbook**：针对"空结果率突增"这一条告警，写出前 3 步排查动作和对应命令。
   （能写出来，说明 §八 真的读进去了。）

---

## 十、面试要点速记

1. **"delete 是逻辑删除，空间靠 rebuild 归还"**——这句一出口，面试官立刻知道你碰过生产。
2. **索引会老化，三个原因**：墓碑累积、**数据分布漂移**、贪心邻居不回溯优化。第二个最少人知道。
3. **探针查询集**：固定标注集定时算 Recall，是发现退化的唯一手段，因为它不产生错误日志。
4. **换 Embedding 模型必须全量重建，且不能在同一集合原地换**——混用向量空间 = 无声的噪声。
5. **向量是派生数据，源文档才是资产**：备份重心在源 + 切块 + 模型版本标识，不在向量快照。
6. **把向量索引当"物化视图"设计**：幂等消费、版本号、对账任务，三件套缺一不可；**对账是唯一最后防线**。
7. **写入瓶颈多半在 Embedding 配额，不在向量库**——先分段测量再优化。
8. **可推导 id（doc_id + chunk_index）是幂等的前提**，重复数据会挤占 Top-K 造成静默质量下降。
9. **蓝绿索引切换**：别名/配置原子切、影子比对先行、旧索引保留回滚窗口。
10. **万能排查第一步**：暴力精确检索对比线上索引结果，把问题切成"索引层"还是"表示层"。

---

## 十一、延伸阅读

- 索引算法与不可能三角：[02-索引算法](./02-索引算法-向量检索怎么从全表扫描里活下来.md)
- 选型时的过滤与分区约束：[03-选型与容量规划](./03-选型与容量规划.md)
- 本仓库的增量同步实现：[`../02-RAG全栈实战/08-知识库增量同步实战-RocketMQ驱动向量化.md`](../02-RAG全栈实战/08-知识库增量同步实战-RocketMQ驱动向量化.md)
- 消息可靠性与死信：`../../04-中间件/08-RocketMQ事务消息与死信队列.md`
- SLO 与告警分级方法论：[`../../13-稳定性工程/01-SLI-SLO与错误预算.md`](../../13-稳定性工程/01-SLI-SLO与错误预算.md)、[`../../13-稳定性工程/07-告警治理.md`](../../13-稳定性工程/07-告警治理.md)
