# 05-面试专练与追问链

> 前两章的知识要能在 3 分钟内讲清楚才算真的会。本篇给答题框架、追问演练、手撕代码和一份面试前 30 分钟速览卡。
> 与 [`../面试题/04-向量数据库与Embedding.md`](../面试题/04-向量数据库与Embedding.md) **互补不重复**：
> 那边是概念速记卡（"是什么"），这边是**判断、权衡、排查、设计**（"怎么选、出了事怎么办"）。

---

## 一、四层答题模板（本模块所有题都用这个结构）

```
第 1 层 结论      一句话给判断，并立刻交代它依赖的前提
第 2 层 原理      为什么成立（机制，不是名词）
第 3 层 权衡      什么情况下这个结论会翻转 —— ★ 拉开差距的就在这一层
第 4 层 实践      数字 + 踩过的坑 + 我怎么验证的
```

**同一个问题的三种答法对比**（题目："你们用什么向量索引？"）：

| 层次 | 回答 | 面试官听到的信号 |
|---|---|---|
| ❌ 只有名词 | "我们用 HNSW。" | 抄的默认配置 |
| ⚠️ 名词 + 参数 | "HNSW，M=16，efConstruction=200。" | 用过，但不知道为什么是这些数 |
| ✅ 四层齐 | "当前是暴力精确检索。数据只有 200 条，索引省不下延迟，反而引入召回损失（第1层）。原因是 n×d 的乘积小到内存都出不了 L2 缓存（第2层）。等缓存条目过 10 万再考虑 HNSW，那时要先解决的是删除导致的图退化（第3层）。我用 200 条探针集量过，暴力 P99 0.4ms，换索引没有收益（第4层）。" | 有判断力、能量化、知道边界 |

**核心心法**：**"我没用 X" 和 "我不知道 X" 听起来完全不同，前提是你说得出用 X 的代价。**

---

## 二、高频 20 题（按考察意图分组）

> 每题给【骨架】和【失分点】。骨架要用自己的话展开，别背条目。

### A 组：规模判断（考察会不会过度设计）

| # | 题目 | 骨架 | 失分点 |
|---|---|---|---|
| 1 | 10 万条向量需要上向量数据库吗？ | 先算内存（10万×1024×4B≈400MB）→ 矩阵化暴力可到 ms 级 → 若已在应用进程内，嵌入式索引或直接暴力即可 → 但若有过滤/多副本/独立扩缩需求，仍值得独立服务 | 只答"数据量小不需要"，忽略**服务化价值不来自性能** |
| 2 | 百万条 1024 维，要多少内存？ | `4 GB 裸 → ×1.4 索引 → + 元数据 → ×副本 → ÷0.7 水位`，把这条链说出来 | 只报 4 GB（漏元数据、漏副本、漏水位=三连失分） |
| 3 | 十亿级怎么办？ | 单机不可能 → 分片 + 量化（PQ/SQ8）+ 磁盘索引（DiskANN）；同时**反问是否真需要十亿条向量**，切块与去重常能砍一个数量级 | 直接报产品名，不提"先降需求" |
| 4 | 什么时候不该用 ANN？ | ① 要求 100% 精确（合规/去重判定）② 数据量小到暴力够快 ③ 过滤极强（ANN 召回本已受损，再叠过滤不可控） | 只会说"数据量大就用" |

### B 组：算法原理（考察是不是只背名词）

| # | 题目 | 骨架 | 失分点 |
|---|---|---|---|
| 5 | HNSW 为什么快？ | 分层（上层长边粗定位）+ 第 0 层小世界稠密图精搜 → 只访问全图极小一部分节点，把内存搬运量从 O(N) 降到近似 O(log N) | 只答"多层图，一层层往下找"，说不出"省的是内存访问" |
| 6 | `efSearch` 和 `efConstruction` 区别？ | 一个**在线每次查询**的候选宽度（可调，直接换召回↔延迟）；一个**建库一次性**投入，决定召回天花板 | 混为一谈，或说"都是越大越好" |
| 7 | IVF 为什么会漏掉最近邻？ | 真最近邻落在没被 `nprobe` 命中的相邻簇（质心边界问题）→ 改进：加大 nprobe / 多质心冗余分配 / 混合检索兜底 | 只答"只搜部分簇所以会漏"，说不出漏的几何图像 |
| 8 | PQ 到底压缩了什么，为什么还能快？ | 切 m 段 → 每段独立 256 码本 → 每段 1 字节；查询用 **ADC 查找表**（m×256 预计算）把距离计算变成 m 次查表加法，SIMD 友好；"库量化、query 不量化"叫非对称 | 只答"把向量压小了"，答不出查询加速机制 |
| 9 | Recall@10 = 0.95 什么意思？分母是谁？ | 与同数据集**暴力精确 top-10** 的交集比；平均 10 个里对 9.5 个 | 不知道要跟精确解比；把它当成"准确率" |
| 10 | 归一化后三种度量什么关系？ | 余弦 = 点积；欧氏与余弦单调等价 → 选择变成"库对哪个支持好" | 死记"文本用余弦" |

### C 组：工程与运维（区分"做过"和"看过"）

| # | 题目 | 骨架 | 失分点 |
|---|---|---|---|
| 11 | 为什么删了数据内存不降？ | 图索引物理删节点会断连通 → 墓碑标记 → 空间靠 compact/rebuild 归还；副作用：召回退化、延迟上升 | 说"缓存没清""重启就好" |
| 12 | 换 Embedding 模型要做什么？ | **全量重建**（不同模型向量空间不可比）→ 集合按模型命名或带 `model_version` → 蓝绿切换 → 别原地替换 | 说"改个配置就行"（这是最致命的暴露） |
| 13 | 加了过滤条件后结果数骤减，为什么？ | post-filter 先取 topK 再过滤 → 强选择性下必然不足；解法：分集合 / filter-aware 索引 / 放大 topK（但只是缓解） | 说"数据本来就没有" |
| 14 | 搜索结果变差但没有任何报错，怎么排查？ | ① 先确认配置/模型/切块有没有变 ② **取该 query 向量做暴力精确检索对比**：暴力能命中→索引层（墓碑/ef/过滤/老化）；暴力也命不中→表示层（Embedding/切块/内容本身） | 说不出这个"一分为二"的手法 |
| 15 | 源文档更新了 AI 还在按旧内容回答，怎么防？ | 当**物化视图**设计：幂等消费（可推导 chunk_id）+ 版本号过滤 + **定时对账**（源更新时间 vs last_indexed_at）；对账是唯一最后防线 | 只答"加个 MQ 异步更新"，没有对账兜底 |
| 16 | 向量库怎么做备份？ | 向量是**派生数据**（源 + 模型 + 切块策略可重算）→ 备份重心在源文档与切块/版本元数据；快照救"库挂了"，救不了"模型换错" | 张口就是"定期快照" |
| 17 | 相似度阈值 0.7 是怎么来的？ | 用自己的标注集画"命中 vs 误召"分数分布取交叉点；且要和"有没有 Rerank"联动决策（有精排则阈值可放宽） | 承认是抄示例值，且没有替代方案 |

### D 组：架构与设计题（考察系统能力）

| # | 题目 | 骨架 | 失分点 |
|---|---|---|---|
| 18 | 已有 ES，为什么还要独立向量库？（或反过来：为什么要合并？） | 合并派：少一个组件、BM25+向量天然混合、运维统一；独立派：向量性能/内存模型更优、职责隔离、扩缩容独立。**结论取决于向量规模与查询 QPS** | 只站一派，说不出另一派的合理条件 |
| 19 | 多租户怎么隔离？ | 库级 / 分区级 / 元数据过滤三档，按租户数与隔离强度选；混合策略（大客户独立 + 长尾共用）；注意隔离粒度也决定搜索空间（性能红利） | 只答"加个 tenant_id 字段" |
| 20 | RAG 里向量检索漏了唯一正确文档怎么办？ | 召回层：宽 topK + 混合检索（关键词互为兜底）+ Query 改写扩写；判定层：无依据时明确拒答而非编造；评估层：标注集测端到端"有答率/正确率" | 答"调低阈值"（会引入噪声，且没解决"漏"本身） |

---

## 三、四层追问演练：一道题被打穿的全过程

真实面试里，一个话题会被连续追 3~4 层。**提前演练过的人不会在第三层卡住。**

> **L0：你们向量检索用的什么方案？**
> 你：数据量十万级、单查询毫秒内可接受，所以用向量库自带的索引 + 宽召回后 Rerank 精排。
>
> **L1：为什么宽召回取 20 而不是 5？**
> 你：召回和精排是两个目标。向量相似度是"语义接近"的近似，真正的相关性要靠 cross-encoder
> 逐对打分判断。取 20 是给精排足够的候选去重排；取 5 的话，排序错了就没有纠错机会。
> **代价是精排要算 20 次，所以精排模型的延迟决定了召回宽度上限。**
>
> **L2：那如果第 21 名才是正确答案呢？**
> 你：这就不是"调大 K"能解决的问题了——继续放大 K 会让精排成本和噪声同步上升，且有边际递减。
> 正确做法是**换通道而不是换参数**：混合检索（BM25 抓字面精确匹配，向量抓语义）+ Query 改写
> （把用户口语扩写成文档里的规范表述）。两个通道互为兜底，用 RRF 融合排名。
> 同时用标注集定位这个 case 属于"召回缺失"还是"排序错误"，两者药方完全不同。
>
> **L3：混合检索里两路分数怎么融合？为什么不直接加权？**
> 你：BM25 是无界分数，余弦是 [-1,1]，量纲不同，归一化方式的选择会静默改变结果，
> 而且相关性分布随 query 变化，固定权重不可移植。**RRF（Reciprocal Rank Fusion）绕开了分数**：
> 只用排名 `score = Σ 1/(k + rank)`，k 常取 60。它对两路的分数分布不敏感、无需调参、可解释。
> 真要精细控制，就上一个小模型做学习排序（L2R），但那需要标注数据和额外线上成本。
>
> **L4（考官换个方向切你的软肋）：你说的这套，哪一步最难落地？**
> 你：**评估**。上面每个决策（K 取多少、阈值定多少、要不要加改写通道）都需要
> "带标注的查询-文档对"才能判断好坏。没有标注集时，所有调优都是在猜，而且线上看不出来——
> 检索变差了不会报错。所以我的落地顺序是先花成本建 200 条探针集和标注流程，
> 再谈优化，否则改一版不知道是进步还是退步。

**这段演练的可迁移套路**：
- 每层回答都以"**代价是什么**"或"**什么时候不成立**"收尾 → 主动把追问引到你准备好的方向
- L2 这类"参数追不动了"的问题，标准解法是**换机制维度（换通道）而不是继续调参数**
- L4 被问软肋时，**诚实指出最难的一环并说明你的应对顺序**，比强行说"都还好"好得多

---

## 四、手撕题（三个，覆盖 90% 考察点）

### 4.1 余弦相似度（Java）

```java
static double cosine(float[] a, float[] b) {
    double dot = 0, normA = 0, normB = 0;
    for (int i = 0; i < a.length; i++) {
        dot   += (double) a[i] * b[i];
        normA += (double) a[i] * a[i];
        normB += (double) b[i] * b[i];
    }
    if (normA == 0 || normB == 0) return 0;              // ② 零向量防御
    return dot / Math.sqrt(normA * normB);
}
```

**面试官会追问的点**：
1. **为什么累加用 `double` 而参数是 `float[]`？**（① 1024 个 float 平方累加，`float` 尾数只有 24 位，
   累加会明显丢精度甚至量级失真；这是"用类型换正确性"的标准手法）
2. 长度不一致怎么办？（先校验并抛异常，别静默截断——向量维度错配通常意味着混用了模型）
3. 能不能更快？（向量已归一化 → 退化成点积，省掉两次开方与除法；再快就是 SIMD/矩阵批量，
   把 `float[]` 换成连续 `float[]` 大数组一次算多条，即暴力检索的矩阵化实现）

### 4.2 从 N 条向量里取 Top-K（考数据结构，与 [`../../11-数据结构与算法/04-树堆与TopK.md`](../../11-数据结构与算法/04-树堆与TopK.md) 同源）

```java
record Candidate(int id, double score) {}

static List<Candidate> topK(float[] query, float[][] vectors, int k) {
    // 大小固定为 k 的【小顶堆】：堆顶是"目前入选者中最差的"
    PriorityQueue<Candidate> heap = new PriorityQueue<>(Comparator.comparingDouble(Candidate::score));
    for (int i = 0; i < vectors.length; i++) {
        double s = cosine(query, vectors[i]);
        if (heap.size() < k) {
            heap.offer(new Candidate(i, s));
        } else if (s > heap.peek().score()) {   // 比最差的强才挤掉它
            heap.poll();
            heap.offer(new Candidate(i, s));
        }
    }
    List<Candidate> out = new ArrayList<>(heap);
    out.sort(Comparator.comparingDouble(Candidate::score).reversed());   // 按需；只要集合则可省
    return out;
}
```

**必答的复杂度对比**（这题的分全在这里）：

| 做法 | 时间 | 额外空间 | 结论 |
|---|---|---|---|
| 全部算完再排序 | O(n log n) | O(n) | 浪费：为次要元素也排了序 |
| **小顶堆维护 k 个** | **O(n log k)** | **O(k)** | ✅ 标准答案，n 大而 k 小时优势显著 |
| QuickSelect（nth_element） | 平均 O(n) | O(n) 或原地 | 只要 top-k 无序时最快 |

**追问预案**："为什么用**小**顶堆而不是大顶堆？"
→ 因为要不断淘汰"当前入选集合里最差的"，小顶堆让堆顶恰好就是那个最差元素，比较一次即可。
用大顶堆则取不到最小值，退化。

### 4.3 最小可用的语义缓存（把本模块串成一个组件）

```java
/** 相似问命中则跳过 Embedding 与 LLM 调用。容量有界 + 余弦阈值，两个"必须"缺一不可。 */
final class SemanticCache {
    private final int capacity;
    private final double threshold;
    // accessOrder=true → 每次访问把条目移到队尾，队首即最久未用 = LRU
    private final LinkedHashMap<String, float[]> vecCache = new LinkedHashMap<>(16, 0.75f, true) {
        @Override protected boolean removeEldestEntry(Map.Entry<String, float[]> e) {
            return size() > capacity;                      // ① 容量有界：无界缓存 = 内存泄漏
        }
    };

    SemanticCache(int capacity, double threshold) { this.capacity = capacity; this.threshold = threshold; }

    synchronized String match(float[] query) {             // ② 小 n 才敢 O(n) 全扫（见 02 §九）
        String hitKey = null; double best = threshold;
        for (var e : vecCache.entrySet()) {
            double s = cosine(query, e.getValue());
            if (s > best) { best = s; hitKey = e.getKey(); }
        }
        return hitKey;
    }

    synchronized void put(String key, float[] vec) { vecCache.put(key, vec); }
}
```

**这里能引出的加分点**（主动说出来）：
- **容量必须有界**，否则这是个内存泄漏；配套要写淘汰策略（LRU/LFU）与 TTL
- **阈值不是随便定的**：0.92 以上的余弦才敢"措辞不同也当同一问题"，太低会答错（宁可 miss 不可答错）
- **为什么用 O(n) 而不是 ANN 索引**：n 上限 200 时，ANN 的召回损失直接侵蚀缓存收益 —— 见 [02 §九](./02-索引算法-向量检索怎么从全表扫描里活下来.md)
- **`synchronized` 是够用而不是最优**：读多写少可换读写锁或分片；但**先问要不要并发优化**，
  200 条微秒级扫描不构成瓶颈 —— 这句"先判断是否需要优化"最能体现工程成熟度
- **命中时是否还要记录会话历史**？要。缓存只替代"生成"，不能替代"留痕"（业务一致性与缓存命中是两件事）

---

## 五、把"我们项目没用向量数据库"讲成加分项（STAR 脚本）

```
S  我们的语义缓存要判断"新问题是否和已答过的问题同义"，本质是一次向量相似度检索。
T  缓存条目上限 200 条，每条 1024 维（bge-m3），命中就省掉一次完整的 LLM 调用。
A  按"数据量决定方案"的路子算了一遍：200×4KB = 800KB，数据都在 L2 缓存里，
   O(n) 余弦扫描是微秒级。上 HNSW 不会带来可感知的延迟收益，反而会引入召回损失——
   而在这个场景里漏一次命中的代价就是一整次 LLM 调用费用，等于优化把收益优化掉了。
   所以我选了暴力精确扫描，并且把"为什么不用索引"写进代码注释和文档，避免后来人"顺手加个向量库"。
R  该层命中率维持在目标区间，缓存侧延迟从未进入整体链路瓶颈。
   同时我留了个明确的升级触发条件：条目上限提高约 2 个数量级、或扫描进入毫秒级时，
   再评估 ANN，并且那时要先解决图退化和删除的问题。
```

**这个脚本的三个杀伤力**：
1. 展示了**先算再选**而不是先选再找理由
2. "把为什么不做什么写进注释" —— 反直觉细节，只有真做过的人会讲
3. 主动给出**升级触发条件**（可证伪的判断才有价值），这是资深信号

---

## 六、反问环节值得问的问题

| 问题 | 你能得到的信息 | 面试官得到的印象 |
|---|---|---|
| 现在向量规模多大、增长曲线如何？ | 判断是真在优化还是刚起步 | 你关心的是问题不是技术 |
| 检索质量现在怎么衡量，有标注集吗？ | 🔑 这一家的成熟度分水岭；有则谈调优，无则这是最大机会点 | 你知道评估才是瓶颈 |
| 过滤和权限主要靠向量库还是靠前置的数据层？ | 判断他们是否踩过 pre/post-filter 的坑 | 你有生产经验 |
| Embedding 模型换过版本吗，当时怎么迁移的？ | 有没有做蓝绿/对账的工程能力 | 你经历过真实升级 |
| 检索延迟现在在整体链路里占多少？ | 判断优化向量检索是不是真的值得做 | 你有全局成本观 |

---

## 七、面试前 30 分钟速览卡

```
【数字链】
  向量字节 = 维度 × 4B          1024 维 = 4 KB
  百万条 1024 维 ≈ 4 GB 裸 → 容量规划 ×1.4 索引 → +元数据 → ×副本 → ÷0.7 水位
  暴力检索慢在内存带宽不在计算；10 万条矩阵化可到 ms 级
  SQ8：内存 ÷4，召回掉 1~2 点（第一步优化）
  PQ(m=64)：4096B → 64B（64 倍），靠切段 + 每段 256 码本 + ADC 查找表

【三算法一句话】
  HNSW   分层图，上层粗定位 + 第0层精搜；M 图密度 / efConstruction 天花板 / efSearch 在线旋钮
         缺点：全内存、构建慢、删除留墓碑、强过滤下召回崩
  IVF    k-means 分桶 + 只探 nprobe 个桶；nlist 建库定死；漏因"最近邻在没探的相邻簇"
  PQ     量化压缩，不是索引而是"让全量能塞进内存"的手段；常与 IVF 组合

【三度量】归一化后 余弦 = 点积，且与欧氏单调等价 → 选择取决于库支持

【Recall@k】分母是同数据集暴力精确 top-k；平均值掩盖单次事故

【运维三句】
  delete 是逻辑删除，空间靠 rebuild 归还
  换 Embedding 模型 = 全量重建，禁止原地混用不同向量空间
  索引会老化（墓碑 / 分布漂移 / 贪心邻居不回溯），靠固定探针集测 Recall 才能发现

【万能排查】取该 query 向量做暴力精确检索对比：
  暴力命中而线上不命中 → 索引层；暴力也不命中 → 表示层（Embedding/切块/内容）

【答题纪律】每层回答以"代价是什么/什么时候不成立"收尾
```

---

## 八、易错点清单（考前最后扫一眼）

- [ ] 没说数据量就报产品名
- [ ] 忘了 Recall 要跟**精确解**比
- [ ] 混淆 `efSearch` 与 `efConstruction`
- [ ] 说"文本一律用余弦"而不是"归一化后等价，看库支持"
- [ ] 容量估算漏元数据 / 漏副本 / 漏水位（三连失分）
- [ ] 以为 delete 会释放内存
- [ ] 以为换 Embedding 模型可以只改配置
- [ ] 把"检索效果差"归因给索引（效果主要由模型和切块决定）
- [ ] 强过滤导致结果变少时只会"调大 topK"，说不出换索引机制或分集合
- [ ] 说"定期给向量库做快照"当作备份方案（向量是派生数据，源才是资产）
- [ ] 只讲性能不讲评估（没有标注集时，任何调优都是猜）
- [ ] 手撕 `cosine` 用 `float` 累加，被追问精度时答不上来

---

## 九、延伸阅读

- 知识本体：[01 向量与相似度](./01-向量与相似度-把文字变成坐标.md) ·
  [02 索引算法](./02-索引算法-向量检索怎么从全表扫描里活下来.md) ·
  [03 选型与容量](./03-选型与容量规划.md) · [04 生产运维](./04-生产运维-向量库跑起来之后才会遇到的事.md)
- 概念速记卡（背诵用）：[`../面试题/04-向量数据库与Embedding.md`](../面试题/04-向量数据库与Embedding.md)
- RAG 应用侧问答：[`../面试题/03-RAG检索增强生成.md`](../面试题/03-RAG检索增强生成.md)
- 数据结构与复杂度（手撕题根）：[`../../11-数据结构与算法/04-树堆与TopK.md`](../../11-数据结构与算法/04-树堆与TopK.md)、
  [`../../11-数据结构与算法/09-缓存淘汰-LRU-LFU与两级缓存.md`](../../11-数据结构与算法/09-缓存淘汰-LRU-LFU与两级缓存.md)
- 算法类面试题清单：[`../../11-数据结构与算法/11-高频面试题清单.md`](../../11-数据结构与算法/11-高频面试题清单.md)
