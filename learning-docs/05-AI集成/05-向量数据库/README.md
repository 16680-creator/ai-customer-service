# 05-向量数据库

> 本模块回答一个问题：**"语义检索"这件事，在工程上到底是怎么实现的、怎么选、怎么养、怎么答。**
> 面向零基础：所有术语第一次出现时先用生活语言解释，再给正式定义和公式。

---

## 一、为什么单独开这个模块

向量数据库是 RAG / 语义搜索 / 推荐召回 / 图片检索的**共同底座**。它看起来只是"一个存向量的库"，
但它同时横跨三件难事：

| 难点 | 具体表现 |
|---|---|
| 数学抽象 | 高维空间、相似度度量、量化压缩——不看推导就一直悬着 |
| 算法权衡 | 召回率、延迟、内存三者互斥，没有"最好的索引"，只有"最适合你这批数据的索引" |
| 运维陌生 | 删除是墓碑、更新是重insert、索引要重建、内存墙来得比想象中早——这些只有踩过才知道 |

本仓库原来的覆盖是这样的：

- [`../面试题/04-向量数据库与Embedding.md`](../面试题/04-向量数据库与Embedding.md)：**问答体速记卡**，给结论不给推导（"HNSW 是分层小世界图"——然后呢？）
- [`../02-RAG全栈实战/03-RAG向量检索实战.md`](../02-RAG全栈实战/03-RAG向量检索实战.md)：**代码走读**，讲本项目 Spring AI 怎么调用，不讲被调用的东西内部发生了什么
- `../../04-中间件/09-AI与治理中间件部署.md`：**部署向**，Chroma 怎么起容器

也就是说：**"为什么"和"出了事怎么办"这两层是空的。** 本模块补这两层。

---

## 二、篇目与学习顺序

按顺序读，每篇都依赖前一篇的概念。

| 顺序 | 文档 | 一句话 | 建议用时 |
|---|---|---|---|
| 1 | [01-向量与相似度：把文字变成坐标](./01-向量与相似度-把文字变成坐标.md) | 向量到底是什么、Embedding 怎么来的、余弦/欧氏/点积怎么选 | 40 min |
| 2 | [02-索引算法：向量检索怎么从全表扫描里活下来](./02-索引算法-向量检索怎么从全表扫描里活下来.md) | Flat / HNSW / IVF / PQ 原理 + 召回率-延迟-内存三角权衡（**本模块核心**） | 90 min |
| 3 | [03-选型与容量规划](./03-选型与容量规划.md) | 三类产品怎么挑、内存怎么估算、什么规模该换库 | 50 min |
| 4 | [04-生产运维：向量库跑起来之后才会遇到的事](./04-生产运维-向量库跑起来之后才会遇到的事.md) | 增删改的真相、索引重建、分片副本、监控指标、故障排查 | 60 min |
| 5 | [05-面试专练与追问链](./05-面试专练与追问链.md) | 分层答题模板、高频 20 题、四层追问演练、手撕 | 40 min |

**只想要速成的读法**（1.5 小时）：01 §1-3 → 02 全篇 → 03 §三 决策树 → 05 §二。
**打基础的读法**：全部，并且每篇末尾的"动手练习"真的去做。

---

## 三、术语速查表（看不懂名词先查这里）

> 按"读到的顺序"分五组，每条给一句**人话解释**。第一遍通读时不用背，
> 正文里撞到不懂的词再回来查；正文首次出现处也都做了就地解释。

### A. 基础概念（详见 [01](./01-向量与相似度-把文字变成坐标.md)）

| 术语 | 人话解释 |
|---|---|
| 向量（Vector） | 一串按固定顺序排列的小数，把一段文字在"语义空间"里的位置表示成一个点 |
| 维度（d） | 这串数有多少个；本项目 bge-m3 是 1024 维 |
| Embedding / 向量化 | 用模型把文字翻译成向量的过程（也指翻译出来的那串数本身） |
| Token | 模型处理文本的最小单位（约等于半个到一个字）；模型限长和 API 计费都按它算 |
| Transformer / 自注意力 | 当前主流的神经网络结构，核心机制是"每个词都看一遍句子里其他的词"来理解上下文 |
| 池化（Pooling） | 把一句话里所有 token 的小向量压缩成一个句子向量 |
| 归一化（L2 归一化） | 把向量长度缩放成 1、方向不变；做完后"余弦相似度"和"点积"变成一回事 |
| float32 | 最常用的数字精度，一个数占 4 字节；单条向量字节数 = 维度 × 4 |
| 余弦相似度 / 欧氏距离 / 点积（IP） | 三种"算两个向量像不像"的公式；归一化后三者可互换 |
| Top-K | 只取分数最高的 K 条结果，K 就是"要几条" |
| BM25 | Elasticsearch 这类搜索引擎默认的关键词打分算法（词出现越多、词本身越稀有，分越高）——只看字面 |
| RAG | 检索增强生成：先去知识库搜资料，把资料塞给大模型，让它照着资料回答而不是凭空编 |
| LLM | 大语言模型，负责"生成回答"的那个模型（区别于负责"搜索"的向量库） |
| Rerank / 重排 / 精排 | 用更准但更慢的模型，对召回的 20 条候选重新打分排序、只留前 5 |
| chunk / 切块 | 把长文档切成 300~800 token 的小段，每段单独变一个向量 |
| Matryoshka / 套娃表示 | 训练时让向量的"前缀维度"也能独立使用，可以直接砍掉后半截省存储 |
| bge / m3e / text-embedding-3 | 三个常用 Embedding 模型家族名（前两个开源中文常用，最后一个是 OpenAI 的） |

### B. 检索算法与性能（详见 [02](./02-索引算法-向量检索怎么从全表扫描里活下来.md)）

| 术语 | 人话解释 |
|---|---|
| 暴力检索 / Flat / KNN | 全库逐条精确比较，结果 100% 准但慢；数据量小时它反而是最优解 |
| ANN | 近似最近邻：允许偶尔漏掉正确答案，换回成百上千倍速度的索引算法总称 |
| Recall@k | ANN 返回的前 k 条与"精确答案前 k 条"的重合比例，衡量索引漏得多不多 |
| SIMD | CPU 的"一条指令同时算多个数"的并行指令（AVX-512 / AMX 是具体型号） |
| BLAS | 底层矩阵运算库；`V @ q` 这种一行矩阵乘法就是调它，远快于手写循环 |
| L1/L2 缓存 | CPU 芯片内置的超高速小内存，比主内存快上百倍；数据够小能整个放进去 |
| HNSW | 当前最主流的向量索引：给向量搭一张"多层地图"，上层粗定位、底层精搜索 |
| M / efConstruction / efSearch | HNSW 三个旋钮：每个点连几条边 / 建库时搜多细（一次性）/ 查询时搜多细（每次） |
| 跳表（Skip List） | 给有序链表加"快速通道"的结构，是 HNSW 分层思想的来源 |
| beam search / 束搜索 | 不只盯着一条最优路径走，而是维护一个候选队伍并行扩展，搜得更全 |
| k-means | 把一堆点自动分成 K 组的聚类算法；IVF 用它来"分桶" |
| 质心（centroid） | 一组点的"平均位置"，代表这个组的中心 |
| IVF | 倒排文件索引：先聚类分桶，查询时只搜离得最近的几个桶 |
| nlist / nprobe | IVF 两个旋钮：总共分多少桶 / 每次查几个桶 |
| PQ（乘积量化） | 把长向量切成 64 段、每段用 1 字节编号代替，4 KB 压成 64 B |
| 码本（codebook） | 每段那 256 个"代表子向量"的清单；压缩就是替换成清单里最近的编号 |
| ADC | 非对称距离计算：库内存压缩码、查询保持精确，靠预计算查找表把距离算成查表加法 |
| SQ8 | 每维 float32 压成 1 字节整数：内存 ÷4、召回只掉 1~2 点，性价比之王 |
| DiskANN | 图结构放内存、原始向量放 SSD 的方案，单机扛十亿级 |
| MIPS | 最大内积检索（Maximum Inner Product Search）：点积场景下检索问题的学名 |
| QPS | 每秒查询数（Queries Per Second），衡量系统要扛多大的查询压力 |
| P50 / P99 | 延迟百分位：P99 = 99% 的请求都比这条线快；比平均值更能反映"最慢的那批人" |
| 分片（shard） | 数据量单机装不下时，横向切成几份分给多台机器 |
| 副本（replica） | 同一份数据再存一份拷贝，坏一台不丢数据、读请求也能分摊 |

### C. 选型与工程（详见 [03](./03-选型与容量规划.md)）

| 术语 | 人话解释 |
|---|---|
| 嵌入式（Library 型） | 向量库以依赖包形式跑在你应用进程里（FAISS、hnswlib），没有独立服务 |
| 专用向量库 | 独立部署的服务（Milvus、Qdrant、Chroma），向量检索是它的主业 |
| pgvector | PostgreSQL 数据库的向量扩展插件，让老数据库直接会搜向量 |
| payload / 元数据 | 挂在每条向量上的普通字段（分类、时间、租户），用于过滤和展示 |
| pre-filter / post-filter | 先按条件过滤再搜向量 / 先搜向量再过滤；后者在强过滤下结果会骤减 |
| 过滤选择性 | 过滤条件能筛掉多大比例的数据；筛得越狠，post-filter 的坑越深 |
| 高基数 | 某个字段的取值种类特别多（基数 = 取值种类数；tenant_id 有 10 万个值就是高基数） |
| 谓词 | 过滤条件里的比较表达式：=、IN、范围、AND/OR 组合 |
| 多租户 / tenant_id | 一套系统同时服务多家客户，用这个字段标记每条数据属于谁 |
| CapEx / OpEx | 一次性买服务器的资本开支 / 按月付服务费的运营开支 |
| K8s（Kubernetes） | 容器编排平台；"没人会 K8s" = 没人维护需要它才能跑起来的重型组件 |
| etcd | 分布式键值存储，给分布式系统当"公共记事本"用（存元数据、选主） |
| OOM | Out Of Memory，内存耗尽、进程被操作系统杀掉 |
| page cache | 操作系统把最近读过的磁盘文件顺手缓存在空闲内存里 |
| B-tree | 关系数据库最普通的索引结构（tenant_id 这类等值过滤走的就是它） |
| Jaccard | 两个集合"交集 ÷ 并集"的重合度，0~1，用来比新旧库结果是否一致 |
| 影子读 | 新旧两个库同时接线上查询，只对外返回旧库结果，暗中比对差异 |
| 灰度 / 切流 | 按百分比把流量一点点切到新版本，随时能切回来 |
| 蓝绿切换 | 新旧两套同时就绪，一个开关瞬间整体切换，旧的保留作回滚 |
| HA | 高可用（High Availability）：坏一台机器服务不中断 |

### D. 生产运维（详见 [04](./04-生产运维-向量库跑起来之后才会遇到的事.md)）

| 术语 | 人话解释 |
|---|---|
| 幂等 | 同一操作做一次和做一百次效果一样；消息重复投递也不会造成重复数据 |
| upsert | "有则更新、无则插入"的写入语义 |
| 墓碑（tombstone） | 向量的删除只是打标记：查询时跳过，但数据和空间都还在 |
| compact / rebuild | 把墓碑真正剔除、重建索引，从而归还内存的维护动作 |
| RPM / TPM | API 配额限制：每分钟最多多少次请求 / 多少个 token |
| tokenizer | 把文本切成 token 的分词器；算 token 数要用它实测，不靠估算 |
| 物化视图 | 把上游数据算好存下来的"缓存表"，随时可从源头重算——向量索引就该这样看待 |
| 事务消息 / outbox | 保证"数据库改成功"和"消息发出去"要么都成、要么都不成的机制 |
| 死信队列 | 重试用尽仍然失败的消息的去处；攒在那里告警、等人处理 |
| ACK | 消费者回复"这条消息处理完了"；没确认的消息会被重新投递 |
| 对账 | 定时比对两边数据（源文档 vs 向量库），兜住一切"静默失败"的最后防线 |
| SLO | 服务可靠性目标（如 P99 < 50ms、可用性 99.9%），告警该不该响以它为准 |
| runbook | 故障处理手册：告警响了照着一步步执行，不靠临场回忆 |
| GC | 垃圾回收：运行时自动清理内存的机制，停顿会造成延迟抖动 |
| mmap | 把磁盘文件映射进内存地址空间，用到哪页才加载哪页；冷启动慢是它 |
| 探针集 | 固定的一批带标准答案的查询，定时跑出 Recall 曲线，是索引的"心电图" |

### E. 检索评估与面试（详见 [05](./05-面试专练与追问链.md)）

| 术语 | 人话解释 |
|---|---|
| cross-encoder | 把"问题+候选文档"成对送进模型打相关分的精排模型；准，但每对都要跑一次所以慢 |
| RRF | 倒数排名融合：BM25 和向量两路结果按"排名倒数之和"融合，不看原始分数 |
| 标注集 | 人工标好"这个问题正确答案应该是哪条文档"的数据集；没有它一切调优都在猜 |
| MRR / nDCG | 两个检索质量指标：MRR 看正确答案排多前，nDCG 看整份排序的质量 |
| STAR | 面试叙事结构：情境 Situation → 任务 Task → 行动 Action → 结果 Result |
| LRU / LFU | 淘汰策略：最近最少使用的先淘汰 / 使用频次最低的先淘汰 |
| TTL | 存活时间（Time To Live）：缓存条目到期自动失效 |
| QuickSelect | 平均 O(n) 就能选出前 k 名的选择算法（`numpy.argpartition` / C++ `nth_element`） |

---

## 四、前置要求

| 需要 | 不需要 |
|---|---|
| 会一点 Python 或 Java 的基础语法 | 线性代数课程背景（本模块自己补直觉） |
| 知道"数组/List"是什么 | 概率论、测度论、矩阵分解推导 |
| 读过 [`../02-RAG全栈实战/01-RAG检索增强生成.md`](../02-RAG全栈实战/01-RAG检索增强生成.md)（可选，有助于理解应用场景） | 真正跑过一个向量数据库（没有更好，本模块从零讲） |

涉及乘法、平方根、对数的地方都会写明"这一步在干什么"，不会有跳步。

---

## 五、读完你应该达到

### 认知层

- [ ] 能用自己的话说清"为什么两段文字距离近就代表意思相近"，以及这个说法哪里不严谨
- [ ] 看到 `余弦相似度 0.82` 能判断这算高还是低，并知道阈值该怎么定出来的（而不是背一个 0.7）
- [ ] 能说清 ANN 为什么必然损失召回，以及"召回率 95%"是在跟谁比

### 技术层

- [ ] 能在白板画出 HNSW 的分层结构和一次查询的走向，并解释 `M` / `efConstruction` / `efSearch` 各控制什么
- [ ] 能解释 IVF 的 `nlist` 和 `nprobe` 为什么是"召回率-速度"的直接旋钮
- [ ] 能说明 PQ 为什么能把 4096 字节压到 64 字节，代价是什么
- [ ] 拿到"1200 万条、1024 维、P99 < 50ms"的需求，能算出内存量级并给出索引选型

### 工程层

- [ ] 知道向量库删除后内存为什么不降，以及怎么让它降
- [ ] 知道为什么"先 post-filter 再截断 topK"会导致结果数量莫名变少
- [ ] 能设计出"源文档更新 → 向量索引最终一致"的链路
- [ ] 会看向量库的核心监控指标，出问题时知道先查什么

### 面试层

- [ ] 用"结论 → 原理 → 权衡 → 实践"四层模板回答任意一个向量库问题
- [ ] 被连续追问四层不慌（本模块 05 篇做了演练）
- [ ] 能把"我们项目没用向量数据库"讲成一个有判断力的加分项，而不是短板

---

## 六、常见误区速查（先给你打预防针）

| 误区 | 现实 | 详见 |
|---|---|---|
| "维度越高效果越好" | 维度过高在有限样本下反而稀释距离差异，且成本线性上升 | [01 §五](./01-向量与相似度-把文字变成坐标.md) |
| "余弦相似度 0.9 就是很相关" | 不同模型的值域分布差异极大，阈值必须用自己的数据标定 | [01 §四](./01-向量与相似度-把文字变成坐标.md) |
| "上了 HNSW 就是最优解" | 十万级以下 HNSW 常打不过暴力扫描；内存紧张时它反而是负担 | [02 §三](./02-索引算法-向量检索怎么从全表扫描里活下来.md) |
| "ANN 和精确最近邻结果差不多，随便用" | "差不多"是统计结论；具体某次查询可能漏掉唯一正确的那条 | [02 §二](./02-索引算法-向量检索怎么从全表扫描里活下来.md) |
| "数据量小，不需要考虑向量库运维" | 小数据量下的删除墓碑、元数据过滤问题照样存在 | [04 §二](./04-生产运维-向量库跑起来之后才会遇到的事.md) |
| "向量库能存元数据，那就都塞进去" | 标量元数据是内存大户，且过滤能力普遍弱于关系库 | [03 §二](./03-选型与容量规划.md) |
| "换了 Embedding 模型，老向量还能继续用" | 不同模型/不同版本的向量空间互不兼容，必须全量重建 | [04 §三](./04-生产运维-向量库跑起来之后才会遇到的事.md) |
| "向量检索效果好坏只能凭感觉" | 有标准做法：标注集 + Recall@k / MRR / nDCG | [05 §二](./05-面试专练与追问链.md) |

---

## 七、与其他模块的边界（避免重复阅读）

| 主题 | 归属 | 本模块怎么处理 |
|---|---|---|
| RAG 整体架构、切块策略、检索编排 | [`../02-RAG全栈实战/`](../02-RAG全栈实战/) | 不重复，本模块只讲"向量检索"这一环的内部 |
| ES BM25 + 向量 RRF 混合检索 | [`../02-RAG全栈实战/05-混合检索实战-ES-BM25与向量RRF融合.md`](../02-RAG全栈实战/05-混合检索实战-ES-BM25与向量RRF融合.md) | 只在 [03 §四](./03-选型与容量规划.md) 讲"什么时候该选带向量的搜索引擎" |
| 大 O 复杂度、TopK 堆、LRU | [`../../11-数据结构与算法/`](../../11-数据结构与算法/) | 直接使用其结论，不重新讲 |
| 向量缓存与余弦扫描的性能 | [`../../12-性能工程/06-接口性能优化实战.md`](../../12-性能工程/06-接口性能优化实战.md) | 交叉引用，本模块从算法视角再讲一次取舍 |
| Chroma/Neo4j 部署清单 | `../../04-中间件/09-AI与治理中间件部署.md` | 不涉及部署步骤，只讲部署完之后 |
| 向量库速记卡式问答 | [`../面试题/04-向量数据库与Embedding.md`](../面试题/04-向量数据库与Embedding.md) | **互补**：那边给结论便于背诵，这边给推导便于理解。05 篇刻意出"那边没有的题" |

---

## 八、本仓库的真实锚点（供对照，非必读）

本模块以通用知识为主，但在关键判断处会引用本项目的真实做法，方便你把"书上的道理"和"跑着的代码"对上：

| 通用概念 | 本项目对应 |
|---|---|
| Embedding 模型与维度 | `BAAI/bge-m3`，1024 维（`tools/nacos-config/aics-shared.yml`） |
| 向量集合与库 | Chroma，collection `aics-knowledge`（`ai-cs-chat/src/main/resources/application.yml`） |
| 宽召回 + 精排 | `recall-top-k: 20` → Rerank 取 top 5（`tools/nacos-config/ai-cs-chat.yml`） |
| **小数据量下不用 ANN 索引** | `SemanticCacheService` 对 ≤200 条做 O(n) 余弦扫描（`ai-cs-chat/.../cache/`） |
| 向量结果缓存 | L1 进程内 LRU + L2 Redis（`application.yml` `aics.cache` 段） |
| 避免重复向量化 | `CachingEmbeddingModel` 装饰器 |
| 源文档 → 向量最终一致 | RocketMQ 驱动增量向量化（见 [`../02-RAG全栈实战/08-...`](../02-RAG全栈实战/08-知识库增量同步实战-RocketMQ驱动向量化.md)） |

> 引用这些是为了给判断提供落脚点；**本模块的价值不依赖本项目**，换任何一个向量库这些原理都成立。

---

## 九、动手环境（可选）

想把例子跑起来，最低成本组合：

```bash
# 1. 纯本地、零部署：用内存版向量检索跑通全流程（推荐新手从这里开始）
pip install numpy scikit-learn faiss-cpu

# 2. 想体验真正的向量数据库服务
docker run -p 8000:8000 chromadb/chroma:0.5.20

# 3. 想对比索引算法差异（本模块 02 篇的实验都可用 hnswlib 复现）
pip install hnswlib
```

`02` 篇的每个参数实验都用 `hnswlib` 或 `faiss` 给了可直接粘贴的最小代码。

---

## 十、来源与更新

| 日期 | 变更 |
|---|---|
| 2026-09-06 | 初版：新增本模块，README + 5 篇正文，补齐"向量库原理与运维"层的文档空白 |
| 2026-09-07 | 新增 §三"术语速查表"；01~05 篇对首次出现的术语就地补充人话解释 |
