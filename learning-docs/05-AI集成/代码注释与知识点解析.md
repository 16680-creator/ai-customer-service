# AI 集成核心模块：代码注释与知识点解析（总览）

> 本文档是 `learning-docs/05-AI集成` 的总览式「代码地图 + 知识点」索引，配合各分篇章（01~04 目录）与源码中文注释使用。
> 聚焦"为什么这么设计"与易错点，不涉及逐行注释。涉及模块：
> - `ai-cs-chat`（对话 / RAG / 多模型路由 / 可观测 / NL2SQL / 安全 / Agent / 多模态）
> - `ai-cs-knowledge`（知识库切分、向量化入库、增量同步、运营）
> - `ai-cs-message`（会话历史持久化、LLM 追踪/用量落库）
> - `ai-cs-search`（向量 / ES / 混合检索底座）
> - `ai-cs-common`（通用 Result / BusinessException）

---

## 一、Spring AI 核心装配 —— `SpringAiConfig`

### 1.1 两类模型，两套链路
| 模型 | 实际提供方 | 协议 | 用途 |
|------|-----------|------|------|
| ChatModel（对话） | **DeepSeek** | OpenAI 兼容 | 生成式回答 |
| EmbeddingModel（向量化） | **硅基流动 BAAI/bge-m3** | OpenAI 兼容（手动 `@Primary`） | 文档/问题向量化 |

DeepSeek 官方不提供 `/v1/embeddings`，故 Embedding 单独指向硅基流动。两者均 OpenAI 兼容协议，一套 starter 适配。

### 1.2 装配要点
- 启动类 `ChatApplication` 用 `exclude = OpenAiEmbeddingAutoConfiguration.class` 关闭默认 Embedding 自动装配，本类手动声明 `@Primary` 的 `siliconFlowEmbeddingModel`，供 Chroma `VectorStore` 优先注入。
- `ChatClient` 三件套：`defaultSystem`（系统提示词）、`defaultToolCallbacks`（`OrderQueryService` 的 `@Tool`）、`defaultAdvisors`（`QuestionAnswerAdvisor` 自动 RAG，`similarityThreshold=0.3` + `topK=5`）。

### 1.3 两条 RAG 路径并存（易错点）
- `chat`（普通对话）走 **Advisor 自动 RAG**（ChatClient 默认带 ragAdvisor）。
- `chatWithRag` / `chatStreamSse(hasKb=true)` 走**手动 RAG**（`KnowledgeBaseService.search` + `buildContext` + 拼 Prompt）。注意区分，避免重复检索或漏检索。

---

## 二、向量存储与 Chroma —— `VectorStoreConfig`
- Chroma 独立部署，`collection-name: aics-knowledge`（chat 读、knowledge 写**共用同一集合**，必须用同一 Embedding 模型 bge-m3）。
- `vectorStore.add(chunks)` 内部调用 EmbeddingModel 转向量 → 写 Chroma；`vectorStore.delete("documentId == '123'")` 按元数据批量删。
- 易错点：chat 与 knowledge 共集合共模型，**Embedding 模型不能换**，否则历史向量与新向量空间不一致，检索失效。

---

## 三、RAG 全链路 —— `ChatServiceImpl` + `KnowledgeBaseService` + `DocumentLoader`

### 3.1 检索编排 `retrieveRagDocs(...)`
支持四种模式（枚举 `RetrievalMode`）：
- `VECTOR`：纯向量（`KnowledgeBaseService.search(kb, q, topN=5, recallThreshold=0.3)`）
- `HYBRID`：混合检索（`HybridRetriever` = ES BM25 + 向量 + RRF 融合）
- `HYBRID_QUERY_REWRITE`：先 `QueryRewriteService` 改写/HyDE 再混合召回
- `GRAPH_RAG`：`GraphRagService` 知识图谱优先，未命中降级纯向量

任何增强失败 → **自动降级到纯向量**，保证可用。

### 3.2 问答返回带溯源
`chatWithRag(sessionId, message, knowledgeBase, hybrid, rewrite)` → `Result<ChatRagResponseDTO>`，含输入/输出 Guardrail 审核、`RagAclFilter` 过滤、`ResilientAiService` 弹性调用、在线评估埋点；`citations` 来自命中文档元数据（documentId/title），前端可展示来源。

### 3.3 Rerank 精排
`SiliconFlowRerankService`（`RerankProperties`：baseUrl/apiKey/model=**BAAI/bge-reranker-v2-m3**/topN/timeoutMs）对宽召回结果精排。详见 `02-RAG全栈实战/04-RAG进阶实战-Rerank重排序.md`。

---

## 四、多模型路由 —— `modelrouter/` 包

> 本包是「LLM 工程化」的核心演进：把"写死的 ChatClient"升级为"按场景选模型 + 多模型降级 + 成本/配额治理"。

关键类：
| 类 | 职责 |
|----|------|
| `ModelScenario`（枚举，9 个） | CHAT / RAG / SUMMARY / INTENT / AGENT / NL2SQL / REWRITE / CHART / JUDGE，每个场景声明 `pricingKey`（计费名）与 `capabilities` |
| `ModelCapability`（枚举） | 模型能力（如 `REASONING`、`VISION`、`STREAMING` 等） |
| `ModelDefinition` | 单个模型定义：`name`（展示）、`pricingKey`（计费）、`timeoutMs`、`capabilities`、`tier`(cheap/standard/premium) |
| `ModelClientHolder` | 持有每个 modelId 对应的 `ChatClient`（按定义构建） |
| `ChatModelRegistry` | 注册所有 `ModelDefinition` 与 `ModelClientHolder`，提供 `getClient(modelId)` |
| `ModelRouter` | 路由核心：`route(scenario)` → `RouteDecision(primary, fallbacks, reason)`；`RouteReason` 记录 SCENARIO_DEFAULT / PRIMARY_UNAVAILABLE / QUOTA_DOWNGRADE 等；超配额时 `QuotaRouteProperties` 驱动切 `cheap` 档 |
| `ModelHealthRegistry` | **模型粒度**熔断器 `breaker(modelId)` |
| `RoutedChatClientFactory` | 按 `RouteDecision` 产出 `ChatClient` |
| `ChatClientCustomizer` | 接口，定制不同场景的 ChatClient 构建 |

知识点：
- **展示名与计费名解耦**：`ModelScenario.pricingKey` 与 `ModelDefinition.pricingKey` 用于用量计量，模型改名不影响计费对账。
- **降级链**：`fallbacks` 依次尝试，最终失败才走文本降级。
- **配额联动**：`QuotaService` 超阈值 → `ModelRouter` 走 `QUOTA_DOWNGRADE`。

---

## 五、弹性调用与可观测 —— `ResilientAiService` + `observability/`

### 5.1 弹性调用 `ResilientAiService`
方法首参为 `ModelScenario`，底层经 `ModelRouter` 路由 + `ModelHealthRegistry` 熔断：
- `callChat` / `callRagChat` / `callSummary` / `callSseStream` / `callSseRagStream`（及 `fallbackSummary`）
- 超时按模型 `timeoutMs` 用 `CompletableFuture.get(timeoutMs)` + `cancel(true)` 实现
- **仅瞬时故障重试一次**：`callWithTransientRetry` 仅对 `ResourceAccessException / SocketTimeoutException / TimeoutException` 重试一次
- **流式绝不重试**：`callSseStream` / `callSseRagStream` 的 Flux 不可重放
- 最终 `fallbackText(scenario)`：通用返回 "AI 助手暂时繁忙，请稍后重试。"（SUMMARY 返回空），而非早期设计里的 `fallbackChat/fallbackSseStream`

> 残留配置：`application.yml` 的 `resilience4j.chatService` / `sseChatService` 已不再被此服务使用；仅视觉 `VisionModelClient.visionService` 仍用 `@TimeLimiter/@Retry/@CircuitBreaker` 注解。

### 5.2 可观测 `observability/`
| 类 | 职责 |
|----|------|
| `TraceSpans` | 统一埋点入口 `observe` / `observeReturn` |
| `TraceSpanObservationHandler` | `onStop` 时把 Observation 组装为 `TraceSpan` 挂到 `TraceContext` |
| `TraceContextHolder` | `capture()/restore()` 跨线程传播（supplyAsync / SSE 回调 / Rerank 线程池） |
| `ModelUsageRecorder` | `record(pricingKey, ...)` 按内部计费名计量 Token/费用 |
| `ObservabilityExecutorConfig` | `usageExecutor`(1/4/1000)、`evalExecutor`(1/2/500) 异步线程池 |

埋点字段（`startLlmObservation`）：`modelId` / `routeReason` / `fallbackFrom` / `attempt` / `firstTokenMs` 五个高基数 key；场景由 `TraceInterceptor` 按入口推导（agent / vision / rag / sse / history / chat）。

---

## 六、NL2SQL 与图表 —— `nl2sql/` 包

| 类 | 职责 |
|----|------|
| `Nl2SqlQueryService` | 自然语言 → SQL（经 `ModelRouter` 的 NL2SQL 场景），结果落库查询 |
| `Nl2SqlProperties` / `Nl2SqlDataSourceConfig` | 只读数据源配置（防止写库） |
| `chart/ChartType`（枚举） | LINE / BAR / PIE / … |
| `chart/ChartTypeDetector` | 根据查询语义/数据推断图表类型 |
| `chart/EChartsOptionBuilder` | 生成 ECharts option JSON |
| `chart/ChartAnswerGenerator` + `ChartAnswer` | 生成"图表 + 文字解读" |
| `chart/ChartController` | 对外 `POST /chart/query` 等接口 |

安全要点：NL2SQL 受 `security/SqlGuard` 约束，仅允许 SELECT，禁止写/DDL；数据源为只读。

---

## 七、AI 安全网关 —— `security/` + `util/`

| 类 | 职责 |
|----|------|
| `ContentReviewer` / `RegexContentReviewer` | 正则内容审核（内置 `BUILTIN_RULES`，可配置扩展） |
| `ContentSafetyService` | 输入/输出审核编排，降级策略可配 |
| `ToolAuthorizationService` + `ToolAuthResult` | 工具调用鉴权（哪些角色可用哪些 `@Tool`） |
| `RagAclFilter` | RAG 检索结果按用户角色/知识库 ACL 过滤 |
| `SqlGuard` | NL2SQL 生成 SQL 的安全校验 |
| `SecurityAuditRecorder` + `SecurityEventType` | 安全事件审计落库 |
| `SecurityProperties` / `UserRoleResolver` | 安全配置 / 角色解析 |
| `util/PiiMasker` | **5 类**脱敏：身份证/银行卡/手机号/邮箱/地址门牌号，带数字边界防误匹配 |
| `util/ImageUrlValidator` | 图片地址 SSRF 白名单（未配置则默认拒绝所有） |

> `PiiMasker` / `ImageUrlValidator` 同时在 VLM 多模态链路（第三章）复用。

---

## 八、Agent 编排与人工转接 —— `agent/` 包
- `EscalationService` / `HumanHandoff*`：意图识别触发转人工；转接后对话进入人工队列，RAG/LLM 暂停。
- Agent 模式下的 LLM 调用统一走 `ModelRouter`(AGENT 场景) + `ResilientAiService`，可观测字段含 `agent` 场景标记。
- 详见 `01-SpringAI框架集成/04-智能客服Agent编排与人工转接实现文档.md`。

---

## 九、多模态视觉理解 —— `VisionModelClient` / `VisionChatServiceImpl`
- 两段式：`VisionModelClient`（**Qwen/Qwen3-VL-32B-Instruct**，OpenAI 兼容协议）"看图" → DeepSeek "回答"。
- 链路：图片 URL 校验（`ImageUrlValidator` SSRF 白名单）→ PII 脱敏（`PiiMasker`）→ 视觉描述 → RAG 回答。
- 接口：`POST /chat/vision`（同步）、`/chat/vision/sse`（流式）、`/chat/upload-image`（MinIO 上传）。
- 可观测：`Observation("chat.vision", scenario=vision)`，经 `ModelUsageRecorder.record("vision", ...)`。

---

## 十、会话历史持久化 —— `ai-cs-message`
- `MessageService.getMessagesBySessionKey` / `ChatMessageMapper.selectBySessionKey`：按 `sessionKey` 分页拉取历史（驱动 `ai-cs-chat` 的 `/chat/history` 长上下文注入）。
- `MessageController` 对外提供历史接口；LLM 追踪/用量/配额/在线评估经 `LlmTraceController` / `ModelUsageController` / `ModelUsageQuotaController` / `OnlineEvalController` 落库。
- 详见 `04-规格驱动工具/01-OpenSpec实战-会话历史持久化.md`。

---

## 十一、知识库增量同步 —— `ai-cs-knowledge`
- `KnowledgeServiceImpl` 文档 CRUD → 投递 RocketMQ 同步消息（CREATE/UPDATE/DELETE）。
- `KnowledgeSyncConsumer` 消费 → `KnowledgeVectorService.vectorize()`：切块（`TokenTextSplitter` 默认 800 token/块）→ 附元数据 → `VectorStore.add()` 写 Chroma。
- `DocumentLoader` 多格式：`loadPdf` / `loadText` / `loadTika`（按后缀路由）。
- 运营能力：`EmbeddingClusterService`（向量聚类去重）、`KnowledgeGapDetector`（知识缺口检测）、`FaqService`（FAQ 提炼）。

---

## 十二、检索底座 —— `ai-cs-search`
- 提供向量检索 / ES(BM25) 检索 / 混合检索（ES + 向量 + RRF 融合），经 Feign 供 `ai-cs-chat` 的 `HybridRetriever` 调用。

---

## 十三、通用封装 —— `ai-cs-common`
- `Result<T>`：统一响应封装。
- `BusinessException`：业务异常，被 `ResilientAiService` 的 `ignore-exceptions` 显式排除（不重试）。

---

## 附：模块 × 文档索引
| 模块 | 核心文档 |
|------|---------|
| 多模型路由 / 弹性 / 可观测 | `01-SpringAI框架集成/02-LLM工程化实践.md`、`05-LLM可观测性评估与成本治理实现文档.md` |
| RAG / Rerank / 混合检索 | `02-RAG全栈实战/01~04` 四篇 |
| VLM 多模态 | `01-SpringAI框架集成/03-多模态图生文实战-VLM视觉理解.md` |
| 安全网关 | `01-SpringAI框架集成/06-AI安全网关与Guardrails实现文档.md` |
| Agent / 转人工 | `01-SpringAI框架集成/04-智能客服Agent编排与人工转接实现文档.md` |
| 会话历史 | `04-规格驱动工具/01-OpenSpec实战-会话历史持久化.md` |
| 规格驱动(SDD) | `04-规格驱动工具/02~04` 三篇 |
