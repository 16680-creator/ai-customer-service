# ai-cs-chat / ai-cs-knowledge / ai-cs-message 代码注释与知识点解析

> 本文档配合三模块源码注释使用，对复杂处做原理拆解。源码已补充中文 Javadoc 与行内注释，本文聚焦"为什么这么设计"与易错点。
> 涉及模块：`ai-cs-chat`（28 文件，对话/RAG/弹性核心）、`ai-cs-knowledge`（12 文件，知识库与增量同步）、`ai-cs-message`（11 文件，会话持久化）。

---

## 一、Spring AI 核心装配 —— `SpringAiConfig`

### 1.1 两个模型，两条链路

| 模型 | 实际提供方 | 协议 | 配置位置 | 用途 |
|------|-----------|------|---------|------|
| ChatModel（对话） | **DeepSeek** | OpenAI 兼容 | `spring.ai.openai.*`（starter 自动装配） | 生成式回答 |
| EmbeddingModel（向量化） | **硅基流动 BAAI/bge-m3** | OpenAI 兼容 | 本类手动 `@Bean` + `@Primary` | 文档/问题向量化 |

### 1.2 为什么 Chat 和 Embedding 用不同提供方
DeepSeek 官方**不提供 `/v1/embeddings` 接口**，无法做文档向量化。所以：
- ChatModel 走 DeepSeek（便宜、中文好）。
- EmbeddingModel 手动装配一个指向硅基流动的 `OpenAiEmbeddingModel`，用开源 `bge-m3`（1024 维，中英文效果好，免费额度足）。

> 关键：两者都用 **OpenAI 兼容协议**，所以 Spring AI 的 `spring-ai-openai-spring-boot-starter` 一套代码兼容两家。DeepSeek/硅基流动只需改 `base-url` + `api-key` + `model`。

### 1.3 `@Primary` 与启动类 exclude
启动类 `ChatApplication` 用 `exclude = OpenAiEmbeddingAutoConfiguration.class` 关闭默认 OpenAI Embedding 自动装配；本类再手动声明一个 `@Primary` 的 `siliconFlowEmbeddingModel`。这样 `VectorStore`（Chroma）注入时优先用它，避免与默认实例冲突。

### 1.4 三个 Bean 的协作
```
ChatClient（业务调用入口）
  ├── defaultSystem      → 系统提示词（约束身份/能力/风格）
  ├── defaultToolCallbacks → OrderQueryService 的 @Tool 方法（LLM 可调用）
  └── defaultAdvisors    → QuestionAnswerAdvisor（RAG 顾问，自动检索注入上下文）
```
`QuestionAnswerAdvisor`（RAG Advisor）：每次 `ChatClient` 调用前自动把用户问题送入 `VectorStore` 检索，命中结果拼到 Prompt。配置 `similarityThreshold=0.3`（宽召回）+ `topK=5`。

> 易错点：`ChatServiceImpl` 的 `chatWithRag` 走的是**手动 RAG**（自己检索 + buildContext + 拼 Prompt），而 `chat`（普通对话）走的是 **Advisor 自动 RAG**（ChatClient 默认带 ragAdvisor）。两条路径并存，注意区分。

---

## 二、向量存储与 Chroma —— `VectorStoreConfig`

- Chroma 是独立部署的向量数据库（持久化），配置在 `application.yml` 的 `spring.ai.vectorstore.chroma`：
  - `collection-name: aics-knowledge`（chat 与 knowledge 模块**共用同一集合**，向量空间必须一致——都用 bge-m3）。
  - `initialize-schema: true`（首次启动自动建集合）。
  - `host` 需带 `http://` 前缀（Spring AI 1.1.x 要求）。
- `vectorStore.add(chunks)` 内部自动：调用 EmbeddingModel 把每个 chunk 转向量 → 写入 Chroma。
- `vectorStore.delete("documentId == '123'")` 用 Chroma 过滤表达式按元数据批量删除。

> 易错点：chat 模块（读路径：检索）与 knowledge 模块（写路径：向量化入库）共用同一 Chroma 集合与同一 Embedding 模型，**Embedding 模型不能换**，否则历史向量与新向量维度/空间不一致，检索失效。

---

## 三、RAG 全链路 —— `ChatServiceImpl` + `KnowledgeBaseService` + `DocumentLoader`

### 3.1 两条 RAG 路径
```
chatWithRag（同步 RAG）           chatStreamSse（流式 RAG，hasKb=true）
   │                                  │
   ├─ search(kb, q, top5, 0.5)        ├─ search(kb, q, top5, 0.5)
   ├─ buildContext → 【资料】         ├─ buildContext → 【资料】
   ├─ 拼 RAG Prompt                  ├─ 拼 RAG Prompt
   ├─ callRagChat(prompt)            ├─ callSseRagStream(prompt)
   ├─ buildCitations(docs)           ├─ buildCitations(docs)（缓存到 done 事件）
   └─ 返回 DTO(回答 + citations)      └─ 订阅 Flux 逐 token 推送
```

### 3.2 RAG Prompt 设计
```
请严格基于下面的【知识库资料】回答用户问题。
1. 如果资料中没有相关信息，请如实告知："我暂时没有这方面的资料"，不要编造内容
2. 回答时优先引用资料中的内容，不要提及"根据资料/检索结果"之类的表述

【知识库资料】
{context}      ← buildContext 拼接的检索片段

【用户问题】
{message}
```
> 设计意图：约束 LLM 只基于检索资料回答，降低幻觉；不暴露"检索"机制给用户。

### 3.3 引用溯源 `buildCitations`
从检索到的 `Document` 提取元数据构建 `CitationItemDTO`：
- `documentId` / `title` ← 入库时写入的 metadata
- `page` ← metadata 的 `page_number`（PDF 解析时由 PagePdfDocumentReader 写入）
- `score` ← 向量相似度
- `content` ← 命中片段原文

> 这样前端可以展示"回答出自哪份文档第几页"，增强可信度。

### 3.4 `DocumentLoader` 文档解析
用 Spring AI 的 **Tika** 文档读取器（`spring-ai-tika-document-reader`）支持 PDF/Word/Excel/PPT/HTML/TXT 等多格式，再经 `TokenTextSplitter` 按 token 切分。

### 3.5 会话历史压缩
`MAX_HISTORY_SIZE=20`：历史超过 20 条时，把旧消息交给 LLM 生成 1-3 句摘要（`callSummary`），保留最近 `KEEP_RECENT_SIZE=10` 条。压缩失败则回退为截断（只保留最近 10 条），保证不阻塞主流程。

---

## 四、Rerank 两阶段检索 —— `SiliconFlowRerankService`

### 4.1 为什么要两阶段
向量检索（第一阶段）召回快但精度一般，可能混入相关度低的片段。Rerank（第二阶段）用更重的 cross-encoder 模型对召回结果重新打分，提升 Top-K 精度。

```
用户问题
  │
  ▼
向量召回 Top-N（快，粗）  ← VectorStore.similaritySearch
  │
  ▼
Rerank 精排 Top-K（慢，精）← SiliconFlow bge-reranker-v2-m3
  │
  ▼
拼入 Prompt 的最终资料
```

### 4.2 响应式链路（关键知识点）
```java
return Mono.fromCallable(() -> doRerank(...))   // 同步阻塞调用包成 Mono（冷源，订阅时才执行）
        .subscribeOn(Schedulers.boundedElastic()) // ⚠️ 必须：把阻塞调用调度到弹性线程池
        .timeout(Duration.ofMillis(timeoutMs))    // 超时抛 TimeoutException
        .onErrorResume(e -> Mono.empty());         // 任何异常降级为空 Mono
```

> **易错点（曾踩坑）**：`Mono.fromCallable` 的 callable 在**订阅线程同步执行**。若不显式 `subscribeOn(boundedElastic)`，阻塞调用会占用调用方线程，且 `.timeout()` 的计时器要等阻塞调用返回后才启动——**超时配置形同虚设**。必须先 `subscribeOn` 把阻塞调用切到弹性线程池，`timeout` 才能真正生效。

### 4.3 `RestClient.Builder` clone
```java
RestClient restClient = restClientBuilder.clone()   // 复用框架注入的构建器（消息转换器/拦截器）
        .baseUrl(...)
        .defaultHeader("Authorization", "Bearer " + apiKey)
        .build();
```
> 不 `new RestClient.builder()`：保留 Spring Boot 自动配置的默认能力。
> 不复用同一 builder 实例：避免多线程下污染 builder 状态。`clone()` 是折中。

### 4.4 降级策略
任何异常（超时/网络错误/无 API Key）→ `Mono.empty()` → 调用方 `block()` 得 `null` → **回退为向量相似度排序**。保证 Rerank 挂了不影响主流程。

---

## 五、Resilience4j 弹性架构 —— `Resilience4jConfig` + `ResilientAiService`

### 5.1 四道防线
```
ResilientAiService（所有 LLM 调用的包装层）
  ├── @TimeLimiter     → 超时保护（CompletableFuture + Future.get(timeout)）
  ├── @Retry           → 重试（仅网络异常）
  ├── @CircuitBreaker  → 熔断保护（失败率超阈值则熔断）
  └── @Fallback        → 降级友好提示
```

### 5.2 为什么所有方法返回 `CompletableFuture`
`@TimeLimiter` 通过 `Future.get(timeout)` 实现超时：把同步阻塞的 LLM 调用包成 `CompletableFuture.supplyAsync(...)`，TimeLimiter 在外层调度 watchdog，到期 `cancel(true)` 中断 Future 抛 `TimeoutException`。所以方法签名**必须**返回 `CompletableFuture<...>` 才能被切面拦截。

### 5.3 流式与非流式为何用独立实例
| 实例 name | TimeLimiter | Retry | 场景 |
|-----------|-------------|-------|------|
| `chatService` | 30s | 配置（仅网络异常） | 非流式：等完整响应 |
| `sseChatService` | 60s | **不配** | 流式：只限制"首次 token 到达"时间 |

> 流式不配 Retry：流一旦开始推送就**不可重放**，重试会导致前端重复接收 token。
> 流式 TimeLimiter 只管"Flux 就绪前"的等待；一旦开始流式返回，超时由 `SseEmitter`（5 分钟）兜底。

### 5.4 Retry 只配网络异常、忽略业务异常
`retry-exceptions` 只含 `SocketTimeoutException`/`ConnectException`/`ResourceAccessException` 等网络类；`ignore-exceptions` 含 `BusinessException`/`IllegalArgumentException`。
> 业务异常（如 4xx 鉴权失败）重试也是失败，不浪费配额。

### 5.5 降级
每个公开方法都有 `fallbackMethod`：超时/熔断/重试耗尽时返回友好提示而非原始异常。流式 fallback 返回 `Flux.just("[ERROR]" + msg)`，`ChatServiceImpl` 订阅时识别 `[ERROR]` 前缀推送 error 事件。

### 5.6 异常友好化
`getFriendlyMessage` 把 `CallNotPermittedException`（熔断）→ "AI服务当前负载较高，已被熔断保护"；`TimeoutException` → "AI服务响应超时"。避免把技术异常直接抛给用户。

---

## 六、SSE 流式输出 —— `ChatController` + `ChatServiceImpl`

### 6.1 端点
- `POST /chat/stream/sse` → 返回 `SseEmitter`（5 分钟超时）。
- `ChatController` 调 `ChatServiceImpl.chatStreamSse`。

### 6.2 流式协议设计
```
订阅 Flux<String>：
  onNext(chunk):
    ├── chunk 以 "[ERROR]" 开头 → 推 {error: msg} 事件并结束（降级标记）
    └── 否则 → 累积到 full，推 {content: chunk} 事件
  onError(e):
    └── 推 {error: msg} 事件，emitter.completeWithError(e)
  onComplete():
    └── 清洗 full → 落库 → 推 {done:true, content:全文, citations:[...]} 事件
```
> 设计要点：`done` 事件携带**完整回复 + 引用溯源**，前端可在流结束时一次性拿到完整内容和出处，无需自己拼接 token。

### 6.3 两条流式分支
`hasKb`（knowledgeBase 非空）→ 走 RAG 流式（检索 + 缓存 citations）；否则走普通流式。citations 在 `onComplete` 的 done 事件里推送。

---

## 七、会话历史与跨服务持久化

### 7.1 架构（关键：Redis 在 chat 侧，MySQL 在 message 侧）
```
ai-cs-chat                              ai-cs-message
┌─────────────────────┐                 ┌──────────────────┐
│ ChatHistoryService  │  ① Feign 查询   │ MessageController│
│  ├─ Redis 缓存(读)  │ ◄──────────────► │  └─ MessageService│
│  └─ Feign→message   │                 │     └─ MySQL 落库 │
└─────────┬───────────┘                 └────────▲─────────┘
          │ ② RocketMQ 异步落库                   │ ③ 消费
          ▼                                      │
   ChatMessageProducer ──RocketMQ──► ChatMessageConsumer
```

> **重要修正**：Redis 缓存层在 **chat 模块的 `ChatHistoryService`**（读加速），`message` 模块的 `MessageServiceImpl` **只做 MySQL 单写**，无 Redis 双写。`ChatHistoryService.load` = Redis 优先，未命中回源 message 表（Feign）。

### 7.2 双标识设计
- `sessionId`（Long）：message 表主键，message 模块内部用。
- `sessionKey`（String）：chat 模块使用的跨服务字符串标识，用于跨服务对齐会话上下文。
- 两种查询维度：`getSessionMessages(sessionId)` 与 `getMessagesBySessionKey(sessionKey)`（上限 200 条，时间升序）。

### 7.3 异步落库链路
chat 模块产生消息 → `ChatMessageProducer` 发 RocketMQ → message 模块 `ChatMessageConsumer` 消费 → `MessageServiceImpl.saveMessage` insert MySQL。
> 解耦：chat 模块不阻塞等落库，消息持久化失败不影响对话响应。

### 7.4 实体设计
- `ChatSession`：会话（状态机 status、逻辑删除 `@TableLogic`、渠道 channel）。
- `ChatMessage`：消息（sessionId + sessionKey 双轨、role、content，createTime 由 MyBatis-Plus 自动填充）。
- 会话与消息 1:N。

---

## 八、知识库增量同步 —— `KnowledgeVectorService` + RocketMQ

### 8.1 为什么 knowledge 模块自己向量化
- ai-cs-chat 的 VectorStore 面向**检索（读路径）**，knowledge 模块是**写入方（写路径）**。
- 两者共用同一 Chroma 集合 + 同一 Embedding 模型（bge-m3），向量空间必须一致。
- knowledge 模块负责自身内容全生命周期（CRUD → 切块/Embedding/写库/删库），避免对话模块耦合知识库内部结构。

### 8.2 向量化流程（`vectorize`）
```
内容校验（空内容跳过）
  → TokenTextSplitter 按 token 切块（适配 bge-m3 上下文窗口）
  → 每块附加 metadata（knowledgeBase / documentId / title，供检索过滤与溯源）
  → vectorStore.add(chunks)（内部 Embedding + 写 Chroma）
```

### 8.3 RocketMQ 增量同步
```
KnowledgeController（CRUD）
  → KnowledgeServiceImpl 写 MySQL
  → KnowledgeSyncProducer 发消息（TOPIC + action tag：CREATE/UPDATE/DELETE）
  → KnowledgeSyncConsumer 消费
     ├─ CREATE/UPDATE → KnowledgeVectorService.vectorize
     └─ DELETE        → KnowledgeVectorService.deleteByDocumentId（Chroma 过滤表达式删除）
```
> 解耦：DB 写入与向量化异步，知识库变更对对话侧无感；保证 DB 删除后向量库无残留。

### 8.4 `deleteByDocumentId`
`vectorStore.delete("documentId == '123'")` 用 Chroma 过滤表达式按元数据批量删除该文档所有分块。

---

## 九、跨服务协作（Feign）

`ai-cs-chat` 通过 Feign 调用其他模块：
- `MessageFeignClient` → ai-cs-message：查会话历史（按 sessionKey）、查用户会话列表。
- `OrderFeignClient` → ai-cs-order：订单查询（也作为 LLM 的 `@Tool` 工具回调）。

> 设计：chat 模块作为对话编排中心，通过 Feign + RocketMQ 与 message/knowledge/order 解耦协作。

---

## 附：注释覆盖情况

| 模块 | 文件数 | 已加注释 | 说明 |
|------|--------|---------|------|
| ai-cs-chat | 28 | 12 | 16 个已有详尽注释或为 trivial（DTO/接口），无需补充 |
| ai-cs-knowledge | 12 | 12 | 全部补充 |
| ai-cs-message | 11 | 11 | 全部补充 |

所有注释为中文，采用 Javadoc `/** */`（类/方法）与行内 `//`（复杂逻辑）两种形式。**未修改任何代码逻辑/字符/格式**（经 `git diff` 验证：所有删除行均为注释行，无非注释代码行被改动）。

---

## 知识点速查（面试/复盘向）

1. **OpenAI 兼容协议**：DeepSeek/硅基流动都实现了 OpenAI Chat Completions 协议，Spring AI 一套 starter 通吃，只改 base-url/key/model。
2. **Chat 与 Embedding 分家**：DeepSeek 无 Embedding 接口 → Embedding 走硅基流动 bge-m3；`@Primary` + 启动类 exclude 避免冲突。
3. **RAG 两条路径**：Advisor 自动 RAG（普通对话）vs 手动 RAG（chatWithRag，自建 Prompt + 引用溯源）。
4. **Rerank 响应式**：`fromCallable` 必须 `subscribeOn(boundedElastic)`，否则 `timeout` 失效——这是最容易踩的坑。
5. **Resilience4j 超时**：`@TimeLimiter` 要求返回 `CompletableFuture`，通过 `Future.get(timeout)` 实现。
6. **流式不重试**：SSE Flux 不可重放，重试会导致前端重复接收 token。
7. **Redis 在 chat 侧**：会话历史 Redis 缓存在 `ChatHistoryService`，message 模块只做 MySQL 落库（异步 via RocketMQ）。
8. **向量空间一致性**：chat（读）与 knowledge（写）共用同一 Chroma 集合 + 同一 Embedding 模型，不可擅自更换。
9. **增量同步解耦**：知识库 CRUD → RocketMQ → 异步向量化，DB 与向量库最终一致。
10. **降级链**：Rerank 挂 → 回退向量排序；LLM 超时/熔断 → fallback 友好提示；历史压缩失败 → 回退截断。每层都有兜底。
