# 第8章：AI 应用架构与系统设计

> 系统设计题是高级岗位和架构师面试的核心，考察全局视野和工程落地能力。

---

## 8.1 AI 微服务架构

### Q1：如何将 AI 能力集成到现有的 Spring Cloud 微服务架构中？★★★★★

**参考答案：**

**推荐架构模式：AI 能力层作为独立服务**

```
┌─────────────────────────────────────────────────────────┐
│                    API Gateway                          │
├────────┬──────────┬──────────┬───────────┬──────────────┤
│ 用户   │  商品    │  订单    │   支付    │   AI 服务     │
│ 服务   │  服务    │  服务    │   服务    │  （新增）     │
│        │          │          │           │              │
│        │          │          │           │ ┌──────────┐ │
│        │          │          │           │ │Chat 服务 │ │
│        │          │          │           │ ├──────────┤ │
│        │          │          │           │ │Knowledge │ │
│        │          │          │           │ │  服务    │ │
│        │          │          │           │ ├──────────┤ │
│        │          │          │           │ │Search    │ │
│        │          │          │           │ │  服务    │ │
│        │          │          │           │ └──────────┘ │
├────────┴──────────┴──────────┴───────────┴──────────────┤
│            Nacos / RocketMQ / Redis / MySQL              │
└─────────────────────────────────────────────────────────┘
```

**设计原则：**

1. **AI 服务独立部署**：不与业务服务耦合，可独立扩缩容
2. **统一 AI Gateway**：所有 LLM 调用通过统一入口，便于限流、计费、监控
3. **异步优先**：长耗时的 AI 任务（如 RAG、Agent）使用异步处理
4. **降级策略**：AI 服务不可用时，降级到规则引擎或人工处理

**Spring Cloud 集成要点：**

```yaml
# AI 服务注册到 Nacos
spring:
  application:
    name: ai-cs-chat
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR}

# Gateway 路由到 AI 服务
spring.cloud.gateway.routes:
  - id: ai-chat
    uri: lb://ai-cs-chat
    predicates:
      - Path=/api/chat/**
```

---

### Q2：AI 服务的高并发架构如何设计？★★★★★

**参考答案：**

**挑战：** LLM 推理延迟高（1~10s）、成本高，不能像普通 API 一样简单水平扩展。

**架构方案：**

```
高并发请求
   ↓
[1] API Gateway 层
├── 限流：令牌桶/滑动窗口（每用户 QPS 限制）
├── 鉴权：API Key / JWT 校验
└── 负载均衡：轮询 / 最少连接
   ↓
[2] 缓存层
├── 语义缓存：相似问题直接返回缓存结果
├── 热门问答缓存：Redis 存储高频 QA
└── 向量缓存：避免重复 Embedding 计算
   ↓
[3] 队列层（削峰填谷）
├── RocketMQ 消息队列
├── 优先级队列（VIP 用户优先）
└── 超时丢弃（用户已关闭的请求）
   ↓
[4] AI 推理层
├── 多模型实例（负载均衡）
├── 动态扩缩容（基于 GPU 利用率）
└── 请求批处理（batch inference）
   ↓
[5] 异步响应
├── SSE 流式推送（减少等待）
├── WebSocket 长连接
└── 回调通知（任务完成后推送）
```

**关键策略详解：**

**语义缓存：**

```java
public class SemanticCache {
    public Optional<String> get(String query) {
        // 1. 将查询向量化
        float[] queryVector = embeddingModel.embed(query);

        // 2. 在缓存向量库中搜索相似问题
        List<Document> similar = cacheVectorStore.similaritySearch(
            SearchRequest.builder()
                .query(query)
                .topK(1)
                .similarityThreshold(0.95)  // 高度相似才命中缓存
                .build()
        );

        if (!similar.isEmpty()) {
            return Optional.of(similar.get(0).getMetadata().get("answer"));
        }
        return Optional.empty();
    }

    public void put(String query, String answer) {
        Document doc = new Document(answer, Map.of("query", query));
        cacheVectorStore.add(List.of(doc));
    }
}
```

---

### Q3：如何设计一个多租户 AI 客服系统？★★★★★

**参考答案：**

**多租户隔离维度：**

```
租户A（电商公司）
├── 独立知识库（商品FAQ、退换货政策）
├── 独立 System Prompt（品牌调性、话术风格）
├── 独立模型配置（可选 GPT-4 或开源模型）
└── 独立计费

租户B（金融公司）
├── 独立知识库（产品说明、合规文档）
├── 独立 System Prompt（专业严谨风格）
├── 独立模型配置（要求私有化部署）
└── 独立计费
```

**数据隔离方案：**

| 方案                     | 隔离级别 | 成本  | 适用场景        |
| ---------------------- | ---- | --- | ----------- |
| 独立数据库                  | 最高   | 高   | 大客户、合规要求高   |
| Schema 隔离              | 高    | 中   | 中等规模客户      |
| 行级隔离 + Metadata Filter | 中    | 低   | 小客户、SaaS 模式 |

**行级隔离实现：**

```java
// 向量库按租户隔离
SearchRequest request = SearchRequest.builder()
    .query(userQuery)
    .filterExpression("tenant_id == '" + currentTenantId + "'")
    .topK(5)
    .build();

// System Prompt 按租户配置
String systemPrompt = tenantConfigService.getSystemPrompt(currentTenantId);

// 对话记忆按租户隔离
String conversationId = currentTenantId + ":" + userId;
```

---

## 8.2 成本控制

### Q4：如何控制 AI 应用的 LLM 调用成本？★★★★★

**参考答案：**

**成本组成：**

```
总成本 = 输入 Token 费用 + 输出 Token 费用 + 工具调用费用 + 基础设施费用
```

**优化策略：**

| 策略        | 节省幅度   | 实现方式                   |
| --------- | ------ | ---------------------- |
| 语义缓存      | 30~60% | 相似问题复用缓存结果             |
| Prompt 精简 | 10~30% | 去掉冗余指令、压缩上下文           |
| 模型降级      | 20~50% | 简单问题用小模型，复杂问题用大模型      |
| 批量处理      | 10~20% | 合并多个请求为一次 batch 调用     |
| 短回答模式     | 10~20% | 指导 LLM 简洁回答，减少输出 Token |
| 上下文压缩     | 15~30% | 只注入最相关的 RAG 片段         |

**模型路由（智能降级）：**

```java
public class ModelRouter {
    public String route(String userQuery) {
        // 简单问题 → 小模型（成本低）
        if (isSimpleQuery(userQuery)) {
            return "gpt-4o-mini";  // 或 Qwen-7B
        }
        // 复杂推理 → 大模型（能力强）
        if (isComplexQuery(userQuery)) {
            return "gpt-4";  // 或 Qwen-72B
        }
        // 默认 → 中等模型
        return "gpt-4o";
    }
}
```

---

### Q5：如何监控和优化 Token 使用量？★★★★☆

**参考答案：**

**监控指标：**

```java
// 每次 LLM 调用记录 Token 使用
record TokenUsage(
    String tenantId,
    String modelName,
    int promptTokens,      // 输入 Token
    int completionTokens,  // 输出 Token
    int totalTokens,       // 总 Token
    double costInDollars   // 本次调用成本
) {}

// 监控大盘指标
// - 每租户每日 Token 消耗量
// - 每模型 Token 消耗分布
// - 每接口平均 Token 使用
// - 成本趋势
```

**优化手段：**

1. **Token 预算控制**：每租户每月设置 Token 使用上限
2. **实时告警**：Token 使用异常飙升时告警
3. **Prompt 缓存**：OpenAI Prompt Caching（相同前缀的 Prompt 缓存，输入 Token 减半价）
4. **按需注入上下文**：RAG 只注入 top-K 相关片段，不贪多

---

## 8.3 高可用设计

### Q6：AI 服务如何实现高可用和容灾？★★★★☆

**参考答案：**

**高可用架构：**

```
                    ┌─── AI Provider A（OpenAI）─── 主
请求 → Gateway ────┤
                    ├─── AI Provider B（Azure OpenAI）── 备
                    │
                    └─── AI Provider C（本地 Ollama）── 兜底
```

**故障场景与应对：**

| 故障场景       | 应对策略            |
| ---------- | --------------- |
| LLM API 超时 | 超时重试 + 切换备用模型   |
| LLM API 限流 | 请求队列 + 退避重试     |
| LLM 服务不可用  | 多 Provider 故障转移 |
| 向量数据库故障    | 降级为关键词检索        |
| GPU 资源不足   | 请求排队 + 弹性扩缩     |

**多模型故障转移实现：**

```java
public class FailoverChatModel implements ChatModel {
    private final List<ChatModel> models;  // [主模型, 备模型, 兜底模型]

    @Override
    public ChatResponse call(Prompt prompt) {
        for (ChatModel model : models) {
            try {
                return model.call(prompt);
            } catch (Exception e) {
                log.warn("Model {} failed, trying next", model.getClass().getSimpleName());
            }
        }
        throw new AllModelsUnavailableException("所有模型不可用");
    }
}
```

---

### Q7：如何设计 AI 客服系统中的会话管理？★★★★☆

**参考答案：**

**会话状态机：**

```
[新建] → [AI处理中] → [AI已回答]
                ↓              ↓
          [转人工]      [用户追问] → [AI处理中]
                ↓
          [人工处理中] → [已解决] → [满意度评价]
```

**会话存储设计：**

```java
// 会话元数据
record ChatSession(
    String sessionId,
    String userId,
    String tenantId,
    SessionStatus status,        // NEW/AI_PROCESSING/AI_ANSWERED/HUMAN/RESOLVED
    String assignedAgentId,      // 人工客服ID（如果转人工）
    int messageCount,            // 消息计数
    double satisfactionScore,    // 满意度评分
    LocalDateTime createdAt,
    LocalDateTime lastActiveAt
) {}

// 消息记录
record ChatMessage(
    String messageId,
    String sessionId,
    MessageRole role,            // USER/AI/HUMAN_AGENT
    String content,
    String modelUsed,            // 使用的模型
    int tokenUsage,
    List<ToolCall> toolCalls,    // 工具调用记录
    LocalDateTime timestamp
) {}
```

---

## 8.4 系统设计实战题

### Q8：设计一个企业级 RAG 知识库问答系统（系统设计题）。★★★★★

**参考答案（架构设计）：**

```
┌──────────────────────────────────────────────────────────────────┐
│                        客户端层                                  │
│  Web/App/API → SSE 流式响应                                       │
└───────────────────────┬──────────────────────────────────────────┘
                        ↓
┌───────────────────────┴──────────────────────────────────────────┐
│                    API Gateway + 限流/鉴权                         │
└───────────────────────┬──────────────────────────────────────────┘
                        ↓
┌───────────────────────┴──────────────────────────────────────────┐
│                    AI Chat Service                               │
│  ┌─────────┐  ┌──────────┐  ┌──────────┐  ┌────────────────┐    │
│  │Query    │→ │Semantic  │→ │RAG       │→ │LLM Generation  │    │
│  │Rewrite  │  │Cache     │  │Retrieval │  │+ Guardrails    │    │
│  └─────────┘  └──────────┘  └──────────┘  └────────────────┘    │
└───────────────────────┬──────────────────────────────────────────┘
                        ↓
┌───────────────────────┴──────────────────────────────────────────┐
│                    知识管理层                                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
│  │文档上传   │→ │文档解析   │→ │Chunking  │→ │Embedding │→ Store  │
│  │(OSS)     │  │(Tika)    │  │+ 清洗    │  │+ Index   │        │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘        │
└──────────────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────────────┐
│                    数据存储层                                     │
│  PGVector(向量) │ MySQL(业务) │ Redis(缓存) │ RocketMQ(消息)       │
└──────────────────────────────────────────────────────────────────┘
```

**关键设计决策：**

1. **检索**：混合检索（向量 + BM25）+ Rerank
2. **缓存**：语义缓存 + Redis 热门 QA 缓存
3. **安全**：输入 Guardrail + 输出 Guardrail + 审计日志
4. **扩展**：知识库增量更新、多租户隔离、模型故障转移

---

## 本章小结

**必背 TOP 5：**

1. AI 能力集成到微服务架构的模式
2. 高并发设计（缓存 + 队列 + 异步）
3. 多租户数据隔离方案
4. LLM 成本控制策略（缓存 + 路由 + 精简）
5. 高可用设计（多模型故障转移）
