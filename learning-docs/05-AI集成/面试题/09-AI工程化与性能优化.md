# 第9章：AI 工程化与性能优化

> 工程化能力是区分"能做 Demo"和"能上生产"的关键，中高级岗位面试重点考察。

---

## 9.1 性能优化

### Q1：LLM 调用延迟高（1~10 秒），如何优化用户体验？★★★★★

**参考答案：**

**前端优化：**

1. **流式输出（SSE/WebSocket）**：逐 token 显示，用户无需等完整响应
2. **骨架屏 + 打字动画**：减少感知等待时间
3. **进度反馈**："正在思考中..." → "正在查询知识库..." → "正在生成回答..."

**后端优化：**

1. **语义缓存**：命中缓存时响应 <100ms
2. **并行处理**：RAG 检索 + 意图识别并行执行
3. **预热**：首次调用前建立连接池，减少首次延迟
4. **模型选择**：简单问题用小模型（更快），复杂问题用大模型

**架构优化：**

```
用户提问
   ↓
[并行分支1] 语义缓存查询 → 命中？→ 直接返回（<100ms）
[并行分支2] 意图识别 → 判断需要哪些工具
[并行分支3] RAG 检索 → 获取相关知识
   ↓ 全部完成后
LLM 生成（流式输出）→ 用户实时看到回答
```

---

### Q2：如何优化 RAG 系统的端到端延迟？★★★★★

**参考答案：**

**RAG 延迟分解：**

```
总延迟 = Embedding延迟 + 向量检索延迟 + Rerank延迟 + LLM生成延迟
         ~200ms        ~100ms        ~500ms      ~2000ms
```

**各阶段优化策略：**

| 阶段              | 原始延迟    | 优化后    | 优化方法                 |
| --------------- | ------- | ------ | -------------------- |
| Query Embedding | ~200ms  | ~50ms  | 本地 Embedding 模型 / 缓存 |
| 向量检索            | ~100ms  | ~20ms  | HNSW 索引 / 合理参数       |
| Rerank          | ~500ms  | ~200ms | 本地 Rerank 模型 / 减少候选数 |
| LLM 首 Token     | ~1000ms | ~500ms | 小模型 / Prompt 精简      |
| LLM 生成          | ~2000ms | 不变     | 流式输出减少感知延迟           |

**关键优化实践：**

```java
// 1. Embedding 缓存：相同查询不重复计算
@Cacheable(value = "embedding", key = "#query")
public float[] getEmbedding(String query) {
    return embeddingModel.embed(query);
}

// 2. 并行执行：检索和意图识别并行
CompletableFuture<List<Document>> searchFuture = 
    CompletableFuture.supplyAsync(() -> vectorStore.similaritySearch(request));
CompletableFuture<String> intentFuture = 
    CompletableFuture.supplyAsync(() -> classifyIntent(query));

// 等待两者完成
CompletableFuture.allOf(searchFuture, intentFuture).join();

// 3. Rerank 优化：只对 top-10 做 rerank，而非 top-50
List<Document> candidates = vectorStore.similaritySearch(topK=10);
List<Document> reranked = rerankModel.rerank(candidates, query, topK=3);
```

---

### Q3：如何提高 LLM 的吞吐量（Throughput）？★★★★☆

**参考答案：**

**吞吐量指标：** 每秒处理的请求数（RPS）或每秒生成的 Token 数（TPS）。

**优化方法：**

1. **请求批处理（Batching）**

   ```text
   不使用批处理：
   请求1 → LLM → 响应1（GPU 利用率低）
   请求2 → LLM → 响应2
   请求3 → LLM → 响应3

   使用批处理：
   [请求1, 请求2, 请求3] → LLM → [响应1, 响应2, 响应3]
   GPU 利用率提高 3 倍
   ```

2. **vLLM PagedAttention**
- 动态分配 KV Cache 内存，避免浪费
- 支持连续批处理（continuous batching）
- 吞吐量提升 2~4 倍

3. **模型量化**
- FP16 → INT8：速度提升 ~1.5 倍，精度损失 <1%
- INT8 → INT4：速度提升 ~2 倍，精度损失 1~3%
- 使用 GPTQ / AWQ / GGUF 量化格式

4. **推测解码（Speculative Decoding）**
- 用小模型（Draft Model）快速生成候选 token
- 大模型验证候选 token 是否正确
- 正确则接受（一次验证多个 token），速度提升 2~3 倍

---

## 9.2 可观测性

### Q4：AI 应用的可观测性（Observability）如何设计？★★★★★

**参考答案：**

**AI 应用可观测性的三大支柱：**
```

┌──────────────────────────────────────────────────────┐
│                  AI Observability                    │
├──────────────┬──────────────┬────────────────────────┤
│   Metrics    │    Logs      │     Traces             │
│   （指标）    │   （日志）    │    （链路追踪）          │
├──────────────┼──────────────┼────────────────────────┤
│ Token 使用量  │ 用户输入      │ 请求全链路              │
│ 响应延迟     │ LLM 输出      │ ├── Query Rewrite      │
│ 缓存命中率   │ 工具调用记录   │ ├── Embedding          │
│ 幻觉率      │ 安全拦截记录   │ ├── Vector Search      │
│ 用户满意度   │ 错误堆栈      │ ├── Rerank             │
│ 模型调用次数  │              │ ├── LLM Generation     │
│ 成本统计     │              │ └── Guardrails         │
└──────────────┴──────────────┴────────────────────────┘

```
**关键 Metrics：**
```java
// 使用 Micrometer 注册指标
@Component
public class AIMetrics {
    private final Counter llmCallCounter;
    private final Timer llmLatencyTimer;
    private final Counter tokenUsageCounter;
    private final Counter cacheHitCounter;
    private final Counter guardrailBlockCounter;

    public AIMetrics(MeterRegistry registry) {
        llmCallCounter = Counter.builder("ai.llm.calls")
            .tag("model", "gpt-4")
            .register(registry);
        llmLatencyTimer = Timer.builder("ai.llm.latency")
            .register(registry);
        tokenUsageCounter = Counter.builder("ai.tokens.used")
            .tag("type", "input")  // input/output
            .register(registry);
    }
}
```

**链路追踪集成（OpenTelemetry）：**

```java
// Spring AI 自动集成 OpenTelemetry
// 每个环节自动记录 span：
// span: ai.chat (parent)
//   ├── span: ai.embedding
//   ├── span: ai.vectorstore.search
//   ├── span: ai.rerank
//   ├── span: ai.llm.call
//   └── span: ai.guardrail
```

**推荐工具栈：**

- Metrics → Prometheus + Grafana
- Logs → ELK / Loki
- Traces → Jaeger / Zipkin / SkyWalking
- AI 专用 → LangSmith / Langfuse / Phoenix

---

### Q5：如何使用 Langfuse/LangSmith 监控 AI 应用？★★★★☆

**参考答案：**

**Langfuse（开源可自托管）：**

```java
// Spring AI 集成 Langfuse
// 配置
spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
    observability:
      langfuse:
        public-key: ${LANGFUSE_PUBLIC_KEY}
        secret-key: ${LANGFUSE_SECRET_KEY}
        host: https://cloud.langfuse.com

// 自动追踪：
// - 每次 ChatModel 调用
// - 输入输出 Token 数
// - 延迟
// - 使用的模型
// - 完整的 Prompt 和 Response
```

**Langfuse 看板关注的指标：**

- 每日/每周 Token 消耗趋势
- 每接口平均延迟和 P99 延迟
- 缓存命中率
- 用户反馈评分
- 幻觉率趋势
- 成本统计

---

## 9.3 CI/CD 与测试

### Q6：AI 应用如何做自动化测试？★★★★★

**参考答案：**

**AI 应用的测试挑战：** LLM 输出不确定性，传统断言（assertEquals）不适用。

**测试分层策略：**

```
┌─────────────────────────────────────────────────┐
│  L4: E2E 测试（端到端）                           │
│  真实 LLM 调用，LLM-as-Judge 评估质量              │
├─────────────────────────────────────────────────┤
│  L3: 集成测试                                    │
│  Mock LLM，测试 RAG 流水线、工具调用链路             │
├─────────────────────────────────────────────────┤
│  L2: 单元测试                                    │
│  测试 Chunking、Embedding、向量检索逻辑              │
├─────────────────────────────────────────────────┤
│  L1: 回归测试集                                   │
│  固定 QA 数据集，监控回答质量变化                     │
└─────────────────────────────────────────────────┘
```

**LLM-as-Judge 测试示例：**

```java
@SpringBootTest
class ChatServiceE2ETest {

    @Test
    void should_answer_product_question_correctly() {
        // 给定用户问题
        String question = "iPhone 16 Pro 的电池容量是多少？";

        // 当调用 AI 客服
        String answer = chatService.chat(question);

        // 然后用另一个 LLM 评估回答质量
        String evalPrompt = String.format("""
            评估以下回答的质量：
            问题：%s
            回答：%s

            请判断：
            1. 回答是否正确（true/false）
            2. 回答是否完整（true/false）
            3. 回答是否有幻觉（true/false）

            以JSON格式输出：{"correct": bool, "complete": bool, "hallucination": bool}
            """, question, answer);

        EvalResult eval = evaluateWithLLM(evalPrompt);
        assertTrue(eval.correct());
        assertTrue(eval.complete());
        assertFalse(eval.hallucination());
    }
}
```

**回归测试集管理：**

```java
// 维护标准 QA 测试集
record TestCase(String question, String expectedAnswer, String category) {}

List<TestCase> testSuite = List.of(
    new TestCase("退货流程是什么？", "您可以在订单详情页点击申请退货...", "after-sales"),
    new TestCase("你们支持哪些支付方式？", "支持微信支付、支付宝、银行卡...", "payment"),
    // ... 100+ 测试用例
);

// CI/CD 中运行回归测试
@Test
void regression_test() {
    int passCount = 0;
    for (TestCase tc : testSuite) {
        String answer = chatService.chat(tc.question());
        if (evaluateQuality(tc, answer)) {
            passCount++;
        }
    }
    double passRate = (double) passCount / testSuite.size();
    assertTrue(passRate >= 0.85, "通过率低于85%，当前：" + passRate);
}
```

---

### Q7：AI 应用的 CI/CD 流程如何设计？★★★★☆

**参考答案：**

```
代码提交
   ↓
[CI 流水线]
├── 代码质量检查（SonarQube）
├── 单元测试（Mock LLM）
├── 集成测试（Mock LLM + 真实向量库）
├── Prompt 回归测试（固定数据集 + LLM-as-Judge）
├── 安全扫描（Prompt Injection 测试集）
└── 构建 Docker 镜像
   ↓
[CD 部署]
├── 部署到 Staging 环境
├── E2E 测试（真实 LLM 调用）
├── 性能测试（延迟、吞吐量基准）
├── A/B 测试（新旧 Prompt/模型对比）
└── 灰度发布 → 全量上线
```

**关键 CI 检查项：**

- Prompt 变更必须跑回归测试
- 模型版本变更需要性能基准测试
- 安全测试集通过率 >= 95%
- 回归测试通过率 >= 85%

---

## 9.4 模型部署

### Q8：本地部署开源模型（如 Qwen/LLaMA）需要考虑哪些因素？★★★★☆

**参考答案：**

**硬件需求估算：**

| 模型规模 | FP16 显存 | INT8 量化 | INT4 量化 | 推荐 GPU          |
| ---- | ------- | ------- | ------- | --------------- |
| 7B   | ~14GB   | ~7GB    | ~4GB    | RTX 4090 (24GB) |
| 14B  | ~28GB   | ~14GB   | ~8GB    | A100 (40GB)     |
| 32B  | ~64GB   | ~32GB   | ~18GB   | A100 (80GB)     |
| 70B  | ~140GB  | ~70GB   | ~40GB   | 2×A100 (80GB)   |

**部署方案选择：**

```
开发环境：
├── Ollama（一键部署，适合本地开发测试）
└── docker run ollama/ollama

生产环境：
├── vLLM（高吞吐，推荐首选）
│   docker run vllm/vllm-openai --model Qwen/Qwen2.5-7B
│
├── TGI（HuggingFace 生态）
│   docker run ghcr.io/huggingface/text-generation-inference
│
└── llama.cpp（纯 CPU / 边缘设备）
```

**Java 集成：**

```yaml
# 本地 Ollama
spring.ai.ollama:
  base-url: http://localhost:11434
  chat.model: qwen2.5:7b

# 生产 vLLM（兼容 OpenAI API）
spring.ai.openai:
  base-url: http://vllm-server:8000
  api-key: not-needed  # 本地部署不需要
  chat.options.model: qwen2.5-7b-instruct
```

---

## 本章小结

**必背 TOP 5：**

1. LLM 调用延迟优化（缓存 + 并行 + 流式）
2. RAG 端到端延迟优化
3. AI 可观测性三大支柱
4. AI 应用测试策略（LLM-as-Judge）
5. 开源模型部署方案选型
