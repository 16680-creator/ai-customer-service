# 前端技术全景介绍 —— 写给 Java 后端的第一课

> **本篇定位**：不写一行代码，先帮你建立前端世界的"全景地图"。搞清楚前端有哪些技术、它们解决什么问题、彼此是什么关系，再进入后续篇章就不会晕。
> **前置知识**：无。会写 Spring Boot 接口即可。
> **阅读时长**：约 20 分钟。
> **下一篇**：[01-HTML与CSS基础](./01-HTML与CSS基础.md)

---

## 一、前端到底在做什么

你写的 Spring Boot 服务是这样的：

```
用户 → [???] → HTTP JSON 接口 → Spring Boot → MySQL
```

中间那个 `[???]`，就是前端的全部职责：**把后端吐出来的数据，变成用户看得见、点得动的界面**。

前后端分离后的完整链路：

```
┌─────────────── 浏览器（用户电脑） ───────────────┐
│                                                  │
│   index.html + app.js + style.css   ← 静态资源    │
│        │                                         │
│        ▼                                         │
│   JS 执行 → 调 axios 发 HTTP 请求 ──────────┐     │
│                                            │     │
└────────────────────────────────────────────┼─────┘
                                             ▼
                              ┌──────────────────────────┐
                              │  Nginx（静态资源 + 反向代理）│
                              └──────────┬───────────────┘
                                         ▼ /api/**
                              ┌──────────────────────────┐
                              │  Spring Boot（你负责的）    │
                              └──────────┬───────────────┘
                                         ▼
                              ┌──────────────────────────┐
                              │  MySQL / Redis / MQ       │
                              └──────────────────────────┘
```

关键认知转变：

| 你原来的世界 | 前端的世界 |
|---|---|
| JSP/Thymeleaf：后端拼好 HTML 整页返回 | 后端只出 JSON 接口，HTML/JS 在浏览器里"现场生成"页面 |
| 会话状态存在服务端 Session | 状态大量存在浏览器内存/localStorage 里 |
| 一改代码重启服务 | 改完代码浏览器热更新（Vite HMR），不重启 |
| 用户请求由 Tomcat 线程池处理 | "页面逻辑"由浏览器里**单线程**的 JS 处理 |

> 📌 **SPA（Single Page Application，单页应用）**：现代中后台的标准形态。浏览器只加载一次 HTML，之后所有页面跳转都由 JS 在前端完成（不刷新整页），只向后端要数据。你现在维护的 `ai-cs-frontend` 就是 SPA。

---

## 二、Web 三大基石：HTML / CSS / JavaScript

一切前端技术都是这三样的封装和演化。先用 Java 思维建立类比：

| 三大件 | 干什么 | Java 世界类比 |
|---|---|---|
| **HTML** | 描述页面结构："这里有个标题、这里有个表格、这里有个输入框" | ≈ 数据库表结构 / DTO 的字段骨架 |
| **CSS** | 描述外观："这个标题 24px 加粗、表格隔行变色" | ≈ 皮肤主题 / 拦截器式的全局装饰 |
| **JavaScript** | 描述行为："点击按钮 → 调接口 → 更新表格" | ≈ Service 业务逻辑，也是唯一的"可编程语言" |

三者的关系一句话：**HTML 是骨架，CSS 是皮肤，JS 是肌肉和神经**。浏览器是它们的运行时（类比 JVM）。

> 详细展开见 [01-HTML与CSS基础](./01-HTML与CSS基础.md) 和 [02-JavaScript核心基础](./02-JavaScript核心基础.md)。

---

## 三、前端框架为什么存在

### 3.1 远古时代：手写 DOM（命令式）

2010 年前后用 jQuery 写页面，改一行数据要手动操作 DOM：

```javascript
// jQuery 时代：命令式 —— 每一步都亲手操作界面
$.ajax({ url: '/api/users', success: function(data) {
  $('#user-list').empty();
  data.forEach(u => {
    $('#user-list').append('<li>' + u.name + '</li>');  // 手动拼 HTML
  });
}});
```

数据一变，你要自己找到所有受影响的 DOM 位置逐一更新。页面复杂后必然失控——**这就像不用 ORM，手写每一条 SQL 和 ResultSet 逐列映射**。

### 3.2 现代：数据驱动视图（声明式）

Vue/React 的核心思想只有一句话：

> **你只负责维护"状态"（数据），视图由框架根据状态自动算出来。状态变了，界面自动变。**

```vue
<!-- Vue 3：声明式 —— 只声明"界面长什么样由数据决定" -->
<template>
  <ul>
    <li v-for="u in users" :key="u.id">{{ u.name }}</li>
  </ul>
</template>

<script setup>
import { ref, onMounted } from 'vue'
const users = ref([])                    // 这就是"状态"

onMounted(async () => {
  users.value = await fetch('/api/users').then(r => r.json())
  // 只改数据，列表自动渲染，不用碰任何 DOM
})
</script>
```

类比到 Java：

| 前端 | Java 类比 |
|---|---|
| jQuery 手写 DOM | JDBC 手写 SQL + 手动逐列映射 ResultSet |
| Vue/React 数据驱动 | MyBatis-Plus / Spring Data：声明"要什么"，底层细节交给框架 |
| 组件（Component） | Spring Bean：可复用、可组合、可注入的功能单元 |
| `<UserTable :users="users">` | 方法调用 `render(new UserTable(users))`，传参即配置 |

### 3.3 组件化：前端界的"微服务"

整个页面被拆成一颗组件树，每个组件 = 自己的模板 + 逻辑 + 样式，像一个高内聚的小模块：

```
App
├── Sidebar（侧边导航）
├── Header（顶栏）
└── ChatView（AI 对话页）
    ├── MessageList（消息列表）── 状态：messages[]
    │   └── MessageItem × N
    └── InputBox（输入框）──── 状态：inputText
```

组件之间通过 **props 传数据下行、事件通知上行**——类似"入参 + 回调"，保证数据流向单一、可追溯（详见 [08-Vue3进阶：组件设计与组合式函数](./08-Vue3进阶：组件设计与组合式函数.md)）。

---

## 四、主流框架对比与选型

### 4.1 四大框架一张表

| 维度 | **Vue 3** | **React** | Angular | Svelte |
|---|---|---|---|---|
| 出品方 | 尤雨溪（个人→社区） | Meta | Google | 社区 |
| 语法风格 | 模板 + 响应式（自动追踪依赖） | JSX（JS 里写 HTML）+ 手动声明更新 | 模板 + TypeScript 强绑定 | 编译期响应式，无虚拟 DOM |
| 学习曲线 | ⭐ 最平缓，中文文档一流 | ⭐⭐ 需适应"一切皆 JS" | ⭐⭐⭐⭐ 概念多、最陡 | ⭐⭐ 小众但优雅 |
| 心智负担 | 低（框架帮你追依赖） | 中（要自己管依赖数组、不可变更新） | 高 | 低 |
| 国内生态 | 🏆 中后台统治级（Element Plus / Ant Design Vue / Naive UI） | 🏆 大厂主流 + React Native | 金融/外企存量多 | 少 |
| 对应移动端 | uni-app（Vue 语法写小程序/App） | React Native | Ionic | — |
| 对应全栈框架 | Nuxt 3 | Next.js | — | SvelteKit |

### 4.2 给 Java 转全栈的选型建议

> ✅ **主修 Vue 3，辅修 React。**
>
> 理由：① 国内中后台管理系统 Vue 占据统治地位，Element Plus 生态完整；② 中文文档质量极高，自学成本低；③ Vue 的响应式模型对后端更直觉（改数据→界面自动变，像改了缓存字段界面跟着刷）；④ 你本仓库的 `ai-cs-frontend` 就是 Vue 3。
>
> React 也要会读会写：面更广、Next.js/React Native 都在 React 系，见 [11-React快速上手](./11-React快速上手.md)（专门为 Vue 开发者写的映射篇）。
>
> ❌ Angular/Svelte：知道存在即可，不必投入。

---

## 五、前端生态地图（对照 Java 世界）

这是本篇最重要的一张表——**前端每个工具几乎都能在 Java 世界找到对应物**：

| 前端技术 | 干什么 | Java 世界对应物 |
|---|---|---|
| **npm / pnpm** | 包管理与仓库（npmjs.com） | Maven / Maven Central |
| **package.json** | 依赖声明 + 脚本入口 | pom.xml |
| **package-lock.json** | 锁定依赖版本 | （Maven 的 version range 锁定，等价物） |
| **Vite** | 开发服务器 + 打包构建（本项目在用） | Spring Boot DevTools + maven-shade-plugin |
| **Webpack** | 上一代构建工具（老项目常见） | 同上，理解即可 |
| **Vue Router** | 前端路由：URL ↔ 组件映射 | @RequestMapping + 网关路由 |
| **Pinia / Vuex / Redux** | 全局状态管理 | 应用内共享状态（注意：不是缓存，见下） |
| **axios / fetch** | HTTP 客户端 | RestTemplate / OpenFeign |
| **Element Plus / Ant Design** | 现成 UI 组件库（表格/表单/弹窗） | 若依/ JeecgBoot 后台模板里的成品组件 |
| **Sass / Less** | CSS 预处理（变量/嵌套） | （无直接对应，类似模板增强） |
| **Tailwind CSS** | 原子化 CSS（class 即样式） | （无直接对应， utility-first 风格） |
| **ESLint** | 代码质量检查 | CheckStyle / SpotBugs / SonarQube |
| **Prettier** | 代码格式化 | formatter-maven-plugin |
| **Vitest / Jest** | 单元测试 | JUnit 5 |
| **Playwright / Cypress** | E2E 测试 | Selenium |
| **Node.js** | JS 的服务器运行时 + 全部前端工具链的地基 | JDK 之于 Java |
| **Express / NestJS** | Node 后端框架（NestJS≈Spring） | Spring Boot |
| **pnpm workspace / Nx** | monorepo 管理 | Maven 多模块聚合工程 |

> 📌 **关于"状态管理"**：前端说的"状态"（state）指当前界面依赖的所有可变数据（登录用户、选中 tab、表单草稿……），因为它全在浏览器内存里，组件树又要共享，才需要 Pinia 这类"全局响应式仓库"。别和后端 Redis 缓存划等号——生命周期完全不同。详见 [07-Pinia状态管理实战](./07-Pinia状态管理实战.md)。

> 📌 **关于 Node.js**：你至少要会三件事——用 npm 装包、跑 `npm run dev/build`、看懂 `package.json`。Node 本身还能写后端（Express/NestJS），但 Java 后端转全栈通常继续用 Spring Boot 做服务端，Node 只当工具链用。详见 [09-前端工程化：Vite构建与规范](./09-前端工程化：Vite构建与规范.md)。

---

## 六、不同场景下的"前端"长什么样

"学前端"不是学一样东西，而是按目标产品选技术栈：

| 产品形态 | 典型技术栈 | 你需要学的 | 学习优先级 |
|---|---|---|---|
| **中后台管理系统**（OA、运营后台、客服系统） | Vue 3 / React + 组件库 + Vite | 本目录 01~09 篇 | 🏆 最高，你的主战场 |
| **C 端 H5**（营销页、活动页） | 同上 + 移动适配（rem/vw） | [13-移动端与小程序开发](./13-移动端与小程序开发.md) 第二节 | ⭐⭐ |
| **微信小程序** | 原生小程序 / uni-app / Taro | uni-app（Vue 语法直接复用） | ⭐⭐ |
| **App** | uni-app / React Native / Flutter | 按团队栈 | ⭐ |
| **官网 / 内容站**（要 SEO） | Nuxt 3 / Next.js（SSR/SSG） | [12-Nuxt与SSR入门](./12-Nuxt与SSR入门.md) | ⭐⭐ |
| **桌面客户端** | Electron（VSCode 同款方案） | 概念了解即可 | ⭐ |

结论：**把"Vue 3 全家桶 + 工程化"这一条线走通（本目录 01~10 篇），其余场景都是它的变体**——uni-app 用 Vue 语法、Electron 用 Vue 渲染界面、Nuxt 是 Vue 的 SSR 上层封装。

---

## 七、前后端怎么协作（后端读者的主场）

这部分你有天然优势，建立几个共识即可：

1. **契约先行**：后端先定接口（URL、入参、统一返回体 `R<T>`），用 Swagger/Knife4j 或 Apifox 导出文档。前端按文档开发，甚至用 Mock 先行——**等价于你熟悉接口设计 + 契约测试**。
2. **类型同步**：进阶玩法是用工具从 OpenAPI 生成前端 TypeScript 类型（openapi-typescript），保证后端改字段前端编译期就报错——这是后端"防甲方式改需求"经验的直接迁移（见 [03-TypeScript快速上手](./03-TypeScript快速上手.md)）。
3. **联调环境**：本地前端 `5173` 端口调后端 `8080`，跨域问题用 Vite proxy 解决（开发期）/ Nginx 反代（生产期），这是前后端联调第一坑，详见 [10-浏览器原理与HTTP跨域](./10-浏览器原理与HTTP跨域.md)。
4. **鉴权**：JWT 由后端签发，前端负责"存哪、怎么带、过期怎么办"三件事（详见 10 篇第四节）。
5. **实时推送**：消息通知、AI 流式回答用 WebSocket/SSE，后端 SSE 端点 + 前端 EventSource/fetch 流式读取（详见 10 篇第五节）。

---

## 八、Java 后端转前端的认知差异

先泼三盆冷水，再给你四个红利：

**三盆冷水（最容易被后端经验反噬的点）：**

| 差异 | 说明 | 对策 |
|---|---|---|
| ❌ 没有"编译期"兜底 | JS 是解释型 + 弱类型，字段写错名运行时才炸，且报错堆栈远不如 Java 友好 | 上 TypeScript（03 篇）+ ESLint（09 篇），把错误提前到"编辑期" |
| ❌ 异步无处不在 | 所有 IO 都是异步回调/Promise，没有"阻塞式写法"（严格说 async/await 是语法糖） | 02 篇异步章节逐行吃透，类比 CompletableFuture |
| ❌ 用户环境不可控 | 你控制不了用户用什么浏览器、什么网络、什么缩放比例 | 学会用 F12 开发者工具自检（01 篇），关心兼容性基线 |

**四个红利（后端经验直接复用）：**

1. **工程化思维**：lint/CI/CD/依赖管理，前端全有对应物（09 篇）。
2. **接口设计能力**：RESTful、统一返回体、错误码体系，前端消费接口的体验直接由你的设计质量决定。
3. **性能意识**：你懂缓存、批量、索引；对应到前端就是懒加载、防抖、按需打包（14 篇面试题）。
4. **架构能力**：分层、解耦、单向依赖，组件设计原则一模一样（08 篇）。

---

## 九、学习路线总览（本目录导航）

详细版在 [README](./README.md)，这里给一张浓缩图：

| 阶段 | 篇目 | 目标 | 预计耗时 |
|---|---|---|---|
| ⓪ 认知 | **本篇** | 知道前端世界有什么、为什么 | 1 天 |
| ① 三大基石 | [01](./01-HTML与CSS基础.md) → [02](./02-JavaScript核心基础.md) → [03](./03-TypeScript快速上手.md) | 能读写 HTML/CSS；JS 吃透异步；TS 能上手 | 2~3 周 |
| ② Vue 全家桶 | [04](./04-Vue3核心基础.md) → [05](./05-ElementPlus与前后端联调.md) → [06](./06-路由守卫与Axios请求封装.md) → [07](./07-Pinia状态管理实战.md) | 能独立开发 ai-cs-frontend 这样的管理端 | 3~4 周 |
| ③ 进阶与工程化 | [08](./08-Vue3进阶：组件设计与组合式函数.md) → [09](./09-前端工程化：Vite构建与规范.md) → [10](./10-浏览器原理与HTTP跨域.md) | 组件设计、构建部署、跨域缓存全打通 | 2~3 周 |
| ④ 拓宽视野 | [11](./11-React快速上手.md) → [12](./12-Nuxt与SSR入门.md) → [13](./13-移动端与小程序开发.md) | React 能上手；SSR/跨端会选型 | 2 周 |
| ⑤ 检验 | [14-前端面试题精选](./14-前端面试题精选.md) | 自检 + 面试 | 持续 |

---

## 本章小结

- 前端 = 把后端 JSON 变成可用界面，现代形态是 SPA：JS 在浏览器里生成页面、跳转不刷新。
- 三大基石 HTML/CSS/JS，一切框架都是对它们的封装；浏览器是 JS 的运行时。
- 框架的革命：**数据驱动视图**——你管数据，框架管 DOM（类比 ORM 之于 JDBC）。
- 选型建议：**主修 Vue 3、辅修 React**；中后台是 Java 全栈的主战场。
- 生态工具几乎都有 Java 对应物：npm≈Maven、Vite≈打包插件、ESLint≈CheckStyle、axios≈OpenFeign。
- 后端转前端最大认知差：没有编译兜底、异步思维、用户环境不可控；最大红利：工程化与架构思维直接迁移。

## 动手练习

1. 打开 `ai-cs-frontend/package.json`，对照第五节的表格，说出每个依赖是干嘛的（devDependencies 和 dependencies 分别对应 Maven 的什么 scope？）。
2. 打开本项目 AI 对话页（`ChatView.vue`），数一数它被拆成了几个组件，画出组件树，并思考：哪些状态是组件私有的？哪些该进 Pinia？
3. 在浏览器打开任意管理后台，按 F12 → Network 面板，刷新页面，观察：一共发了几个请求？哪些是拿数据的 XHR/Fetch？状态码分布如何？（这个习惯以后天天用。）
