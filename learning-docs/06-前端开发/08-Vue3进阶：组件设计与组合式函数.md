# Vue 3 进阶：组件设计与组合式函数

> **前置知识**：[04-Vue3核心基础](./04-Vue3核心基础.md)、[05-ElementPlus与前后端联调](./05-ElementPlus与前后端联调.md)、[06-路由守卫与Axios请求封装](./06-路由守卫与Axios请求封装.md)、[07-Pinia状态管理实战](./07-Pinia状态管理实战.md)。
> **本篇解决**："会用"到"会设计"的跨越——组件怎么拆、怎么通信、重复逻辑怎么抽。

---

## 一、组件设计原则

### 1.1 单向数据流：前端界的"读写分离"

```text
        props（数据下行，只读）
父组件 ────────────────────▶ 子组件
       ◀────────────────────
        emit（事件上行，通知）
```

- **props 下行**：父传子数据，子组件**只读不改**（改了 Vue 会警告）。类比：方法入参——你不会在方法里修改调用方的入参对象还指望它生效。
- **emit 上行**：子组件不自己"改数据"，而是发出事件请父组件改。类比：回调/监听器。

为什么这么严？——**数据变化永远可追溯**。界面错了，顺着"谁改了这个状态"一条线查下去就行。这是前端排障的命脉，等价于后端"变更日志 + 明确的写入方"。

### 1.2 什么时候该拆组件

三个信号，满足其一就拆：

1. **模板超过 ~150 行**，或同一段结构复制了两遍以上；
2. **独立的职责**：搜索表单、表格、分页器本就是三件事；
3. **独立的更新频率**：聊天输入框高频变化，消息列表低频变化——拆开避免无关重渲染。

容器组件 vs 展示组件：页面级组件（`views/`）负责取数和编排；`components/` 里的组件只吃 props 吐事件、不直接调接口——**类比 Controller 编排 + 领域对象不碰 DAO**。

---

## 二、组件通信方式全家桶

先把 6 种方式一表收编，再逐个讲清适用层级：

| 方式 | 方向 | 适用场景 | Java 类比 |
|---|---|---|---|
| `props` / `emit` | 父 ↔ 子 | **默认选择**，90% 场景 | 入参 / 回调 |
| `v-model`（defineModel） | 父 ↔ 子 | 表单类组件的双向绑定 | getter/setter 语法糖 |
| 模板 `ref` + `defineExpose` | 父 → 子 | 父要调子组件方法（如重置表单） | 拿到另一个 Bean 的引用调方法 |
| `provide` / `inject` | 祖先 → 后代 | 跨多层注入（主题、全局配置） | Spring 依赖注入 |
| `mitt` 事件总线 | 任意 | ⚠️ 尽量别用（事件流失控） | 早期 Spring 的 ApplicationEvent 滥用 |
| Pinia | 全局 | 跨页面共享状态 | （独立的全局仓库，见 07 篇） |

### 2.1 defineModel：双向绑定语法糖（Vue 3.4+）

```vue
<!-- SearchInput.vue：封装一个受控输入框 -->
<script setup>
const keyword = defineModel('keyword', { default: '' })
// 等价于老写法：props.keyword + emit('update:keyword') 的整套样板
</script>

<template>
  <el-input v-model="keyword" placeholder="搜索" clearable />
</template>
```

```vue
<!-- 父组件使用：和用 el-input 一样自然 -->
<SearchInput v-model:keyword="query.keyword" />
```

### 2.2 模板 ref + defineExpose：显式"导出"子组件能力

```vue
<!-- UserForm.vue：默认什么也不暴露，必须 defineExpose 才能被父拿到 -->
<script setup>
import { ref } from 'vue'
const formRef = ref()
const resetForm = () => formRef.value?.resetFields()
const validate = () => formRef.value?.validate()

defineExpose({ resetForm, validate })   // 只导出这两个方法，内部状态不外泄
</script>
```

```vue
<!-- 父组件 -->
<UserForm ref="userFormRef" />
<!-- userFormRef.value.resetForm() —— 类比：@Autowired 注入后调它公开的方法 -->
```

### 2.3 provide / inject：跨层注入

```vue
<!-- App.vue：祖先提供 -->
<script setup>
import { provide, ref } from 'vue'
const theme = ref('dark')
provide('theme', theme)          // 整棵子树都能取到
</script>
```

```vue
<!-- 任意深度的后代组件 -->
<script setup>
import { inject } from 'vue'
const theme = inject('theme', ref('light'))   // 第二个参数是默认值
</script>
```

类比 Spring：`provide ≈ 注册 Bean`，`inject ≈ @Autowired`——区别是作用域限定在**组件子树**而非全局容器。适合主题、国际化、权限标识这类"全局横切配置"；业务数据别走这里，用 props 或 Pinia。

---

## 三、插槽：父组件注入"渲染逻辑"

插槽解决的通信问题是反向的：**子组件提供数据与结构，父组件决定长什么样**——这正是**模板方法模式**（父类定骨架、子类填实现）。

```vue
<!-- GenericTable.vue：通用表格，复用 el-table 的同时把"每列怎么渲染"留给使用者 -->
<template>
  <el-table :data="data" v-loading="loading">
    <el-table-column v-for="col in columns" :key="col.prop" :prop="col.prop" :label="col.label">
      <template #default="{ row }">
        <!-- 作用域插槽：把 row 交出去，父组件决定怎么渲染这一格 -->
        <slot :name="col.prop" :row="row">{{ row[col.prop] }}</slot>
      </template>
    </el-table-column>
  </el-table>
</template>

<script setup>
defineProps({ columns: Array, data: Array, loading: Boolean })
</script>
```

```vue
<!-- 使用方：普通列直接传 columns，特殊列用插槽定制 -->
<GenericTable :columns="columns" :data="users" :loading="loading">
  <template #status="{ row }">
    <el-tag :type="row.status === 'ENABLE' ? 'success' : 'danger'">
      {{ row.status === 'ENABLE' ? '启用' : '禁用' }}
    </el-tag>
  </template>
  <template #actions="{ row }">
    <el-button size="small" @click="edit(row)">编辑</el-button>
  </template>
</GenericTable>
```

三种插槽一句话：**默认插槽**传一块内容；**具名插槽**传多块（`#header`、`#footer`）；**作用域插槽**还能反向拿到子组件的数据（上例 `{ row }`）。

---

## 四、组合式函数（Composables）：逻辑复制的终结者

### 4.1 为什么不是 mixin

老项目常见的 mixin 有三宗罪，导致它被官方边缘化：

| mixin 的罪 | 后果 |
|---|---|
| 来源不明 | 模板里用了个 `loading`，不知道是哪个 mixin 注入的 |
| 命名冲突 | 两个 mixin 都有 `reset()`，合并规则隐蔽 |
| 隐式耦合 | mixin 依赖组件里的某个 data，拆开就崩 |

组合式函数 = **普通函数 + 响应式原语**，来源（import 显式）、冲突（解构重命名）、耦合（入参返回值即契约）全部消除。类比：从"继承注入"进化到"组合 + 依赖注入"。

### 4.2 实战一：useRequest（loading + 防抖 + 取消）

```javascript
// src/composables/useRequest.js
import { ref, shallowRef, onUnmounted } from 'vue'
import axios from 'axios'

export function useRequest(fetcher, { immediate = true, debounceMs = 300 } = {}) {
  const data = shallowRef(null)      // 大对象用 shallowRef，避免深响应式开销
  const loading = ref(false)
  const error = ref(null)
  let controller = null
  let timer = null

  async function run(...args) {
    controller?.abort()              // 上一次还没回来？取消（防止旧响应覆盖新响应）
    controller = new AbortController()
    loading.value = true
    error.value = null
    try {
      data.value = await fetcher({ ...args, signal: controller.signal })
    } catch (e) {
      if (!axios.isCancel(e)) error.value = e
    } finally {
      loading.value = false
    }
  }

  // 防抖：搜索框每敲一个字不该发一次请求
  const debouncedRun = (...args) => {
    clearTimeout(timer)
    timer = setTimeout(() => run(...args), debounceMs)
  }

  onUnmounted(() => {                // 组件卸载必须清理（≈ @PreDestroy）
    clearTimeout(timer)
    controller?.abort()
  })

  if (immediate) run()
  return { data, loading, error, run, debouncedRun }
}
```

```javascript
// 使用：搜索列表
const keyword = ref('')
const { data, loading, debouncedRun } = useRequest(
  (p) => listUsers({ ...p, keyword: keyword.value }),
  { debounceMs: 300 }
)
watch(keyword, () => debouncedRun())
```

### 4.3 实战二：useWebSocket（重连 + 心跳）

`ai-cs-frontend` 的 AI 对话/消息通知需要长连接，这个组合式函数是项目刚需：

```javascript
// src/composables/useWebSocket.js
import { ref, onUnmounted } from 'vue'

export function useWebSocket(url, { onMessage, heartbeatMs = 30000 } = {}) {
  const connected = ref(false)
  let ws = null
  let heartbeat = null
  let retry = 0

  function connect() {
    ws = new WebSocket(url)

    ws.onopen = () => {
      connected.value = true
      retry = 0
      // 心跳：探活，防止 Nginx/网关掐掉空闲连接
      heartbeat = setInterval(() => ws?.send('{"type":"PING"}'), heartbeatMs)
    }
    ws.onmessage = (e) => {
      if (e.data === 'PONG') return
      onMessage?.(JSON.parse(e.data))
    }
    ws.onclose = () => {
      connected.value = false
      clearInterval(heartbeat)
      // 指数退避重连：1s、2s、4s…封顶 30s，避免打爆服务端
      const delay = Math.min(1000 * 2 ** retry++, 30000)
      setTimeout(connect, delay)
    }
  }

  connect()
  onUnmounted(() => { clearInterval(heartbeat); ws?.close() })   // 路由切换必须断开

  return { connected, send: (msg) => connected.value && ws.send(msg) }
}
```

> 📌 同理可写 `useSSE`（AI 流式对话用 EventSource / fetch ReadableStream），网络层原理见 [10-浏览器原理与HTTP跨域](./10-浏览器原理与HTTP跨域.md) 第五节。

---

## 五、生命周期与侦听器

### 5.1 生命周期全景表

| 选项式（旧） | 组合式（本项目） | 时机 / 该做什么 | Java 类比 |
|---|---|---|---|
| — | `setup()`（即 `<script setup>` 顶层） | 初始化响应式状态、注册逻辑 | 构造方法 |
| `created` | — （setup 就覆盖） | — | — |
| `mounted` | `onMounted(fn)` | **DOM 已就绪**：发首屏请求、初始化 ECharts、拿 DOM 尺寸 | `@PostConstruct` |
| `updated` | `onUpdated(fn)` | 渲染后（少用，容易死循环） | — |
| `unmounted` | `onUnmounted(fn)` | **必须清理**：定时器、WebSocket、事件监听 | `@PreDestroy` |
| `beforeUnmount` | `onBeforeUnmount(fn)` | DOM 还在，做善最后一步 | — |

> ❌ **最常见内存泄漏**：`onMounted` 里 `setInterval` / `addEventListener`，`onUnmounted` 忘了清理。路由一切走，旧页面的定时器还在跑。记住对称律：**有挂载必有无载，有注册必有注销**。

### 5.2 watch vs watchEffect

| 维度 | `watch(source, cb)` | `watchEffect(fn)` |
|---|---|---|
| 依赖 | **显式声明**听谁 | 自动追踪 fn 里用到的响应式数据 |
| 首次执行 | 默认不执行（`immediate: true` 开启） | 立即执行一次 |
| 拿旧值 | ✅ `(newVal, oldVal)` | ❌ |
| 适用 | 提交前校验、某字段变化触发请求 | "状态→副作用"的自动联动（如同步 localStorage） |

```javascript
// watch：明确的"监听 X 干 Y"，参数里能拿新旧值
watch(() => route.params.id, (newId, oldId) => reload(newId), { immediate: true })

// watchEffect：依赖自动收集，适合派生副作用
watchEffect(() => localStorage.setItem('keyword', keyword.value))
```

> ✅ **建议**：业务逻辑里优先 `watch`（依赖清晰、可控）；`watchEffect` 留给"声明式副作用"。

---

## 六、性能优化清单

| 手段 | 原理 | 类比 |
|---|---|---|
| `v-if` vs `v-show` | v-if 真销毁（切换贵）；v-show 靠 display（首次渲染贵、切换便宜） | 条件创建 Bean vs 常驻 Bean |
| `v-for` 的 `key` 用业务 id | diff 算法靠 key 匹配旧新节点 | 主键 vs 用下标当主键的灾难 |
| `computed` | 依赖不变直接返回缓存 | 方法级缓存 |
| `shallowRef` / `shallowReactive` | 大数组只追踪第一层，减少深代理开销 | 深拷贝 vs 浅拷贝 |
| `defineAsyncComponent` / 路由懒加载 | 首屏不加载全部分包 | 懒加载 Bean / 按需初始化 |
| 长列表虚拟滚动 | 只渲染可视区 DOM（vueuse 的 `useVirtualList`） | 分页 |

**key 用 index 的经典事故**：列表 `[A,B,C]` 删掉 A 后变 `[B,C]`，用 index 当 key，diff 认为"key=0 从 A 变成 B、key=1 从 B 变成 C"，**全部节点重渲染且带内部状态的组件（输入框）内容串位**。key 必须用稳定业务 id。

---

## 七、高频踩坑速查

```javascript
// ❌ 坑 1：解构 props 丢失响应式
const { user } = props          // user 从此是快照，不再更新
// ✅ 保持响应式：toRefs(props) 或始终 props.user

// ❌ 坑 2：直接修改 props（单向数据流破坏）
props.user.name = 'hacked'      // Vue 警告；应 emit 让父组件改

// ❌ 坑 3：reactive 整体替换失效
let state = reactive({ list: [] })
state = newState                // ❌ 引用换了，旧代理还在被模板引用
// ✅ 用 ref 包整个对象：state.value = newState

// ❌ 坑 4：v-html 渲染用户输入（XSS！）
<div v-html="msg.content" />    // AI 输出/用户输入必须先消毒（DOMPurify），或用文本插值
```

---

## 本章小结

- 组件设计第一性原理：**props 下行、事件上行**的单向数据流；三个信号决定何时拆组件。
- 通信选型：父子用 props/emit/v-model/ref+expose；跨层用 provide/inject（≈依赖注入）；跨页面用 Pinia；**慎用事件总线**。
- 作用域插槽 = 模板方法模式：子组件给数据、父组件定渲染，通用表格封装的核心手法。
- 组合式函数是 mixin 的替代品：显式 import、无命名冲突、契约清晰；`useRequest` / `useWebSocket` 是中后台两大刚需。
- 生命周期对称律：**有挂载必有无载**——定时器、连接、监听器都要在 `onUnmounted` 清理。
- 性能三板斧：正确 key、v-if/v-show 按场景、大列表 shallowRef + 虚拟滚动。

## 动手练习

1. 把 `ai-cs-frontend` 的用户管理页拆成 `SearchForm` / `UserTable` / `PaginationBar` 三个展示组件 + 一个容器组件，画出 props/emit 数据流图。
2. 用作用域插槽给 03 节的 `GenericTable` 增加「操作列」和「状态列」自定义渲染。
3. 编写 `useCountdown` 组合式函数（验证码 60 秒倒计时）：暴露 `seconds` 与 `start()`，保证组件卸载时定时器被清理；并用它改造登录页。
