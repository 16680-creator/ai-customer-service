# 13-移动端与小程序开发：跨端全景与 uni-app 实战

> 前置知识：[04-Vue3核心基础](./04-Vue3核心基础.md)（本篇主力武器 uni-app 直接复用 Vue3 语法）、[06-路由守卫与Axios请求封装](./06-路由守卫与Axios请求封装.md)（请求封装思想平移）、[10-浏览器原理与HTTP跨域](./10-浏览器原理与HTTP跨域.md)（WebView 与渲染进程）。
> 目标定位：补齐全栈视野——各端技术怎么选、H5 适配的坑在哪、小程序为什么长那样、用 uni-app 把 ai-cs 的能力带进微信小程序与桌面端。

---

## 一、跨端开发全景图

### 1.1 为什么"一次开发、多端运行"是刚需

目标平台矩阵：iOS、Android、H5（手机浏览器/内嵌页）、微信/支付宝/抖音各家小程序、Windows/macOS 桌面。若全部原生分端开发，每端一个团队——中小团队人力根本不够，所以"一套代码、多端运行"是刚需而非炫技。

### 1.2 技术谱系大表

| 技术路线 | 原理 | 性能 | 生态 | 学习成本 | 适合场景 | Vue/React 技能复用 |
|---|---|---|---|---|---|---|
| 响应式 H5 | 移动端适配的网页 | 中 | 最大 | 最低 | 资讯、营销、轻交互 | Vue/React 直接用 |
| Hybrid WebView 壳（Cordova/Ionic 时代） | 原生壳 + 网页 | 较差 | 老旧 | 低 | 已边缘化 | 高 |
| 微信小程序原生 | 双线程 + WXML | 中上 | 微信系 | 中（自有语法） | 只做微信 | 低（自有 DSL） |
| uni-app | Vue3 语法编译到小程序/H5/App | 中上 | 国内最活跃 | 低 | 国内多端业务 | 极高（Vue 直接写） |
| Taro | React（兼 Vue）语法编译到小程序 | 中上 | 活跃 | 低 | React 团队多端 | 高（React 复用） |
| React Native | JS 写原生组件 | 较好 | 大 | 中 | 独立 App | React 复用 |
| Flutter | Dart + 自绘引擎 | 最好 | 中 | 高（学 Dart） | 高性能独立 App | 低 |
| 原生开发 | Swift / Kotlin | 最好 | 最全 | 最高 | 重交互、系统级 | 无 |

选型口诀：**国内多端业务选 uni-app（Vue）或 Taro（React）；独立 App 看团队栈（RN/Flutter）；重度体验最终回归原生。**

### 1.3 给 Java 转全栈者的推荐路径

**优先 uni-app**：刚学完的 Vue 3 组合式 API、Pinia、请求封装思路全部直接复用，同一份代码可发微信小程序 + H5 + Android/iOS App；桌面端再用 Electron（第五节）补齐。这条路径技能复用率最高，没必要为了写 App 先学 Dart。

### 1.4 本篇 Java 类比汇总表

| 前端概念 | Java 类比 |
|---|---|
| 小程序双线程 + setData | Service ↔ View 分离，setData 是一次 RPC |
| uni-app 条件编译 `#ifdef` | Maven profile / 预处理宏 |
| `pages.json` | 路由表 + Nginx 站点配置合体 |
| `manifest.json` | pom.xml + application.yml 合体 |
| `uni.navigateTo` | `router.push` |
| wx.login + code2session | OAuth 授权码模式精简版 |
| Electron | 桌面版 JVM |
| main / preload / renderer 进程 | Boot 主进程 / 防腐层(DMZ) / 页面进程 |
| Service Worker | 浏览器内置的可编程网关 |

## 二、移动端 H5 适配

### 2.1 viewport 与像素概念

```html
<!-- index.html —— 不写这行，移动浏览器按 980px 桌面宽度渲染再缩小，字会小成蚂蚁 -->
<meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
```

像素三概念一段话：**物理像素**是屏幕真实的发光点（375pt 宽的 iPhone 可能对应 750 个物理点）；**CSS 像素**是逻辑单位，我们写样式用的 px；**dpr（devicePixelRatio）= 物理像素 / CSS 像素**，表示一块 CSS 像素顶几个物理点——所以 750px 宽的设计稿（@2x）在 375 CSS px 的屏幕上正好 1:1。

### 2.2 rem 方案与 vw 方案

- **rem 方案**：根字号随屏宽变（JS 设 html 的 font-size = 屏宽/10），元素用 rem——老主流，需 JS 配合，有舍入误差。
- **vw 方案**：`1vw = 屏宽的 1%`，纯 CSS、无 JS、无误差，当下主流。

两者都不用手动换算：**postcss-px-to-viewport** 在构建期把设计稿 px 自动转成 vw，你照常按设计稿写 px。

```css
/* 设计稿宽 375：一个 200px 宽的按钮 */
.btn { width: 200px; }
/* 构建产物 → width: 53.33vw（200 / 375 * 100），任意屏宽下都占 200/375 比例 */
```

```js
// postcss.config.js —— 一次配置，全项目生效
export default { plugins: { 'postcss-px-to-viewport': { viewportWidth: 375 } } }
```

### 2.3 安全区 safe-area-inset（刘海屏）

```css
/* 底部操作栏避开 iPhone 的 Home 指示条 */
.footer {
  padding-bottom: calc(20px + constant(safe-area-inset-bottom)); /* 老 iOS */
  padding-bottom: calc(20px + env(safe-area-inset-bottom));      /* 标准写法 */
}
```

### 2.4 经典历史坑一则

- ❌ **300ms 点击延迟**：早年移动浏览器为区分"单击还是双击缩放"，点击后等 300ms 才触发 click；配了 viewport 且禁缩放后浏览器取消该延迟，FastClick 库是历史产物，新项目不需要。
- ❌ **1px 边框**：CSS 写 1px，在 dpr=2/3 的屏上实际占 2/3 个物理点显得粗；经典解法是伪元素 + `transform: scale(0.5)`。

这两个坑都是"物理像素 vs CSS 像素"概念不清的直接产物——理解了 2.1 就能自己推导出解法。

## 三、微信小程序：和 Web 不一样的运行环境

### 3.1 双线程模型：为什么没有 DOM

浏览器里 JS 与 DOM 同线程，随手 `document.querySelector` 就能改页面。小程序为管控与性能，把两者拆成两个线程：

- **渲染层**（多个 WebView）：跑 WXML/WXSS，只管显示；
- **逻辑层**（JsCore/V8）：跑你的 JS，**没有 DOM/BOM**；
- 两线程经 bridge 通信，`this.setData({ x: 1 })` 本质是把数据序列化后发给渲染层 diff 更新——一次 setData 传大对象等于一次大 IPC。

Java 类比：**逻辑层 ≈ Service 层，渲染层 ≈ View 层，setData ≈ 一次远程调用（RPC）**——所以小程序优化第一条就是：少 setData、传小数据。

### 3.2 页面四件套与生命周期

```js
// pages/login/login.js —— 每个页面 = wxml(结构) + wxss(样式) + js(逻辑) + json(配置)
Page({
  data: { phone: '', loading: false },   // ≈ data()，但改它必须走 setData
  onLoad(query) { /* 页面加载，取路由参数 ≈ created + route.query */ },
  onShow() { /* 每次显示触发，切后台回来也触发 ≈ onActivated */ },
  async submit() {
    this.setData({ loading: true })      // ❗ 不是 this.data.loading = true（无效且不渲染）
    await wxLogin(this.data.phone)
    this.setData({ loading: false })
  },
})
```

Page 生命周期与 Vue 对照：

| 小程序钩子 | 触发时机 | Vue 近似物 |
|---|---|---|
| `onLoad(query)` | 页面首次加载，可取路由参数 | `created` + `route.query` |
| `onShow` | 每次展示，含切后台返回 | `onActivated` |
| `onReady` | 首次渲染完成 | `mounted` |
| `onHide` | 切后台 | `onDeactivated` |
| `onUnload` | 页面卸载 | `onUnmounted` |

### 3.3 Why 框架：原生写法的两个痛点

1. **绑定单平台**：WXML/WXSS/wx.* 出了微信不能用，支付宝/抖音小程序要再写一套；
2. **无响应式**：改数据必须手动 setData，代码里全是 setData 噪音，没有 Vue 的自动追踪。

所以业界用 uni-app/Taro：**用 Vue/React 语法写，编译期转成各平台代码**——类似"用高级语言写、编译到不同 CPU 架构"，你保留响应式与组件生态，编译器消化平台差异。

## 四、uni-app 实战入门

### 4.1 创建与目录结构

HBuilderX 可视化创建，或 CLI：`npx degit dcloudio/uni-preset-vue#vite my-uniapp`（Vue3 + Vite 模板）。关键文件：

| 文件 | 作用 | 类比 |
|---|---|---|
| `pages.json` | 页面注册 + 路由规则 + tabBar + 窗口样式 | ≈ 路由表 + Nginx 站点配置合体 |
| `manifest.json` | 应用清单：appid、图标、各端特有配置 | ≈ pom.xml + application.yml 合体 |
| `pages/` | 页面（每目录一个 vue 文件） | ≈ views/ |
| `App.vue` | 应用生命周期（onLaunch 等） | ≈ main.ts + 全局守卫 |
| `static/` | 静态资源 | ≈ public/ |

### 4.2 条件编译：处理平台差异

```vue
<!-- pages/index.vue -->
<script setup>
// #ifdef MP-WEIXIN
import { loginByWeixin } from '@/api/wx'   // 只编译进微信小程序包
// #endif
// #ifdef H5
import { loginByForm } from '@/api/h5'     // 只编译进 H5 包
// #endif
</script>
<template>
  <!-- #ifdef H5 -->
  <button @click="downloadApp">下载 App</button>  <!-- 小程序禁止引导下载，此按钮不编译进去 -->
  <!-- #endif -->
</template>
```

`#ifdef MP-WEIXIN / H5 / APP-PLUS` 相当于 **Maven profile + 预处理宏**：同一份源码，按目标平台裁剪出不同产物。

### 4.3 页面跳转与请求封装

```js
// 路由：uni.navigateTo ≈ router.push
uni.navigateTo({ url: `/pages/chat/detail?id=${id}` })
uni.switchTab({ url: '/pages/index/index' })   // tabBar 页专用
uni.redirectTo({ url: '/pages/login/login' })  // 关闭当前页 ≈ router.replace

// 请求：把 06 篇的 axios 封装思想平移到 uni.request
const BASE = import.meta.env.VITE_API_BASE
export function request(options) {
  return new Promise((resolve, reject) => {
    uni.request({
      ...options,
      url: BASE + options.url,
      header: { Authorization: `Bearer ${uni.getStorageSync('token')}` }, // ≈ getToken()
      success: res => (res.statusCode === 200 ? resolve(res.data) : reject(res)),
      fail: reject,
    })
  })
}
```

### 4.4 实战：登录页核心代码（对应动手练习 1）

```vue
<!-- pages/login/login.vue —— Vue3 语法原样写，uni-app 编译到各端 -->
<script setup>
import { ref } from 'vue'
import { request } from '@/utils/request'    // 4.3 封装的请求工具

const phone = ref(''); const password = ref(''); const loading = ref(false)

async function handleLogin() {
  if (!phone.value || !password.value) {
    return uni.showToast({ title: '请填写完整', icon: 'none' })   // ≈ ElMessage
  }
  loading.value = true
  try {
    const { token } = await request({
      url: '/auth/login', method: 'POST',
      data: { phone: phone.value, password: password.value },
    })
    uni.setStorageSync('token', token)          // ≈ setToken()（06 篇的 auth.js）
    uni.switchTab({ url: '/pages/index/index' }) // ≈ router.push 进首页
  } catch (e) {
    uni.showToast({ title: e.message || '登录失败', icon: 'none' })
  } finally {
    loading.value = false
  }
}
</script>

<template>
  <view class="login">                 <!-- 小程序里用 view/text 代替 div/span -->
    <input v-model="phone" placeholder="手机号" />
    <input v-model="password" password placeholder="密码" />
    <button :loading="loading" @click="handleLogin">登录</button>
  </view>
</template>
```

和 Vue SPA 唯一的区别：容器标签换成 `view/input/button`，API 前缀从 axios/ElMessage 换成 `uni.*`——**组件心智与状态管理零迁移**。

### 4.5 wx.login 登录时序（后端视角）

```text
小程序端                     Java 后端                            微信服务器
   │ ① wx.login() 拿临时 code        │                                  │
   │ ② POST /auth/wx-login {code} ──> │ ③ GET jscode2session ─────────> │
   │                                  │    (appid + secret + code)      │
   │                                  │ <──④ openid + session_key ──────│
   │                                  │ ⑤ 按 openid 查/建用户、签发 JWT（老本行） │
   │ <─────⑥ 返回 token + 用户信息 ───│                                  │
```

要点：**code 一次性且 5 分钟有效；AppSecret 只能放 Java 后端；openid 是用户唯一标识**。后端唯一要实现的接口：收 code → 调微信 `jscode2session`（appid+secret+code）→ 按 openid 查/建用户 → 发 JWT。这就是标准 OAuth 授权码模式的"精简版"，你在 [10-浏览器原理与HTTP跨域](./10-浏览器原理与HTTP跨域.md) 见过的授权码流程换个马甲。

### 4.6 发布流程简述

HBuilderX / 微信开发者工具 → 本地真机预览 → 上传代码（生成开发版本）→ 提交审核（1~7 天，注意类目与隐私协议）→ 审核通过 → 全量发布。H5 端直接发 Nginx，App 端打 apk/ipa 或走应用市场。

## 五、桌面端：Electron

VSCode、钉钉、Slack 都是 Electron——**用 Chromium + Node.js 把 Web 技术打包成跨平台桌面 App**。类比：**Electron 之于桌面 ≈ JVM 之于后端**——不追求原生性能，而是把"跨平台"问题转化为"运行时带得动就行"（代价：内存占用高，空壳 100MB+）。

进程模型一段话：

- **main 主进程**：Node.js 环境，管窗口、菜单、系统 API、文件——≈ Spring Boot 主进程；
- **renderer 渲染进程**：每个窗口一个，跑你的 Vue/React 页面——≈ 浏览器标签页；
- **preload 预加载脚本**：运行在受限环境，把白名单 API 暴露给页面——≈ 防腐层/DMZ；
- 通信靠 **IPC**：`ipcMain.handle` / `ipcRenderer.invoke` 一对一请求响应——≈ 本地 RPC。

你的 Vue 3 组件几乎零改动就能装进 Electron 窗口，适合给 ai-cs 做桌面客服工作台。

```js
// electron/main.js —— 主进程：开窗口、挂 preload、注册 IPC
const { app, BrowserWindow, ipcMain } = require('electron')
const path = require('path')

function createWindow() {
  const win = new BrowserWindow({
    width: 1200, height: 800,
    webPreferences: { preload: path.join(__dirname, 'preload.js') },  // 受控桥
  })
  win.loadURL('http://localhost:5173')  // 开发期加载 Vite dev server；生产 loadFile('dist/index.html')
}
ipcMain.handle('read-config', () => readConfigFile())   // 渲染进程 ipcRenderer.invoke('read-config') 调用
app.whenReady().then(createWindow)
```

打包发布用 `electron-builder`：一条命令产出 Windows/macOS/Linux 安装包——角色相当于 JDK 自带的 `jpackage`。

## 六、PWA：一段话了解

PWA（Progressive Web App）= 让 Web 应用可安装到桌面/主屏、离线可用、可推送通知：核心是 **Service Worker**——一个独立于页面、能拦截全部网络请求的脚本（≈ 给浏览器内置了个可编程网关，缓存策略自己写），再配 manifest.json 即可"添加到主屏幕"。适合内容型站点低成本获得类 App 体验；国内受微信小程序生态挤压，存在感一般，了解概念即可。

## 本章小结

- 跨端选型：国内多端业务 uni-app（Vue 复用）或 Taro（React 复用）；独立 App 看 RN/Flutter；重度体验回归原生。
- H5 适配三件套：viewport meta、px→vw 自动转换（postcss-px-to-viewport）、safe-area 安全区；300ms 延迟与 1px 边框是物理像素概念不清的历史坑。
- 小程序本质：双线程、无 DOM/BOM、setData 是跨线程通信（≈ RPC），因此有 uni-app/Taro 做"一次编写、多端编译"。
- uni-app 关键词：pages.json（路由+tabBar）、manifest.json（应用清单）、条件编译（≈ Maven profile）、uni.request 封装 ≈ axios 封装平移。
- 小程序登录：wx.login 拿 code → Java 后端 jscode2session 换 openid → 签发 JWT，AppSecret 永不出后端。
- Electron ≈ 桌面版 JVM：main/preload/renderer 三进程 + IPC；PWA = Service Worker 驱动的可安装离线 Web 应用。

## 动手练习

1. 用 uni-app 写一个调用后端接口的登录页：表单（手机号+密码）→ 4.3 封装的 `request` 调 ai-cs 后端 `/auth/login` → 成功后 `uni.setStorageSync('token', ...)` 并 `uni.switchTab` 进首页。
2. 安装微信开发者工具，用测试号新建小程序项目，把练习 1 的页面跑起来，体验"编译 → 预览 → 真机调试"链路，并打开编译产物里的 wxml 感受 uni-app 做了什么转换。
3. 给一个现有 H5 页面补上 viewport meta 与 `safe-area-inset-bottom`，用浏览器 DevTools 的 iPhone 机型模拟对比改动前后的布局差异。
