# 前端面试题精选：后端转全栈自检 50 题

> **定位**：这是一份「自检 + 面试」两用的高频题集，不是教材。建议先遮住答案口算/默写，再对照要点查漏。
> 八个分组共 50 题，覆盖 HTML/CSS、JavaScript、Vue3、TypeScript、工程化、浏览器与网络、React 基础与场景开放题。
> 系统学习请回对应篇目：[01-HTML与CSS基础](./01-HTML与CSS基础.md)、[02-JavaScript核心基础](./02-JavaScript核心基础.md)、[03-TypeScript快速上手](./03-TypeScript快速上手.md)、[04-Vue3核心基础](./04-Vue3核心基础.md)、[06-路由守卫与Axios请求封装](./06-路由守卫与Axios请求封装.md)、[07-Pinia状态管理实战](./07-Pinia状态管理实战.md)、[09-前端工程化](./09-前端工程化：Vite构建与规范.md)、[10-浏览器原理与HTTP跨域](./10-浏览器原理与HTTP跨域.md)、[11-React快速上手](./11-React快速上手.md)。

---

## 一、HTML/CSS（6 题）

### 1. 标准盒模型与 IE 盒模型的区别？box-sizing 怎么用？

- 标准模型（`content-box`）：`width` 只等于 content，实际占宽 = width + padding + border。
- IE 模型（`border-box`）：`width` = content + padding + border，改 padding 不会撑破容器。
- 主流实践：全局重置 `*, *::before, *::after { box-sizing: border-box; }`，宽高即"所见即所得"。
- Element Plus 等组件库内部均按 border-box 计算，混用时注意对齐。

### 2. BFC 是什么？怎么触发？举一个应用场景 ⭐ 关键词：格式化上下文、flow-root

- BFC（Block Formatting Context）：一块独立的渲染区域，内部布局不影响外部，外部也影响不到内部。
- 触发条件（记 4 个）：根元素 `<html>`；`float` 非 none；`position: absolute/fixed`；`overflow` 非 visible；以及现代首选 `display: flow-root`。
- 应用一：清除浮动——子元素全浮动导致父容器高度塌陷，父元素加 `overflow: hidden` 或 `display: flow-root` 即可包含浮动子元素。
- 应用二：阻止相邻块级元素 margin 塌陷（合并）——把其中一个元素包进新的 BFC 容器，两个 margin 就会叠加而非取较大值。

### 3. 水平垂直居中，至少写 3 种

```css
/* 1. Flex（首选） */
.parent { display: flex; justify-content: center; align-items: center; }

/* 2. Grid（最短） */
.parent { display: grid; place-items: center; }

/* 3. 绝对定位 + transform（不知道子元素尺寸时） */
.child { position: absolute; left: 50%; top: 50%; transform: translate(-50%, -50%); }

/* 4. 绝对定位 + margin: auto（已知尺寸时） */
.child { position: absolute; inset: 0; margin: auto; width: 200px; height: 100px; }
```

- 追问点：transform 方案不触发回流（走合成层），性能好；`inset: 0` 是 `top/right/bottom/left: 0` 的简写。

### 4. 选择器优先级如何计算？!important 该怎么用？

- 权重四档：行内 style(1000) > ID(100) > class/伪类/属性选择器(10) > 元素/伪元素(1)，同权重时后声明的覆盖先声明的。
- `!important` 优先级最高，但会破坏级联规则、难以覆盖（只能再用带 `!important` 的更高权重对抗），原则上只在覆盖第三方组件库样式时局部使用。
- 继承的样式没有权重，任何直接命中的选择器都能覆盖它。

### 5. 重绘与回流的区别？如何优化？

- 回流（reflow）：几何属性（宽高、位置）变化，浏览器重新计算布局——代价大；重绘（repaint）：仅视觉属性（颜色、背景）变化，不涉及布局。
- 回流必然引起重绘，重绘不一定回流。
- 优化手段：批量操作 DOM（`DocumentFragment` 或一次性 innerHTML）；读写分离（避免循环里"读 offsetWidth → 改样式"交替触发强制同步布局）；用 `transform`/`opacity` 代替 `top/left/width` 动画（走合成层，不回流）；对 resize/scroll 监听加防抖节流。

### 6. display:none / visibility:hidden / opacity:0 三者对比

| 特性 | display:none | visibility:hidden | opacity:0 |
| --- | --- | --- | --- |
| 是否占位 | 否（脱离文档流） | 是 | 是 |
| 能否交互（点击） | 否 | 否 | 能（可加事件） |
| 触发 | 回流 + 重绘 | 仅重绘 | 走合成层，可 GPU 加速 |
| transition | 不支持过渡 | 支持 | 支持 |

- 面试结论：需要彻底移除用 display；需要保留布局占位用 visibility；需要过渡动画的显隐用 opacity。

---

## 二、JavaScript（10 题）

### 1. JS 有哪些数据类型？typeof 有哪些坑？

- 8 种：7 种原始类型（number、string、boolean、undefined、symbol、bigint、null）+ object（含 Array、Function 等）。
- 坑一：`typeof null === 'object'`（历史 bug，永不会修）。
- 坑二：`typeof []` 也是 `'object'`，判断数组要用 `Array.isArray()` 或 `Object.prototype.toString.call(x)`。
- 坑三：函数是 `'function'`，未声明变量 `typeof x` 返回 `'undefined'` 而不报错。

### 2. == 和 === 的区别？举一个隐式转换的坑 ⭐ 关键词：ToPrimitive

- `===` 不做类型转换；`==` 会先隐式转换再比较，规则难记，工程上**永远用 ===**（ESLint 的 eqeqeq 规则强制）。
- 经典坑：`0 == ''` 为 true；`null == undefined` 为 true 但 `null === undefined` 为 false；`NaN` 与任何值都不相等（包括自己）。
- 更阴的坑：`[1,2] == '1,2'` 为 true（对象触发 ToPrimitive 转字符串）；`'0' == false` 为 true。
- 特例记忆：`null == undefined` 是唯一一个"不同类型还相等"的组合。

### 3. let / const / var 的区别？

- var：函数作用域、变量提升（声明前访问为 undefined）、可重复声明、在浏览器全局会挂到 window。
- let/const：块级作用域、有暂时性死区（TDZ，声明前访问直接 ReferenceError）、不可重复声明。
- const：声明时必须初始化且不可重新赋值，但对象内容仍可修改（const 锁的是引用，不是值）。
- 实践：默认 const，需要重新赋值才用 let，禁用 var。

### 4. 什么是闭包？说一个应用和一个经典的坑

- 闭包：函数与其 lexical environment（词法作用域）的组合——内层函数记住并访问外层函数的变量，即使外层函数已返回。Java 类比：类似匿名内部类捕获 effectively final 局部变量，但 JS 闭包捕获的是"变量本身"，可读写。
- 应用：防抖节流（用闭包保存 timer）、IIFE 模块化私有变量、柯里化。
- 经典坑：`for (var i = 0; i < 3; i++) setTimeout(() => console.log(i))` 输出 3 3 3——var 是函数作用域，三次回调共享同一个 i；改用 let（每次迭代生成新的块级绑定）输出 0 1 2。

### 5. 一句话讲清原型链，instanceof 的原理是什么？

- 原型链：访问对象属性时，若自身没有则沿 `__proto__`（即构造函数的 `prototype`）向上查找，直到 `Object.prototype`，尽头是 null。
- `a instanceof B`：沿着 a 的原型链逐层查找，是否存在某一层的原型对象严格等于 `B.prototype`。
- 追问：`instanceof` 不能跨 iframe/realm 判断，`Array.isArray` 可以；`Object.create(null)` 创建的对象没有原型链。

### 6. this 指向的四条规则 + 箭头函数

- 规则一（默认绑定）：独立调用，非严格模式指向 window，严格模式 undefined。
- 规则二（隐式绑定）：`obj.fn()` 指向 obj——看调用点"点号前面是谁"。
- 规则三（显式绑定）：`call/apply/bind` 指定，bind 返回的新函数 this 永久固定。
- 规则四（new 绑定）：指向新创建的实例。优先级：new > 显式 > 隐式 > 默认。
- 箭头函数：没有自己的 this，沿用**定义处外层**的 this，不能被 call/bind 改变，也没有 arguments——所以事件回调、定时器回调常用箭头函数"继承"外层组件 this。

### 7. 事件循环：宏任务与微任务 ⭐ 关键词：Call Stack、Task、Microtask Queue

先口算下面代码的输出顺序，再对答案：

```js
console.log('A')
setTimeout(() => console.log('B'), 0)
Promise.resolve().then(() => console.log('C'))
setTimeout(() => {
  Promise.resolve().then(() => console.log('D'))
}, 0)
console.log('E')
```

逐行解析：

1. `'A'`：同步代码，直接执行打印。
2. `setTimeout(B)`：B 进入宏任务队列 `[B]`。
3. `then(C)`：C 进入微任务队列 `[C]`。
4. 第二个 setTimeout：其回调进入宏任务队列 `[B, T2]`；注意 D 的 then 此刻**还没入队**，要等 T2 被执行时才注册。
5. `'E'`：同步代码打印；至此调用栈清空。
6. 清空全部微任务：打印 `'C'`。
7. 取宏任务 B 执行：打印 `'B'`；栈空后清微任务（无）。
8. 取宏任务 T2执行：回调体注册微任务 D → 该宏任务结束 → 立刻清微任务，打印 `'D'`。

- **答案：A E C B D**。核心规则：每执行完一个宏任务，先把微任务队列整个清空，再取下一个宏任务。
- 常见宏任务：setTimeout/setInterval、I/O、UI 渲染；微任务：Promise.then/catch/finally、MutationObserver、queueMicrotask。async/await 中 await 之后的代码等价于放进 then（微任务）。

### 8. Promise 三种状态？all / allSettled / race 的区别与场景？

- 三状态：pending → fulfilled / rejected，状态一旦改变不可逆。
- `Promise.all`：全成功才成功（结果按顺序），任一失败立即整体 reject——适合"全部必需"的并行请求，如页面初始化同时拉用户信息和菜单。
- `Promise.allSettled`：等全部结束，返回每个的 `{status, value/reason}`——适合批量操作允许部分失败，如批量删除。
- `Promise.race`：谁先落定用谁（含失败）——适合请求超时控制（与 setTimeout 竞速）。
- 补充：`Promise.any` 返回第一个成功的。Java 类比：all ≈ `CompletableFuture.allOf`，race ≈ `anyOf` 的语义加上"首个落定即胜出"。

### 9. 手写防抖 debounce（节流一句话说区别） ⭐ 关键词：高阶函数、闭包

```js
function debounce(fn, delay = 300) {
  let timer = null                 // 闭包保存定时器
  return function (...args) {
    clearTimeout(timer)            // 每次触发都取消上一次
    timer = setTimeout(() => {
      fn.apply(this, args)         // 修正 this 与参数
      timer = null
    }, delay)
  }
}
// 典型使用：搜索框输入 300ms 后才发请求
```

- 节流 throttle 一句话：防抖是"停止触发 n 毫秒后才执行一次"，节流是"无论触发多频繁，每 n 毫秒最多执行一次"（适合滚动、resize）。
- 加分项：讲清楚为什么用 `function` 而不是箭头函数（需要动态 this）、`apply(this, args)` 保留调用方上下文与参数。

### 10. ES6+ 你最常用的 5 个特性

- 解构赋值 + 展开运算符：`const { name, ...rest } = obj`、`[...arr1, ...arr2]`，React/Vue 的 props 传递离不开。
- 模板字符串：多行拼接、表达式内嵌，替代 `+` 拼接。
- 箭头函数 + 默认参数 + 剩余参数：词法 this 解决回调指向问题。
- Promise / async-await：异步串行写成同步风格，`for...of` 配 await 顺序请求。
- ES Module（import/export）+ 可选链 `?.` 与空值合并 `??`：工程化基础与取值防御。

---

## 三、Vue3（10 题）

### 1. Vue3 的 Proxy 相比 Vue2 的 defineProperty 好在哪？ ⭐ 关键词：响应式原理

- defineProperty 只能劫持**已存在**属性：新增/删除属性不响应（Vue2 需要 `$set/$delete`）；数组靠重写 7 个原型方法实现，通过下标改数组、修改 length 监听不到。
- Proxy 劫持整个对象：新增/删除属性、数组下标与 length 修改全部拦截，共 13 种拦截陷阱。
- Vue2 需要递归遍历所有属性逐个 defineProperty（初始化开销大）；Proxy 是**惰性代理**，访问到嵌套对象才代理，初始化更快。
- 代价：Proxy 无法 polyfill 到 IE11，所以 Vue3 放弃了 IE。

### 2. ref 和 reactive 的区别？怎么选？

- ref：包装任意类型（含基本类型），通过 `.value` 访问；`ref(obj)` 内部会调用 reactive 深度转换；模板中自动解包。
- reactive：只用于对象/数组/Map/Set，直接访问属性；**解构或重新赋值会丢失响应式**（Proxy 引用被替换）。
- 推荐：默认用 ref（统一心智、不会踩解构坑），reactive 用于聚合一组状态的"状态对象"，且用 `toRefs` 导出以防解构失活。

### 3. v-if 和 v-show 的区别？

- v-if：真实的创建/销毁组件，惰性渲染（初始为 false 不渲染），切换开销大；可以配合 `<template>` 使用，也支持 v-else。
- v-show：始终渲染，只切换 `display: none`，初始渲染开销大，切换开销小。
- 结论：频繁切换用 v-show（如 Tab 页签、侧边栏）；条件很少变化或涉及权限卸载用 v-if（如按钮级权限）。
- 追问：v-if 与 v-for 同元素时 Vue3 中 v-if 优先级更高（不推荐同用）；v-show 不能用于 `<template>`。

### 4. v-for 的 key 有什么作用？用 index 当 key 出过什么事故？

- key 是 VNode 的身份标识，diff 时同层节点靠 key 判断"是否可复用"——没有 key 时 Vue 会用"就地复用 + 补丁"策略，有 key 才能正确移动/复用节点。
- index 当 key 的事故：列表**头部插入或中间删除/排序**时，index 与实际数据错位——Vue 会错误复用旧节点，导致输入框内容串行、勾选状态错乱、带动画的过渡错乱。
- 正确做法：用数据中稳定的唯一 id（如后端主键）；只有"纯展示且不会增删排序"的列表才勉强可用 index。
- 反例记忆：删除第 2 项后，原第 3 项的 index 变成 2，Vue 以为"第 2 项还在"，只是 patch 了内容，组件内部状态（未受控的输入值）没被重置。

### 5. computed 和 watch 的区别？watchEffect 呢？

- computed：**依赖缓存的计算值**——依赖不变时多次读取不会重算；必须有返回值；适合模板中的派生数据（如全选状态、过滤后的列表）。
- watch：**监听执行副作用**——数据变化时执行回调（请求、DOM 操作、localStorage 写入），没有缓存概念；`{ deep: true, immediate: true }` 是高频配置。
- watchEffect：立即执行一次并自动收集依赖，回调里用到谁就监听谁，适合"多个来源变化都要重跑"的场景；显式 watch 更可控、能拿到旧值。
- 选型一句话：要一个值用 computed，要做一个动作用 watch。

### 6. Vue3 组件通信方式，列举至少 6 种

- props / emit：父子一对一，最基本。
- v-model（`modelValue` + `update:modelValue`）：父子双向绑定的语法糖。
- ref + defineExpose：父组件拿到子组件实例，调用其暴露的方法（命令式，如调子组件的 reset()）。
- provide / inject：跨层级注入，适合主题配置、深层共享的上下文（如 table 注入给 column）。
- Pinia：全局状态，跨任意组件共享。
- 事件总线 mitt / 全局事件：任意组件解耦通信，Vue3 已移除 `$on`，需借助 mitt 库，慎用（数据流难追踪）。
- 补充：`useAttrs()`、插槽 slot 也可算"传内容"的通信。层级建议：父子用 props/emit，隔代用 provide/inject，全局共享用 Pinia。

### 7. 组合式 API 的生命周期钩子，onMounted 相当于 Java 的什么？

- setup 即创建阶段（替代 beforeCreate/created，写在 setup 里的代码就是 created 时机）。
- 常用：`onMounted`（DOM 就绪，发首屏请求、初始化第三方库）、`onUpdated`、`onUnmounted`（清理定时器/事件/WebSocket——等价于 Spring Bean 销毁回调）。
- 对后端最好理解的类比：`onMounted ≈ @PostConstruct`，`onUnmounted ≈ @PreDestroy`——挂载后初始化、卸载前释放资源。
- 注意：onMounted 中操作 DOM 前要判断 SSR 场景；同一个钩子可以注册多次。

### 8. 路由守卫的执行顺序？登录鉴权应该放在哪一层？ ⭐ 关键词：beforeEach、鉴权

- 执行顺序：全局 `beforeEach` → 路由独享 `beforeEnter` → 组件内 `beforeRouteEnter`（还有全局 `afterEach`、解析守卫 `beforeResolve`）。
- 每一层调用 `next()` 放行、`next(false)` 中断、`next('/login')` 重定向；Vue Router 4 也支持直接 return 路由地址。
- 鉴权分层：**全局 beforeEach 做登录态校验**（无 token 重定向登录页，并带上 redirect 参数），**路由独享 beforeEnter 或 meta.roles 做页面级权限**（结合后端返回的权限码），组件内守卫只做"离开前未保存提示"这类局部逻辑。
- 项目实践（见 [06-路由守卫与Axios请求封装](./06-路由守卫与Axios请求封装.md)）：addRoute 动态注册有权限的路由，afterEach 里做页面标题与埋点。

### 9. Pinia 和 Vuex 的区别？为什么 Pinia 成为官方推荐？

- 去 mutation：直接改 state 或写 action，不再需要 `commit('xxx')` 的样板代码。
- 完整 TypeScript 推断：defineStore 自动推导 state/getters/actions 类型，无需手写复杂泛型（Vuex4 的 TS 体验很痛苦）。
- 天然支持组合式写法：store 内可以用 setup 风格定义，还能在 store 里调用其他 store、使用组合式函数。
- 模块化扁平：多个 defineStore 即多模块，没有 modules 嵌套与命名空间问题；支持插件持久化（pinia-plugin-persistedstate 存 token 很常用）。
- 细节：Pinia 也支持 Vue Devtools、SSR；Devtools 中看不到 mutation 时间线是唯一小损失。

### 10. keep-alive 的作用？nextTick 为什么是异步的？ ⭐ 关键词：批处理、微任务

- keep-alive：缓存组件实例（`<keep-alive include="List,Detail">`），切换时触发 activated/deactivated 而不是重建；配合路由页缓存列表滚动位置，配合 max 控制 LRU 上限。
- nextTick：Vue 的响应式更新是**异步批处理**的——一个 tick 内多次修改 state 只触发一次渲染（类似数据库事务合并写入 / Netty 的 flush 聚合），修改数据后立刻读 DOM 拿到的还是旧值。
- 解决：`await nextTick()` 或 `nextTick(() => {...})`，其回调被推入微任务队列，在 DOM 更新完成后执行。
- 追问：Vue3 把更新队列的清空放在 `Promise.then`（微任务）中，所以 nextTick 是微任务级别的异步。

---

## 四、TypeScript（5 题）

### 1. any、unknown、never 的区别？

- any：放弃类型检查，可赋给任何类型、也可接收任何类型——传染源，能不用就不用。
- unknown：安全的 any——可以接收任何值，但**使用前必须收窄**（typeof/instanceof/断言），适合泛型返回值、JSON.parse 结果、catch 的 error。
- never：永不存在类型的类型——函数抛异常/死循环的返回值、穷举检查（switch 覆盖所有联合成员后 else 分支可声明为 never 以在新增成员时报错）。

### 2. interface 和 type 怎么选？

- 大部分场景等价；团队统一即可。常见约定：**描述对象/类的公共形状用 interface，类型运算（联合、交叉、映射、条件类型）用 type**。
- interface 可被类 implements、可声明合并（同名自动合并——既是特性也是坑）；type 可以表达联合类型、元组、基本类型别名。
- 扩展方式：interface 用 `extends`，type 用 `&`；注意 interface 重复声明会合并，type 重复声明直接报错。

### 3. 泛型与常用工具类型，举两个例子

```ts
// 泛型：约束返回类型与入参一致
function pick<T, K extends keyof T>(obj: T, key: K): T[K] { return obj[key] }

// Partial：请求体更新时所有字段可选；Omit/Pick：裁剪字段
type User = { id: number; name: string; password: string }
type UpdateUser = Partial<Omit<User, 'id'>>
type SafeUser = Omit<User, 'password'>
```

- 高频工具类型：`Partial/Required/Readonly`、`Pick/Omit`、`Record<K,V>`（枚举映射表）、`ReturnType<T>`、`Awaited<T>`（拿 Promise 的值类型）。
- Java 类比：泛型思想同 `<T extends Comparable<T>>`，只是 TS 的泛型是结构化且编译期擦除为"纯类型标注"。

### 4. 类型收窄（narrowing）有哪些手段？

- typeof 判断（注意 typeof null === 'object'，需先排除 null）、instanceof、in 检查属性、字面量相等判断（=== 'a'）。
- 可辨识联合：给每个成员一个 kind/tag 字面量字段，switch(kind) 全覆盖，收窄最可靠。
- 自定义类型守卫：`function isUser(x: unknown): x is User { ... }`；断言函数 `asserts x is User`。
- 兜底：`as` 断言（最后手段）、`satisfies` 校验但不拓宽类型。

### 5. 怎么看待"类型体操"？

- 态度：**可读性优先**。业务代码把参数、返回值、实体类型标对就是 80 分；复杂条件类型留给库作者（如 axios、Vue 的源码）。
- 自己手写超过 5 层嵌套的 `infer/递归条件类型` 前，先想想三个月后同事能否看懂。
- 有价值的体操值得学：把 `DeepPartial`、`CamelCaseKeys` 这类通用工具沉淀进团队 utils，一次写对处处受益。

---

## 五、工程化（6 题）

### 1. Vite 为什么比 Webpack 快？

- 开发期：Webpack 需要把所有模块打包成 bundle 才能启动；Vite 启动**只预构建依赖**（esbuild，Go 编写，比 JS 打包器快 10-100 倍），业务源码以**原生 ESM 按需编译**——浏览器请求哪个模块，Vite 才转译哪个，启动与热更新（HMR）与项目规模解耦。
- esbuild 还负责 TS/JSX 转译（不做类型检查，类型检查交给 tsc/vue-tsc 在构建期做）。
- 生产期：Vite 用 Rollup 打包（新版在向 Rolldown 迁移），产物质量与 Webpack 同级。
- 追问：Vite 的预构建结果缓存在 `node_modules/.vite`，依赖变化才重建。

### 2. tree-shaking 的原理？什么情况下会失效？

- 原理：ESM 是静态结构（import/export 必须在顶层，不能条件引入），打包器据此做**无用导出标记与剔除**，配合 `sideEffects: false` 声明整个包无副作用后激进删除。
- 失效场景：CJS（require）模块；import 了整个包（`import _ from 'lodash'` 而非 `lodash-es`）；代码有副作用（顶层执行、给原型挂方法、polyfill）被保守保留；动态 `import()` 变量拼接路径。
- 实践：选 ESM 版本的库（lodash-es、vue 的 ESM 构建）；组件库按需引入本质也是 tree-shaking/按模块引入。

### 3. 首屏性能优化清单（至少 8 条，按收益从高到低） ⭐ 关键词：FCP、LCP

1. **路由懒加载**（`() => import('./views/xxx.vue')`）：首屏只加载当前页代码，收益最大。
2. **构建分包**（manualChunks 把 element-plus、echarts 等大依赖拆 vendor）：命中缓存且并行加载。
3. **Gzip/Brotli 压缩**（Nginx gzip 或构建期产 .gz）：文本资源体积降 60-80%。
4. **CDN + HTTP 缓存**：静态资源就近分发，回源少。
5. **图片优化**：WebP/AVIF、懒加载（loading="lazy"）、尺寸合适的响应式图。
6. **HTTP/2**：多路复用减少队头阻塞（Nginx 一行配置）。
7. **骨架屏/Loading**：感知性能提升，白屏变骨架。
8. **组件异步加载**：defineAsyncComponent 拆弹窗、图表等非首屏组件。
9. 进阶：SSR/SSG（见 [12-Nuxt与SSR入门](./12-Nuxt与SSR入门.md)）、预加载关键资源（preload/prefetch）、接口合并与预取。

### 4. hash 与发版缓存策略怎么设计？ ⭐ 关键词：contenthash、永久缓存

- 策略核心一句话：**index.html 不缓存（no-cache，每次协商），js/css/图片带 contenthash 永久缓存（max-age=31536000, immutable）**。
- Vite 默认产物即 `assets/index-[hash].js`，内容变则 hash 变、文件名变，浏览器自然请求新版本，旧缓存无需失效。
- 事故点：若把 index.html 也强缓存，用户拿到的还是旧 hash 引用，而旧文件已被新版本部署删除 → 白屏/404；这就是发版后"清缓存才好"的根因。
- Nginx 配置要点：`location / { add_header Cache-Control "no-cache"; }`，`location /assets/ { expires 1y; add_header Cache-Control "public, immutable"; }`。
- 配套：发版保留上一版本静态文件，避免正在使用的旧页面加载旧 chunk 失败。

### 5. ESLint 和 Prettier 的分工？

- ESLint：**代码质量与逻辑问题**——未使用变量、循环依赖、hooks 依赖、eqeqeq 等规则，部分可自动修复。
- Prettier：**纯格式**——缩进、换行、引号、分号，全量重排，性能好。
- 协作：用 `eslint-config-prettier` 关闭 ESLint 中与格式冲突的规则，避免互相打架；再配 husky + lint-staged 在提交前只对暂存文件执行，最后 CI 兜底。
- 一句话：ESLint 管"对不对"，Prettier 管"好不好看"，格式之争交给工具别交给 code review。

### 6. pnpm 为什么快、省磁盘？

- 内容寻址存储（CAS）：每个包在全局 store 只存一份，项目里的 node_modules 通过**硬链接**指向 store，不复制文件——100 个项目用同一个 react 也只有一份物理文件。
- 符号链接组织依赖结构：node_modules 下只有 package.json 声明的直接依赖（扁平可见），其余收进 `.pnpm` 目录——天然解决幽灵依赖（phantom dependency，未声明却 require 成功）。
- 安装并行 + 缓存命中，冷装快于 npm/yarn，热装（store 已有）接近秒装。

---

## 六、浏览器与网络（7 题）

### 1. 从输入 URL 到页面渲染，1 分钟版 ⭐ 关键词：渲染流水线

- DNS 解析（缓存→hosts→递归查询）→ 建立 TCP 连接（三次握手）→ TLS 握手（HTTPS）→ 发送 HTTP 请求。
- 服务器响应 → 浏览器解析：HTML → DOM 树；CSS → CSSOM；二者合成渲染树 → 布局（Layout 计算几何）→ 分层绘制（Paint）→ 合成（Composite）显示。
- 遇到 `<script>` 阻塞解析（defer/async 可避免）；遇到图片等资源异步加载。
- 后续请求进入缓存与 HTTP 生命周期，首屏指标 FCP/LCP 就发生在这一条流水线上。

### 2. 强缓存与协商缓存的区别？ ⭐ 关键词：Cache-Control、304

- 强缓存：浏览器直接用本地副本，**不发请求**，Devtools 显示 `200 (from memory/disk cache)`；由 `Cache-Control: max-age` 控制（优先于过时的 Expires）。
- 协商缓存：强缓存过期后，带上 `If-None-Match: <ETag>`（或 `If-Modified-Since`）问服务器，资源没变返回 `304`（无 body），变了返回 `200` + 新资源。
- 实践组合：HTML 用 no-cache 走协商，带 hash 的静态资源用强缓存一年（见第五节第 4 题）。

### 3. 什么是跨域？说 3 种解决方案。为什么 Postman 没有跨域问题？

- 跨域：浏览器同源策略限制——协议/域名/端口任一不同即跨域，XHR/fetch 的响应会被浏览器拦截（请求其实发出了，是响应被扣下）。
- 方案一 CORS（标准解法）：后端设置 `Access-Control-Allow-Origin` 等响应头，Spring 可用 `@CrossOrigin` 或全局 CorsFilter。
- 方案二 Vite 开发代理：`vite.config.ts` 里 `server.proxy` 把 `/api` 转发到后端，请求变成同源（仅开发期）。
- 方案三 Nginx 同域反代（生产标准做法）：前后端同域名不同路径（`/api` 反代到后端端口），根本不存在跨域。
- 追问答案：跨域是**浏览器的安全行为**，Postman/curl/服务端间调用没有"同源策略"这个角色，自然不受限；同理WebSocket 与 SSE 也不受同源限制约束响应读取。

### 4. 什么时候会触发 OPTIONS 预检请求？

- 非简单请求必触发预检：只要满足以下任一——非 GET/HEAD/POST 方法；Content-Type 不是 `application/x-www-form-urlencoded`、`multipart/form-data`、`text/plain` 之一；携带自定义头（如 `Authorization`、`X-Token`）。
- 所以我们项目里 **JSON 请求 + Authorization 头 = 必发 OPTIONS**；预检通过后浏览器才发真实请求并缓存预检结果（`Access-Control-Max-Age`）。
- 排查要点：预检 404/403 时检查后端是否放行了 OPTIONS 方法（Spring Security 需放行）；网关重复 CORS 头会报"multiple values"。

### 5. Cookie / localStorage / sessionStorage 对比

| 特性 | Cookie | localStorage | sessionStorage |
| --- | --- | --- | --- |
| 容量 | 约 4KB | 约 5-10MB | 约 5MB |
| 生命周期 | 可设过期时间 | 永久（手动删） | 当前标签页会话 |
| 随请求发送 | 每次同域请求自动携带 | 不发送 | 不发送 |
| 场景 | 会话凭证（配合 HttpOnly） | token、主题偏好 | 临时表单、一次性状态 |

- 安全注意：Cookie 加 `HttpOnly`（防 XSS 读取）、`Secure`（仅 HTTPS）、`SameSite=Lax`（防 CSRF）；localStorage 存 token 需权衡 XSS 暴露面。

### 6. XSS 与 CSRF 的原理与防御清单 ⭐ 关键词：HttpOnly、SameSite、Token

- XSS：注入的恶意脚本在**受害者浏览器**执行。防御：输出转义/富文本白名单过滤（前端展示用文本插值而非 v-html）、CSP 响应头、HttpOnly Cookie、输入校验。
- CSRF：借用户已登录身份，在第三方站点**伪造请求**（浏览器自动带 Cookie）。防御：SameSite Cookie、CSRF Token（表单/请求头携带，服务端校验）、关键操作二次确认；前后端分离 + Authorization 头的方案天然免疫（token 不会自动携带）。
- 前后端分工：XSS 主要靠前端转义 + 后端过滤入库；CSRF 主要靠后端校验 + Cookie 属性；接口层永远以服务端鉴权为底线。

### 7. HTTPS 握手，30 秒版

- ClientHello（支持的加密套件 + 随机数）→ ServerHello（选定套件 + 随机数 + **证书**）。
- 客户端验证证书链（根 CA → 中间 CA → 站点证书，内置根证书逐级签名验签），通过后用非对称算法（如 ECDHE）协商出**对称会话密钥**。
- 之后全程用对称密钥加密通信（性能远好于非对称），且 ECDHE 每次会话新密钥，提供前向保密。
- 一句话总结：**非对称只用来安全地交换对称密钥，证书链解决"服务器是我"的身份信任**。

---

## 七、React 基础（4 题）

### 1. 虚拟 DOM 与 diff 的要点？

- 虚拟 DOM：用 JS 对象描述 UI，状态变化先 diff 新旧两棵树，最小化更新真实 DOM（真实 DOM 操作昂贵且难以批量优化）。
- diff 策略（降低复杂度到 O(n)）：**同层比较**不跨层级；不同类型元素直接销毁重建子树；**同层列表靠 key** 匹配复用与移动。
- 与 Vue 对比：Vue 有编译时的静态提升/patchFlag，运行时 diff 更少；React 依赖运行时全量 diff + Fiber 调度（可中断、分片）。

### 2. 为什么 setState（及更新函数）必须不可变更新？

- React 用 `Object.is` 比较新旧引用判断是否重渲染——直接 `state.list.push(x)` 引用不变，React 认为没变，不更新。
- 正确写法：`setList([...list, x])`、`setUser({...user, name})`、删除用 `filter`。
- 配合 useMemo/useEffect 的依赖浅比较也依赖引用稳定；复杂嵌套对象可用 Immer 写"可变代码"产不可变对象。
- 对后端的直觉类比：像 Java 里把可变 Date 换成不可变 LocalDate——用新对象表达新状态，历史可追溯（时间旅行调试的基础）。

### 3. useEffect 的闭包陷阱（stale closure）？

- useEffect 回调捕获的是**那次渲染的 state 快照**；依赖数组漏写时，回调里读到的永远是旧值。
- 经典事故：`useEffect(() => { setInterval(() => setCount(count + 1), 1000) }, [])`——count 恒为 0，每秒 setCount(1)；修复：函数式更新 `setCount(c => c + 1)`。
- 定时器/订阅场景的完整修复：在 cleanup 中 clearInterval，或用 useRef 存最新值。
- 通用原则：effect 用到的响应式值都写进依赖数组（eslint-plugin-react-hooks 的 exhaustive-deps 规则盯着），确实要逃逸用 useRef。

### 4. Hooks 的两条铁律？

- 铁律一：只在**函数组件顶层**调用 Hook，不能在 if/循环/嵌套函数里调用——React 按**调用顺序**对应每个 Hook 的状态，顺序乱了状态全错位。
- 铁律二：只在 React 函数组件或自定义 Hook（use 开头的函数）中调用。
- 推论：条件逻辑放在 Hook 内部（`if (cond) useEffect(...)` 改成 `useEffect(() => { if (cond) ... })`）。

---

## 八、场景与开放题（5 题，给答题框架，没有标准答案）

### 1. 线上页面白屏，如何排查？答题框架五步

1. **先看现象范围**：全量用户还是个别？特定路由还是全站？发布后才出现还是一直有？（缩小变量）
2. **Console 报错**：JS 异常（语法错、undefined 读属性）、资源 404、CORS 报错，看第一行堆栈定位 chunk。
3. **Network 面板**：index.html 与首屏 JS 是否 200；JS 是否加载了旧 hash 但服务器已删文件（发版窗口期）；接口是否阻塞渲染。
4. **路由与资源路径**：history 模式部署后刷新 404（Nginx 缺 `try_files $uri $uri/ /index.html`）；构建 `base` 配置与部署子路径不一致导致资源 404。
5. **兜底与恢复**：先回滚版本止血，再补 ErrorBoundary、资源加载失败重载、发布脚本保留旧版本的机制。

### 2. 设计一个前端权限系统（对接后端 RBAC）的答题框架

- 模型对齐：后端 RBAC（用户→角色→权限/菜单），前端只是**消费方**：登录后拉取权限码集合与菜单树存 Pinia。
- 三层控制：**路由级**（全局 beforeEach 校验 meta.code + addRoute 动态注册路由，无权跳 403）、**菜单级**（根据后端返回的菜单树渲染侧边栏）、**按钮级**（自定义指令 `v-permission="'user:delete'"`，无权移除/禁用 DOM）。
- 体验细节：刷新后权限重建（守卫里判断 store 是否已初始化）、路由 404 兜底放最后注册。
- **底线声明（必答分）**：前端所有控制都是 UI 层体验，**真正的安全靠后端接口鉴权**（Spring Security 校验权限码），前端隐藏的按钮直接调接口必须被 403。

### 3. AI 流式对话的前端如何实现？（结合 ai-cs 项目）

- 传输选型：对话流式输出用 **SSE**（`fetch` + `ReadableStream` 手动解析 `data:` 帧，比 EventSource 多支持 POST/自定义头）；站内通知用 WebSocket（双向、服务端主动推）。
- 打字机效果：流式 chunk 到达后 push 进响应式数组/字符串，Vue 的异步批处理 + 逐字插入即呈现打字机；高频 chunk 可用 requestAnimationFrame 或节流合并渲染。
- 可中断：`AbortController` 传给 fetch 的 signal，用户点"停止生成"时 `abort()`，后端流断开即停止推理，前端 catch AbortError 静默处理。
- 工程要点：Markdown 流式渲染（未闭合代码块容错）、自动滚动到底部（用户上滚时暂停）、失败重试、消息记录本地缓存。
- 答题亮点：说出"为什么不用 WebSocket 做对话"——SSE 更轻、文本单向流足够、自动重连、走 HTTP 便于网关鉴权。

### 4. 前端监控怎么做？答题框架

- 错误监控：`window.onerror`（同步 JS 错误）、`window.addEventListener('unhandledrejection')`（Promise 未捕获）、资源错误用捕获阶段监听 error 事件；React 用 ErrorBoundary，Vue 用 `app.config.errorHandler`。
- 性能监控：Performance API / `PerformanceObserver` 采集 **FCP**（首次内容绘制）、**LCP**（最大内容绘制，核心体验）、**CLS**（累计布局偏移，视觉稳定性）、INP；接口层记录耗时与错误率。
- 上报策略：批量合并（队列满 N 条或定时 flush）、`navigator.sendBeacon`（页面卸载也不丢）、采样率控制、版本号与 sourcemap 还原堆栈。
- 数据闭环：上报 → 聚合平台（如自建或 Sentry）→ 告警 → 定位到发版 diff。

### 5. 后端工程师转前端，你的优势和要注意的差异？

- 可迁移优势：**工程化思维**（模块边界、分层、构建与 CI，与 Maven/Gradle 心智相通）、**接口设计能力**（RESTful 语义、错误码、分页约定，联调时能跟后端对齐契约）、**性能与安全意识**（缓存、并发去重、XSS/CSRF 理解成本更低）、异步模型（事件循环 vs 线程池、CompletableFuture vs Promise）一点就通。
- 需要转换的差异：前端是**事件驱动 + UI 状态机**，"一份数据多个视图"要关注状态一致性（Pinia/组件拆分）；异步无处不在且面向用户，感知性能（骨架屏、乐观更新）与技术性能同样重要；样式与交互细节（响应式、无障碍）没有后端等价物，需要刻意练习。
- 收尾话术：全栈的价值不在两端都会语法，而在**打通契约与体验**——能从接口设计反推交互，也能从交互反推接口。

---

## 自检打分表

对着题目自测，能脱稿讲清即达标。达标项连成的水平线就是你当前的阶段结论。

| 自检项 | 达标标准 | 结论 | 对应篇目 |
| --- | --- | --- | --- |
| 事件循环 | 第七题混排代码口算全对，能讲清宏微任务清空顺序 | JS 入门合格 | [02-JavaScript核心基础](./02-JavaScript核心基础.md) |
| 闭包与防抖 | 手写 debounce 无语法错误，说清 this/参数/闭包 | JS 基本功合格 | [02-JavaScript核心基础](./02-JavaScript核心基础.md) |
| 响应式原理 | 讲清 Proxy vs defineProperty 与数组坑 | Vue 进阶 | [04-Vue3核心基础](./04-Vue3核心基础.md) |
| 组件设计 | 能独立设计组件 API（props/emit/v-model/插槽/暴露方法） | Vue 组件化合格 | [08-Vue3进阶：组件设计与组合式函数](./08-Vue3进阶：组件设计与组合式函数.md) |
| 状态管理 | 说清 Pinia 选型理由并落地持久化 | 全局状态合格 | [07-Pinia状态管理实战](./07-Pinia状态管理实战.md) |
| 缓存与跨域 | 讲清强/协商缓存 + 3 种跨域方案 + OPTIONS 触发条件 | 联调合格 | [10-浏览器原理与HTTP跨域](./10-浏览器原理与HTTP跨域.md) |
| 路由与鉴权 | 守卫执行顺序 + 分层鉴权 + 动态路由 | 中后台项目合格 | [06-路由守卫与Axios请求封装](./06-路由守卫与Axios请求封装.md) |
| 工程化 | 首屏优化 8 条 + hash 发版缓存策略 | 工程化合格 | [09-前端工程化：Vite构建与规范](./09-前端工程化：Vite构建与规范.md) |
| TS 应用 | 工具类型 Partial/Omit/Pick 信手拈来，unknown 收缩成习惯 | TS 合格 | [03-TypeScript快速上手](./03-TypeScript快速上手.md) |
| 流式对话 | 能画出 SSE 流式渲染 + AbortController 中断的完整链路 | 项目实战合格 | [05-ElementPlus与前后端联调](./05-ElementPlus与前后端联调.md) |
| React 入门 | Hooks 两条铁律 + useEffect 闭包陷阱 + 不可变更新 | React 入门合格 | [11-React快速上手](./11-React快速上手.md) |
| 场景开放题 | 白屏五步 / 权限三层 / 监控上报能各讲 3 分钟 | 综合面试合格 | 本篇 + 全系列 |

## 推荐继续深入

1. **Vue 官方文档**（cn.vuejs.org）：深度指南与"深入响应式系统"章节，面试答案的权威出处。
2. **MDN Web Docs**（developer.mozilla.org）：事件循环、HTTP 头、Web API 的第一手定义。
3. **TypeScript Handbook**（typescriptlang.org/docs/handbook）：官方手册，泛型与 narrowing 讲得比多数教程准确。
4. **《JavaScript 高级程序设计》第 4 版**（红宝书）：查漏补缺的案头书，重点读第 8 章 BOM、第 10 章对象与闭包相关章节。
5. **react.dev**：React 官方新文档，用正确的心智模型学 Hooks，重点读 "Escape Hatches" 板块。
