# 06-优雅停机与零丢失滚动更新

> 对应项目文件：11 个服务的 `application.yml`（`server.shutdown: graceful` 等）、`deploy/k8s/services/*.yaml`（滚动更新 + 待补强的终止配置）
> 定位：运维篇收官。前面解决了"怎么部署、怎么监控、怎么调度"，这一篇解决最后一块——**发布更新那一瞬间，怎么保证一个请求都不丢**。

---

## 零、先定位：滚动更新时，旧实例经历了什么

Deployment 滚动更新（[02 篇](./02-Kubernetes入门.md)、[03 篇](./03-JenkinsCICD流水线.md)都见过 `set image + rollout status`）的本质是：

```
新 Pod 创建 → readiness 通过 → 杀一个旧 Pod → 循环，直到全部换完
                              ↑
                    本篇的全部内容都发生在"杀旧 Pod"这 30 秒里
```

**核心矛盾**：K8s 只知道"杀容器"，它不知道容器里还有正在处理的请求、还有消费到一半的消息。优雅停机就是给这个粗暴的"杀"加一套告别流程。

---

## 一、没有优雅停机时会发生什么（三类事故）

旧实例收到 SIGTERM 后默认**立即关闭**：

```
【事故①：HTTP 请求被切断】
  用户请求正在处理中（可能 2s，可能 20s）→ 连接直接 reset
  经过网关转发时表现为 502 / connection reset

【事故②：流量打到正在死亡的实例】
  时序陷阱：新 Pod 已经 Ready，但"旧 Pod 死亡"的消息
  还没传播到 kube-proxy/网关的负载均衡列表里
  → 新请求继续发给一个正在关闭的实例

【事故③：MQ 消息丢失重投】
  RocketMQ 消费者拉了一批消息，处理到一半实例死了
  → 消息没确认 → broker 重新投递 → 重复消费（配合幂等才能无害）
  → 更糟：实例死得突兀，broker 要等心跳超时才发现
```

**为什么"部署了 3 个副本还是会丢请求"？**——因为丢的不是"容量"，是"被杀那个实例上正在飞的请求"。副本越多，滚动更新时被杀的概率越分散，但单次更新的丢失面始终存在。

---

## 二、应用层：两行配置 + 底层原理

本项目 11 个服务的 `application.yml` 已统一加上（2026-08 落地）：

```yaml
server:
  shutdown: graceful        # SIGTERM 后：停止接新请求，等在途请求处理完再关闭

spring:
  lifecycle:
    timeout-per-shutdown-phase: 30s   # 最多等 30s，超时强制关（防止无限挂起）
```

生效后的完整时序：

```
SIGTERM 到达
   │
   ▼
① WebServer 进入 graceful shutdown
   监听端口关闭 / 拒绝新连接        ← 新请求进不来了
   等 active 请求归零               ← 在途请求正常处理完、正常返回
   │
   ▼（同时）
② 其他 SmartLifecycle 组件按 phase 从高到低依次 stop
   RocketMQ listener 停止拉取、等已拉取消息消费完
   调度线程池、连接池……全部受同一个 30s 超时约束
   │
   ▼
③ 日志打出 "Graceful shutdown complete" → 进程退出
   （30s 还没收完尾 → 超时强制关闭，宁可超时不无限挂）
```

> 原理关键词：Spring Boot 3 的 `SmartLifecycle` 体系。WebServer 的优雅停机、MQ 消费者的停止、线程池的关闭都是 Lifecycle bean，容器统一编排它们的停止顺序（phase）和总超时。想深入就看 `GracefulShutdownLifecycle` 和 `DefaultLifecycleProcessor` 两个类。

---

## 三、K8s 层：完整终止时序 + 本项目现状核对

### 3.1 一个 Pod 被删除时 K8s 做的事（按顺序）

```
kubectl delete / 滚动更新要杀旧 Pod
   │
   ① Pod 标记 Terminating
      同时把它从 Service Endpoints 摘除
      → readiness 系统层面不再给它新流量（但已有连接还在）
   │
   ② preStop hook（如果配了）：给"摘除消息传播"留时间
      常见写法 sleep 5s —— 见 3.2 本项目现状
   │
   ③ 发 SIGTERM → Spring graceful shutdown 生效（第二节整套流程）
      在途请求处理完（最长 timeout-per-shutdown-phase = 30s）
   │
   ④ terminationGracePeriodSeconds 计时到 → 还没退出就 SIGKILL 强杀
      默认 30s！
```

### 3.2 本项目现状核对（诚实说：有个贴边风险）

对照 `deploy/k8s/services/*.yaml` 实际内容：

| 配置项 | 本项目现状 | 后果 |
|--------|-----------|------|
| `server.shutdown: graceful` | ✅ 11 个服务都配了 | 应用层就绪 |
| `timeout-per-shutdown-phase: 30s` | ✅ 11 个服务都配了 | 最多收尾 30s |
| `terminationGracePeriodSeconds` | ❌ **没配**（默认 30s） | ⚠️ 和应用层 30s 正好贴边 |
| `preStop` sleep | ❌ 没配 | ⚠️ 端点摘除传播慢时会丢新请求 |

**为什么"贴边"危险**：K8s 在 SIGTERM 之前还要做 ①② 两步，这段时间是算在总宽限里的。应用拿到 SIGTERM 时可能只剩 20 多秒，而它被允许等 30s——尾部请求照样被 SIGKILL 砍掉。

**改进 YAML**（练习用，也是生产标准写法）：

```yaml
# deploy/k8s/services/*.yaml 的 Pod spec 里补充：
spec:
  terminationGracePeriodSeconds: 40        # ① 总宽限 > 应用 30s 超时，留足余量
  containers:
    - name: ai-chat-service
      lifecycle:
        preStop:
          exec:
            command: ["sh", "-c", "sleep 5"]   # ② 等 Endpoints 摘除传播到所有转发节点
```

**匹配公式背下来：`terminationGracePeriodSeconds`（40）> `preStop sleep`（5）+ `timeout-per-shutdown-phase`（30）**。任何一层比上层短，收尾就会被强杀截断。

### 3.3 三层防护全景图（把本章串起来）

```
                    【发布瞬间不丢请求的三道防线】

第1道 摘流：Service Endpoints 摘除 + readinessProbe
       → 系统层面不再派新请求（preStop sleep 补传播延迟）

第2道 收尾：SIGTERM → graceful shutdown
       → 在途请求处理完再关（30s 上限）

第3道 兜底：terminationGracePeriodSeconds
       → 无论如何 40s 后必死，防止僵尸 Pod 阻塞发布

读数关系：40s ＞ 5s + 30s ✓
```

---

## 四、验证方法：亲眼看到"零丢失"

### 4.1 本地验证（不依赖 K8s）

```bash
# ① 起服务，准备一个慢接口（比如代码里 Thread.sleep(3000) 模拟）
# ② 压测同时杀进程：
ab -n 1000 -c 20 http://localhost:8087/api/order/1 &    # 持续压测
kill -TERM <服务pid>                                     # 相当于 K8s 发的信号

# ③ 看两个地方：
#    服务日志：出现 "Commencing graceful shutdown"
#              → 在途请求正常返回 → "Graceful shutdown complete"
#    ab 结果：  Failed requests 应为 0（对照实验：kill -9 则大量失败）
```

### 4.2 K8s 验证

```bash
# 滚动更新期间持续压测网关
ab -n 5000 -c 50 http://192.168.56.10:30080/api/order/1
kubectl set image deployment/order-service order-service=...:v2 -n ai-customer-service
# 断言：全部响应 2xx，Failed requests = 0
```

补充一个运维篇内呼应：正在执行的 XXL-Job 任务被 SIGTERM 打断怎么办？`timeout-per-shutdown-phase` 会等它收尾；等不完的部分靠任务幂等 + 下轮扫描重试兜底（[05 篇](./05-XXL-Job分布式调度.md)的幂等设计在这里闭环）。

---

## 五、面试要点

- **graceful shutdown 的实现层次**（从外到内）：容器信号处理（SIGTERM）→ WebServer 拒绝新连接 → SmartLifecycle 按 phase 依次收尾 → 超时兜底强杀。
- **"配了优雅停机为什么还丢请求"**：最常见就是 Endpoints 摘除有传播延迟（iptables/eBPF 规则同步到所有节点需要时间），新请求继续打到将死实例——用 preStop sleep 补偿；其次是 `terminationGracePeriodSeconds` 小于应用收尾时间，被 SIGKILL 截断。
- **`kill -9` 为什么永远别用于下线**：跳过信号处理，等价于断电，在途请求全部失败、MQ 消息全部重投、连接池脏关闭。K8s 的 SIGKILL 只该作为超时兜底存在。
- **滚动更新零丢失的完整条件**：readinessProbe 准确 + Endpoints 摘除传播时间 < preStop sleep + graceful timeout < terminationGracePeriodSeconds，四个参数环环相扣。

---

## 动手练习

1. 本地复现事故：起一个服务，压测中 `kill -9`，统计 ab 的 Failed requests；再 `kill -TERM` 对比
2. 给 `deploy/k8s/services/order-service.yaml` 按第 3.2 节补上 `terminationGracePeriodSeconds: 40` 和 `preStop sleep 5`，重新部署后做 4.2 的压测验证
3. 观察日志关键词：分别在正常关闭、超时强杀两种情况下 grep `Graceful shutdown complete`
4. 思考题：如果把 `timeout-per-shutdown-phase` 改成 120s 而不动 K8s 宽限期，发布时会更快还是更慢？会不会更安全？（答：不会更安全——30s 一到 K8s 照样 SIGKILL，应用等了也白等；这就是"匹配公式"的意义）

---

## 学习检查清单

- [ ] 能画出滚动更新"杀旧 Pod"的完整时序（摘流 → preStop → SIGTERM → 宽限期 → SIGKILL）
- [ ] 理解 `server.shutdown: graceful` 两行配置各自的作用和 SmartLifecycle 原理
- [ ] 背出匹配公式：K8s 总宽限 > preStop sleep + 应用停机超时，并知道本项目清单默认值贴边的风险
- [ ] 知道三类停机事故（请求切断 / 打到将死实例 / MQ 重投）分别由哪道防线解决
- [ ] 会用 ab + kill -TERM 在本地做零丢失验证
- [ ] 能回答"配了优雅停机为什么还丢请求"

---

## 下一步

→ 运维篇到此收官。回到 [00-学习路线总览](../00-学习路线总览/README.md) 查漏补缺，或进入 [08-测试](../08-测试/) 补强 `SKIP_TESTS=false` 背后的测试体系。
