# 06-优雅停机与零丢失滚动更新

> 2026-08 落地记录（03-P4）：全项目 11 个服务补齐 `server.shutdown: graceful` +
> `spring.lifecycle.timeout-per-shutdown-phase`，配合 K8s 滚动更新做到在途请求零丢失。

## 一、问题：没有优雅停机时会发生什么

滚动更新（或 `docker stop`、`kill`）时，旧实例收到 SIGTERM 后默认**立即关闭**：

- 正在处理的 HTTP 请求被切断（客户端收到 connection reset / 502）
- k8s 里新 Pod 已 Ready、旧 Pod 还没收尾 → 请求打到正在死亡的实例
- RocketMQ 消费者不优雅下线 → 消息取走了没处理完，触发重投

## 二、Spring Boot 侧：两行配置

```yaml
server:
  shutdown: graceful        # SIGTERM 后停止接收新请求，等在途请求处理完再关闭

spring:
  lifecycle:
    timeout-per-shutdown-phase: 30s   # 最长等 30s，超时强制关闭（防止无限挂起）
```

原理：Boot 3 的 `SmartLifecycle` 体系。SIGTERM → WebServer 开始 graceful shutdown
（拒绝新连接、等待 active 请求归零）→ 同时 RocketMQ listener、调度线程池等
`Lifecycle`/`SmartLifecycle` 组件按 phase 依次 stop，都受同一个超时约束。

本项目落点：11 个服务的 `application.yml` 已统一加上（2026-08）。

## 三、K8s 侧：摘流时序（terminationGracePeriod > 超时）

```
kubectl 滚动更新
  → Pod 标记 Terminating，endpoint 摘除（ readiness 失败不再接新流量）
  → preStop hook（可选 sleep 几秒，给 iptables/负载均衡留摘除传播时间）
  → 发 SIGTERM → Spring graceful shutdown 生效，处理完在途请求（最长 30s）
  → 超过 terminationGracePeriodSeconds（须 > 30s，建议 40s）→ SIGKILL 强杀
```

核对点（`deploy/k8s/services/*.yaml`）：

- `terminationGracePeriodSeconds` 必须 **大于** `timeout-per-shutdown-phase`，
  否则还在等在途请求就被 SIGKILL
- 有 readinessProbe 时，graceful 期间探针失败只会加速摘流，不影响收尾

## 四、验证方法

1. 本地：起服务 → 压测慢接口（Thread.sleep 模拟）→ `kill -TERM <pid>` →
   观察日志出现 `Commencing graceful shutdown` → 在途请求正常返回 →
   `Graceful shutdown complete`
2. K8s：滚动更新期间用 `ab`/`wrk` 压测，断言 0 个非 2xx 响应

## 五、面试要点

- graceful shutdown 的实现层次：容器信号处理（SIGTERM）→ WebServer 拒新 →
  SmartLifecycle 按 phase 收尾 → 超时兜底强杀
- k8s 滚动更新为什么还会丢请求：endpoint 摘除有传播延迟 → preStop sleep 补偿；
  terminationGracePeriodSeconds 与应用超时的匹配关系
- `kill -9` 为什么要避免：跳过所有收尾，等价于断电
