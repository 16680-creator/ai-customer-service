# 03-一二梯队技术落地验证清单（Sentinel/Redisson/Seata/事务消息/可观测性/XXL-Job）

> 2026-08 一二梯队补齐后的**集成验证清单**：每项「开发 + 测试」的完成证据，
> 按本清单可随时回归。**标注 [✅ 已验证]** 的条目已在真实环境（远程 MySQL/Redis +
> 本地 RocketMQ/Seata TC + 11 服务全栈）跑通（2026-08-30）。

## 0. 前置状态

- 基础设施：Nacos/RocketMQ（tools/start-infra.ps1）、Seata TC（tools/seata/seata/bin/seata-server.bat）
- 建表：`deploy/mysql/seata-undo-log.sql`（order/product 两库 undo_log）、
  `deploy/mysql/xxl-job-init.sql`（调度中心库，admin 就绪前仅建表）
- Nacos 配置已重新发布（management 暴露、seata 配置块、pay 事务消息开关）

## 1. Sentinel（chat：入口限流）

| 项 | 证据 |
|---|---|
| 单测 | `SentinelFlowConfigTest`（规则数量/资源名/QPS 阈值/WarmUp 效果） |
| 启动日志 | [✅ 已验证] `Sentinel 流控规则已注册: chat-send QPS<=10 (WarmUp 10s), chat-rag QPS<=5` + SentinelWebInterceptor 注册 |
| 压测 | 并发打 `/api/chat/send` 超过阈值 → 返回 429 业务码「当前对话人数过多」 |

## 2. Redisson（order：下单防重锁）

| 项 | 证据 |
|---|---|
| 单测 | [✅ 已验证] `OrderCreateLockServiceTest` 4 用例（竞争拒绝/成功释放/异常释放/零等待） |
| 集成 | Redis 出现 `lock:order:create:{userId}` 锁 key；并发同用户下单第二个请求快速失败 |
| 单测 | `OrderServiceTest` 锁直通改造后 74 用例全绿 |

## 3. Seata AT（order ↔ product：下单扣库存）

| 项 | 证据 |
|---|---|
| TC | [✅ 已验证] seata-server 8091 LISTENING，控制台 http://localhost:7091（seata/seata） |
| 客户端注册 | [✅ 已验证] order 日志：TM/RM 通道注册到 TC（RegisterTMRequest/RegisterRMRequest，resourceIds=order 库） |
| 正向 | [✅ 已验证] 下单成功（orderNo=20260830023736010001，payAmount=189）：Redisson 锁内执行全局事务，库存扣减 + 订单落库 |
| 反向 | 库存不足下单 → order 事务回滚 + product 分支被 undo_log 镜像补偿（库存还原） |
| 配置 | 两服务 seata 配置块（vgroup-mapping/grouplist 直连 8091），自动数据源代理开启 |

## 4. RocketMQ 事务消息 + DLQ（pay → order）

| 项 | 证据 |
|---|---|
| 单测 | [✅ 已验证] `PaySuccessTransactionListenerTest` 6 用例（COMMIT/ROLLBACK/回查四态） |
| 正向 | [✅ 已验证] mock 支付回调 → pay 日志「事务消息本地事务执行成功 + SEND_OK + localTx=COMMIT_MESSAGE」→ order 日志「收到支付成功事件」→「支付确认成功」→ 订单状态 PAID |
| 回查 | 半消息未决时 Broker 回查按支付流水状态二次判定（checkLocalTransaction） |
| DLQ | notify 消费者 maxReconsumeTimes=3，毒消息耗尽重试进 `%DLQ%notify-consumer-group` |

## 5. 可观测性

| 项 | 证据 |
|---|---|
| 指标 | [✅ 已验证] chat 59 条、order 71 条 OpenMetrics 指标（jvm_/http_server_/hikaricp_ 等） |
| 编排 | `deploy/docker-compose/docker-compose-observability.yml`（Prometheus/Grafana/Tempo/Collector） |
| 限制 | 本机无 Docker，指标端点已验证；看板链路在有 Docker 环境一键拉起 |

## 6. XXL-Job（order：超时扫描执行器）

| 项 | 证据 |
|---|---|
| 单测 | [✅ 已验证] `XxlJobConfigTest`（开关关闭不装配/打开装配 executor） |
| 建表 | 远程 `xxl_job` 库 8 张表就绪 |
| 启用 | `XXL_JOB_ENABLED=true` + admin 部署（见 07-运维部署/05 文档）后自动注册 |

## 7. 评估记录（暂缓项）

- Testcontainers：本机无 Docker，接入清单已写入 08-测试/02 文档
- Spring Security/Sa-Token：与网关 JWT 架构重复，取舍分析见同文档
