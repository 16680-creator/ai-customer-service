# 接口安全与限流策略

> 本项目在 **网关（ai-cs-gateway）** 和 **common 模块** 实现了接口安全的三道防线：白名单、CORS、参数校验，并预留了 Sentinel 限流。
> 对应项目文件：`ai-cs-gateway/src/main/java/com/aics/gateway/filter/AuthFilter.java`、`ai-cs-chat/src/main/java/com/aics/chat/config/CorsConfig.java`

---

## 一、接口安全的三道防线

```
第一道：网关层  → 白名单 + JWT 鉴权 + CORS
第二道：应用层  → 统一参数校验 + 全局异常处理
第三道：流量层  → 限流（Sentinel）防刷
```

---

## 二、网关白名单（不需要登录的接口）

```java
// ai-cs-gateway/.../AuthFilter.java
private static final List<String> WHITE_LIST = List.of(
        "/api/user/login",       // 登录
        "/api/user/register",    // 注册
        "/api/user/captcha",     // 验证码
        "/doc.html",             // 接口文档
        "/webjars/",
        "/v3/api-docs",          // OpenAPI 文档
        "/swagger-resources"
);
```

**原则**：越少越好。只有真正无需登录的接口才放行（登录、注册、验证码、文档），其余接口一律走 JWT 校验。

---

## 三、CORS 跨域配置

```java
// ai-cs-chat/src/main/java/com/aics/chat/config/CorsConfig.java
@Configuration
public class CorsConfig {

    @Bean
    public CorsFilter corsFilter() {
        CorsConfiguration config = new CorsConfiguration();
        config.addAllowedOrigin("http://localhost:5173");  // 只允许前端域名
        config.addAllowedMethod("*");
        config.addAllowedHeader("*");
        config.setAllowCredentials(true);   // 允许携带 Cookie

        UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();
        source.registerCorsConfiguration("/**", config);
        return new CorsFilter(source);
    }
}
```

### CORS 概念

```
浏览器同源策略：www.a.com 的页面不能直接请求 www.b.com 的数据
CORS：服务端通过响应头告诉浏览器"我允许你跨域访问"

跨域请求会先发一个 OPTIONS 预检请求（Preflight）
```

> 注意：生产环境 `addAllowedOrigin` 不要用 `*`（用了 `*` 就不能 `allowCredentials=true`），应明确列出前端域名，或从配置中心读取。

---

## 四、统一参数校验

### 4.1 依赖

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-validation</artifactId>
</dependency>
```

### 4.2 控制器参数校验

```java
// ai-cs-chat/.../ChatController.java
import jakarta.validation.constraints.NotBlank;
import org.springframework.validation.annotation.Validated;

@RestController
@RequestMapping("/chat")
@Validated   // 开启参数校验
public class ChatController {

    @PostMapping("/send")
    public Result<String> chat(
        @RequestParam @NotBlank(message = "会话ID不能为空") String sessionId,
        @RequestParam @NotBlank(message = "消息内容不能为空") String message) {
        return chatService.chat(sessionId, message);
    }
}
```

### 4.3 常用校验注解

| 注解 | 作用 |
|------|------|
| `@NotBlank` | 字符串不能为 null 且不能全空白 |
| `@NotNull` | 不能为 null |
| `@Size(min, max)` | 长度范围 |
| `@Min / @Max` | 数值范围 |
| `@Email` | 邮箱格式 |
| `@Pattern` | 正则校验 |

### 4.4 DTO 校验（配合 @RequestBody）

```java
public class OrderCreateDTO {
    @NotNull(message = "用户ID不能为空")
    private Long userId;

    @NotEmpty(message = "商品列表不能为空")
    private List<CartUpdateDTO> items;
}
```

```java
@PostMapping("/create")
public Result<Long> create(@Valid @RequestBody OrderCreateDTO dto) {
    return orderService.createOrder(dto);
}
```

---

## 五、限流（Sentinel，预留）

> 技术栈中已引入 Sentinel，用于接口限流和熔断。核心思路：

```yaml
# 限流规则示意（也可通过控制台动态配置）
spring:
  cloud:
    sentinel:
      transport:
        dashboard: localhost:8088   # Sentinel 控制台
        port: 8719
```

```java
// 在方法上声明资源，配合限流规则
@SentinelResource(value = "chat:send", fallback = "chatFallback")
public Result<String> chat(String sessionId, String message) {
    return chatService.chat(sessionId, message);
}

// 触发限流后的兜底方法
public Result<String> chatFallback(String sessionId, String message, BlockException ex) {
    return Result.fail(429, "请求过于频繁，请稍后再试");
}
```

### 常见限流策略

| 策略 | 说明 | 适用 |
|------|------|------|
| QPS 限流 | 每秒最多 N 次 | 接口防刷 |
| 并发线程数 | 同时最多 N 个 | 保护下游 |
| 热点参数 | 按参数值限流（如按 userId） | 防单用户刷 |
| 熔断降级 | 失败率过高时快速失败 | 保护依赖 |

---

## 六、其他常见安全实践

1. **SQL 注入**：使用 MyBatis-Plus / JPA 参数化查询，禁止字符串拼 SQL
2. **XSS**：前端对用户输入做转义，后端对富文本过滤
3. **密码安全**：存 `BCrypt` 哈希，不存明文
4. **敏感信息**：密钥从 Nacos 配置中心读取，不写死在代码
5. **日志脱敏**：打印日志时隐藏手机号、身份证等敏感字段

---

## 七、动手练习

1. 在 AuthFilter 白名单里新增一个接口路径，验证该接口无需 Token 可访问
2. 给一个接口加 `@Min` / `@NotBlank` 参数校验，用非法参数访问看返回
3. 配置 CORS 只允许 `localhost:5173`，用其他来源访问观察被拦截
4. 接入 Sentinel，给对话接口加一个 QPS=10 的限流规则，压测触发限流

---

## 学习检查清单

- [ ] 理解接口安全的三道防线（网关/应用/流量）
- [ ] 理解白名单的用途和最小化原则
- [ ] 会用 `@NotBlank` / `@NotNull` / `@Valid` 做参数校验
- [ ] 理解 CORS 及 `allowCredentials` 的限制
- [ ] 理解限流 / 熔断 / 降级的概念
- [ ] 知道 SQL 注入、XSS、密码安全的应对

---

## 下一步

→ [04-中间件/02-RocketMQ消息队列](../04-中间件/02-RocketMQ消息队列.md)