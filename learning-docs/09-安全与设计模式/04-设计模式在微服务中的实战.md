# 04-设计模式在微服务中的实战

> 2026-09 落地记录：本分类此前只有安全三篇，"设计模式"有名无实（仅 [01-JWT鉴权与异常处理](01-JWT鉴权与异常处理.md) 第八节带过单例/工厂/策略）。本篇用项目里的真实类逐个兑现：每个模式给出"类名 + 包路径 + 为什么在这里用它"。
> 诚实盘点：模板方法在主源码零实例（grep `abstract class` 无命中）、无标准 GoF 观察者——这些"没有"同样写进第九节，不硬套。

---

## 一、为什么用真实代码学设计模式

背"23 种模式的 UML"没有意义，有意义的是回答：**这个模式在这个位置解决了什么代价？** 本篇每个模式都按同一模板讲：没有它会怎样 → 项目里长什么样 → 边界在哪。

## 二、策略模式：PayChannel 四渠道

- 类：`com.aics.pay.channel.PayChannel`（接口）+ `AlipayChannel` / `WechatNativeChannel` / `UnionpayChannel` / `MockPayChannel`
- 详解见 [02-Spring微服务/13-支付渠道集成与回调一致性](../02-Spring微服务/13-支付渠道集成与回调一致性.md) 第三节

**没有它**：`PayService` 里 `if (method==ALIPAY) {...} else if (method==WECHAT) {...}`——四渠道的 SDK 依赖全部堆进一个类，新增渠道改核心类，单元测试无法隔离。

**边界**：策略适合"同一件事的多种做法"；如果各渠道还有公共骨架（如"下单→落流水→返回"三步固定），那才轮到模板方法——本项目没有这个公共骨架，所以只有策略，没有模板方法，两不硬凑。

## 三、工厂 + 策略：PayChannelFactory 的注册表套路

类：`com.aics.pay.channel.PayChannelFactory`

```java
public PayChannelFactory(List<PayChannel> channels) {        // Spring 注入所有实现
    this.channelMap = channels.stream()
            .collect(Collectors.toMap(PayChannel::getMethod, Function.identity()));
}
public PayChannel getChannel(String method) {
    PayChannel channel = channelMap.get(method);
    if (channel == null) throw new BusinessException(ResultCode.ORDER_PAYMENT_METHOD_INVALID);
    return channel;
}
```

要点是**构造器注入 List**：Spring 把接口的所有实现都递给你，工厂只需建一个 Map 索引。"工厂"退化成十行代码，因为**实例管理交给了 IoC 容器**——传统 GoF 工厂解决的是 new 的解耦，Spring 环境下它只剩"按 key 选用"这一个职责。面试时能讲清这个演变，比背 UML 加分得多。

## 四、状态机（状态模式的工程化形态）：一个项目两种写法

本项目有两台状态机，恰好构成"框架版 vs 手写版"对照：

| | 订单状态机 | Agent 状态机 |
|---|---|---|
| 类 | `com.aics.order.statemachine.OrderStateMachineConfig`（Spring Statemachine 4.0） | `com.aics.chat.agent.state.AgentStateMachine` |
| 定义方式 | `StateMachineBuilder` DSL 配置迁移 | `EnumMap<AfterSaleState, Set<AfterSaleState>>` 手写迁移表 |
| 状态 | PENDING_PAY→PAID→REFUNDING→REFUNDED / CANCELLED / TIMEOUT | START→CLASSIFY_INTENT→LOCATE_ORDER→…→COMPLETED/HANDOFF/FAILED（每状态附 ALLOWED_TOOLS 工具白名单） |
| 事实来源 | DB status 是唯一事实，不用内存 persister | 每步迁移留痕（AgentTraceRecorder） |
| 适合场景 | 状态多、事件多、需要守卫/动作钩子 | 迁移简单、要极致可控可测 |

手写版的精髓（值得背的注释原则）：**终态无出边、迁移表即文档、非法迁移直接拒绝**。状态机与 [12-订单状态机治理](../02-Spring微服务/12-订单状态机治理.md) 的关系：那是"怎么治理订单状态"，本节是"两台状态机在模式光谱上的位置"。

## 五、装饰器模式：给 Embedding 套缓存

类：`com.aics.chat.cache.CachingEmbeddingModel` —— `implements EmbeddingModel`，内部持有 delegate（真正的模型）+ `VectorCacheStore`，只覆写 `call(EmbeddingRequest)`：先查缓存（键带模型名命名空间），未命中才透传 delegate 并回填。

**没有它**：缓存逻辑散进每个调用点，或去改 Spring AI 源码。**有了它**：对 Spring AI 而言它就是一个普通 `EmbeddingModel`（L1 LRU 4096 + L2 Redis 168h 的实现细节完全透明），`@Primary` 装配即全链路生效。装饰器在 Spring 生态无处不在：`BufferedInputStream`、Resilience4j 的 `@TimeLimiter/@Retry/@CircuitBreaker`（`com.aics.chat.config.Resilience4jConfig` 三实例 chatService/sseChatService/visionService）、`QuestionAnswerAdvisor`（RAG 顾问，本质是给 ChatClient 调用"装饰"一段检索注入，见 `SpringAiConfig.ragAdvisor`）——**同一个形状：实现接口 + 持有同类引用 + 选择性覆写**。

配套的降级近亲：`com.aics.common.ai.embedding.HashEmbeddingModel`（特征哈希零依赖兜底，未配 embedding key 时自动顶上）——装饰器管"增强"，它管"没有真货时的替身"。

## 六、责任链变体：网关过滤器链

类：`com.aics.gateway.filter.AuthFilter`（GlobalFilter，`order = HIGHEST_PRECEDENCE + 100`）→ `RateLimitFilter`（order = +150）；限流算法本身又是两个策略：`SlidingWindowRateLimiter` / `TokenBucketRateLimiter`。

Spring Cloud Gateway 的 GlobalFilter 天然按 order 排成一条链：**鉴权必须先于限流**（先确定身份，再按身份限流）——order 常量里的 +100/+150 就是这条链的"合同"。与标准责任链的差异：不用手动组织 successor 指针，容器按 order 自动编排；每个过滤器可以选择"处理后放行"或"直接响应终结请求"（如 AuthFilter 鉴权失败直接 401，链到此为止）。

## 七、建造者：复杂参数对象的组装

素材：`com.aics.pay.channel` 的 `NotifyContext` / `PayContext` / `PayResult` / `NotifyResult` / `RefundResult` 全部 Lombok `@Builder`：

```java
NotifyContext.builder().params(queryParams).headers(wechatHeaders).body(rawJson).build();
```

为什么这五个类需要 builder：都是**多可选字段的值对象**（渠道不同，有的字段有、有的没有），重载构造器会爆炸。另一个进阶素材是 Spring Statemachine 的 `StateMachineBuilder.Builder`（DSL 式建造者）。**边界**：两个字段的简单 DTO 别用 builder，是噪音。

## 八、静态工厂与横切规范：Result 体系别硬套模式

`com.aics.common.result.Result<T>`（私有构造 + 静态 `Result.success(...)`）+ `ResultCode` 枚举 + `BusinessException` + `GlobalExceptionHandler`（`@RestControllerAdvice`）。

如实归类：这是**统一响应封装 + 集中异常处理**，`Result.success()` 有静态工厂方法的"味道"，但整套不是某个 GoF 模式的实现——它是团队横切规范。给初学者的提醒：**见到静态方法就喊"工厂模式"、见到接口就喊"策略"，是模式过敏症**；模式的判定标准是"解决了什么代价"，不是"长得像 UML"。

## 九、如实盘点：项目里"没有"的模式

| 模式 | 结论 | 依据 |
|---|---|---|
| 模板方法 | **无实例** | 主源码 grep `abstract class` 零命中；`Result` 等基类无骨架/钩子结构 |
| GoF 观察者 | **无标准实现** | `ModelUsageRecorder`（`com.aics.chat.observability`）是"异步计量上报器"（线程池 + Feign 落库 + 费用估算），不是事件广播；最接近的近亲是 Spring 事件监听 `com.aics.order.event.OrderTimeoutEventListener` |
| 单例 | 不用 GoF 手法 | IoC 容器默认单例；Python 侧用 `@lru_cache`（见 [10-Python服务/01](../10-Python服务/01-FastAPI对话服务实战.md)） |
| 原型/享元/命令等 | 无典型实例 | 不硬凑 |

把"没有"写明白，和把"有"讲清楚一样重要——这决定了你在面试里是背书的人，还是读过代码的人。

## 十、面试要点总结

> 本项目的模式密度集中在三处：支付域以策略（PayChannel 四渠道）+ 注册表工厂（构造器注入 List 建 Map）实现渠道扩展零侵入；AI 域以装饰器为主旋律（CachingEmbeddingModel 缓存装饰、Resilience4j 注解式弹性装饰、QuestionAnswerAdvisor 检索装饰）；网关以 GlobalFilter 有序链落实责任链变体（鉴权 order+100 先于限流 +150，算法再以策略分裂为滑窗/令牌桶）。另有双状态机对照（Spring Statemachine DSL vs 手写 EnumMap 迁移表）与 Lombok @Builder 值对象；同时如实保留空白：无模板方法实例、无标准观察者。

```text
一图背版：
策略 = PayChannel×4 · 工厂 = PayChannelFactory(List→Map)
状态机 = OrderStateMachineConfig(Spring DSL) vs AgentStateMachine(EnumMap 表)
装饰器 = CachingEmbeddingModel / Resilience4j / QuestionAnswerAdvisor
责任链 = AuthFilter(+100) → RateLimitFilter(+150) · 算法策略 = 滑窗/令牌桶
建造者 = NotifyContext.builder() · 静态工厂味 = Result.success()
没有 = 模板方法(0 个 abstract class) · 标准观察者(只有 Spring 事件近亲)
```

## 学习检查清单

- [ ] 对每个模式能答"没有它会付出什么代价"
- [ ] 能解释 Spring 环境下工厂模式为什么退化成十行 Map
- [ ] 能对照说出两台状态机的设计差异与适用边界
- [ ] 能指出项目里"没有"的两个模式并给出证据
