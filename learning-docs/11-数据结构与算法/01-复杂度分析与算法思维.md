# 复杂度分析与算法思维

> 对应项目：`ai-cs-chat/.../cache/SemanticCacheService.java`（O(n) 线性扫描 vs HNSW 近似索引的取舍）、
> `ai-cs-chat/.../rag/retrieve/MultiQueryMerger.java`（多路融合的复杂度推导）、
> `01-Java基础/05-集合框架与源码.md`（HashMap 扩容的均摊分析）。

---

## 一、为什么要先学复杂度

复杂度不是"写完代码后算一下"的装饰品，而是**写代码之前的选择依据**。

看一个本项目的真实决策：

```java
// ai-cs-chat/.../cache/SemanticCacheService.java
// 语义缓存查找：候选集上限 200 条，逐条算余弦相似度
for (Map.Entry<String, String> e : candidates.entrySet()) {
    double sim = cosine(queryVec, parse(e.getValue()));
    if (sim >= threshold) { return e.getValue(); }
}
```

这段代码是 **O(n)** 的线性扫描。第一反应可能是"太慢了，上向量索引（HNSW）"。
但算一下：n ≤ 200，向量维度 768。

- 余弦相似度 = 768 次乘加 → 约 10^3 次浮点运算
- 200 条候选 → 约 2×10^5 次浮点运算 ≈ **0.1 毫秒级**
- 而一次 Redis 网络往返 ≈ 1 毫秒级，一次 LLM 调用 ≈ 秒级

**结论**：在这个量级下，O(n) 扫描的耗时被网络 I/O 完全淹没，引入 HNSW（O(log n) 但实现复杂、需要额外内存和调参）是过度设计。

这就是算法思维的实战价值：**先量化，再决策**。不是所有 O(n) 都需要优化成 O(log n)。

---

## 二、大 O 表示法

### 2.1 定义（精确表述）

存在正常数 `c` 和 `n₀`，使得当 `n ≥ n₀` 时，`0 ≤ T(n) ≤ c·f(n)`，则记 `T(n) = O(f(n))`。

三个关键理解：

1. **上界，不是精确值**：快排平均 O(n log n)、最坏 O(n²)，说"快排是 O(n log n)"其实不严谨，应说"平均 O(n log n)"。
2. **忽略常数和低阶项**：`3n² + 100n + 5` 就是 O(n²)。但**工程上常数很重要**——当 n 小时，`100n` 的 O(n) 算法可能比 `3n²` 的 O(n²) 慢。
3. **n 的定义要明确**：排序里 n 是元素个数；图论里常用 `V`（顶点）+ `E`（边）；字符串匹配里是 `m`（模式串）+ `n`（主串）。

### 2.2 常见量级排序

```
O(1) < O(log n) < O(n) < O(n log n) < O(n²) < O(n³) < O(2ⁿ) < O(n!)

常数   对数      线性    线性对数      平方      立方      指数     阶乘
                                                        （不可解）
```

**记忆锚点**：
- `O(log n)`：每次操作把问题规模**砍半**（二分查找、树的高度）
- `O(n log n)`：砍半 × 每层处理 n 个（归并、快排、堆排）
- `O(2ⁿ)`：每个元素**选或不选**的穷举（子集、0/1 暴力搜索）

### 2.3 直观量级感受

| 复杂度 | n=1000 | n=10^5 | n=10^6 | 典型场景 |
|---|---|---|---|---|
| O(1) | 1 | 1 | 1 | 哈希查找、数组下标 |
| O(log n) | 10 | 17 | 20 | 二分、红黑树、跳表 |
| O(n) | 10³ | 10^5 | 10^6 | 一次遍历、线性扫描 |
| O(n log n) | 10^4 | 1.7×10^6 | 2×10^7 | 快排、归并、TopK 排序法 |
| O(n²) | 10^6 | **10^10** | — | 双层循环、冒泡排序 |

> 经验法则：单次操作约 10^8 次/秒（Java）。
> **n=10^5 时 O(n²) = 10^10 次操作 ≈ 100 秒 → 必然超时**。这是刷题时最常见的 TLE 原因。

---

## 三、如何分析一段代码的复杂度

### 3.1 循环：看嵌套层数

```java
// O(n)
for (int i = 0; i < n; i++) { ... }

// O(n²)：嵌套，且内层次数依赖外层
for (int i = 0; i < n; i++) {
    for (int j = 0; j < i; j++) { ... }   // 总次数 = 0+1+...+(n-1) = n(n-1)/2
}
// n(n-1)/2 = (n² - n)/2 → 忽略低阶项 → O(n²)

// O(log n)：每次砍半
for (int i = 1; i < n; i *= 2) { ... }    // 1,2,4,8... 共 log₂n 次

// O(n log n)：外层砍半 × 内层线性
for (int i = 1; i < n; i *= 2) {
    for (int j = 0; j < n; j++) { ... }
}
```

### 3.2 易错：循环次数不直接等于 n

```java
// 陷阱：外层是 log n，内层也是 log n → O((log n)²)，不是 O(log n)
for (int i = 1; i < n; i *= 2) {
    for (int j = 1; j < n; j *= 2) { ... }
}

// 陷阱：两个独立循环是相加，取最大项 → O(n + m)，不是 O(n·m)
for (int i = 0; i < n; i++) { ... }
for (int j = 0; j < m; j++) { ... }
```

**规则**：
- **串行（并列）** → 复杂度**相加**，取最大项
- **嵌套** → 复杂度**相乘**

### 3.3 递归：递归树法

以归并排序为例：

```
T(n) = 2·T(n/2) + O(n)
            归并：两个子问题各 n/2，合并需要 O(n)

递归树：
层 0：              n                 总工作量 = n
层 1：        n/2      n/2            总工作量 = n
层 2：    n/4   n/4  n/4   n/4        总工作量 = n
...
层 log n： 1  1  1 ...  1             总工作量 = n

树高 = log n，每层 O(n)  → 总计 O(n log n)
```

### 3.4 主定理（Master Theorem，了解即可）

对于递推式 `T(n) = a·T(n/b) + O(n^d)`（a≥1, b>1, d≥0）：

| 情况 | 条件 | 结果 | 例子 |
|---|---|---|---|
| 1 | `d < log_b a` | `O(n^(log_b a))` | `T(n)=2T(n/2)+O(1)` → O(n) |
| 2 | `d = log_b a` | `O(n^d · log n)` | `T(n)=2T(n/2)+O(n)` → O(n log n) ← 归并 |
| 3 | `d > log_b a` | `O(n^d)` | `T(n)=2T(n/2)+O(n²)` → O(n²) |

**直觉**：比较"子问题合并的代价 `n^d`"与"叶子节点总数 `n^(log_b a)`"，谁大谁主导，相等则多乘一个 log n。

> 面试不常考主定理本身，但**递归树画图**的过程经常要讲。

---

## 四、均摊复杂度（Amortized Complexity）—— 面试高频

> **考点**：`ArrayList` 尾部插入（add）是 O(1) 还是 O(n)？

**答案：均摊 O(1)。**

### 4.1 ArrayList 扩容分析

```java
// JDK 源码（简化）
private Object[] grow(int minCapacity) {
    int oldCapacity = elementData.length;
    int newCapacity = oldCapacity + (oldCapacity >> 1);   // 1.5 倍
    return elementData = Arrays.copyOf(elementData, newCapacity);
}
```

从空数组开始连续 `add` n 次：

| 操作 | 何时扩容 | 复制代价 |
|---|---|---|
| add 第 1 次 | 0 → 10 | 0（首次分配） |
| add 第 11 次 | 10 → 15 | 复制 10 |
| add 第 16 次 | 15 → 22 | 复制 15 |
| add 第 23 次 | 22 → 33 | 复制 22 |
| ... | ×1.5 | ... |

扩容发生在容量 10、15、22、33、49… 即约 `10·1.5^k` 的位置。
总复制次数（等比求和）≈ `10 + 15 + 22 + ... + n` < `n/0.5` = **2n**（等比级数收敛）。

**n 次 add 的总代价 = n（写入）+ 2n（复制）= O(n)**
**均摊到每次 add = O(n)/n = O(1)**

### 4.2 为什么必须是"倍数扩容"

反例：如果改成**每次固定 +1** 个容量：

- add 第 11、12、13… 次都要复制，每次复制 O(k)
- 总代价 = 1 + 2 + 3 + ... + n = **O(n²)**
- 均摊到每次 = **O(n)** ❌ 退化

**结论**：倍数扩容（1.5 或 2 倍）保证均摊 O(1)；固定增量扩容导致均摊 O(n)。

> HashMap 扩容同理（2 倍），均摊 O(1) 插入，见 [01-Java基础/05-集合框架与源码](../01-Java基础/05-集合框架与源码.md)。

### 4.3 均摊 vs 平均 vs 最坏

| 概念 | 含义 | 保证 | 例子 |
|---|---|---|---|
| 最坏 | 单次操作的最大代价 | 严格上界 | ArrayList 单次 add 可能 O(n)（扩容那次） |
| 均摊 | **连续多次操作**的总代价 ÷ 次数 | 保证序列总代价 | ArrayList n 次 add 均摊 O(1) |
| 平均 | 在**输入分布**上的期望 | 概率性 | 快排平均 O(n log n)，最坏 O(n²) |

**关键区别**：
- 均摊是**确定性的**，保证任意 n 次连续操作总代价 ≤ O(n)，不依赖运气
- 平均是**概率性的**，单次可能被最坏情况卡住（快排遇到有序数组）

---

## 五、空间复杂度

### 5.1 常见场景

| 场景 | 空间 | 说明 |
|---|---|---|
| 原地交换（冒泡、堆排） | O(1) | 只用常数个临时变量 |
| 快排 | O(log n) | **递归调用栈**深度（平均） |
| 归并排序 | O(n) | 需要辅助数组合并 |
| 递归（深度 d） | O(d) | 每层栈帧占用 |
| 哈希表存 n 个 | O(n) | 装填因子以下 |

### 5.2 易忽略：递归栈空间

```java
// 斐波那契递归：时间 O(2ⁿ)，空间 O(n)（树的最大深度），不是 O(2ⁿ)
int fib(int n) {
    if (n <= 1) return n;
    return fib(n - 1) + fib(n - 2);
}
```

很多人误以为空间也是 O(2ⁿ)。实际上**同时存在的栈帧**最多是树高 n，因为兄弟子树是先后执行、栈帧会释放。

> 但 O(2ⁿ) 的时间是真的慢：n=50 就要算约 10^15 次。优化方向：记忆化 → O(n) 时间 O(n) 空间；滚动数组 → O(n) 时间 O(1) 空间。

### 5.3 项目现场：MultiQueryMerger 的复杂度

```java
// ai-cs-chat/.../rag/retrieve/MultiQueryMerger.java
public static List<Document> merge(List<List<Document>> results, int topK, int rrfK) {
    Map<String, Double> scoreMap = new LinkedHashMap<>();   // docId -> RRF 分
    Map<String, Document> docMap  = new LinkedHashMap<>();   // docId -> 文档

    for (List<Document> list : results) {          // R 路
        int rank = 1;
        for (Document doc : list) {                // 每路 K 条
            scoreMap.merge(id, 1.0 / (k + rank), Double::sum);
            docMap.putIfAbsent(id, doc);
            rank++;
        }
    }
    List<String> sortedIds = new ArrayList<>(scoreMap.keySet());
    sortedIds.sort(...);                           // 排序
    ...
}
```

**推导**（设 R 路、每路 K 条、去重后 D 个文档、返回 topK 个）：

| 阶段 | 时间 | 说明 |
|---|---|---|
| 累加 | **O(R·K)** | 遍历所有文档，Map 操作 O(1) |
| 排序 | **O(D log D)** | D ≤ R·K |
| 截断 | O(topK) | |
| **总计** | **O(D log D)**（D ≤ R·K） | 排序主导 |

**空间 = O(D)**（两个 Map + 排序列表）。

**工程启发**：R=3 路、K=20 条 → D ≤ 60，排序 60 个元素约 60·log₂60 ≈ 354 次比较，可忽略。
如果这里用**堆**取 Top-K 而非全排序，可降到 O(D·log topK)。但当 D 只有几十时，全排序更简单且更快（常数小、缓存友好）——**又是"先量化再决策"**。

---

## 六、算法思维的四个常用套路

### 6.1 双指针 / 滑动窗口

**适用**：连续子数组、子串问题，把 O(n²) 降到 O(n)。

```java
// 长度最小的子数组（LeetCode 209）：O(n) 滑动窗口
int minSubArrayLen(int target, int[] nums) {
    int left = 0, sum = 0, min = Integer.MAX_VALUE;
    for (int right = 0; right < nums.length; right++) {
        sum += nums[right];
        while (sum >= target) {                 // 满足条件就收缩左边界
            min = Math.min(min, right - left + 1);
            sum -= nums[left++];
        }
    }
    return min == Integer.MAX_VALUE ? 0 : min;
}
```

**为什么是 O(n)**：`right` 和 `left` 都只向右移动，各走 n 步，总共 2n 次 → O(n)。
（注意：虽然有两层循环，但**不是** O(n²)，因为内层 while 的总执行次数受限于 left 的移动次数。）

### 6.2 空间换时间

**适用**：重复计算、频繁查找。

| 问题 | 暴力 | 加缓存后 |
|---|---|---|
| 两数之和 | O(n²) 双重循环 | O(n)：哈希表存"值 → 下标" |
| 斐波那契 | O(2ⁿ) | O(n)：记忆化 / DP |
| 重复子问题 DP | 指数级 | 多项式级 |

### 6.3 分治

**适用**：问题可拆成结构相同的子问题（归并、快排、二分）。

### 6.4 预处理 / 前缀和

**适用**：频繁区间查询。

```java
// 前缀和：区间 [i,j] 的和 = pre[j+1] - pre[i]，O(n) 预处理后每次查询 O(1)
int[] pre = new int[n + 1];
for (int i = 0; i < n; i++) pre[i + 1] = pre[i] + nums[i];
int rangeSum = pre[j + 1] - pre[i];   // O(1)
```

---

## 七、性能优化的正确顺序

```
1. 先测量，不要猜
   └─ 用 JMH / Arthas / Prometheus 找到真正的瓶颈
      （本项目：07-运维部署/04-Prometheus可观测性.md）

2. 再算复杂度
   └─ 确认是不是算法量级问题，而不是常数问题

3. 然后优化算法
   └─ O(n²) → O(n log n) → O(n)

4. 最后优化常数
   └─ 减少对象创建、用基本类型、批量 I/O、利用缓存局部性

5. 重新测量验证
```

> **反例警告**：在没有 profile 的情况下，把 O(n) 的清晰代码改成 O(log n) 但难懂且带 bug 的实现，
> 而实际瓶颈是一次数据库查询 —— 这是过度优化，净收益为负。

---

## 八、面试高频问答

**Q1：ArrayList 的 add 是 O(1) 吗？**
A：均摊 O(1)，最坏 O(n)（扩容时复制）。1.5 倍扩容保证 n 次连续 add 总代价 O(n)。若固定增量扩容则退化为均摊 O(n)。

**Q2：O(n) 一定比 O(n log n) 快吗？**
A：不一定。当 n 小时常数主导，如 n=10 时，`1000n` 的 O(n) 算法（10^4 次）比 `n log n`（约 33 次）慢很多。大 O 描述的是**增长趋势**，不是绝对耗时。

**Q3：如何分析递归的复杂度？**
A：写递推式 → 画递归树 → 算每层工作量 × 树高。或者套主定理 `T(n) = aT(n/b) + O(n^d)`。

**Q4：时间复杂度和实际性能不一致怎么办？**
A：大数据看复杂度（量级差异压倒常数），小数据看常数和实现（缓存局部性、分支预测、对象分配）。本项目语义缓存的 O(n) 线性扫描就是典型案例。

**Q5：空间复杂度怎么算递归？**
A：看**最大递归深度**（同时存在的栈帧数），不是总调用次数。斐波那契递归时间 O(2ⁿ) 但空间 O(n)。

---

## 九、动手练习

1. 分析 `MultiQueryMerger.merge` 中若把 `sortedIds.sort(...)` 换成 `PriorityQueue` 取 Top-K，复杂度变成多少？在什么 R、K 取值下更快？（答案：O(D·log topK)，当 topK << D 时更优）
2. 如果语义缓存候选集从 200 涨到 10 万，O(n) 线性扫描还能用吗？需要什么结构？（答案：不能，需要 HNSW / IVF 等近似最近邻索引，O(log n)）
3. 证明：ArrayList 从空表连续 add n 次，总复制次数 < 2n。（提示：等比级数求和）

---

> 下一篇：[02-线性结构：数组·链表·栈·队列](./02-线性结构-数组链表栈队列.md)
