# 线性结构：数组 · 链表 · 栈 · 队列

> 对应项目：`ai-cs-chat/.../security/SecurityAuditRecorder.java`（`ArrayDeque` 有界队列，最近 200 条审计）、
> `ai-cs-gateway/.../filter/SlidingWindowRateLimiter.java`（`ConcurrentLinkedDeque` 时间戳窗口）、
> `ai-cs-order/.../OrderServiceImpl.java` + `OrderTimeoutScheduler.java`（延迟队列与订单超时）。

---

## 一、内存布局决定性能：数组 vs 链表

这是理解所有线性结构的**第一性原理**。

```
数组（连续内存）：
┌───┬───┬───┬───┬───┬───┐
│ a │ b │ c │ d │ e │ f │   ← 一段连续的内存
└───┴───┴───┴───┴───┴───┘
  0   1   2   3   4   5
      地址 = 首地址 + i × 元素大小   → 随机访问 O(1)

链表（离散内存）：
┌───┬──┐   ┌───┬──┐   ┌───┬──┐
│ a │●─┼──▶│ b │●─┼──▶│ c │◌ │
└───┴──┘   └───┴──┘   └───┴──┘
  节点散落在堆各处，靠指针串起来 → 第 i 个必须从头走 i 步，O(n)
```

### 1.1 缓存局部性（为什么 LinkedList 实测常慢于 ArrayList）

CPU 读内存不是"一个字节一个字节"读，而是**一次读一整块（Cache Line，通常 64 字节）**到高速缓存。

| 结构 | 遍历时 Cache 命中 | 原因 |
|---|---|---|
| 数组 | **高** | 读 a[0] 时顺带把 a[1..7] 都载入缓存，后续 7 次访问全命中 |
| 链表 | **低** | 节点分散，读 next 时几乎必然 Cache Miss，每次都要访问主存 |

**主存访问 ≈ 100ns，L1 缓存 ≈ 1ns，差 100 倍。**

**实测结论**：即使理论复杂度相同，ArrayList 的**遍历**通常比 LinkedList 快数倍。
LinkedList 只在"已知节点位置，做 O(1) 插入/删除"时才占优——但这个场景在实际业务中极少（因为找到那个节点本身就要 O(n)）。

> **工程建议**：Java 里默认用 `ArrayList`。别再说"频繁增删用 LinkedList"——除非你手里已经拿着节点引用（如 LRU 的双向链表实现，见 [09-缓存淘汰](./09-缓存淘汰-LRU-LFU与两级缓存.md)）。

---

## 二、ArrayList：动态数组

### 2.1 三个核心操作

| 操作 | 复杂度 | 说明 |
|---|---|---|
| `get(i)` / `set(i, v)` | **O(1)** | 首地址 + i × 4（引用压缩后） |
| `add(e)` 尾部 | **均摊 O(1)** | 扩容时 O(n)，1.5 倍扩容保证均摊（见 [01](./01-复杂度分析与算法思维.md) §4） |
| `add(i, e)` 中间插入 | **O(n)** | 需要 `System.arraycopy` 搬移 i 之后的所有元素 |
| `remove(i)` | **O(n)** | 同样需要搬移 |
| `contains(e)` | O(n) | 线性扫描 + equals |

### 2.2 扩容源码要点

```java
// JDK17 ArrayList.grow（简化）
private Object[] grow(int minCapacity) {
    int oldCapacity = elementData.length;
    int newCapacity = oldCapacity + (oldCapacity >> 1);  // 1.5 倍（右移 1 位 = /2）
    if (newCapacity - minCapacity <= 0) newCapacity = minCapacity;
    if (newCapacity > MAX_ARRAY_SIZE) newCapacity = hugeCapacity(minCapacity);
    return elementData = Arrays.copyOf(elementData, newCapacity);
}
```

**工程技巧**：已知容量时**预分配**，避免多次扩容复制。

```java
// 坏：从 0 加到 10000，扩容约 log1.5(10000) ≈ 23 次，累计复制约 20000 个元素
List<Order> list = new ArrayList<>();

// 好：一次到位
List<Order> list = new ArrayList<>(10000);
```

### 2.3 易错：remove 时的下标漂移

```java
// ❌ 错误：删除中改变 size，会漏元素甚至越界
for (int i = 0; i < list.size(); i++) {
    if (list.get(i).isExpired()) list.remove(i);   // 删了 i，后面元素左移，i++ 会跳过一个
}

// ✅ 方案1：倒序遍历（推荐，不影响未处理部分的下标）
for (int i = list.size() - 1; i >= 0; i--) {
    if (list.get(i).isExpired()) list.remove(i);
}

// ✅ 方案2：removeIf（内部用迭代器，最简洁）
list.removeIf(Order::isExpired);

// ✅ 方案3：迭代器
Iterator<Order> it = list.iterator();
while (it.hasNext()) if (it.next().isExpired()) it.remove();
```

---

## 三、栈与队列：用 ArrayDeque，不要用 Stack

### 3.1 Java 的选择题

| 需求 | ❌ 别用 | ✅ 用 | 原因 |
|---|---|---|---|
| 栈 | `Stack`（继承 Vector，所有方法 synchronized，慢且设计过时） | `ArrayDeque` | 循环数组，O(1) 且缓存友好 |
| 队列 | `LinkedList` | `ArrayDeque` | 同上，且省去节点对象开销 |
| 优先队列 | — | `PriorityQueue` | 堆实现，见 [04](./04-树堆与TopK.md) |
| 并发队列 | — | `ConcurrentLinkedQueue` / `ArrayBlockingQueue` | 无锁 / 有界阻塞 |

```java
Deque<Integer> stack = new ArrayDeque<>();
stack.push(1);        // 入栈
stack.pop();          // 出栈
stack.peek();         // 看栈顶

Queue<Integer> queue = new ArrayDeque<>();
queue.offer(1);       // 入队
queue.poll();         // 出队
queue.peek();         // 看队头
```

### 3.2 ArrayDeque 的循环数组

ArrayDeque 内部是**循环数组**（环形缓冲区），不是环形链表：

```
  head↓              tail↓
┌───┬───┬───┬───┬───┬───┬───┬───┐
│   │ b │ c │ d │ e │   │   │   │
└───┴───┴───┴───┴───┴───┴───┴───┘
  0   1   2   3   4   5   6   7
       ←────── 有效元素 ──────→

队尾插入 e 后 tail=5；若 tail 到 8 则回绕到 0（取模）
判空：head == tail
判满：(tail + 1) % n == head（故意浪费一格区分空/满）
扩容：满时 2 倍扩容并重新排布
```

**关键**：`push`/`pop`/`offer`/`poll` 全是 **O(1)**（均摊，扩容时 O(n)）。

---

## 四、项目现场一：有界队列（最近 N 条审计）

```java
// ai-cs-chat/.../security/SecurityAuditRecorder.java
private static final int MAX_RECENT = 200;
private final Deque<SecurityEventDTO> recent = new ArrayDeque<>();

public void record(...) {
    // ... 构造 dto（脱敏 + 截断）
    synchronized (recent) {
        recent.addLast(dto);                      // 新事件放队尾
        while (recent.size() > MAX_RECENT) {
            recent.removeFirst();                 // 超容量则从队头丢弃最旧的
        }
    }
    // ... 落库（尽力而为，失败只告警）
}
```

**算法解读**：这是一个典型的 **有界队列 / 滑动保留窗口**。

| 操作 | 复杂度 |
|---|---|
| `addLast` | 均摊 O(1) |
| `removeFirst` | O(1) |
| `size()` | O(1)（内部维护计数器） |
| 单次 record | **O(1)**（最多移除 1 个） |

**为什么用 `while` 而不是 `if`**：防御性编程——若 MAX_RECENT 被改小或历史数据超量，while 能收敛到阈值；if 只会删一个。

**为什么用 ArrayDeque 而非 LinkedList**：审计只做"尾部追加 + 头部丢弃"，正是 Deque 的典型场景；ArrayDeque 无节点对象开销、缓存局部性好。

**并发处理**：`ArrayDeque` **不是线程安全的**，这里用 `synchronized (recent)` 包住复合操作。
对比网关的 `ConcurrentLinkedDeque`（见 [10-限流算法](./10-限流算法-四大算法与网关实现.md)）——后者是并发容器但 `size()` 是 O(n) 遍历，所以限流场景改用 `size()` 前需谨慎。

> **面试点**：`ConcurrentLinkedDeque.size()` 是 **O(n)**（需遍历计数，因为并发下无法维护准确计数）。
> 频繁调用会有性能问题。这也是很多限流实现改用"固定窗口计数器"而非"链表窗口"的原因之一。

---

## 五、项目现场二：延迟队列与订单超时

```java
// ai-cs-order/.../service/impl/OrderServiceImpl.java
// 下单后发送延迟消息：delayLevel 16 ≈ 30 分钟
Message<Long> message = MessageBuilder.withPayload(orderId).build();
rocketMQTemplate.syncSend(TOPIC, message, 5000, 16);   // timeout=5000ms, delayLevel=16

// ai-cs-order/.../task/OrderTimeoutScheduler.java
@Scheduled(fixedDelay = 300000, initialDelay = 60000)   // 每 5 分钟扫一次
public void scanTimeoutOrders() { ... }
```

**两种延迟任务实现的对比**：

| 方案 | 数据结构 | 精度 | 复杂度 | 适用 |
|---|---|---|---|---|
| **RocketMQ 延迟消息** | Broker 内部按 delayLevel 分队列定时扫描 | 受限于预设 level（18 个固定档位） | 投递 O(1) | 固定时长的超时（30 分钟关单） |
| **定时扫表** | SQL `WHERE status=0 AND created < now-30min` | 取决于扫描周期（5 分钟） | 每次 O(n) 扫索引 | 兜底补偿、批量修复 |
| **JDK DelayQueue** | `PriorityQueue`（堆，按到期时间排序） | 精确 | 入队 O(log n)，取到期 O(log n) | 单机、内存级、精度要求高 |
| **时间轮 HashedWheelTimer** | 环形数组 + 每格链表 | 取决于 tickDuration | **O(1)** 入队 | 海量定时任务（Netty、Kafka、Dubbo 用） |

### 5.1 时间轮原理（进阶）

```
时间轮（tickDuration = 1s，wheelSize = 8）：

        ┌───┬───┬───┬───┬───┬───┬───┬───┐
slot:   │ 0 │ 1 │ 2 │ 3 │ 4 │ 5 │ 6 │ 7 │
        └─┬─┴───┴─┬─┴───┴───┴───┴───┴───┘
          │       │
          ▼       ▼
       [taskA]  [taskC]→[taskD]      ← 每格挂一个双向链表

指针每秒前进一格，处理当前格链表上的所有任务。
任务延迟 10s → 放在 (当前 + 10) % 8 = 2 号格，并记录圈数 rounds=1
         → 指针第一轮经过 slot 2 时 rounds-- ≠ 0，不执行
         → 第二轮经过时才执行
```

**优势**：插入 O(1)（算个下标挂链表），无需像 `PriorityQueue` 那样 O(log n) 上浮调整。
代价：精度受 tickDuration 限制；延迟特别大时需要多层时间轮（像时钟的时分秒）。

> 本项目用 RocketMQ 延迟消息 + 定时扫表兜底，是**分布式场景下的标准做法**：
> 消息保证及时性，扫表保证最终一致（消息丢失时兜底）。这比单机 DelayQueue 更适合微服务。

---

## 六、单调栈（高频算法技巧）

**适用场景**：找"左边/右边第一个比它大/小的元素"，O(n) 解决。

**例题：每日温度**（LeetCode 739）—— 求每个温度之后第几天会升温。

```java
public int[] dailyTemperatures(int[] temperatures) {
    int n = temperatures.length;
    int[] ans = new int[n];
    Deque<Integer> stack = new ArrayDeque<>();   // 存下标，保持栈内温度单调递减

    for (int i = 0; i < n; i++) {
        // 当前温度 > 栈顶温度 → 栈顶找到了"右边第一个更高"，弹栈结算
        while (!stack.isEmpty() && temperatures[i] > temperatures[stack.peek()]) {
            int idx = stack.pop();
            ans[idx] = i - idx;                  // 相隔天数
        }
        stack.push(i);
    }
    return ans;   // 没被结算的位置默认 0（后面没有更高的）
}
```

**为什么是 O(n)**：每个元素**最多入栈一次、出栈一次**，总操作 2n → O(n)。

**核心套路**：
- 找"右边第一个更大" → 维护**单调递减**栈，遇到更大的就弹栈结算
- 找"右边第一个更小" → 维护**单调递增**栈，遇到更小的就弹栈结算
- 栈里存**下标**（既能取值又能算距离），不存值

同类题：84（柱状图最大矩形）、42（接雨水）、496（下一个更大元素）。

---

## 七、滑动窗口（双指针的进阶）

**适用场景**：连续子数组/子串的最值、计数、覆盖问题，把 O(n²) → O(n)。

```java
// 模板：无重复字符的最长子串（LeetCode 3）
public int lengthOfLongestSubstring(String s) {
    Map<Character, Integer> window = new HashMap<>();
    int left = 0, max = 0;
    for (int right = 0; right < s.length(); right++) {
        char c = s.charAt(right);
        window.put(c, window.getOrDefault(c, 0) + 1);       // 扩大右边界

        while (window.get(c) > 1) {                          // 出现重复，收缩左边界
            char d = s.charAt(left++);
            window.put(d, window.get(d) - 1);
        }
        max = Math.max(max, right - left + 1);               // 更新答案
    }
    return max;
}
```

**通用模板**：

```java
int left = 0;
for (int right = 0; right < n; right++) {
    // 1. 扩大窗口：把 right 纳入
    window.add(arr[right]);
    // 2. 收缩窗口：while 窗口不满足条件
    while (invalid(window)) {
        window.remove(arr[left++]);
    }
    // 3. 更新答案（此时窗口一定合法）
    ans = update(ans, window);
}
```

**复杂度**：`left` 和 `right` 各移动 n 次 → **O(n)**，空间 O(k)（窗口内元素种类数）。

---

## 八、项目现场三：网关的滑动窗口限流

```java
// ai-cs-gateway/.../filter/SlidingWindowRateLimiter.java
private final Map<String, Deque<Long>> windows = new ConcurrentHashMap<>();

public boolean tryAcquire(String key, int maxRequests, int windowSeconds) {
    long now = System.currentTimeMillis();
    long windowStart = now - windowSeconds * 1000L;
    Deque<Long> timestamps = windows.computeIfAbsent(key, k -> new ConcurrentLinkedDeque<>());
    synchronized (timestamps) {
        while (!timestamps.isEmpty() && timestamps.peekFirst() < windowStart) {
            timestamps.removeFirst();          // 惰性清理过期时间戳
        }
        if (timestamps.size() >= maxRequests) return false;   // 超限，拒绝
        timestamps.addLast(now);               // 记录本次
        return true;
    }
}
```

**数据结构视角**：这就是一个**滑动窗口双指针**的变体——`peekFirst` 是左指针（过期淘汰），`addLast` 是右指针（新请求入窗）。

**复杂度**：
- 均摊 O(1)（每个时间戳入队一次、出队一次）
- 空间 O(maxRequests)（窗口内最多保留 maxRequests 个时间戳）
- ⚠️ `ConcurrentLinkedDeque.size()` 是 **O(n)** 遍历！窗口满时每次都要 O(n) 计数

> 优化方向：改用 `AtomicInteger` 维护计数，或换用固定窗口计数器（O(1) size）。
> 详细对比见 [10-限流算法](./10-限流算法-四大算法与网关实现.md)。

---

## 九、面试高频问答

**Q1：ArrayList 和 LinkedList 怎么选？**
A：绝大多数用 ArrayList。优势：随机访问 O(1)、缓存局部性好、内存更省。LinkedList 只在"已持有节点引用做 O(1) 插入删除"时占优（如手写 LRU 的双向链表），实际业务极少。遍历场景下 ArrayList 通常快数倍。

**Q2：为什么 Java 不推荐用 Stack 类？**
A：`Stack` 继承 `Vector`，方法被 `synchronized` 修饰，单线程下白白付出加锁开销；且它暴露了 `Vector` 的 `get(i)`/`add(i,e)` 等破坏栈语义的方法（设计缺陷）。用 `ArrayDeque` 替代。

**Q3：ArrayDeque 为什么不能存 null？**
A：`null` 被用作 `poll()`/`peek()` 的"队列为空"返回值哨兵。如果允许存 null，调用者无法区分"取到了 null 元素"和"队列空"。`LinkedList` 允许 null 是因为它用节点是否存在判断，场景不同。

**Q4：如何实现一个 O(1) 的队列和栈？**
A：`ArrayDeque`（循环数组）。栈和队列都是均摊 O(1)。

**Q5：ArrayDeque 的 size() 是 O(1) 吗？ConcurrentLinkedDeque 的呢？**
A：ArrayDeque 是 O(1)（内部维护计数）；`ConcurrentLinkedDeque.size()` 是 **O(n)**（并发下无法原子维护计数，需遍历）。高频调用需注意——本项目网关限流就踩在这个点上。

**Q6：单调栈为什么能 O(n)？**
A：每个元素最多入栈一次、出栈一次，虽然代码是两层循环，但内层 while 的总执行次数 ≤ n，所以是 O(n) 而非 O(n²)。

---

## 十、动手练习

1. 用 `ArrayDeque` 实现一个**最小栈**（LeetCode 155）：O(1) 时间拿到栈中最小值。（提示：双栈，辅助栈存当前最小值）
2. 用**双栈实现队列**（LeetCode 232）：`push` 入栈 A，`pop` 时若栈 B 空则把 A 全部倒入 B。（均摊 O(1)）
3. 改进 `SlidingWindowRateLimiter`：用 `AtomicInteger` 计数替代 `size()`，把 O(n) 降为 O(1)。
4. 分析：本项目 `SecurityAuditRecorder` 若把 `MAX_RECENT` 从 200 改成 10 万，会有问题吗？（提示：内存占用 + `recentEvents()` 返回全量快照的 O(n) 复制开销）

---

> 上一篇：[01-复杂度分析与算法思维](./01-复杂度分析与算法思维.md) ｜ 下一篇：[03-哈希：冲突、扰动与分片路由](./03-哈希-冲突扰动与分片路由.md)
