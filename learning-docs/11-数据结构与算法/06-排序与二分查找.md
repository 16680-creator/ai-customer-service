# 排序与二分查找

> 对应项目：`ai-cs-pay/.../channel/AlipayChannel.java`（`TreeMap` 字典序排序后签名）、
> `ai-cs-chat/.../cache/SemanticCacheService.java`（候选集按相似度排序取 Top-1）、
> `ai-cs-product/.../ProductRecommendServiceImpl.java`（推荐结果排序）。

---

## 一、十大排序总览

| 算法 | 最好 | 平均 | 最坏 | 空间 | 稳定 | 核心思想 |
|---|---|---|---|---|---|---|
| 冒泡 | O(n) | O(n²) | O(n²) | O(1) | ✅ | 相邻比较交换 |
| 选择 | O(n²) | O(n²) | O(n²) | O(1) | ❌ | 每轮选最小放前面 |
| 插入 | O(n) | O(n²) | O(n²) | O(1) | ✅ | 逐个插入已排区间 |
| 希尔 | O(n log n) | O(n^1.3) | O(n²) | O(1) | ❌ | 分组插入排序 |
| **快排** | O(n log n) | **O(n log n)** | **O(n²)** | O(log n) | ❌ | 分区 + 分治 |
| **归并** | O(n log n) | **O(n log n)** | O(n log n) | **O(n)** | ✅ | 分治 + 合并 |
| **堆排** | O(n log n) | **O(n log n)** | O(n log n) | **O(1)** | ❌ | 建堆 + 反复取极值 |
| 计数 | O(n+k) | O(n+k) | O(n+k) | O(k) | ✅ | 统计频次 |
| 桶 | O(n+k) | O(n+k) | O(n²) | O(n+k) | ✅ | 分桶 + 桶内排序 |
| 基数 | O(d·(n+k)) | O(d·(n+k)) | O(d·(n+k)) | O(n+k) | ✅ | 按位排序（d=位数） |

**三个重点**：快排（最常用）、归并（稳定 + 外排序）、堆排（原地 + TopK）。

---

## 二、快速排序（最重要）

### 2.1 核心思想：分区（Partition）

```
数组: [3, 1, 4, 1, 5, 9, 2, 6]
选 pivot = 3（最右或随机）

分区后: [1, 1, 2] 3 [5, 9, 4, 6]
        ↑ 都 ≤3 ↑  ↑ 都 >3 ↑

pivot 已在最终位置（第 4 名），只需递归排左右两边
```

**关键洞察**：一次分区 O(n)，且能确定 pivot 的最终位置，无需再动它。

### 2.2 双指针分区实现

```java
public void quickSort(int[] arr, int left, int right) {
    if (left >= right) return;
    int p = partition(arr, left, right);   // O(n) 分区，返回 pivot 最终位置
    quickSort(arr, left, p - 1);           // 递归左半
    quickSort(arr, p + 1, right);          // 递归右半
}

private int partition(int[] arr, int left, int right) {
    // 随机选 pivot 并与最右交换：避免有序数组退化到 O(n²)
    int rand = left + (int) (Math.random() * (right - left + 1));
    swap(arr, rand, right);
    int pivot = arr[right];

    int i = left;                          // i 指向"≤ pivot 区间"的下一个位置
    for (int j = left; j < right; j++) {
        if (arr[j] <= pivot) {
            swap(arr, i, j);
            i++;                           // 把小的换到左边
        }
    }
    swap(arr, i, right);                   // pivot 归位到 i
    return i;
}
```

### 2.3 为什么平均 O(n log n)，最坏 O(n²)？

**平均情况**：每次分区大致对半 → 递归树高 log n，每层总工作量 O(n) → **O(n log n)**。

```
层 0:        n                  总 O(n)
层 1:    n/2   n/2              总 O(n)
层 2:  n/4 ×4                   总 O(n)
...
层 log n                        总 O(n)
→ O(n log n)
```

**最坏情况**：每次分区极不均匀（如已排序数组 + 取最右为 pivot）→ 递归树退化成链，树高 n → **O(n²)**。

```
已排序 [1,2,3,4,5]，pivot 取最右 5
分区后: [1,2,3,4] 5 []     ← 左 n-1，右 0
再分区: [1,2,3] 4 [] 5
...
树高 = n → O(n²)
```

**三种规避手段**：
1. **随机选 pivot**（上面代码）：概率上避免最坏，工程最常用
2. **三数取中**：取 left/mid/right 的中位数作 pivot
3. **小区间用插入排序**：n < 47 时插入排序更快（常数小），JDK 的 `DualPivotQuickSort` 就这么做

### 2.4 快排为什么不稳定？

```
[3a, 2, 3b]   （3a 和 3b 值相等，a 在前）
pivot = 3b（最右）

分区过程：j=0 时 arr[0]=3a <= 3 → swap(arr[0], arr[0])，i=1
         j=1 时 arr[1]=2 <= 3  → swap(arr[1], arr[1])，i=2
swap(arr[2], arr[2])  → pivot 归位

结果 [3a, 2, 3b]，似乎稳定？再看：
[3a, 3b, 2]  pivot=2
         j=0: 3a <= 2? 否
         j=1: 3b <= 2? 否
swap(arr[0], arr[2]) → [2, 3b, 3a]   ← 3a 和 3b 的相对顺序反了！
```

**本质**：远距离交换（swap）会跨越相等元素，破坏稳定性。

### 2.5 Java 的实际选择

| 场景 | JDK 实现 | 稳定性 |
|---|---|---|
| `Arrays.sort(int[])` 等基本类型 | **双轴快排**（Dual-Pivot QuickSort） | ❌ 不稳定（基本类型无所谓） |
| `Arrays.sort(Object[])` / `Collections.sort` | **TimSort**（归并 + 插入混合） | ✅ 稳定 |
| `Arrays.parallelSort` | 并行归并 | ✅ 稳定 |

**为什么基本类型用快排、对象用 TimSort？**
- 基本类型：稳定性无意义（两个 5 无法区分），快排更快、更省空间
- 对象：稳定性重要（可能按年龄排序后，同年龄的保持原姓名顺序），且 TimSort 对**部分有序**数据表现极好（O(n)）

---

## 三、归并排序（稳定 + 可外排序）

```java
public void mergeSort(int[] arr, int left, int right, int[] temp) {
    if (left >= right) return;
    int mid = left + (right - left) / 2;
    mergeSort(arr, left, mid, temp);        // 排左半
    mergeSort(arr, mid + 1, right, temp);   // 排右半
    merge(arr, left, mid, right, temp);     // 合并两个有序区间
}

private void merge(int[] arr, int left, int mid, int right, int[] temp) {
    int i = left, j = mid + 1, k = left;
    while (i <= mid && j <= right) {
        // ⭐ 关键：相等时取左边的（<=），保证稳定性
        if (arr[i] <= arr[j]) temp[k++] = arr[i++];
        else                  temp[k++] = arr[j++];
    }
    while (i <= mid)  temp[k++] = arr[i++];   // 左半剩余
    while (j <= right) temp[k++] = arr[j++];  // 右半剩余
    System.arraycopy(temp, left, arr, left, right - left + 1);
}
```

**复杂度**：严格 O(n log n)（最好/平均/最坏都一样），空间 O(n)。

**为什么稳定**：合并时相等元素取左边的（`arr[i] <= arr[j]`），且合并是顺序写入，不会跨越交换。

**优势场景**：
1. **需要稳定排序**（对象排序）
2. **外排序**：数据量超过内存，可分段排序后归并（MySQL 的 `filesort`、大文件排序）
3. **链表排序**：归并是链表排序的最优解（O(1) 额外空间，只需改指针；快排在链表上性能差）

**劣势**：O(n) 额外空间，且常数比快排大。

---

## 四、堆排序（原地 + 可流式取 TopK）

```java
public void heapSort(int[] arr) {
    int n = arr.length;
    // 1. 建大顶堆：O(n)
    for (int i = (n >> 1) - 1; i >= 0; i--) siftDown(arr, i, n);

    // 2. 反复把堆顶（最大值）换到末尾，缩小堆范围
    for (int i = n - 1; i > 0; i--) {
        swap(arr, 0, i);        // 堆顶（当前最大）放到位置 i
        siftDown(arr, 0, i);    // 对前 i 个重新调整为大顶堆
    }
}
// 结果：升序（因为每次把最大的放到末尾）
```

**复杂度**：建堆 O(n) + n 次 siftDown O(log n) = **O(n log n)**，空间 **O(1)**。

**建大顶堆 → 得到升序**：这是个易错点。大顶堆堆顶最大，依次放到末尾 → 数组从小到大。
（若要降序，建小顶堆。）

**堆排 vs 快排**：

| 维度 | 堆排 | 快排 |
|---|---|---|
| 最坏 | **O(n log n)** ✅ | O(n²) ❌ |
| 空间 | **O(1)** ✅ | O(log n)（递归栈） |
| 常数 | 大（跳着访问，缓存不友好）❌ | 小（顺序访问）✅ |
| 稳定性 | ❌ | ❌ |
| 实际速度 | 比快排慢 2-3 倍 | 更快 |

**为什么堆排缓存不友好**：`siftDown` 时访问 `arr[i]`、`arr[2i+1]`、`arr[2i+2]`，下标跳跃大，几乎每次都是 Cache Miss。快排是顺序扫描，Cache 命中率高。

**堆排的独特价值**：O(1) 空间 + 最坏 O(n log n)，适合**内存极度受限**、**不能接受最坏退化**的场景（如实时系统）。
另外，**只取前 K 个时无需排完整数组**（见 [04-TopK](./04-树堆与TopK.md)）。

---

## 五、线性时间排序（计数 / 桶 / 基数）

### 5.1 计数排序

**条件**：数据范围 k 不大（如年龄 0-150、考试分数 0-100）。

```java
// 成绩排序：分数 0-100
public int[] countingSort(int[] scores) {
    int k = 101;
    int[] count = new int[k];
    for (int s : scores) count[s]++;              // O(n) 统计频次

    int[] result = new int[scores.length];
    int idx = 0;
    for (int v = 0; v < k; v++) {                 // O(k) 按值展开
        while (count[v]-- > 0) result[idx++] = v;
    }
    return result;
}
```

**复杂度 O(n + k)** —— 当 k = O(n) 时是**线性**，快于任何比较排序的 Ω(n log n) 下界。

> **理论下限**：基于比较的排序（快排/归并/堆排）最坏不可能优于 **O(n log n)**。
> 证明：n 个元素有 n! 种排列，比较排序的决策树是二叉树，需要 log₂(n!) ≈ n log n 次比较才能区分所有排列。
> 计数/桶/基数排序**不基于比较**，可以突破这个下界。

### 5.2 桶排序

**思路**：把数据分到 m 个桶，桶内排序（用插入排序），再按桶顺序拼接。

**适用**：数据**均匀分布**（如 0-1 之间的浮点数）。

**复杂度**：平均 O(n + m)，最坏 O(n²)（全落一个桶）。

### 5.3 基数排序

**思路**：按位排序（从低位到高位），每位用计数排序。要求**每位稳定**。

```
[170, 45, 75, 90, 802, 24, 2, 66]

按个位: [170, 90, 802, 2, 24, 45, 75, 66]
按十位: [802, 2, 24, 45, 66, 170, 75, 90]
按百位: [2, 24, 45, 66, 75, 90, 170, 802]  ← 有序
```

**复杂度 O(d × (n + k))**，d = 最大位数，k = 每位取值范围（十进制 k=10）。

---

## 六、排序稳定性：什么时候重要？

**定义**：相等元素在排序前后的**相对顺序不变**。

**为什么重要**：多字段排序。

```
按"年龄"升序排一组学生（原本按姓名字母序）：

稳定排序结果：
  18岁: Alice, Bob        ← 保持了字母序
  20岁: Carol, David

不稳定排序结果：
  18岁: Bob, Alice        ← 字母序被打乱
  20岁: David, Carol
```

**工程解法**：
1. 用稳定排序（TimSort、归并）
2. 或一次比较多个字段：`Comparator.comparing(Student::getAge).thenComparing(Student::getName)`

**Java 各排序的稳定性**：

| 方法 | 稳定 |
|---|---|
| `Arrays.sort(Object[])` / `List.sort` | ✅ TimSort |
| `Collections.sort` | ✅ |
| `Stream.sorted()` | ✅ |
| `Arrays.sort(int[])` | ❌（基本类型，无意义） |
| `PriorityQueue` 排序 | ❌（堆不稳定） |

---

## 七、二分查找（看似简单，实则全是坑）

### 7.1 核心思想

**前提**：数组**有序**（且支持随机访问）。每次排除一半。

**复杂度**：O(log n)。n = 10 亿只需 30 次比较。

### 7.2 三个易错点（必背）

| 坑 | 说明 | 正确做法 |
|---|---|---|
| **① 溢出** | `mid = (left + right) / 2` 中 left+right 可能溢出 int | `mid = left + (right - left) / 2` |
| **② 死循环** | 区间缩小时没有真正缩小 | 明确区间定义，更新时 ±1 |
| **③ 边界** | 返回值是 left 还是 right？ | 定义清楚"查找什么"，统一模板 |

### 7.3 标准模板（推荐：左闭右闭 `[left, right]`）

```java
// 精确查找：找到返回下标，找不到返回 -1
public int binarySearch(int[] arr, int target) {
    int left = 0, right = arr.length - 1;      // 闭区间 [0, n-1]
    while (left <= right) {                     // ⭐ 注意是 <=（闭区间，left==right 时仍有意义）
        int mid = left + (right - left) / 2;    // ⭐ 防溢出
        if (arr[mid] == target) return mid;
        else if (arr[mid] < target) left = mid + 1;   // ⭐ mid 已排除，±1
        else                       right = mid - 1;
    }
    return -1;
}
```

### 7.4 边界查找（找第一个 / 最后一个）

**这是面试真正的难点**。核心：**找到目标后不立即返回，而是继续收缩边界**。

```java
// 找第一个 ≥ target 的位置（lower_bound）
public int lowerBound(int[] arr, int target) {
    int left = 0, right = arr.length;      // ⭐ 右开区间 [left, right)
    while (left < right) {                  // ⭐ 开区间用 <
        int mid = left + (right - left) / 2;
        if (arr[mid] < target) {
            left = mid + 1;                 // 小于目标，答案在右边
        } else {
            right = mid;                    // ⭐ 等于也不返回，继续向左收缩
        }
    }
    return left;   // left == right，可能是 arr.length（target 比所有元素都大）
}

// 找第一个 > target 的位置（upper_bound）
public int upperBound(int[] arr, int target) {
    int left = 0, right = arr.length;
    while (left < right) {
        int mid = left + (right - left) / 2;
        if (arr[mid] <= target) {           // ⭐ 区别只在 <=
            left = mid + 1;
        } else {
            right = mid;
        }
    }
    return left;
}
```

**用 lower_bound / upper_bound 解决所有边界问题**：

| 需求 | 表达式 |
|---|---|
| 第一个 ≥ x 的位置 | `lowerBound(arr, x)` |
| 第一个 > x 的位置 | `upperBound(arr, x)` |
| 最后一个 < x 的位置 | `lowerBound(arr, x) - 1` |
| 最后一个 ≤ x 的位置 | `upperBound(arr, x) - 1` |
| x 出现的次数 | `upperBound(arr, x) - lowerBound(arr, x)` |
| x 是否存在 | `lowerBound(arr, x) < n && arr[lowerBound(arr, x)] == x` |

### 7.5 二分答案（进阶，工程也有用）

**思路**：不二分数组下标，而是二分**答案的值域**，用 `check(ans)` 判断可行性。

**例题**：在 D 天内运完所有货物，船的最小载重是多少？

```java
public int shipWithinDays(int[] weights, int days) {
    // 答案值域：最小 = 最重货物（否则装不下），最大 = 全部货物总重（一次运完）
    int left = 0, right = 0;
    for (int w : weights) {
        left = Math.max(left, w);
        right += w;
    }

    while (left < right) {
        int mid = left + (right - left) / 2;
        if (canShip(weights, days, mid)) {
            right = mid;          // 能装下，尝试更小的载重
        } else {
            left = mid + 1;       // 装不下，必须加大
        }
    }
    return left;
}

private boolean canShip(int[] weights, int days, int capacity) {
    int used = 1, cur = 0;
    for (int w : weights) {
        if (cur + w > capacity) { used++; cur = 0; }
        cur += w;
    }
    return used <= days;
}
```

**识别二分答案的特征**：问题问"**最小的最大值**"或"**最大的最小值**"，且可行性关于答案是单调的。

---

## 八、项目现场

### 8.1 支付宝签名：TreeMap 字典序排序

```java
// ai-cs-pay/.../channel/AlipayChannel.java
// 支付宝要求参数按 key 的字典序排序后拼接签名
Map<String, String> params = new TreeMap<>();   // ⭐ TreeMap 自动按 key 排序
params.put("app_id", appId);
params.put("method", "alipay.trade.page.pay");
params.put("charset", "utf-8");
params.put("timestamp", timestamp);
// ...

// 拼接 key=value& 形式（已按字典序）
StringBuilder sb = new StringBuilder();
for (Map.Entry<String, String> e : params.entrySet()) {
    sb.append(e.getKey()).append("=").append(e.getValue()).append("&");
}
String signContent = sb.substring(0, sb.length() - 1);
String sign = rsaSign(signContent, privateKey);
```

**为什么必须排序**：签名是"对固定字符串做哈希/加密"。若参数顺序不固定，同样的参数会产生不同的待签字符串，服务端验签就会失败。**排序是为了让待签字符串唯一确定**。

**为什么用 TreeMap**：红黑树实现，插入即有序（O(log n)），遍历即得字典序。
（同样场景也见于微信支付 V3、OAuth 签名等。）

**注意**：字典序是**字符串的字典序**（按字符编码比较），不是"插入顺序"，也不是"长度排序"。

### 8.2 相似度排序取 Top-1

```java
// ai-cs-chat/.../cache/SemanticCacheService.java（简化）
// 候选集按余弦相似度降序，取最高且超过阈值的
for (Map.Entry<String, String> e : candidates.entrySet()) {
    double sim = cosine(queryVec, parse(e.getValue()));
    if (sim >= threshold) {
        best = max(best, sim);   // 只需维护最大值，无需排序 → O(n)
    }
}
```

**优化点**：这里只需要**最大值**，用一次遍历 O(n) 即可，不必排序 O(n log n)。
若需要 Top-K 才用堆（O(n log k)）——见 [04-TopK](./04-树堆与TopK.md)。

**常见过度优化**："先排序再取第一个"是新手常犯的浪费，O(n log n) 做 O(n) 的事。

---

## 九、面试高频问答

**Q1：快排最坏什么时候退化到 O(n²)？如何避免？**
A：数组已排序（或逆序）且固定取最左/最右为 pivot 时，每次分区极不均匀。避免：随机选 pivot、三数取中、小区间改用插入排序。JDK 的双轴快排就是这套组合拳。

**Q2：为什么快排比堆排快，但堆排有人用？**
A：快排顺序访问数组，缓存命中率高，常数小；堆排 siftDown 下标跳跃（i → 2i+1），Cache Miss 频繁，常数大约 2-3 倍。但堆排最坏也是 O(n log n) 且 O(1) 空间，适合内存受限、不能接受最坏退化的场景（实时系统、嵌入式）。

**Q3：归并排序的空间能降到 O(1) 吗？**
A：数组版本不能（合并需要辅助空间），但**链表版本可以** O(1)（只需改指针，无需搬移元素）。这也是链表排序首选归并的原因。

**Q4：基于比较的排序最快能到多少？为什么？**
A：Ω(n log n)。n 个元素有 n! 种排列，比较排序的决策树是二叉树，叶子数 ≥ n!，树高 ≥ log₂(n!) ≈ n log n。计数/桶/基数排序不基于比较，可突破到 O(n)。

**Q5：什么是排序稳定性？Java 里哪些排序稳定？**
A：相等元素排序前后相对顺序不变。`Arrays.sort(Object[])`、`Collections.sort`、`Stream.sorted()` 用 TimSort，稳定；`Arrays.sort(基本类型)` 用双轴快排，不稳定（但基本类型无需稳定性）。

**Q6：mid 为什么要写成 left + (right-left)/2？**
A：防 `left + right` 溢出 int（当 left、right 都接近 2^31-1 时）。虽然实际数组没这么大，但这是代码习惯问题，面试必答。

**Q7：二分查找的数组必须有序吗？链表能二分吗？**
A：必须有序。链表**不适合**二分——无法 O(1) 随机访问 mid，找 mid 就要 O(n)，整体退化成 O(n log n)，不如线性查找 O(n)。但**跳表**通过多级索引让链表支持 O(log n) 查找（见 [04](./04-树堆与TopK.md) §4.5）。

**Q8：什么是二分答案？什么时候用？**
A：不二分下标，而是二分答案的值域，用 `check(x)` 判断 x 是否可行。适用于问"最小的最大值 / 最大的最小值"且可行性单调的问题（如"最小船载重"、"最快完成时间"）。

---

## 十、动手练习

1. 实现 `lowerBound` / `upperBound`，并用它们求"有序数组中 x 出现的次数"（要求 O(log n)）。
2. 给定本项目"算法现场依赖表"，用拓扑排序（[05](./05-图论与搜索-BFS-DFS-拓扑排序.md)）给出学习顺序。
3. 分析：对 100 万个 **部分有序** 的数组排序，`Arrays.sort(Object[])`（TimSort）和手写快排哪个快？为什么？（提示：TimSort 对已有序的 run 只需 O(n) 识别）
4. 把 §8.2 的"取最大相似度"改成"取 Top-3 相似结果"，应该用堆还是排序？写出复杂度。

---

> 上一篇：[05-图论与搜索](./05-图论与搜索-BFS-DFS-拓扑排序.md) ｜ 下一篇：[07-字符串匹配：Trie 与 PII 脱敏](./07-字符串匹配-Trie与PII脱敏.md)
