# 动态规划与贪心

> 对应项目：`ai-cs-order`（订单金额分摊、优惠券最优组合、退款金额计算）、
> `ai-cs-chat/.../agent`（Agent 工具调用路径规划）、
> `ai-cs-product/.../ProductRecommendServiceImpl.java`（推荐结果排序的组合优化）。

---

## 一、动态规划（DP）：解决重复子问题

### 1.1 三个必要特征（缺一不可）

| 特征 | 含义 | 反例（不适合 DP） |
|---|---|---|
| **重叠子问题** | 递归时会反复计算相同的子问题 | 归并排序（子问题不重叠，是分治不是 DP） |
| **最优子结构** | 问题的最优解由子问题的最优解构成 | 最长路径问题（有环，子问题相互影响） |
| **无后效性** | 当前状态确定后，后续决策不受之前路径影响 | 带状态的游戏（如棋类，历史影响未来） |

**DP 与分治的区别**：
- 分治：子问题**不重叠**（归并排序左右两半各排各的）→ 直接递归
- DP：子问题**大量重叠**（斐波那契 `fib(5)` 依赖 `fib(4)` 和 `fib(3)`，而 `fib(4)` 又依赖 `fib(3)`）→ 必须缓存

### 1.2 解题四步法（通用模板）

```
① 定义状态：dp[i] 或 dp[i][j] 表示什么？（最关键的一步）
② 状态转移：dp[i] 如何由 dp[i-1]、dp[i-2]... 推出？（递推方程）
③ 初始化：dp[0]、dp[1] 等边界值是多少？
④ 返回值：最终答案是 dp[n] 还是 max(dp)？
```

> **口诀**：先想"状态是什么"，再想"怎么转移"，最后处理边界。
> 新手最容易卡在第①步——状态定义错了，后面全错。

---

## 二、入门：从斐波那契看 DP 的三种写法

### 2.1 暴力递归 O(2ⁿ)

```java
int fib(int n) {
    if (n <= 1) return n;
    return fib(n - 1) + fib(n - 2);
}
```

```
                 fib(5)
              /         \
        fib(4)           fib(3)
       /     \          /     \
   fib(3)  fib(2)    fib(2)  fib(1)
   /   \    /   \    /   \
fib(2) fib(1) ...  ← fib(3)、fib(2) 被重复计算了多次！

时间复杂度 O(2ⁿ)，n=50 就要算约 10^15 次
```

### 2.2 记忆化搜索（自顶向下）O(n)

```java
int fib(int n, int[] memo) {
    if (n <= 1) return n;
    if (memo[n] != 0) return memo[n];        // 已计算过，直接返回
    return memo[n] = fib(n - 1, memo) + fib(n - 2, memo);
}
```

**思路**：递归 + 缓存，把重复子问题的结果存起来。

### 2.3 动态规划（自底向上）O(n)

```java
int fib(int n) {
    if (n <= 1) return n;
    int[] dp = new int[n + 1];
    dp[0] = 0;
    dp[1] = 1;
    for (int i = 2; i <= n; i++) {
        dp[i] = dp[i - 1] + dp[i - 2];       // 状态转移方程
    }
    return dp[n];
}
```

### 2.4 滚动数组优化空间 O(1)

```java
int fib(int n) {
    if (n <= 1) return n;
    int prev = 0, curr = 1;
    for (int i = 2; i <= n; i++) {
        int next = prev + curr;
        prev = curr;                 // 只保留最近两个状态
        curr = next;
    }
    return curr;
}
```

**为什么可行**：`dp[i]` 只依赖 `dp[i-1]` 和 `dp[i-2]`，更老的永远用不到 → 只需两个变量。

**这是 DP 空间优化的核心技巧**：观察转移方程依赖哪些历史状态，只保留必要的。

| 写法 | 时间 | 空间 | 特点 |
|---|---|---|---|
| 暴力递归 | O(2ⁿ) | O(n) 栈 | 不可用 |
| 记忆化搜索 | O(n) | O(n) + 栈 | 自顶向下，只算需要的状态，代码接近递归思路 |
| DP 数组 | O(n) | O(n) | 自底向上，无递归栈 |
| 滚动数组 | O(n) | **O(1)** | ⭐ 最优 |

---

## 三、经典 DP 一：0/1 背包（最重要）

**问题**：n 个物品，重量 `w[i]`、价值 `v[i]`，背包容量 W，每个物品**最多选一次**，求最大价值。

### 3.1 状态定义

`dp[i][c]` = 只考虑**前 i 个**物品、背包容量为 `c` 时能获得的最大价值。

### 3.2 转移方程

```java
// 对第 i 个物品，两种选择：
// ① 不选：dp[i][c] = dp[i-1][c]
// ② 选（前提是 c >= w[i]）：dp[i][c] = dp[i-1][c - w[i]] + v[i]
// 取两者最大
dp[i][c] = max(dp[i-1][c], dp[i-1][c - w[i]] + v[i]);
```

### 3.3 二维实现

```java
public int knapsack(int[] w, int[] v, int W) {
    int n = w.length;
    int[][] dp = new int[n + 1][W + 1];

    for (int i = 1; i <= n; i++) {
        for (int c = 0; c <= W; c++) {
            dp[i][c] = dp[i - 1][c];                       // 不选第 i 个
            if (c >= w[i - 1]) {
                dp[i][c] = Math.max(dp[i][c],
                        dp[i - 1][c - w[i - 1]] + v[i - 1]);  // 选第 i 个
            }
        }
    }
    return dp[n][W];
}
```

**复杂度**：O(n × W)，空间 O(n × W)。

### 3.4 一维滚动数组优化（必背）

**核心**：`dp[i][c]` 只依赖 `dp[i-1][...]`（上一行），可用一维数组 + **倒序遍历**。

```java
public int knapsack1D(int[] w, int[] v, int W) {
    int[] dp = new int[W + 1];        // dp[c] = 容量 c 的最大价值

    for (int i = 0; i < w.length; i++) {
        // ⭐ 必须倒序！从大到小
        for (int c = W; c >= w[i]; c--) {
            dp[c] = Math.max(dp[c], dp[c - w[i]] + v[i]);
        }
    }
    return dp[W];
}
```

**为什么必须倒序？**（面试必考）

```
正序遍历的问题：
  dp[c - w[i]] 在本轮已经被更新过（用了第 i 个物品）
  → 同一个物品被重复使用 → 变成完全背包！

倒序的好处：
  计算 dp[c] 时，dp[c - w[i]] 还是上一轮（前 i-1 个物品）的值
  → 保证每个物品只被用一次 → 正确的 0/1 背包
```

**记忆**：
- **0/1 背包**（每个物品一次）→ 容量**倒序**遍历
- **完全背包**（物品无限次）→ 容量**正序**遍历

### 3.5 完全背包（对比）

```java
// 物品可以选无限次 → 容量正序遍历
for (int i = 0; i < w.length; i++) {
    for (int c = w[i]; c <= W; c++) {      // ⭐ 正序
        dp[c] = Math.max(dp[c], dp[c - w[i]] + v[i]);
    }
}
```

### 3.6 项目关联：优惠券最优组合

```
场景（ai-cs-order 优惠计算）：
  订单金额 300 元，可用优惠券：
    A: 满 100 减 20
    B: 满 150 减 35
    C: 满 200 减 50
  规则：可叠加使用，每类限用一张，总优惠不得超过订单金额

抽象：这就是 0/1 背包——
  背包容量 W = 订单金额（门槛）
  物品重量 w[i] = 优惠券门槛
  物品价值 v[i] = 优惠金额
  目标：在不超过订单金额的前提下，最大化优惠
```

**注意**：真实业务的优惠计算远比背包复杂（有品类限制、互斥规则、阶梯价、分摊到 SKU 后退款等），
背包只是**思想原型**。生产系统通常用**规则引擎**（Drools、Aviator）或**贪心 + 回溯**求解，
而不是纯 DP——因为约束条件太多时状态空间会爆炸。

---

## 四、经典 DP 二：最长递增子序列（LIS）

**问题**：`[10, 9, 2, 5, 3, 7, 101, 18]` 的 LIS 是 `[2, 3, 7, 101]`，长度 4。

### 4.1 解法一：DP O(n²)

```java
public int lengthOfLIS(int[] nums) {
    int n = nums.length;
    int[] dp = new int[n];
    Arrays.fill(dp, 1);              // 每个元素自己至少是长度 1
    int max = 1;

    for (int i = 1; i < n; i++) {
        for (int j = 0; j < i; j++) {
            if (nums[i] > nums[j]) {
                dp[i] = Math.max(dp[i], dp[j] + 1);   // 接在 j 后面
            }
        }
        max = Math.max(max, dp[i]);
    }
    return max;
}
```

**状态定义**：`dp[i]` = 以 `nums[i]` **结尾**的 LIS 长度。

### 4.2 解法二：贪心 + 二分 O(n log n) ⭐

```java
public int lengthOfLIS(int[] nums) {
    int[] tails = new int[nums.length];
    int size = 0;                    // tails 的有效长度

    for (int num : nums) {
        // 二分找 tails 中第一个 >= num 的位置
        int left = 0, right = size;
        while (left < right) {
            int mid = left + (right - left) / 2;
            if (tails[mid] < num) left = mid + 1;
            else                   right = mid;
        }
        tails[left] = num;           // 替换（或追加）
        if (left == size) size++;    // 追加到末尾，长度 +1
    }
    return size;
}
```

**核心思想**：`tails[i]` = 长度为 `i+1` 的递增子序列的**最小结尾元素**。

**为什么"最小结尾"最优**：结尾越小，后面越容易接上更大的数。

```
nums = [10, 9, 2, 5, 3, 7, 101, 18]

10:  tails=[10]          size=1
9:   tails=[9]           9 < 10 → 替换（长度1的最小结尾是9）
2:   tails=[2]           2 < 9  → 替换
5:   tails=[2,5]         5 > 2  → 追加，size=2
3:   tails=[2,3]         3 替换 5（长度2的结尾从5降到3）
7:   tails=[2,3,7]       追加，size=3
101: tails=[2,3,7,101]   追加，size=4
18:  tails=[2,3,7,18]    18 替换 101

答案 = 4
```

⚠️ 注意：`tails` 数组**不是** LIS 本身（如最后 `tails=[2,3,7,18]` 恰好是，但中间过程不一定），只有 `size` 是答案。

**复杂度**：O(n log n)（每个元素二分 O(log n)）。

---

## 五、经典 DP 三：最长公共子序列（LCS）

**问题**：`"abcde"` 和 `"ace"` 的 LCS 是 `"ace"`，长度 3。

```java
public int longestCommonSubsequence(String s1, String s2) {
    int m = s1.length(), n = s2.length();
    int[][] dp = new int[m + 1][n + 1];   // dp[i][j] = s1前i个 与 s2前j个 的 LCS 长度

    for (int i = 1; i <= m; i++) {
        for (int j = 1; j <= n; j++) {
            if (s1.charAt(i - 1) == s2.charAt(j - 1)) {
                dp[i][j] = dp[i - 1][j - 1] + 1;      // 字符相同，长度 +1
            } else {
                dp[i][j] = Math.max(dp[i - 1][j], dp[i][j - 1]);   // 取删掉一个的最大
            }
        }
    }
    return dp[m][n];
}
```

**状态转移的直觉**：
- 末尾字符相同 → 一定在 LCS 里，`+1`
- 末尾字符不同 → LCS 不可能同时包含两者，取"删 s1 末尾"和"删 s2 末尾"的较大值

**复杂度**：O(mn)，空间 O(mn)（可优化到 O(min(m,n))）。

**应用**：diff 工具（Git diff）、文本相似度、DNA 序列比对。

---

## 六、区间 DP 与状态压缩（了解）

### 6.1 区间 DP

**特征**：`dp[i][j]` 表示区间 `[i, j]` 的最优解，按**区间长度**从小到大递推。

**例题**：戳气球（LeetCode 312）、石子合并。

```java
// 模板
for (int len = 2; len <= n; len++) {          // 枚举区间长度
    for (int i = 0; i + len - 1 < n; i++) {   // 枚举起点
        int j = i + len - 1;                  // 终点
        for (int k = i; k < j; k++) {         // 枚举分割点
            dp[i][j] = Math.max(dp[i][j], dp[i][k] + dp[k+1][j] + cost(i,k,j));
        }
    }
}
```

**复杂度**：O(n³)。

### 6.2 状态压缩 DP

**特征**：状态是一维/二维的"选择情况"，用**二进制位**表示。

**例题**：旅行商问题（TSP）——访问 n 个城市各一次，最短路径。

```java
// dp[state][i] = 已访问集合为 state、当前在城市 i 的最短距离
// state 用二进制表示：第 k 位为 1 表示城市 k 已访问
// n ≤ 20 时可行（2^20 × 20 ≈ 2000 万状态）
```

**复杂度**：O(2^n × n²)，只适用于 n ≤ 20-25。

---

## 七、贪心算法

### 7.1 定义

**每一步都做当前看起来最优的选择**，希望通过局部最优达到全局最优。

### 7.2 贪心 vs DP

| 维度 | 贪心 | DP |
|---|---|---|
| 决策 | 每步选当前最优，**不回头** | 枚举所有选择，取全局最优 |
| 复杂度 | 通常 O(n log n)（需排序） | 通常 O(n²) 或 O(nW) |
| 正确性 | **需要证明**（贪心选择性质） | 转移方程正确即正确 |
| 适用 | 问题具有贪心选择性质 | 有重叠子问题 + 最优子结构 |

**关键**：贪心**必须证明**，不能"感觉对"。很多看似能贪心的问题其实是错的。

### 7.3 经典反例：0/1 背包不能贪心

```
背包容量 W = 10
物品：A(重量 6, 价值 12)   B(重量 5, 价值 8)   C(重量 5, 价值 8)

贪心策略①（按价值最大）：选 A(12)，剩余容量 4，装不下 B/C → 总价值 12
贪心策略②（按重量最小）：选 B(8)，剩余 5，选 C(8) → 总价值 16 ✅
贪心策略③（按价值密度）：A=2.0, B=1.6, C=1.6
                        选 A(12)，剩余 4 → 总价值 12

最优解：B + C = 16        ← 三种贪心策略都没得到最优解！
```

**原因**：0/1 背包的"选或不选"会影响后续容量，局部最优不等于全局最优。
**分数背包**（物品可切割）则可以用价值密度贪心——因为切割让选择可以"部分进行"。

### 7.4 经典正例：能用贪心的问题

| 问题 | 贪心策略 | 证明思路 |
|---|---|---|
| **活动选择**（最多不冲突会议） | 按**结束时间**升序，能选就选 | 选结束最早的，留给后续最多时间 |
| **分数背包** | 按**价值密度**降序 | 密度高的单位收益最大 |
| **Huffman 编码** | 每次合并**权重最小**的两棵子树 | 贪心选择 + 最优子结构 |
| **Dijkstra** | 每次取**距离最小**的未确定节点 | 非负权下，该点距离已不可能更短 |
| **最小生成树**（Prim/Kruskal） | 每次选**最小**的安全边 | 切割性质 / 环路性质 |

### 7.5 活动选择（经典贪心）

```java
// 给定 n 个会议 [start, end]，求最多能参加几个（不冲突）
public int maxMeetings(int[][] meetings) {
    // ⭐ 按结束时间升序排序
    Arrays.sort(meetings, Comparator.comparingInt(m -> m[1]));

    int count = 0, lastEnd = Integer.MIN_VALUE;
    for (int[] m : meetings) {
        if (m[0] >= lastEnd) {          // 开始时间 ≥ 上个会议的结束时间
            count++;
            lastEnd = m[1];
        }
    }
    return count;
}
```

**复杂度**：排序 O(n log n) + 扫描 O(n) = **O(n log n)**。

**为什么按结束时间而非开始时间或时长？**
- 按开始时间：一个很早开始但很长的会议会占满全天 ❌
- 按时长：短会议可能夹在两个长会议中间，导致选不了更多 ❌
- **按结束时间**：结束越早，留给后面会议的时间越多 ✅

---

## 八、项目关联场景

### 8.1 订单金额分摊（贪心 + 精度处理）

```
场景：订单总额 100 元，3 个商品原价 33.33、33.33、33.34，
     使用 10 元优惠券，需分摊到每个 SKU（退款时按分摊后金额退）

思路：按比例分摊 + 尾差调整（最大余数法，本质是贪心）

分摊后：
  33.33 / 100 × 90 = 29.997 → 30.00
  33.33 / 100 × 90 = 29.997 → 30.00
  33.34 / 100 × 90 = 30.006 → 30.00
  合计 90.00 ✓（若直接四舍五入可能得 89.99 或 90.01）

工程要点：
  1. 用 BigDecimal，绝不用 double（浮点误差累积）
  2. 尾差放到"金额最大"或"最后一个"商品上
  3. 保证 sum(分摊后) == 实付金额（对账要求）
```

**算法视角**：这是一个**带约束的分配问题**，用贪心（比例分配 + 尾差兜底）即可，不需要 DP——因为约束简单（只有一个总额约束）。

### 8.2 Agent 工具调用路径

```
场景（ai-cs-chat Agent）：用户提问 → 需要调用多个工具 → 工具间有依赖

例：查询订单 → 需要 user_id → 需先调用"获取用户信息"

抽象：这是 DAG 上的路径规划，用拓扑排序（[05](./05-图论与搜索-BFS-DFS-拓扑排序.md)）确定执行顺序，
     用 DP/搜索求"最少调用次数"或"最短执行时间"
```

---

## 九、面试高频问答

**Q1：DP 和分治的区别？**
A：都拆分问题，但分治的子问题**不重叠**（归并排序），DP 的子问题**大量重叠**（斐波那契）。DP 必须缓存子问题结果。

**Q2：0/1 背包一维优化为什么容量要倒序遍历？**
A：正序时 `dp[c - w[i]]` 在本轮已被更新（已包含第 i 个物品），会导致同一物品被重复使用（变成完全背包）。倒序保证计算 `dp[c]` 时用到的是"前 i-1 个物品"的状态。

**Q3：完全背包和 0/1 背包的遍历顺序区别？**
A：0/1 背包容量**倒序**（每个物品用一次）；完全背包容量**正序**（物品可重复用）。外层都是遍历物品。

**Q4：LIS 的 O(n log n) 解法中 tails 数组是 LIS 本身吗？**
A：**不是**。`tails[i]` 只是"长度为 i+1 的递增子序列的最小结尾元素"，用于贪心地让后续更容易接上。只有 `size`（tails 的有效长度）是答案。

**Q5：贪心和 DP 怎么选？**
A：先看是否满足贪心选择性质（局部最优能推出全局最优）。贪心复杂度低但**必须证明**；不能证明就用 DP 枚举所有选择。0/1 背包是典型的"贪心失效、必须 DP"的例子。

**Q6：为什么 0/1 背包不能用价值密度贪心？**
A：因为物品不可切割，选了高密度物品会占用容量，可能导致后面装不下更多物品。反例：W=10，A(6,12)、B(5,8)、C(5,8)，按密度选 A 得 12，最优是 B+C 得 16。

**Q7：DP 空间如何优化？**
A：看转移方程依赖哪些历史状态。只依赖前 k 个状态就可用滚动数组（斐波那契 O(n)→O(1)，背包 O(nW)→O(W)）。矩阵类 DP（LCS）可从 O(mn) 优化到 O(min(m,n))。

**Q8：如何判断一道题该不该用 DP？**
A：三问：① 有没有重叠子问题？② 有没有最优子结构？③ 无后效性（当前状态确定后不受之前路径影响）？三个都是才适合 DP。

---

## 十、动手练习

1. 用一维数组实现完全背包，并构造一个例子验证"正序遍历"确实允许重复使用同一物品。
2. 用 LIS 的 O(n log n) 解法求 `[10, 9, 2, 5, 3, 7, 101, 18]` 的长度，并打印每一步的 `tails` 数组。
3. 证明活动选择问题按结束时间贪心是正确的。（提示：交换论证——任何最优解的第一个活动都可以替换成结束最早的那个，不减少后续可选空间）
4. 设计一个订单优惠分摊算法：总额 100，三个 SKU 各 33.33/33.33/33.34，优惠 10 元，用 `BigDecimal` 实现保证 `sum(分摊) == 90.00`。

---

> 上一篇：[07-字符串匹配](./07-字符串匹配-Trie与PII脱敏.md) ｜ 下一篇：[09-缓存淘汰：LRU·LFU](./09-缓存淘汰-LRU-LFU与两级缓存.md)
