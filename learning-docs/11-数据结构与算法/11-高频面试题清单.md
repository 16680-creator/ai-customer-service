# 高频面试题清单

> 本清单按本专题的知识点组织，每题附**答题要点**与**本项目可引用的真实现场**。
> 用法：先看题目自己答一遍，再对照要点检查；能引用项目实例会显著加分。

---

## 一、复杂度分析

| # | 题目 | 难度 | 答题要点 | 项目现场 |
|---|---|---|---|---|
| 1 | ArrayList 的 add 是 O(1) 吗？ | ⭐ | **均摊 O(1)**，最坏 O(n)（扩容复制）。1.5 倍扩容保证 n 次连续 add 总代价 O(n)；固定增量扩容会退化成均摊 O(n) | — |
| 2 | O(n) 一定比 O(n log n) 快吗？ | ⭐ | 不一定。n 小时常数主导。大 O 描述**增长趋势**非绝对耗时 | `SemanticCacheService` 的 O(n) 扫描（n≤200）比 HNSW 更合适 |
| 3 | 如何分析递归复杂度？ | ⭐⭐ | 写递推式 → 画递归树 → 每层工作量 × 树高；或套主定理 | — |
| 4 | 递归的空间复杂度怎么算？ | ⭐⭐ | 看**最大递归深度**（同时存在的栈帧数），非总调用次数。fib 递归时间 O(2ⁿ) 但空间 O(n) | — |
| 5 | 均摊、平均、最坏的区别？ | ⭐⭐⭐ | 均摊是**确定性**的（保证连续 n 次操作总代价）；平均是**概率性**的（依赖输入分布） | — |

---

## 二、线性结构

| # | 题目 | 难度 | 答题要点 | 项目现场 |
|---|---|---|---|---|
| 6 | ArrayList vs LinkedList 怎么选？ | ⭐⭐ | 绝大多数用 ArrayList。随机访问 O(1)、**缓存局部性好**、省内存。LinkedList 只在"已持有节点引用做 O(1) 增删"时占优 | `SecurityAuditRecorder` 尾部追加+头部丢弃用 Deque |
| 7 | 为什么 Java 不推荐 Stack？ | ⭐⭐ | 继承 Vector，方法被 synchronized 修饰（慢）；暴露 `get(i)` 等破坏栈语义的方法（设计缺陷）。用 `ArrayDeque` | — |
| 8 | ArrayDeque 为什么不能存 null？ | ⭐⭐ | null 被用作 `poll()`/`peek()` 的"空"返回值哨兵，存 null 会导致语义歧义 | — |
| 9 | `ConcurrentLinkedDeque.size()` 复杂度？ | ⭐⭐⭐ | **O(n)**！并发下无法原子维护计数，需遍历。高频调用要注意 | `SlidingWindowRateLimiter` 每次 `tryAcquire` 都调 `size()` |
| 10 | 单调栈为什么能 O(n)？ | ⭐⭐⭐ | 每个元素最多入栈一次、出栈一次，内层 while 总执行次数 ≤ n | — |
| 11 | 如何用栈实现队列？ | ⭐⭐⭐ | 双栈：入队栈 A，出队时若栈 B 空则把 A 全部倒入 B。均摊 O(1) | — |
| 12 | 用队列实现栈？ | ⭐⭐⭐ | 双队列，或单队列（入队后把前面所有元素依次出队再入队，使新元素在队头） | — |

---

## 三、哈希

| # | 题目 | 难度 | 答题要点 | 项目现场 |
|---|---|---|---|---|
| 13 | HashMap 为什么用链地址法而非开放寻址？ | ⭐⭐⭐ | 开放寻址需 α<0.7（浪费空间）、删除需墓碑；链地址容忍高装载因子、删除简单。JDK8 加红黑树优化 | — |
| 14 | 为什么容量必须是 2 的幂？ | ⭐⭐⭐ | 用 `(n-1)&hash` 替代 `hash%n`（位运算快）；n 为 2 的幂时 `n-1` 二进制全 1，分布均匀 | — |
| 15 | 为什么要扰动函数 `h ^ (h>>>16)`？ | ⭐⭐⭐ | `(n-1)&hash` 只用低位，不扰动则高位差异全部丢失，导致大量冲突 | — |
| 16 | hashCode 相同 equals 一定相同吗？ | ⭐⭐ | **不一定**（哈希碰撞）。反之必须成立 → 重写 equals 必须重写 hashCode | — |
| 17 | String.hashCode() 能当唯一键吗？ | ⭐⭐⭐ | 不能。32 位，约 7.7 万元素就有 50% 碰撞概率（生日悖论） | `VectorCacheStore` 用 SHA-256 而非 hashCode |
| 18 | 取模分片扩容为什么要迁移大量数据？ | ⭐⭐⭐ | n→n+1 时 `x%n` 与 `x%(n+1)` 几乎全不同，迁移比例 n/(n+1) | `UserIdLast4ModShardingAlgorithm` |
| 19 | 一致性哈希解决什么问题？ | ⭐⭐⭐ | 迁移比例从 n/(n+1) 降到 **1/(n+1)**。哈希环 + 顺时针找最近节点 | — |
| 20 | 虚拟节点的作用？ | ⭐⭐⭐ | 解决数据倾斜。每物理节点映射 150-200 个虚拟节点，使分布均匀 | — |
| 21 | 布隆过滤器能删除吗？为什么？ | ⭐⭐⭐ | 不能（多 key 共享 bit，置 0 会影响其他 key）。要删除用 Counting Bloom Filter | — |
| 22 | 布隆过滤器解决穿透还是击穿？ | ⭐⭐⭐ | **穿透**（查不存在的 key）。击穿是热点 key 过期，需互斥锁或逻辑过期 | — |

---

## 四、树 · 堆 · TopK

| # | 题目 | 难度 | 答题要点 | 项目现场 |
|---|---|---|---|---|
| 23 | TopK 用大顶堆还是小顶堆？ | ⭐⭐⭐ | 求最大 K 个用**小顶堆**（堆顶是门槛，比它大才替换）。反之亦然 | — |
| 24 | TopK 堆解法复杂度？ | ⭐⭐⭐ | **O(n log K)**，空间 O(K)。堆大小 ≤ K 所以是 log K 非 log n | — |
| 25 | 建堆为什么能 O(n)？ | ⭐⭐⭐ | Floyd 法从最后一个非叶子倒着 siftDown。底层节点多但下沉距离为 0，加权求和收敛 | — |
| 26 | PriorityQueue 的 remove 复杂度？ | ⭐⭐⭐ | **O(n)**。不维护"元素→下标"索引，需线性扫描。频繁删除要自建索引 | — |
| 27 | 堆排 vs 快排？ | ⭐⭐⭐ | 堆排最坏 O(n log n) + O(1) 空间，但常数大（下标跳跃，Cache Miss）；快排常数小但最坏 O(n²) | — |
| 28 | Redis ZSET 为什么用跳表不用红黑树？ | ⭐⭐⭐⭐ | 范围查询更高效（底层链表顺序扫描 O(log n + m)），实现简单，易并发优化 | `HotQaCacheService` 热度榜 |
| 29 | RRF 融合为什么用排名不用分数？ | ⭐⭐⭐⭐ | 各路检索分数尺度不可比（BM25 vs 余弦），直接相加需归一化调参。RRF 只用名次，天然免疫 | `MultiQueryMerger` |
| 30 | 红黑树 vs AVL？ | ⭐⭐⭐ | AVL 严格平衡（高度差≤1），查找略快但插入删除旋转频繁；红黑树近似平衡（最长路径≤2×最短），旋转最多 3 次，工程首选 | — |

---

## 五、图论与搜索

| # | 题目 | 难度 | 答题要点 | 项目现场 |
|---|---|---|---|---|
| 31 | BFS 和 DFS 各用什么结构？ | ⭐⭐ | BFS 队列，DFS 栈（或递归）。都是 O(V+E) | — |
| 32 | BFS 为什么能保证无权图最短路径？ | ⭐⭐⭐ | 按"距离起点的层数"逐层扩散，首次到达即最短 | — |
| 33 | 拓扑排序的前提？如何检测环？ | ⭐⭐⭐ | 前提是 DAG。Kahn 算法中结果长度 < 顶点数即有环 | 微服务 `depends_on` 依赖顺序 |
| 34 | 并查集为什么快？ | ⭐⭐⭐ | 路径压缩 + 按秩合并 → O(α(n))，α(n)≤5，工程视为 O(1) | — |
| 35 | 路径压缩和按秩合并各自作用？ | ⭐⭐⭐ | 压缩让 find 快；按秩让 union 时树不退化。**两者结合**才达 O(α(n)) | — |
| 36 | Dijkstra 为什么不能处理负权边？ | ⭐⭐⭐⭐ | 贪心——节点出堆即认定距离确定。负权边可能让已确定节点变得更短 | — |
| 37 | 邻接矩阵 vs 邻接表？ | ⭐⭐ | 默认邻接表（O(V+E) 空间）。邻接矩阵只在稠密图或需 O(1) 判边时用 | — |
| 38 | 递归 DFS 有什么风险？ | ⭐⭐⭐ | 深度过大 StackOverflowError（Java 默认约 1000-10000 层）。用显式栈 + 深度上限 | 分类树遍历（脏数据可能成环） |

---

## 六、排序与二分

| # | 题目 | 难度 | 答题要点 | 项目现场 |
|---|---|---|---|---|
| 39 | 快排最坏何时退化？如何避免？ | ⭐⭐⭐ | 已排序 + 固定取最右 pivot → O(n²)。随机 pivot / 三数取中 / 小区间插入排序 | — |
| 40 | 归并的空间能降到 O(1) 吗？ | ⭐⭐⭐ | 数组版不能，但**链表版可以**（只改指针，无需搬移元素） | — |
| 41 | 比较排序的下界是多少？为什么？ | ⭐⭐⭐⭐ | **Ω(n log n)**。n! 种排列，决策树叶子数 ≥ n!，树高 ≥ log₂(n!) ≈ n log n | — |
| 42 | 什么是排序稳定性？Java 哪些稳定？ | ⭐⭐⭐ | 相等元素相对顺序不变。`Arrays.sort(Object[])`、`List.sort`、`Stream.sorted()` 用 TimSort，**稳定** | 支付宝签名用 TreeMap 排序 |
| 43 | 为什么 mid 写成 `left+(right-left)/2`？ | ⭐⭐ | 防 `left+right` 溢出 int | — |
| 44 | 链表能二分查找吗？ | ⭐⭐⭐ | 不适合。找 mid 需 O(n)，整体 O(n log n)，不如线性查找 O(n)。跳表可解决 | Redis ZSET 用跳表 |
| 45 | 什么是二分答案？ | ⭐⭐⭐⭐ | 不二分下标，二分**答案值域**，用 check(x) 判可行性。适用"最小的最大值/最大的最小值" | — |
| 46 | 求 x 出现次数（O(log n)）？ | ⭐⭐⭐⭐ | `upperBound(x) - lowerBound(x)` | — |

---

## 七、字符串

| # | 题目 | 难度 | 答题要点 | 项目现场 |
|---|---|---|---|---|
| 47 | KMP 优化在哪？next 数组含义？ | ⭐⭐⭐⭐ | 失配时主串指针**不回退**，只移模式串到最长相等前后缀位置。`next[j]` = `pattern[0..j-1]` 的最长相等前后缀长度。O(n+m) | — |
| 48 | RK 的滚动哈希怎么 O(1) 更新？ | ⭐⭐⭐ | `hash(新) = (hash(旧) - 离开字符×R^(m-1))×R + 新字符`。命中后必须 equals 确认防碰撞 | — |
| 49 | Trie 查找为什么与单词数无关？ | ⭐⭐⭐ | 沿字符串逐字符走，每步 O(1)（Map/数组取值），总步数 = 字符串长度 m | — |
| 50 | Trie vs 哈希表？ | ⭐⭐⭐ | 只需精确匹配用哈希表（省空间）；需**前缀匹配**、自动补全用 Trie | — |
| 51 | 敏感词过滤用什么算法？ | ⭐⭐⭐⭐ | 词库小用 Trie（O(n)）；万级词库用 **AC 自动机**（Trie + fail 指针，严格 O(n)） | — |
| 52 | `(?<!\d)` `(?!\d)` 是什么？ | ⭐⭐⭐ | 零宽断言（负向后行/先行），只判断不消耗字符。本项目用它防订单号被误判为身份证/手机号 | `PiiMasker` |
| 53 | 什么是灾难性回溯？ | ⭐⭐⭐⭐ | 嵌套量词 `(a+)+b` 遇长输入时回溯次数指数级增长（ReDoS）。避免嵌套量词、限制输入长度 | `PiiMasker` + 输入截断 512 |
| 54 | Luhn 算法作用？ | ⭐⭐⭐ | 银行卡校验位。隔位乘 2（>9 减 9），和能被 10 整除。本项目用它**过滤误判** | `PiiMasker.luhnValid()` |

---

## 八、动态规划与贪心

| # | 题目 | 难度 | 答题要点 | 项目现场 |
|---|---|---|---|---|
| 55 | DP 和分治的区别？ | ⭐⭐ | 分治子问题**不重叠**（归并）；DP 子问题**大量重叠**，必须缓存 | — |
| 56 | 0/1 背包一维优化为何倒序？ | ⭐⭐⭐⭐ | 正序时 `dp[c-w[i]]` 本轮已更新（已含第 i 个物品）→ 重复使用变完全背包。倒序保证用"前 i-1 个"状态 | — |
| 57 | 完全背包遍历顺序？ | ⭐⭐⭐ | 容量**正序**（可重复使用）。0/1 背包倒序 | — |
| 58 | LIS 的 O(n log n) 解法中 tails 是答案吗？ | ⭐⭐⭐⭐ | **不是**。`tails[i]` 是"长度 i+1 的子序列的最小结尾"，只有 size 是答案 | — |
| 59 | 贪心和 DP 怎么选？ | ⭐⭐⭐ | 贪心复杂度低但**必须证明**贪心选择性质；不能证明就用 DP 枚举 | — |
| 60 | 0/1 背包能用价值密度贪心吗？ | ⭐⭐⭐⭐ | **不能**。反例：W=10，A(6,12)、B(5,8)、C(5,8)，按密度选 A 得 12，最优 B+C 得 16 | — |
| 61 | 活动选择为什么按结束时间排序？ | ⭐⭐⭐ | 结束越早，留给后续会议的时间越多。按开始时间或时长都有反例 | — |

---

## 九、缓存淘汰

| # | 题目 | 难度 | 答题要点 | 项目现场 |
|---|---|---|---|---|
| 62 | **手写 LRU（LeetCode 146）** | ⭐⭐⭐⭐ | 哈希表 + 双向链表 + 虚拟头尾哨兵。get 移到尾，put 超容量删头并**同步删 map** | `VectorCacheStore` 用 LinkedHashMap |
| 63 | 为什么用双向链表不用单链表？ | ⭐⭐⭐ | 删节点需知道前驱，单链表找前驱 O(n) | — |
| 64 | LinkedHashMap 怎么实现 LRU？ | ⭐⭐⭐ | `accessOrder=true` 使 get 也移动节点；重写 `removeEldestEntry` 超容量删最老 | `VectorCacheStore.l1Cache` |
| 65 | LRU vs LFU 优缺点？ | ⭐⭐⭐ | LRU 简单 O(1)，但全表扫描会污染缓存；LFU 适合稳定热点，但有历史包袱（旧热点难淘汰、新数据饿死） | — |
| 66 | Redis LRU 为什么是"近似"的？ | ⭐⭐⭐⭐ | 严格 LRU 需全局链表（每 key +24 字节 + 每次访问移动节点）。Redis 改为**随机采样**（默认 5 个）取最久未访问 | — |
| 67 | Redis LFU 的 8 位 counter 怎么表示百万次？ | ⭐⭐⭐⭐ | **对数近似**（概率递增，counter 越大增长越慢）+ **时间衰减**（16 位记录上次衰减时间） | — |
| 68 | 穿透/击穿/雪崩区别？ | ⭐⭐⭐ | 穿透=查不存在的（布隆过滤器）；击穿=热点 key 过期瞬间（互斥锁/逻辑过期）；雪崩=大量 key 同时过期（TTL 加抖动） | — |
| 69 | 为什么本项目用两级缓存？ | ⭐⭐⭐⭐ | L1 进程内纳秒级无网络，吃掉最热流量；L2 Redis 多实例共享。Redis 挂了降级 L1-only | `VectorCacheStore` |

---

## 十、限流与熔断

| # | 题目 | 难度 | 答题要点 | 项目现场 |
|---|---|---|---|---|
| 70 | 固定窗口有什么问题？ | ⭐⭐⭐ | **边界双倍放行**。跨边界的两个窗口各自放满，实际 2 倍流量 | — |
| 71 | 滑动窗口 vs 令牌桶？ | ⭐⭐⭐⭐ | 滑动窗口严格限制窗口内总次数，不允许突发，空间 O(maxRequests)；令牌桶限制长期平均速率，**允许突发**，空间 O(1)/key | 本项目**两种都实现** |
| 72 | 令牌桶 vs 漏桶？ | ⭐⭐⭐⭐ | 令牌桶允许突发（控制平均速率）；漏桶平滑流量（控制处理速率，匀速出水） | — |
| 73 | 为什么令牌用 double？ | ⭐⭐⭐⭐ | 防低 QPS 饿死。qps=0.5 时用 int 截断每次补 0.5→0，永远凑不齐 1 个 | `TokenBucketRateLimiter`（源码注释有说明） |
| 74 | 为什么用 nanoTime 不用 currentTimeMillis？ | ⭐⭐⭐⭐ | currentTimeMillis 是墙钟时间，受 NTP/手动改时间影响可能倒流；nanoTime 单调递增，适合算时间差 | `TokenBucketRateLimiter` |
| 75 | 为什么用惰性补充不用定时任务？ | ⭐⭐⭐⭐ | 定时任务要遍历所有 key（O(n) 扫描）+ 管理线程。惰性补充利用速率恒定性，访问时一次性补足，数学等价 | `TokenBucketRateLimiter` |
| 76 | 单机限流多实例有什么问题？ | ⭐⭐⭐ | 3 实例 × 100 QPS = 实际 300 QPS。解法：Redis + Lua 原子脚本 | — |
| 77 | 分布式限流为何必须用 Lua？ | ⭐⭐⭐⭐ | "读-算-写"三步并发会超卖。Lua 在 Redis 单线程原子执行 | — |
| 78 | 熔断器三态？ | ⭐⭐⭐ | CLOSED（统计失败率）→ 超阈值 → OPEN（快速失败）→ 冷却期 → HALF_OPEN（探测）→ 恢复/回 OPEN | `ModelHealthRegistry` |
| 79 | 为什么需要 minimumNumberOfCalls？ | ⭐⭐⭐ | 防样本太少误熔断。调 1 次失败率 100%，可能只是偶发抖动 | `ModelHealthRegistry`（=3） |
| 80 | 为什么指数退避而非固定间隔？ | ⭐⭐⭐ | 固定间隔会形成"重试风暴"加剧下游过载。指数退避 + 抖动让重试分散 | `Resilience4jConfig` |

---

## 十一、答题技巧

### 11.1 STAR 结构回答"项目里用过什么算法"

```
S（情境）：什么场景下遇到什么问题
T（任务）：需要达成什么目标
A（行动）：用了什么算法/数据结构，为什么选它（对比其他方案）
R（结果）：量化收益（命中率、耗时、QPS）

示例：
"我们的 RAG 系统同一个问题反复追问会重复调用 Embedding API（S），
 需要降低成本和延迟（T），
 于是设计了 L1 进程内 LRU + L2 Redis 的两级缓存，L1 用
 LinkedHashMap 的 accessOrder 实现，缓存键用 SHA-256 避免
 hashCode 碰撞导致返回错误向量（A），
 最终把重复 Embedding 调用降为零，P95 延迟从 800ms 降到 50ms（R）。"
```

### 11.2 遇到不会的题：展示思考过程

```
❌ 沉默 / 直接说不会
✅ "我先用最直观的方法想：暴力解法是 O(n²)...
    瓶颈在于每步都要重复扫描，可以用哈希表把查找降到 O(1)...
    这样总复杂度是 O(n)，空间 O(n)。
    如果内存受限，还可以考虑双指针（需要排序，O(n log n) 但 O(1) 空间）。"
```

**关键**：面试官看重**分析过程**和**权衡意识**，不只看最终答案。

### 11.3 必背的手写题（考前过一遍）

| 优先级 | 题目 | 出处 |
|---|---|---|
| ⭐⭐⭐⭐⭐ | LRU 缓存（哈希表 + 双向链表） | [09](./09-缓存淘汰-LRU-LFU与两级缓存.md) §4 |
| ⭐⭐⭐⭐⭐ | 二分查找 + lower/upper bound | [06](./06-排序与二分查找.md) §7 |
| ⭐⭐⭐⭐⭐ | TopK（小顶堆） | [04](./04-树堆与TopK.md) §4.2 |
| ⭐⭐⭐⭐ | 快速排序（分区 + 递归） | [06](./06-排序与二分查找.md) §2 |
| ⭐⭐⭐⭐ | KMP（next 数组 + 匹配） | [07](./07-字符串匹配-Trie与PII脱敏.md) §4 |
| ⭐⭐⭐⭐ | 令牌桶限流 | [10](./10-限流算法-四大算法与网关实现.md) §4 |
| ⭐⭐⭐ | 并查集（路径压缩 + 按秩合并） | [05](./05-图论与搜索-BFS-DFS-拓扑排序.md) §5 |
| ⭐⭐⭐ | 拓扑排序（Kahn） | [05](./05-图论与搜索-BFS-DFS-拓扑排序.md) §4 |
| ⭐⭐⭐ | 0/1 背包（一维优化） | [08](./08-动态规划与贪心.md) §3.4 |
| ⭐⭐⭐ | Trie（插入 + 查找 + 前缀匹配） | [07](./07-字符串匹配-Trie与PII脱敏.md) §5.2 |

### 11.4 反问环节可以问的

- 团队目前遇到的性能瓶颈是什么？（展示你关注工程落地）
- 这个岗位对算法的要求偏"刷题"还是"系统设计"？
- 有没有机会参与基础组件/中间件的开发？

---

## 十二、一页纸速查

```
【复杂度】
ArrayList.add 均摊 O(1)（1.5倍扩容）  HashMap 均摊 O(1)
归并/快排 O(n log n)    建堆 O(n)     TopK 堆 O(n log K)
Dijkstra O((V+E)logV)   并查集 O(α(n))≈O(1)   KMP O(n+m)

【选型口诀】
频繁查、少增删 → ArrayList（缓存友好）
要栈/队列      → ArrayDeque（别用 Stack）
要最值         → PriorityQueue（堆）
要前缀匹配     → Trie
要精确匹配     → HashMap
求 TopK        → 小顶堆（求最大）/ 大顶堆（求最小）
频繁删除任意元素 → 自建索引的堆（PriorityQueue.remove 是 O(n)）

【限流】
允许突发 + O(1) 空间 → 令牌桶（本项目默认）
严格限制窗口次数     → 滑动窗口
保护下游 + 匀速      → 漏桶
分布式              → Redis + Lua

【缓存】
单机 + 时间局部性 → LinkedHashMap LRU（accessOrder=true）
分布式 + 全局     → Redis（allkeys-lru / allkeys-lfu）
防穿透            → 布隆过滤器
防击穿            → 互斥锁重建 / 逻辑过期
防雪崩            → TTL 加随机抖动

【排序】
对象排序（要稳定）→ TimSort（Arrays.sort(Object[])）
基本类型         → 双轴快排（Arrays.sort(int[])）
小范围整数       → 计数排序 O(n+k)
链表排序         → 归并（O(1) 空间）

【易错点】
mid = left + (right-left)/2      防溢出
背包一维：0/1 倒序，完全正序
LRU 淘汰要同步删 map 的 key
ConcurrentLinkedDeque.size() 是 O(n)
递归深度大 → StackOverflowError，改显式栈
正则嵌套量词 (a+)+ → 灾难性回溯 ReDoS
```

---

> 返回 [数据结构与算法总览](./README.md) ｜ 返回 [学习路线总览](../00-学习路线总览/README.md)
