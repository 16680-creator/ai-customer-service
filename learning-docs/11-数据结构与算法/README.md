# 数据结构与算法

> 本专题是 `learning-docs` 的 **第十一个模块**（`11-数据结构与算法`），补齐 `01`~`10` 里"只用结论、没讲原理"的那部分底层能力。
> **最大特色**：每一个算法都能在 `ai-customer-service` 项目里找到**真实落地的源码**，不是纸上谈兵。
> 前置阅读：[00-学习路线总览](../00-学习路线总览/README.md)、[01-Java基础/05-集合框架与源码](../01-Java基础/05-集合框架与源码.md)

---

## 一、为什么要在工程之外单独学算法

`01`~`10` 教你"怎么用框架"，本模块教你"框架底层为什么这样设计"以及"面试怎么答"。

| 场景 | 只懂框架 | 懂算法之后 |
|---|---|---|
| 用 `LinkedHashMap` 做 LRU | 知道加 `accessOrder=true` | 知道它为什么是 O(1)、`removeEldestEntry` 在哪个时机回调、手写双向链表版本 |
| 网关限流 | 会配 Sentinel | 能说清令牌桶/漏桶/滑动窗口的取舍，能纯 Java 手写一个 |
| Redis ZSET 做热度榜 | 会调 `reverseRange` | 知道 ZSET 底层是跳表 + 哈希表，Top-K 用堆是 O(n log k) 而非 O(n log n) |
| 分库分表路由 | 会配 ShardingSphere | 知道取模路由为什么范围查询要广播、一致性哈希能解决什么问题 |
| 写业务代码 | 功能能跑 | 能主动识别 O(n²) 嵌套循环、能估算数据量增长后的性能拐点 |

一句话：**框架会过时，算法不会。** 本项目的令牌桶、滑动窗口、LRU 缓存都是**纯 Java 手写**的（不依赖任何库），这正是本模块最好的教材。

---

## 二、知识地图

```
                        数据结构与算法
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
   【基础】                【结构】                【思想】
        │                     │                     │
   复杂度分析              线性结构              贪心 / 分治
   ├ 大 O 表示法           ├ 数组 / ArrayList     ├ 二分思想
   ├ 均摊复杂度            ├ 链表 / LinkedList    ├ 双指针 / 滑动窗口
   ├ 递归树 / 主定理       ├ 栈（ArrayDeque）     ├ 回溯
   └ 最好/最坏/平均        ├ 队列 / 双端队列      └ 位运算
                          └ 哈希表
                              │
                          【非线性】
                          ├ 树（二叉/红黑/B+/Trie）
                          ├ 堆（PriorityQueue）
                          └ 图（邻接表/邻接矩阵）
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
   【经典算法】          【工程算法】            【实战】
        │                     │                     │
   排序（快排/归并/堆排）  缓存淘汰 LRU/LFU      面试高频题
   二分查找              限流（令牌桶/漏桶/      刷题路线
   字符串匹配 KMP        滑动窗口/固定窗口）     复杂度速查
   图搜索 BFS/DFS        分库分表路由取模
   拓扑排序 / 并查集      RRF 多结果融合
   动态规划 / 贪心        PII 脱敏正则
```

---

## 三、文档清单与学习路线

### 第一阶段：思维地基（1 周）

| 序号 | 文档 | 核心内容 | 难度 |
|---|---|---|---|
| 01 | [复杂度分析与算法思维](./01-复杂度分析与算法思维.md) | 大 O、均摊分析（ArrayList/HashMap 扩容）、递归树、主定理、复杂度速查表 | ⭐ |
| 02 | [线性结构：数组·链表·栈·队列](./02-线性结构-数组链表栈队列.md) | ArrayList vs LinkedList 实测、ArrayDeque 有界队列、单调栈、滑动窗口双指针 | ⭐⭐ |

### 第二阶段：核心结构（1-2 周）

| 序号 | 文档 | 核心内容 | 难度 |
|---|---|---|---|
| 03 | [哈希：冲突、扰动与分片路由](./03-哈希-冲突扰动与分片路由.md) | 哈希函数设计、冲突解决、SHA-256 缓存键、取模分片与范围广播 | ⭐⭐ |
| 04 | [树·堆与 TopK](./04-树堆与TopK.md) | 堆性质、`PriorityQueue`、TopK 四种解法、RRF 融合与 ZSET 热度榜实战 | ⭐⭐⭐ |
| 05 | [图论与搜索：BFS·DFS·拓扑排序](./05-图论与搜索-BFS-DFS-拓扑排序.md) | 邻接表、BFS 最短路、DFS 回溯、拓扑排序、并查集 | ⭐⭐⭐ |

### 第三阶段：经典算法（1-2 周）

| 序号 | 文档 | 核心内容 | 难度 |
|---|---|---|---|
| 06 | [排序与二分查找](./06-排序与二分查找.md) | 十大排序对比、快排分区、归并、二分查找边界模板 | ⭐⭐ |
| 07 | [字符串匹配：Trie 与 PII 脱敏](./07-字符串匹配-Trie与PII脱敏.md) | BF/RK 滚动哈希、KMP next 数组、Trie 字典树、敏感词过滤实战 | ⭐⭐⭐ |
| 08 | [动态规划与贪心](./08-动态规划与贪心.md) | DP 三要素、背包、LCS、最长递增子序列、贪心与反例 | ⭐⭐⭐⭐ |

### 第四阶段：工程算法实战（1 周，本项目重点）

| 序号 | 文档 | 核心内容 | 难度 |
|---|---|---|---|
| 09 | [缓存淘汰：LRU·LFU 与两级缓存](./09-缓存淘汰-LRU-LFU与两级缓存.md) | LRU 两种手写实现、LFU、FIFO、Redis 淘汰策略、`VectorCacheStore` 源码走读 | ⭐⭐⭐ |
| 10 | [限流算法：四大算法与网关实现](./10-限流算法-四大算法与网关实现.md) | 固定窗口/滑动窗口/令牌桶/漏桶、边界双倍放行漏洞、Sentinel WarmUp、熔断滑动窗口 | ⭐⭐⭐ |

### 收尾

| 序号 | 文档 | 核心内容 |
|---|---|---|
| 11 | [高频面试题清单](./11-高频面试题清单.md) | 按知识点分类的面试题库 + 答题要点 + 本项目可引用的现场 |

---

## 四、本项目中的算法现场（核心特色）

> 学完理论后，**回到这里对照源码看一遍**，效果远好于刷十道题。

| 算法 | 项目实现 | 文件路径 | 对应文档 |
|---|---|---|---|
| **LRU 缓存**（进程内） | `LinkedHashMap(accessOrder=true)` + `removeEldestEntry` | `ai-cs-chat/.../cache/VectorCacheStore.java` | [09](./09-缓存淘汰-LRU-LFU与两级缓存.md) |
| **两级缓存 L1+L2** | 进程内 LRU + Redis，读路径回填、写路径双写、Redis 挂了降级 L1-only | `ai-cs-chat/.../cache/VectorCacheStore.java` | [09](./09-缓存淘汰-LRU-LFU与两级缓存.md) |
| **分布式 LRU**（ZSET 时间戳） | Redis ZSET，score = 最近命中时间戳，`range(0, n)` 淘汰最旧 | `ai-cs-chat/.../cache/SemanticCacheService.java` | [09](./09-缓存淘汰-LRU-LFU与两级缓存.md) |
| **频次统计 / 近似 LFU** | `ZINCRBY` 累加频次，达阈值提升进缓存 | `ai-cs-chat/.../cache/HotQaCacheService.java` | [09](./09-缓存淘汰-LRU-LFU与两级缓存.md) |
| **Top-N 热度榜** | ZSET `reverseRange(0, topN-1)` | `ai-cs-chat/.../cache/HotQaCacheService.java` | [04](./04-树堆与TopK.md) |
| **RRF 倒数排名融合** | `1/(k+rank)` 跨路累加，`LinkedHashMap` 保序去重 + Top-N 截断 | `ai-cs-chat/.../rag/retrieve/MultiQueryMerger.java` | [04](./04-树堆与TopK.md) |
| **向量余弦相似度** | O(n) 线性扫描（n≤200，刻意不用 HNSW 近似索引） | `ai-cs-chat/.../cache/SemanticCacheService.java` | [01](./01-复杂度分析与算法思维.md) |
| **令牌桶限流** | 惰性补充、`double` 令牌防低 QPS 饿死、`ConcurrentHashMap` + per-bucket 锁 | `ai-cs-gateway/.../filter/TokenBucketRateLimiter.java` | [10](./10-限流算法-四大算法与网关实现.md) |
| **滑动窗口限流** | `ConcurrentLinkedDeque` 时间戳队列 + 惰性清理 | `ai-cs-gateway/.../filter/SlidingWindowRateLimiter.java` | [10](./10-限流算法-四大算法与网关实现.md) |
| **有界队列（最近 N 条）** | `ArrayDeque` 超容量 `removeFirst()`，审计事件留痕 | `ai-cs-chat/.../security/SecurityAuditRecorder.java` | [02](./02-线性结构-数组链表栈队列.md) |
| **哈希取模分片路由** | 用户 ID 后 4 位取模（库 2 / 表 4），范围查询广播全分片 | `ai-cs-user/.../sharding/UserIdLast4ModShardingAlgorithm.java` | [03](./03-哈希-冲突扰动与分片路由.md) |
| **SHA-256 定长缓存键** | 长文本哈希成定长 key，防 Redis key 超长 | `ai-cs-chat/.../cache/VectorCacheStore.java` | [03](./03-哈希-冲突扰动与分片路由.md) |
| **熔断滑动窗口** | Resilience4j COUNT_BASED：窗口 10 / 最小调用 3 / 失败率 50% / OPEN 30s | `ai-cs-chat/.../modelrouter/ModelHealthRegistry.java` | [10](./10-限流算法-四大算法与网关实现.md) |
| **指数退避重试** | 30s/60s TimeLimiter → 退避重试 3 次 → 熔断 | `ai-cs-chat/.../config/Resilience4jConfig.java` | [10](./10-限流算法-四大算法与网关实现.md) |
| **延迟队列（订单超时）** | RocketMQ `delayLevel 16 ≈ 30min` + 定时扫描兜底 | `ai-cs-order/.../OrderServiceImpl.java`、`OrderTimeoutScheduler.java` | [02](./02-线性结构-数组链表栈队列.md) |
| **PII 正则脱敏** | 手机号/身份证/银行卡掩码 | `ai-cs-chat/.../util/PiiMasker.java` | [07](./07-字符串匹配-Trie与PII脱敏.md) |
| **字典序排序（签名）** | `TreeMap` 按 key 字典序排参数后签名 | `ai-cs-pay/.../channel/AlipayChannel.java` | [06](./06-排序与二分查找.md) |

---

## 五、复杂度速查表

> 背下来，分析任何算法都从这里出发。

### 数据结构操作复杂度

| 结构 | 查找 | 插入 | 删除 | 备注 |
|---|---|---|---|---|
| 数组 | O(n) | O(n) | O(n) | 按下标访问 O(1) |
| ArrayList | O(n) | **均摊 O(1)**（尾部） | O(n) | 扩容 1.5 倍复制 |
| LinkedList | O(n) | O(1)（已知节点） | O(1)（已知节点） | 缓存不友好，实测常慢于 ArrayList |
| 栈 / 队列 | O(n) | O(1) | O(1) | `ArrayDeque` 实现 |
| 哈希表 | **O(1)** | O(1) | O(1) | 冲突退化 O(n)，JDK8 后树化 O(log n) |
| 二叉搜索树 | O(log n) | O(log n) | O(log n) | 退化成链表 O(n) |
| 红黑树 / TreeMap | O(log n) | O(log n) | O(log n) | 自平衡，HashMap 树化后同此 |
| 堆 PriorityQueue | O(n)（查任意） | O(log n) | O(log n) | 查最值 O(1) |
| 跳表（Redis ZSET） | O(log n) | O(log n) | O(log n) | 范围查询 O(log n + m) |
| B+ 树（DB 索引） | O(log n) | O(log n) | O(log n) | 层数低、叶子链表利于范围扫描 |

### 排序算法复杂度

| 算法 | 最好 | 平均 | 最坏 | 空间 | 稳定 |
|---|---|---|---|---|---|
| 冒泡 | O(n) | O(n²) | O(n²) | O(1) | ✅ |
| 选择 | O(n²) | O(n²) | O(n²) | O(1) | ❌ |
| 插入 | O(n) | O(n²) | O(n²) | O(1) | ✅ |
| 希尔 | O(n log n) | O(n^1.3) | O(n²) | O(1) | ❌ |
| **快排** | O(n log n) | **O(n log n)** | O(n²) | O(log n) | ❌ |
| **归并** | O(n log n) | **O(n log n)** | O(n log n) | O(n) | ✅ |
| **堆排** | O(n log n) | **O(n log n)** | O(n log n) | O(1) | ❌ |
| 计数 | O(n+k) | O(n+k) | O(n+k) | O(k) | ✅ |
| 桶 | O(n+k) | O(n+k) | O(n²) | O(n+k) | ✅ |
| 基数 | O(nk) | O(nk) | O(nk) | O(n+k) | ✅ |

> Java 的 `Arrays.sort(对象)` 用 **TimSort**（归并 + 插入混合，稳定）；
> `Arrays.sort(基本类型)` 用**双轴快排**（不稳定，基本类型无需稳定性）。

### 常见量级对应（估算性能用）

| 复杂度 | n=10 | n=100 | n=1000 | n=10^5 | 直觉 |
|---|---|---|---|---|---|
| O(1) | 1 | 1 | 1 | 1 | 哈希查找 |
| O(log n) | 3 | 7 | 10 | 17 | 二分/树 |
| O(n) | 10 | 100 | 1000 | 10^5 | 一次遍历 |
| O(n log n) | 33 | 664 | 10^4 | 1.7×10^6 | 快排/归并 |
| O(n²) | 100 | 10^4 | 10^6 | 10^10 ⚠️ | 双层循环，n=10^5 必超时 |

---

## 六、学习方法建议

1. **先跑通再优化**：先写 O(n²) 的暴力解通过，再优化到 O(n log n)。能分析出"为什么慢"比直接背最优解重要。
2. **画图和举例**：树、图、DP 的题，用纸笔画一遍执行过程再写代码。
3. **对照本项目源码**：本专题每篇都有"项目现场"小节，读完原理立刻看真实代码，理解会深一层。
4. **手写而非调库**：LRU、令牌桶、KMP 至少手写一遍，面试要求能白板写出。
5. **量化而非感觉**：说"这个接口慢"之前，先算它的复杂度是多少、数据量翻倍后会怎样。

---

## 七、刷题路线（LeetCode 热题，按本专题顺序）

> 建议每篇文档学完做对应标签的 5-10 题，贪多嚼不烂。

| 阶段 | 知识点 | 推荐题号 |
|---|---|---|
| 01 复杂度 | 双指针 / 滑动窗口 | 3、76、438、567（滑动窗口）；15、11（双指针） |
| 02 线性结构 | 栈 / 队列 / 链表 | 20、155、232、739（栈）；206、141、21、25（链表） |
| 03 哈希 | 哈希表 / 前缀和 | 1、49、128、560、242 |
| 04 树堆 TopK | 堆 / 优先队列 | **215（TopK，必背）**、347、23、295 |
| 05 图论 | BFS / DFS / 拓扑 | 200、994、133、207、210、547（并查集） |
| 06 排序二分 | 二分查找 | **704、35、34（边界模板）**、33、153 |
| 07 字符串 | KMP / Trie | **28（实现 strStr，KMP 必背）**、208（Trie） |
| 08 DP / 贪心 | 背包 / LIS / LCS | **70、322、518（背包）**、300（LIS）、1143（LCS）、55（贪心） |
| 09 缓存 | 设计题 | **146（LRU，必背）**、460（LFU） |
| 10 限流 | 设计题 | 362（Hit Counter，滑动窗口原型） |

---

> 返回 [学习路线总览](../00-学习路线总览/README.md)
