# GC 日志与停顿分析

> 对应项目：`tools/nacos/bin/startup.sh:114`（仓库内唯一的真实 `-Xlog:gc*` 配置）、`deploy/docker-compose/docker-compose-all.yml:175`（业务服务 `JAVA_OPTS`，目前未开 GC 日志）。
> 相关：[03-JVM内存模型与GC实战](../01-Java基础/03-JVM内存模型与GC实战.md)（G1 机制/IHOP/`jstat -gcutil` 入门已讲，本篇专讲"日志逐字段读懂 + 用停顿数据反推参数"）、[01-JVM参数与GC选型](./01-JVM参数与GC选型.md)（参数怎么定）、[04-内存泄漏排查实战](./04-内存泄漏排查实战.md)（GC 数据怀疑泄漏之后的动作）。

---

## 一、先给结论：GC 分析的三板斧

| 步骤 | 用什么 | 回答什么问题 |
|---|---|---|
| ① 日志 | `-Xlog:gc*` 滚动文件 | 停顿多久、多久一次、哪个代在回收、有没有异常（晋升失败等） |
| ② 实时观测 | `jstat -gcutil` | 不动配置就能看：各代占用%、FGC 次数是否增长、元空间是否打满 |
| ③ 反推参数 | 停顿/频率数据 + 决策表 | 堆该不该加、年轻代该不该调、IHOP 该不该降 |

**纪律：先有日志再谈调优。** 没有日志的 GC 调优 = 蒙着眼睛拧螺丝。

---

## 二、`-Xlog:gc*`：开启与逐字段解读

### 2.1 仓库内的真实配置

Nacos 启动脚本（JDK 9+ 分支）是本仓库唯一真实落地的 GC 日志配置：

```bash
# tools/nacos/bin/startup.sh:114（JDK 9+ 分支）
JAVA_OPT="${JAVA_OPT} -Xlog:gc*:file=${BASE_DIR}/logs/nacos_gc.log:time,tags:filecount=10,filesize=100m"
#                     └─输出器─┘└──────文件───────┘ └装饰器┘ └───滚动策略────┘
```

语法拆解：`-Xlog[:[what][:[output][:[decorators][:[output-options]]]]]`——`gc*` 表示 GC 及其所有子标签（alloc、age、heap、refine 等）；`time,uptime` 是行首装饰器（日历时间 + 进程运行秒数）；`filecount=10,filesize=100m` 共 1GB 滚动上限。JDK 8 老语法（`-XX:+PrintGCDetails` 等）在同一脚本 `:118` 也有对照——新项目一律用统一日志。

业务服务照抄改路径即可（加进 `docker-compose-all.yml` 的 `JAVA_OPTS`）：

```
-Xlog:gc*:file=/app/logs/gc.log:time,uptime:filecount=5,filesize=20m
```

### 2.2 一条 G1 年轻代日志逐字段精读

```
[2026-09-03T21:14:07.382+0800][12.456s][info][gc] GC(12) Pause Young (Normal) (G1 Evacuation Pause)
[2026-09-03T21:14:07.382+0800][12.456s][info][gc,heap] GC(12) PSYoungGen: 128M(192M)->6M(192M)
[2026-09-03T21:14:07.382+0800][12.456s][info][gc,metaspace] GC(12) Metaspace: 95M(100M used)->95M(100M used)
[2026-09-03T21:14:07.385+0800][12.459s][info][gc] GC(12) Pause Young (Normal) (G1 Evacuation Pause) 128M->8M(512M) 3.412ms
[2026-09-03T21:14:07.385+0800][12.459s][info][gc,phases] GC(12) Pre Evacuate Collection Set: 0.1ms
[2026-09-03T21:14:07.385+0800][12.459s][info][gc,phases] GC(12) Evacuate Collection Set: 2.9ms
[2026-09-03T21:14:07.385+0800][12.459s][info][gc,phases] GC(12) Post Evacuate Collection Set: 0.3ms
[2026-09-03T21:14:07.385+0800][12.459s][info][gc,phases] GC(12) Other: 0.1ms
```

| 字段 | 含义 | 怎么用 |
|---|---|---|
| `[2026-09-03T21:14:07.382+0800]` | 日历时间（`time` 装饰器） | 和慢 SQL 日志、访问日志对时，找"GC 与超时同刻" |
| `[12.456s]` | 进程启动后秒数（`uptime`） | 区分"启动期抖动"与"稳态问题"：启动 30s 内的 GC 不必紧张 |
| `[info][gc]` | 级别与标签（`tags`） | `gc,heap`/`gc,phases` 是 `gc*` 展开的子标签 |
| `GC(12)` | JVM 内回收编号 | 全局唯一，跨行关联同一次回收 |
| `Pause Young (Normal)` | 回收类型 | `(Normal)` 常规、`(Concurrent Start)` 并发标记起点、`(Mixed)` 混合回收 |
| `(G1 Evacuation Pause)` | 停顿成因：复制存活对象 | Evacuation = 把活对象从旧 Region 搬到新 Region |
| `128M->8M(512M)` | 回收前后堆占用（堆总量） | 回收效率：128→8 说明新生代健康；若回收后仍贴着上限，去查老年代 |
| `3.412ms` | **本次停顿** | 盯 p99：单次没意义，聚合看分布 |
| `Evacuate Collection Set: 2.9ms` | 停顿大头在复制阶段 | 复制慢 = 存活对象多，查是否有大对象提前晋升 |

`user/sys/real` 三元组若出现在老式输出里：`real` 是墙钟（用户感知），`user>>real` 说明多 GC 线程并行，`user≈real` 是单线程（串行 Full GC 的特征）。

### 2.3 两条"事件型"日志：并发标记与混合回收

```
# IHOP（默认 45%）被越过，启动并发标记——注意这是并发阶段，不停顿
GC(18) Concurrent Mark Cycle 124M->124M(512M) 48.7ms      # 总耗时含并发，别当停顿看

# 标记完成后进入若干轮 Mixed GC（年轻代 + 部分老年代 Region 一起回收）
GC(21) Pause Young (Mixed) (G1 Evacuation Pause) 210M->52M(512M) 5.1ms
```

> G1 的 Region/`MaxGCPauseMillis`/SATB 机制见 [03-JVM内存模型与GC实战 §4.1](../01-Java基础/03-JVM内存模型与GC实战.md)。本篇关注的是**从日志形态识别故障**，见 §四。

### 2.4 日志量与开销

`gc*` 全量标签在 512m 小堆上每分钟几十行，磁盘滚动 100MB 足够存数周；开销 <1%，生产常开。只想要精简版用 `-Xlog:gc`（无 `*`），但排查晋升问题时子标签信息缺失，不建议省。

---

## 三、`jstat -gcutil`：不改配置的实时观测

> 基础列含义见 [03-JVM内存模型与GC实战 §5.1](../01-Java基础/03-JVM内存模型与GC实战.md)。这里给"异常形态→结论"的对照表——`jstat` 的价值是 30 秒内给方向。

```bash
jps -l                       # 1. 找 pid（容器内：docker exec <c> jps -l 或 jcmd 1）
jstat -gcutil <pid> 1000 20  # 2. 每秒采一次，共 20 次
```

| 观测形态 | 含义 | 下一步 |
|---|---|---|
| `O` 稳定在 40~70% 徘徊 | 老年代健康（Mixed GC 在压） | 正常 |
| `O` 爬到 90%+ 且 `FGC` 持续 +1 | 老年代回收不动：泄漏或堆太小 | 抓 dump（见 [04](./04-内存泄漏排查实战.md)） |
| `E` 每秒清零一轮、`YGC` 疯涨 | 年轻代太小或对象分配速率过高 | 看日志分配速率，调大新生代比例 |
| `M` 逼近 100%、`FGC` 增长但 `O` 正常 | **元空间**满了（类加载器泄漏） | dump 后查 `java.lang.Class` 实例 |
| `FGCT` 单次增量 > 1s | 发生了串行长停顿 Full GC | 回日志找 `Pause Full` 成因 |

`jstat` 与日志互为备份：容器里临时排查看 `jstat`（零配置），事后复盘看 `-Xlog:gc*` 文件（有全量细节）。

**30 秒采样实录（健康形态参考）**：

```
  S0     S1     E      O      M     CCS    YGC    YGCT    FGC    FGCT     GCT
  0.00 100.00  61.34  52.87  95.62  90.35    42    0.318     2    0.096    0.414
  0.00 100.00  78.02  53.01  95.62  90.35    42    0.318     2    0.096    0.414
  0.00 100.00   9.87  53.20  95.62  90.35    43    0.329     2    0.096    0.425
```

判读：`E` 每几秒清零一次（YGC 正常节奏）、`O` 稳在 53% 上下（Mixed 在压）、`M` 95.62% 三行不变（类加载已稳态，未持续增长）、`FGC` 2 次不增长——**健康**。三行对比法是 jstat 的精髓：看的是**变化趋势**不是单行数值。

---

## 四、G1 的三类故障日志：晋升失败 / To-space exhausted / Full GC

这三种形态是 G1 排查的"必考题"，日志一眼可辨：

### 4.1 To-space exhausted（疏散区耗尽）

```
GC(26) Pause Young (Normal) (G1 Evacuation Pause) 240M->96M(512M) 18.2ms
GC(26)   To-space exhausted                    ← 紧跟其后出现
```

**机制**：Evacuation 时活对象要复制到"目标 Region"（To-space）。GC 开始前 G1 预留的目标 Region 不够用了，复制途中**边复制边现取新 Region、还可能就地不搬**，导致本次停顿暴涨（上面 3ms 级→18ms 级），且部分对象降级为"不整理"。频繁出现的根因通常是：老年代占用已高、并发标记启动太晚（IHOP 太高）、或碎片化严重。

**对策**（按优先级）：① `-Xmx` 有余量就加大堆；② 降低 `-XX:InitiatingHeapOccupancyPercent`（默认 45，降到 30~35 提前启动标记）；③ `-XX:G1ReservePercent`（默认 10）升到 15~20，多留疏散缓冲；④ 排查 Humongous 大对象（见下）。

### 4.2 晋升失败（Promotion Failure / Evacuation Failure 同源）

年轻代对象熬过阈值要晋升老年代，但老年代凑不出连续可用 Region → 日志出现 `(Evacuation Failure)` 计数或 Humongous 相关字样。与 4.1 常互为因果：To-space exhausted 的下一次 GC 往往就是晋升失败。

**Humongous 检查**：单个对象 ≥ Region 一半即"巨型对象"，直接进老年代 Region，是碎片化头号推手：

```
-Xlog:gc+humongous=debug   # 单独开大对象日志，看谁在制造巨型 byte[]/大集合
```

本项目对应现实：RAG 检索把多个片段拼成大 `context` 字符串、Jackson 反序列化 float[] 向量（4096 维缓存），都是潜在 Humongous 来源——`-XX:G1HeapRegionSize` 默认按堆自动算（512m 堆约 1m Region），512KB 的数组就算巨型对象。

### 4.3 Pause Full（G1 兜底 Full GC）

```
GC(30) Pause Full (G1 Compaction Pause) 400M->180M(512M) 340ms   # 整堆压缩，停顿秒级风险
```

G1 的 Full GC 是**单线程整堆压缩**（JDK 17 仍是），出现即意味着前面的机制都没兜住。先查它前面的一条并发标记是否以 `Abort` 结尾（To-space exhausted 连锁），再查 dump 看老年代里谁站着不走（→ [04](./04-内存泄漏排查实战.md)）。

---

## 五、用停顿数据反推参数（决策表）

> 拿着 §二~§四的日志数据查这张表。**一次只改一个参数**，改完必须重测（方法论见 [08](./08-性能问题定位方法论.md)）。

| 日志/观测现象 | 反推结论 | 参数动作 |
|---|---|---|
| YGC 频繁（<10s 一次）但每次回收后 Eden 很空 | 分配速率高或年轻代太小 | 加 `-Xmx`；或调 `-XX:G1NewSizePercent`(默认5)/`G1MaxNewSizePercent`(默认60) 抬高年轻代下限 |
| Young 停顿 > 目标（如 >100ms） | 单次回收集合太大 | 降 `-XX:MaxGCPauseMillis`（默认200）→ G1 自动缩年轻代；⚠️ 副作用见下 |
| 停顿达标但 YGC 更频繁了 | 上面那条的副作用 | MaxGCPauseMillis 别低于 50ms；否则回到"加大堆"正解 |
| To-space exhausted 反复出现 | 疏散缓冲不足/标记太晚 | 降 IHOP 到 30~35；`G1ReservePercent` 升 15；加堆 |
| Mixed GC 轮数少、老年代压不下去 | 标记启动太晚或标记周期太长 | 降 IHOP；`-XX:G1MixedGCCountTarget=8`（默认 8） |
| 老年代只进不出、Mixed 后仍 90%+ | 泄漏或缓存无界 | 不是参数问题：抓 dump 定位（[04](./04-内存泄漏排查实战.md)） |
| `Metaspace` 行持续增长不封顶 | 类加载器泄漏 | `-XX:MaxMetaspaceSize` 封顶 + dump 查类（[04 §四](./04-内存泄漏排查实战.md)） |
| Full GC 伴随 `user≈real`、停顿秒级 | 退化到单线程整堆压缩 | 按晋升失败链条逐级回查 |

**两个反直觉提醒**：
1. `MaxGCPauseMillis` 是**目标不是承诺**——设 20ms 会把年轻代压得过小，GC 更频繁、总吞吐反降，512m 小堆建议 100~200；
2. IHOP 降太多（如 20）会让并发标记空转浪费 CPU，30~35 是经验起点，依据是"标记周期耗时 < 从 IHOP 涨到满的时间"。

---

## 六、可复制命令：一次完整的 GC 体检

```bash
# 0. 给业务服务补上日志参数（改 docker-compose-all.yml 的 JAVA_OPTS 后）
docker compose -f deploy/docker-compose/docker-compose-all.yml up -d ai-chat-service

# 1. 看日志：最近的停顿分布
docker exec aics-chat-service sh -c "grep 'Pause' /app/logs/gc.log | tail -20"

# 2. 提取停顿时长列，看 p95（真实排查时导入表格/脚本算）
docker exec aics-chat-service sh -c \
  "grep -oP 'Pause Young.*\K[0-9.]+(?=ms)' /app/logs/gc.log | sort -n | tail -5"

# 3. 实时观测 30 秒
docker exec aics-chat-service jstat -gcutil 1 1000 30

# 4. 有 To-space exhausted 吗？
docker exec aics-chat-service sh -c "grep -c 'To-space exhausted' /app/logs/gc.log"

# 5. Nacos 的 GC 日志（仓库现成配置已在跑的对照物）
docker exec aics-nacos tail -20 /home/nacos/logs/nacos_gc.log
```

### 日志分析 grep 命令集（复制即用）

```bash
G=/app/logs/gc.log
grep -c 'Pause Young' $G                    # 年轻代回收总次数
grep -c 'Pause Full'  $G                    # Full GC 次数（>0 就要深挖）
grep -c 'To-space exhausted' $G             # 疏散失败次数
grep -c 'Concurrent Mark Cycle' $G          # 并发标记周期次数（IHOP 触发频率）
grep 'Pause Young' $G | grep -oP '\d+\.\d+ms$' | sort -n | awk '{a[NR]=$1} END{print a[int(NR*0.95)]}'  # Young 停顿 p95
grep 'Metaspace' $G | tail -3               # 元空间是否还在涨
grep 'humongous' $G | head                  # 需 -Xlog:gc*（含 humongous 子标签）时可见
```

> 这些命令就是"测量"的落地形态：每次调参前后各跑一遍，输出直接贴进优化记录（[08 §5.3](./08-性能问题定位方法论.md) 的表格模板）。

---

## 七、面试高频问答

**Q1：`-Xlog:gc` 和 `-Xlog:gc*` 的区别？**
A：`gc` 只输出 GC 事件主干行；`gc*` 展开全部子标签（`gc,heap`、`gc,phases`、`gc,metaspace`、`gc+humongous` 等），能看到各代前后占用、停顿内部分阶段耗时。排查晋升/大对象问题必须 `gc*`；其开销 <1%，生产建议直接 `gc*` + 滚动文件。

**Q2：一条 G1 Young GC 日志里你最先看哪几个字段？**
A：三个：①回收前后对 `128M->8M(512M)`——回收后是否贴上限；②停顿毫秒数——和目标比；③括号里的类型与成因——`(Normal)`/`(Mixed)`/`(Concurrent Start)`，以及有没有伴随 `To-space exhausted`。三个字段就能完成"健康/异常"初筛。

**Q3：jstat -gcutil 的 O 和 FGC 列怎么判断泄漏？**
A：单点值不能下结论。看趋势：`O` 每轮 Mixed 后回落有效（90%→60%）是正常回收；`O` 爬到 90%+ 后无论 YGC/Mixed 都压不下来、`FGC` 次数持续增长且每次 `FGCT` 增量在秒级，基本可判老年代回收失效——泄漏或堆严重不足，转 dump 分析。

**Q4：To-space exhausted 是什么？出现了怎么办？**
A：G1 疏散（复制存活对象）时预留的目标 Region 不够用，只能边复制边找新 Region 甚至放弃整理，导致本次停顿暴涨并加剧碎片。处理：加大堆（根治）、降低 IHOP 提前启动并发标记、提高 `G1ReservePercent` 多留缓冲、排查 Humongous 大对象。它频繁出现是"堆快满"的前兆信号。

**Q5：MaxGCPauseMillis 设得越小越好吗？**
A：不是。它是目标不是承诺，G1 靠缩小每次回收的 Region 集合来达标——代价是年轻代变小、GC 更频繁、吞吐下降。设得过低（如 <50ms）容易适得其反。正确姿势：先看业务能接受的 p99（本项目接口类 100~200ms 足够），把吞吐损失控制在合理范围。

**Q6：G1 的 Full GC 为什么比 Young GC 慢几十倍？**
A：Young/Mixed 只疏散部分 Region 且多线程并行；Full GC 是单线程的整堆标记-压缩（Stop The World 内完成全部整理），堆越大、存活对象越多停顿越长。它是 G1 所有前置机制（并发标记、Mixed、疏散缓冲）失效后的兜底，出现 Full GC 应视为"方案需要重新设计"而非"调个参数"。

**Q7：怎么用 GC 日志判断分配速率？**
A：取相邻两次 YGC 的时间差 Δt 和年轻代回收量 ΔEden，分配速率 ≈ ΔEden/Δt。日志里 `gc,heap` 行的 Eden 前值即当轮分配量。速率是选容量的依据：年轻代应容纳"一个 GC 间隔的分配量"，同时用"分配速率 × 对象期望存活时间"估算存活集合是否健康。

**Q8：停顿数据反过来指导参数的完整闭环是什么？**
A：测（`-Xlog:gc*` + jstat 建立停顿/频率/占用基线）→ 诊（对照故障形态：频繁 YGC、To-space exhausted、Full GC）→ 改（一次一个参数，按决策表）→ 复测（同负载压测对比 p99 与频率）。闭环的关键在"复测对照"，否则无法归因。

---

## 八、动手练习

1. 给 `ai-cs-chat` 的 `JAVA_OPTS` 追加 §2.1 的日志参数，压测（`scripts/loadtest/k6/sse-chat.js`）同时用 `jstat -gcutil 1 1000` 观察 30 秒，把 YGC 频率与 Eden 曲线截图进学习笔记。
2. 从 gc.log 提取全部 `Pause` 毫秒数，用 `sort -n` + `awk` 算 p50/p95/p99，与 k6 接口 p95 对比：GC 停顿占接口延迟的比例是多少？
3. 把 `-XX:MaxGCPauseMillis` 从默认 200 改成 30，重跑同一压测，对比 YGC 频率与吞吐变化，验证"目标设太小反而不划算"。
4. 故意把堆降到 `-Xmx256m` 复现 `To-space exhausted`（或构造大对象分配），然后按 §五决策表逐条调参救回来，记录每次改动的日志变化。
5. 读 `tools/nacos/bin/startup.sh:114` 与 `:118` 的新旧两套 GC 日志参数，写 3 行说明：统一日志相比 `-XX:+PrintGCDetails` 系列好在哪。

---

> 上一篇：[01-JVM参数与GC选型](./01-JVM参数与GC选型.md) ｜ 下一篇：[03-火焰图与性能剖析](./03-火焰图与性能剖析.md)
