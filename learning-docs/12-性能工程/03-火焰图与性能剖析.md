# 火焰图与性能剖析

> 对应项目：**无（⚠️ 工程未落地，本篇为目标态/预研方案）**——全仓 `grep -rni "async-profiler|asprof|StartFlightRecording|FlightRecorder|arthas"`（java/xml/yml/Dockerfile/sh，排除 learning-docs）**0 命中**，没有任何剖析工具被引入镜像、compose 或依赖；CPU 热点目前只能靠 `top -Hp` + `jstack` 粗查（[03-JVM内存模型与GC实战 §6 现场2](../01-Java基础/03-JVM内存模型与GC实战.md)）。
> 相关：[07-运维部署/04-Prometheus可观测性](../07-运维部署/04-Prometheus可观测性.md)（指标是"水位计"，剖析是"内窥镜"，互补）、[02-GC日志与停顿分析](./02-GC日志与停顿分析.md)、[08-性能问题定位方法论](./08-性能问题定位方法论.md)。
> **现状证据与要补什么**：证据如上 grep；要补的是 ①镜像引入 async-profiler（或至少 JFR 参数）②compose 透传挂载 ③一次基线剖析报告。§六给出具体落地步骤。

---

## 一、先给结论：剖析是"最后一公里"，但价值是十倍级的

`jstack` 是"快照"（抓一次线程栈，靠猜），火焰图是"统计画像"（采样几万次叠加成图，一眼定位热点）。排查"CPU 100% 但 jstack 看不出规律""接口慢但各段监控都正常"这类问题，剖析是终极大招。

| 你的处境 | 该用哪个 |
|---|---|
| 生产偶发 CPU 尖刺，事后想留证据 | **JFR**（常开低开销，事件留档） |
| 一次性定位某服务 CPU 热点函数 | **async-profiler**（开销最低，出火焰图） |
| 不想装任何东西，临时救火 | **Arthas profiler**（attach 即用，功能中等） |
| 本项目现状 | ⚠️ 三者都没落地——先用 JFR（零依赖）起步是最低成本路径 |

---

## 二、三个工具对比

| 维度 | async-profiler | JFR（JDK Flight Recorder） | Arthas profiler |
|---|---|---|---|
| 实现 | 基于 `perf_events`（ON-CPU）+ `AsyncGetCallTrace`，绕过安全点 | JVM 内置事件记录器，Oracle 贡献，商用级 | 基于字节码增强/内置 profiler 命令包装 |
| 采样 CPU | ✅（精度最高的生产方案） | ✅（方法采样 + 执行样本） | ⚠️ 粒度较粗 |
| OFF-CPU/锁/分配 | ✅ wall-clock、lock、alloc 四类全支持 | ✅ 事件模型丰富（io、lock、alloc 全有） | 部分（watch 命令可看锁） |
| 开销 | ~1% | 官方口径 <1%（可调 profile 模板） | attach 后 5~15%，复杂命令更高 |
| 产物 | `flamegraph.svg`（一步出图）、collapsd 栈 | `.jfr` 文件（JMC / `jfr print` 解析） | 火焰图或文本 |
| 依赖/侵入 | 需要 so 文件进容器 + `SYS_PTRACE` 权限 | **JDK 17 自带，零依赖**（本项目已满足：pom.xml:30 为 Java 17） | 需要 attach 一个 arthas 包 |
| 安全性 | attach 有暂停瞬间 | 完全可控，常开安全 | 动态改字节码有风险 |
| 典型命令 | `asprof -d 60 -f cpu.html <pid>` | `jcmd <pid> JFR.start duration=60s filename=a.jfr` | `profiler start` / `profiler stop --format html` |

**选型结论**：本项目 JDK 17 → **JFR 零依赖先跑通**；需要精确 ON-CPU 火焰图再上 async-profiler；Arthas 适合"临时看一眼"但别在生产长挂。

### JFR 事件类型速览（决定你开哪些记录）

| 事件 | 回答的问题 | 对应本项目场景 |
|---|---|---|
| ExecutionSample | CPU 时间花在哪些方法 | SHA-256/余弦/正则热点验证 |
| Allocation (TLAB/外部) | 对象在哪分配、什么类型占大头 | 大 context 字符串、float[] 向量（[04 §4.2](./04-内存泄漏排查实战.md) Humongous 预防） |
| Java Monitor Blocked / Lock | 锁等待在哪 | synchronized 段、池化资源竞争 |
| Socket Read/Write | 网络等待 | 等 LLM API / Feign 的 OFF-CPU 证据 |
| GC 相关（Pause/STW） | GC 停顿分布 | 与 `-Xlog:gc*` 交叉验证（[02](./02-GC日志与停顿分析.md)） |
| Thread Start/End | 线程创建风暴 | 池未复用、频繁建线程 |

`settings=profile` 模板已启用上述高频事件；想自定义用自定义 `.jfc` 模板（进阶，不展开）。

### 路线 C：Arthas 临时救火（不改镜像文件的折中）

```bash
# 目标态：把 arthas-boot.jar 挂进容器临时目录（compose volumes 挂载，不进镜像）
docker exec aics-chat-service java -jar /tmp/arthas-boot.jar    # attach 进程
# 会话内：
profiler start                       # 底层即 async-profiler 内核（新版本内置）
profiler stop --format html --output /tmp/cpu.html
thread -n 3                          # CPU 最高线程的栈（jstack 的易用版）
```

---

## 三、火焰图怎么读

### 3.1 四个铁律

```
        ┌─────────┬────────┬───────┐          ← 顶层叶子 = CPU 正在执行的函数
        │ Jackson │ 正则    │ SHA-256│             宽度越大 = 采样命中越多 = 越热
    ┌───┴─────────┴────────┴───────┴───┐      ← 每层是"调用它的父函数"
    │        chatStreamSse()            │
    │                                   │
    ┌───────────────────────────────────┐
    │        Tomcat worker (http-nio)   │      ← 最底层 = 线程入口
    └───────────────────────────────────┘
```

1. **宽度 = 占比**：某函数横向占 30% 宽 ≈ 它（含其子调用）消耗了 30% 的 CPU。
2. **栈自底向上**：最底下是线程入口（`main`/Tomcat worker），往上是被调用者。父框宽 = 它的所有下游都热。
3. **x 轴不是时间**：左右位置是按字母排序的调用树展开，**没有先后顺序含义**——这是新手最大误区。
4. **y 轴是栈深度**：层数越深，调用链越长；扁宽型火焰比瘦高型更容易定位。

### 3.2 找"平顶"（热点判据）

```
瘦高型（无热点）：          宽平顶（热点在此）：
┌────┐                     ┌──────────────────┐
│    │                     │  cosineSimilarity │  ← 顶又宽又平：CPU 全烧在这
│    │                     └──────────────────┘
┌───┴────┐                 ┌─────────────────────┐
│ chainA │                 │  RAG retrieve        │
```

**平顶 = 采样时大量线程停在同一函数自身**——就是热点。宽但没有平顶（每一层均匀）= 热点分散在下游，继续看子树。自底向上看宽度递减是正常的；**某层突然比父层还宽是不可能的**（子调用时间含于父）。

### 3.3 本项目预判的热点候选（基于代码，非实测）

| 候选 | 代码依据 | 类型 |
|---|---|---|
| 向量余弦相似度 O(n) 线性扫描（n≤200） | `ai-cs-chat/.../cache/SemanticCacheService.java`（[11-数据结构与算法/09](../11-数据结构与算法/09-缓存淘汰-LRU-LFU与两级缓存.md) 分析过：刻意不用 HNSW） | ON-CPU |
| 文本 SHA-256（每次向量查询都算） | `VectorCacheStore.java:105-118` | ON-CPU |
| PII 正则掩码（长文本回溯风险） | `ai-cs-chat/.../util/PiiMasker.java` | ON-CPU |
| Jackson 序列化/反序列化（Redis 缓存 JSON） | `VectorCacheStore.java:70` `:90` | ON-CPU |
| GC 线程（`GC task thread` 若占宽） | 堆 512m 高分配速率时 | ON-CPU |
| 等待 LLM/下游 Feign 响应 | `ResilientAiService` 调用链 | **OFF-CPU**（不在火焰图上，见 §四） |

> ⚠️ 上表是**从代码推断的假设**，不是测量结论——这正是"工程未落地"的含义。落地剖析后用实测修正它。

---

## 四、ON-CPU vs OFF-CPU

| | ON-CPU 剖析 | OFF-CPU 剖析 |
|---|---|---|
| 测什么 | CPU 正在执行什么代码 | 线程在**等待**什么（锁、IO、下游响应） |
| 采样对象 | 运行态（Running）线程 | 阻塞/等待态线程 |
| 工具 | `perf_events`（async-profiler 默认） | async-profiler `wall` 模式、JFR 的 io/lock 事件 |
| 图形 | 经典火焰图 | 火焰图（栈顶=等待发生处）或锁竞争图 |
| 典型结论 | "80% CPU 在序列化" | "Tomcat 线程 60% 时间在等 Feign" |
| 本项目对照 | §3.3 的 SHA-256/正则/JSON | SSE 等待 LLM 首 token、`ChatServiceImpl` 里 `futureFlux.get()`、Hikari 拿连接 |

**为什么这个二分重要**：接口慢 ≠ CPU 忙。对话链路的大头时间在**等 LLM**（OFF-CPU），ON-CPU 火焰图上根本看不见——此时优化 CPU 热点毫无收益，该优化的是缓存命中（[06 §四](./06-接口性能优化实战.md)）。先判断忙在 CPU 还是忙在等，再选工具。

经验法则：CPU 利用率高 → ON-CPU；CPU 低但吞吐也低/延迟高 → OFF-CPU。

**剖析前的两分钟定性判断流**（决定采哪种图）：

```
CPU 利用率？ ──高──→ ON-CPU 火焰图：找平顶 → 热点函数
     │低
响应慢且吞吐低？ ──是──→ wall/OFF-CPU：栈顶看"在等谁"
     │否（只是吞吐上不去）
压测打不满？ ──是──→ 看线程池/连接池饱和度（[07](./07-线程池调优与背压.md) §五§六）
```

---

## 五、采样偏差：火焰图的三个坑

1. **安全点偏差（Safepoint Bias）**：JVM 自带采样器（含部分 JFR 模式）只在线程到达安全点时记录栈。热点若在无安全点的长循环里（如大数组数值计算——恰好是余弦相似度这种），样本会被"推迟"到循环结束才记，栈貌失真。**async-profiler 用 `perf_events` 直接采样，天然绕开此偏差**——这是它被推崇为"生产级 CPU 剖析金标准"的原因。
2. **低频短方法测不到**：采样率 100Hz 时，单次耗时 <10ms 的方法大概率不出现。 Flame 图上没有 ≠ 不存在；要看清就说"这段时间的 CPU 主要花在哪"，不是"代码跑了几次"。
3. **时段/负载偏差**：冷启动期、缓存全空的压测初期采样，结论不能代表稳态。剖析必须先预热（对照 [08-性能压测实战](../08-测试/05-性能压测实战-k6与JMeter.md) 的阶梯加压思路），在稳态区间采满 60s 以上。

附带：GC 线程在火焰图上占宽大时，先解决 GC 问题再谈业务热点（[02](./02-GC日志与停顿分析.md)），否则你优化的是"被 GC 放大的代码"。

---

## 六、目标态落地：在 docker-compose 服务里挂剖析工具

> 以下全部是**目标态方案**（现状未落地）。两条路线，建议先 A 后 B。

### 路线 A：JFR 起步（零依赖，当天可做）

```bash
# 1. 不改任何文件，直接对运行中的容器开 60 秒记录（JDK17 自带 jcmd）
docker exec aics-chat-service jcmd 1 JFR.start name=cpu duration=60s \
    filename=/tmp/chat-cpu.jfr settings=profile

# 2. 期间用 k6 打流量制造负载
k6 run -e BASE=http://localhost:8080 -e PATH=/api/chat/stream scripts/loadtest/k6/sse-chat.js

# 3. 取出并解析（宿主机 JDK17 自带 jfr 命令）
docker cp aics-chat-service:/tmp/chat-cpu.jfr .
jfr print --events ExecutionSample chat-cpu.jfr | head -50
jfr summary chat-cpu.jfr     # 看事件类型分布：执行样本/分配/锁各多少

# 4. 要图形化：JMC（JDK Mission Control）打开 .jfr，或转火焰图工具渲染
```

若想"开机即录、事后可查"，在 `docker-compose-all.yml` 对应服务的 `JAVA_OPTS` 追加：

```
-XX:StartFlightRecording=maxsize=100m,maxage=1h,dumponexit=true,filename=/app/logs/aics.jfr
```

### 路线 B：async-profiler 进镜像（生产金标准）

```dockerfile
# ① 改造 ai-cs-chat/Dockerfile：下载并解压 async-profiler 发行包
ADD https://github.com/async-profiler/async-profiler/releases/download/v3.0/asprof-3.0-linux-x64.tar.gz /tmp/asprof.tar.gz
RUN tar -xzf /tmp/asprof.tar.gz -C /opt && mv /opt/asprof-3.0-linux-x64 /opt/asprof
```

```yaml
# ② compose 里给服务补两项（以 docker-compose-all.yml 的 ai-chat-service 为例）
    ai-chat-service:
      image: ai-cs-chat:${VERSION:-latest}
      # …原 environment 略（JAVA_OPTS 见 :236）…
      pid: "host"          # ③ perf 采样需要 host pid 命名空间
      cap_add:
        - SYS_PTRACE       # ④ attach 权限
```

```bash
# ⑤ 采 60 秒 CPU 火焰图（容器内执行）
docker exec aics-chat-service /opt/asprof/bin/asprof -d 60 -f /tmp/cpu.html 1

# ⑥ OFF-CPU（wall clock）火焰图：看线程都在等谁
docker exec aics-chat-service /opt/asprof/bin/asprof -d 60 -e wall -f /tmp/wall.html 1

# ⑦ 分配剖析：找大对象来源（对应 02 §4.2 的 Humongous 排查）
docker exec aics-chat-service /opt/asprof/bin/asprof -d 60 -e alloc -f /tmp/alloc.html 1

docker cp aics-chat-service:/tmp/cpu.html .   # 浏览器打开
```

**验收标准**：一次 60s 稳态采样产出 cpu.html/wall.html，能对 §3.3 候选表逐条给出"实测占比"，并把结论回填到本文档——这就算把 ⚠️ 消掉了。

> 落地后的第一件事：把 §3.3 候选表从"推断"改标为"实测"，未验证的猜测留在表里就是下一轮排查的噪音源。

---

## 七、面试高频问答

**Q1：火焰图怎么读？宽度、颜色、y 轴分别代表什么？**
A：宽度 = 采样命中占比（约等于 CPU 时间占比）；y 轴 = 调用栈深度，自底向上是调用方向，顶上是正在执行的叶子；颜色默认按包名/随机着色，**不含时间与热度语义**。核心动作是找"宽且平顶"的框——大量样本停在的那个函数就是热点。x 轴不是时间轴。

**Q2：async-profiler、JFR、Arthas 怎么选？**
A：JFR 是 JVM 内置、零依赖、可常开、事件模型全（CPU/分配/锁/IO），适合留档与常规剖析；async-profiler 基于 perf_events + AsyncGetCallTrace，无安全点偏差、开销 ~1%、一步出火焰图，是精确 CPU 剖析金标准，但需要 so 进容器与 ptrace 权限；Arthas 胜在 attach 即用、命令丰富，适合临时救火，字节码增强开销与风险更高。常规组合：JFR 常开留档 + async-profiler 定点深挖。

**Q3：什么是安全点偏差？谁解决了它？**
A：部分采样器（含 JVM 自带方法采样）只在线程运行到 safepoint 时才能取栈。热点若在无安全点插入的长循环（纯数值计算）里，采样被推迟到循环出口，导致该热点在数据里"消失"或被记到循环外的调用者。async-profiler 改用 perf_events 在信号处理里直接采样栈，不依赖安全点，从而消除该偏差。

**Q4：CPU 利用率不高但接口很慢，火焰图有用吗？**
A：经典 ON-CPU 火焰图没用（没人占 CPU），该用 OFF-CPU/wall-clock 剖析：它对等待态线程采样，栈顶显示线程在等什么——锁、socket 读（下游响应）、连接池获取。AI 客服场景里"等 LLM 首 token、等 Feign"都属此类，表现是 CPU 低、延迟高、吞吐上不去，优化方向是缓存与并行化而非热点函数。

**Q5：采样式剖析会不会漏掉短方法？结果可信吗？**
A：会。100Hz 采样下单次 <10ms 的方法很难被记录。但剖析回答的是"时间都花在哪"这个概率问题：高频调用的短方法会因调用次数多而累计出宽度，真正孤立的 10ms 事件对总量影响本来就小。需要确认单次路径时用跟踪型手段（JFR 的 method tracing、Arthas trace）补足。

**Q6：本项目为什么"工程未落地剖析工具"，最小成本补法是什么？**
A：现状全仓 grep 无 async-profiler/JFR/Arthas 任何踪迹，CPU 定位只能 jstack 快照。最小成本补法是 JFR：JDK 17 自带 jcmd，`docker exec <c> jcmd 1 JFR.start duration=60s settings=profile` 一条命令即可采样，零镜像改动；之后再把 async-profiler 按镜像 + `pid: host` + `SYS_PTRACE` 三步引入，获得无偏差火焰图。

**Q7：火焰图上 GC 线程占了很大宽度，下一步做什么？**
A：说明 CPU 被回收活动吃掉，业务热点被掩盖。先按 GC 数据处理：看 `-Xlog:gc*` 的停顿与频率、`jstat -gcutil` 的老年代形态，判断是堆太小、分配速率过高还是泄漏（见 02/04 两篇），解决后再重新采样——否则火焰图上优化的是"被 GC 放大的业务代码"。

**Q8：分配剖析（alloc 火焰图）能回答什么 CPU 剖析回答不了的问题？**
A：它回答"对象在哪里被创建、什么类型占大头"，用于降低分配速率与 GC 压力、定位内存抖动和大对象（Humongous）来源。CPU 图只显示执行时间不显示对象生产量——一个每秒造 1GB 临时对象的代码在 CPU 图上可能只是窄窄一条，alloc 图上却是巨宽顶。

---

## 八、动手练习

1. 零改动落地路线 A：`docker exec aics-chat-service jcmd 1 JFR.start duration=60s settings=profile filename=/tmp/a.jfr`，同时压 `sse-chat.js`，用 `jfr summary` 列出事件类型分布。
2. 用 `jfr print --events ExecutionSample a.jfr | grep -A5 "com.aics.chat"` 粗看热点栈，对照 §3.3 候选表，验证哪条猜对了。
3. 预研路线 B：fork 思路改一份 `Dockerfile.async`（只加 async-profiler，不动原 Dockerfile），本地构建后在 compose 里试采 `cpu.html`——注意先给容器加 `cap_add: [SYS_PTRACE]`。
4. 分别采 ON-CPU（默认）与 wall 模式两张图，回答：对话链路的时间到底花在"算"还是"等"上？各举一个证据框。
5. 给 `-XX:StartFlightRecording=maxsize=100m,maxage=1h` 的常开方案算一笔账：磁盘占用、性能开销、事故后能回溯的时长，写 5 行 ADR 判断值不值得常开。

---

> 上一篇：[02-GC日志与停顿分析](./02-GC日志与停顿分析.md) ｜ 下一篇：[04-内存泄漏排查实战](./04-内存泄漏排查实战.md)
