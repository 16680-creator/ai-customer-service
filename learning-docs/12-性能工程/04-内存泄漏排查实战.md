# 内存泄漏排查实战

> 对应项目：`ai-cs-chat/.../cache/VectorCacheStore.java:48-54`（**有界 LRU 正例**：`removeEldestEntry` 超容量即淘汰）、`ai-cs-chat/.../agent/AgentController.java:177-180`（**ThreadLocal 清理正例**：`finally` 里 `ChatUserContext.clear()`）、`docs/05-部署上线.md:52-53`（OOM 自动 dump 示例，**实际 Dockerfile 未落地** ⚠️）。
> 相关：[03-JVM内存模型与GC实战](../01-Java基础/03-JVM内存模型与GC实战.md)（MAT 三步法速览在 §5.3，本篇深化到支配树实操与五大泄漏源）、[09-缓存淘汰-LRU-LFU与两级缓存](../11-数据结构与算法/09-缓存淘汰-LRU-LFU与两级缓存.md)（LRU 原理与本项目两级缓存）、[02-GC日志与停顿分析](./02-GC日志与停顿分析.md)（怀疑泄漏前的 GC 证据）、[03-火焰图与性能剖析](./03-火焰图与性能剖析.md)（alloc 剖析可替代性预防）。

---

## 一、先给结论：泄漏的发现、取证、定罪三段式

| 阶段 | 动作 | 依据 |
|---|---|---|
| **发现** | `jstat -gcutil`：`O` 老年代爬到 90%+ 且 Mixed/YGC 压不下来、`FGC` 持续增长；或容器 RSS 单调上涨直至 OOMKilled | [02 §三](./02-GC日志与停顿分析.md) 的异常形态表 |
| **取证** | OOM 自动 dump（最佳）/ `jcmd GC.heap_dump`（安全）/ `jmap -dump`（慎用） | 本文 §二 |
| **定罪** | MAT 泄漏报告 → 支配树按 Retained 排序 → Path to GC Roots 找持有者 | 本文 §三 |

**核心心法**：内存泄漏 = **对象"用完了但 GC 够不着它的引用链"**。GC Roots（线程栈、静态字段、JNI 等）到死对象之间有一条活引用。所有排查手段本质都在回答：**这条链是谁牵着的？**

---

## 二、heap dump 获取的四种方式

| 方式 | 命令 | 优点 | 风险/限制 |
|---|---|---|---|
| ① OOM 自动 dump | `-XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/app/logs/` | 案发瞬间自动留现场，零人工 | 必须提前配好；**容器 OOMKilled（exit 137）不触发**——那是内核杀进程，JVM 来不及反应 |
| ② jcmd | `jcmd <pid> GC.heap_dump /tmp/heap.hprof` | 官方推荐，与 jmap 等价但更稳 | dump 期间长时间 STW，小堆（512m）约数秒 |
| ③ jmap | `jmap -dump:live,format=b,file=heap.hprof <pid>` | 教科书命令 | `:live` 会**先触发一次 Full GC**——生产高负载时这一下可能就是压垮的最后一根稻草；不带 `live` 则 dump 垃圾对象，文件更大 |
| ④ Arthas | `heapdump /tmp/heap.hprof` | 与 jmap 等价，交互友好 | 同样的 STW 风险 |

**⚠️ 本项目现状（改造点）**：`docs/05-部署上线.md:50-54` 的 ENTRYPOINT 示例里写了 `-XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/app/logs/`，但 9 份真实 Dockerfile（`ai-cs-chat/Dockerfile:21` 等只透传 `JAVA_OPTS`）与 compose 的 `JAVA_OPTS`（`docker-compose-all.yml:175-294`）都**没有**这两个参数——按 §六命令补上，OOM 才有现场可查。

**容器取 dump 的完整路径**：

```bash
# 事前：确认 HeapDumpPath 目录存在且可写；事后：
docker cp aics-chat-service:/app/logs/java_pid1.hprof ./heap.hprof
```

> 文件大小 ≈ 堆大小（512m 堆约 500MB 文件）， compose 场景注意挂载到宿主机卷，别撑爆容器可写层。

---

## 三、MAT 实战：支配树与泄漏报告

> [03-JVM内存模型与GC实战 §5.3](../01-Java基础/03-JVM内存模型与GC实战.md) 给了三步法骨架；本节展开每一步"看什么数字、下什么结论"。

### 3.1 第一步：Leak Suspects 自动报告

打开 hprof → Overview → Leak Suspects Report。MAT 会给一张嫌疑饼图，典型句式：

```
One instance of "com.aics.chat.cache.XxxStore" loaded by ... occupies 412MB (78%).
The memory is accumulated in one instance of "java.util.LinkedHashMap$Entry" ...
Keywords: java.util.LinkedHashMap$Entry; java.util.HashMap$Node
```

**判读**：报告说"某类的一个实例占了 78% 堆，大头是一个集合"。它只给嫌疑不给结论——下一步人工验证。

### 3.2 第二步：支配树（Dominator Tree）——本节核心

先分清两个指标（面试必考）：

| 指标 | 定义 | 用途 |
|---|---|---|
| Shallow Heap | 对象自身占的浅层内存（引用 4/8 字节，数组算内容） | 看单对象大小 |
| **Retained Heap** | **若回收该对象，能连带释放的总内存**（它独占支配的子图） | **找"一个拖死一片"的元凶** |

```
支配树示例（Retained Heap 降序）：
Retained=420MB  ← ThreadLocalMap（它一释放，连带 420MB 都能回收）
 └─ Retained=418MB  ← VectorCacheStore.l1Cache (LinkedHashMap)
     └─ Retained=410MB  ← 4096 个 Entry，每个 value 是 float[4096]
```

操作：MAT → Dominator Tree → 按 Retained Heap 倒序 → 展开最顶部几层。凡是"Retained 占比 > 30% 且业务上不该这么大"的节点即为嫌疑人。

### 3.3 第三步：Path to GC Roots——定罪引用链

对嫌疑对象右键 → **Path to GC Roots → exclude weak/soft references**（弱/软引用会被 GC 自行清理，排除后才看真凶）。

```
java.util.LinkedHashMap$Entry
  ← l1Cache: java.util.Map          (VectorCacheStore 字段)
  ← instance: com.aics.chat.cache.VectorCacheStore   (@Component 单例)
  ← java.lang.Class                 (静态上下文 = GC Root)
  ★ 结论：@Component 单例的 Map 字段持有全部条目 → 是否该有界/该淘汰？
```

读链的诀窍：看**倒数第二个节点**（直接持有者），改代码就是改它（加容量上限/改弱引用/及时 remove）。

### 3.4 OQL 与类直方图（补充手段）

```sql
-- MAT OQL：找所有超过 10 万个实例的类（配合 Histogram 使用）
SELECT * FROM java.util.LinkedHashMap$Entry e WHERE e.@retained > 1048576
```

`Histogram`（类直方图）按类聚合实例数与浅层大小：`byte[]`/`char[]`/`java.lang.Class` 异常多时分别指向大文本缓存、字符串驻留、类加载器泄漏。

---

## 四、五大常见泄漏源与本项目对照

### 4.1 无界 Map / 缓存（最常见）

```java
// ❌ 反面教材（[03-JVM内存模型与GC实战 §6 现场1](../01-Java基础/03-JVM内存模型与GC实战.md) 同款）
private final Map<String, ChatResponse> cache = new HashMap<>();   // 永不清理

// ✅ 本项目正例：VectorCacheStore（见 §五逐行走读）
```

**特征**：`O` 老年代只进不出；MAT 支配树顶端是某个 `ConcurrentHashMap$Node` / `LinkedHashMap$Entry` 集合。
**对治**：容量上限 + 淘汰策略（LRU/LFU）；或换 Caffeine（`maximumWeight` + `expireAfterAccess`）。

### 4.2 ThreadLocal（线程池放大器）

**机制**：每个线程自带 `ThreadLocalMap`，key 是 ThreadLocal 的**弱引用**、value 是**强引用**。线程池里线程永生 → 忘了 `remove()` 的 value 永远跟着线程走。key 被回收后还出现 `key=null` 的"孤儿 entry"，经典泄漏形态。

```java
// ✅ 本项目正例：AgentController.java:146-180
void runAgentTurn(SseEmitter emitter, Long uid, AgentRequestDTO request) {
    try {
        ChatUserContext.setUserId(uid);        // :148 异步线程里注入身份
        ...
    } finally {
        ChatUserContext.clear();               // :177-180 兜底清理，防线程池复用串号
    }
}
```

`ChatServiceImpl` 类注释（`:98-105`）明确写了同款设计原因："ThreadLocal 在异步线程中无法传递（**线程池复用导致数据污染**）……Controller 的 finally 块中清理 ThreadLocal"——业务正确性与内存安全一举两得。
**排查特征**：MAT 里搜 `ThreadLocalMap$Entry` 实例数量是否 ≈ 活跃线程数 × 存活业务对象数。

### 4.3 静态集合

```java
// ❌ 只增不减：static Map 常驻到进程死亡
private static final Map<String, byte[]> auditLog = new ConcurrentHashMap<>();
```

**特征**：支配树顶端是 `Class` 对象下面的 static 字段；**对治**：上限+淘汰（同 4.1）、或改用"环形最近 N 条"（本项目 `SecurityAuditRecorder` 用 `ArrayDeque` 超容量 `removeFirst()` 实现有界留痕，见 [11-数据结构与算法/02](../11-数据结构与算法/02-线性结构-数组链表栈队列.md)）。

### 4.4 监听器 / 回调未注销

```java
// ❌ 注册后忘了 removeListener：发布者持有订阅者强引用
eventBus.register(bigListener);     // ...永远没有 unregister
```

**特征**：支配树里出现大量 listener/`Consumer` 被"服务级单例"持有；Spring 场景注意 `ApplicationListener`/`@EventListener` 由容器管理不算此坑，坑在**自己手写的 EventBus/观察者列表**。
**对治**：注册与注销成对出现（try/finally 或生命周期钩子 `@PreDestroy`）；Spring Bean 依赖注入天然注销。

### 4.5 类加载器泄漏（Metaspace 溢出）

**机制**：每加载一个类，其 Class 对象引用定义它的类加载器；类加载器要回收必须其加载的**所有**类都不可达。动态生成类（CGLIB 代理、Groovy/热部署、重复 `defineClass`）在容器频繁 redeploy 或动态代理失控时把 Metaspace 顶爆——表现为 `M` 列 95%+、`OutOfMemoryError: Metaspace`。
**排查**：MAT 里 `java.lang.ClassLoader` 子类数量异常多、或某 ClassLoader 下的类数量持续增长。
**对治**：`-XX:MaxMetaspaceSize` 封顶让它显式爆炸（[01 §四](./01-JVM参数与GC选型.md)）；动态代理缓存化（Spring 已做）。

---

## 五、正例源码走读：VectorCacheStore 为什么不会泄漏

```java
// ai-cs-chat/.../cache/VectorCacheStore.java:48-54
private final Map<String, float[]> l1Cache = Collections.synchronizedMap(
        new LinkedHashMap<>(128, 0.75f, true) {          // accessOrder=true → LRU
            @Override
            protected boolean removeEldestEntry(Map.Entry<String, float[]> eldest) {
                return size() > Math.max(1, properties.getVector().getL1MaxEntries());
            }                                            // ⭐ 容量上限来自配置，不是魔法数
        });
```

**防泄漏的四道设计**（对应 §4.1 的每个风险点）：

| 风险点 | VectorCacheStore 的答案 | 出处 |
|---|---|---|
| Map 无界 | `removeEldestEntry` 超容量淘汰最久未访问条目，**条目数硬上限** | `:51-53` |
| 上限写死 | 容量读配置 `l1MaxEntries`（`application.yml:175` = 4096），改配置不改代码 | `:52` |
| value 无限大 | value 是定长 `float[]`（Embedding 维度固定），单条内存可预算：4096 维 × 4B ≈ 16KB，总量上限 ≈ 4096 × 16KB ≈ **64MB**——堆预算事先可知 | `:48` |
| key 无限长 | key 用 SHA-256 定长十六进制（64 字符），超长文本不撑大 key | `:101-103` |

再加上 L2 层 Redis 侧的 TTL 兜底（`application.yml:176` `ttl-hours: 168`，写入时 `Duration.ofHours(...)` 见 `:91-92`），整个两级缓存**每一层都有界**。语义缓存/热榜缓存同理：`application.yml:181` `max-entries: 200`、`:186` `promote-threshold: 3`——分布式侧也有淘汰上限（ZSET LRU 实现，见 [11-数据结构与算法/09 §6.2](../11-数据结构与算法/09-缓存淘汰-LRU-LFU与两级缓存.md)）。

**这就是"防泄漏要从数据结构设计开始"的完整样本**：有界、可配、可预算、双兜底。

---

## 六、可复制命令：一次完整泄漏排查

```bash
# 0.（一次性）给目标服务补 OOM 自动 dump：JAVA_OPTS 追加
#    -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/app/logs/

# 1. 发现：实时看老年代形态
docker exec aics-chat-service jstat -gcutil 1 2000 15

# 2. 取证：安全 dump（512m 小堆，STW 数秒，压测期避开）
docker exec aics-chat-service jcmd 1 GC.heap_dump /app/logs/leak.hprof
docker cp aics-chat-service:/app/logs/leak.hprof .

# 3. 快速初筛（不开 MAT 先看类直方图）
jmap -histo:live leak.hprof | head -15     # 离线分析不触发 Full GC，放心用

# 4. MAT：Leak Suspects → Dominator Tree(按 Retained 倒序)
#    → Path to GC Roots (exclude weak/soft) → 定罪直接持有者

# 5. 修复后验证：同一负载压测，对比 O 列是否稳定回落
```

---

## 七、面试高频问答

**Q1：什么是内存泄漏和内存溢出的区别与联系？**
A：泄漏（Leak）是对象不再使用却仍被 GC Roots 引用链持有、无法回收，属于"该死没死"；溢出（OOM）是内存不够分配新对象，属于"装不下了"。联系：泄漏是溢出最常见的原因之一，持续泄漏最终耗尽堆触发 `OutOfMemoryError`（或堆外部分触发容器 OOMKilled）；但溢出不一定由泄漏引起——堆配小了、瞬时大对象也会溢出。

**Q2：容器里 Java 进程 exit 137，heap dump 参数为什么没生成 dump？**
A：exit 137 = 内核 OOM Killer 杀进程（cgroup RSS 超限），不是 JVM 主动判定内存耗尽，`-XX:+HeapDumpOnOutOfMemoryError` 只响应 JVM 内部的 `OutOfMemoryError`，根本没机会执行。这意味着**堆外内存（元空间/线程栈/直接内存）失控时该参数是无效的**，要靠容器层（docker inspect 的 OOMKilled 字段）识别，并在堆外预算与监控上做预防。

**Q3：Shallow Heap 和 Retained Heap 的区别？为什么排查用后者排序？**
A：Shallow 是对象自身占用；Retained 是"回收它会连带释放多少"——即它支配的、不被其他 GC Roots 直达的子图总和。排查用 Retained 排序是因为泄漏元凶的特征是"一个入口拖住一大片"（如一个 Map 持有百万 Entry），按 Shallow 排序它只是个几十字节的 Map 头，按 Retained 才能一眼锁定。

**Q4：ThreadLocal 为什么会泄漏？怎么防？**
A：ThreadLocalMap 的 key 是对 ThreadLocal 的弱引用、value 是强引用。线程池线程长期存活：①不 remove 则 value 永久可达；②key 被回收后 value 成孤儿仍占内存。防法三条：用完 `finally` 里 `remove()`（本项目 AgentController.java:177-180 的做法）；用完后置 `threadLocal = null` 让 key 连同 entry 失效；业务侧尽量用显式参数传递而非 ThreadLocal 跨层携带。

**Q5：MAT 排查泄漏的完整步骤？**
A：①Leak Suspects 自动报告拿嫌疑；②Dominator Tree 按 Retained 倒序找"拖死一片"的节点；③对嫌疑对象 Path to GC Roots（exclude weak/soft）看谁直接持有；④结合业务判断该持有者是否应该有界/清理；⑤Histogram + OQL 辅助确认异常类。报告只给嫌疑，定罪要人工对业务语义。

**Q6：写内存缓存时要注意什么才能不泄漏？**
A：四件套：有容量上限（LRU/LFU 淘汰，如本项目 VectorCacheStore 的 removeEldestEntry）；value 可预算（定长类型、避免挂大对象）；key 定长化（哈希截断）；带 TTL 或弱引用兜底。更进一步用 Caffeine 的 maximumWeight + expireAfterAccess。对照反面：无界 HashMap 缓存是最经典的泄漏源。

**Q7：Metaspace 泄漏是怎么发生的？**
A：类加载器回收的前提是其加载的所有类都不可达。动态生成类（CGLIB、热部署、脚本引擎）若被静态集合或长生命周期对象持有，类加载器就无法回收，反复 redeploy/动态代理会持续累加 Class 对象直至 Metaspace 溢出。排查看 MAT 的 ClassLoader 实例与各类加载器下类数量；预防是 MaxMetaspaceSize 封顶 + 控制动态类生成。

**Q8：`jmap -dump:live` 为什么生产慎用？**
A：`:live` 会先触发一次 Full GC 才 dump——生产高负载下这次主动 Full GC 可能带来秒级停顿，放大事故。替代顺序：优先依赖 OOM 自动 dump（案发自动留现场）；需要人工取时用 `jcmd GC.heap_dump`（同样 STW 但行为更可控），并避开流量高峰；离线分析直方图用 `jmap -histo` 的文件输出。

---

## 八、动手练习

1. 故意造泄漏：写一个测试端点往 `static Map<String, byte[]>` 里每请求塞 1MB 不清理，`-Xmx256m` 启动，用 `jstat -gcutil` 观察 O 与 FGC，直到 OOM——确认 dump 文件自动生成在 HeapDumpPath。
2. 用 MAT 分析练习 1 的 dump：记录 Leak Suspects 的嫌疑描述、支配树顶端节点的 Shallow/Retained 值、Path to GC Roots 的完整链条。
3. 读 `VectorCacheStore.java:48-54` 与 `application.yml:173-176`，手算 L1 缓存的内存上限（4096 条 × 4096 维 float），并给出"把 L1 调到 16384 条需要多大堆"的预算结论。
4. 把练习 1 的反例改造成 LRU 有界缓存（照抄 `removeEldestEntry` 模式），重跑相同压测验证 O 列稳定回落。
5. 写一个"ThreadLocal 清理"检查清单：在本项目里 grep 所有 `setUserId`/`ThreadLocal` 用点，逐个确认是否在 finally 清理（正例参照 `AgentController.java:177-180`）。

---

> 上一篇：[03-火焰图与性能剖析](./03-火焰图与性能剖析.md) ｜ 下一篇：[05-SQL调优与执行计划](./05-SQL调优与执行计划.md)
