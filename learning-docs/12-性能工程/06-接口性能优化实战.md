# 接口性能优化实战

> 对应项目：`ai-cs-chat/.../service/impl/ChatServiceImpl.java:513-567`（对话链路三级缓存命中）、`ai-cs-order/.../service/impl/OrderServiceImpl.java:92-170`（订单链路：循环扣库存/循环 insert 现场改造点）、`ai-cs-chat/.../service/impl/ChatHistoryServiceImpl.java:48-75`（会话历史 Redis 优先读 + MQ 异步落库）。
> 相关：[05-SQL调优与执行计划](./05-SQL调优与执行计划.md)（SQL 层怎么慢的）、[09-缓存淘汰-LRU-LFU与两级缓存](../11-数据结构与算法/09-缓存淘汰-LRU-LFU与两级缓存.md)（缓存结构原理）、[07-线程池调优与背压](./07-线程池调优与背压.md)（并行化的线程从哪来）、[08-性能问题定位方法论](./08-性能问题定位方法论.md)（优化前后怎么测）。

---

## 一、先给结论：接口慢的六类原因与优化顺序

| 原因 | 典型症状 | 本项目案例 | 优化方向 |
|---|---|---|---|
| N+1 查询/调用 | 循环里发 SQL/Feign | `OrderServiceImpl.java:105-107` 循环扣库存 | 批量化（§二） |
| 串行等无依赖调用 | 各段耗时相加 | 订单创建里的校验类调用 | 并行化（§三） |
| 重复算/重复调 | 相同输入反复全链路 | 同一问题重复走完整 RAG | 缓存分层（§四） |
| 大对象传输/序列化 | 响应体大、GC 压力大 | RAG 大 context、历史消息 | 裁剪+流式（§五） |
| 连接池配置不当 | 高并发下等连接 | 未显式配置（改造点） | 参数化（§六） |
| 下游慢 | 自己没错但延迟高 | LLM 秒级响应 | 超时/熔断/降级（→[07 §八](./07-线程池调优与背压.md)） |

**顺序纪律**：先测量定位瓶颈段（[08](./08-性能问题定位方法论.md)），再按下表从左到右处理——**缓存 < 批量 < 并行 < 参数 < 重构**，边际成本递增。

---

## 二、N+1 查询与批量写入（本项目真实改造点）

### 2.1 现场：订单创建的两组循环

```java
// ai-cs-order/.../OrderServiceImpl.java:105-107 —— 循环 Feign（远程调用 N+1）
for (CartItem item : cartItems) {
    productClient.deductStock(item.getProductId(), item.getQuantity());
}

// ai-cs-order/.../OrderServiceImpl.java:132-142 —— 循环 insert（写入 N+1）
for (CartItem item : cartItems) {
    OrderItem orderItem = new OrderItem();
    // ... 8 行字段赋值 ...
    orderItemMapper.insert(orderItem);        // 每件商品一次网络往返 + 一次事务内 SQL
}
```

**为什么慢**：购物车 10 件商品 = 10 次 HTTP（Feign）+ 10 次 INSERT。每次 Feign 有 TCP/序列化/网络开销（几 ms 级），每次 insert 有一次往返；串行叠加后，DB 与网络延迟直接乘以 N。

### 2.2 改造方案（目标态，项目未落地——grep 全仓无 `saveBatch` 调用）

```java
// ① 批量扣库存：给 product 服务加批量接口，一次 Feign 传全量
productClient.deductStockBatch(items.stream()
        .map(i -> new StockDeductDTO(i.getProductId(), i.getQuantity())).toList());
// 服务端仍可用行级原子 UPDATE 保证并发正确（锁语义见 05-MySQL锁 §3.2）

// ② 批量插入订单项：MyBatis-Plus saveBatch 或手写 foreach insert
orderItemService.saveBatch(orderItems);       // 默认 batchSize=1000，一次往返插 N 行
```

**注意两个坑**：
- `saveBatch` 本质是 JDBC batch——确认 JDBC URL 带上 `rewriteBatchedStatements=true`，否则 MySQL 驱动仍逐条发，白改；
- 批量扣库存失败时**整批回滚**的语义要与 Seata 分支事务兼容（`OrderServiceImpl.java:99-104` 注释里 Seata AT 的 XID 传播机制照旧适用）。

**收益估算模板**：10 件商品，Feign 单次 3ms、insert 单次 2ms → 改前 `10×3+10×2=50ms`，改后 `3+2=5ms`，**10 倍**。链路越长、N 越大，批量收益越显著。

---

## 三、CompletableFuture 并行调用

### 3.1 本项目已有的并行范式

```java
// ai-cs-chat/.../ResilientAiService.java:176 —— 非流式 LLM 调用的异步模板
return CompletableFuture.supplyAsync(() -> {
    TraceContextHolder.restore(captured);     // 异步线程恢复 trace 上下文
    // 路由 → 逐模型 fallback → 观测 ...
});

// ChatServiceImpl.java:584-601 —— SSE 链路拿到 CompletableFuture<Flux<String>> 后 .get()
CompletableFuture<Flux<String>> futureFlux = resilientAiService.callSseRagStream(...);
Flux<String> flux = futureFlux.get();         // 受 TimeLimiter 保护，不会无限等待
```

> 两个细节值得记住：①异步前必须手动捕获 ThreadLocal（`ResilientAiService.java:173-175` 的 `TraceContextHolder.capture()`），线程池不传播上下文；②`supplyAsync` 不传 executor 走 **ForkJoinPool.commonPool**——通用任务可以，SSE 编排这类长任务应换专用池（[07 §七](./07-线程池调优与背压.md)）。

### 3.2 并行化改造：无依赖调用用 `allOf`

```java
// 目标态示例：下单前"商品校验 + 优惠券核销校验"互不依赖，可并行
CompletableFuture<Boolean> stockOk = CompletableFuture.supplyAsync(() -> productClient.checkStock(ids));
CompletableFuture<Boolean> couponOk = CompletableFuture.supplyAsync(() -> promotionClient.checkCoupon(couponId));
CompletableFuture.allOf(stockOk, couponOk).get(2, TimeUnit.SECONDS);   // 总耗时 = max(a, b) 而非 a + b
```

**适用判据**：只有**互相独立**（无数据依赖、无先后因果）的调用才并行；有依赖的串行是正确性要求。并行前先问"这两步哪个先失败都一样吗"。

---

## 四、缓存分层：对话链路的三级缓存（项目现成范本）

### 4.1 命中顺序即耗时阶梯

```yaml
# ai-cs-chat/src/main/resources/application.yml:165（配置注释原文）
# 查询命中顺序：热门问答（精确 O(1)）→ 语义缓存（向量相似）→ 完整 RAG 链路
cache:
  vector:   { enabled: true, l1-max-entries: 4096, ttl-hours: 168 }   # Embedding 向量两级缓存
  semantic: { enabled: true, threshold: 0.92, max-entries: 200 }      # 相似问题直接返回答案
  hot-qa:   { enabled: true, promote-threshold: 3, ttl-hours: 24 }    # 重复 3 次的问题缓存答案
```

### 4.2 代码路径：命中即短路

```java
// ChatServiceImpl.java:542-548 —— RAG 流式路径先查缓存
if (hasKb) {
    java.util.Optional<ChatRagResponseDTO> cached = lookupCachedAnswer(knowledgeBase, message);
    if (cached.isPresent()) {
        // :552-560 缓存命中：整段推送回答 + done 事件（cacheHit=true），不走 LLM
```

| 层级 | 命中耗时 | 挡掉什么 | 成本 |
|---|---|---|---|
| 热门问答（Redis 精确 key） | ~1ms（Redis 往返） | 完全相同问题 | 极低 |
| 语义缓存（向量相似 ≥0.92） | ~几 ms（向量化走 L1 缓存 + 余弦扫描 200 条） | 措辞不同意图相同的问题 | 每条缓存一次向量化 |
| 完整 RAG + LLM | **秒级**（检索 + 生成） | —（兜底） | token 费用 + 秒级延迟 |

**设计要点**：越便宜越靠前；每层都有界（`max-entries`/`promote-threshold`，防泄漏见 [04 §五](./04-内存泄漏排查实战.md)）；缓存 miss 才降级到下一层，且"会话历史仍记录"（`ChatServiceImpl.java:550-551`）保证多轮上下文不因缓存命中而断裂。

### 4.3 同思路的读缓存：会话历史

```java
// ChatHistoryServiceImpl.java:48-55 —— Redis LRANGE 优先读；miss 经 Feign 回源 message 服务并重建缓存
// :63-75 —— 写路径双写：Redis RPUSH（热缓存）+ RocketMQ 异步落库 chat_message 表
```

**异步化写入的意义**：把"写 MySQL"从用户请求路径上摘下来交给 MQ 消费者（ai-cs-mq），用户只承担 Redis 写的耗时；MQ 投递失败仅告警不阻断（`:64` 注释明确降级语义）。

---

## 五、大对象与序列化

| 场景 | 问题 | 本项目做法 / 建议 |
|---|---|---|
| LLM 上下文膨胀 | token 费用与延迟随长度涨 | 历史超 20 条压缩成摘要，保留最近 10 条（`ChatServiceImpl.java:150-153` 的 `MAX_HISTORY_SIZE=20`/`KEEP_RECENT_SIZE=10`） |
| 一次性返回大列表 | 响应体大、序列化耗 CPU | 分页 + 只查需要的列（SQL 层见 [05 §四](./05-SQL调优与执行计划.md)） |
| 逐 token 流式 vs 攒大 JSON | 等整段生成完才首字节 | SSE 逐 token 推送——**首字节延迟从"全文生成完"降到"首 token"**，这是本项目最值钱的一次"大对象"优化 |
| 缓存序列化开销 | 每次 Redis 读写 JSON 编解码 | 向量缓存 L1 层存原生 `float[]`，只有 L2 走 JSON（`VectorCacheStore.java:66-71` `:89-92`）——热数据零序列化 |

**判据**：凡是"内容大小由用户输入决定"的字段（消息、context、历史），必须有上限与压缩策略，否则 GC 与网络双双受累（alloc 剖析可见，[03 §七](./03-火焰图与性能剖析.md)）。

---

## 六、连接池参数

### 6.1 Hikari（MySQL）

**现状**：仓库内 `application.yml` 未显式配置 hikari 池参数（数据源细节由 Nacos 共享配置管理；[03-数据库与ORM/01 §6](../03-数据库与ORM/01-MySQL核心知识.md) 给的 `maximum-pool-size: 20` 是示例建议值）——按服务压测后显式化是改造点。

| 参数 | 建议起点 | 依据 |
|---|---|---|
| `maximum-pool-size` | 10~20，**不是越大越好** | MySQL 侧总连接 = 池大小 × 实例数，`deploy/mysql/mysql.cnf:14` 的 `max-connections=500` 是上限（1 主 2 从还要再分配） |
| `minimum-idle` | 与 max 相同或接近 | Hikari 官方建议固定大小池，避免动态伸缩抖动 |
| `connection-timeout` | 3000~5000ms | 拿不到连接快速失败（背压），别让请求干等 30s |
| `max-lifetime` | < MySQL `wait_timeout`（mysql.cnf:15 = 28800s） | 防拿到已被服务端关闭的死连接 |

**心法**：连接池是"下游数据库的并发闸门"。池太大压垮 DB（锁竞争、上下文切换），太小自产排队。正确顺序：先 DB 能扛多少 → 再定池大小 → 用压测验证排队位置。

### 6.2 其他池一句话

| 组件 | 关键点 |
|---|---|
| Lettuce（Redis） | 单连接多路复用，一般不用调池；注意大 value 序列化阻塞事件循环 |
| Feign/OkHttp | 每目标 host 的连接上限与 keep-alive；连接数要覆盖"并行调用"（§三）需求 |
| RocketMQ producer | 单实例复用，无需池化；关注发送超时与重试次数 |

---

## 七、两个链路案例的耗时分解

### 7.0 动手前的基本功：链路分段计时

优化前先拿到"每段占多少"的数据，三种手段按侵入度排序：

```java
// ① 日志埋点（零依赖，临时排查首选）
long t0 = System.currentTimeMillis();
List<Document> docs = retrieveRagDocs(knowledgeBase, message, 5, 0.5, hybrid, rewrite);
log.info("RAG检索耗时={}ms, docs={}", System.currentTimeMillis() - t0, docs.size());
```

```yaml
# ② Micrometer Observation（项目已接 ObservationRegistry，ResilientAiService 的 span 即此）
#    每个阶段 startLlmObservation/finishLlmObservation 成对出现（ResilientAiService.java:200-221）
```

```bash
# ③ 外部测量：k6 的 http_req_waiting（近似首字节）与 http_req_duration（整体）差值
#    = "尾段耗时"，对 SSE 链路即"生成阶段"的量级估计
```

**判读纪律**：占比 <5% 的段不做优化（§5.2 阿姆达尔）；段间无法拆分时先用 ① 打临时点，测完删掉。

### 7.1 对话链路（SSE）

```
用户请求 → Guardrail 输入审核(ms) → 三级缓存判定(1~10ms 或 miss)
  → RAG 检索(检索库 ms~百ms) → 组装 context → 模型路由(内存)
  → LLM 首 token(秒级，TTFT) → 逐 token 流式推送 → done 事件落历史
```

- 缓存命中路径把"秒级"压到"毫秒级"（§4.2），是**数量级**优化；
- 缓存 miss 时瓶颈 100% 在 LLM 首 token——项目已把 TTFT 做成指标（`ResilientAiService.java:279-286` 用 `AtomicLong firstTokenMs` 记录首 token 耗时），k6 侧对应阈值 `http_req_waiting p95<2000`（`scripts/loadtest/k6/sse-chat.js`）——**测量抓手已备好**，优化缓存命中率前先看当前命中率数据。

### 7.2 订单链路优化清单（按收益排序）

| # | 动作 | 位置 | 预期收益 |
|---|---|---|---|
| 1 | 循环扣库存 → 批量 Feign | `OrderServiceImpl.java:105-107` | N×网络 → 1× |
| 2 | 循环 insert → `saveBatch` + `rewriteBatchedStatements` | `:132-142` | N×往返 → 1× |
| 3 | 保留 MQ 延迟消息做超时取消 | `:156-158`（已有，勿动） | 异步化范本 |
| 4 | `confirmPay` 幂等已防重放 | `:237`（已有） | 正确性基础 |
| 5 | listOrders 加 `(user_id, create_time)` 联合索引 | DB 层 | 消除 filesort（[05 §七](./05-SQL调优与执行计划.md)） |

---

## 八、面试高频问答

**Q1：什么是 N+1 问题？本项目哪里有？怎么改？**
A：1 次主查询引发 N 次从属查询/调用。ORM 关联懒加载是经典形态；本项目的形态是循环远程调用与循环写入——`OrderServiceImpl.java:105-107` 每件商品一次 Feign 扣库存、`:132-142` 每件商品一次 insert。改法：批量接口一次传全量扣减；`saveBatch`（配合 `rewriteBatchedStatements=true`）一次往返插 N 行。判据是"循环体内出现 RPC/SQL"。

**Q2：CompletableFuture 并行调用要注意什么？**
A：四点——①只并行无依赖调用，有依赖串行；②线程池要显式指定，`supplyAsync` 默认走 ForkJoinPool.commonPool，长任务会污染全局并行流与公共池；③ThreadLocal 不跨线程传播，异步前捕获、线程内恢复（本项目 ResilientAiService.java:173-177 的 TraceContext 模式）；④并行段整体要有超时（allOf + get(timeout)），一个慢调用不该拖死整组。

**Q3：多级缓存怎么设计层级顺序？**
A：越便宜的越靠前、命中即短路：进程内（纳秒）→ Redis（毫秒）→ 原链路。本项目对话链路是热门问答（Redis 精确 O(1)）→ 语义缓存（向量相似）→ 完整 RAG+LLM（秒级兜底）。要点：每层有界防泄漏、miss 逐层降级、命中后仍要维护业务一致性（如会话历史照记）。

**Q4：缓存与 DB 的一致性怎么权衡？（对话场景）**
A：AI 客服答案类缓存是"性能优化而非事实数据"，容忍短暂陈旧，用 TTL（本项目 24~168h）自然过期即可；但**会话历史**必须实时可读，所以写路径是"Redis 热缓存同步写 + MQ 异步落库"，读永远先读 Redis——一致性分级：事实数据强一致、答案数据最终一致。

**Q5：大对象序列化会引发什么性能问题？**
A：三连击：序列化/反序列化吃 CPU（火焰图上 Jackson 占宽）；大字符串/大数组易成 G1 Humongous 对象加剧碎片与 GC；网络传输拉长响应时间。对治：上限与裁剪（历史压缩摘要）、流式替代整包（SSE 逐 token）、热数据免序列化（L1 存原生对象）。

**Q6：连接池是不是越大越好？**
A：不是。池大小决定的是"同时打到下游的并发上限"：太大→DB 锁竞争与上下文切换恶化、`max-connections`（本项目 mysql.cnf:14=500）被多实例瓜分后溢出；太小→应用侧排队。方法：先测 DB 单连接能力，按"下游容量 ÷ 实例数"定池，`connection-timeout` 设小实现快速失败（背压），压测验证排队发生在哪一层。

**Q7：接口优化后怎么证明有效？**
A：同负载前后对照：k6 的 p95/p99 与吞吐、GC 停顿、下游调用次数（N+1 改批量的最硬证据是"调用次数下降"）。单变量改动、基线可复现（[08 §四§五](./08-性能问题定位方法论.md)）。本项目现成抓手：TTFT 指标、k6 两个脚本、慢查询日志。

**Q8：为什么说"缓存命中路径要早、要便宜"？**
A：延迟优化的第一性原理是"别做重复的昂贵计算"。把判定放在请求最前端（Guardrail 之后、RAG 之前），命中时连向量化和检索都不做；层级按成本递增排列保证"最常发生的请求走最便宜的路径"。本项目 `application.yml:165` 注释即此原则的落地宣言。

---

## 九、动手练习

1. 在本地起全量 compose，压测下单链路（购物车放 5 件商品），用日志统计 `deductStock` 与 `insert` 的调用次数与耗时占比，验证 §2.1 的 N+1 现场。
2. 实现 `deductStockBatch` 批量扣库存接口 + `saveBatch` 改造（别忘了 JDBC URL 参数），复测同一压测，记录前后总耗时对照。
3. 把 §3.2 的 allOf 模板用于"创建订单前的两个校验"，显式传专用线程池，压测对比串行/并行的 p95。
4. 统计对话缓存命中率：给 `lookupCachedAnswer` 命中/未命中各打一个指标或日志计数，压测后计算命中率，并估算"命中率每提升 10% 节省多少 token 费用"。
5. 给一个业务服务的 hikari 显式配置（§6.1 建议值起步），用 `jmeter` 或 k6 打出连接池排队现象（connection-timeout 报错），再调参消除，记录"池大小 vs DB 并发"的取舍结论。

---

> 上一篇：[05-SQL调优与执行计划](./05-SQL调优与执行计划.md) ｜ 下一篇：[07-线程池调优与背压](./07-线程池调优与背压.md)
