# 线程池调优与背压

> 对应项目：`ai-cs-chat/.../config/ObservabilityExecutorConfig.java:27-58`（真实自研双线程池：usage/eval）、`ai-cs-chat/.../agent/AgentController.java:138`（SSE 用默认 `ForkJoinPool` 的现场）、`ai-cs-gateway/.../filter/RateLimitFilter.java:59-63`（超限 429 背压闸门）。
> 相关：[04-并发编程进阶](../01-Java基础/04-并发编程进阶.md)（并发基础与 `Executors` 四兄弟的坑已讲）、[10-限流算法-四大算法与网关实现](../11-数据结构与算法/10-限流算法-四大算法与网关实现.md)（**令牌桶/滑动窗口算法原理在那边——本篇只讲线程与容量维度**）、[05-SSE与WebSocket实时通信](../04-中间件/05-SSE与WebSocket实时通信.md)、[08-性能问题定位方法论](./08-性能问题定位方法论.md)。

---

## 一、先给结论：线程池的三种死法与三条军规

| 死法 | 机制 | 现象 |
|---|---|---|
| **队列撑爆 OOM** | 无界队列（如 `Executors.newFixedThreadPool`）无限堆积任务 | 堆里全是排队任务对象（[01-Java基础/04 §三](../01-Java基础/04-并发编程进阶.md) 已详述） |
| **线程无限涨** | `newCachedThreadPool`（max=Integer.MAX_VALUE） | 每线程 1MB 栈 + 内核调度开销，OOMKilled |
| **下游拖垮** | 池内线程全阻塞在慢下游，新请求排队 | 上游超时雪崩——**背压要解决的就是它** |

**三条军规**：①永远 `new ThreadPoolExecutor(...)` 显式七参数；②队列必须有界；③必须配拒绝策略 + 对拒绝可观测（打日志/上报）。

---

## 二、七参数与执行流程（速查）

> 参数含义与提交流程在 [01-Java基础/04](../01-Java基础/04-并发编程进阶.md) 有完整版，这里放调优视角的浓缩表。

| 参数 | 调优视角 |
|---|---|
| `corePoolSize` | 常驻兵力；低负载时也保留（`allowCoreThreadTimeOut` 可省资源） |
| `maximumPoolSize` | 上限兵力；**注意 JVM 行为：队列满了才扩容到 max**，不是先扩容再排队 |
| `keepAliveTime` | 超过 core 的空闲线程回收时间 |
| `workQueue` | **必须有界**；容量 = 削峰缓冲的深度 |
| `threadFactory` | 命名前缀必配（排查时 `usage-report-1` 一眼识别） |
| `handler` | 拒绝策略（§四） |
| （隐性）`RejectedExecutionHandler` 触发时机 | 队列满 **且** 达到 max 时才拒绝 |

**本项目真实范本**（照抄结构即可）：

```java
// ai-cs-chat/.../ObservabilityExecutorConfig.java:27-40（usageExecutor）
executor.setCorePoolSize(1);                  // 用量上报低频轻量：1 个常驻够用
executor.setMaxPoolSize(4);                   // 高峰最多扩到 4
executor.setQueueCapacity(1000);              // 削峰缓冲：1000 个任务排队
executor.setThreadNamePrefix("usage-report-");
executor.setWaitForTasksToCompleteOnShutdown(true);  // 优雅停机：等队列跑完，不丢用量数据
```

**为什么独立线程池**（`:10-17` 注释原文要义）：①不占 Tomcat 工作线程（HTTP 线程默认 200，被同步 Feign 落库拖垮会阻塞正常对话）；②故障隔离——message 服务挂了只影响上报队列，主链路 LLM 调用无感；③队列削峰。**"可观测性不能反过来观测死业务"**——这就是线程池隔离思想的落地。

---

## 三、IO 密集 vs CPU 密集：估算法

| 类型 | 公式 | 本项目服务对照 |
|---|---|---|
| **CPU 密集** | `N_cpu + 1`（N=核数） | 本项目业务服务基本没有纯 CPU 计算（SHA-256/余弦/正则都是短平快），无此类型池 |
| **IO 密集** | `N_cpu × (1 + 等待时间/计算时间)` | chat（等 LLM 秒级）、order（等 Feign/DB）、search（等 ES）全是此类 |

**IO 密集的核心矛盾**：等待/计算比可以是 10~1000（等 LLM 3 秒 vs 组装 3ms = 1000）。按公式"理论线程数"会大得离谱——所以公式只给方向，**实际上限由下游承受力与内存预算决定**：

```
线程数上限 = min( 下游并发承受力, 内存允许的线程栈总量 )
以 chat 为例：等 LLM 30s 超时（application.yml:72），下游 LLM API 并发有限
→ 池大小不该按"等 30s 也能全部并行"设，而要配合限流把并发压在下游能力内（§八）
```

**Little's Law 快算**（容量规划利器）：

```
并发数 L = 到达率 λ × 平均停留时间 W
例：对话接口 λ=20 QPS、平均停留 2s → 需要约 40 个并发执行位
→ 若 Tomcat 200 线程足够承载；若停留 30s（超时前），40 QPS 就需要 1200 并发位 → 必须限流/异步
```

这条公式直接解释了 §七：**SSE/LLM 场景"停留时间 W"极长，用同步线程扛并发会算出天文数字**——必须换执行模型或上背压。

---

## 四、队列与拒绝策略选型

### 4.1 队列

| 队列 | 特点 | 适用 |
|---|---|---|
| `LinkedBlockingQueue(容量)` | 有界 FIFO | **默认选择**（本项目 1000/500） |
| `ArrayBlockingQueue(n)` | 有界、预分配、略省内存 | 固定深度场景 |
| `SynchronousQueue` | 零容量，交接即走 | 配大 max，"快速失败"型（newCachedThreadPool 同款） |
| 无界 `LinkedBlockingQueue` | ❌ 永不拒绝 | **禁止**（OOM 死法之一） |

### 4.2 拒绝策略

| 策略 | 行为 | 本项目对应 |
|---|---|---|
| `AbortPolicy`（默认） | 抛异常，调用方决定 | **usageExecutor 现状**：`:33` 注释"拒绝策略默认 AbortPolicy，队列满时报错由调用方捕获告警" |
| `CallerRunsPolicy` | 提交者自己执行（天然减速） | 可接受降速的任务型场景；⚠️ 会阻塞 Tomcat 线程，SSE 主链路慎用 |
| `DiscardPolicy` | 静默丢弃 | 只适合允许丢的指标/埋点（丢数据不可见，慎用） |
| `DiscardOldestPolicy` | 丢最老的 | 有"最新值有意义"语义的监控类任务 |
| 自定义 | 打点 + 降级 + 告警 | **推荐**：拒绝发生 = 容量告警信号，绝不该静默 |

**选型口诀**：重要数据（用量计费）→ Abort + 调用方兜底；能丢的（采样评估）→ 小队列 + Discard 可接受；主链路 → 根本不该让任务进池排队（前面限流挡掉）。

---

## 五、动态调参与监控

**运行时可调**：`ThreadPoolTaskExecutor.setCorePoolSize()/setMaxPoolSize()` 在运行期即时生效（JDK 原生支持），无需重启。

**动态化思路**（目标态，项目未做集中管理）：

```
Nacos 配置 aics-threadpool.yml（命名空间 aics，项目已接 Nacos 配置中心）
   → @RefreshScope 的配置类读 core/max/queue
   → 监听 refresh 事件时调用 executor.setCorePoolSize(...)
   → 压测期在线调参，不改代码不重启
```

**必须监控的四个数**（暴露给 Prometheus，对接 [07-运维部署/04](../07-运维部署/04-Prometheus可观测性.md)）：

| 指标 | 含义 | 告警线 |
|---|---|---|
| `getActiveCount()` | 正在执行的线程数 | 持续 = max |
| 队列 `size()` | 排队深度 | > 容量 80% |
| 拒绝次数 | 池容量耗尽次数 | > 0 即告警 |
| 完成数速率 | 吞吐基线 | 突降 = 下游卡住 |

---

## 六、Tomcat 线程配置与本项目现状

**现状**：`ai-cs-chat/src/main/resources/application.yml` 等**没有**任何 `server.tomcat.*` 配置——全部吃 Spring Boot 默认值：

| 参数 | 默认值 | 含义 |
|---|---|---|
| `server.tomcat.threads.max` | **200**（`ObservabilityExecutorConfig.java:12` 注释"HTTP 请求线程数量有限（默认 200）"即指此） | 工作线程上限 |
| `server.tomcat.threads.min-spare` | 10 | 常备空闲 |
| `server.tomcat.max-connections` | 8192 | 已 accept 未处理的连接上限 |
| `server.tomcat.accept-count` | 100 | 线程和连接都满时的等待队列 |

**水位关系**：请求进来 → 工作线程处理（≤200）→ 连接层缓冲（≤8192）→ accept 队列（≤100）→ 拒绝。三层就是 Tomcat 的内置背压，但默认值是"通用值"不是"你的值"。

**改造建议**（按服务角色差异化）：

```yaml
# 普通 CRUD 服务（user/order/product）：IO 密集，停留下游 ms 级
server:
  tomcat:
    threads: { max: 100, min-spare: 20 }   # Little's Law：λ×W 算出来再定
# chat 服务：等 LLM 秒级，同步线程模型下"线程数 × 停留时间"立刻爆炸
#   → 调大线程数治标（栈内存 1MB/线程），治本是 §七的异步化
```

> Undertow 与 Tomcat 的取舍：Undertow 以 XNIO worker 模型著称、内存占用略低，但 Spring Boot 3 本项目生态（SSE/文件上传）在 Tomcat 上验证最充分，**没有测量证据支持切换时不动**——这也是一条性能工程纪律。

---

## 七、SSE 长连接占线程：问题与异步化

### 7.1 问题的定量形态

```java
// ChatServiceImpl.java:156 —— SSE emitter 5 分钟超时
private static final long SSE_EMITTER_TIMEOUT = 5 * 60 * 1000L;
// AgentController.java:117 —— Agent 编排 2 分钟
private static final long AGENT_STREAM_TIMEOUT = 120_000L;
```

Servlet（Tomcat）模型下，**一个 HTTP 请求占一个工作线程直到响应完成**。SSE 响应"完成"= 流结束 = LLM 生成完（可达数十秒）。按 Little's Law：20 并发会话 × 30s 停留 = 需要 600 并发位 > 默认 200 线程——**SSE 是本项目最吃线程的链路**。

### 7.2 现状一：SseEmitter + 订阅回调（主对话链路，已异步订阅）

```java
// ChatServiceImpl.java:525 创建 emitter；:645-646 订阅 Flux
SseEmitter emitter = new SseEmitter(SSE_EMITTER_TIMEOUT);
flux.subscribe(chunk -> { ... emitter.send(...); ... });
```

`subscribe` 的回调跑在 **Reactor 调度线程**上，不占 Tomcat 线程——主链路的"推流"阶段已经不阻塞容器线程。但请求处理线程仍要等到 `futureFlux.get()`（`:601`）返回 Flux 后才释放，路由与 RAG 检索段仍是同步占用。

### 7.3 现状二：Agent 链路用默认 ForkJoinPool（⚠️ 改造点）

```java
// AgentController.java:136-138
// 立即返回 emitter，编排提交工作线程异步执行——若在控制器线程同步执行，响应未提交前 send() 会失败
CompletableFuture.runAsync(() -> runAgentTurn(emitter, uid, request));
```

`runAsync` 不传 executor → 跑在 **`ForkJoinPool.commonPool()`**：这是 JVM 全局共享池（并行流、其他 supplyAsync 都在用）。Agent 编排含多步 LLM 调用（分钟级），高并发时**把公共池塞满会拖垮全进程所有异步逻辑**。修复：

```java
// 目标态：专用编排池（对齐 ObservabilityExecutorConfig 的隔离思想）
@Bean(name = "agentSseExecutor")
public ThreadPoolTaskExecutor agentSseExecutor() {
    ThreadPoolTaskExecutor ex = new ThreadPoolTaskExecutor();
    ex.setCorePoolSize(8); ex.setMaxPoolSize(16);
    ex.setQueueCapacity(50);                  // SSE 任务长，队列浅一点，超限快速拒绝
    ex.setThreadNamePrefix("agent-sse-");
    ex.setRejectedExecutionHandler(new ThreadPoolExecutor.AbortPolicy());  // 配合网关限流
    ex.initialize();
    return ex;
}
// AgentController.java:138 改为 CompletableFuture.runAsync(() -> runAgentTurn(...), agentSseExecutor)
```

### 7.4 异步 Servlet / 响应式的光谱

| 方案 | 线程占用 | 本项目适配度 |
|---|---|---|
| 同步 Servlet | 全程占线程 | ❌ SSE 场景不可行 |
| **SseEmitter（异步 Servlet 封装）** | 返回 emitter 后释放容器线程，推流在回调线程 | ✅ **现状**，主链路已用 |
| DeferredResult | 同上语义，自己控制结果 | 备选，语义等价 |
| WebFlux/Netty 全响应式 | 事件驱动，少量线程扛大量长连接 | 网关已是（`RateLimitFilter` 基于 `Mono`/`GlobalFilter`）；业务服务迁移成本高，不做 |

**结论**：本项目"Servlet 栈 + SseEmitter + Flux 桥接"（`ChatServiceImpl.java:607-644` 大段注释解释了这个桥接设计）是务实选择；剩余改造点只有 7.3 的专用池。

---

## 八、背压：从限流到拒绝的完整链条

> 令牌桶/滑动窗口的**算法原理与代码精读**在 [10-限流算法-四大算法与网关实现](../11-数据结构与算法/10-限流算法-四大算法与网关实现.md)——本节只讲它们在"容量防御体系"里的位置。

### 8.1 容量防御的四级闸门

```
请求 → ①网关限流(429) → ②Tomcat 线程/连接上限 → ③业务线程池队列 → ④下游超时+熔断
        RateLimitFilter      server.tomcat.*        workQueue+handler      TimeLimiter/CircuitBreaker
```

每级闸门的语义一致：**当需求超过容量，尽早、明确地拒绝，而不是默默积压到雪崩**。

### 8.2 网关现场（第一级闸门）

```java
// ai-cs-gateway/.../RateLimitFilter.java:59-63 —— 超限直接 429，不转发下游
if (!tryAcquire(key)) {
    log.warn("请求触发限流: key={}, path={}", key, exchange.getRequest().getPath());
    return tooManyRequests(exchange.getResponse());      // HttpStatus.TOO_MANY_REQUESTS
}
// :47-48 两个限流器常驻内存；:107 order=HIGHEST_PRECEDENCE+150（认证后执行，防伪造 X-User-Id 刷桶）
```

**线程/容量维度解读**（算法略）：
- 限流在**网关层**执行，被拒请求根本不产生下游线程/连接占用——这是成本最低的背压点；
- 网关自身是 WebFlux 响应式（`GlobalFilter` + `Mono`），限流判定是非阻塞的，**网关层几乎不因限流逻辑消耗线程**；
- `RateLimitFilter` 用内存态 `ConcurrentHashMap` 计数（`TokenBucketRateLimiter.java:25`）——单机有效，多实例网关要换 Redis 计数（演进方向）。

### 8.3 下游超时是最后一道闸门

```yaml
# ai-cs-chat/src/main/resources/application.yml:71-77
timelimiter:
  instances:
    chatService:    { timeout-duration: 30s, cancel-running-future: true }
    sseChatService: { timeout-duration: 60s, cancel-running-future: true }
```

`cancel-running-future: true` 的容量意义：超时即**取消**挂起的任务并释放执行线程——没有它，超时的调用仍占着线程直到 LLM 自己返回。超时参数要与 SSE emitter 超时（5min/2min）形成包络关系（emitter 超时 > 内部调用超时之和），否则 emitter 先死、内部调用白算。

---

## 九、面试高频问答

**Q1：线程池七个参数是什么？提交流程怎么走？**
A：corePoolSize、maximumPoolSize、keepAliveTime、unit、workQueue、threadFactory、rejectedExecutionHandler。提交顺序：核数未满建核心线程 → 满了入队 → **队列满才扩到 max** → 再满触发拒绝。注意"队列满才扩容"这个顺序是多数人答错点——不是先扩满线程再排队。

**Q2：IO 密集和 CPU 密集线程数怎么估？**
A：CPU 密集 = 核数 + 1（避免多余上下文切换）；IO 密集 = 核数 × (1 + 等待/计算)，但要受"下游承受力"与"线程栈内存"双重封顶，用 Little's Law（并发 = QPS × 平均停留）反推需求再做容量匹配。等待比极端大（如等 LLM 数十秒）时，同步线程模型算出的线程数不现实，应改异步/响应式或前置限流。

**Q3：拒绝策略怎么选？**
A：Abort + 调用方兜底告警适合"任务不可丢"（计费/审计）；CallerRuns 天然降速但会阻塞提交线程，主链路慎用；Discard 系只给可丢弃数据。核心原则：拒绝是容量告警信号，必须可观测（打点计数），不能静默。本项目 usageExecutor 即默认 AbortPolicy、调用方捕获告警。

**Q4：为什么 SSE 会占线程？怎么解？**
A：Servlet 同步模型一个请求占一个工作线程直到响应完成，SSE 响应持续到流结束（LLM 生成可达数十秒到 5 分钟），Little's Law 下少量并发就打满线程池。解法：异步 Servlet 封装（SseEmitter/DeferredResult）让容器线程尽早释放、推流在回调线程完成；或整体迁移 WebFlux/Netty。本项目主对话链路已是"SseEmitter + Flux.subscribe 桥接"，遗留改造是 Agent 链路 runAsync 默认 ForkJoinPool 换专用池。

**Q5：`CompletableFuture.runAsync` 不传 executor 有什么风险？**
A：跑在 ForkJoinPool.commonPool——JVM 全局共享：并行流、无参 supplyAsync、其他框架任务都在里面。长任务（Agent 编排分钟级）占满 commonPool 会拖垮全进程所有依赖它的异步逻辑，且线程数固定为核数-1 无法扩。风险控制：长任务必须显式传隔离的专用池。

**Q6：什么是背压？本项目哪几层实现了背压？**
A：背压 = 下游容量不足时，向上游传递"减速/拒绝"信号，防止无限积压。本项目的四级：网关限流（RateLimitFilter 超限 429，内存令牌桶/滑动窗口）；Tomcat 连接与线程上限；业务线程池有界队列 + 拒绝策略；下游 TimeLimiter 超时 + 熔断（Resilience4j）。设计哲学：宁可快速失败也不要排队到雪崩。

**Q7：如何动态调整线程池参数？**
A：JDK 原生支持运行期 `setCorePoolSize/setMaxPoolSize`（即时生效，正在排队的任务不受影响）。工程化做法：参数外置到配置中心（项目已用 Nacos），@RefreshScope 监听变更后调用 setter，配合队列深度/活跃数/拒绝次数四个指标观察效果。注意 max 调小时需先处理队列积压，否则可能出现"max < 活跃数"的过渡态。

**Q8：网关限流计数在内存里，多实例部署有什么问题？**
A：每个网关实例各有一份 `ConcurrentHashMap` 计数（TokenBucketRateLimiter.java:25），限流阈值实际变成"单实例阈值 × 实例数"，且实例间流量不均时防护失真。演进：计数状态迁到 Redis（原子 INCR/Lua 或 Redisson RRateLimiter），代价是每次判定多一次网络往返——或者用"实例数感知"动态折算单机阈值。这是从单机背压到分布式背压的必经一步。

---

## 十、动手练习

1. 读 `ObservabilityExecutorConfig.java:27-58`，为两个池各写一段"为什么这个参数组合合理"的评审意见（提示：任务时长、下游是谁、可不可丢）。
2. 给 `AgentController.java:138` 落地 §7.3 的 `agentSseExecutor`：创建 Bean、替换 runAsync 参数，压测 `sse-chat.js` 验证线程名前缀变为 `agent-sse-`。
3. 写一个压测脚本观察 Tomcat 线程饱和：把 chat 服务 `server.tomcat.threads.max` 临时调到 20，用 k6 打 50 并发 SSE，观察 `accept-count` 拒绝与延迟变化，回填 Little's Law 计算值对照。
4. 给 usageExecutor 加监控端点：暴露 activeCount/queueSize/拒绝计数三个指标，压测到队列打满观察拒绝告警。
5. 演练动态调参：把 evalExecutor 的 maxPoolSize 从 2 在线改成 4（不重启），用 `jstack` 确认新线程出现——体会"参数外置 + 运行时生效"的价值。

---

> 上一篇：[06-接口性能优化实战](./06-接口性能优化实战.md) ｜ 下一篇：[08-性能问题定位方法论](./08-性能问题定位方法论.md)
