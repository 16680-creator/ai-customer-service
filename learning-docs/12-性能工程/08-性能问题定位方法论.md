# 性能问题定位方法论

> 对应项目：`scripts/loadtest/k6/sse-chat.js`、`scripts/loadtest/k6/gateway-rate-limit.js`（基线测量工具，**用法与脚本精读见 [05-性能压测实战-k6与JMeter](../08-测试/05-性能压测实战-k6与JMeter.md)，本篇只讲"拿到数据之后怎么定位"**）、`deploy/mysql/mysql.cnf:19-21`（慢日志）、`ai-cs-chat/.../ResilientAiService.java:279-286`（TTFT 指标）。
> 相关：[04-Prometheus可观测性](../07-运维部署/04-Prometheus可观测性.md)（指标从哪来）、[01-JVM参数与GC选型](./01-JVM参数与GC选型.md)~[07-线程池调优与背压](./07-线程池调优与背压.md)（本模块各层的"深挖入口"）、[03-JVM内存模型与GC实战 §6](../01-Java基础/03-JVM内存模型与GC实战.md)（CPU 100% 五步法在那边）。

---

## 一、先给结论：定位的三条纪律

| 纪律 | 内容 | 反面案例 |
|---|---|---|
| **先测量后优化** | 没有"优化前数据"就没有"优化后收益"，一切结论用数字说话 | "我觉得加了缓存会快"——加完反而 GC 变频繁 |
| **一次只改一个变量** | 同时改堆大小 + 线程数 + 索引，改善了你不知道归功于谁 | 无法归因 → 无法固化 → 无法回滚 |
| **优化必须有收益测算** | 改动成本 vs 收益（p95 降幅 / 吞吐提升 / 成本节省）先算后做 | 三天优化一个 p50 快 5ms 的冷路径 |

**定位的完整闭环**：`测量（基线）→ 假设（分层归因）→ 验证（对照实验）→ 优化（最小改动）→ 复测（同负载对照）→ 固化（文档/监控）`。本篇 §三的决策树负责"假设"这一步的效率。

---

## 二、USE 与 RED：两套指标盘面

### 2.1 RED（服务视角）：用户感受到什么

| 指标 | 含义 | 本项目现成抓手 |
|---|---|---|
| **R**ate | 每秒请求数 | k6 压测的到达率、网关访问日志 |
| **E**rrors | 错误率 | HTTP 4xx/5xx、`RateLimitFilter` 的 429 计数 |
| **D**uration | 延迟分布（p50/p95/p99） | k6 `http_req_waiting`（SSE 首 token）、`http_req_duration`（整条流），阈值见 `sse-chat.js` 的 thresholds |

RED 回答"**服务病了吗、多严重**"——它是告警的来源。注意 **只看均值是自欺**：p95/p99 才是用户体验的长尾，慢日志抓的也常是长尾请求。

### 2.2 USE（资源视角）：瓶颈压在哪

每个资源问三件事：**U**tilization（使用率）、**S**aturation（饱和度/排队）、**E**rrors（错误）。

| 资源 | Utilization | Saturation | Errors |
|---|---|---|---|
| CPU | `docker stats`/top 的 %CPU | run queue 长度、负载均值 | — |
| 内存 | RSS vs 容器限额 | swap、回收速率 | OOMKilled（exit 137） |
| JVM 堆 | `jstat` O 列 % | FGC 频率、GC 停顿 | `OutOfMemoryError` |
| 线程池 | activeCount | 队列深度、拒绝次数 | RejectedExecution |
| 连接池 | 活跃连接 | `connection-timeout` 等待 | 获取超时异常 |
| 磁盘 IO | %util | await、队列长度 | IO error |
| 网络 | 带宽 | 重传率、RTT | 超时/拒绝 |
| MySQL | 连接数/`max-connections`（mysql.cnf:14） | 慢查询数、锁等待 | 死锁、主从延迟 |
| MQ | 生产/消费 TPS | 堆积量（consumer lag） | 发送失败、消费重试超限 |

**两套盘的联动**：RED 告诉你"哪个接口病了"，USE 告诉你"哪个资源在发烧"。定位 = 用 RED 的时段与接口，去 USE 的资源上找同时刻的异常——时间轴对齐是关键（GC 日志、慢日志都带时间戳，[02 §2.2](./02-GC日志与停顿分析.md) 的 `time` 装饰器就是为此）。

---

## 三、从现象到根因：六层排查决策树

> 口诀：**先应用后系统、先软件后硬件、先近期变更后历史包袱**。每层给"典型现象 + 第一个命令 + 深挖文档"。

```
接口慢/报错
│
├─ ① 应用层（代码逻辑、缓存命中率）
│     现象：单接口慢，其他正常；日志里业务异常
│     第一命令：grep 该接口耗时日志 / 查缓存命中标记（cacheHit=true 与否）
│     深挖：[06-接口性能优化实战](./06-接口性能优化实战.md)
│     ├─ 命中缓存仍慢？→ ②
│     └─ N+1/循环调用？→ §二② 修代码
│
├─ ② JVM 层（GC、线程、内存）
│     现象：周期性抖动、偶发超时、CPU 高但业务线程闲
│     第一命令：jstat -gcutil <pid> 2000；jstack 抓线程态
│     深挖：[02-GC日志与停顿分析](./02-GC日志与停顿分析.md)、[04-内存泄漏排查实战](./04-内存泄漏排查实战.md)、
│           [03-火焰图与性能剖析](./03-火焰图与性能剖析.md)（热点定位）、[07-线程池调优与背压](./07-线程池调优与背压.md)（线程饱和）
│     ├─ GC 停顿占比 > 20%？→ 调 GC（02 §五）
│     ├─ 线程全 BLOCKED/WAITING？→ 锁竞争或等下游 → ③④
│     └─ 老年代只涨不跌？→ [04](./04-内存泄漏排查实战.md) 抓 dump
│
├─ ③ DB 层（慢 SQL、锁、连接）
│     现象：涉及 DB 的接口慢；连接池等待超时
│     第一命令：tail 慢日志（mysql.cnf:19-21 已开）→ EXPLAIN
│     深挖：[05-SQL调优与执行计划](./05-SQL调优与执行计划.md)、[05-MySQL锁机制](../03-数据库与ORM/05-MySQL锁机制与主从复制读写分离.md)
│     ├─ type=ALL / rows 巨大？→ 索引（05 §二§三）
│     └─ 锁等待？→ [05-MySQL锁 §四](../03-数据库与ORM/05-MySQL锁机制与主从复制读写分离.md)
│
├─ ④ 中间件层（Redis/MQ/ES/Nacos）
│     现象：一类操作整体慢（所有缓存读写、所有消息）；中间件自身告警
│     第一命令：redis-cli --latency；MQ 消息堆积查询；ES _cluster/health
│     深挖：[01-Redis缓存实战](../04-中间件/01-Redis缓存实战.md)、[02-RocketMQ](../04-中间件/02-RocketMQ消息队列.md)
│     ├─ Redis 大 key / 慢命令？→ SLOWLOG GET
│     └─ MQ 堆积？→ 消费线程/逻辑排查（[07](./07-线程池调优与背压.md)）
│
├─ ⑤ OS / 容器层（CPU、内存、IO、cgroup）
│     现象：exit 137、整机负载高、所有服务都慢
│     第一命令：docker stats --no-stream；docker inspect .State.OOMKilled
│     深挖：[01-JVM参数与GC选型 §五](./01-JVM参数与GC选型.md)（容器内存预算）
│     ├─ OOMKilled？→ 堆+堆外预算重算
│     └─ CPU 饱和？→ 火焰图找热点（[03](./03-火焰图与性能剖析.md)）
│
└─ ⑥ 网络层（DNS、RTT、丢包）
      现象：跨机房慢、偶发 Connect timeout、时好时坏
      第一命令：ping/traceroute 目标；对比同机直连与走网络的延迟
      深挖：[15-计算机基础] 网络篇（建设中）；先排除：别把下游慢误判成网络慢
```

**树的使用方法**：自上而下过一遍，每层用"第一命令"做**30 秒排除法**，多数问题在前三层解决。真实案例路径参考：CPU 100% 的五步法（`top → top -Hp → printf %x → jstack → 看栈顶`）在 [03-JVM内存模型与GC实战 §6 现场2](../01-Java基础/03-JVM内存模型与GC实战.md)，若栈顶是 `GC task thread` 则切到 ②→GC 分支。

### 决策树实战三例（演练脚本）

**例 1：SSE 首 token 偶尔 5s，多数时候 1s 内**

```
① 应用层：缓存命中时毫秒级 → 慢的全是 miss 请求
② JVM 层：jstat 无异常，YGC 频率正常 → 排除
③ DB 层：无涉及 → 排除
④ 中间件层：Redis 延迟正常，但慢请求都发生在"模型路由选了备用模型"之后
   → 查 ResilientAiService 降级日志，主模型偶发超时触发 fallback 重试
⑤ 结论：不是单点性能问题，是下游主模型 P99 抖动 → 优化方向：主模型超时收紧 +
   熔断阈值调整（application.yml:111-117），而不是优化任何本地代码
```

**例 2：容器 CPU 长期 300%，接口反而变慢**

```
② JVM 层：top -Hp 发现多个 "GC task thread" 占 CPU
   → jstat -gcutil：O 98%、FGC 每分钟 +3 → 切 GC 分支
   → gc.log 出现 Pause Full 700ms → 按晋升失败链条回查
④ 深挖：dump 显示某个无界缓存 Map → 修复为有界 LRU（[04](./04-内存泄漏排查实战.md)）
⑤ 结论：症状是 CPU，根因是内存——决策树的价值在于把"CPU 高"引导到正确分支
```

**例 3：压测 20 VU 后吞吐不涨反降**

```
① 应用层：无错误、无慢 SQL
② JVM 层：jstack 大量 http-nio 线程 WAITING 在 futureFlux.get()
③ 判断：Tomcat 200 线程被等 LLM 的同步段占满（Little's Law：20×30s=600 并发位需求）
④ 优化：SSE 链路缓存前置 + Agent 换专用池（[07 §七](./07-线程池调优与背压.md)）+ 网关限流收口
⑤ 复测：同 20 VU 吞吐回升 → 固化参数进基线
```

三例的共同点：**每一步都有数据支撑，结论落在"改什么"而不是"怀疑谁"**。

---

## 四、先测量后优化：基线怎么建

| 层 | 基线内容 | 工具/脚本 | 频率 |
|---|---|---|---|
| 接口 | p50/p95/p99、吞吐、错误率 | `scripts/loadtest/k6/sse-chat.js`（SSE：首 token p95<2s、整流 p95<15s）、`gateway-rate-limit.js`（限流拐点） | 每次发布前 |
| JVM | 停顿 p99、YGC/分钟、老年代稳态 | `-Xlog:gc*` + jstat（[02 §六](./02-GC日志与停顿分析.md) 体检命令） | 压测时采集 |
| DB | 慢 SQL 清单、EXPLAIN 基线 | 慢日志（`long-query-time` 建议压测期降到 0.5） | 每次发版 |
| 容器 | RSS 稳态、CPU 均值/峰值 | `docker stats` + Prometheus（[04](../07-运维部署/04-Prometheus可观测性.md)） | 持续 |

**基线三原则**：①同环境同负载（压测脚本参数固化）；②预热后采样（避开 JIT 与冷缓存，[03-火焰图与性能剖析 §五](./03-火焰图与性能剖析.md) 的时段偏差同理）；③记录在案可复现（k6 的 thresholds 即"基线即代码"——直接写进脚本随版本管理）。

---

## 五、优化收益测算

### 5.1 三个必算的数

```
① 延迟收益：p95_前 - p95_后（ms），以及"达标率"变化（如 p95<2s 的请求占比）
② 吞吐收益：同延迟约束下 QPS_后 / QPS_前
③ 成本收益：吞吐/实例 提升 → 支撑同量流量需要的实例数变化（含 token 费用这类真金白银）
```

### 5.2 阿姆达尔定律：别在非瓶颈上使劲

```
整体加速比 = 1 / ((1-P) + P/S)
P：可优化部分占比；S：该部分加速倍数

例：对话链路总延迟 3000ms，其中 LLM 首 token 2800ms（P≈93%），
   检索 200ms（P≈7%）——把检索优化到 0（S=∞）：
   加速比 = 1 / (0.07/∞ + 0.93/1) ≈ 1.075  → 总延迟仅降 7%（≈225ms）

   反过来，缓存命中把 2800ms 的 LLM 段直接跳过（S=93倍）：
   加速比 ≈ 1 / (0.008 + 0.01) ≈ 55 → 秒级 → 毫秒级
```

**结论**：先算各段耗时占比（链路分解见 [06 §七](./06-接口性能优化实战.md)），**只优化占比大的段**。这也是"对话链路先做缓存命中率、SQL 只在慢日志出现时才动"的理论依据。

### 5.3 收益测算表模板（优化记录用）

| 字段 | 示例 |
|---|---|
| 现象 | 下单链路 p95 800ms |
| 基线 | 循环扣库存 10 次 Feign ≈ 300ms、循环 insert 10 次 ≈ 200ms |
| 假设 | 批量化后两段各剩 ~30ms |
| 改动 | `deductStockBatch` 接口 + `saveBatch`（[06 §二](./06-接口性能优化实战.md)） |
| 复测 | p95 800ms → 380ms（-52%） |
| 代价 | 新增批量接口开发 0.5 天；库存批量扣减失败语义确认 |
| 结论 | 收益/成本比高，固化；监控项：批量接口错误率 |

---

## 六、反模式清单（每条都值得贴在工位）

| 反模式 | 纠正 |
|---|---|
| 拍脑袋优化（"我觉得是数据库慢"） | 先 EXPLAIN / 先分段计时，占比说了算（§5.2） |
| 只看均值不看分位 | p95/p99 + 慢日志 |
| 同时改多个变量 | 单变量对照，改一步测一步 |
| 微基准代替端到端 | 压测脚本打真实链路，单元级优化要能对上端到端收益 |
| 无基线上线"优化" | 没有前后对照的优化 = 未发生的优化 |
| 优化到不可读 | 可读性/复杂度是成本；收益 <10% 的复杂改动通常不值得 |
| 忽视测量的观测者效应 | 全链路 debug 日志本身会拖慢系统——测量工具要开关注册 |
| 忘了先修正确性 | 性能优化不能引入数据错误（批量化的失败语义，[06 §2.2](./06-接口性能优化实战.md)） |
| 用生产经验代替复现实验 | 生产环境变量不可控，结论要在可复现环境里验证后回推 |
| 优化完不留档 | 下次别人（或三个月后的你）会再踩一遍——优化记录必须入库 |

---

## 七、面试高频问答

**Q1：USE 方法和 RED 方法分别是什么？怎么配合？**
A：RED 是服务视角（Rate/Errors/Duration），回答"哪个接口病了"，驱动告警；USE 是资源视角（每个资源的 Utilization/Saturation/Errors），回答"哪个资源发烧"。配合方式：用 RED 定位问题接口与时间段，再到 USE 的资源指标上找同时刻异常，逐层下钻——服务数据指方向，资源数据找根因。

**Q2：拿到"接口 p95 暴涨"的告警，你的排查顺序？**
A：①看告警时段有无发布/配置变更（先近期变更）；②RED 细分：错误率也涨？（可能是故障不是性能）只慢不错？（容量问题）；③六层决策树自上而下 30 秒排除法：应用日志缓存命中→jstat GC→慢日志→中间件健康→容器资源→网络；④找到异常层后用对应专项手段深挖（GC 日志/EXPLAIN/火焰图）。全程记录假设与证据，避免"边查边改"破坏现场。

**Q3：为什么性能优化要"先测量后优化"？**
A：三个原因：①定位——没有数据无法知道瓶颈在哪层，改错方向浪费且引入风险；②归因——无基线则优化后无法证明有效（可能是波动）；③止损——收益测算（阿姆达尔）会告诉你某些"优化"根本不值得做。先测量还强迫你固化压测脚本与负载模型，这本身是性能工程最重要资产。

**Q4：解释阿姆达尔定律及工程意义。**
A：整体加速比 = 1/((1-P)+P/S)，P 是可优化部分占比、S 是加速倍数。工程意义：优化的上限由"可优化部分占比"决定——占比 7% 的部分就算优化到零，整体也只快 7%。所以动手前必做链路耗时分解，把力气花在占比最大的段。AI 对话链路里 LLM 生成占 90%+ 延迟，因此缓存命中（跳过整段）的收益远大于优化检索细节。

**Q5：怎么建立一条接口的性能基线？**
A：四要素：固定负载（k6 脚本场景与参数进版本库，如 sse-chat.js 的 ramping-vus 阶梯）；固定环境（同 compose/同数据量，预热后采样）；关键指标（p50/p95/p99、吞吐、错误率）；记录与复核（基线写进 thresholds 或压测文档，每次发版对照）。基线即代码，人走基线留。

**Q6：一次只改一个变量，实践中怎么平衡效率？**
A：用"实验矩阵"补效率：灰度环境并行跑多组配置（每组只改一个变量），同时对照而不是串行等待；生产则严格单变量 + 快速回滚预案。禁止的是"一把梭改五个参数还带上线"——出了改善无法归因，出了问题无法回滚，最终把调优变成玄学。

**Q7：优化记录（优化前后对照）应该包含哪些字段？**
A：现象与基线数据、根因假设与验证证据、具体改动（commit/配置 diff）、同负载复测数据（收益量化）、改动代价（复杂度/风险/资源）、后续监控项与回滚条件。这份记录既是团队资产，也是面试时"我优化过什么"的证据链——数字、路径、文件行号齐全。

**Q8：生产环境能直接压测吗？怎么避免压测本身打垮生产？**
A：优先影子/灰度环境压测；必须在生产压测时：限流保护（本项目网关 RateLimitFilter 的 429 正好是护栏）、逐步加压（ramping-vus）观察 RED拐点即停、避开业务高峰、压测流量打标记便于统计剔除。压测观察者效应也要管理：debug 级日志与额外采样在压测期统一开关，保证基线可比。

---

## 八、动手练习

1. 给 sse-chat.js 补充一个"基线记录"字段：跑 3 次取 p95 中位数，连同 JVM 版本、堆参数、数据量写进你的基线卡（模板见 §四表）。
2. 按 §三决策树做一次 30 秒排除演练：人为制造一个 GC 问题（`-Xmx128m` 启动 chat），从"接口变慢"出发逐层走树，记录每层"第一命令"的输出与你的判断。
3. 用 §5.2 的阿姆达尔公式测算：把对话链路的 RAG 检索段从 200ms 优化到 50ms，总延迟收益是多少？再算缓存命中率从 0 提到 60% 的收益——对比后写 3 行结论。
4. 制作一份"优化记录"文档（§5.3 模板），对象选 [06 §二](./06-接口性能优化实战.md) 的订单批量改造：即使没实施，也把基线/假设/预期收益三段写实。
5. 给本模块 8 篇画一张"定位速查地图"：每个排查层标注对应的文档与两三条关键命令，贴在学习笔记首页——检验你是否能把方法论与工具串成一条链。

---

> 上一篇：[07-线程池调优与背压](./07-线程池调优与背压.md)
