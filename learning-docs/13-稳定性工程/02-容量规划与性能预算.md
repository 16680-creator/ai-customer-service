# 容量规划与性能预算

> 对应项目：`deploy/docker-compose/docker-compose-all.yml`（全量单机部署，各组件内存参数）、
> `deploy/mysql/master.cnf` / `slave.cnf` / `slave2.cnf`（连接数与缓冲池预算）、
> `deploy/redis/redis.conf`（maxmemory 与淘汰策略）、
> `ai-cs-*/src/main/resources/application.yml`（各服务 JVM 参数与端口）。
> 相关：[01-SLI-SLO与错误预算](./01-SLI-SLO与错误预算.md)（SLI 是容量水位的数据源）、[04-高可用架构模式](./04-高可用架构模式.md)（容量冗余是高可用的前提）、[08-测试/05-性能压测实战](../08-测试/05-性能压测实战-k6与JMeter.md)（实测压容量）。

---

## 一、容量规划五步法：从业务量到实例数

容量规划回答一个问题：**多少业务量，需要多少资源，先崩的是谁**。

```
业务量（日活/订单数）
    │  ① 拆请求：每用户每天发多少次请求
    ▼
日均请求量
    │  ② 二八原则：80% 流量集中在 20% 时间 → 求峰值
    ▼
峰值 QPS
    │  ③ 估单机能力：压测或按经验值
    ▼
所需实例数 = 峰值 QPS / 单机 QPS × 安全系数
    │  ④ 顺着调用链往下推：每个依赖被放大多少倍
    ▼
依赖容量预算（MySQL 连接 / Redis 内存 / MQ TPS）
    │  ⑤ 留余量：水位线 + N+1 冗余
    ▼
最终容量方案
```

**核心思想**：容量不是拍出来的数字，而是一条可回溯的推演链。每一步都能用压测（`scripts/loadtest/k6/`）或监控数据（Prometheus 9090）校准。

---

## 二、二八原则与峰值估算

### 2.1 峰值 QPS 公式

```
日均请求量 D
80% 的请求发生在 20% 的时间（约 4.8 小时 ≈ 17300 秒）
峰值时段平均 QPS = (D × 0.8) / 17300
再乘突发系数 2~3（秒级尖峰）
```

**算例**（以 ai-cs 对话场景为例的假设值）：日活 1 万用户，人均每天 20 次对话相关请求 → D = 20 万。

| 步骤 | 计算 | 结果 |
|---|---|---|
| 峰值时段均量 | 200000 × 0.8 / 17300 | ≈ 9.2 QPS |
| 乘突发系数 3 | 9.2 × 3 | ≈ 28 QPS |
| 每次对话的放大 | 1 次对话 = 前端 1 请求 + 网关 1 + chat 内部向量检索/语义缓存/模型调用若干 | 链路放大 3~5 倍 |
| chat 服务实际要扛 | 28 × 1（入口） | 28 QPS，但内部依赖各扛 28~140 |

> 以上业务量为**假设**，用于演示推演方法；项目暂无生产流量数据。k6 基线实测（[08-测试/05](../08-测试/05-性能压测实战-k6与JMeter.md) 中标注"基线报告待执行"）跑通后回填真实单机能力。

### 2.2 为什么网关 replenish-rate=5 是个容量信号

`ai-cs-gateway/src/main/resources/application.yml:48-49`：`replenish-rate: 5` + `burst-capacity: 10`——每用户稳态 5 QPS。反推：**1000 并发活跃用户的稳态流量 = 5000 QPS**，这已远超单机 chat 服务能力（512MB 堆 + LLM 外调）。含义有二：

1. 该限流值是为**学习/演示环境**定的，不是容量规划产物（`learning-docs/00-学习路线总览/05-技术缺口分析与补全计划.md:44` 明确说参数"无压测数据背书"）；
2. 真实放量前必须走完本篇五步法重新推导，这正是"性能预算"的含义——**限流参数是容量结论的执行手段，不是容量规划的起点**。

### 2.3 峰值系数参考表（业务类型决定系数）

二八原则里的"突发系数"不是拍脑袋，跟业务类型强相关：

| 业务类型 | 流量形态 | 突发系数 | 说明 |
|---|---|---|---|
| B 端后台 | 工作时间平稳 | 1.5 | 峰谷差小，容量好规划 |
| C 端内容/客服 | 时段集中 + 事件驱动 | 2~3 | 客服高峰跟随业务活动 |
| 电商大促 | 秒杀式脉冲 | 5~10 | 必须提前压测 + 限流兜底 |
| 本项目（客服对话） | 工作时段集中，LLM 外调长尾 | 3 起步 | 对话时长长，**并发会话数**比 QPS 更关键（见 §5） |

---

## 三、项目现场：docker-compose-all.yml 内存账本

全量单机部署（`deploy/docker-compose/docker-compose-all.yml`）把 7 个基础设施 + 7 个微服务放在一台机器上，先把内存账算清楚：

### 3.1 基础设施内存参数（实测行号）

| 组件 | 容器 | 内存参数 | 位置 |
|---|---|---|---|
| MySQL | aics-mysql | 未显式限制（InnoDB 缓冲池在 `deploy/mysql/mysql.cnf`） | `docker-compose-all.yml:9-30` |
| Redis | aics-redis | 未在 compose 限制，上限走 `deploy/redis/redis.conf` `maxmemory 256mb` | `docker-compose-all.yml:33-49`、`redis.conf:24` |
| Nacos | aics-nacos | `JVM_XMS: 256m` / `JVM_XMX: 512m` | `docker-compose-all.yml:67-68` |
| Elasticsearch | aics-elasticsearch | `ES_JAVA_OPTS: "-Xms512m -Xmx512m"`，单节点模式 | `docker-compose-all.yml:90-91` |
| RocketMQ NameServer | aics-rocketmq-namesrv | `JAVA_OPT_EXT: "-Xms256m -Xmx256m"` | `docker-compose-all.yml:114` |
| RocketMQ Broker | aics-rocketmq-broker | `JAVA_OPT_EXT: "-Xms512m -Xmx512m"` | `docker-compose-all.yml:135` |
| MinIO | minio | 未限制 | `docker-compose-all.yml:144-163` |

### 3.2 微服务 JVM 参数

7 个 Java 服务（gateway/user/knowledge/chat/search/message/notify，端口 8080~8086）统一 `JAVA_OPTS: "-Xms256m -Xmx512m"`（如 gateway 在 `docker-compose-all.yml:175`，chat 在 `:236`）。堆上限 512MB、初始 256MB。

### 3.3 单机内存总账

| 类别 | 计算 | 合计 |
|---|---|---|
| Java 服务堆 | 7 × 512MB | 3.5 GB |
| Nacos | 512MB | 0.5 GB |
| ES | 512MB（堆外另有约等量开销） | ≈ 1.0 GB |
| RocketMQ | 256MB + 512MB | 0.75 GB |
| Redis | 256MB + 进程开销 | ≈ 0.4 GB |
| MySQL | 缓冲池 + 连接线程内存 | ≈ 0.5 GB |
| OS + Docker + 页缓存余量 | — | ≥ 2 GB |
| **单机总需求** | | **≈ 9 GB** |

**结论**：这套 compose 单机跑需要 16GB 内存的机器才从容（8GB 会频繁 swap）。这就是"容量预算"最朴素的应用——**先把账算出来，再决定机器规格**，而不是启动后看 OOM 再调。

---

## 四、依赖容量预算：连接数与内存

### 4.1 MySQL：连接数是最容易爆的预算

`deploy/mysql/master.cnf` 关键参数（实测行号）：

```ini
innodb-buffer-pool-size=256M    # 行 14：缓冲池，InnoDB 性能命脉
max-connections=500             # 行 24：最大连接数
long-query-time=2               # 行 31：慢查询阈值 2s
```

**连接数怎么被吃掉**：每个 Java 服务一个 Hikari 连接池（默认 10 连接），7 个服务 × 10 = 70 连接起步；叠加 XXL-Job、Canal、Seata 等后台组件，连接是**加法**不是乘法，但每次扩容服务实例都要重新加总。

| 服务实例数 | Hikari 连接（10/实例） | 占 500 上限 |
|---|---|---|
| 7（现状） | 70 | 14% |
| 每服务 3 副本（21 实例） | 210 | 42% |
| 每服务 5 副本（35 实例） | 350 | 70% ⚠️ 接近水位线 |

**推论**：MySQL 连接预算决定了微服务横向扩容的**天花板在数据库层先到**——这也是 1主2从 + 读写分离（`deploy/docker-compose/docker-compose-master-slave.yml:8-80`，见 [04-高可用](./04-高可用架构模式.md)）存在的意义：读连接分流到从库，把主库连接留给写。

### 4.2 Redis：256MB 是硬上限，靠淘汰策略兜底

`deploy/redis/redis.conf:24-25`：

```ini
maxmemory 256mb
maxmemory-policy allkeys-lru
```

- **预算含义**：所有 key（会话缓存、向量缓存 L2、语义缓存索引、热榜 ZSET、限流令牌桶）共享 256MB，超了就按 LRU 驱逐——**驱逐对业务是静默的**，命中率下降不会报错，只会变慢。
- **结构性风险**：限流的令牌桶 key 被驱逐 = 限流失效。Redis 里高价值小 key（令牌桶）与大体积缓存 key 混存且同一淘汰池，这是容量分配没做隔离的典型案例。
- 缓解：`allkeys-lru` 对本项目"缓存为主"的用途是合理默认；更讲究的做法是拆两个 Redis 实例或用 `volatile-*` 系列把 TTL 数据与限流数据隔离。

### 4.3 ES 与 RocketMQ

| 组件 | 预算 | 风险点 |
|---|---|---|
| ES 512MB 堆 | `docker-compose-all.yml:91` | ES 官方建议堆 ≤ 物理内存一半且 ≤ 31GB；单节点单副本索引，磁盘满会转只读 |
| Broker 512MB | `docker-compose-all.yml:135` | 消息积压吃磁盘而非堆，磁盘容量才是 MQ 的真实预算；死信堆积需监控 |

---

## 五、实例数推演：从 QPS 到副本数

**单实例能力估算（chat 服务为例）**：堆 512MB（`docker-compose-all.yml:236`）+ SSE 长连接模型。SSE 每会话占用一个 Servlet 异步连接（`ChatController.java:167` 的 `SseEmitter`），内存开销主要在会话上下文与模型流缓冲。

```
所需实例数 = 峰值 QPS / 单实例 QPS × 安全系数

例：峰值 28 QPS，压测得单实例 SSE 并发安全值 ≈ 20 会话（k6 sse-chat.js 的 20 VU 阶梯即为此设计）
   → 28/20 × 1.5（安全系数）≈ 2.1 → 3 副本
```

**三个修正项**：

| 修正项 | 说明 |
|---|---|
| LLM 外调是瓶颈 | chat 的耗时大头在模型 API（TimeLimiter 60s 兜底），CPU/堆往往不紧张——扩实例前先确认瓶颈在谁 |
| 有状态约束 | SSE 长连接要求会话粘住同一实例，水平扩容需网关/负载均衡配合 |
| 依赖预算同步扩 | 实例 ×3 后，Hikari 连接 ×3、Redis 连接 ×3——回到 §4 重算依赖预算 |

---

## 六、弹性余量与水位线

| 原则 | 数值 | 本项目落点 |
|---|---|---|
| **容量水位线** | 稳态运行 ≤ 峰值容量的 50%；超 70% 触发扩容评审 | 依赖监控供数（Prometheus 已就绪），当前无自动水位告警（见 [07-告警治理](./07-告警治理.md) 目标态） |
| **N+1 冗余** | 任一组件应能在少 1 个实例时扛住峰值 | 与 [04-高可用](./04-高可用架构模式.md) 的无单点清单联动：现状多数组件 N=1，谈不上 N+1 |
| **磁盘水位** | ES/MQ 数据盘 ≤ 70% | MQ 死信与 ES 索引增长需纳入巡检 |
| **突发缓冲** | 突发系数 2~3 已计入峰值估算 | 网关 burst-capacity=10 是秒级突发缓冲的限流侧表达 |

**预算要留到哪一层**：只给无状态服务留弹性是不够的——瓶颈常在最底下（MySQL 连接、Redis 内存、ES 磁盘）。每一层都要有自己的水位线，这正是"依赖容量预算"与"实例数推演"必须成对出现的原因。

### 6.1 水位线的监控落点

水位线要变成告警才有意义（[07 篇](./07-告警治理.md) 接入 Alertmanager 后即为目标态）。每层一条代表性指标：

| 层 | 水位指标 | 采集来源 |
|---|---|---|
| JVM | `jvm_memory_used_bytes{area="heap"}` / 上限 | `/actuator/prometheus`（各服务） |
| MySQL 连接 | `Threads_connected / max_connections` | `SHOW GLOBAL STATUS LIKE 'Threads_connected'` |
| Redis 内存 | `used_memory / maxmemory`（§4.2 的 256mb 上限） | `redis-cli info memory` |
| 连接池 | `hikaricp_connections_active / max` | `/actuator/prometheus` |
| MQ 堆积 | Broker 队列积压量（consumer offset 差值） | RocketMQ 指标/控制台 |
| ES 磁盘 | 数据目录磁盘占用率 | `df -h` / `curl localhost:9200/_cat/allocation` |
| 入口限流水位 | 网关 429 占比（配额是否被正常流量吃满） | `http_server_requests_seconds_count{status="429"}` |

---

## 七、面试高频问答

**Q1：线上服务怎么做容量规划？**
A：五步法：业务量拆请求 → 二八原则算峰值 QPS → 压测估单机能力 → 沿调用链推依赖放大倍数 → 留水位线与 N+1 余量。关键是要有推演链，每步可用压测和监控校准，而不是拍一个实例数。

**Q2：二八原则在容量估算中怎么用？**
A：80% 流量集中在 20% 时间。用日均量 × 0.8 ÷ 峰值时段秒数得峰值均值，再乘 2~3 突发系数。注意分布式链路还有"放大效应"——入口 1 QPS 在内部可能是 5 次依赖调用，要逐层算。

**Q3：微服务扩容时，为什么先爆的往往是数据库？**
A：每个应用实例都带连接池，实例数 × 池大小是加法膨胀，而 MySQL max-connections 是硬上限。本项目 MySQL 500 连接上限，7 服务 ×3 副本就占 42%。解法是读写分离分流读连接、收敛池大小、必要时引入连接代理。

**Q4：Redis 的 maxmemory 与淘汰策略怎么选？**
A：先估数据集大小（缓存条目 × 平均体积）留 30% 余量定 maxmemory；纯缓存选 allkeys-lru，有 TTL 语义需求的选 volatile 系列；限流计数器这类高价值小 key 不能和大数据量缓存混在一个淘汰池，否则会被静默驱逐导致限流失效。

**Q5：JVM 堆内存怎么定？越大越好吗？**
A：不是。堆越大 GC 停顿越长（尤其 Full GC），且吃机器内存。要按对象存活集合估算：本学习贯彻性服务 256MB 起、512MB 上限够用；数据缓存型服务（如 ES）堆更吃紧但也不超过物理内存一半。先压测再调，别凭感觉。

**Q6：SSE/长连接服务的容量估算和普通 HTTP 服务有什么不同？**
A：长连接占用的是并发会话数而非 QPS——容量单位从"每秒请求"变成"同时挂着的连接"。每个连接持有会话上下文与流缓冲，内存是主要约束；且连接有粘性，水平扩容要考虑会话迁移。压测要用阶梯 VU 模型（本项目 sse-chat.js 的 ramping-vus 到 20 VU 就是这个思路）。

**Q7：怎么判断该扩容了？**
A：看三个水位：资源水位（CPU/内存/连接池 >70%）、体验水位（p95 时延逼近 SLO 红线）、饱和度水位（MQ 积压、队列长度持续上涨）。任一越线触发扩容评审。前提是这些指标已经在采（Prometheus），否则只能靠用户报障——那就太晚了。

**Q8：压测在容量规划里扮演什么角色？**
A：三个用途：① 校准"单实例能力"这个最大不确定项（推演链里唯一必须实测的数）；② 找拐点（多少并发后 p95 开始非线性恶化，见动手练习 5）；③ 验证扩容后的容量结论。注意压测环境要与生产同构（数据量、依赖、JVM 参数——本项目 compose 的 JAVA_OPTS 就是压测前提），否则数字没有迁移意义。脚本可复用 `scripts/loadtest/k6/` 两枚：恒定到达率测限流拐点、阶梯 VU 测 SSE 并发拐点。

**Q9：单机部署的容量账怎么算成"集群版"？**
A：单机总账（§3.3）是下限，集群化后账本变化三点：① 每个节点要去掉"故障转移余量"——按 N+1 原则，任意单节点宕机时剩余节点必须扛住全量；② 跨节点通信开销（Nacos 心跳、MySQL 复制、ES 集群状态同步）本身吃带宽与内存，单组件内存预算要上浮 10~20%；③ 分片类组件（ES/Redis Cluster）要按"分片数 × 副本数"重算，比单机全量复制更省内存。本项目的三机拆分骨架 `docker-compose-host1/2/3.yml` 已在 `deploy/docker-compose/` 备好，可作为分机重算的起点。

---

## 八、动手练习

1. 按 §3.3 口径给 `deploy/docker-compose/docker-compose-host1.yml`（分机部署版）重算一份内存总账，对比单机全量版差多少。
2. 检查各 Java 服务 Hikari 池配置（grep `maximum-pool-size` 或读 `ai-cs-*/src/main/resources/application.yml`），把 §4.1 的连接加总表填成真实值。
3. 假设 chat 服务要扩到 3 副本：列出所有需要同步重算的依赖预算（MySQL 连接、Redis 连接、Nacos 心跳、LLM API 配额），写成一页扩容检查单。
4. 用 `redis-cli info memory` 观察本机 Redis 的 used_memory 与 maxmemory 比例；再 `redis-cli info stats` 看 evicted_keys 是否大于 0——大于 0 说明已发生静默驱逐（§4.2 的风险已现形）。
5. 给 `sse-chat.js` 写一个 200 VU 的压力变体（只改 stages），在本机跑通后记录：多少 VU 时首 token p95 跌破 §01 篇定义的 2s SLO——这就是该服务的实测容量拐点（⚠️ 注意在本地环境跑）。

---

> 上一篇：[01-SLI-SLO与错误预算](./01-SLI-SLO与错误预算.md) ｜ 下一篇：[03-故障演练与混沌工程](./03-故障演练与混沌工程.md)
