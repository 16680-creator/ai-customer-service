# 故障演练与混沌工程

> 对应项目：目前**没有**演练工具与演练记录（本篇为预研方案 + 可直接执行的清单）；
> 演练的"预期现象"全部锚定真实代码行为：`ai-cs-chat/src/main/java/com/aics/chat/cache/VectorCacheStore.java`（Redis 降级 L1-only）、
> `ai-cs-chat/src/main/resources/application.yml`（resilience4j 配置）、
> `ai-cs-gateway/src/main/resources/application.yml`（限流 Redis 兜底开关）。
> 相关：[04-高可用架构模式](./04-高可用架构模式.md)（演练验证的就是架构假设）、[05-降级预案与开关治理](./05-降级预案与开关治理.md)（预案要在演练里验证才可信）、[08-oncall手册与故障响应](./08-oncall手册与故障响应.md)（演练也是值班训练）。

---

## 一、混沌工程是什么、不是什么

> **⚠️ 工程未落地，本篇为预研方案 + 本地演练清单。**
> 现状证据：`learning-docs/00-学习路线总览/05-技术缺口分析与补全计划.md:51` 的 B 类缺口表明确记录——"故障演练 | 治理计划提到'真实故障演练'未执行 | 演练脚本 + 记录文档（kill -9 / 网络分区 / MQ 不可用）| 验收标准：每项演练有'现象-恢复-验证'三段记录"。全仓 grep `chaos` 无任何 ChaosBlade / Chaos Mesh 部署痕迹。
> 要补什么：本篇第五节给出可直接执行的 6 个演练项；工具引入见第四节对比与选型。

| 混沌工程**是** | 混沌工程**不是** |
|---|---|
| 主动注入故障，**验证**容错设计是否真的生效 | 随手把生产服务搞挂的"胆子大" |
| 基于假设："我以为 Redis 挂了系统还能跑" → 用实验证实/证伪 | 盲目破坏：没有假设、没有观察、没有预期 |
| 有爆炸半径控制（一次只炸一个、随时可停） | 连环炸：同时破坏多个组件，现象无法归因 |
| 产出物：演练记录 + 架构改进项 | 产出物：一次"还好没炸出事"的心跳回忆 |

**为什么值得做**：本项目的容错代码（`VectorCacheStore` 的降级、Resilience4j 的熔断）都只被**单元测试**验证过"代码路径正确"，从没被验证过"真实 Redis 掉线时行为符合预期"——两者之间隔着超时参数、连接池行为、线程阻塞等一整层现实。

---

## 二、演练场景设计四要素

每个演练项必须先回答四个问题，缺一个就别开炸：

| 要素 | 问题 | 反例 |
|---|---|---|
| **假设** | 我预期系统如何表现？ | "炸了看看" |
| **爆炸半径** | 影响哪些用户/功能？如何最小化？ | 在共享环境炸 MySQL |
| **观察手段** | 用什么指标/日志判断现象？ | 全靠肉眼感觉 |
| **中止条件** | 出现什么情况立即恢复？ | 没有回滚开关 |

---

## 三、演练场景总表（绑定 ai-cs 真实部署）

场景设计原则：**从"最可能先挂的依赖"炸起**。下表按此排序（Redis 被最多业务路径依赖，MQ 决定异步链路生死）：

| # | 场景 | 注入方式 | 预期现象（锚定代码） | 验证重点 |
|---|---|---|---|---|
| 1 | Redis 进程死亡 | `docker stop aics-redis` | 向量缓存降级 L1-only（`VectorCacheStore.java:73-77` 捕获异常仅 warn）；网关分布式限流失效（`application.yml:11-13` 注释自述） | 对话链路是否仍可用；限流失效后 QPS 表现 |
| 2 | chat 服务被 kill -9 | `docker kill aics-chat-service` | 网关 502/超时；Nacos 心跳超时后实例摘除 | 实例摘除耗时；进行中 SSE 流的中断表现 |
| 3 | MQ 不可用 | `docker stop aics-rocketmq-broker` | 消息服务发送失败、订单超时兜底扫描接管 | 生产者重试与本地兜底是否触发 |
| 4 | MySQL 主库宕机 | `docker stop aics-mysql-master` | 写接口全部失败（无自动切换，见 04 篇）；从库只读 | 故障影响面清单；是否误把写打到只读从库 |
| 5 | Nacos 失联 | `docker stop aics-nacos` | 已注册实例间调用短期正常，新实例发现失效；配置热更新失效 | 服务发现的降级窗口 |
| 6 | 网络分区 | `docker network disconnect aics-network <容器>` | 同 #1/#5 但更真实（进程活着、网络不通） | TCP 超时参数下的表现 vs 进程死亡的差异 |

---

## 四、ChaosBlade vs Chaos Mesh 选型对比

| 维度 | ChaosBlade | Chaos Mesh |
|---|---|---|
| 出品 | 阿里开源 | PingCAP 开源，CNCF 孵化 |
| 形态 | 单二进制 CLI（blade create ...），可无守护进程直接注入 | K8s CRD（PodChaos/NetworkChaos/...），需部署 controller |
| 依赖 | 无，二进制即用 | 强依赖 Kubernetes |
| 适配本项目 | ✅ 本地 Docker Compose / 主机进程均可直接用 | ⚠️ 仅 `deploy/k8s/` 场景可用，且 compose 部署形态用不上 |
| 故障类型 | 进程杀、CPU/内存压力、网络丢包延迟、磁盘满 | 同类全覆盖 + 时间偏移、IO 故障，实验编排更强 |
| 学习成本 | 低（一条命令一个实验） | 中（CRD + 实验生命周期） |
| **本项目建议** | **首选**：与当前 Compose 部署形态匹配，起步零成本 | 进阶：K8s 化之后引入（`deploy/k8s/` 已有 8 份 service manifest） |

> 选型结论：先用 ChaosBlade 在本地 Compose 环境跑通第五节清单，K8s 化后迁移 Chaos Mesh 做常态化演练。没有工具时，`docker stop` / `docker kill` / `docker network disconnect` 也足以完成本篇全部演练——**工具不是演练的门槛，记录才是**。

---

## 五、可直接执行的演练清单（本地环境）

> **⚠️ 安全声明：以下命令只允许在本地 Docker 环境（`deploy/docker-compose/docker-compose-all.yml`）执行，严禁在任何共享/生产环境执行。** 每项演练 = 准备 → 注入 → 记录现象 → 恢复 → 验证。

### 演练 1：Redis 宕机 → 向量缓存降级

```bash
# 准备：服务已起，确认基线正常
curl -s localhost:8083/actuator/health
redis-cli ping                       # PONG

# 注入
docker stop aics-redis

# 预期现象（来自代码承诺）：
#   VectorCacheStore 读路径捕获异常，日志出现
#   "向量缓存 L2 读取失败，降级 L1-only"（VectorCacheStore.java:75）
#   对话接口仍返回，只是 Embedding 每次现算（变慢不失败）
# 验证命令：
curl -s -X POST "localhost:8083/chat/stream/sse?sessionId=drill1&message=你好" -H "Accept: text/event-stream"
docker logs aics-chat-service 2>&1 | grep "降级 L1-only" | tail -3

# 恢复
docker start aics-redis && redis-cli ping

# 验证恢复：日志不再新增 warn，Redis 读写恢复
```

**记录要点**：降级是否静默成功？首 token 时延劣化了多少（k6 前后对比）？网关限流同时失效了（Redis 是分布式令牌桶的存储），是否需要按 `ai-cs-gateway/src/main/resources/application.yml:36-40` 注释指引切回本地限流兜底？

### 演练 2：kill -9 chat 服务

```bash
# 注入（kill -9 模拟进程崩溃，无优雅停机）
docker kill aics-chat-service        # 区别于 docker stop（会发 SIGTERM 走优雅停机）

# 预期现象：
#   网关对该路由 502/503；Nacos 在心跳超时后摘除实例（默认 15s 无心跳 + 30s 剔除）
# 验证命令：
curl -s -o /dev/null -w "%{http_code}\n" localhost:8080/api/chat/stream/sse   # 期望非 200
docker start aics-chat-service       # 恢复
# 验证恢复：
curl -s localhost:8083/actuator/health
```

**记录要点**：从 kill 到网关报错多久？Nacos 摘除实例实际耗时多久（对照 `docker-compose-all.yml:53-58` 的 Nacos 配置）？恢复后服务重新注册耗时？

### 演练 3：MQ 不可用

```bash
# 注入
docker stop aics-rocketmq-broker     # NameServer 还活着，但路由上已无可用 Broker

# 预期现象：
#   message 服务发送消息报 RemotingException/超时；消费停止
#   订单超时场景：延迟消息不可用，需靠 XXL-Job/定时扫描兜底路径接管
# 验证命令：
docker logs aics-message-service 2>&1 | tail -20
# 恢复
docker start aics-rocketmq-broker
```

**记录要点**：发送方重试了几次？有没有消息丢失（对比发送与消费条数）？兜底扫描是否真的接管了订单超时关闭？

### 演练 4：MySQL 主库宕机

```bash
# 注入（需要主从 compose：deploy/docker-compose/docker-compose-master-slave.yml）
docker stop aics-mysql-master

# 预期现象：
#   所有写接口失败（本项目无主从自动切换，切换路径见 04 篇）
#   从库仍在，但 read-only=1（deploy/mysql/slave.cnf:11），写请求打到从库会报错
# 验证命令：
mysql -h127.0.0.1 -P3307 -uroot -proot -e "SHOW VARIABLES LIKE 'read_only';"
# 恢复
docker start aics-mysql-master
# 验证：从库追平主库
mysql -h127.0.0.1 -P3307 -uroot -proot -e "SHOW SLAVE STATUS\G" | grep Seconds_Behind_Master
```

### 演练 5：Nacos 失联

```bash
# 注入
docker stop aics-nacos

# 预期现象：
#   已启动实例间的调用暂时正常（客户端有本地服务列表缓存），但：
#   新实例注册失败、实例摘除失效、配置热更新失效
# 验证命令：
curl -s -o /dev/null -w "%{http_code}\n" localhost:8848/nacos/v1/console/health/readiness
# 恢复
docker start aics-nacos
```

**记录要点**：服务发现降级窗口内跨服务调用是否全部正常？多久后开始报错？——答案决定 Nacos 单点的爆炸半径（见 04 篇）。

### 演练 6：网络分区（进阶）

```bash
# 注入：把 Redis 从网络里"拔掉"（进程活着、网络不通，比 stop 更接近真实故障）
docker network disconnect aics-network aics-redis

# 预期现象：同演练 1，但关注 TCP 超时路径——连接超时 vs 拒绝连接的错误类型差异
# 验证命令：
docker exec aics-chat-service sh -c "timeout 3 sh -c 'echo > /dev/tcp/aics-redis/6379' && echo ok || echo fail"
# 恢复
docker network connect aics-network aics-redis
```

---

## 六、演练记录三段式模板

> 这正是缺口文档（`05-技术缺口分析与补全计划.md:51`）定的验收标准："每项演练有'现象-恢复-验证'三段记录"。每演完一项，按此模板归档。

```markdown
## 演练记录：Redis 宕机 → 向量缓存降级
- 日期/执行人/环境：2026-XX-XX / xxx / 本机 docker compose
- 假设：Redis 挂掉后对话链路可用，仅 Embedding 变慢
- 爆炸半径：语义缓存失效、分布式限流失效、网关退回本地限流需手动切

### 一、现象（注入后观察到什么）
- 14:02 docker stop aics-redis
- 14:02 chat 日志出现 "向量缓存 L2 读取失败，降级 L1-only"（符合预期 ✅）
- 14:03 对话接口仍 200，首 token p95 从 1.8s 升至 3.1s（k6 前后对比）
- 14:03 意外发现：网关限流完全放行（分布式令牌桶失效，预期外 ⚠️）

### 二、恢复（做了什么）
- 14:06 docker start aics-redis
- 14:06 redis-cli ping → PONG

### 三、验证（怎么确认恢复 + 遗留风险）
- 14:07 日志无新增 warn；健康检查 UP
- 遗留项：①网关限流 Redis 依赖无自动兜底（改进项 → 05 篇预案）②首 token 劣化幅度待定 SLO 后量化（→ 01 篇）
```

**三段式的价值**：现象回答"发生了什么"，恢复回答"多久能回来"，验证回答"下次怎么早知道"——最后一行的**遗留改进项**就是混沌工程的真实产出：每一条都应进入 [06-RCA](./06-RCA根因分析与复盘.md) 的行动项闭环跟踪。

**演练频率建议**：新人加入必做一遍核心三项（演练 1/2/3）；预案每次变更后回归对应演练；每季度至少一轮全清单（6 项）——演练的价值随间隔衰减，半年不练，预案和人的记忆一起失效。

---

## 七、面试高频问答

**Q1：什么是混沌工程？和故障演练有什么区别？**
A：混沌工程是对分布式系统主动注入故障、验证容错假设的纪律，强调假设-爆炸半径-观察-中止四要素；故障演练是它的工程化落地形式。区别在方法论：演练可以只验证单一预案（如切换脚本），混沌工程追求建立常态化实验机制、系统性暴露未知弱点。

**Q2：为什么要在"没出事"的时候故意制造故障？**
A：容错代码只有在真实故障路径上才会执行。单元测试只验证了代码分支正确，验证不了超时参数、连接池耗尽、线程阻塞这些现实因素。在受控环境提前暴露，代价是可控的演练记录；在深夜生产暴露，代价是 P1 故障。选择前者是工程成熟度的标志。

**Q3：ChaosBlade 和 Chaos Mesh 怎么选？**
A：看部署形态。物理机/Compose 环境选 ChaosBlade——单二进制无依赖，一条命令注入；已 K8s 化选 Chaos Mesh——CRD 声明式实验、可编排、CNCF 生态。本项目当前 Compose 部署，首选 ChaosBlade，K8s 化后可迁移。

**Q4：演练的爆炸半径怎么控制？**
A：一次只注入一个故障；先在本地/测试环境；准备好中止与恢复命令再动手；对共享资源（数据库、MQ）的演练安排在无人使用时段；生产演练必须从最小流量比例开始（如先对 1% 请求注入延迟）并有一键终止开关。

**Q5：演练记录为什么要"现象-恢复-验证"三段？**
A：现象沉淀对故障的真实认知（含预期外发现）；恢复量化 MTTR 并暴露预案缺口；验证确保系统真的回到基线且把遗留风险转成改进项。只有三段都写，演练才从"折腾一次"变成"资产沉淀"。

**Q6：你们项目最该先演练哪个故障？**
A：Redis 宕机。理由：它是被依赖最多的组件（向量缓存、语义缓存、限流令牌桶、分布式锁都用它），且降级代码已存在（VectorCacheStore 捕获异常降级 L1-only）但从未在真实故障下验证；演练还能顺带暴露分布式限流失效这个预期外风险，一次投入双重回报。

**Q7：演练发现预案没生效，接下来怎么处理？**
A：按 RCA 流程走：记录现象 → 定位预案失效原因（代码 bug？参数不对？条件没覆盖？）→ 修复后回归演练验证 → 改进项闭环跟踪。绝不能"发现就完了"，否则下次真实故障会在同一个地方摔倒——这正是演练与 06 篇复盘机制的衔接点。

---

## 八、动手练习

1. 在本地环境执行演练 1 完整流程，按第六节模板写一份真实的三段式记录，特别验证"分布式限流失效"这个预期外现象是否出现。
2. 对比 `docker kill` 与 `docker stop aics-chat-service` 的差异（后者触发 `application.yml:2` 的 graceful shutdown），分别记录进行中 SSE 流的表现差异。
3. 把演练 4 跑通后回答：从库 `read-only=1` 但 `super-read-only=0`（`deploy/mysql/slave.cnf:12`）意味着什么？哪个账号仍能写从库？
4. 编写一个 shell 脚本 `drill-redis.sh`，实现演练 1 的注入/验证/恢复三步一键执行，并在脚本开头加环境安全检查（如校验 `docker ps` 里只有本地容器才允许继续）。
5. 演练 5 结束后，翻看各服务日志中 Nacos 重连的表现，回答：Nacos 失联 10 分钟，哪些功能仍正常、哪些开始异常？把结论补进 04 篇的单点清单。

---

> 上一篇：[02-容量规划与性能预算](./02-容量规划与性能预算.md) ｜ 下一篇：[04-高可用架构模式](./04-高可用架构模式.md)
