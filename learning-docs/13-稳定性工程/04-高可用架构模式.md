# 高可用架构模式

> 对应项目：`deploy/docker-compose/docker-compose-all.yml`（单机全量部署，现状基线）、
> `deploy/docker-compose/docker-compose-master-slave.yml`（1主2从 + 3 Redis 节点拓扑）、
> `deploy/mysql/master.cnf` / `slave.cnf` / `slave2.cnf`、`deploy/redis/redis.conf`、
> `deploy/k8s/services/`（8 份 K8s manifest）。
> 相关：[02-容量规划与性能预算](./02-容量规划与性能预算.md)（冗余的容量账）、[03-故障演练与混沌工程](./03-故障演练与混沌工程.md)（演练验证切换路径）、[03-数据库与ORM/05-MySQL锁机制与主从复制读写分离](../03-数据库与ORM/05-MySQL锁机制与主从复制读写分离.md)（复制原理）。

---

## 一、可用性：几个 9 与停机预算

| 可用性 | 年停机 | 达成手段量级 |
|---|---|---|
| 99% | 3.65 天 | 单机 + 重启脚本 |
| 99.9% | 8.76 小时 | **冗余 + 自动故障转移**（本篇主题） |
| 99.99% | 52.6 分钟 | 多机房 + 全自动化 + 常态演练 |
| 99.999% | 5.26 分钟 | 极高成本，一般业务不必追求 |

**冗余只解决"有备份"，故障转移解决"备份怎么顶上"，仲裁解决"谁来宣布切换"**——三者缺一，多副本只是多了几个同时挂的组件。脑裂则是转移机制自身的故障态：两个节点都认为自己是主，各收各的写。

### 1.1 四种冗余模式一览

| 模式 | 结构 | 切换方式 | 一致性代价 | 典型代表 |
|---|---|---|---|---|
| 主从复制 | 1 主写 + N 从读/备 | 手工或外部组件（哨兵类） | 异步有丢数据窗口 | MySQL 主从、Redis 主从 |
| 多数派协议 | N 节点选主，写入需 quorum 确认 | **自动**（协议内置） | 已提交不丢，写延迟 = 多数派 | RocketMQ Dledger（Raft）、ZooKeeper、etcd |
| 分片集群 | 数据拆分到多组主从 | 组内转移 + 客户端路由 | 每组各自保证 | Redis Cluster、ES 分片副本 |
| 双活/多活 | 两地各一套完整服务 | 流量调度 | 最高（冲突解决复杂） | 大厂单元化架构 |

**本项目定位**：当前唯一成形的是"主从复制"（MySQL），演进方向按组件分别是：MySQL 主从→半同步/自动切换，Redis→哨兵，RocketMQ→Dledger，都是**向多数派协议演进**以获得自动故障转移——这是理解本篇所有组件建议的主线。

---

## 二、项目现场：当前部署形态盘点（如实记录）

先给结论：**本项目当前绝大部分中间件是单点**，1主2从 MySQL 是唯一成形的冗余拓扑，且切换仍需手工。

### 2.1 docker-compose-all.yml（单机全量版）形态

| 组件 | 形态 | 证据（file:line） | 单点判定 |
|---|---|---|---|
| MySQL | 单实例 | `docker-compose-all.yml:9-30` | ❌ 单点 |
| Redis | 单实例 | `docker-compose-all.yml:33-49` | ❌ 单点 |
| Nacos | **standalone 模式** | `docker-compose-all.yml:60`（`MODE: standalone`） | ❌ 单点 |
| ES | **single-node** | `docker-compose-all.yml:90`（`discovery.type: single-node`） | ❌ 单点 |
| RocketMQ | 1 NameServer + 1 Broker，无 slave | `docker-compose-all.yml:107-141` | ❌ 单点 |
| 微服务 ×7 | 每服务 1 实例 | `docker-compose-all.yml:166-299` | ❌ 单点（`restart: always` 只兜进程崩溃，不兜宿主机故障） |

### 2.2 docker-compose-master-slave.yml（冗余版）形态

| 组件 | 形态 | 证据 | 判定 |
|---|---|---|---|
| MySQL | **1主2从**（3306/3307/3308） | `docker-compose-master-slave.yml:8-80` | ✅ 冗余 / ⚠️ 手工切换 |
| Redis | 3 个节点，但**未组集群、未配哨兵** | `docker-compose-master-slave.yml:83-138`；`deploy/redis/redis.conf` 无 `cluster-enabled`/`replicaof`/`sentinel` 配置 | ⚠️ 三台**互相独立**的 Redis，应用只连一台，冗余未成形 |
| Nacos | 仍是 standalone | `docker-compose-master-slave.yml:148-149` | ❌ 单点 |
| ES | 仍是 single-node | `docker-compose-master-slave.yml:179` | ❌ 单点 |
| RocketMQ | 仍是 1+1 无 slave | `docker-compose-master-slave.yml:195-233` | ❌ 单点 |
| Seata | 单实例 | `docker-compose-master-slave.yml:259-279` | ❌ 单点 |

### 2.3 一处真实的部署缺口（盘点时发现）

1. **K8s 部署清单缺服务**：`deploy/k8s/services/` 只有 8 份 manifest（gateway/user/knowledge/chat/search/message/notify/product），order/pay/mq 缺失——与 `learning-docs/00-学习路线总览/05-技术缺口分析与补全计划.md:50` 记录一致。

> 一处要澄清的"疑似漂移"：`docker-compose-master-slave.yml:66-68` 给 mysql-slave2 挂载的是
> `./deploy/mysql/slave2.cnf:/etc/mysql/conf.d/slave.cnf`（server-id=3），slave1 挂的是
> `slave.cnf`（server-id=2），两从库 server-id 并不重复。容易误读的点在于：两个容器内的
> 挂载目标名**都叫** `slave.cnf`——容器内路径相同不代表宿主配置文件相同，盘点时要以
> 冒号左侧的宿主源文件为准。这正是"高可用盘点必须逐行核对配置"的活教材。

**分机部署的现状**：`deploy/docker-compose/` 下另有三份分机编排（`docker-compose-host1.yml` / `host2.yml` / `host3.yml`），说明"多机拆分"的设计意图存在；但无论哪份编排，Nacos standalone、ES single-node、单 Broker 的中间件形态未变——**拆机器不等于去单点**，跨机后单点问题依旧，只是爆炸半径从"整机"变成"组件"。

---

## 三、MySQL 1主2从：切换路径与复制延迟

### 3.1 现有拓扑与复制配置

```
            ┌─ mysql-slave1 (3307, server-id=2, read_only=1)
mysql-master│   relay-bin ← IO线程 ← binlog(mysql-bin, ROW)
(3306, id=1)└─ mysql-slave2 (3308, server-id=3, read_only=1)
```

| 配置 | 值 | 位置 | 高可用含义 |
|---|---|---|---|
| binlog 格式 | ROW | `deploy/mysql/master.cnf:8` | 行级复制，数据一致性最好 |
| 复制模式 | 异步（默认） | master.cnf 无半同步插件配置 | 主库提交不等待从库，**宕机可能丢最近事务** |
| 从库只读 | `read-only=1`，`super-read-only=0` | `deploy/mysql/slave.cnf:11-12` | 普通账号只读，但 **root 仍可写**——切换时要注意 |
| 复制过滤 | replicate-do-db ×4 | `slave.cnf:15-18` | 只复制 4 个库，新增库要改配置否则不同步 |

### 3.2 切换路径（手工，现状无自动切换）

主库宕机时，本项目**没有** MGR/Orchestrator/哨兵类组件，切换必须手工执行：

```bash
# ① 确认主库真死了（不要误切）
mysqladmin -h127.0.0.1 -P3306 -uroot -proot ping    # 失败才继续

# ② 选数据最新的从库（Seconds_Behind_Master 最小者）
mysql -h127.0.0.1 -P3307 -uroot -proot -e "SHOW SLAVE STATUS\G" | grep -E "Seconds_Behind|Exec_Master"
mysql -h127.0.0.1 -P3308 -uroot -proot -e "SHOW SLAVE STATUS\G" | grep -E "Seconds_Behind|Exec_Master"

# ③ 停复制并清空从库身份，提升为新主
mysql -h127.0.0.1 -P3307 -uroot -proot -e "STOP SLAVE; RESET MASTER; SET GLOBAL read_only=0;"

# ④ 另一从库改指向新主（MASTER_LOG_FILE/POS 取自新主 SHOW MASTER STATUS）
mysql -h127.0.0.1 -P3308 -uroot -proot -e "STOP SLAVE; CHANGE MASTER TO MASTER_HOST='mysql-slave1', MASTER_LOG_FILE='mysql-bin.00000X', MASTER_LOG_POS=XXX; START SLAVE;"

# ⑤ 应用侧数据源切到新主（改 Nacos 里的 DB_HOST 配置 → 重启相关服务）
# ⑥ 旧主恢复后降级为从库挂到新主，避免双主
```

### 3.3 复制延迟的处理

| 手段 | 说明 | 本项目落点 |
|---|---|---|
| 监控延迟 | `SHOW SLAVE STATUS\G` 的 `Seconds_Behind_Master` | 已具条件（拓扑在），无采集告警（→ [07](./07-告警治理.md)） |
| 读写分离感知延迟 | 读自己写过的数据走主库（会话级路由） | ⚠️ 应用侧未接读写分离（[03-数据库与ORM/05](../03-数据库与ORM/05-MySQL锁机制与主从复制读写分离.md) 已记录此差距） |
| 半同步复制 | 至少 1 从库收到 binlog 才确认提交，降低丢数据窗口 | 需装 `rpl_semi_sync` 插件，现未配置 |
| GTID 复制 | `gtid_mode=ON` 简化切换时的位点计算 | master.cnf 未开启，切换脚本才需要手工对位点 |

**GTID 改造示例**（master.cnf 与 slave.cnf 同步追加，主从需全部重启）：

```ini
# master.cnf 追加
gtid_mode=ON
enforce-gtid-consistency=ON
# slave.cnf 追加后，CHANGE MASTER 改用：
#   CHANGE MASTER TO MASTER_AUTO_POSITION=1;   -- 不再需要手工对 MASTER_LOG_FILE/POS
```

**延迟观测命令**（纳入每周巡检，见 [08 篇](./08-oncall手册与故障响应.md) 作业单）：

```bash
mysql -h127.0.0.1 -P3307 -uroot -proot -e "SHOW SLAVE STATUS\G" | grep -E "Seconds_Behind_Master|Slave_IO_Running|Slave_SQL_Running"
# 期望：Seconds_Behind_Master = 0（或个位数毫秒级），IO/SQL 线程均 Yes
# 任一线程 No = 复制断链，比延迟严重一个量级，立即排查（常见：server-id 冲突、复制过滤漏库）
```

---

## 四、Redis：哨兵 vs 集群

### 4.1 两种方案对比

| 维度 | 哨兵（Sentinel） | 集群（Cluster） |
|---|---|---|
| 数据分布 | 全量数据在每个主从对上（主从复制） | 16384 slot 分片，数据拆到多主 |
| 解决的问题 | **高可用**（主挂自动选主） | 高可用 + **容量横向扩展** |
| 多 key 操作 | 无限制 | 跨 slot 受限（需 hash tag） |
| 客户端要求 | 普通客户端即可 | 必须支持集群协议（SMART 客户端） |
| 复杂度 | 低：3 哨兵 + 1主N从 | 高：至少 3主3从 |
| 适用 | 数据量单机放得下、要自动故障转移 | 数据量超单机、要写扩展 |

### 4.2 本项目的选择与现状差距

- **应选哨兵**：本项目 Redis 承载缓存+限流+锁，`maxmemory 256mb`（`deploy/redis/redis.conf:24`）说明数据量单机充裕，痛点是可用性而非容量——哨兵是更小步的改造。
- **现状**：master-slave compose 起了 3 个 Redis 容器（`docker-compose-master-slave.yml:83-138`），但 redis.conf 无 `replicaof`/`sentinel` 配置——**三个独立实例，无复制无仲裁**，应用连的哪个，哪个就是单点。
- **目标态**：node1 为主，node2/3 `replicaof` 挂主 + 3 个 sentinel 进程（quorum=2）→ 主挂自动切换，脑裂防护靠 `min-replicas-to-write=1`（主库收不到从库确认时拒绝写，防"孤立主"）。

---

## 五、RocketMQ：主从与 Dledger

| 方案 | 复制 | 切换 | 数据丢失窗口 | 说明 |
|---|---|---|---|---|
| 单 Master（**现状**） | 无 | 无 | 宕机丢未刷盘消息 | `docker-compose-master-slave.yml:195-233` 仅 1 namesrv + 1 broker；deploy 下**没有** broker.conf（无法确认刷盘策略，默认 ASYNC_FLUSH） |
| 主从（多副本） | 同步/异步复制 | **手工**（brokerId=0 为写主，主挂需人工改配置） | 同步复制≈0 | 运维简单但切换不自动 |
| **Dledger（Raft）** | 多数派复制 | **自动选主** | 已提交消息不丢 | RocketMQ 4.5+ 内置，3 节点起，quorum=2 |

**本项目建议**：订单超时、支付回调这类消息不允许丢（[04-中间件/08](../04-中间件/08-RocketMQ事务消息与死信队列.md) 已讲事务消息），生产态应上 Dledger 3 节点自动选主；学习环境保持 1+1 并把"Broker 是单点"写进预案（[05](./05-降级预案与开关治理.md)）。

---

## 六、Nacos 集群化

现状：两个 compose 里 Nacos 都是 `MODE: standalone`（`docker-compose-all.yml:60`、`docker-compose-master-slave.yml:149`），且配置数据存 MySQL（`MYSQL_SERVICE_DB_NAME: nacos_config`）。

**单点的双重含义**：Nacos 挂 → 服务注册发现失效（新实例不可见、摘除失效）+ 配置中心热更新失效；但客户端有本地快照缓存，**存量实例间调用可撑一段时间**——这个"降级窗口"有多长，正是 [03-故障演练](./03-故障演练与混沌工程.md) 演练 5 要实测的。

**集群化三要素（目标态）**：

1. **≥3 节点**：`MODE: cluster`，三实例互为 peer（8848/9848 端口全开）；
2. **共享 MySQL 存储**：三节点指向同一 nacos_config 库（本项目已有此库），避免 Raft 内嵌存储的额外运维；
3. **客户端配集群地址列表**：各服务 `spring.cloud.nacos.server-addr` 从单地址改为逗号分隔三地址；前端再加 SLB 收敛入口。

---

## 七、无单点检查清单

> 用法：每行如实勾选。当前状态为 2026-XX 盘点结果，**改进目标**列给出达成该行所需的最小改造。

| # | 组件 | 冗余 | 故障转移 | 当前判定 | 改进目标 |
|---|---|---|---|---|---|
| 1 | MySQL | ✅ 1主2从 | ❌ 手工切换 | 半高可用 | 半同步 + GTID；评估 Orchestrator/MHA 自动切换 |
| 2 | Redis | ❌ 3 独立实例 | ❌ 无 | 单点 | 哨兵 ×3 + 1主2从 + `min-replicas-to-write=1` |
| 3 | Nacos | ❌ standalone | ❌ 无 | 单点 | `MODE: cluster` ×3 + 共享 MySQL 存储 |
| 4 | ES | ❌ single-node | ❌ 无 | 单点 | 3 节点集群 + 副本分片 ≥1 |
| 5 | RocketMQ | ❌ 1 broker | ❌ 无 | 单点 | Dledger ×3 自动选主；备好 broker.conf |
| 6 | 网关 | ❌ 1 实例 | ❌ 无 | 单点 | ≥2 副本 + 前置 SLB（8080 入口收敛） |
| 7 | 业务服务 | ❌ 各 1 实例 | Nacos 心跳摘除（仅注册层） | 单点 | 核心服务（chat/order/pay）≥2 副本 |
| 8 | K8s 化 | — | — | 8/11 服务有 manifest | 补 order/pay/mq manifest（`deploy/k8s/services/`） |
| 9 | 宿主机 | 全部单机 | ❌ | **所有组件共生死** | 拆多机（host1/host2/host3 compose 已备而未用） |
| 10 | 切换演练 | — | — | 未执行 | 按 [03 篇](./03-故障演练与混沌工程.md) 清单逐项演练验证 |

> 用法提醒：这张清单**每季度重盘一次**，并作为演练选题的输入——清单里"❌ 无"最多的组件，就是下一轮演练该炸的对象；每次架构改进（如上了哨兵）后更新对应行，让清单始终反映实机而非计划。

---

## 八、面试高频问答

**Q1：高可用架构的核心三要素？**
A：冗余（有备份）、故障转移（备份能自动顶上）、仲裁（有权威方决定谁当主）。只做冗余不解决转移，故障时仍需人工介入；没有仲裁就可能脑裂——两个节点都自称主、各收各的写，比单点更难收拾。

**Q2：什么是脑裂？怎么防？**
A：网络分区时，原主与备主同时存活且都接受写，数据分叉。防护手段：仲裁方多数派投票（哨兵 quorum、Raft 多数派）、资源隔离 fencing（旧主被 STONITH 杀掉或降权）、写入侧保险（如 Redis `min-replicas-to-write`：主库连不上足够从库就拒绝写）。

**Q3：MySQL 主从切换的完整路径？**
A：确认主库真死 → 比较各从库 `Seconds_Behind_Master` 选最新者 → 停复制、提升为新主（清从库身份、关只读）→ 其余从库 CHANGE MASTER 指向新主 → 应用数据源切换 → 旧主恢复后降级为从。本项目无自动切换组件，以上全部手工，且 master.cnf 未开 GTID，位点要手工核对。

**Q4：主从复制延迟怎么办？**
A：先量化（Seconds_Behind_Master 监控）；再按业务分治——强一致读走主库（如"下单后立刻查订单"），可容忍读走从库；写后立读的会话级路由（读自己刚写的数据回主库）；架构上可上半同步复制缩小窗口。本项目应用侧尚未接读写分离，从库目前只作冗余备份。

**Q5：Redis 哨兵和集群怎么选？**
A：看瓶颈是可用性还是容量。数据单机放得下、只要主挂自动切——哨兵（3 哨兵 + 1主N从），复杂度低、多 key 操作无限制；数据超单机、要写扩展——集群（16384 slot 分片，至少 3主3从），但跨 slot 操作受限、客户端要求高。本项目 maxmemory 256mb 单机充裕，选哨兵更合适。

**Q6：RocketMQ 的 Dledger 解决了什么问题？**
A：传统主从模式下 broker 主挂了不能自动切换（要人工改 brokerId 配置）。Dledger 基于 Raft：多数派复制 + 自动选主，3 节点挂 1 个仍可写，已提交消息不丢。代价是至少 3 节点的资源成本与 Raft 协议的运维认知。

**Q7：Nacos 单点挂了系统会立刻瘫痪吗？**
A：不会立刻。客户端有服务列表本地快照，存量实例间调用可继续一段时间；但新实例注册、故障实例摘除、配置热更新立即失效。真正的风险窗口是"降级期间再有实例挂掉无人摘除"。所以要演练实测这个窗口时长，再决定集群化的优先级。

**Q8：`restart: always` 算高可用吗？**
A：不算。它只覆盖"进程退出"这一种故障，进程卡死不退出（如死锁、连接池耗尽）不会触发重启；宿主机宕机、依赖不可达它都无能为力。健康检查 + 编排层重建（K8s liveness）+ 冗余副本 + 故障转移才是完整的链路。

**Q9：MySQL 半同步复制为什么没有普及到所有场景？**
A：代价是写延迟——每次提交要等至少一个从库确认 binlog，跨机房部署时往返延迟直接叠加到写 RTT；且从库全挂时存在"阻塞写"还是"退回异步"的策略抉择（`rpl_semi_sync_master_timeout`）。所以它适合"主从同机房、写延迟预算充裕"的场景；跨机房/高写入场景更常用组复制（MGR）或干脆靠业务幂等 + 对账兜底。本项目单机学习环境装半同步零成本，值得作为演练的一部分验证切换时数据零丢失。

---

## 九、动手练习

1. 核对两从库的配置互不冲突：确认 compose 中 slave1 挂 `slave.cnf`（server-id=2）、slave2 挂 `slave2.cnf`（server-id=3，注意容器内挂载名相同但宿主文件不同），起拓扑后用 `SHOW SLAVE STATUS\G` 验证两从库 IO/SQL 线程均为 Yes。
2. 在本地把三个 Redis 节点改造成"1主2从 + 3 哨兵"：给 node2/node3 加 `replicaof`，写 3 份 sentinel.conf（quorum=2），然后 `docker stop` 主节点验证自动切换耗时。
3. 跑通 §3.2 的手工切换脚本全流程（本地环境），记录每步耗时，回答"手工切换的 RTO 大概多少分钟"。
4. 给 master.cnf 设计半同步复制改造：写出需要追加的配置行（插件安装 + `rpl_semi_sync_master_wait_for_slave_count=1`），并说明对写时延的影响。
5. 把 §7 清单逐行核对一遍你本机的实际部署（`docker ps` 对照），更新"当前判定"列——高可用盘点必须以实机为准，而不是以文档为准。
6. 对比 `docker-compose-all.yml` 与 `docker-compose-host1.yml` 的中间件形态差异，回答：把组件拆到三台机器后，§7 清单里哪些行的判定会变、哪些不会？

---

> 上一篇：[03-故障演练与混沌工程](./03-故障演练与混沌工程.md) ｜ 下一篇：[05-降级预案与开关治理](./05-降级预案与开关治理.md)
