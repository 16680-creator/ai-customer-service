# 降级预案与开关治理

> 对应项目：`ai-cs-chat/src/main/java/com/aics/chat/cache/VectorCacheStore.java`（Redis 挂 → 降级 L1-only，现成正例）、
> `ai-cs-chat/src/main/java/com/aics/chat/config/Resilience4jConfig.java`（降级文案分级说明）、
> `ai-cs-chat/src/main/java/com/aics/chat/modelrouter/ModelHealthRegistry.java`（按模型隔离熔断）、
> `ai-cs-gateway/src/main/resources/application.yml`（限流本地兜底开关）。
> 相关：[04-高可用架构模式](./04-高可用架构模式.md)（冗余防故障，降级防故障扩散）、[06-RCA根因分析与复盘](./06-RCA根因分析与复盘.md)（预案失效的复盘）、[03-故障演练与混沌工程](./03-故障演练与混沌工程.md)（预案要演练验证）。
>
> **分工声明**：Sentinel/Resilience4j/Feign 等组件**怎么用**已在 [04-中间件/06-Sentinel限流与熔断降级](../04-中间件/06-Sentinel限流与熔断降级.md) 与 [02-Spring微服务/08-Feign熔断与降级语义](../02-Spring微服务/08-Feign熔断与降级语义.md) 讲透；本篇只讲**预案体系**——什么该降、降到什么程度、开关怎么治理。

---

## 一、降级的本质：核心依赖 vs 非核心依赖

**降级 = 有损服务**：主动牺牲非核心功能/体验，保住核心链路存活。它回答的问题与冗余不同——冗余防"组件挂"，降级防"故障扩散"（雪崩、级联失败）。

判断一个依赖可不可以降级，只问一个问题：**它挂了，核心链路是"不能跑"还是"跑得慢/糙"？**

| 分类 | 判定 | 处理原则 | 例子 |
|---|---|---|---|
| 核心依赖（强依赖） | 挂了核心功能直接不可用 | 不降级，靠冗余 + 故障转移（[04 篇](./04-高可用架构模式.md)） | MySQL（订单数据）、Nacos（服务发现） |
| 非核心依赖（弱依赖） | 挂了只是变慢/变糙 | **必须可降级**，设计时就写好降级路径 | 缓存、推荐、热榜、消息通知 |

**反模式**：把非核心依赖写成核心依赖——缓存查询抛异常直接让请求失败，就是最典型的"弱依赖强绑定"。本项目的 `VectorCacheStore` 是反面教材的**正解**（见 §3.1）。

### 1.1 降级的五种形态

"降级"不只有"返回兜底文案"一种，按改动面从小到大：

| 形态 | 做法 | 代价 | 例子 |
|---|---|---|---|
| **兜底值** | 失败返回默认/缓存旧值 | 数据可能略旧 | 热榜读不到就返回上一份快照 |
| **旁路跳过** | 非核心步骤直接跳过 | 功能缺失 | 推荐位不展示，主内容照常 |
| **功能关闭** | 开关关闭整个非核心功能 | 功能不可用 | 关闭语义缓存，只用实时检索 |
| **链路缩窄** | 降级到更弱的实现 | 体验下降 | ES 检索降级 MySQL LIKE |
| **读写限制** | 只读/排队/限流 | 写入延迟或拒绝 | DB 故障时全站只读、下单排队 |

**设计纪律**：降级形态要在**写代码时**就决定好（写在设计文档里），而不是故障时现场选——现场只会选"最省事"的那种，而最省事往往是最错的（比如直接抛异常）。

---

## 二、预案分级：P0 ~ P3

预案也要分级——不是所有故障都值得"一键全停"。分级让响应者不需要临场判断"该动多大的手术"：

| 级别 | 触发场景 | 动作量级 | ai-cs 示例 |
|---|---|---|---|
| **P3 预防性** | 性能劣化但未故障 | 提前限流/扩容 | 网关在流量爬坡期收紧 `burst-capacity` |
| **P2 旁路切换** | 非核心依赖故障 | 切到备用路径 | Redis 挂 → 向量缓存 L1-only；LLM 主模型熔断 → 路由备选模型 |
| **P1 功能降级** | 核心链路部分受损 | 关闭非核心功能 | 关闭热榜/推荐，保对话主链路；ES 检索降级为 MySQL LIKE |
| **P0 终极兜底** | 核心链路濒危 | 全局限流/只读/排队 | 全站限流保命、下单改排队异步处理 |

**三条纪律**：

1. **预案必须预先写好**——故障现场没人有时间设计降级方案，只有时间执行或找预案文档（没有就现场发明，发明就会错）。
2. **预案必须有验证手段**——执行后用什么命令确认"降级生效了"（见 §3 各正例的验证方式）。
3. **预案必须可回退**——故障恢复后能无损切回，否则预案本身变成新风险。
4. **预案必须留痕**——降级生效的时段在群里/值班记录声明，防止"系统带伤运行却无人知晓"（见 Q9 的防滥用）。

---

## 三、项目现场：四个现成的降级正例

### 3.1 VectorCacheStore：非核心依赖降级的教科书实现

`ai-cs-chat/src/main/java/com/aics/chat/cache/VectorCacheStore.java:73-77`（读路径）：

```java
} catch (Exception e) {
    // Redis 不可用/数据损坏：降级为 L1-only，不阻断检索
    log.warn("向量缓存 L2 读取失败，降级 L1-only: err={}", e.getMessage());
    return null;          // 返回 null → 上游去调 Embedding API 现算，链路继续
}
```

写路径同样降级（`:93-95`，Redis 写失败仅 warn 不抛出）。**它做对了三件事**：

| 要点 | 说明 |
|---|---|
| 捕获异常不扩散 | Redis 故障被隔离在缓存层，检索主链路无感 |
| 降级有痕迹 | `log.warn` 留下可观测证据（演练验证就 grep 这行日志） |
| 降级有损但可控 | 代价只是每次现算 Embedding（慢 + 费 API 配额），功能不缺失 |

**对照反例**：若这里不 catch，Redis 抖动 = 全站对话失败——一个 256MB 的缓存实例（`deploy/redis/redis.conf:24`）决定整个系统的生死，这就是弱依赖强绑定。

### 3.2 Resilience4j：按失败类型给出不同降级文案

`ai-cs-chat/src/main/java/com/aics/chat/config/Resilience4jConfig.java:26-31` 把降级语义写成了文档：

```
超时降级：返回"AI 助手响应超时，请稍后重试"
熔断降级：返回"AI 助手当前负载较高，服务暂时不可用"
重试耗尽：返回"AI 助手暂时繁忙，请稍后重试"
```

配置本体在 `ai-cs-chat/src/main/resources/application.yml:67-130`：非流式 30s TimeLimiter + 指数退避重试 3 次（`:72`、`:85-88`）→ COUNT_BASED 熔断（窗口 10 / 最小调用 3 / 失败率 50% / OPEN 30s，`:112-117`）；SSE 链路 60s 首 token 限制（`:74-77`）。组件用法详见 [02-Spring微服务/08](../02-Spring微服务/08-Feign熔断与降级语义.md)。

**预案体系视角的看点**：同一下游故障，按**失败阶段**给出不同降级——重试阶段（瞬时抖动）→ 熔断阶段（持续故障）→ 各自的用户话术。降级不是"报错"，是**设计过的用户沟通**。

### 3.3 ModelHealthRegistry：按模型隔离熔断 = 路由级降级

`ai-cs-chat/src/main/java/com/aics/chat/modelrouter/ModelHealthRegistry.java:18-21,34-43`：

```java
// 设计要点：熔断器按 modelId 隔离——若共享一个熔断器，
// 主模型故障会连累备用模型，路由降级就失去意义
public CircuitBreaker breaker(String modelId) {
    return breakers.computeIfAbsent(modelId, id -> circuitBreakerRegistry.circuitBreaker(id, config()));
}
```

配合 `isAvailable()` 只把 **OPEN** 状态的模型从路由候选剔除（HALF_OPEN 保留作恢复探测，`:23-31`）。这是一个完整的 P2 级预案：主模型持续失败 → 该模型熔断 OPEN → 路由自动切备选模型 → HALF_OPEN 探测恢复。**熔断器粒度 = 降级粒度**，共享一个熔断器会让"降级路由"名存实亡。

### 3.4 网关限流兜底开关：配置开关的真实现场

`ai-cs-gateway/src/main/resources/application.yml:11-13,36-40`：

```yaml
    # Redis 不可用时分布式限流失效，可临时把下方 aics.gateway.rate-limit.enabled
    # 改回 true 切回本地内存限流兜底
    rate-limit:
      enabled: false     # 默认关闭：已由 RequestRateLimiter（Redis 分布式令牌桶）替代
```

Redis 挂时的预案动作是现成的：把 `enabled` 改回 `true`（Nacos 配置热更新）→ 网关退回本地内存限流（单机配额，多实例不共享，但聊胜于无）。这正是 [03-故障演练](./03-故障演练与混沌工程.md) 演练 1 要验证的预案——**预案写在配置注释里也是预案，只要有人知道、演练验证过、能一键回退**。

---

## 四、功能开关 vs 配置开关

| 维度 | 功能开关（Feature Flag） | 配置开关（Config Switch） |
|---|---|---|
| 控制什么 | 代码行为的启停（新功能/灰度/实验） | 参数与外部资源指向（阈值、地址、兜底路径） |
| 载体 | 专门的开关服务（如 Unleash、自建 Flag 表） | 配置中心（本项目 = **Nacos**） |
| 生效方式 | 运行时读取判断分支 | 配置热更新 + `@RefreshScope`/监听器 |
| 粒度 | 用户/流量维度可分（灰度发布的基础） | 全局或实例维度 |
| 典型生命周期 | 上线前开 → 稳定后**删代码里的开关** | 长期存在（预案开关常驻） |

**本项目现状**：没有专门的功能开关框架（⚠️ 工程未落地），现有开关载体是 Nacos 配置中心——§3.4 的 `rate-limit.enabled` 就是标准配置开关。要补什么：若需要按流量灰度，参考缺口文档建议的"基于 Nacos 元数据 + 网关路由断言的灰度方案"（`learning-docs/00-学习路线总览/05-技术缺口分析与补全计划.md:43`）。

**开关治理四条军规**（目标态规范）：

1. **命名可检索**：`aics.switch.<场景>.<动作>`，一看键名知道控制什么；
2. **默认安全**：预案类开关默认值 = 最保守状态（如限流兜底开关的语义要写清 true/false 各是什么）；
3. **有主有据**：每个开关记录 owner、创建原因、关联预案文档；
4. **定期清理**：功能开关随代码删除；配置开关至少每季度审一次"还有没有引用"（缺口文档附录也记录过"死配置"问题——`pay.sandbox` 无人消费）。

### 6.1 配置热更新的最小实现（Nacos 是现成载体）

本项目各服务已接入 Nacos 配置（如网关 `application.yml:28-31` 的 `config.import: optional:nacos:aics-shared.yml`），消费一个预案开关只需 `@RefreshScope`：

```java
// 消费端示例：一个可热切换的降级开关（目标态示例代码）
@RestController
@RefreshScope                     // Nacos 配置变更时重建 Bean，无需重启
public class SearchFallbackController {
    @Value("${aics.switch.search.es-fallback:false}")   // 默认 false = 正常走 ES
    private boolean esFallback;

    @GetMapping("/search/mode")
    public String mode() {
        return esFallback ? "mysql-like（降级）" : "elasticsearch（正常）";
    }
}
```

验证方式：Nacos 控制台改 `aics.switch.search.es-fallback: true` → 不重启 → `curl localhost:8084/search/mode` 应立即返回降级态。**这就是"配置开关"与"改代码发版"的本质差别：预案生效时间从分钟级降到秒级**。

### 6.2 预案上线前的验证清单

| # | 检查项 | 不达标的后果 |
|---|---|---|
| 1 | 开关在 Nacos 有键、有默认值、默认值安全 | 故障时找不到开关 / 默认行为是错的 |
| 2 | 降级代码路径有日志痕迹 | 降级"悄悄发生"，没人知道系统已带伤运行 |
| 3 | 降级后的用户体验口径确认（文案/功能边界） | 降级引发二次报障 |
| 4 | 回退步骤演练过（[03 篇](./03-故障演练与混沌工程.md)） | 恢复时切不回去，预案变新故障 |
| 5 | 预案写进对应服务 runbook（[08 篇](./08-oncall手册与故障响应.md)） | 值班的人不知道有这个预案 |
| 6 | 开关语义在键名/注释里写清 true/false 各是什么 | 故障现场理解反了，把仅存的兜底也关掉 |

---

## 五、可降级依赖识别：ai-cs 依赖清单

预案体系的地基是一张**全量依赖清单**——把每个依赖的故障后果与预案写清楚：

| 依赖 | 用途 | 挂了会怎样 | 核心还是可降级 | 现有预案 | 缺口 |
|---|---|---|---|---|---|
| MySQL | 业务数据 | 写接口全失败 | **核心** | 无（单点见 04 篇） | 只读降级/排队写 |
| Redis | 缓存/限流/锁 | 变慢 + 限流失效 | **可降级** | ✅ §3.1 L1-only + §3.4 兜底开关 | 限流兜底需手动切换 |
| ES | 全文检索 | 搜索不可用 | 可降级 | ❌ 无 | 降级 MySQL LIKE/缩小检索范围 |
| RocketMQ | 异步解耦 | 消息堆积/丢失风险 | 视场景 | 定时扫描兜底订单超时 | 发送失败的重试与本地落库 |
| Nacos | 注册/配置 | 服务发现降级窗口 | 核心（有缓存缓冲） | 客户端本地快照 | 实测窗口时长（演练 5） |
| LLM API | 对话生成 | 对话失败 | **可降级** | ✅ §3.2/§3.3 熔断+模型路由 | 全模型不可用时的兜底话术 |
| MinIO | 知识库文件 | 上传/解析失败 | 可降级 | ❌ 无 | 上传排队/提示稍后再试 |
| MongoDB | 对话审计归档 | 审计写入失败（对话不受影响） | 可降级 | 视实现而定 | 本地缓冲/重投（见 [04-中间件/10](../04-中间件/10-MongoDB对话审计归档.md)） |
| XXL-Job | 定时调度兜底 | 定时任务停摆（如订单超时扫描） | 视任务而定 | ❌ 无 | 核对每个执行器与 MQ 兜底的互补关系 |

**识别方法**：顺着每条核心链路（对话/下单/支付）走一遍，画出依赖树，给每个节点标注"挂了会怎样"。凡是"挂了会怎样"答不上来的依赖，就是没做故障分析的依赖。

**这张表的维护**：依赖清单是活文档——新增依赖（如接入新 LLM 渠道）必须同步补一行，"现有预案"列与 [03 篇](./03-故障演练与混沌工程.md) 演练记录联动：预案被演练验证过打 ✅，否则打 ⚠️。评审时这张表可以直接回答"这个依赖挂了会怎样"的经典面试题。

---

## 六、面试高频问答

**Q1：什么是降级？和熔断、限流什么关系？**
A：限流是"少接客"（入口控制流量），熔断是"不去了"（下游故障时停止调用防雪崩），降级是"换个法子服务"（返回兜底数据/关闭非核心功能）。三者常组合：限流挡住超额流量 → 熔断阻止故障扩散 → 降级保住核心体验。

**Q2：怎么判断一个依赖该不该降级？**
A：问"挂了核心链路是不能跑还是跑得糙"。不能跑=核心依赖，用冗余+转移解决；跑得糙=非核心依赖，必须在设计期写好降级路径。反例是把缓存异常直接抛给请求——弱依赖强绑定。

**Q3：说一个你项目里真实的降级实现。**
A：`VectorCacheStore` 两级缓存：读路径 L1→L2→Embedding 现算，Redis 异常被 catch 后仅 warn 日志"降级 L1-only"，返回 null 让上游现算，检索链路不阻断；写路径失败同样静默。要点是三件事：异常不扩散、降级有日志痕迹、有损但可控。

**Q4：预案分级 P0-P3 怎么定？**
A：按动作量级从小到大：P3 预防性（限流/扩容）、P2 旁路切换（备用路径，如缓存降级、模型路由）、P1 功能降级（关非核心功能）、P0 终极兜底（全局限流/只读）。定级原则：能用低级别解决绝不升级，每级预案预先写好、可验证、可回退。

**Q5：功能开关和配置开关的区别？**
A：功能开关控制代码行为启停（需专门的 Flag 机制，支持按用户/流量维度），配置开关控制参数与资源指向（配置中心热更新即可）。预案开关通常是配置开关（如本项目网关 rate-limit.enabled 用 Nacos 管理），灰度发布才需要真正的功能开关体系。

**Q6：熔断器的粒度怎么设计？**
A：按"故障域"隔离——共享熔断器会让一个下游的故障连累其他候选路径。本项目 ModelHealthRegistry 按 modelId 每个模型独立熔断器，主模型熔断不影响备选模型被路由，路由降级才有意义。通用原则：一个熔断器对应一个独立故障域（模型/渠道/下游服务）。

**Q7：开关太多了怎么办？**
A：开关治理四军规：命名可检索、默认安全、有主有据、定期清理。功能开关随功能稳定从代码里删除；配置开关季度审计引用情况，消灭"死配置"。开关的本质是负债——每一个都要有存在理由，清理不掉的开关说明需求本身该重审。

**Q8：降级预案写好了但从来没执行过，可信吗？**
A：不可信。预案和代码一样需要"测试"——在演练里真实验证（Redis 停掉后 L1-only 是否如日志承诺生效、兜底开关切换后限流是否恢复）。没有演练过的预案只是文档，这正是混沌工程存在的意义。

**Q9：降级会不会被滥用，变成"反正有降级就不修了"？**
A：会，这是降级体系的腐化路径。防线有二：① 降级是**临时态**——每次降级生效都应自动/手动登记，长期降级状态进入问题清单限期根治；② 从数据上监控"降级时长占比"，一条链路 30 天内有 20% 时间在降级，说明根因没修，该进 [06 篇](./06-RCA根因分析与复盘.md) RCA 而不是继续忍。降级保的是可用性下限，不能替代对根因的修复。

**Q10：预案文档放哪、给谁看？**
A：两份分开：**预案清单**（本篇 §5 的依赖表 + 各预案的触发条件与动作）挂在模块级文档，给所有人建立全景认知；**执行步骤**（用什么开关、执行什么命令、验证什么指标）写进对应服务 runbook（[08 篇](./08-oncall手册与故障响应.md) §5 模板的"常见故障与预案"表），给故障现场的人按单执行。放错位置的预案等于没有预案——没人会在深夜翻 300 行设计文档找开关名。

---

## 七、动手练习

1. 走读 `ai-cs-chat/.../service/impl/ResilientAiService.java`（`@CircuitBreaker`/`@TimeLimiter` 引用处），画出"超时→重试→熔断→降级文案"的完整状态流转图，对照 `application.yml:67-130` 标注每个环节的参数。
2. 给 ES 检索设计一个 P1 级降级：ES 不可用时 chat 的混合检索退化为纯向量检索（或 MySQL LIKE），写出改动点清单与验证命令（不要实现，先写设计）。
3. 把 §5 依赖清单补完整：检查 `ai-cs-chat/src/main/resources/application.yml` 里还有哪些外部依赖（如视觉模型、MongoDB），逐个标注核心/可降级。
4. 在 Nacos 控制台给网关建一个配置开关 `aics.switch.search.es-fallback`（仅练习），用 `@RefreshScope` 消费它，验证热更新生效——体会"配置开关"与"改代码重启"的差别。
5. 结合 [03 篇](./03-故障演练与混沌工程.md) 演练 1 的记录，检查 §3.4 网关兜底预案：从"Redis 挂"到"切回本地限流"实际需要几步操作？设计一个能把步骤缩到 1 步的方案（提示：Nacos 配置联动或客户端自动降级）。
6. 给 §3.2 的三种降级文案（超时/熔断/重试耗尽）各补充一条"前端展示建议"——同一段 AI 话术，SSE 流式与同步接口的降级呈现方式应有什么不同？
7. 审计一个真实开关：读 `ai-cs-gateway/src/main/resources/application.yml:36-46` 的 `rate-limit` 段，用 §6 四军规逐条检查（命名？默认安全？有注释说明语义？），把不满足的项写成整改清单。

---

> 上一篇：[04-高可用架构模式](./04-高可用架构模式.md) ｜ 下一篇：[06-RCA根因分析与复盘](./06-RCA根因分析与复盘.md)
