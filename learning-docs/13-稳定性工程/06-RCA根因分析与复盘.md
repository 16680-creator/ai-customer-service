# RCA 根因分析与复盘

> 对应项目：目前**没有**复盘机制与 RCA 记录模板（⚠️ 工程未落地，本篇为方法论 + 目标态模板）；
> 案例素材全部锚定真实代码与配置：`ai-cs-chat/src/main/java/com/aics/chat/cache/VectorCacheStore.java`、
> `ai-cs-chat/src/main/resources/application.yml`（resilience4j）、`deploy/redis/redis.conf`（maxmemory 256mb）。
> 相关：[03-故障演练与混沌工程](./03-故障演练与混沌工程.md)（演练记录是 RCA 的上游输入）、[05-降级预案与开关治理](./05-降级预案与开关治理.md)（行动项常落在预案上）、[01-SLI-SLO与错误预算](./01-SLI-SLO与错误预算.md)（故障定级的客观标尺）。

---

## 一、RCA 是什么：从"谁的锅"到"流程哪漏了"

RCA（Root Cause Analysis）是故障结束后的结构化复盘，目标只有一个：**同类故障不再发生**。它追问的不是"谁犯的错"，而是"什么流程允许了这个错误发生且没被拦住"。

| 追责式复盘 | RCA 式复盘 |
|---|---|
| "小王把 Redis 配置改小了" | "配置变更没有评审与回滚记录，为什么能直接生效？" |
| 结论：批评小王 | 结论：变更流程加灰度与回滚演练 → 改进项 |
| 效果：当事人学会隐藏错误 | 效果：同类问题被流程拦住，全员受益 |

**为什么无指责（Blameless）有效**：人犯错是常态，系统应假设"人一定会犯错"并设计兜底（这正是高可用设计的思路——冗余防单点，流程防单人的失误）。追责文化下，故障信息会层层美化，时间线失真，复盘失去原料。

---

## 二、故障分级定级

复盘前先定级——级别决定响应规格与复盘深度。定级看两个维度：**影响面（多少用户/什么功能）× 紧急度（是否在扩大）**。

| 级别 | 定义 | ai-cs 对应场景示例 | 响应要求 | 复盘要求 |
|---|---|---|---|---|
| **P0** | 核心功能全不可用 | 网关 8080 全路由 5xx；MySQL 主库宕机写全失败 | 立即响应，全员升级 | 48h 内 RCA + 全员复盘会 |
| **P1** | 核心功能部分受损 | chat 对话可用性跌穿 99.9% SLO（月预算烧穿）；支付回调失败 | 15 分钟内响应 | 48h 内 RCA |
| **P2** | 非核心功能受损 | ES 搜索不可用；热榜/语义缓存失效 | 当天处理 | 周报级记录，酌情 RCA |
| **P3** | 体验劣化无功能损失 | 首 token p95 从 2s 涨到 4s；日志告警噪音 | 排期处理 | 问题清单跟踪 |

**定级技巧**：用 [01 篇](./01-SLI-SLO与错误预算.md) 的 SLO 做客观标尺——"错误预算烧穿速度"比"感觉严重不严重"更服众。定级可以随影响面变化**升级**（先 P2 后升 P1 很常见），但要记录原因。

---

## 三、时间线还原

时间线是 RCA 的骨架。用"关键事件 + 绝对时间 + 数据来源"三要素还原，**每个时间点必须可追溯**（日志时间戳、监控截图、工单记录），不允许"大概几点"。

| 时间点类型 | 要抓什么 | ai-cs 数据来源 |
|---|---|---|
| T0 故障发生 | 第一个错误日志/指标拐点 | Loki 日志、Prometheus 指标拐点 |
| T1 告警/发现 | 谁发现的？用户报障还是告警？（用户先发现 = 告警缺口） | 告警记录 / 群消息 |
| T2 开始响应 | 谁开始处理、按什么预案 | 值班记录（[08 篇](./08-oncall手册与故障响应.md) SOP） |
| T3 定位 | 定位到根因的耗时与路径 | 排查作业单的执行记录 |
| T4 止损/恢复 | 执行了什么动作、效果 | 操作记录 |
| T5 完全恢复 | 指标回到基线的时间 | 监控 |
| T6 复盘完成 | RCA 文档 + 行动项归档 | 本篇 |

**两个核心指标**从时间线直接读出：**MTTD**（T1−T0，多久发现的）与 **MTTR**（T5−T0 或 T4−T0，多久恢复的）。MTTD 长 → 补 [07 篇](./07-告警治理.md) 告警；MTTR 长 → 补预案与 runbook。

### 3.1 时间线三要素核对（防"失真时间线"）

| 要素 | 核对问题 | 失真后果 |
|---|---|---|
| 关键事件 | 每个事件是否有日志/操作记录对应？ | 时间线变口述回忆，两点之间全靠猜 |
| 绝对时间 | 各来源时间戳是否对齐时区与时钟漂移？ | 容器时区不一致（本项目 TZ=Asia/Shanghai 已设，`docker-compose-all.yml:17`）会导致跨源对不上 |
| 数据来源 | 每条是否标注了出处（日志/监控/群消息）？ | 无法复核，复盘会变成"记忆辩论赛" |

> 实操：SEV1/2 响应时就应有人（通常 IC）实时记时间线，不要等复盘时重建——事后重建的时间线平均丢失 30% 以上的关键动作。时间线归档时把原始日志片段以折叠/附录形式贴入正文，别只留结论。

---

## 四、根因分析两板斧：5Why 与鱼骨图

### 4.1 5Why：沿着因果链往下钻

规则：每个"为什么"的答案必须是**可验证的过程事实**，不是"因为有人失误"这种终点。钻到流程/系统层（能改的东西）为止。

```
故障：对话服务大量超时
Why1 为什么超时？ → Redis 连接池被占满，新请求拿不到连接
Why2 为什么池满？  → 大量连接等待 Redis 响应超过 socket 超时
Why3 为什么 Redis 慢？ → maxmemory 256mb 触发 allkeys-lru 驱逐 + AOF 重写 IO 飙高
Why4 为什么驱逐会伤到对话链路？ → 限流令牌桶 key 与大体积向量缓存混存同一淘汰池
Why5 为什么会混存？ → 缓存键设计评审时没有"按价值分层"的检查项，无 TTL 数据混入
→ 根因（可改的）：缓存键准入规范缺失 + 无 maxmemory 分层隔离
→ 行动项：向量缓存键加 TTL；限流 key 迁独立实例或 volatile 策略（都是可执行、可验证的）
```

**5Why 的三个失败模式**：① 钻到"人失误"就停（应继续问流程为何没拦住）；② 单线推理遗漏并行原因（配合鱼骨图）；③ 把症状当根因（"重启就好了"不是根因）。

### 4.2 鱼骨图：先把原因摆全，再挑主线

```
  人员            流程              技术
    \               \                 \
     \ 演练没做过     \ 无变更评审      \ 无容量告警
      \ 值班无手册     \ 键设计无规范    \ maxmemory 混存
       ──────────────────────────────────────────→ 故障：对话超时
      / 变更在高峰期   / 预案文档缺失    / 无驱逐监控
     / 无双人复核     / 无回滚步骤      / 连接池无熔断
    /               /                 /
  环境            管理              监控
```

用法：先发散（每一根刺至少 brainstorm 3 条），再用日志/监控**逐条排除**，留下可验证的主因线进 5Why。**鱼骨图防遗漏，5Why 防肤浅**。

---

## 五、行动项闭环与防复发

复盘没有行动项 = 团建。行动项管理四要素：

| 要素 | 要求 | 反例 |
|---|---|---|
| **可执行** | 动词开头 + 明确改动物 | "加强监控" ❌ → "给 redis_evicted_keys 配 P2 告警" ✅ |
| **有 owner** | 具体到人，不复盘会现场认领 | "大家看着办" |
| **有期限** | 与故障级别挂钩：P0 行动项 ≤ 2 周 | "下个季度吧" |
| **有验证** | 完成后用什么证据证明有效（最好复跑一次演练） | 自评"已完成" |

**防复发三问**（每个 RCA 收尾必答）：
1. 同类问题还有哪些地方存在？（举一反三：限流 key 混存 → 其他 Redis key 是否也混存？）
2. 为什么此前没发现？（监控缺口 / 演练缺口 / 流程缺口 → 分别补）
3. 如果重演一次，现在的防线能拦住吗？（用 [03 篇](./03-故障演练与混沌工程.md) 演练验证，而不是口头保证）

### 5.1 复盘会怎么开：45 分钟议程

| 时长 | 议程 | 产出 |
|---|---|---|
| 5 min | 时间线过一遍（会前已发文档，只对齐分歧点） | 确认版时间线 |
| 15 min | 根因讨论：鱼骨图发散 → 5Why 收敛 | 确认版根因 |
| 10 min | 检查"为什么此前没发现"（监控/演练/流程三层） | 缺口清单 |
| 10 min | 行动项认领（owner 现场指定，不散会后认领） | 行动项表 |
| 5 min | 防复发三问 + 历史行动项进度检查 | 收尾结论 |

**会议纪律**：只对事不对人（"我们"开头，不用"你/他"）；提出改进必须落到行动项，否则不入纪要；超时未收敛的根因转为"待验证实验"（用演练验证），不强行下结论。

> 学习团队没有真实故障也能开会：把 [03 篇](./03-故障演练与混沌工程.md) 演练暴露的预期外现象当"故障"开复盘会——演练记录就是现成的时间线与现象材料，流程一步不打折。

### 5.2 复盘反模式对照

| 反模式 | 表现 | 纠正 |
|---|---|---|
| 追责会 | "谁的代码 / 谁的配置" | 无指责原则，改问流程为何没拦 |
| 故事会 | 生动描述抢救过程，没有根因与行动项 | 时间线+根因+行动项三要素缺一不散会 |
| 甩锅给"偶发" | "网络抖动，无法避免" | 网络抖动是输入，系统应容忍它——继续问 5Why |
| 行动项蒸发 | 复盘会上慷慨激昂，两周后无人记得 | 四要素闭环 + 下次复盘会先查历史项 |
| 复盘完美化 | RCA 把过程写得比实际规范 | 用原始日志/截图佐证时间线，允许记录慌乱与误判 |

---

## 六、RCA 报告模板 + 教学示例

> **以下案例为教学示例，人物与事件均为虚构**，但所有技术细节（配置、日志格式、代码行为）均取自本项目真实代码，方便对照学习。模板可直接复制使用。

```markdown
# RCA 报告：2026-XX-XX 对话服务大面积超时（P1）

> 定级依据：chat 可用性 SLO 99.9%，本次月预算烧穿 58%；影响：对话链路首 token p95 2.1s→11s，
> 语义缓存命中率 62%→0%。MTTD 8min / MTTR 34min。

## 1. 影响面
- 时间：14:02 ~ 14:36（恢复基线 14:47）
- 范围：AI 对话主链路（/chat/stream/sse），知识库问答与普通对话均受影响
- 用户：全部在线用户（教学项目按并发会话 120 计）

## 2. 时间线（数据来源：docker logs / Prometheus / 群记录）
- 13:54 Redis AOF 重写启动（日志：aics-redis "Background AOF rewrite started"）
- 14:02 Prometheus 可见 chat p95 拐点（T0）；无告警触发 ⚠️
- 14:10 用户在群内报障（T1：用户先于告警发现 = 告警缺口）
- 14:12 值班同学介入，按 runbook 排查（T2）
- 14:18 定位：Redis maxmemory 256mb 触发 allkeys-lru 全力驱逐，
  向量缓存反复 miss → Embedding API 调用量 ×6 → chat 线程池排队（T3）
- 14:26 临时止损：调大 maxmemory 至 512mb + 对话接口临时限流（T4）
- 14:36 首 token p95 回到 2.4s；14:47 恢复基线（T5）

## 3. 根因（5Why 见正文 §4.1）
- 直接原因：Redis 内存触顶，驱逐导致向量缓存 L2 全量 miss，Embedding 外调放大 6 倍
- 根因：缓存键无准入规范——高价值限流 key 与大体积缓存 key 混存同一淘汰池，
  且无驱逐率监控（redis evicted_keys 无告警）

## 4. 为什么此前没发现
- 容量篇只估了"256mb 够不够装"，没估"驱动逐出的行为"；无演练覆盖该场景（B 类缺口）

## 5. 行动项
| # | 行动项 | Owner | 期限 | 验证方式 |
|---|---|---|---|---|
| 1 | 向量缓存 L2 写入增加 TTL（隔离驱逐池） | 张三 | 1 周 | redis-cli 抽查 TTL 生效 |
| 2 | evicted_keys / memory 水位告警接入 | 李四 | 2 周 |人为压测触发告警 |
| 3 | 演练清单新增"Redis 内存触顶"场景并执行 | 王五 | 2 周 | [03 篇]三段式记录 |
| 4 | 缓存键准入规范写入评审 checklist | 张三 | 1 周 | 评审模板更新 |

## 6. 防复发三问
- 举一反三：语义缓存索引 ZSET 同样无 TTL，一并整改（行动项 1 扩展）
- 未发现原因：监控缺 evicted_keys 指标告警（→ 行动项 2）
- 重演拦截：maxmemory 调整 + TTL 隔离后重跑演练确认（→ 行动项 3）
```

**模板用法**：第三节时间线每条必须带数据来源；行动项表四要素（可执行/owner/期限/验证）缺一不可；收尾必答防复发三问。

**模板使用注意事项**：

1. **先填时间线再写根因**——顺序反了，根因会被预先的结论"倒推"出来；
2. 影响面用数字（受影响接口数/会话数/预算烧穿比例），不用形容词（"影响很大"）；
3. 行动项里出现"加强""重视""提升意识"就是没写完，必须落到改文件/改配置/加代码；
4. 教学示例中的日志格式（如 "Background AOF rewrite started"）是 Redis 真实日志格式，动手时可用 `docker logs aics-redis 2>&1 | grep -i rewrite` 验证本机有没有类似前兆；
5. 定级（P1）与 SLO 数据（烧穿 58%）互相引用——定级有客观依据，这是 [01 篇](./01-SLI-SLO与错误预算.md) 与本篇的接口；
6. 文档命名与归档：`RCA-YYYYMMDD-<一句话现象>.md`，与本篇同目录或团队知识库统一存放——复盘文档查不到，等于没复盘。

---

## 七、面试高频问答

**Q1：说一次你处理过的线上故障。（高频）**
A：用本篇模板结构答：影响面（什么功能、多少用户）→ 时间线（发现→定位→恢复的关键节点与耗时）→ 根因（5Why 钻到流程层）→ 行动项（防复发的具体改动）。素材可用本篇教学示例的框架 + 自己真实做过的演练（如 Redis 降级演练），切忌只讲"重启就好了"。

**Q2：为什么复盘要"无指责"？**
A：目的是让同类故障不再发生，而人是会犯错的常态变量，系统与流程才是可改的变量。追责会让故障信息层层美化、时间线失真、当事人隐藏问题，复盘失去原料。无指责不等于无改进——改进对象是流程、监控、预案。

**Q3：5Why 什么时候会失效？**
A：三种情况：钻到"人失误"就停（应继续问流程为何没拦）；单线推理漏掉并行原因（先用鱼骨图发散）；把对策当根因（"重启恢复"是处置不是原因）。正确终点是"可改变的流程/系统因素"。

**Q4：MTTD 和 MTTR 分别反映什么短板？**
A：MTTD（发现耗时）长说明监控告警有缺口——指标没采、阈值不对、告警没人值；MTTR（恢复耗时）长说明预案/runbook 缺失或没演练过。两条都要用时间线数据说话，优化方向完全不同。

**Q5：故障定级为什么要有客观标尺？**
A：避免"谁嗓门大定谁的级"。用 SLO 错误预算烧穿速度做标尺（月预算烧穿 >50% 至少 P1），响应规格与复盘深度随之确定，也避免小事惊动全员、大事被轻描淡写。

**Q6：行动项怎么保证不烂尾？**
A：四要素：可执行（动词+改动物）、有 owner、有期限（与级别挂钩，P0 ≤2 周）、有验证（完成要有证据，最好复跑演练）。再加一个机制：每次新 RCA 先过一遍历史行动项完成情况，烂尾会当场现形。

**Q7：复盘的"防复发三问"是什么？**
A：同类问题还有哪些地方存在（举一反三）、为什么此前没发现（监控/演练/流程缺口）、重演一次现有防线能否拦住（用演练验证）。三问把单次故障转成系统性改进，这是 RCA 区别于"写事故报告"的核心。

**Q8：复盘会和 RCA 文档哪个更重要？**
A：RCA 文档是载体，复盘会是质量把关。文档先行（会前写好时间线与根因草案，与会者带证据来挑错），会议控制在 45 分钟内对齐定级、根因与行动项；没有文档的复盘会容易开成故事会，没有复盘会的文档容易漏掉关键视角。

**Q9：多个原因共同导致故障时，根因怎么定？**
A：区分**诱因（trigger）**与**根因（root cause）**：诱因是扣扳机的（那次发版、那条慢 SQL），根因是"系统为什么这么脆弱"（没有评审、没有超时、没有告警）。一条 RCA 可以有多个根因（各配行动项），诱因只有一个——把诱因当根因，就会得出"下次不发版就好了"的荒唐结论。

**Q10：怎么证明复盘起了作用？**
A：看两个滞后指标：① 同类故障复发间隔是否拉长（最好按根因类别统计）；② 历史行动项完成率与验证率。前者是结果，后者是过程——行动项完成率高但故障没减少，说明根因找浅了，要回看 5Why 是否钻到流程层。

---

## 八、动手练习

1. 把第六节教学示例的 RCA 报告补全时间线细节：为每个时间点写出你会在本地用什么命令取证（如 `docker logs --since "14:00" aics-redis`）。
2. 对教学示例做举一反三：检查 `ai-cs-chat/.../cache/` 下三个缓存类（VectorCacheStore/SemanticCacheService/HotQaCacheService）的 Redis key 是否都有 TTL 或容量上限，仿照"行动项 1"各写一条整改项。
3. 自己跑一次 [03 篇](./03-故障演练与混沌工程.md) 演练 1，基于真实演练记录写一份迷你 RCA（时间线 + 5Why 两层即可），体会"演练是 RCA 的安全沙盒"。
4. 为"Redis 主从切换时网关限流失效"设计 5Why 链（起点：429 拦截失效，大量请求穿透到下游），终点必须落在可改的流程或配置上。
5. 给本模块所有文档中的行动项建一张跟踪表（编号/来源文档/owner/期限/验证方式），体会"行动项闭环"作为跨文档机制的运作方式。

---

> 上一篇：[05-降级预案与开关治理](./05-降级预案与开关治理.md) ｜ 下一篇：[07-告警治理](./07-告警治理.md)
