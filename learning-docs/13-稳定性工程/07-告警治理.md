# 告警治理

> 对应项目：`deploy/docker-compose/docker-compose-observability.yml`（可观测栈现状）、
> `deploy/observability/prometheus.yml`（采集配置，本篇告警规则的数据底座）、
> `deploy/docker-compose/docker-compose-all.yml`（被监控的 11 个服务端口 8080~8090）。
> 相关：[01-SLI-SLO与错误预算](./01-SLI-SLO与错误预算.md)（burn-rate 告警的 SLO 底座）、[08-oncall手册与故障响应](./08-oncall手册与故障响应.md)（告警打到谁、响了怎么办）、[06-RCA根因分析与复盘](./06-RCA根因分析与复盘.md)（"告警没人响"的复盘案例）。
>
> **分工声明**：指标**怎么采集**、Tempo 链路、Grafana 看板见 [07-运维部署/04-Prometheus可观测性](../07-运维部署/04-Prometheus可观测性.md)；日志**怎么聚合**见 [07-运维部署/07-Loki日志聚合](../07-运维部署/07-Loki日志聚合.md)。本篇只讲**告警规则设计与治理**——什么时候叫人、叫谁、怎么不被吵死。

---

## 一、现状：告警链路缺失（如实盘点）

> **⚠️ 工程未落地，本节现状部分是证据记录，接入方案是目标态。**
> 现状证据：
> ① `deploy/docker-compose/docker-compose-observability.yml:19-68` 仅部署 prometheus / grafana / tempo / otel-collector 四个服务，**无 alertmanager**；
> ② `deploy/observability/prometheus.yml` 全文只有 `global` + `scrape_configs` 两段，**没有 `rule_files` 与 `alerting` 段**——即连告警规则文件都没有挂载；
> ③ 全仓 grep `alertmanager` 零命中。
> 要补什么：Alertmanager 容器 + Prometheus 告警规则文件 + 通知渠道（本节给出最小可用接入）。

```
现状：   Micrometer 指标 → Prometheus（存与查）→ ⛔ 到此为止，无人叫人
目标态： Micrometer 指标 → Prometheus → 告警规则评估 → Alertmanager → 分级通知 → oncall
```

好在**底座已备**：`prometheus.yml` 已按 15s 间隔抓取 11 个服务的 `/actuator/prometheus`（job `ai-cs-services`，target 8080~8090），SLI 数据现成——告警缺的只是"规则 + 路由"最后一公里。

---

## 二、什么是好告警：三条标准

| 标准 | 含义 | 反例 |
|---|---|---|
| **可行动（Actionable）** | 收到告警的人有明确动作可做 | "CPU 80%"（没有动作）vs "连接池使用率 >90% 持续 10min，扩容或查慢 SQL"（有动作） |
| **有症状导向** | 告警用户能感知的问题，而非所有原因 | 对"可用性跌穿 SLO"告警（症状），而不是给每个进程重启单独告警（原因） |
| **可静音** | 能按 label 精确静音正在处理的告警 | 告警无实例维度，静音只能整条关掉 |

**核心原则**：每条告警都应该能回答——"我现在该干什么？"。答不上来的告警进入治理名单（见 §5）。

---

## 三、告警分级：级别决定通知方式

| 级别 | 触发条件（示例） | 通知方式 | 响应要求 | ai-cs 示例 |
|---|---|---|---|---|
| **P0 Page** | 核心链路跌穿 SLO / 服务全不可用 | 电话 + 群 @，7×24 | 立即 | `up{job="ai-cs-services"} == 0`；对话可用性烧穿月预算 |
| **P1 Page** | 核心链路快速劣化 | 群 @ + 推送 | 15 分钟响应 | chat 首 token p95 > 5s 持续 5min；MQ 积压持续增长 |
| **P2 Ticket** | 非核心劣化 / 慢速烧预算 | 工单 + 日报 | 工作时间处理 | ES 集群 yellow；Redis evicted_keys > 0 |
| **P3 Info** | 需要人知道但无需动作 | 周报汇总 | 无 | 证书 30 天内到期；磁盘 70% |

**对应关系**：告警分级要和 [06 篇](./06-RCA根因分析与复盘.md) 的故障定级、[05 篇](./05-降级预案与开关治理.md) 的预案分级对齐——P0 告警响 = P0/P1 故障预案待执行，三张表一张逻辑。

---

## 四、告警疲劳：三大来源与治理

告警疲劳 = 告警多到被人无视，**比没有告警更危险**（狼来了效应）。三大来源与对策：

| 来源 | 典型表现 | 对策 |
|---|---|---|
| **阈值拍脑袋** | 静态阈值过高天天响 / 过低从不响 | 改用 SLO/burn-rate（§6）或历史基线（p95 同比） |
| **风暴不聚合** | 宿主机挂 → 11 个服务的 up 告警齐响 | Alertmanager group_by 聚合（§5.1） |
| **重复触发** | 同一问题每 15s 评估一次就通知一次 | repeat_interval 拉长 + 抑制规则（§5.2） |

**治理动作（季度执行）**：统计各告警 30 天触发次数与"触发后是否有动作"——零动作告警直接删或降级为 P3；这是把 [06 篇](./06-RCA根因分析与复盘.md) 的"行动项闭环"思想用到告警上。

### 4.1 告警规则反模式对照

| 反模式 | 后果 | 改法 |
|---|---|---|
| `up == 0` 之外还给每个进程崩溃配独立告警 | 一个故障响 N 条 | 抑制规则：实例级告警吞掉细粒度 |
| 用 `for: 0s`（瞬时触发） | 毛刺也响 | 至少 `for: 2m` 或双窗确认 |
| 对"重启次数""GC 次数"这类中间原因告警 | 响了也没动作 | 只对症状（SLO/用户可感知）告警，原因进看板 |
| 阈值照抄别人家的 | 与本系统容量不匹配 | 从 SLI 实测分布出发定（[01 篇](./01-SLI-SLO与错误预算.md)） |
| 告警文案不带上下文 | 收到还要查半天 | annotations 里带服务名/端口/首查命令 |

---

## 五、抑制与聚合：Alertmanager 两个核心机制

### 5.1 聚合（Grouping）

```yaml
route:
  group_by: ['alertname', 'job']     # 同名告警按服务聚成一条通知
  group_wait: 30s                     # 等 30s 凑一批再发（防瞬时抖动刷屏）
  group_interval: 5m                  # 同组新告警 5m 才再发一条
  repeat_interval: 4h                 # 未恢复的告警 4h 重复提醒一次
```

效果：宿主机宕机导致 11 个 `up==0`，只收到 **1 条**"ai-cs-services 11/11 down"而不是 11 条轰炸。

### 5.2 抑制（Inhibition）

```yaml
inhibit_rules:
  # 实例挂了 → 该实例上的一切细粒度告警都是它的结果，抑制掉只留根因
  - source_matchers: [ 'alertname = InstanceDown' ]
    target_matchers: [ 'severity = page' ]
    equal: ['instance']
  # 网关整体不可用 → 抑制其下游（chat/search/...）的时延类告警
  - source_matchers: [ 'alertname = GatewayDown' ]
    target_matchers: [ 'alertname =~ "HighLatency|HighErrorRate"' ]
```

语义：**告"因"就不告"果"**——根因告警激活期间，它导致的结果告警被静音。这是"第一眼看到的一定是最值得查的"的机制保障。

### 5.3 静默（Silence）：维护窗口的正确姿势

计划内变更（发版、演练、扩容）会产生**预期内的告警**，正确做法不是"忍着"也不是"关掉告警"，而是预先建 Silence：

```bash
# 例：演练期间静音 chat 实例的 page 级告警 2 小时（9093 API）
curl -X POST localhost:9093/api/v2/silences -d '{
  "matchers": [{"name":"instance","value":"host.docker.internal:8083","isRegex":false},
               {"name":"severity","value":"page","isRegex":false}],
  "startsAt": "2026-XX-XXT14:00:00+08:00",
  "endsAt":   "2026-XX-XXT16:00:00+08:00",
  "createdBy": "oncall-drill",
  "comment": "03 篇演练窗口"
}'
```

**纪律**：Silence 必须带时间窗与理由（`comment`），到期自动失效；运维黄金法则——**正在静默期间发生的真故障会被掩盖**，所以静默范围（实例+级别）要收到最小。

---

## 六、burn-rate 告警：接第 1 篇的 SLO

静态阈值（"错误率 >1% 就响"）的问题：不知道**多快地错**该多急地响。burn-rate 把 [01 篇](./01-SLI-SLO与错误预算.md) 的错误预算变成告警燃料：

```
burn rate = 实际错误率 / 允许错误率（= 1 − SLO）
```

### 6.1 多窗口组合（Google SRE 实践）

| 窗口 | burn rate | 级别 | 语义 |
|---|---|---|---|
| 5m + 1h 双窗 | 14.4 | P0 Page | 2 天就烧完全月预算 → 立刻叫人 |
| 30m + 6h 双窗 | 6 | P1 Page | 约烧 5 天预算/当前速率 → 尽快处理 |
| 2h + 1d 双窗 | 3 | P2 Ticket | 慢速烧 → 工作时间处理 |

**双窗的意义**：长窗确认"真的在烧"（防瞬时抖动误报），短窗确认"现在还在烧"（防历史积累的假警报）。两个条件同时满足才触发，误报率大幅下降。

### 6.2 以 chat 服务 99.9% 可用性 SLO 为例的规则（目标态）

```yaml
# deploy/observability/alerts.yml —— 挂到 prometheus.yml 的 rule_files（见 §7）
groups:
  - name: slo-burn-rate
    rules:
      # 允许错误率 = 1 - 99.9% = 0.001；快窗 burn 14.4 → 短窗错误率阈值 0.0144
      - alert: ChatSloFastBurn
        expr: |
          (
            sum(rate(http_server_requests_seconds_count{job="ai-cs-services",
                  instance="host.docker.internal:8083", status=~"5.."}[5m]))
            /
            sum(rate(http_server_requests_seconds_count{job="ai-cs-services",
                  instance="host.docker.internal:8083"}[5m]))
          ) > (14.4 * 0.001)
          and
          (
            sum(rate(http_server_requests_seconds_count{job="ai-cs-services",
                  instance="host.docker.internal:8083", status=~"5.."}[1h]))
            /
            sum(rate(http_server_requests_seconds_count{job="ai-cs-services",
                  instance="host.docker.internal:8083"}[1h]))
          ) > (14.4 * 0.001)
        labels: { severity: page }
        annotations:
          summary: "chat 服务错误预算快速烧穿（burn>14.4）"
# 指标名为 Micrometer 标准命名，上线前以实际 /actuator/prometheus 输出校准
```

---

## 七、从 0 接入 Alertmanager（目标态最小方案）

三步走，全部是**增量修改**（新增文件 + 两个 compose/配置段），不动现有采集：

```yaml
# ① deploy/docker-compose/docker-compose-observability.yml 追加服务：
  alertmanager:
    image: prom/alertmanager:v0.27.0
    container_name: aics-alertmanager
    ports:
      - "9093:9093"
    volumes:
      - ../observability/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
    networks: [aics-observe]

# ② deploy/observability/prometheus.yml 追加两段：
global:            # 已有段内追加
  # evaluation_interval: 15s   （已有）
rule_files:
  - /etc/prometheus/alerts.yml
alerting:
  alertmanagers:
    - static_configs:
        - targets: ['alertmanager:9093']
# prometheus 服务 volumes 追加： - ../observability/alerts.yml:/etc/prometheus/alerts.yml:ro

# ③ deploy/observability/alertmanager.yml 最小路由：
route:
  receiver: 'default'
  group_by: ['alertname', 'job']
  group_wait: 30s
  repeat_interval: 4h
receivers:
  - name: 'default'
    webhook_configs:
      - url: 'http://your-webhook/notify'   # 学习环境可先指向日志打印或 IM 群机器人
```

验收：`curl localhost:9093/-/healthy` 返回 OK；在 Prometheus UI（9090）`/alerts` 页能看到规则评估状态；手工构造一条告警（如 `up == 0`）验证通知到达。

---

## 八、面试高频问答

**Q1：什么样的告警是好告警？**
A：可行动（收到就知道该干什么）、症状导向（告用户能感知的问题而非每个可能原因）、可静音（能按 label 精确静音正在处理的）。判断法：把告警内容念一遍，如果不知道收到后做什么，它就不该存在。

**Q2：告警疲劳是怎么形成的，怎么治理？**
A：三大来源：静态阈值拍脑袋（改 SLO/burn-rate 或基线告警）、风暴不聚合（group_by 聚合）、重复触发（repeat_interval + 抑制）。治理靠定期审计：30 天触发零动作的告警删除或降级。

**Q3：burn-rate 告警和静态阈值告警的区别？**
A：静态阈值不知道"多快地错"该"多急地响"；burn-rate 把错误率换算成预算烧穿速度，多窗口组合分级——5m+1h 双窗 burn>14.4 电话叫人（两天烧光月预算），2h+1d burn>3 走工单。且双窗同时满足才触发，天然防抖动误报。

**Q4：Alertmanager 的抑制和聚合分别解决什么问题？**
A：聚合解决"一个事件产生 N 条通知"（同组告警合并成一条）；抑制解决"结果告警淹没根因告警"（实例挂时静音它上面的细粒度告警，网关挂时静音下游时延告警）。两者共同保证第一眼看到的是最值得处理的告警。

**Q5：为什么说"告警太多"比"没有告警"更危险？**
A：没有告警的故障模式是明确的（漏报）；告警疲劳的故障模式是隐蔽的（真告警淹没在噪音里，值班形成"点了就关"的肌肉记忆）——关键时刻唯一的真告警也被当噪音处理。历史上多起重大事故的直接诱因都是告警被忽略。

**Q6：P0 告警为什么不能发太多？**
A：P0 的语义是"立即响应"，背后是电话/深夜打扰的成本。P0 太多必然导致脱敏。原则：P0 只给"核心 SLO 跌穿 + 快速烧穿"这种需要人马上动手的；其余一律降级为 Ticket/Info，用工作时间的节奏处理。

**Q7：没有 Alertmanager，Prometheus 能告警吗？**
A：能评估规则并显示 firing 状态（Prometheus 自带规则引擎，UI 的 /alerts 页可见），但没有通知路由、聚合、抑制、静音、重试这些"治理"能力。本项目现状即如此——规则与 Alertmanager 都未部署，采集数据已有。最小接入只需新增一个容器 + 两个配置段。

**Q8：你们项目第一优先级该配哪些告警？**
A：按"核心链路 + 现成指标"排序：① 11 个服务 `up == 0`（最便宜的保底）；② chat 服务 5xx 率双窗 burn-rate（对接 99.9% SLO）；③ Redis evicted_keys > 0 与内存水位（容量篇分析过的静默驱逐风险）；④ MQ 积压持续增长。先跑起来再治理，不要等"告警体系想完美了"再上。

---

## 九、动手练习

1. 完成 §7 的三步接入（本地 compose 环境），用 `curl localhost:9093/-/healthy` 与 Prometheus `/alerts` 页验证链路通。
2. 写一条 P1 告警规则：chat 实例 8083 的 `http_server_requests_seconds` p95 持续 5 分钟 > 3s（提示：`histogram_quantile(0.95, sum by (le) (rate(...[5m])))`），用 `ab`/k6 制造慢请求触发它。
3. 为 §7 接入的 Alertmanager 补一组抑制规则：`InstanceDown` 触发时静音该实例的所有 `severity=page` 告警，并手工验证抑制生效（9093 UI 的 Silences 页）。
4. 把 §6.2 的 burn-rate 规则改成 SSE 首 token SLO 版本：SLI 用 `sseChatService` 熔断打开事件近似（`resilience4j_circuitbreaker_state{name="sseChatService", state="open"}`，指标名以实际输出为准），讨论这种近似法的误差来源。
5. 做一次"告警审计"演练：假设你收到 §6 的三条告警，按 [08 篇](./08-oncall手册与故障响应.md) 的排查作业单走一遍，记录每条告警"收到后第一分钟该做什么"——写不出来的一条，就是不合格告警。

---

> 上一篇：[06-RCA根因分析与复盘](./06-RCA根因分析与复盘.md) ｜ 下一篇：[08-oncall手册与故障响应](./08-oncall手册与故障响应.md)
