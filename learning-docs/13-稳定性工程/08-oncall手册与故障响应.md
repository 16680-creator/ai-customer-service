# oncall 手册与故障响应

> 对应项目：目前**没有**值班机制与 runbook（⚠️ 工程未落地，本篇为方法论 + 可直接启用的模板）；
> 作业单绑定的端口与命令全部来自真实部署：`deploy/docker-compose/docker-compose-all.yml`（服务 8080~8086 与中间件端口）、
> 各服务 `ai-cs-*/src/main/resources/application.yml`（order 8087 / product 8088 / pay 8089 / mq 8090）、
> `ai-cs-py-chat/app/main.py:3`（Python 服务 8000）、`deploy/docker-compose/docker-compose-observability.yml`（观测栈端口）。
> 相关：[07-告警治理](./07-告警治理.md)（告警是值班系统的主要输入）、[06-RCA根因分析与复盘](./06-RCA根因分析与复盘.md)（事后跟进的终点）、[05-降级预案与开关治理](./05-降级预案与开关治理.md)（响应时的预案执行）。

---

## 一、oncall 是什么：从"救火"到"值班工程学"

> **⚠️ 工程未落地，本节为目标态。**
> 现状证据：全仓无值班轮转表、无 runbook 目录、无故障响应 SOP 文档；`learning-docs/00-学习路线总览/05-技术缺口分析与补全计划.md` 的 B 类缺口中与之相邻的"故障演练"亦未执行。
> 要补什么：本篇给出可直接启用的三件套——值班轮转表（§2）、SEV 响应分级（§3）、ai-cs 专属 runbook（§5~§6）。学习项目也可以"虚拟值班"：每周一人按作业单巡检一次，就是 oncall 的最小实践。

oncall（值班）不是"半夜被叫起来重启服务"，而是一套让故障响应**不依赖英雄**的机制：轮转保证总有人、分级保证响对节奏、runbook 保证新手也能按单操作、事后跟进保证同样的火不烧第二次。

---

## 二、值班轮转

| 机制 | 做法 | 说明 |
|---|---|---|
| **轮转周期** | 周制（1 周一换）最常见；团队 ≥4 人才可持续 | 值班周不排新功能开发 |
| **主备双岗** | 主 oncall 接告警，备 oncall 主无响应 15 分钟自动升级接手 | 防单人生病/失联 |
| **交接** | 值班周结束写交接单：本周告警趋势、未关闭问题、待观察项 | 交接单是下任的起跑线 |
| **补偿** | 值班时长计入工作量；深夜 Page 次日调休 | 让值班可持续，防止疲劳值班引发新事故 |
| **工单清单** | 本周值班产生的问题清单周四前清完 | 防止"值完了事没办" |

**本项目的最小实践**（学习团队适用）：双人一组周轮转；每周固定时间按 §6 作业单做一次**主动巡检**（不是等告警）；巡检结果写进共享文档当作交接单。

### 2.1 交接单模板

```markdown
# 值班交接单（YYYY-MM-DD 当周）
- 值班人：A → 接手人：B
- 本周告警趋势：共 N 条，P0 x 条 / P1 x 条；主要来源：<组件>
- 未关闭问题：
  | # | 问题 | 级别 | Owner | 进展 |
  |---|---|---|---|---|
  | 1 | Redis evicted_keys 偶发 | SEV3 | A | 待评估拆实例（→ 02 篇 §4.2） |
- 待观察项：chat 容器上轮重启过，观察 OOM 与连接池水位
- 巡检结果：作业单 §6 全绿 / B6 复制延迟 2s（见附截图）
- 本周演练：无 / 演练 1（记录链接）
```

---

## 三、故障响应分级与升级路径

### 3.1 SEV 分级（与 06 篇故障定级、07 篇告警分级对齐）

| 级别 | 判定 | 响应动作 | 通知范围 |
|---|---|---|---|
| **SEV1** | 核心链路全不可用（网关全路由 5xx、MySQL 主库宕） | 立即拉应急群，主 oncall + 对应服务 owner + 技术负责人 | 全团队 + 负责人 |
| **SEV2** | 核心链路部分受损/快速劣化 | 主 oncall 处理，30 分钟无进展升级 | 团队群 |
| **SEV3** | 非核心受损（搜索不可用、缓存降级已生效） | 工单跟踪，工作时间处理 | 团队群（静默） |
| **SEV4** | 无用户影响（巡检发现的风险、日志异常） | 问题清单排期 | 周报 |

### 3.2 升级路径（防"一个人卡死在故障里"）

```
告警触发 → 主 oncall（0~15min 无进展）
              ↓ 升级
           备 oncall 接手（15~30min 无进展）
              ↓ 升级
           服务 owner + 技术负责人进入应急群（SEV1 起点即此层）
              ↓
           决策层：对外沟通 / 资源协调（扩容、回滚、预案 P0 级动作）
```

**升级不是问责**，是把更多上下文和决策权拉进来。SEV1 的应急群规则：只发时间线与操作记录，不讨论责任（呼应 [06 篇](./06-RCA根因分析与复盘.md) 无指责原则）。

### 3.3 应急群沟通规范（SEV1/2 生效）

| 规则 | 做法 | 目的 |
|---|---|---|
| 指定事件指挥（IC） | 主 oncall 默认担任，负责派活与对外同步 | 多人同时动手 = 动作互相覆盖 |
| 只发事实与操作 | "14:03 执行 docker restart aics-chat-service" | 复盘要用原始记录 |
| 关键操作先报备 | 动写操作/预案开关前在群里说一声 | 防止两人同时切主、同时改配置 |
| 定期同步 | 每 15 分钟一条状态（影响面/当前动作/下一步） | 管理层不用逐条刷屏追问 |
| 禁止讨论责任 | 追责留到 RCA，群里只解决"现在怎么办" | 保护当事人如实汇报 |

---

## 四、故障响应 SOP：确认 → 止损 → 定位 → 恢复 → 跟进

| 步骤 | 要点 | 常见错误 |
|---|---|---|
| **① 确认** | 影响面多大？在扩大吗？定 SEV 级 | 没定级就开干，投入与影响不匹配 |
| **② 止损** | **先恢复再定位**：切预案（[05 篇](./05-降级预案与开关治理.md)）、回滚、限流 | 执着于找根因，放任故障扩大 |
| **③ 定位** | 按 runbook 逐层排查（§6 作业单），记录每步结果 | 凭感觉乱试，动作不可复现 |
| **④ 恢复** | 验证指标回基线，观察 30 分钟再降级响应 | 指标刚好转就宣布恢复，二次故障打脸 |
| **⑤ 跟进** | 时间线归档 → RCA（SEV1/2）→ 行动项闭环 | "好了就翻篇"，同类故障复发 |

**止损优先于定位**是新手最容易违反的原则——用户在乎"什么时候能用"，不在乎"根因是什么"。定位可以在止损后的稳定状态下慢慢做。

---

## 五、Runbook 模板

> 每个核心服务一份，放在服务仓库 `docs/runbook.md`（⚠️ 本项目尚无此目录，按此模板创建即补上缺口）。

```markdown
# Runbook：<服务名>

## 基本信息
- 职责：一句话（如：AI 对话编排、SSE 流式输出、模型路由）
- 端口：8083 ｜ 注册中心：Nacos（aics 命名空间）｜ 日志：容器 stdout
- 关键依赖：Redis（缓存/限流）、LLM API（核心下游）、MySQL（会话）、Nacos
- 核心链路：POST /chat/stream/sse（SSE 对话）
- SLO：可用性 99.9%/月；首 token p95 < 2s（见 13-稳定性工程/01）

## 常用命令
- 健康检查：curl -s localhost:8083/actuator/health
- 指标：curl -s localhost:8083/actuator/prometheus | head -30
- 日志：docker logs --tail 100 -f aics-chat-service
- 重启：docker restart aics-chat-service（graceful，见 application.yml:2,7）

## 常见故障与预案
| 现象 | 初判 | 动作 | 预案文档 |
|---|---|---|---|
| 首 token 全部超时 | LLM API 故障 | 确认熔断状态；等待路由切换备选模型 | Resilience4jConfig 降级文案 |
| 检索变慢、日志见"降级 L1-only" | Redis 故障 | 验证对话仍可用；评估网关限流兜底开关 | VectorCacheStore / 网关 rate-limit.enabled |
| 大量 429 | 流量超限 | 区分攻击/正常峰值；评估调 replenish-rate | 网关 application.yml:48 |

## 升级
- 30 分钟无进展 → 备 oncall；仍无进展 → 服务 owner + 技术负责人
```

---

## 六、ai-cs 实操作业单：服务不可用排查顺序

> 场景：用户反馈"客服机器人没反应"。按顺序执行，**每步记录结论再走下一步**（这就是 runbook 的核心价值——把老手的排查顺序交给新手）。中间件端口均来自 `docker-compose-all.yml` / `docker-compose-master-slave.yml` 实测。

### 第 0 步：报障复现（1 分钟）

```bash
curl -s -o /dev/null -w "gateway:%{http_code}\n"  localhost:8080/actuator/health
curl -s -o /dev/null -w "py:%{http_code}\n"       localhost:8000/docs
```

网关 8080 不通 → 问题在入口层，走 A 线；网关通但对话失败 → 走 B 线。

### A 线：网关/入口层

| 步 | 命令 | 判定 |
|---|---|---|
| A1 | `docker ps --format '{{.Names}}\t{{.Status}}'` | 有容器非 Up → 直接看该容器 `docker logs --tail 50` |
| A2 | `curl -s localhost:8080/actuator/health` | 网关自身 down → 重启网关，查 Nacos 连接（A3） |
| A3 | `curl -s localhost:8848/nacos/v1/console/health/readiness` | Nacos 失联（8848/9848）→ 按 [03 篇演练 5] 评估服务发现窗口，恢复 Nacos |
| A4 | 限流误伤？`curl -s -o /dev/null -w "%{http_code}" localhost:8080/api/product/page?current=1&size=10` | 批量 429 → replenish-rate=5/burst=10 语义下属预期，评估调参或开关兜底 |

### B 线：链路层（自上而下）

| 步 | 对象 | 命令 | 判定与动作 |
|---|---|---|---|
| B1 | py-chat (8000) | `curl -s localhost:8000/docs -o /dev/null -w "%{http_code}"` | 不通 → `uvicorn` 进程问题，看 `ai-cs-py-chat` 日志 |
| B2 | chat (8083) | `curl -s localhost:8083/actuator/health` | down → `docker logs aics-chat-service --tail 100`；重启（graceful 停机 30s） |
| B3 | SSE 链路 | `curl -s -N -X POST "localhost:8083/chat/stream/sse?sessionId=oncall&message=hi"` | 有流 → chat 正常，问题在更上游；无流 → B4 |
| B4 | LLM 下游 | chat 日志 grep 熔断：`docker logs aics-chat-service 2>&1 \| grep -i circuit` | OPEN → 模型 API 故障，确认模型路由降级是否生效（ModelHealthRegistry） |
| B5 | Redis (6379) | `redis-cli ping` | 超时/无响应 → 向量缓存自动降级 L1-only（属预期）；确认网关限流兜底预案 |
| B6 | MySQL (3306) | `mysqladmin -h127.0.0.1 -P3306 -uroot -proot ping` | 主库不通 → 写链路全断；从库 3307/3308 只读不可写（slave.cnf:11），走 [04 篇 §3.2] 切换路径 |
| B7 | ES (9200) | `curl -s localhost:9200/_cluster/health?pretty` | red/yellow → 检索降级路径；评估 P2/P3 级别 |
| B8 | MQ (9876/10911) | `docker exec aics-rocketmq-broker sh -c "echo > /dev/tcp/localhost/10911 && echo ok"` | broker 不通 → 异步链路受损，对话主链路应不受影响，确认影响面再定级 |

### 观测兜底（任何一步拿不准时）

```bash
curl -s localhost:9090/-/healthy                                   # Prometheus 自身（9090）
curl -s 'localhost:9090/api/v1/targets' | grep -o '"health":"[a-z]*"' | sort | uniq -c
# 一条命令看 11 个服务 target 的 up/down 分布；Grafana（3000）看趋势
```

### 作业单收尾

- 恢复判定：B3 的 SSE 流恢复 + Prometheus 中 chat target up + 首 token p95 回基线，**观察 30 分钟**；
- 记录：时间线按 [06 篇](./06-RCA根因分析与复盘.md) 模板归档；SEV1/2 → 48h 内 RCA；
- 改进：本次排查中"写不出来"的步骤，回填进本作业单——**作业单是活的文档**。

---

## 七、事后跟进

故障关闭 ≠ 事件结束。但先谈一件事：**主动巡检能减少故障本身**——巡检发现的是 SEV3/SEV4，等告警发现的就是 SEV1/SEV2。

### 7.1 周巡检清单（值班周必做，主动发现不等告警）

| # | 巡检项 | 命令/位置 | 异常标准 |
|---|---|---|---|
| 1 | 11 个服务健康 | `curl -s localhost:9090/api/v1/targets` 或逐个 `/actuator/health` | 有 down / 非绿 |
| 2 | Redis 驱逐与内存 | `redis-cli info memory \| grep -E "used\|maxmemory"`；`info stats \| grep evicted` | evicted_keys > 0；内存 > 80% |
| 3 | MySQL 复制 | [04 篇](./04-高可用架构模式.md) §3.3 观测命令 | Seconds_Behind_Master > 5 或线程 No |
| 4 | MQ 积压 | RocketMQ 控制台 / `mqadmin consumerProgress` | 积压持续增长 |
| 5 | 磁盘水位 | `df -h`（ES/MQ 数据卷） | > 70% |
| 6 | 慢 SQL | MySQL slow log（master.cnf:29-31 已开启，阈值 2s） | 有新条目即记问题单 |

### 7.2 事后跟进清单

| 项 | 时限 | 负责 |
|---|---|---|
| 时间线归档（含操作记录与截图） | 当天 | 主 oncall |
| RCA 完成并评审（SEV1/2） | 48 小时 | oncall + 服务 owner |
| 行动项建立跟踪（owner/期限/验证） | RCA 评审会上 | 全员 |
| 用户/协作方通报（如有外部影响） | 恢复后 1 小时内 | 技术负责人 |
| 防复发验证（复跑演练/回归测试） | 行动项期限内 | 行动项 owner |
| runbook/作业单回填本次经验 | 1 周内 | 主 oncall |

最后一项最常被忽略也最有价值：**本次排查卡壳的地方，就是 runbook 要补的地方**——值班体系的成熟度就是这样一轮轮故障攒出来的。

---

## 八、面试高频问答

**Q1：你们怎么做值班（oncall）？没值过班怎么答？**
A：机制答法：周轮转 + 主备双岗 + 升级路径 + 交接单；再答值班的核心产出不是"修故障"而是"沉淀 runbook 与行动项"。可以补充本项目实践：按 runbook 作业单做每周主动巡检，用 [03 篇](./03-故障演练与混沌工程.md) 演练替代真实故障来训练响应能力。

**Q2：故障响应的原则是什么？**
A：先止损再定位（用户在乎恢复不在乎根因）；先定级再投入（响应规格与影响面匹配）；全程记录时间线（复盘的原料）。新手最常犯的是跳过止损执着找根因。

**Q3：升级路径为什么重要？**
A：防单人卡死。15/30 分钟两道升级线，把更多上下文与决策权拉进应急群；SEV1 直接从 owner 层起步。升级是机制不是问责——说了什么、做了什么都记录在时间线里，复盘时用。

**Q4：runbook 里最重要的部分是什么？**
A：可执行的排查顺序与每步的判定动作（做什么、看什么、什么结果走哪条线）。有命令无判定的是命令备忘录，有判定无命令的是空话；两者成对才是 runbook。且必须是活文档——每次故障后回填卡壳点。

**Q5：值班交接要交接什么？**
A：告警趋势（变多变少、什么在响）、未关闭问题清单（owner 与进展）、待观察项（刚恢复的服务、刚上线的变更）、工具变更（新的告警/预案开关）。交接单写不清的部分，本周就是断档。

**Q6：SEV1 和 SEV2 的区别怎么把握？**
A：看核心链路是否全断：全断/数据风险 = SEV1（拉全员应急群）；部分受损或快速劣化 = SEV2（oncall 处理，限时升级）。拿不准时**就高定级**——降级永远比升级容易，晚升级的代价是响应窗口流失。

**Q7：怎么降低深夜告警？**
A：从源头治理而不是忍耐：07 篇的告警审计（零动作告警删/降级）、burn-rate 双窗防抖动、聚合与抑制防风暴；再配合自动化（预案开关化、自愈脚本）。深夜 Page 的每一次都应在第二天问"这条告警该不该在深夜叫人"。

**Q8：故障恢复后为什么要观察 30 分钟？**
A：指标回基线可能是暂时的（流量低谷、缓存还在撑），二次故障往往在恢复后短时间内发生（回滚不彻底、降级未摘除、连接池未重建）。观察期用 [01 篇](./01-SLI-SLO与错误预算.md) 的 SLI 持续确认，再降级响应等级。

---

## 九、动手练习

1. 按 §5 模板为 ai-cs-message（8085，RocketMQ 消费侧）写一份 runbook：依赖列 MQ/MySQL，常见故障至少覆盖"消息积压""消费失败进死信"两条，命令必须本机可执行。
2. 完整走一遍 §6 作业单（正常状态下走一遍熟悉路径），然后执行 [03 篇](./03-故障演练与混沌工程.md) 演练 2（kill chat 容器）再走一遍 B 线，对比两次的差异并记录卡壳点。
3. 给 §6 作业单补一条"C 线：观测系统自身故障"（Prometheus 9090 / Grafana 3000 挂了时怎么办——提示：作业单的命令大多不依赖观测栈，这正是设计意图）。
4. 设计你们学习团队的值班轮转表：轮转周期、主备分工、交接单模板、补偿规则，并把每周 §6 巡检写进职责。
5. 复盘演练：假设 §6 B4 发现 sseChatService 熔断 OPEN，写出你执行的完整命令序列与每步预期输出——写不出来的输出，去 `ai-cs-chat` 源码与 `application.yml:125-130` 确认后再补上。

---

> 上一篇：[07-告警治理](./07-告警治理.md)
