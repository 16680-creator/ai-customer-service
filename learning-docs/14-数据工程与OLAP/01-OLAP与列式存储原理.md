# OLAP 与列式存储原理

> 对应项目：本篇是原理篇——项目当前**没有** ClickHouse/列式存储（⚠️ 工程未落地，现状证据见 [00-学习路线总览/05-技术缺口分析与补全计划](../00-学习路线总览/05-技术缺口分析与补全计划.md) B 类缺口"ClickHouse 用量分析"），
> 但"为什么要列存"的答案就藏在项目现有的两套存储里：`deploy/mysql/llm-observability-init.sql` 的 `model_usage` 计量表（行存，将来要迁 CH 的数据源）与 `ai-cs-search` 的 Elasticsearch `product_catalog` 索引（另一种"列优先"思想）。
> 相关：[02-ClickHouse实战](./02-ClickHouse实战.md)（目标态落地）、[03-数据库与ORM/05-MySQL锁机制与主从复制读写分离](../03-数据库与ORM/05-MySQL锁机制与主从复制读写分离.md)（B+ 树与 InnoDB）、[04-中间件/03-Elasticsearch搜索引擎](../04-中间件/03-Elasticsearch搜索引擎.md)（倒排索引）。

---

## 一、先结论：OLTP 与 OLAP 是两个世界

| 维度 | OLTP（在线事务处理） | OLAP（在线分析处理） |
|---|---|---|
| 典型操作 | 单行点查、小事务写 | 全表/大范围聚合 |
| 一次读多少数据 | 几行（索引定位） | 几百万~几十亿行 |
| 读写比 | 写多读少（相对） | 读多写少（Append-only 为主） |
| 响应要求 | 毫秒级、高并发 | 秒级~分钟级、低并发 |
| 数据模型 | 高度规范化（3NF） | 宽表/星型模型 |
| 代表存储 | MySQL（B+ 树行存） | ClickHouse / Doris / StarRocks（列存） |
| 本项目例子 | 下单扣库存（`ai-cs-order`） | "近 30 天各 scenario 的 LLM 花费"（目前还只能在 MySQL 上硬查） |

**为什么一个 MySQL 不够用**：B+ 树的索引和页布局都是为"快速找到一行"设计的；而聚合查询恰恰相反——要**扫过几乎所有行，但每行只用几列**。两种访问模式在同一存储结构上不可兼得，这就是 OLAP 存储存在的根本理由。

---

## 二、行存 vs 列存

### 2.1 磁盘布局对比

```
行存（MySQL InnoDB，按主键聚簇组织的一页）：
页 1：
┌─────────────────────────────────────────────┐
│ id=1 │ chat │ qwen3-32b │  812 │  356 │ 0.031 │   ← 一行的所有列挨着
│ id=2 │ rag  │ deepseek  │ 1210 │  480 │ 0.052 │
│ id=3 │ chat │ qwen3-32b │  790 │  322 │ 0.028 │
│ ...                                          │
└─────────────────────────────────────────────┘

列存（ClickHouse MergeTree 的一个 granule）：
model_usage.bin：
  scenario.bin : [chat, rag, chat, chat, rag, ...]        ← 同一列的所有值挨着
  input_tokens.bin : [812, 1210, 790, 990, 1105, ...]
  output_tokens.bin : [356, 480, 322, 410, 502, ...]
  ...（每列一个文件/流）
```

### 2.2 聚合查询的 IO 对比

问：`SELECT scenario, SUM(estimated_cost) FROM model_usage GROUP BY scenario`——只用到 2 列，表共 12 列，1 亿行：

| | 行存 | 列存 |
|---|---|---|
| 必须读的磁盘量 | 1 亿行 × 整行宽度 ≈ 全表（**12 列都读**） | 只有 `scenario` + `estimated_cost` 两列 ≈ 全表 **1/6**（若再压缩，更少） |
| 缓存友好度 | 一页里混着本次不需要的列 | 读进来的每个字节都是本次要算的 |
| CPU 分支预测 | 逐行解析整行记录头 | 同一列同构类型，可连续处理 |

**经验法则**：聚合只涉及 k 列、表共 n 列，列存 IO 约为行存的 k/n；再叠加列压缩（§3），常见再降 3~10 倍。这就是"亿级聚合秒级出数"的物理基础。

### 2.3 写入特征对比

| | 行存 | 列存 |
|---|---|---|
| 单行 UPDATE | 原地更新 + undo log（InnoDB MVCC） | 不支持原地改；用 `ALTER ... UPDATE` 重写、或 ReplacingMergeTree 合并时去重 |
| 单行 DELETE | 标记删除 | 同上，靠合并清理 |
| 最优写入 | 逐行小事务 | **大批量追加**（每批数万行起，攒批再写） |

> 记住这一点：**列存是"写入一次、查询千万次"的结构**。把高频小批量写灌进 CH 是新手最常见的翻车点（见 [02-ClickHouse实战](./02-ClickHouse实战.md) 反模式一节）。

---

## 三、列压缩：RLE 与字典编码

列存的第二大红利：同一列的值同构且重复度高，压缩效率远超行存的通用压缩。

### 3.1 RLE（Run-Length Encoding，游程编码）

把"连续相同的值"编码成 `(值, 出现次数)`：

```
scenario 列原始数据（100 万行）：
  chat, chat, chat, chat, chat, rag, rag, rag, agent, agent, ...

RLE 编码后：
  (chat, 5), (rag, 3), (agent, 2), ...
```

场景列这种**低基数、按时间局部聚集**的列，RLE 后往往只有原数据的百分之几。

### 3.2 字典编码（Dictionary Encoding）

把字符串值替换成整数编号，查询时再反查：

```
scenario 列：{chat, rag, agent, summary, vision, nl2sql, eval}  ← 基数只有 7

字典：
  0 = chat, 1 = rag, 2 = agent, 3 = summary ...

编码后数据：0, 1, 0, 0, 2, 1, 0, ...（全是小整数）
```

小整数带来三重收益：**存储小**（1 字节替代 4~32 字节字符串）、**比较快**（整数比较）、**GROUP BY 快**（直接按编号分组，最后才反查字典）。

### 3.3 各编码的适用列（速查）

| 编码 | 适用列 | 本项目 `model_usage` 表举例 |
|---|---|---|
| RLE | 低基数、局部聚集 | `scenario`、`status` |
| 字典编码 | 低基数字符串 | `provider`、`model` |
| Delta 编码 | 单调递增数值 | `create_time` 时间戳、自增 `id` |
| Gorilla | 浮点时间序列 | `estimated_cost` |

> ClickHouse 会自动为每列选择编码（ORDER BY 排序键的设计直接影响 RLE 效果——把 `scenario` 放进排序键，同值行物理聚集，RLE 才能生效）。这是 [02-ClickHouse实战](./02-ClickHouse实战.md) 排序键设计的底层原因。

---

## 四、向量化执行

### 4.1 行式执行 vs 向量化执行

```
火山模型（传统行式执行器）：
  for each row:                      ← 一次算一行，虚函数调用 n 次
      filter(row); aggregate(row);
  每行都经历：解析 → 判断 → 函数调用 → 聚合，CPU 分支预测/流水线效率低

向量化执行（ClickHouse / DuckDB 等）：
  拿到 scenario 列的一段（如 8192 个值，一个 block）
  一次循环处理整个 block：
      sum(cost[i] where scenario[i]==1)   ← 连续内存、无虚函数、SIMD 可并行
```

| 优化点 | 行式 | 向量化 |
|---|---|---|
| 函数调用次数 | 每行每算子一次（虚函数） | 每 block 一次 |
| CPU 缓存利用 | 差（行数据离散） | 好（连续内存 + 预取） |
| SIMD 利用 | 难 | 天然适配（一条指令比较 8 个 int） |

**量化直觉**：向量化引擎在纯扫描聚合上比传统行式执行器快一个数量级，且**数据量越大差距越大**。

---

## 五、为什么 OLAP 不用 B+ 树

B+ 树是为 OLTP 设计的索引结构（本项目 MySQL 所有表都是，见 [03-数据库与ORM/05](../03-数据库与ORM/05-MySQL锁机制与主从复制读写分离.md)）：

| B+ 树的 OLTP 优点 | 在 OLAP 场景下的失效原因 |
|---|---|
| 点查 O(log n)，树高 3~4 层就定位到行 | 聚合要扫全表，索引定位毫无意义——**反正每行都要看** |
| 叶子链表支持小范围扫描 | 亿行范围扫描时逐行回表、逐行解析，IO 放大 |
| 支持原地 UPDATE/DELETE | OLAP 几乎不改数据，为它付出的页内空间/写放大是纯浪费 |
| 二级索引加速随机过滤 | 分析查询的过滤列组合多变，不可能为每种组合建索引 |

**OLAP 的替代品——稀疏索引 + 排序键**（ClickHouse MergeTree 的做法）：

```
数据按排序键物理排序后，每 8192 行为一个 granule，记录每个 granule 的起始主键：

  granule 0: [t=00:00:01 ... t=00:00:47]
  granule 1: [t=00:00:47 ... t=00:01:12]
  ...

WHERE create_time BETWEEN '10:00' AND '10:05'
  → 稀疏索引二分定位：只命中 37 个 granule，其余 1 万个整块跳过
```

| 对比 | B+ 树索引 | 稀疏索引（granule） |
|---|---|---|
| 定位粒度 | 单行 | 8192 行的块 |
| 每行维护成本 | 有（页内目录+树） | 几乎为零 |
| 跳过无关数据 | 逐行 | **整块跳过** |
| 适合 | 点查/小范围 | 大范围扫描 + 预过滤 |

**一句话**：B+ 树花大力气"找到那一行"，稀疏索引花小力气"跳过那些块"——分析场景要的是后者。

---

## 六、LSM 与 MergeTree 家族概览

### 6.1 LSM-Tree（Log-Structured Merge Tree）

写优化结构：不原地改，先写内存表（memtable），满了整块刷成不可变磁盘文件（SSTable），后台定期**合并（compact）**：

```
写入：→ WAL（防丢）→ MemTable（内存，可查可改）
      → 满了排序刷盘 Level-0 文件（不可变）
      → 后台多路归并成更大的 Level-1 文件 ...（合并时去重/清理删除标记）

读取：MemTable + L0 + L1 + ... 逐层找（越新优先）
代价：读放大（要查多层）、合并时的后台 IO
收益：写入永远是顺序追加，写吞吐极高
```

LSM 家族：HBase、Cassandra、RocksDB（ES 的 Lucene 段结构也是同思想：不可变段 + 后台 merge）。

### 6.2 ClickHouse MergeTree：LSM 思想的列存实现

MergeTree 名字就是"合并树"：**数据一次写入后不可变，后台把小 part 异步归并成大 part**。

```
一次 INSERT batch = 一个 part（目录，含各列的 .bin/.idx 文件）
后台线程：把零散小 part 归并成大 part（顺带做去重/聚合/清理）
查询：只读完整的 part，天然跳过合并中的碎片
```

| 对比 | 通用 LSM（RocksDB） | MergeTree |
|---|---|---|
| 存储单元 | 行（KV） | **列**（每列独立压缩） |
| 合并时做什么 | 去重、清 tombstone | 去重 / **预聚合**（引擎决定） |
| 读路径 | 逐层查找 | 稀疏索引剪枝 + 向量化扫描 |

### 6.3 家族成员一览（选型详见 02 篇）

| 引擎 | 合并时行为 | 适用场景 |
|---|---|---|
| MergeTree | 只归并 | 通用明细表 |
| ReplacingMergeTree | 按排序键去重（保留最新） | 可容忍"查询时去重"的更新型数据 |
| AggregatingMergeTree | 合并预聚合状态 | 物化视图预聚合的标准搭档 |
| SummingMergeTree | 按排序键合并数值列 | 纯加总型指标 |

**理解了"不可变 part + 后台合并"，ClickHouse 的建表、去重、物化视图设计就都有了统一的解释框架**——下一篇 [02-ClickHouse实战](./02-ClickHouse实战.md) 全部建立在本篇之上。

---

## 七、面试高频问答

**Q1：行存和列存的本质区别是什么？各自适合什么场景？**
A：本质是磁盘布局——行存一行数据连续存放，列存一列数据连续存放。行存适合按主键定位少量行的点查/事务（OLTP），因为读一行只碰一页；列存适合全表聚合（OLAP），因为只读涉及的列（IO 从全表降到 k/n），且同列数据同构、压缩率高、可向量化。

**Q2：列式存储为什么压缩率比行存高？**
A：同一列的值类型相同、基数低、相似度高。RLE 对连续重复值编码成（值，次数）；字典编码把低基数字符串替换成小整数；Delta 编码处理单调递增的时间戳。行存的行间数据异构，通用压缩算法吃不到这些局部性。排序键设计让同值物理聚集，能进一步放大 RLE 效果。

**Q3：什么是向量化执行？为什么快？**
A：传统火山模型一次处理一行，每行每算子都有虚函数调用，CPU 流水线/分支预测效率低。向量化执行按列成块（如 8192 值）批处理：连续内存、少量函数调用、可用 SIMD 一条指令并行处理多个值，纯扫描聚合场景比行式执行快一个数量级。

**Q4：为什么 OLTP 用 B+ 树，OLAP 却不用？**
A：B+ 树的价值是"快速定位少量行"，而 OLAP 查询要扫几乎所有行，定位无意义；且 B+ 树为支持原地更新付出写放大与页内开销，OLAP 几乎不更新数据，纯属浪费。OLAP 用排序键 + 稀疏索引（按 8192 行 granule 记录边界）实现"整块跳过无关数据"，维护成本几乎为零。

**Q5：LSM-Tree 的写路径和读路径分别是什么？代价是什么？**
A：写路径：WAL → 内存 memtable → 排序刷盘为不可变 SSTable → 后台多路归并，全程顺序写所以写吞吐极高。读路径：从新到旧逐层查找，代价是读放大；合并占用后台 IO。适合写多读少的场景，ClickHouse MergeTree、RocksDB、HBase 都是这个思想。

**Q6：MergeTree 为什么叫这个名字？和 LSM 什么关系？**
A：数据按批次写入形成不可变 part，后台异步把小 part 归并（merge）成大 part，故称合并树。它是 LSM"不可变文件 + 后台合并"思想在列存上的实现，差异在于存储单元是列、合并时还能按引擎语义做去重或预聚合。

**Q7：什么数据不适合直接进列存引擎？**
A：高频小批量写（列存攒批才高效，逐条写会产生海量小 part）、频繁单行更新删除（靠重写/合并，成本高）、强事务要求（无完整 ACID 语义）。正确姿势是 OLTP 库承接业务写，经 CDC/批量导入进入列存做分析——本项目目标态就是 model_usage 在 MySQL 落库、经批量同步进 CH 分析。

---

## 八、动手练习

1. 在本地 MySQL 的 `chat_db.model_usage` 表（`deploy/mysql/llm-observability-init.sql` 建）上分别执行 `SELECT COUNT(*)`、`SELECT scenario, SUM(estimated_cost) GROUP BY scenario`，用 `EXPLAIN` 观察扫描行数——这就是"行存做聚合"的现状基线，作为与 CH 对比的对照组。
2. 手写一个 RLE 编码器（Java，输入 `List<String>`，输出压缩串与解压函数），用 `scenario` 列的真实重复模式测压缩比。
3. 把上面 RLE 扩展成字典编码：构建 `Map<String, Integer>` 字典后先编码再 RLE，对比两步的压缩比差异。
4. 用 JMH 或简单循环对比"逐行求和"与"按 int[] 块求和"的耗时差，体会向量化对 CPU 缓存/分支预测的意义。
5. ⚠️ 目标态演练：假设 `model_usage` 1 亿行、12 列、聚合只涉及 3 列，估算列存（含 5 倍压缩）相对行存的 IO 倍数，写成一行结论放进你自己的学习笔记。

---

> 下一篇：[02-ClickHouse实战](./02-ClickHouse实战.md)
