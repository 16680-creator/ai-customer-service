# 数仓分层与指标口径

> 对应项目：本项目当前**没有数仓**（⚠️ 工程未落地，本篇为目标态/预研方案）——现状是各业务库（MySQL 1 主 2 从，见 `deploy/mysql/init.sql` 系列脚本）+ 统计接口直查（如 `ai-cs-message/.../controller/ModelUsageController.java:65` 的用量统计接口内存聚合）。
> 分层思想的目标态参照物是项目里已有的"读模型"实践：商品搜索索引 `product_catalog`（`ai-cs-search`）与 LLM 计量表 `model_usage`（`deploy/mysql/llm-observability-init.sql:29-49`）。
> 相关：[02-ClickHouse实战](./02-ClickHouse实战.md)（分层结果落在哪）、[04-CDC与实时同步链路](./04-CDC与实时同步链路.md)（ODS 怎么来）、[07-LLM用量计量与成本分析](./07-LLM用量计量与成本分析.md)（口径问题的真实案例）。

---

## 一、先结论：不分层会发生什么

| 不分层的症状 | 根因 | 分层后的解法 |
|---|---|---|
| 五个人写五个"今日营收"，数字互相对不上 | 每人各写各的清洗过滤逻辑 | 清洗规则收敛在 DWD 一处 |
| 报表慢查询打挂业务库 | 分析 SQL 与交易 SQL 抢同一个 MySQL | ODS 先搬出去，分析在数仓跑 |
| 口径改了，历史报表全变 | 逻辑散落各报表脚本，改一处漏三处 | 下层不动，只改上层，历史可重算 |
| 新报表要从头捋十张表 JOIN | 没有中间层沉淀通用逻辑 | DWS 轻度聚合复用 |
| 出错不知道哪层脏了 | 全链路一条大 SQL | 分层校验，逐层定位 |

**一句话**：分层不是为了好看，是把"清洗、聚合、口径"**各收敛一次、处处复用**，并让每一层可独立重跑、可独立追责。

---

## 二、ODS / DWD / DWS / ADS 四层

### 2.1 全景图

```
 业务库（MySQL 1主2从：product / orders / users / chat_db.model_usage ...）
      │  CDC（Canal）/ 批量同步（水位线，见 04、05 篇）
      ▼
┌─────────────────────────────────────────────────────────────┐
│ ODS  贴源层：原样落地，只加技术字段，不改业务语义                  │
│      ods_product / ods_orders / ods_model_usage                │
├─────────────────────────────────────────────────────────────┤
│ DWD  明细层：清洗（空值/枚举/去重）+ 规范化 + 维度外键             │
│      dwd_trade_order_detail / dwd_llm_usage_detail             │
├─────────────────────────────────────────────────────────────┤
│ DWS  汇总层：轻度聚合，主题×常用维度×时间粒度                      │
│      dws_trade_order_1d（按天）/ dws_llm_usage_scenario_1d     │
├─────────────────────────────────────────────────────────────┤
│ ADS  应用层：看板/报表直接读，一个指标一个口径，宁冗余勿join        │
│      ads_dashboard_daily / ads_cost_bill_daily                 │
└─────────────────────────────────────────────────────────────┘
      ▼
 BI 看板（ECharts，见 06 篇）/ Grafana / 成本账单
```

### 2.2 各层职责与"铁律"

| 层 | 存什么 | 粒度 | 铁律 |
|---|---|---|---|
| **ODS** | 源系统数据的原样镜像 | 行级 | **不做任何业务加工**；只补同步时间、批次号、binlog 位点等技术字段 |
| **DWD** | 清洗后的明细事实 | 行级（一行=一个业务动作/事件） | 过滤规则集中此层；枚举/单位/时区在此统一；关联维度产**外键**而非快照列 |
| **DWS** | 主题级轻度汇总 | 聚合（如 天×场景×模型） | 只做"确定性"聚合（sum/count），含口径争议的指标放 ADS |
| **ADS** | 面向看板的最终指标 | 指标 | **口径唯一负责人**；允许冗余宽表，禁止再往下穿透取数 |

> 本项目现有实践对号入座：商品 CDC 同步到 ES 的 `product_catalog`（`ProductCdcConsumer` 消费后落索引）本质上就是一条 **ODS（binlog 原事件）→ DWD（过滤 enabled/table 校验 + 幂等 upsert）→ ADS（搜索读模型）** 的链路；`model_usage` 表对计量主题来说就是 ODS。缺的是中间的 DWD/DWS 规范层与统一口径管理。

### 2.3 目标态：给项目建两条分层流水线（预研 SQL）

```sql
-- ⚠️ 目标态示例：LLM 计量主题（假设 ODS 已同步到 CH，见 02 篇 §6）

-- DWS：按天 × scenario × model 轻度汇总（与 02 篇物化视图产物对应）
--      只做无争议聚合：次数、token 总和、费用总和
SELECT
    toDate(create_time) AS day,
    scenario,
    model,
    count()                       AS call_cnt,
    sum(total_tokens)             AS tokens_total,
    sum(estimated_cost)           AS cost_total,
    countIf(status = 'FAILED')    AS fail_cnt
FROM chat_olap.model_usage
GROUP BY day, scenario, model;

-- ADS：成本看板直供表——口径决策全部显式写死在 SQL 里
-- 口径 1：失败请求的调用计入次数，但费用记 0（FAILED 调用不计费）
-- 口径 2：估算行（estimated=1，流式取不到精确 usage）参与统计并单独占比列出
SELECT
    day,
    sum(cost_total)                                        AS bill_cost,
    round(sumIf(cost_total, est_cnt > 0) / greatest(sum(cost_total), 0.000001), 4)
                                                           AS estimated_cost_ratio,
    sum(fail_cnt)                                          AS failed_calls
FROM (
    SELECT day,
           sum(cost_total)  AS cost_total,
           sum(if(estimated = 1, call_cnt, 0)) AS est_cnt,
           sum(fail_cnt)    AS fail_cnt
    FROM dws_llm_usage_scenario_1d
    GROUP BY day
)
GROUP BY day;
```

---

## 三、维度建模与星型模型

### 3.1 事实表与维度表

| 角色 | 定义 | 特征 | 本项目例子（电商客服场景） |
|---|---|---|---|
| **事实表** | 一行 = 一个业务动作，存**可加的度量** | 外键多、可聚合、行数巨大 | 一笔订单、一次 LLM 调用 |
| **维度表** | 描述"谁、什么、何时何地、为什么" | 属性丰富、行数小、变化慢 | 用户、商品、时间、模型、scenario |

### 3.2 星型模型（Star Schema）

```
                        ┌─────────────┐
                        │ dim_date    │
                        │ (日期维度)   │
                        └──────┬──────┘
┌─────────────┐                │                ┌──────────────┐
│ dim_user    │──────┐         │        ┌───────│ dim_product  │
│ (用户维度)   │      │         │        │       │ (商品维度)    │
└─────────────┘      ┌─┴─────────┴────────┴─┐     └──────────────┘
                     │  fact_order_detail   │
                     │  ── 度量：qty, amount │
                     │  ── 外键：user_id    │
                     │      product_id      │
                     │      date_key        │
                     └──────────────────────┘
                              ▲
                    ┌─────────┴─────────┐
                    │ dim_channel       │
                    │ (渠道维度: 客服AI/ │
                    │  人工/自营商城)    │
                    └───────────────────┘
```

事实表在中心被维度表"包围"，JOIN 只有事实→维度一层（无雪 花），这是 BI 查询友好的关键。

### 3.3 两个关键建模决策

**① 缓慢变化维度（SCD）**：用户改了昵称、商品改了类目，历史事实怎么算？

| 策略 | 做法 | 适用 |
|---|---|---|
| SCD1 | 直接覆盖 | 只关心最新状态（本项目 ES 读模型即如此） |
| SCD2 | 加版本行（生效起止日期 + is_current 标记） | **报表要对历史负责**（营收归属类目要用成交时的类目） |

**② 事实表的"快照外键"原则**：DWD 事实表只记**当时的维度键**（如 `category_key`），不复制维度属性；报表要"成交时类目"就 JOIN SCD2 维度取历史版本。混着来必然口径漂移。

---

## 四、指标口径一致性

### 4.1 什么是"口径"

口径 = 一个指标的**完整定义**：统计对象、时间窗、时间字段、状态过滤、单位、去重规则、负值处理。缺任何一项，两个"同名指标"就会算出两个数。

### 4.2 电商客服场景的口径定义表（可直接抄进指标字典）

| 指标 | 口径定义（唯一权威版） | 常见分歧点 |
|---|---|---|
| **GMV** | 统计周期内**成功创建**的订单应付总额，含未支付、含退款订单、不含运费券抵扣 | 含不含未支付？含不含已取消？下单时刻还是支付时刻？ |
| **营收** | 统计周期内**支付成功**（PAID）订单实付总额，退款按退款发生日**负向冲减** | 算"支付口径"还是"净额口径"？退款冲减记原单日还是退款日？（本表：退款日冲减） |
| **退款率** | 退款率 = 退款金额 ÷ 营收（**同一窗口同一口径分母**）；分母用"该周成交订单的实付"而非"该周退款订单原额" | 分母口径！分子分母时间窗错位是最高频事故 |
| **毛利** | 毛利 = 营收 − 成本；成本 = 按订单明细抓商品成本价快照 × 数量，**不含** LLM 客服调用成本 | 客服成本算不算 COGS？（本表：算费用不算成本；单列"服务成本率"指标） |

### 4.3 口径漂移的三个真实形态（事故复盘式）

**事故一：时间窗错位——"退款率 12% 还是 31%？"**

```
错误算法：退款率 = 本周退款金额 / 本周退款订单的原支付金额
  → 分母只统计"恰好在本周退款"的订单，基数小，比率虚高到 31%

正确算法：退款率 = 本周退款金额 / 本周成交订单实付总额
  → 分母是本周全部成交，比率 12%

根因：两个报表的分母过滤条件不同，且都没写口径注释。
解法：DWS 层同时产出"成交额"与"退款额"两列，比率只在 ADS 层算；
     指标字典给每个指标唯一 SQL。
```

**事故二：状态过滤漂移——"GMV 周报突然涨了 8%"**

```
有人在 GMV 统计里把 CANCELLED 排除条件从"取消时间"改成"创建状态"，
历史一周前"下单后又取消"的订单被重新计入 GMV → 周环比虚高 8%。

根因：口径埋在散落的 WHERE 里，改动无评审。
解法：状态过滤收敛到 DWD（dwd 层只产出"有效订单"明细，定义唯一）；
     上层报表不许自己再过滤状态。
```

**事故三：跨系统时间字段不一致——"客服账单和财务差 3 小时"**

```
报表 A 按 create_time（北京时间，业务库 DATETIME 无时区）切天；
报表 B 按 DBT 里 UTC 时间戳切天 → 每天差 8 小时，月底差出整天的量。

根因：时间字段与时区未在 DWD 统一。
解法：DWD 强制输出规范化列（本地时区 DATETIME + UTC TIMESTAMP 各一列），
     指标字典写明"按哪个时间字段切窗"。
```

### 4.4 口径治理的落地机制

| 机制 | 内容 |
|---|---|
| 指标字典 | 每指标一行：名称/定义/SQL/负责人/变更记录（哪怕就是一张 wiki 表） |
| 口径注释 | 每个指标 SQL 首行注释写全口径（参照 02 篇 §6.3 的写法） |
| 单向依赖 | 口径只允许定义在 DWD/DWS，ADS 只组装不许重定义 |
| 变更评审 | 改口径 = 改"宪法"，需通知所有下游看板并留版本 |
| 对账兜底 | 新旧口径并行跑一个周期，差异表人工确认（见 08 篇） |

> 本项目的现成案例：`ModelUsageRecorder` 的费用估算口径就有三处精细决策——① 未配置单价走默认单价兜底（`ModelUsageRecorder.java:128-129`）；② pricingKey 与展示模型名分离，避免供应商改名导致查不到单价（`ModelUsageRecorder.java:96-97` 注释）；③ 流式估算行打 `estimated` 标记防止"估算当精确"误导成本决策（`ModelUsageRecorder.java:98-100` 注释）。**计量口径设计的三条原则在这一个类里都有体现**，详见 [07-LLM用量计量与成本分析](./07-LLM用量计量与成本分析.md)。

---

## 五、面试高频问答

**Q1：数仓为什么要分层？每层的职责一句话说清。**
A：分层让清洗、聚合、口径各收敛一次处处复用，且每层可独立重跑追责。ODS 原样贴源不动业务语义；DWD 做清洗规范化产明细事实；DWS 做确定性轻度聚合沉淀中间层；ADS 面向应用供最终口径指标，禁止下游穿透取数。

**Q2：ODS 层为什么"什么都不做"？加字段不算加工吗？**
A：ODS 的价值是**可回放的事实存证**——下游任何一层数错了都能从 ODS 重算。补同步时间、批次号、位点这类技术字段不改变业务语义，属于让数据"可追溯"；一旦在 ODS 做业务过滤/转换，事实源就失真了。

**Q3：星型模型是什么？和雪花模型差在哪？为什么 BI 场景偏爱星型？**
A：事实表居中、维度表环绕、事实表只与维度表一层 JOIN。雪花模型把维度再规范化拆子表（如商品→类目→部门），省一点空间但 JOIN 变多。分析查询 JOIN 越多越慢越难写，且维度表本来就小、冗余无妨，所以 BI 偏爱星型。

**Q4：什么是缓慢变化维度？用户改名了历史订单报表该怎么算？**
A：SCD 处理维度属性随时间变化的问题。SCD1 直接覆盖，只看最新态；SCD2 拉链表记录每个版本的生效期与 is_current，历史事实按成交时点的版本 JOIN。对"历史报表要复现当时状态"的场景（如营收按成交时类目归属）必须 SCD2。

**Q5："退款率"这个指标最容易吵起来的是什么？**
A：分母口径。常见错误是分母用"本周发生退款的订单原额"（基数小、比率虚高），正确是分母与分子同一窗口同一总体的成交额；另外分子退款额按"退款发生日"还是"原单日"归期也要在指标字典里定死。核心纪律：比率类指标的分母必须显式定义且与分子同窗。

**Q6：两个报表同一个指标对不上，你的排查步骤？**
A：先比对口径五要素：统计对象过滤（状态/是否含退款）、时间窗与切窗字段（create vs pay vs 退款日）、单位与时区、去重规则、负值处理；再逐层下钻——若都读同一 DWS 层则查上游同步缺口（对账），若各自写 SQL 则把口径收敛到中间层。修复后并行双跑一个周期验证。

**Q7：你们项目的"数据系统"现在处于什么阶段？**
A：属于"业务库 + 读模型直查"阶段：有事实源 MySQL（1 主 2 从）、有 CDC 读模型（Canal→ES 商品索引）、有计量明细表（model_usage）与统计接口，但还没有正式数仓分层与统一指标字典——已在技术缺口计划里立项 ClickHouse 用量分析，目标态是 ODS 进 CH、DWS 靠物化视图按天×场景×模型预聚合、ADS 出成本报表；口径纪律方面计量费用估算已做了单价兜底、pricingKey 与展示名分离、估算行打标三条设计。

---

## 六、动手练习

1. 给 `chat_db.model_usage`（`deploy/mysql/llm-observability-init.sql` 建）手写"今日各场景花费"SQL，并在首行注释里写全口径五要素（对象/时间窗/时间字段/状态/单位）——对比不写注释的版本，感受口径显式化。
2. 按本文 §2.3 把上一题 SQL 改写成"DWS 产出 + ADS 组装"两段式，验证拆分后两版结果一致（这就是口径收敛的最小演练）。
3. 复现事故一：用 `orders` 表（`deploy/mysql/order-init.sql` 建表）分别用两种分母算退款率，记录两个数字并写出各自的隐含口径。
4. 给商品维度设计一张 SCD2 拉链表（`dim_product_scd2`），模拟一次改价，写出"3 个月前某笔订单按成交时价格归属"的 JOIN 查询。
5. ⚠️ 目标态推演：为本项目画一条完整的分层链路图（MySQL 业务库 → ODS → DWD → DWS → ADS → ECharts 看板），标注每层对应的既有组件（如 ODS≈`model_usage` 明细、ADS≈02 篇物化视图）与待建组件（DWD 清洗层、指标字典）。

---

> 上一篇：[02-ClickHouse实战](./02-ClickHouse实战.md) ｜ 下一篇：[04-CDC与实时同步链路](./04-CDC与实时同步链路.md)
