# 批处理与调度编排

> **分工声明**：XXL-Job 是什么、admin/executor 架构、控制台配置、路由策略与部署步骤，已在 [07-运维部署/05-XXL-Job分布式调度](../07-运维部署/05-XXL-Job分布式调度.md) 讲透。本篇不再重复 XXL-Job 用法，聚焦**"数据任务"的批处理设计学**——一个批处理任务怎么做到敢重跑、怎么记水位线、怎么做回填、怎么编排 DAG、失败了怎么重试告警。这些设计与具体调度器无关，换 Quartz/DolphinScheduler/Airflow 一样成立。
> 对应项目：`ai-cs-order/src/main/java/com/aics/order/task/OrderTimeoutScheduler.java`（@Scheduled 批处理本体）、`ai-cs-order/src/main/java/com/aics/order/task/OrderTimeoutScanJob.java`（XXL-Job 入口）、`ai-cs-order/src/main/java/com/aics/order/task/XxlJobConfig.java`（开关式装配）。
> 相关：[07-运维部署/05](../07-运维部署/05-XXL-Job分布式调度.md)（前置必读）、[04-CDC与实时同步链路](./04-CDC与实时同步链路.md)（对账任务的调度形态）、[02-ClickHouse实战](./02-ClickHouse实战.md)（数据同步/回填任务的目标态）、[04-中间件/02-RocketMQ消息队列](../04-中间件/02-RocketMQ消息队列.md)（重试与死信）。

---

## 一、先结论：数据任务与业务接口的四点不同

| 维度 | 业务接口（同步请求） | 数据任务（异步批处理） |
|---|---|---|
| 触发 | 用户请求，一次一次来 | 定时/依赖触发，一次**一批** |
| 失败影响 | 单次请求报错，用户重试 | 一批数据延迟，**下游全堵** |
| 重试语义 | 用户手动重试天然幂等期待 | **必须自己设计幂等**，否则重复扣/重复发 |
| 进度可观测 | 有响应码 | 没有"响应"，**必须设计水位线与告警** |
| 典型形态 | Controller | @Scheduled / XXL-Job handler / 数据同步 Job |

**批处理任务设计四要素**：幂等重跑（§3）、水位线（§4）、回填（§5）、失败重试与告警（§7）。DAG 编排（§6）把它们组合成流水线。

---

## 二、项目现场：三重保障里读出的批处理骨架（已落地）

本项目订单超时关单是最典型的数据任务（背景与三重保障全景见 [07-运维部署/05](../07-运维部署/05-XXL-Job分布式调度.md) 第零节），本篇从"数据任务设计"视角重读它：

```java
// ai-cs-order/src/main/java/com/aics/order/task/OrderTimeoutScheduler.java:29-53（节选）
@Scheduled(fixedDelay = 300000, initialDelay = 60000)     // ① 周期：每 5 分钟，串行（fixedDelay 防重叠）
public void scanExpiredOrders() {
    List<Order> expired = orderMapper.selectList(          // ② 查询即"水位过滤"：
            new LambdaQueryWrapper<Order>()
                    .eq(Order::getStatus, OrderStatus.PENDING_PAY.getCode())   //    状态条件
                    .lt(Order::getExpireTime, LocalDateTime.now()));           //    时间条件
    for (Order order : expired) {                          // ③ 逐条处理
        try {
            orderService.cancelExpiredOrder(order.getOrderNo());  // ④ 单条失败不中断整批
        } catch (Exception e) {
            log.error("定时任务：取消订单失败 orderNo={}", order.getOrderNo(), e);
        }                                                  // ⑤ 失败的下一轮自然重试（幂等保证无害）
    }
}
```

```java
// ai-cs-order/src/main/java/com/aics/order/task/OrderTimeoutScanJob.java:27-32
@XxlJob("orderTimeoutScanJob")                             // 集中调度入口（只做入口）
public void orderTimeoutScanJob() {
    orderTimeoutScheduler.scanExpiredOrders();             // 复用同一份业务逻辑
    XxlJobHelper.handleSuccess("订单超时扫描完成");          // 回报结果给 admin（可观测）
}
```

**对应四要素的映射表**（后面各节展开原理）：

| 要素 | 本项目做法 | 效果 |
|---|---|---|
| 幂等重跑 | `cancelExpiredOrder` 按订单状态机：仅 PENDING_PAY 可流转 CANCELLED | 双通道并发扫、重复触发均无副作用 |
| 水位线 | 无显式水位表——`expire_time < now` 即**由业务时钟推导的天然水位** | 无需持久化进度 |
| 回填 | 不需要——每轮全量扫所有过期未处理单 | 漏扫自动被下一轮覆盖 |
| 失败重试 | 单条 try/catch + 下轮重扫；XXL-Job 层另有失败重试配置 | 单条失败不拖垮整批 |

> 注意这个任务的**特殊性**：它是"收敛型"任务（每轮从头扫，反正状态机幂等），所以可以偷懒不用水位线。**数据同步型任务（增量导入 CH/ES）不收敛，必须显式水位线**——这正是 §4 存在的理由。

---

## 三、幂等重跑：批处理的第一设计原则

### 3.1 幂等的判定问题

写任何批处理任务前先回答三连问：

```
① 这个任务跑两次，第二次会做什么？       （重复扣库存？重复发消息？重复入账？）
② 任务的"输入"能标识吗？               （有唯一键：订单号/request_id/日期分区？）
③ 中途挂了重跑，已完成的部分怎么办？      （跳过？重做？结果一致吗？）
```

三问答不好，任务不能上线。

### 3.2 四种幂等实现（按优先级）

| 手段 | 做法 | 本项目例子 |
|---|---|---|
| **状态机幂等** | 前置条件校验：只有"可流转状态"才执行 | 关单：仅 PENDING_PAY→CANCELLED（`OrderTimeoutScheduler`） |
| **键幂等** | 写入目标以业务唯一键 upsert/ON DUPLICATE KEY | CDC 消费 ES `_id=product.id`（`ProductCdcConsumer.java:49-59`） |
| **分区幂等** | 按"日期分区"先删后插（delete-then-insert），重跑=重算该分区 | 数仓 ADS 表重跑某天 |
| **去重表幂等** | 任务先登记"batch_id 已处理"，重复触发直接跳过 | 通用兜底，多一次 IO |

### 3.3 "先删后插"的分区重跑模板

```sql
-- 数仓/报表表的标准重跑姿势：按分区原子重算
-- ① 重跑 2026-09-01 分区：先删该分区
DELETE FROM ads_dashboard_daily WHERE stat_date = '2026-09-01';
-- ② 再按口径重算插入
INSERT INTO ads_dashboard_daily (stat_date, metric, value)
SELECT '2026-09-01', 'bill_cost', SUM(estimated_cost)
FROM dws_llm_usage_detail WHERE toDate(create_time) = '2026-09-01' AND status = 'SUCCESS';
-- 整个"删+插"放在一个事务/或按分区替换（CH 用 REPLACE PARTITION，见 02 篇）
```

**为什么不是 UPSERT 逐行比对**：批处理追求"整分区可丢弃可重建"，逐行比对成本高且易漏。**可丢弃的分区 > 可修补的行**。

---

## 四、水位线：增量任务的进度记录

### 4.1 水位线是什么

水位线（watermark）= 任务持久化记录的"**我处理到哪个位置了**"，可以是时间戳、自增 ID、binlog 位点、文件偏移。增量任务每轮：

```
① 读水位线：last_watermark = 2026-09-01 12:00:00
② 拉增量：SELECT * FROM source WHERE update_time > last_watermark AND update_time <= now() - 安全滞后
③ 处理（幂等）
④ 推水位线：last_watermark = 本批最大 update_time（原子更新，见 4.3）
```

### 4.2 水位线表设计（可复制）

```sql
CREATE TABLE IF NOT EXISTS batch_watermark (
    job_name     VARCHAR(64)  NOT NULL COMMENT '任务名（唯一）',
    last_value   VARCHAR(128) NOT NULL COMMENT '水位值：时间戳/自增ID/位点',
    updated_at   DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (job_name)
) ENGINE=InnoDB COMMENT='批处理任务水位线表';
```

**三种水位类型**：

| 类型 | 水位值 | 适用 | 风险 |
|---|---|---|---|
| 时间戳 | `update_time` 上界 | 有更新时间列的表 | **必须留安全滞后**（§4.3） |
| 自增 ID | `id` 上界 | 只增不改的流水（追加型） | 分库分表下每库各一条 |
| 位点 | binlog/MQ offset | CDC/流式 | 需存储端配合（见 04 篇 §4） |

### 4.3 三个工程细节（都是事故高发点）

**① 安全滞后（迟到数据兜底）**：拉取上界取 `now() - 5min` 而不是 `now()`——事务提交时间与 `update_time` 写入可能有偏差，不留滞后必漏数据。宁可同一窗口被下一轮重复拉（幂等无害）。

**② 时间戳水位的"边间重复"问题**：用 `>` 还是 `>=`？两条同秒记录会漏或重——**重复靠幂等兜底，漏数据不可救，所以取 `>` 且水位推"本批最大值"**；或改用 `(update_time, id)` 复合水位。

**③ 水位推进与处理结果绑定**：先处理、成功后再推水位；**处理失败不推**（下轮重拉）。反过来"先推再处理"挂了就永久漏段。并发时用 `UPDATE ... WHERE last_value = 旧值` 乐观推进。

---

## 五、回填（Backfill）：补历史数据的受控操作

### 5.1 什么时候需要回填

| 场景 | 例子 |
|---|---|
| 上线新指标 | 历史数据没有该字段/该聚合，要补算 |
| 口径变更 | 退款口径修正，历史 90 天重算 |
| 事故修复 | 同步任务静默失败三天，补三天的量 |
| 新存储接入 | CH 从零灌 6 个月 model_usage（02 篇 §6.2） |

### 5.2 回填四原则

```
① 分区化：按天/月切任务单元，单次只回填一个分区（失败只重跑一个分区）
② 限流化：回填批量拉低线上库？走从库/只读副本、控并发、错峰跑
③ 幂等化：复用 §3 的分区重跑模板——回填就是"定向重跑"
④ 可观测：每个分区记状态表（pending/running/done/failed），断点续填
```

```sql
-- 回填进度表示例
CREATE TABLE backfill_progress (
    backfill_id  VARCHAR(64) NOT NULL,
    partition_key VARCHAR(32) NOT NULL,        -- '2026-05' / '2026-05-01'
    status       VARCHAR(16)  NOT NULL,        -- PENDING/RUNNING/DONE/FAILED
    updated_at   DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (backfill_id, partition_key)
) ENGINE=InnoDB COMMENT='回填任务分区进度';
```

**回填与在线写入冲突怎么办**（CH/ADS 场景）：回填写临时表/影子分区，完成后 `EXCHANGE/REPLACE PARTITION` 原子替换——读方无感知，失败直接丢弃影子分区。

---

## 六、DAG 依赖编排：任务怎么串成流水线

### 6.1 依赖即 DAG

数据任务的依赖天然是**有向无环图**（上游产出 → 下游才能跑）。以本项目目标态（02/03 篇）为例：

```
        ┌────────────┐
        │ ODS同步     │  MySQL → CH（水位线增量，05 篇 §4）
        │ model_usage│
        └─────┬──────┘
              ▼
        ┌────────────┐
        │ DWD 清洗    │  规范化/过滤（03 篇）
        └─────┬──────┘
        ┌─────┴──────┐
        ▼            ▼
  ┌──────────┐  ┌──────────┐
  │ DWS 聚合  │  │ 对账任务  │  ←── 只依赖 ODS（提前发现同步缺口）
  │ (物化视图) │  │ MySQL vs CH│
  └─────┬────┘  └──────────┘
        ▼
  ┌──────────┐
  │ ADS 看板  │ ──▶ 成本报表 / Grafana（06 篇）
  └──────────┘
```

### 6.2 编排的三种实现强度

| 强度 | 做法 | 适用 |
|---|---|---|
| **子任务链** | XXL-Job 任务配置"子任务 ID"，上游成功自动触发下游 | 简单线性链（本项目 XXL-Job 能力范围内，见 07-运维部署/05 控制台配置） |
| **条件触发** | 上游任务末尾写"完成标记"（Redis key/表记录），下游开跑前轮询标记 | 跨服务依赖、不想引调度系统 |
| **专用编排器** | Airflow/DolphinScheduler 管理 DAG、重跑、并行度 | 任务数十个以上的正式数据平台 |

**设计纪律（比工具重要）**：
1. **依赖只声明在"数据"上，别写在时钟上**——错误示范：下游"每天 03:10 跑"赌上游 03:00 完成；正确姿势：显式依赖（子任务链/完成标记）。时钟依赖就是"排期巧合"，上游一慢全链路错。
2. **上游未完成 ≠ 失败**：下游启动时发现上游未完成，应**跳过等待**而非报错轰炸告警。
3. **DAG 允许扇出，禁止环**：出现环说明职责切错了层。

---

## 七、失败重试与告警

### 7.1 重试的分层设计

| 层 | 手段 | 本项目对应 |
|---|---|---|
| **单条级** | 逐条 try/catch，失败记录、不中断整批 | `OrderTimeoutScheduler.java:46-53` 逐单 try/catch |
| **任务级** | 调度平台失败重试（次数+间隔） | XXL-Job 控制台"失败重试次数" |
| **依赖级** | 下游发现上游未完成 → 延迟重试（指数退避） | 完成标记轮询 |
| **跨轮级** | 本轮失败的，下一轮全量重扫自然覆盖 | 关单任务"下轮扫描自然重试" |

**指数退避模板**：重试间隔 1min → 2min → 4min（×2 封顶），避免故障恢复瞬间被重试风暴打二次。**重试必须有上限，上限到了必须告警**——静默重试是数据事故最大的温床。

### 7.2 告警分级

| 级别 | 触发条件 | 动作 |
|---|---|---|
| P0 电话 | 任务连续 N 轮失败 / 水位线滞后超阈值（如同步滞后 > 1 小时） | 值班介入 |
| P1 即时消息 | 单轮失败但历史水位健康、重试可自愈 | 群通知，观察下一轮 |
| P2 日报 | 对账差异在容忍阈值内、回填任务排队中 | 每日摘要 |

**可观测三件套**（数据任务没有"响应码"，全靠主动暴露）：
1. **执行日志**：任务开始/结束/处理量/失败量（XXL-Job 执行日志看板天然提供，见 07-运维部署/05）。
2. **结果回报**：`XxlJobHelper.handleSuccess/handleFail`（`OrderTimeoutScanJob.java:31`），admin 端可查可告警。
3. **业务水位指标**：本轮处理 N 条、水位滞后 M 分钟、对账差异 K 条——进 Prometheus/Grafana，别只让人翻日志。

---

## 八、面试高频问答

**Q1：写一个批处理任务，第一步想什么？**
A：先答幂等三连问：跑两次第二次做什么、输入能否唯一标识、中途挂了重跑已完成部分怎么办。答不出就不该上线。实现按优先级选：状态机幂等、业务键 upsert、分区先删后插、去重表。

**Q2：什么是水位线？没有会怎样？**
A：任务持久化的"已处理到哪"的进度标记（时间戳/自增 ID/位点），增量任务靠它每轮只拉新数据。没有水位线只能全量扫（数据量上来后不可行）或掐时间盲跑（漏数据）。设计要点：拉取上界留安全滞后、失败不推水位、水位推进与处理结果绑定。

**Q3：为什么时间戳水位要留安全滞后？**
A：行的 update_time 与事务提交到对源可见之间存在偏差，按 now() 做上界可能漏掉"刚提交还查不到"的行。取 now()-N 分钟做上界，宁可重复拉取——重复由幂等兜底，漏数据不可救。

**Q4：回填怎么做才不伤线上？**
A：四原则：分区化（按天/月切任务单元，失败只重跑一个分区）、限流化（走从库、控并发、错峰）、幂等化（复用分区重跑模板）、可观测（进度表断点续填）。对 OLAP 表用影子分区+REPLACE PARTITION 原子替换，读方无感知。

**Q5：DAG 编排里最容易犯的设计错误？**
A：把依赖写在时钟上——下游"每天 03:10 跑"赌上游 03:00 完成。正确做法是显式声明数据依赖（子任务链/完成标记），上游未完成时下游跳过等待而非报错。另一个是依赖成环，说明分层职责切错了。

**Q6：本项目订单超时扫描任务体现了哪些批处理设计？**
A：三重保障（RocketMQ 延迟消息主路径 + @Scheduled 本地兜底 + XXL-Job 集中调度）跑同一份幂等逻辑；扫描条件 status=PENDING_PAY 且 expire_time<now 是业务时钟推导的天然水位，无需显式水位表；逐单 try/catch 保证单条失败不中断整批；关单走状态机幂等所以双通道并发无副作用；XXL-Job 入口用 handleSuccess/handleFail 回报结果形成可观测闭环。

**Q7：批处理失败重试怎么设计才不出事故？**
A：分层：单条 try/catch 不拖垮整批；任务级调度重试设次数上限（指数退避防重试风暴）；跨轮自然重试兜底。纪律是"重试必须有上限、上限到了必须告警"——静默无限重试会让故障被掩盖成"数据延迟"，等发现时缺口已经很大。

**Q8：定时任务多实例部署会怎样？怎么防重复执行？**
A：每实例都会按自己的 cron 触发，重复扫描/重复处理。三层防线：调度中心路由只派一台（XXL-Job FIRST/轮询/分片）；应用层分布式锁；数据层业务幂等（重复执行无副作用）。本项目三管齐下且以幂等兜底——即使调度层全失效，状态机保证不产生错误结果。

---

## 九、动手练习

1. 读 `OrderTimeoutScheduler.java` 与 `OrderTimeoutScanJob.java`，对照 §2 映射表逐条找出四要素的落点；然后回答：若把"逐单 try/catch"改成整批一个 try/catch，一条坏订单会造成什么连锁后果？
2. 为"model_usage 每日增量同步到 CH"（02 篇 §6 目标态）设计水位线表记录与任务伪代码：写清安全滞后取多少、水位何时推进、失败如何表现。
3. 实现一个通用的 `batch_watermark` 表 + Java 读写的 DAO（乐观推进 `UPDATE ... WHERE last_value=?`），并写单测覆盖"并发推进只有一方成功"。
4. 设计一次口径变更回填：ADS 成本口径从"含失败请求"改为"仅 SUCCESS"，写出按分区回填的步骤序列（含影子分区替换与进度表状态流转）。
5. 把 §6.1 的目标态 DAG 落成 XXL-Job 子任务链配置清单（每个任务：JobHandler 名、路由策略、阻塞策略、子任务 ID），并标注哪一条依赖改用"完成标记"更稳、为什么。

---

> 上一篇：[04-CDC与实时同步链路](./04-CDC与实时同步链路.md) ｜ 下一篇：[06-BI看板与ECharts可视化](./06-BI看板与ECharts可视化.md)
