# 数据质量与对账

> 对应项目：`ai-cs-search/src/main/java/com/aics/search/cdc/ProductCdcConsumer.java`（MySQL→ES 同步与幂等，对账的对象）、`deploy/mysql/canal-init.sql`（CDC 账号）、`deploy/mysql/llm-observability-init.sql:29-49`（`model_usage` 计量表，检测 SQL 的演练对象）、`deploy/mysql/master.cnf`（binlog `expire-logs-days=7`，对账兜底的动因之一）。
> 项目现状：Canal 同步的商品数据（MySQL→ES）天然需要对账——**对账方案已有设计**（[04-中间件/11-CanalCDC商品索引同步](../04-中间件/11-CanalCDC商品索引同步.md) 第十节"三板斧"之 10.3 对账、第十二节明确"对账任务与 Grafana 告警"尚待完成），**对账任务执行未落地**（依赖 Canal Server 编排先行，见缺口清单"Canal 编排落地"项）。
> 相关：[04-CDC与实时同步链路](./04-CDC与实时同步链路.md)（本篇第 4 节是其 §7 的展开）、[05-批处理与调度编排](./05-批处理与调度编排.md)（对账任务的调度与水位线）、[03-数仓分层与指标口径](./03-数仓分层与指标口径.md)（口径漂移检测的口径来源）、[07-LLM用量计量与成本分析](./07-LLM用量计量与成本分析.md)（计量数据质量哨兵）。

---

## 一、先结论：数据质量六维

| 维度 | 问题 | 本项目对应例子 |
|---|---|---|
| **完整性** | 该有的行/列缺失 | CDC 丢事件 → ES 少了某商品文档；`model_usage.request_id` 空 → 无法回溯 |
| **唯一性** | 一行事实存了多份 | 导入重放 → CH 明细重复计费；MQ 重复消费（若非幂等）→ ES 不受影响但统计翻倍 |
| **一致性** | 同一事实跨系统/跨表不符 | MySQL `price=188` 而 ES 仍 199（读模型滞后/丢更新）——本项目对账的核心对象 |
| **准确性** | 值"存在但错" | 口径漂移（退款率分母错）；`estimated_cost` 用兜底单价算出 | 
| **及时性** | 数据晚到超容忍 | 同步链路堆积，ES 滞后 1 小时；计量落库积压 |
| **有效性** | 值超出合法域 | `status` 出现未定义枚举、`total_tokens ≠ input+output`、负数费用 |

> 六维不是并列的清单，是**排障时的检查顺序**：先完整性（缺不缺）→ 唯一性（重不重）→ 一致性（对不对得上）→ 再深入准确性/及时性/有效性。

---

## 二、空值 / 重复 / 有效性：三类基础检测（可复制 SQL）

### 2.1 完整性：空值与缺失

```sql
-- 计量表的"可回溯性"体检：哪些用量行无法关联请求/用户（llm-observability-init.sql 建）
SELECT
    COUNT(*)                                              AS total_rows,
    SUM(request_id IS NULL)                               AS null_request_id,   -- 完整性
    SUM(user_id IS NULL)                                  AS null_user_id,
    SUM(model IS NULL OR model = '')                      AS null_model,
    SUM(request_id IS NULL) / COUNT(*)                    AS untraceable_ratio  -- >5% 需查 TraceContext 传播
FROM chat_db.model_usage
WHERE create_time >= DATE_SUB(NOW(), INTERVAL 7 DAY);

-- "行级缺失"要跨系统比：MySQL 有而下游没有（对账，见 §4）
```

### 2.2 唯一性：重复检测

```sql
-- 计量表语义上"一次 LLM 调用一条记录"（表注释见 llm-observability-init.sql:28），
-- 同 request_id 多行即计量重复（注意：request_id 幂等去重是 llm_trace 的设计，见 :11；
-- model_usage 的 request_id 无唯一约束，重复上报会成多行，正是本检测要抓的）
SELECT request_id, COUNT(*) AS cnt
FROM chat_db.model_usage
WHERE create_time >= DATE_SUB(NOW(), INTERVAL 7 DAY) AND request_id IS NOT NULL
GROUP BY request_id
HAVING cnt > 1;

-- 跨系统重复（导入了两次）：按"业务键 × 时间窗"分组比行数（CH 导入场景）
-- SELECT request_id, count() FROM ch.model_usage GROUP BY request_id HAVING count() > 1
```

### 2.3 有效性：域校验与逻辑一致性

```sql
-- 逻辑一致性：total_tokens 必须等于 input+output（落库兜底计算，:49 注释）
SELECT COUNT(*) FROM chat_db.model_usage
WHERE total_tokens <> input_tokens + output_tokens
  AND create_time >= DATE_SUB(NOW(), INTERVAL 7 DAY);

-- 域校验：scenario 枚举（:34 注释原文 7 个值）、费用非负
SELECT scenario, COUNT(*) FROM chat_db.model_usage GROUP BY scenario;
-- 出现 7 个合法值之外的即上游传参漂移
```

> **检测即任务**：这些 SQL 不该等人想起来手动跑——固化成定时检测任务（调度设计见 [05-批处理与调度编排](./05-批处理与调度编排.md)），异常计数进指标，超阈值告警。

---

## 三、口径漂移检测：最难也最值钱的一类

口径漂移的特点是**数据"看起来都对"，结论却错了**（[03-数仓分层与指标口径](./03-数仓分层与指标口径.md) §4.3 的三个事故），所以检测手段不是比数据，而是**比对"同一指标的多个独立实现"**：

| 检测手段 | 做法 | 命中的漂移 |
|---|---|---|
| **双口径并行** | 口径变更期新旧 SQL 并跑一个周期，输出差异表 | 静默的口径迁移事故 |
| **恒等式校验** | 毛利 ≤ 营收；退款率 ∈ [0,1]；分项和=总计 | 分母/过滤条件被改 |
| **环比突变告警** | 指标日环比 |Z|>30% 且无业务事件 → 先查口径再查数据 | 状态过滤漂移（GMV 突增 8% 事故） |
| **指标 SQL 版本化** | 指标字典记录 SQL 与变更历史，diff 可审计 | 无记录的口径改动 |

```sql
-- 恒等式校验示例：计量表的三条恒等式（每日检测任务）
-- ① total_tokens = input + output（§2.3）
-- ② SUM(estimated_cost) 与"按当日单价重算"的偏差应=0（单价表被静默修改即暴露）
SELECT DATE(create_time) AS d,
       SUM(estimated_cost)                                     AS billed,
       SUM(ROUND(input_tokens/1000000*p.in + output_tokens/1000000*p.out, 6)) AS recalced
FROM chat_db.model_usage m
JOIN (SELECT '2026-09-01' AS d, 1.0 AS in_price, 2.0 AS out_price) p
  ON DATE(m.create_time) = p.d
GROUP BY d
HAVING ABS(billed - recalced) > 0.01;   -- 偏差>1分/天 → 单价或计算逻辑漂移
```

> 本项目的正面案例：`ModelUsageRecorder.java:96-97` 把 pricingKey 与展示模型名分离、`:128-129` 未配置单价走兜底——这些"防漂移设计"比事后检测更便宜。**能设计掉的漂移，不要靠检测兜**。

---

## 四、跨存储对账：MySQL vs ES vs CH

### 4.1 为什么必然需要对账

本项目数据天然跨三套存储，每套的一致性承诺不同：

```
MySQL（业务事实源，强一致）
  ├─▶ ES product_catalog（Canal CDC 同步，最终一致）  ← 对账对象 1
  └─▶ CH chat_olap（目标态：批量/流式同步，T+分钟）    ← 对账对象 2 ⚠️ 工程未落地
```

丢失面盘点（[04-CDC与实时同步链路](./04-CDC与实时同步链路.md) §7.1）：MQ 重试耗尽进死信、消费端位点误重置、binlog 过期清理（`master.cnf` 配 `expire-logs-days=7`）期间 Canal 宕机超一周、ES/CH 存储自身异常。**任何异步链路都不能只靠"理论上会重试"**——这是 [04-中间件/11](../04-中间件/11-CanalCDC商品索引同步.md) 10.3 的原文结论。

### 4.2 对账三层次

| 层次 | 比什么 | 频率 | 成本 |
|---|---|---|---|
| **总量对账** | 源端 COUNT vs 目标端 _count/CH count | 每 5~10 分钟 | 极低，先跑 |
| **分片对账** | 按类目/主键段分组比对 | 小时级 | 低 |
| **明细对账** | 逐条比关键字段（price/status/deleted） | 每日 + 随机抽样 | 高，兜底 |

### 4.3 项目对账方案（引用 04-中间件/11 的设计并落到可执行 SQL）

```sql
-- ── 层次 1：总量对账（MySQL 侧，每 10 分钟）──
SELECT COUNT(*) FROM aics_product.product WHERE deleted = 0;

-- ── ES 侧（对应请求，与上面同一口径过滤 deleted）──
-- GET product_catalog/_count
-- { "query": { "term": { "deleted": "0" } } }
```

差异处置（[04-中间件/11](../04-中间件/11-CanalCDC商品索引同步.md) 10.3 的四步，原文要点）：

```
不一致时：
  ① 指标告警（差异量进 Prometheus，超阈值升级）
  ② 记录差异商品 id（差异登记表）
  ③ 触发单商品补偿索引（按 MySQL 当前值 upsert ES——消费端幂等所以安全）
  ④ 极端情况下全量重建索引（影子索引灌满 → 别名原子切换）
```

```sql
-- ── 层次 2：分片对账（按主键段，定位差异区域）──
SELECT FLOOR(id / 10000) AS seg, COUNT(*) AS cnt
FROM aics_product.product WHERE deleted = 0
GROUP BY seg;
-- ES 侧按同段 agg 对比 → 哪个段缺就重导哪段

-- ── 层次 3：差异明细登记表（供补偿任务消费）──
CREATE TABLE IF NOT EXISTS sync_reconcile_diff (
    id          BIGINT      NOT NULL AUTO_INCREMENT,
    source_name VARCHAR(32) NOT NULL COMMENT 'product_catalog / model_usage ...',
    biz_key     VARCHAR(64) NOT NULL COMMENT '差异主键（如商品id）',
    diff_type   VARCHAR(16) NOT NULL COMMENT 'MISSING/EXTRA/FIELD_MISMATCH',
    detail      VARCHAR(512) DEFAULT NULL,
    status      VARCHAR(16) NOT NULL DEFAULT 'PENDING' COMMENT 'PENDING/FIXED/IGNORED',
    create_time DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    UNIQUE KEY uk_source_key (source_name, biz_key),
    KEY idx_status (status)
) ENGINE=InnoDB COMMENT='跨存储对账差异登记表';
```

**对账对象 2（CH）**：目标态同步链路建好后，同构复用——MySQL `model_usage` 行数/费用总和 vs CH 明细 vs CH 预聚合（`sumMerge(cost_sum)`）三级互对；CH 预聚合与明细不一致说明物化视图逻辑漂移（[02-ClickHouse实战](./02-ClickHouse实战.md) §4 的 State/Merge 用错就会在此暴露）。⚠️ 该链路**工程未落地**，为设计预案。

### 4.4 与同步端的对账契约：两件事必须先成立

1. **同步端幂等（已成立）**：`ProductCdcConsumer.java:17-18` 注释与实现——ES `_id` 固定为 `product.id`，重复重放是覆盖 upsert、DELETE 反复执行被忽略。**对账补偿就是"受控的重复同步"，没有幂等就没有补偿**。
2. **比较口径统一**：MySQL `deleted=0` 的行集合 vs ES 文档集合，两端的"存在"定义必须一致（逻辑删除在 ES 是删文档，不是打标）；字段级对账还要统一类型（MySQL DECIMAL vs ES float 的精度截断要定容差）。

---

## 五、对账任务的增量设计

对账本身也是批处理任务，同样受 [05-批处理与调度编排](./05-批处理与调度编排.md) 四要素约束，但它有两个特有设计点：

### 5.1 增量对账：只对"最近变更过的"

全量对账天天跑成本高（亿行逐条比），增量思路：**只对账"水位窗口内变更过的键"**——

```
① 源端按 update_time 取窗口内变更键集合（水位线设计同 05 篇 §4）
② 目标端查同键集合（ES mget / CH in 查询）
③ 集合差 + 字段比对 → 差异登记表
④ 全量明细对账降频为每周一次兜底
```

**注意增量对账的盲区**：目标端"多出来的行"（EXTRA，源端没有的）不会被源端水位查出来——所以**总量对账仍须保留**，它专抓 EXTRA 与大面积丢失。

### 5.2 对账任务的自我质量

| 设计点 | 要求 |
|---|---|
| 幂等 | 差异登记表 `uk_source_key` 唯一键 upsert，重复对账不产生重复告警 |
| 容差 | 计数容差（在途窗口内的少量差异属正常，达阈值才告警）、金额容差（分位） |
| 自监控 | **对账任务自己挂了也要告警**（"没有对账结果"比"有差异"更危险——静默失明） |
| 修复闭环 | 差异 → 补偿 → 下轮对账确认 FIXED；长期 IGNORED 的差异要定期人工复核 |

```
对账闭环（每轮）：
  对账 → 发现差异 → 登记 diff（PENDING）
       → 补偿任务消费 PENDING → 按源端当前值 upsert 目标端 → 置 FIXED
       → 下一轮对账确认（不再出现）→ 若复现 → 升级人工（可能是双向写冲突）
```

---

## 六、面试高频问答

**Q1：数据质量六维是什么？实际排障按什么顺序用？**
A：完整性（缺不缺）、唯一性（重不重）、一致性（跨系统对不对得上）、准确性（值错没错）、及时性（晚没晚）、有效性（合不合法）。排障先查前三维——缺、重、不一致是链路问题的直接信号；口径类准确性问题最后查，因为它往往"数据都对、结论错了"。

**Q2：为什么有了同步链路的重试机制还要对账？**
A：重试只覆盖"已知的失败"，兜不住未建模丢失：MQ 重试耗尽进死信无人处理、位点被误重置、binlog 过期清理期间订阅端宕机、目标存储自身丢数据。对账以事实源为准绳周期性比对，是发现"未知的未知"的唯一手段；发现后靠幂等补偿修复。

**Q3：跨存储对账分哪三层？各抓什么问题？**
A：总量对账（COUNT 对 COUNT，低频秒查，抓大面积丢失/堆积）；分片对账（按主键段/类目分组比，定位差异区域）；明细对账（逐条比关键字段，抓字段级不一致，成本高所以降频+抽样）。本项目 MySQL→ES 商品同步即此三层设计，差异处置是"告警→登记差异 id→单条补偿→极端全量重建"。

**Q4：对账为什么依赖消费端幂等？**
A：对账的修复动作就是"按源端当前值重新同步目标端"，本质是受控的重复执行。本项目 ES 文档 id 固定为 product.id，重放是覆盖 upsert、重复删除被忽略——所以补偿安全。没有幂等的链路，对账发现差异后不敢动手修，只能人工核对。

**Q5：增量对账的盲区是什么？怎么补？**
A：增量按"源端最近变更的键"比，抓不到目标端多出来的行（EXTRA）——源端没有这个键就不会去查它。所以总量对账必须保留：计数层面的差异专抓 EXTRA 与大面积丢失；两者组合成"增量抓细节、总量抓整体"的互补结构。

**Q6：口径漂移怎么检测？举一个恒等式的例子。**
A：四手段：双口径并行跑差异表、恒等式校验、环比突变告警（先查口径再查数据）、指标 SQL 版本化审计。恒等式例子：计量表 total_tokens 必须=input+output；SUM(estimated_cost) 与按当日单价重算的偏差应≈0——单价表被静默修改立刻暴露。更好的做法是把漂移设计掉（本项目 pricingKey 与展示名分离、兜底单价显式化）。

**Q7：对账任务自己挂了怎么办？**
A：对账任务必须自监控——"没有对账结果"比"有差异"更危险，等于一致性失明。做法：对账任务的心跳/最近成功时间进指标，超期未跑即告警；差异登记表长期无新增也要核对是"真的没有差异"还是"对账没跑"。

**Q8：你们项目里哪个链路最需要对账？现状如何？**
A：MySQL→ES 的商品 CDC 链路：事实源是 MySQL product 表，读模型是 ES product_catalog，中间隔 Canal+RocketMQ 两层异步，binlog 还有 7 天过期清理。现状：消费端幂等 upsert 已落地（ProductCdcConsumer），对账方案已在中间件文档设计为"三板斧"之一（告警/记差异 id/单条补偿/全量重建），但对账任务与 Grafana 告警本身待 Canal 编排落地后实施——已在技术缺口清单立项。

---

## 七、动手练习

1. 对 `chat_db.model_usage`（先执行 `deploy/mysql/llm-observability-init.sql`）跑 §2 全部三类检测 SQL，把结果整理成一张"数据质量体检报告"（维度/检测项/结果/是否达标）。
2. 复现一致性问题：手工向 MySQL 插一条新商品而 CDC 未启用（`CANAL_PRODUCT_CDC_ENABLED=false`，见 [04-中间件/11](../04-中间件/11-CanalCDC商品索引同步.md) 第八节），执行 §4.3 总量对账 SQL 与 ES `_count`，确认差异被捕获；再启用同步并触发补偿，走完"差异→FIXED"闭环。
3. 把 §2.1/§2.3 的检测固化成一个 XXL-Job handler（`dataQualityDailyCheck`），异常计数用日志+返回值报告（调度配置参照 [05-批处理与调度编排](./05-批处理与调度编排.md)、[07-运维部署/05-XXL-Job分布式调度](../07-运维部署/05-XXL-Job分布式调度.md) 控制台步骤）。
4. 实现 `sync_reconcile_diff` 的补偿消费者：从 PENDING 差异取商品 id，按 MySQL 当前值调用与 `ProductCdcConsumer.syncOne` 相同的 upsert 逻辑，处理完置 FIXED；写单测覆盖 MISSING/EXTRA/FIELD_MISMATCH 三类。
5. ⚠️ 目标态推演：为"MySQL vs CH model_usage"设计三级对账（明细行数、费用总和、预聚合 sumMerge 对比），写出每级 SQL 与预期差异来源（在途窗口/物化视图漂移），并标注哪些部分依赖 02 篇的 CH 目标态先落地。

---

> 上一篇：[07-LLM用量计量与成本分析](./07-LLM用量计量与成本分析.md)
