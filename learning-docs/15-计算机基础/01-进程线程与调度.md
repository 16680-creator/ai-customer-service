# 进程、线程与调度

> 对应项目：`ai-cs-chat/src/main/java/com/aics/chat/controller/ChatController.java`（SSE 端点返回 `SseEmitter`）、
> `ai-cs-chat/src/main/java/com/aics/chat/service/impl/ChatServiceImpl.java`（流式桥接与阻塞等待）、
> `ai-cs-py-chat/app/api/chat.py`（FastAPI 协程版流式，对照用）。
> 相关：[07-IO模型与Netty基础](../01-Java基础/07-IO模型与Netty基础.md)（I/O 模型与 Netty 层，本篇不重复）、
> [04-并发编程进阶](../01-Java基础/04-并发编程进阶.md)（JMM 与线程工具，本篇不重复）、
> [05-SSE与WebSocket实时通信](../04-中间件/05-SSE与WebSocket实时通信.md)（SSE 应用层用法）。

---

## 一、先看项目现象：一次 SSE 对话，线程都去哪了

项目的事实链路（全部可在源码中定位）：

```
POST /api/chat/stream/sse
  → 网关 8080（Netty EventLoop，不占线程等下游）
  → ai-cs-chat 8083（Tomcat + Servlet）
       ChatController.chatStreamSse()           ChatController.java:168
       ChatServiceImpl.chatStreamSse()
         ① 缓存查询 / RAG 检索 / 组装 prompt     ChatServiceImpl.java:543-598（同步段）
         ② futureFlux.get()                     ChatServiceImpl.java:601（阻塞等待 Flux 就绪）
         ③ flux.subscribe(...)                  ChatServiceImpl.java:646（回调里 emitter.send）
       return SseEmitter                        ← Spring MVC 进入异步模式
```

**问题**：Tomcat 默认 200 个工作线程（项目未显式配置 `server.tomcat.threads.max`），LLM 首 token 延迟 2~10 秒。这 200 个线程够不够、谁在什么时候被占着、能不能再加——要回答这些，必须回到 OS 的进程/线程/调度模型。

---

## 二、进程、线程、协程：三个并发模型

| 维度 | 进程 | 线程 | 协程 |
|---|---|---|---|
| 本质 | 资源分配的最小单位 | **CPU 调度的最小单位** | 用户态调度的执行流 |
| 切换者 | 内核 | 内核 | **用户态运行时**（不进内核） |
| 切换成本 | 高（换地址空间 + TLB 刷新） | 中（1~10 μs，见 §三） | 低（几十 ns，保存少量寄存器） |
| 地址空间 | 各自独立 | 同进程内共享 | 同线程栈内共享 |
| 崩溃影响 | 隔离，互不影响 | 一个线程崩溃整个进程退出 | 栈内异常被捕获，但阻塞系统调用会卡住整个事件循环 |
| 栈大小 | — | 固定（HotSpot 默认 `-Xss1m`） | 动态增长（KB 级起步） |
| 数量级 | 几十~几百 | 几百~几千 | **几十万~百万** |
| 项目实例 | 11 个 Java 微服务各一个进程 | Tomcat 工作线程、`CompletableFuture.supplyAsync` 的 ForkJoinPool 线程 | `ai-cs-py-chat` 的 `async def`（uvicorn 事件循环） |

### 2.1 三种模型在项目里同时存在

```
前端 → 网关 ai-cs-gateway（WebFlux + Netty：少量 EventLoop 线程，事件驱动）
     → ai-cs-chat（Servlet + Tomcat：每请求占一个工作线程直到进入异步）
     → ai-cs-py-chat（FastAPI + asyncio：async def 协程，单线程事件循环多路复用）
```

- `ai-cs-py-chat/app/api/chat.py:27` `async def chat(...)`：等待 LLM 响应时协程挂起（`await`），事件循环转去处理其他请求——**一个线程服务几百个并发对话**，因为它本质是把"等 I/O"变成了事件回调。
- Java 侧等价物是虚拟线程（JDK 21 Loom），但项目运行镜像是 `eclipse-temurin:17-jre-jammy`（`ai-cs-chat/Dockerfile:17`），**JDK 17 用不了虚拟线程**——这是项目线程模型的现实约束。

### 2.2 为什么浏览器端 `await` 不能照搬到 Servlet 栈

协程/异步的前提是"等待时不占 OS 线程"。Servlet 栈里代码一旦调用阻塞方法（JDBC、`future.get()`），线程就挂住了。这正是 [07-IO模型与Netty基础](../01-Java基础/07-IO模型与Netty基础.md) §5.2 那张"网关 WebFlux vs 业务服务 Servlet"对照表的根源——**I/O 模型决定并发容量的天花板**。

### 2.3 协程的代价与适用边界（以 ai-cs-py-chat 为例）

协程不是免费的午餐，它的隐含约定是"**整条链路都必须异步**"：

| 约束 | 具体含义 | 项目对应 |
|---|---|---|
| 不能有阻塞调用 | 一个 `time.sleep` / 同步 DB 驱动会**冻结整个事件循环**，所有协程跟着卡 | `ai-cs-py-chat/app/core/llm.py:5,17` 用 `AsyncOpenAI` 异步客户端，`:32` `await` 调用 |
| CPU 密集任务要外移 | 协程适合等 I/O，算数要丢线程池/进程池 | `run_in_executor` 模式 |
| 调试心智不同 | 协程切换点在 `await`，bug 复现依赖调度时序 | 单测里要控制事件循环时序 |

Java 生态的对应演进是 JDK 21 虚拟线程（Loom）：同样是"百万级执行流"，但由 JVM 编排、阻塞调用自动挂起载体线程，**允许你继续写同步风格代码**——Servlet 栈迁移成本远小于改造成 WebFlux。项目卡在 JDK 17，这是选型时必须如实评估的约束。

---

## 三、上下文切换到底贵在哪（数值级）

上下文切换 = 内核保存当前任务状态、恢复下一个任务状态。直觉上很快，实际账单有两部分：

| 成本项 | 量级 | 说明 |
|---|---|---|
| **直接成本** | 1~10 μs | 保存/恢复寄存器、切换内核栈、更新调度队列；进程间切换还要换页表基址（CR3），**TLB 全部失效** |
| **间接成本（缓存污染）** | 可达数十 μs | 换回来的任务冷启动，L1/L2 缓存里的热数据全是别人的；这部分不可见但常是大头 |

**换算成直觉**：一次切换 ≈ 几千条指令的执行时间。若一个服务每秒切换 10 万次，光直接成本就可能吃掉 0.1~1 核，还没算缓存污染。

### 3.1 自愿切换 vs 非自愿切换

| 类型 | 触发 | 项目例子 |
|---|---|---|
| **自愿** | 任务主动让出：等锁、等 I/O、sleep | Tomcat 线程阻塞在 `futureFlux.get()` 等 LLM；工作线程等数据库连接池 |
| **非自愿** | 时间片用尽或被更高优先级抢占 | 并发压测时 200 个 Tomcat 线程 + GC 线程 + Netty 客户端线程抢 CPU |

```bash
# 看每秒上下文切换（cswch=自愿 / nvcswch=非自愿）
pidstat -w -p $(pidof java) 1
vmstat 1    # cs 列 = 全系统每秒切换次数，万级正常，十万级要查

# 定位最忙线程（结合 jstack 把 tid 十六进制转成线程名）
top -H -p $(pidof java)          # 线程级 CPU
printf "%x\n" <tid>              # tid 转 16 进制，去 jstack 输出里搜 nid=0x...
```

---

## 四、调度器概览：CFS 的思路

Linux 对线程（内核里都是 `task_struct`，线程即"共享资源的进程"）的普通调度用 **CFS（完全公平调度器）**：

- 每个任务维护 **vruntime**（虚拟运行时间）：跑得越多欠得越多；
- 调度器永远挑 **vruntime 最小**的任务上 CPU（红黑树最左节点，O(log n)）；
- `nice` 值影响 vruntime 增长速度：nice 越高（优先级越低）长得越快，分到的 CPU 越少。

> 内核 6.6 起 CFS 被 EEVDF 取代，但"按虚拟时间挑最欠调度的任务"思想一致，面试说 CFS 思想即可。

**动手验证调度优先级**（宿主机或容器内）：

```bash
nice -n 10 stress-ng --cpu 1 --timeout 5s &   # 低优先级占一个核
taskset -c 0-3 stress-ng --cpu 4               # 限死前 4 核跑（模拟 cpuset 亲和）
chrt -f 1 sleep 1 2>/dev/null || echo "实时调度需特权"   # FIFO 实时策略（容器默认没有）
```

**容器场景的关联**：K8s 里 `cpu: "1000m"`（`deploy/k8s/services/ai-chat-service.yaml:85`）本质是 CFS 带宽控制（cgroup quota）：每 100ms 周期最多用 100ms CPU 时间——超了就被节流（throttled），表现为"没跑满却抖动"，这是 [06-容器隔离原理](./06-容器隔离原理-namespace与cgroup.md) 的内容。

---

## 五、线程数与 CPU 核数：公式、误区与"线程不是越多越好"

### 5.1 两类负载，两种算法

| 负载类型 | 经验公式 | 例子 |
|---|---|---|
| CPU 密集 | `N` 或 `N+1`（N=核数） | 压缩、加密、序列化；`ForkJoinPool.commonPool` 默认并行度 = 核数-1 |
| I/O 密集 | `N × (1 + 等待时间/计算时间)` | Tomcat 工作线程、DB 访问 |

**I/O 密集公式对项目意味着什么**：一次对话里，线程的"计算时间"（JSON 序列化、prompt 拼接）可能只有几毫秒，"等待时间"（LLM 首 token 2~10s、DB 1ms、Redis 1ms）是几百到几千倍。代入公式：8 核机器理论上可以配几百个线程——但公式**没算**下面三个约束，所以"越多越好"是错的。

### 5.2 为什么 Web 容器线程不是越多越好

| 约束 | 机制 | 数值级 |
|---|---|---|
| **内存** | 每线程预分配栈（HotSpot 默认 1MB） | 2000 线程 ≈ 2GB 栈，直接挤爆 512MB 堆外的容器配额 |
| **调度** | 线程越多，非自愿切换越频繁，缓存污染越重 | 活跃线程数 ≫ 核数时，切换成本线性上涨 |
| **锁竞争** | 线程越多，同一把锁上的排队/自旋越凶，有效并行度反而下降 | TokenBucketRateLimiter 的 per-bucket 锁（见 [08](./08-CPU缓存与内存序.md)） |
| **下游容量** | 线程是并发的"放大器"：下游 DB/LLM 扛不住时，加线程只是更快打挂下游 | MySQL `max_connections=500`（`master.cnf:24`）才是硬顶 |

**结论**：线程数的天花板 = min(内存预算 / 栈大小, 调度与锁的可承受点, 下游容量)。Tomcat 默认 200 是个"大多数场景不闯祸"的保守值；本项目 LLM 等待长，问题不在"线程不够"而在"线程被谁占着"——回到 §六算账。

---

## 六、项目现场：SSE 长连接的线程账本

逐阶段算清"一个并发 SSE 对话占用哪些线程"（行号均可对照源码验证）：

| 阶段 | 占用的线程 | 最长占用 | 证据 |
|---|---|---|---|
| 请求进入 → 参数校验 | Tomcat 工作线程 | 毫秒级 | `ChatController.java:168-180` |
| 缓存查询 / RAG 检索 / 拼 prompt（同步段） | Tomcat 工作线程 | 几百 ms（含向量检索） | `ChatServiceImpl.java:543-598` |
| `futureFlux.get()` 等待 Flux 就绪 | **Tomcat 工作线程阻塞** | **≤60s**（TimeLimiter） | `ChatServiceImpl.java:601`；`ai-cs-chat/application.yml:74-77` |
| `CompletableFuture.supplyAsync` 内部（路由/建流） | ForkJoinPool.commonPool 线程 | 流建立期间 | `ResilientAiService.java:255` |
| 流式推送期（emitter 已返回） | Tomcat 线程已释放（Servlet 异步）；onNext 回调跑在 Reactor/HTTP 客户端线程 | 流生命周长 ≤5min | `ChatServiceImpl.java:156`（`SSE_EMITTER_TIMEOUT = 5 * 60 * 1000L`）、`:646` `flux.subscribe` |

**三个关键结论**：

1. **`SseEmitter` 的设计目的就是"返回后释放请求线程"**——返回 `SseEmitter` 时 Spring MVC 启动 Servlet 异步模式，Tomcat 线程回池。所以"流式推送 5 分钟占 200 线程"的说法不对；**真正占线程的是返回 emitter 之前的同步段，尤其 `futureFlux.get()` 最长可阻塞 60s**。200 个线程在"LLM 建流风暴"下确实可能耗尽，这才是容量瓶颈的精确表述。
2. **`CompletableFuture.supplyAsync`（`ResilientAiService.java:255`）没传自定义执行器**，跑在 `ForkJoinPool.commonPool`（并行度 = 核数-1）上。它被设计给 CPU 密集任务，塞进"等 LLM 网络 I/O"会挤压全 JVM 的并行流等公共资源——这是项目可以改进的点。
3. **线程复用要求 ThreadLocal 清理**：`ChatController.java:88` 注释明说"必须清理：Tomcat 线程被复用，否则下个请求会读到上个用户的 ID"，对应 `:84-90` 的 `try/finally { ChatUserContext.clear(); }`——线程池 = 线程被成百上千次复用，任何不清理的 ThreadLocal 都是串号事故。

### 6.1 线程账本（一张图看懂 20 个并发 SSE）

```
时间轴 ──────────────────────────────────────────────────────────────▶
Tomcat 线程池(200)   [T1: 建流1 get()阻塞≤60s][T1 空闲→服务建流2...][...]
                          ▲ 每个对话建流期占 1 个线程（几十 ms~60s）
ForkJoinPool(common)  [F1: 路由+建流1]      [F1 空闲→对话2...]（并行度=核数-1）
Reactor/HTTP 客户端   [R1: 对话1 token 流推送 ≤5min]（少量线程轮转推送）
                      [R2: 对话2 token 流推送 .................]
```

推论：**同时在线的 SSE 流可以有几百条（Reactor 线程轮转推送），但"同时在建"的对话最多吃掉 200 个 Tomcat 线程**——容量规划要按"建流风暴速率 × 建流时长"估，而不是按"在线会话数 × 5 分钟"估（后者会把容量悲观 100 倍）。

### 6.2 对照：同样的业务，三种线程模型

| 方案 | 等 LLM 时占线程吗 | 容量模型 | 项目现状 |
|---|---|---|---|
| Servlet + `SseEmitter`（现状） | 建流前的同步段占；流式推送期不占 | 200 线程 × (同步段时长) 决定并发上限 | ✅ `ChatController.java:168` |
| Servlet + 全异步编排（DeferredResult/CompletableFuture 链） | 建流也不占（同步段异步化） | 瓶颈转移到 ForkJoinPool/自定义池 | 部分：`futureFlux` 已是 Future，但 `.get()` 把异步又拉回了同步 |
| WebFlux 返回 `Flux` 直接渲染 SSE | 全程不占（事件驱动） | 少量 EventLoop 服务海量连接 | ❌ chat 是 Servlet 栈；网关已是 WebFlux（`RouteConfig` 用 `Mono`/`Flux`） |
| FastAPI `async def` + `StreamingResponse` | 不占（协程挂起） | 事件循环 + 线程池兜底阻塞调用 | ✅ `ai-cs-py-chat/app/api/chat.py`（Python 对照组） |
| JDK 21 虚拟线程 | 占的是虚拟线程（便宜百万级） | 线程数不再稀缺 | ❌ JDK 17 镜像（`ai-cs-chat/Dockerfile:17`），升级 JDK 才可用 |

### 6.3 常见误区速查

| 误区 | 真相 |
|---|---|
| "SSE 长连接 5 分钟，200 线程全被占满" | 占线程的是建流同步段；emitter 返回后请求线程已释放，流式期是 Reactor 线程在推 |
| "协程一定比线程快" | 协程省的是"等 I/O 时的线程占用"，不是计算速度；CPU 密集场景两者一样，协程还多一层调度心智 |
| "线程池调大就能扛更多并发" | 线程是放大器不是容量：下游（LLM/DB）容量、锁竞争、内存栈才是硬约束 |
| "ThreadLocal 只要不 set 就安全" | 线程复用 + 漏 clear = 上个请求的脏数据串号；跨线程池还要面对"不传播"问题（`ResilientAiService.java:253-256` 的快照/恢复就是解法） |
| "多线程程序 CPU 100% 是正常满载" | 可能是锁自旋/上下文切换风暴；先看 `vmstat` 的 cs 与 `top -H` 的线程分布再下结论 |
| "I/O 密集公式算出多少就配多少" | 公式给的是起点：还要受内存栈、锁竞争、下游容量三重封顶（§5.2），且要靠压测校准 |
| "网关是 Netty 所以业务服务也应该上 WebFlux" | WebFlux 的收益以"全链无阻塞"为代价；业务服务有大量 JDBC/阻塞调用时，收益为负——先治理阻塞点再谈模型 |
| "进程数越多吞吐越高" | 每个进程独立地址空间，内存与切换成本随进程数线性涨；微服务拆分要按"独立部署/故障隔离"的需求拆，不按"多开进程提吞吐"拆 |

---

## 七、面试高频问答

**Q1：进程和线程的区别？为什么线程切换比进程快？**
A：进程是资源分配单位（独立地址空间、fd 表），线程是调度单位，同进程线程共享地址空间。进程切换要换页表基址导致 TLB 失效、缓存全冷；线程切换只换寄存器与栈指针，且缓存大体还有效，所以快一个量级。

**Q2：上下文切换的成本是多少？由哪几部分组成？**
A：直接成本 1~10μs（保存恢复寄存器、内核栈、调度队列）；间接成本是缓存污染——任务换回来时 L1/L2 全是别人的热数据，冷启动可再花几十微秒。用 `pidstat -w` 区分自愿（等 I/O/锁）与非自愿（时间片用尽）切换，非自愿占比高说明 CPU 是瓶颈。

**Q3：CPU 密集和 I/O 密集的线程数怎么定？**
A：CPU 密集取核数或核数+1；I/O 密集用 `N × (1 + 等待/计算)` 起步，再受三重约束封顶：内存（每线程 1MB 栈）、调度与锁竞争、下游容量。本项目 Tomcat 默认 200 线程，瓶颈不是线程数而是线程被 `futureFlux.get()` 等阻塞调用占用。

**Q4：为什么 Web 容器线程池不是越大越好？**
A：①栈内存线性增长；②活跃线程远超核数后非自愿切换与缓存污染上升；③锁竞争加剧、有效并行度反降；④线程是下游容量的放大器，DB/LLM 扛不住时加线程只是更快打挂下游。线程池是"并发需求"的解，不是"吞吐量"的解。

**Q5：CFS 调度器怎么保证公平？**
A：每个任务维护 vruntime（虚拟运行时间），调度器用红黑树 O(log n) 挑 vruntime 最小的任务上 CPU；nice 值改变 vruntime 增速实现权重。CPU 时间用得少的任务"更欠调度"，所以长期看每个任务按权重公平分到时间。

**Q6：SseEmitter 返回后 Tomcat 线程还在吗？整个 SSE 生命周期里线程怎么占？**
A：不在。返回 SseEmitter 即启动 Servlet 异步模式，请求线程回池。但返回前的同步段占线程：本项目 `ChatServiceImpl.java:601` 的 `futureFlux.get()` 在 TimeLimiter 60s 内阻塞 Tomcat 线程；流式推送期的 `emitter.send` 跑在 Reactor 回调线程上。所以容量瓶颈在"建流风暴"，不是"长连接占线程"。

**Q7：`CompletableFuture.supplyAsync` 默认用什么线程池？有什么坑？**
A：未传执行器时用 `ForkJoinPool.commonPool()`，并行度为核数-1，它是全 JVM 共享的（parallelStream 也用它）。把长 I/O 等待塞进去会占满公共池、拖累所有并行流任务；生产上应传自定义执行器隔离。本项目 `ResilientAiService.java:255` 就是这个写法，是已知改进点。

**Q8：线程池里 ThreadLocal 忘了清理会怎样？项目怎么处理的？**
A：线程被复用，下个请求会读到上个请求的脏数据（如用户身份串号）。项目在 `ChatController.java:88-90` 用 try/finally 保证 `ChatUserContext.clear()`，并注释了原因；跨异步线程时 ThreadLocal 还不传播，`ResilientAiService.java:253-254` 在进 `supplyAsync` 前先捕获快照、进去后恢复。

**Q9：协程为什么快？Java 什么时候能有？**
A：协程切换在用户态完成，只保存少量寄存器，几十纳秒级，不进内核、不刷新缓存；代价是遇到阻塞系统调用会卡住整个事件循环（所以 Python 侧所有 I/O 都要 await）。Java 侧对应 JDK 21 虚拟线程，项目镜像是 JDK 17 用不了；当前 Servlet 栈的现实解是 SseEmitter/全异步编排。

**Q10：为什么"同时在线连接数"可以比"线程数"大两个数量级？**
A：因为"占线程"的只是 CPU 执行片段与阻塞调用。SSE 场景下 emitter 返回后请求线程已释放，流式推送由少量 Reactor 线程轮转完成（事件驱动）；真正决定线程需求的是"同时处于阻塞中的操作数"。所以 200 线程支撑几百条在线流是正常形态——前提是阻塞段（如 `futureFlux.get()`）被控制在短时间或异步化。

---

## 八、动手练习

1. 用 `pidstat -w -p <chat进程pid> 1` 观察：空闲时与并发 SSE 压测（`scripts/loadtest/k6/sse-chat.js`，20 VU）时，自愿/非自愿切换各是多少？解释差异来源。
2. 给 `ai-cs-chat` 显式配置 `server.tomcat.threads.max: 50` 并压测对比：50 与 200（默认）下首 token p95 与错误率的变化，回答"线程加到 200 有没有意义"。
3. 读 `ChatServiceImpl.chatStreamSse()`（`:520-646`），画出"Tomcat 线程/ForkJoinPool 线程/Reactor 线程"三条泳道，标注 `futureFlux.get()` 与 `emitter.send` 各在谁身上执行。
4. 改造练习：把 `futureFlux.get()`（`:601`）改为"先 return emitter，异步回调里再订阅"，思考：SSE 超时（`:156` 的 5 分钟）与错误事件的推送路径要怎么调整？
5. 在 `ai-cs-py-chat` 上压同一脚本，对比 Java（Tomcat）与 Python（asyncio）在 20/50/100 VU 下的内存与线程数（`docker stats` + `ps -o nlwp`），用数据回答"协程模型的容量优势在哪、代价是什么"。

---

> 下一篇：[02-内存管理与虚拟内存](./02-内存管理与虚拟内存.md)
