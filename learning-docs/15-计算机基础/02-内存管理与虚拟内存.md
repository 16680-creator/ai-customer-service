# 内存管理与虚拟内存

> 对应项目：`deploy/docker-compose/docker-compose-all.yml`（各服务 JVM 堆配置）、
> `deploy/k8s/services/ai-chat-service.yaml`（cgroup 级 resources limits）、
> `deploy/mysql/mysql.cnf`、`deploy/redis/redis.conf`（中间件内存预算）。
> 相关：[03-JVM内存模型与GC实战](../01-Java基础/03-JVM内存模型与GC实战.md)（堆内分区与 GC，本篇不重复）、
> [02-Kubernetes入门](../07-运维部署/02-Kubernetes入门.md)（K8s 用法层）、
> [06-容器隔离原理-namespace与cgroup](./06-容器隔离原理-namespace与cgroup.md)（limit 的底层机制）。

---

## 一、结论先行：JVM 进程的内存 ≠ 堆

排查容器内存问题时最常见的误判是"Xmx 512MB，为什么进程占了 800MB"。因为 JVM 进程至少还有：

| 组成 | 说明 | 量级（本项目 512m 堆的服务） |
|---|---|---|
| 堆（Heap） | `-Xmx` 控制的对象世界 | 512 MB（显式配置） |
| 元空间（Metaspace） | 类元数据 | 几十~100+ MB（微服务类很多） |
| 线程栈 | 每线程预留 `-Xss`（默认 1MB） | 200 Tomcat 线程 ≈ 200 MB |
| Direct Buffer / NIO | Netty 客户端（Redis/ES/MQ）用堆外内存 | 几十 MB 起步，无 Xmx 约束 |
| GC/JIT/符号表 | GC 自身数据结构、CodeCache | 几十 MB |
| **合计** | | **远大于 512MB，通常 800MB~1GB** |

这就是为什么"显式 `-Xmx` 只圈了堆"，容器 limit 必须给堆外留余量——本项目 K8s 配置正是按这个账本设计的（§5.2）。

---

## 二、虚拟内存与分页

### 2.1 虚拟地址空间

每个进程看到的是独立的**虚拟地址空间**，CPU 的 MMU 通过**页表**把虚拟页翻译成物理页帧：

```
malloc(1GB) 成功 ≠ 用了 1GB 物理内存
  → 只是登记了 1GB 虚拟地址范围，物理页按需分配（惰性）
  → 第一次访问某页触发 page fault，内核才真正分配物理页
```

### 2.2 页与 TLB

| 概念 | 说明 |
|---|---|
| 页（Page） | x86-64 默认 **4KB**；虚拟内存与物理内存的最小分配单位 |
| 页表 | 虚拟页号 → 物理页帧号的映射表，多级树结构（x86-64 四级） |
| **TLB** | 页表的硬件缓存，命中则翻译 ~1 个时钟周期；**进程切换换页表基址（CR3）后 TLB 失效**——进程切换贵的一个硬件根源（见 [01-进程线程与调度](./01-进程线程与调度.md) §三） |
| Huge Page | 2MB/1GB 大页减少页表级数与 TLB miss；ES/大堆 JVM 可用 `-XX:+UseLargePages`（项目未启用） |

### 2.3 Page Fault 的两种

| 类型 | 含义 | 代价 | 项目例子 |
|---|---|---|---|
| **Minor（软）** | 页已在页缓存（Page Cache），只缺页表映射 | 微秒级 | 读一个刚被别的进程读过的 jar（`app.jar` 常驻页缓存后，启动变快） |
| **Major（硬）** | 数据在磁盘，要从盘读入 | 毫秒级 | 内存不足时进程被反复换出/换入，`top` 看 `%wa` 飙升 |

### 2.4 RSS vs VSS（排查容器内存必备）

| 指标 | 含义 | 排查口径 |
|---|---|---|
| **VSS/VSZ** | 进程申请的**虚拟**地址空间总量 | 含未实际使用的登记量，普遍虚高，看它判断"快 OOM"会误判 |
| **RSS** | 实际占用的**物理**内存（含与其他进程共享的页，如 libc 的代码页） | 容器 OOM 看它；`docker stats` ≈ cgroup 口径的 RSS |

```bash
ps -o pid,rss,vsz,cmd -p $(pidof java)   # RSS 单位 KB，对比 -Xmx 估算堆外占比
docker stats --no-stream                  # 容器视角（cgroup 计量）
```

---

## 三、swap 与 OOM

### 3.1 swap：把不活跃的匿名页换出到磁盘

- 目的：物理内存不足时，把冷页换到 swap 分区/文件，腾出内存；
- 代价：换入换出都是磁盘 I/O（毫秒级），**JVM 被 swap 后 GC 停顿会变成秒级**——延迟敏感服务通常关 swap；
- 容器实践：K8s 节点通常禁 swap（kubelet 历史 hard requirement），swap 不是容器 OOM 的缓冲垫。

### 3.2 OOM 的两条路径：Java OOM ≠ 容器 OOMKilled

| | Java OOM（`OutOfMemoryError`） | 容器 OOMKilled |
|---|---|---|
| 触发者 | JVM 自身（堆/元空间/直接内存超各自上限） | **内核 OOM Killer**（cgroup 用量超 memory limit） |
| 表现 | Java 异常栈，进程还在 | **exit 137**（128+SIGKILL），无任何 Java 日志 |
| 项目文档 | [03-JVM内存模型与GC实战](../01-Java基础/03-JVM内存模型与GC实战.md) §OOMKilled 问答 | 同文档 Q9、[02-Kubernetes入门](../07-运维部署/02-Kubernetes入门.md) 状态表 |

内核 OOM Killer 按 `oom_score`（大致正比于 RSS × 权重）挑牺牲者。**堆外内存不受 `-Xmx` 控制**，堆配小了、堆外涨了，先死的是整个容器而不是抛 Java 异常——这就是 §四要说清的账本。

### 3.3 Java OOM 家族速查（容器排查时先分类）

| OutOfMemoryError | 实际超掉的区域 | 与容器 limit 的关系 |
|---|---|---|
| `Java heap space` | 堆（`-Xmx` 圈定） | 堆内自愈不了才抛，与 limit 无直接关系 |
| `Metaspace` | 类元数据 | 无 `-XX:MaxMetaspaceSize` 时无上限，是堆外杀手 |
| `Direct buffer memory` | NIO DirectBuffer | 无 MaxDirectMemorySize 约束时失控 → RSS 涨 → OOMKilled |
| `unable to create native thread` | 线程数/虚拟内存耗尽 | 容器 pids 限制或栈内存挤爆 |
| `GC overhead limit exceeded` | 堆（98% 时间在 GC 只回收 2%） | 通常是堆太小的慢性表现 |

> 容器里最常见的组合是最后一种现象链：堆配得刚好 → 堆外（元空间/线程栈/DirectBuffer）涨 → RSS 越过 cgroup limit → 还没来得及抛 OOM 就被内核 SIGKILL。所以排查顺序永远是：先看 exit code，再分类 Java OOM。

```bash
dmesg -T | grep -i "killed process"    # 内核留下的击杀现场（进程名 + RSS）
docker inspect aics-chat-service --format '{{.State.OOMKilled}} {{.State.ExitCode}}'
```

---

## 四、容器 cgroup limit 下 JVM 的行为

### 4.1 三个参数

| 参数 | 含义 | 默认 |
|---|---|---|
| `-XX:+UseContainerSupport` | JVM 读 cgroup 的 CPU/内存限额而非宿主机值（JDK 8u191+/10+ 默认**开**，项目 JDK 17 默认生效） | true |
| `-XX:MaxRAMPercentage` | 未显式给 `-Xmx` 时，堆上限 = 可用内存 × 该百分比 | **25%** |
| `-XX:+UseCGroupMemoryLimitForHeap` | JDK 10 前的旧开关，已被上一条取代 | — |

### 4.2 项目现状：为什么必须显式 `-Xmx`

**部署形态一：docker-compose（`deploy/docker-compose/docker-compose-all.yml`）**

```yaml
# docker-compose-all.yml:236（ai-chat-service，其余服务同款）
JAVA_OPTS: "-Xms256m -Xmx512m"
```

- **关键现状：compose 全文件没有任何 `mem_limit`/`deploy.resources`（已 grep 确认）**——容器本身不受 cgroup 内存限制，只有 JVM 堆被 `-Xmx512m` 圈住。堆外（元空间、线程栈、Netty DirectBuffer）没有上限兜底；
- 若忘了配 `JAVA_OPTS`：JVM 通过容器感知看到的"机器内存"= 宿主机内存（没有 limit 可读），默认堆 = 宿主内存 × 25%——一台 32GB 的机器上单个微服务就会默认申请 8GB 堆，叠加十几个服务必然 OOM。**这就是项目显式传 `JAVA_OPTS` 的根本原因**（Dockerfile 入口 `ENTRYPOINT ["sh","-c","java ${JAVA_OPTS:-} -jar app.jar"]`，`ai-cs-chat/Dockerfile:21`）。

**部署形态二：K8s（`deploy/k8s/services/ai-chat-service.yaml`）**

```yaml
# :78-86
- name: JAVA_OPTS
  value: "-Xms512m -Xmx512m"
resources:
  requests: { cpu: "200m", memory: "512Mi" }
  limits:   { cpu: "1000m", memory: "1Gi" }
```

账本核对：堆 512MB + 元空间 ~100MB + 线程栈 ~200MB + Netty 堆外 ~100MB ≈ **900MB < 1Gi limit**，留了约 12% 余量——配置是自洽的。若把 limit 压到 512Mi，堆外一涨就 OOMKilled（exit 137）。

### 4.3 两套部署的内存语义差异（一表看懂）

| | compose（现状） | K8s（现状） |
|---|---|---|
| cgroup memory limit | 无（不限） | 1Gi（`ai-chat-service.yaml:86`） |
| 堆 | 512MB（`JAVA_OPTS`） | 512MB（`JAVA_OPTS`） |
| OOM 方式 | 只可能宿主机 OOM Killer / 堆外耗尽抛异常 | cgroup 超限 → OOMKilled exit 137 |
| JVM 容器感知 | 读不到 limit（无），靠显式 -Xmx 兜底 | 有 limit 但已显式 -Xmx，`MaxRAMPercentage` 未参与 |

---

## 五、项目现场：中间件内存预算盘点

### 5.1 全家桶配置一览（全部真实可查）

| 组件 | 配置 | 出处 | 说明 |
|---|---|---|---|
| Nacos | `JVM_XMS: 256m` / `JVM_XMX: 512m` | `docker-compose-all.yml:67-68` | 注册/配置中心，单机 standalone |
| Elasticsearch | `ES_JAVA_OPTS: "-Xms512m -Xmx512m"` | `docker-compose-all.yml:91` | 堆内外各半的典型姿势：Lucene 用堆外 mmap，堆只给查询/聚合 |
| RocketMQ NameServer | `JAVA_OPT_EXT: "-Xms256m -Xmx256m"` | `docker-compose-all.yml:114` | 轻量路由组件 |
| RocketMQ Broker | `JAVA_OPT_EXT: "-Xms512m -Xmx512m"` | `docker-compose-all.yml:135` | 消息存储主进程 |
| 9 个 Java 微服务 | `JAVA_OPTS: "-Xms256m -Xmx512m"` | `docker-compose-all.yml:175,193,214,236,254,275,294` 等 | 网关/user/knowledge/chat/search/message/notify 等 |
| MySQL | `innodb-buffer-pool-size=256M` | `mysql.cnf:4`（`master.cnf:14` 同） | InnoDB 数据/索引缓存 = MySQL 内存大头；另有每连接排序/读缓冲 × `max_connections=500`（`master.cnf:24`） |
| Redis | `maxmemory 256mb` + `allkeys-lru` | `redis.conf:24-25` | 超限触发 LRU 淘汰（算法见 [09-缓存淘汰](../11-数据结构与算法/09-缓存淘汰-LRU-LFU与两级缓存.md) §5） |

### 5.2 一台 8GB 机器跑全量 compose 的账单（估算）

```
MySQL    ~350MB（buffer-pool 256M + 连接缓冲 + 进程常驻）
Redis    ~300MB（maxmemory 256M + freelist/常驻，无容器 limit）
ES       ~900MB（堆 512M + Lucene 堆外/mmap ≈ 0.7~1 倍堆）
RocketMQ ~800MB（namesrv 256M + broker 512M + 堆外）
Nacos    ~500MB
9 个微服务 ~7GB（每个 -Xmx512m，实际 RSS ≈ 700MB~1GB）
────────────────────────────────────────────
合计 ≈ 10GB+  →  8GB 机器跑不动全量 compose，这正是文档把服务拆到
host1/host2/host3 三套 compose（deploy/docker-compose/docker-compose-host*.yml）的原因
```

> 诚实说明：上表 RSS 为按配置的工程估算，真实数字以 `docker stats` 实测为准（练习 1 就是让你去量）。

---

## 六、排查命令速查（可复制）

```bash
# ① 进程级：RSS vs VSS
ps -o pid,rss,vsz,cmd -p $(pidof java)

# ② 容器级（cgroup 口径，K8s OOM 判定依据）
docker stats --no-stream

# ③ 容器 limit 实际生效值（在容器内执行）
cat /sys/fs/cgroup/memory.max                      # cgroup v2
cat /sys/fs/cgroup/memory/memory.limit_in_bytes    # cgroup v1

# ④ JVM 视角：容器感知结果
java -XX:+PrintFlagsFinal -version | grep -E "MaxHeapSize|UseContainerSupport"
jcmd 1 VM.flags | grep -o "MaxHeapSize=[^ ]*"

# ⑤ OOM 现场取证
dmesg -T | grep -i "killed process"
docker inspect aics-chat-service --format '{{.State.OOMKilled}} exit={{.State.ExitCode}}'

# ⑥ 页错误统计：major fault 高 = 内存不足在抖动
ps -o min_flt,maj_flt,cmd -p $(pidof java)
sar -B 1    # 或 vmstat 1 看 bi/bo 与 si/so（swap 进出）
```

---

## 七、面试高频问答

**Q1：进程的 VSS 和 RSS 有什么区别？看哪个判断容器快 OOM？**
A：VSS 是申请的虚拟地址空间（含从未使用的登记量），RSS 是实际占用的物理内存（含共享页）。判断 OOM 看 RSS——内核 OOM Killer 按 RSS 排序挑牺牲者，cgroup 计量的也是实际用量；VSS 虚高是常态（malloc 只登记虚拟区间）。

**Q2：什么是 page fault？minor 和 major 的区别？**
A：访问的虚拟页没有对应物理页映射时触发。minor fault：页已在页缓存，只补页表映射，微秒级；major fault：要从磁盘读入，毫秒级。major 频繁说明内存不足在换页/重读，是性能排查的重要信号。

**Q3：为什么容器里的 JVM 要显式设 -Xmx，不能靠默认值？**
A：JDK 10+ 的容器感知（UseContainerSupport 默认开）会让默认堆 = cgroup limit × MaxRAMPercentage(25%)。但 compose 场景没有 memory limit 可读，JVM 退化为按宿主机内存算——32GB 宿主机上单个服务默认 8GB 堆，十几个服务必然挤爆宿主。所以项目用 `JAVA_OPTS: -Xms256m -Xmx512m` 显式圈定。

**Q4：容器被 OOMKilled（exit 137）但 Java 没抛 OutOfMemoryError，为什么？**
A：容器 limit 限的是整个进程树（cgroup 口径 = RSS），JVM 的堆外内存（元空间、线程栈、Netty DirectBuffer）不受 -Xmx 管。堆守规矩、堆外超标时，是内核 OOM Killer 直接 SIGKILL 整个容器，JVM 来不及抛异常也来不及写日志。项目 K8s 配置 512m 堆 + 1Gi limit 正是给堆外留了约 500MB 余量。

**Q5：Java OOM 和容器 OOMKilled 怎么区分与排查？**
A：看退出方式：Java OOM 有异常栈、进程可存活；OOMKilled 是 exit 137 + 无 Java 日志。排查：`dmesg | grep -i killed` 看内核击杀现场，`docker inspect .State.OOMKilled` 确认标志，再对比 cgroup limit 与进程 RSS 组成（jcmd + pmap），通常结论是"堆外内存没预算"。

**Q6：swap 是什么？为什么延迟敏感的 JVM 服务要关？**
A：swap 把不活跃的匿名页换出到磁盘。对 JVM 的毒点是：堆被换出后，一次 full GC 触碰所有页 → 全部换入 → 秒级停顿。且容器环境（K8s 节点）通常禁用 swap，内存不足直接走 OOM Killer 而不是换页。

**Q7：ES 官方建议堆不超过一半内存，为什么？**
A：ES 的查询/聚合在 JVM 堆里，但 Lucene 段文件依赖 OS 页缓存（堆外 mmap）。堆占太多 → 页缓存饿 → 索引读写全走磁盘。项目 `ES_JAVA_OPTS: "-Xms512m -Xmx512m"` 就是把另一半留给页缓存，且 Xms=Xmx 避免运行期扩堆抖动。

**Q8：MySQL 的内存大头是什么？`max_connections` 调大对内存意味着什么？**
A：InnoDB buffer pool（本项目 256M）缓存数据页与索引页，是全局大头；此外每个连接有排序缓冲、join 缓冲等会话级内存，`max_connections=500`（master.cnf:24）意味着峰值可能额外 500 × 每连接缓冲——调大连接数要同步核算内存账，而不是只看连接数。

---

## 八、动手练习

1. 跑起全量 compose（或至少 chat+MySQL+Redis），用 `docker stats --no-stream` 实测每个容器的内存占用，与 §5.2 的估算表逐项对照，解释偏差最大的那一项。
2. 在 chat 容器内分别执行 `cat /sys/fs/cgroup/memory.max`（v2）与 `memory.limit_in_bytes`（v1），确认 compose 部署下 limit 是多少；再对照 K8s 清单 `ai-chat-service.yaml:86` 思考两种部署的 OOM 行为差异。
3. 把 `ai-cs-chat` 的 K8s limits 从 1Gi 改成 512Mi 压测（`k6 run scripts/loadtest/k6/sse-chat.js`），观察是否出现 exit 137；用 `dmesg` 取证后把 limit 恢复，写下你的内存账本。
4. 去掉某个服务 compose 里的 `JAVA_OPTS` 启动，用 `jcmd 1 VM.flags | grep MaxHeapSize` 看默认堆变成了多少，验证 §4.2 "JVM 看宿主机内存"的结论。
5. 用 `ps -o rss,vsz` 采样 chat 进程：冷启动后、首波 SSE 压测后、压测结束 5 分钟后各一次，解释 RSS 曲线与 GC/页缓存的关系。

---

> 上一篇：[01-进程线程与调度](./01-进程线程与调度.md) ｜ 下一篇：[03-文件系统与IO多路复用](./03-文件系统与IO多路复用.md)
