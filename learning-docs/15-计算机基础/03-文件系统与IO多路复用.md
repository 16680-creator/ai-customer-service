# 文件系统与 I/O 多路复用

> 对应项目：`deploy/mysql/master.cnf`、`deploy/mysql/slave.cnf`（InnoDB/binlog/relay-log 刷盘配置）、
> `deploy/redis/redis.conf`（AOF/RDB 持久化与单线程事件循环）、`deploy/docker-compose/docker-compose-master-slave.yml`。
> **划界声明**：[01-Java基础/07-IO模型与Netty基础](../01-Java基础/07-IO模型与Netty基础.md) 已讲 BIO/NIO/AIO、JDK `Selector`、Netty EventLoop 与零拷贝；本篇只讲 **OS 层**——inode/页缓存/fsync、`O_DIRECT`、epoll 的内核内部机制与触发模式。应用层用法见 [05-SSE与WebSocket实时通信](../04-中间件/05-SSE与WebSocket实时通信.md)。

---

## 一、inode：文件在文件系统里的真身

**结论**：文件名只是目录项（dentry）里的一个指针，**文件的真身是 inode**。

| 概念 | 存什么 | 面试/排查要点 |
|---|---|---|
| inode | 文件元数据：大小、权限、时间戳、**数据块指针**；**不含文件名** | `df -i` 看 inode 余量：小文件写爆 inode 后磁盘没满也创建不了文件 |
| dentry（目录项） | 文件名 → inode 编号 的映射 | 同一 inode 多个名字 = **硬链接**（删一个名字数据还在） |
| 软链接 | 独立 inode，内容是目标路径 | 目标删了就悬空 |

**项目关联**：`docker-compose-all.yml:19` 把 `mysql-data:/var/lib/mysql` 挂成 volume——MySQL 的 `.ibd` 表空间、redo/binlog 全靠 inode 里的数据块指针定位；容器删了（inode 释放）数据卷还在，这是 [01-Docker容器化](../07-运维部署/01-Docker容器化.md) 讲 volume 时的底层依据。

---

## 二、页缓存与 fsync：write 到底写到哪了

### 2.1 一次 `write()` 的真实路径

```
应用 write(fd, buf)
  → 内核把数据拷进【页缓存 Page Cache】（内存！非磁盘）     ← write() 返回，其实还没落盘
  → 对应页标记为【脏页 dirty page】
  → 内核后台线程（writeback）在"脏页超时/占比超限"时异步写盘
  → 机器此时断电 = 页缓存里的数据全丢
```

**这是所有"持久化语义"问题的根源**：`write()` 返回成功 ≠ 数据在磁盘上。数据库、MQ、Redis 的所有刷盘参数，本质上都是在回答"**我什么时候替你调 fsync**"。

### 2.2 刷盘 API 语义对比（面试必背）

| 调用 | 刷什么 | 是否等磁盘确认 |
|---|---|---|
| `write()` | 数据 → 页缓存 | 否（返回即成功，但没落盘） |
| `fflush()`（C 库/Java 流） | 用户态缓冲 → 内核（等价多做一次 write） | 否 |
| `fsync(fd)` | 该 fd 的**数据 + 元数据**（如文件大小） | **是，返回即持久** |
| `fdatasync(fd)` | 只刷数据，元数据仅在必要时刷 | 是，通常比 fsync 少一次元数据 I/O |
| `O_SYNC` 打开 | 每次 write 自动等落盘 | 是（不用手动 fsync，但每次写都慢） |

### 2.3 为什么数据库都先写日志（WAL）

随机写 = 多次寻址/fsync，顺序追加日志 = 一次顺序写 + 一次 fsync。所以 InnoDB 的写路径是：

```
事务修改 → 先顺序写 redo log（fsync）→ 崩溃后重放 redo 恢复 → 脏页随后慢慢刷回表空间
```

**用一次便宜的顺序 fsync 换崩溃安全，脏页刷新异步化**——这就是 WAL（Write-Ahead Logging）。MySQL、Redis AOF、RocketMQ CommitLog 全是同一思想。

---

## 三、buffered IO vs direct IO

| | buffered（默认） | direct（`O_DIRECT`） |
|---|---|---|
| 路径 | 磁盘 ⇄ **页缓存** ⇄ 应用缓冲 | 磁盘 ⇄ 应用缓冲（跳过页缓存） |
| 读命中 | 热数据直接命中内存，快 | 每次真读盘 |
| 双缓冲问题 | 应用再自建缓存（如 InnoDB buffer pool）= 同一份数据两处缓存，浪费内存 | 数据只缓存一处 |
| 谁在用 | 绝大多数应用 | 自带精密缓存的数据库：InnoDB 可配 `innodb_flush_method=O_DIRECT` |

**项目现状（grep 确认）**：`master.cnf`/`mysql.cnf` **未配置** `innodb_flush_method`，走默认 `fsync`（数据页、redo 都经页缓存，double buffering：页缓存 + buffer pool 各一份）。256MB 的 buffer pool（`mysql.cnf:4`）在单机测试库可接受；生产大内存实例的常见优化正是改 `O_DIRECT`，把省下的页缓存还给 buffer pool。

---

## 四、select / poll / epoll：内核内部机制

> select/poll/epoll 的 API 层对比表（1024 上限、O(n) 遍历）见 [07-IO模型与Netty基础](../01-Java基础/07-IO模型与Netty基础.md) §1.3，此处讲内核内部为什么能做到 O(1)。

### 4.1 select/poll 的问题在"每次都全量重做"

```
每次 select()：
  ① 把整个 fd 集合从用户态拷进内核
  ② 内核逐个问每个 fd："就绪了吗？"（O(n) 遍历，逐个挂等待队列再摘下来）
  ③ 返回后用户态再 O(n) 找哪些就绪
连接数 1 万、活跃 50 → 每次 9995 次无效检查
```

### 4.2 epoll 的三步与两个数据结构

```
epoll_create() → 内核建 eventpoll 对象
epoll_ctl(ADD, fd) → 把 fd 插进【红黑树】（注册一次，O(log n)，不重复拷贝）
                     同时在 fd 的等待队列上挂一个回调 ep_poll_callback
数据到达 → 网卡中断 → 协议栈处理 → fd 就绪 → 回调触发，把该 fd 挂进【就绪链表 rdllist】
epoll_wait() → 只检查 rdllist：非空就把它返回给用户态（O(就绪数)，与总连接数无关）
```

**精妙之处**：把"轮询问每个 fd"倒转为"fd 就绪时主动回调入链"。连接多而活跃少（长连接服务的典型形态）时，工作量从 O(总连接) 变成 O(活跃连接)。

### 4.3 水平触发（LT）vs 边沿触发（ET）

| | LT 水平触发 | ET 边沿触发 |
|---|---|---|
| 就绪条件 | 只要还有数据没读完，每次 `epoll_wait` 都报 | 仅在状态**变化**（新数据到达）时报**一次** |
| 漏读后果 | 下次还会提醒，安全 | 没读干净就**永远不会再提醒**，连接卡死 |
| 编程要求 | 可以按需读 | 必须非阻塞 fd + 循环读到 `EAGAIN` 才撒手 |
| 谁在用 | JDK NIO `Selector`（Linux 下 epoll LT）；**Redis 的事件循环是 LT** | Netty 原生 epoll transport、Nginx |

> 验证性记忆点：JDK 的 `Selector` 没有暴露 ET 语义，所以 Netty 想用 ET 必须绕开 JDK 另写 native epoll transport——这解释了 Netty 为什么维护两套 transport（见 [07-IO模型与Netty基础](../01-Java基础/07-IO模型与Netty基础.md) §四）。

---

## 五、项目现场一：MySQL 的刷盘与主从复制

### 5.1 事务提交时刻的磁盘动作（全部可在 `master.cnf` 定位）

```ini
# deploy/mysql/master.cnf:14-17
innodb-buffer-pool-size=256M          # 数据/索引页缓存（§三的"应用自建缓存"）
innodb-log-file-size=64M              # redo log 文件大小
innodb-flush-log-at-trx-commit=1      # ⭐ 每次提交都 fsync redo log
innodb-lock-wait-timeout=30
```

`innodb_flush_log_at_trx_commit` 三档语义（本项目 = 1，最安全）：

| 值 | 提交时刻做什么 | 崩溃后果 | fsync 次数/事务 |
|---|---|---|---|
| **1（现状）** | redo log **fsync 到磁盘** | 不丢已提交事务 | ≥1 |
| 2 | redo 写到**页缓存**，每秒后台 fsync | 丢最多 1 秒事务（OS 活着但 MySQL 挂则不丢） | ~0 |
| 0 | 每秒才把 log buffer 写页缓存 + fsync | 丢最多 1 秒事务（连 MySQL 进程挂也丢） | ~0 |

### 5.2 binlog 与主从链路

```ini
# deploy/mysql/master.cnf:8-11 + deploy/mysql/mysql.cnf:21-25
binlog-format=ROW            # 行级变更（Canal CDC 依赖 ROW + binlog-row-image=FULL）
binlog-cache-size=1M         # 事务内 binlog 的会话缓冲，大事务溢出到磁盘临时文件
max-binlog-size=500M         # 单文件上限，超过滚动
expire-logs-days=7

# deploy/mysql/slave.cnf:6-16
relay-log=relay-bin                       # 从库中转日志（IO 线程落盘 → SQL 线程重放）
replicate-do-db=user_db / knowledge_db / chat_db / nacos_config
```

从磁盘 I/O 视角看主从复制：

```
主库：事务 → redo fsync（trx_commit=1）+ binlog 写页缓存 → dump 线程发给从库
从库：IO 线程收 binlog → 写 relay-log → SQL 线程读出重放
复制延迟的 I/O 本质 = 主库刷盘节奏 + 网络传输 + 从库重放速度（单线程重放是大库延迟的经典瓶颈）
```

---

## 六、项目现场二：Redis 的持久化与单线程事件循环

### 6.1 持久化配置（`deploy/redis/redis.conf`）

```ini
# redis.conf:14-21
appendonly yes              # 开 AOF
appendfsync everysec        # ⭐ 后台线程每秒 fsync AOF（主线程只 write 到页缓存，不阻塞）
save 900 1 / 300 10 / 60 10000   # RDB：900s 内 1 次写 / 300s 内 10 次 / 60s 内 1 万次 则 fork 生成快照
```

- **`everysec` 的丢数据窗口**：主线程只做 `write`（进页缓存，微秒级），fsync 交给后台线程每秒一次。最坏情形：上次 fsync 刚做完、本秒数据已 write、进程崩溃——**丢约 1 秒；若后台 fsync 卡住积压，最坏约 2 秒**。这是"性能与持久化"的经典取舍，本项目把它用在会话热缓存上，可接受（兜底是回源 message 服务，见 [01-Redis缓存实战](../04-中间件/01-Redis缓存实战.md)）。
- RDB 的 `save` 触发 fork：fork 用写时复制（COW），瞬时多占的正是 §二讲的页缓存/物理页——大实例上 fork 卡顿的根源。

### 5.3 两个未配置项的现状说明（grep 确认）

| 参数 | 项目现状 | 影响 |
|---|---|---|
| `innodb_flush_method` | 未配置（默认 fsync：数据页与 redo 都过页缓存） | 页缓存与 buffer pool 双缓冲；256M 的 buffer pool 单机可接受，生产大实例常改 `O_DIRECT` |
| `innodb_doublewrite` | 未显式配置（默认 ON） | 双写缓冲防"半页写"——4KB 页在 512B/4KB 扇区上断电可能只有部分落盘，doublewrite 先把整页写到恢复区。面试常问"redo 写了为什么还要 doublewrite"：redo 记录的是变更，恢复时要求页本身完好 |

**读路径补充**：MySQL 大范围扫描（报表/全表）会绕过 LRU 的"中年尾巴"（young/old 区，默认 5/8 与 3/8 分割）——old 区驻留阈值 `innodb_old_blocks_time` 默认 1000ms，一次性扫描页不易挤进 young 区，这是 InnoDB 对"缓存污染"（[09-缓存淘汰](../11-数据结构与算法/09-缓存淘汰-LRU-LFU与两级缓存.md) §2.1）的内核级答案。

### 6.2 单线程事件循环为什么够快

```
Redis 主线程循环（ae 事件框架，Linux 下 epoll LT）：
  epoll_wait → 就绪连接 → 逐个执行命令（纯内存操作，亚微秒）→ 写回响应
```

| 支撑点 | 说明 |
|---|---|
| epoll LT + 非阻塞 | 单线程监听 `maxclients 10000`（`redis.conf:31`）条连接 |
| 命令是内存操作 | 一次命令 ~0.1μs，网络 RTT（~1ms）才是大头——瓶颈在网络不在 CPU |
| 没有锁 | 单线程天然无并发，所有命令原子执行——这是 Lua 脚本/`INCR` 原子性的根源 |
| Redis 6+ 的 `io-threads` | 多线程做网络读写（命令执行仍单线程）；**项目 redis.conf 未配置，默认关闭**（现状诚实说明） |

**对照记忆**：网关 `ai-cs-gateway` 是 Netty 多线程 Reactor，Redis 是单线程事件循环——两者内核机制相同（epoll），差别在"每个事件的处理成本"：Redis 命令亚微秒所以单线程打满，转发逻辑复杂所以多线程。

### 6.3 单线程的另一面：慢命令放大成"全局卡顿"

| 阻塞源 | 机制 | 防御 |
|---|---|---|
| `KEYS *`/大集合全量命令 | O(n) 遍历在单线程里独占事件循环 | 生产禁用/用 SCAN 渐进遍历 |
| 大 key 删除 | 释放一个百 MB hash 一次阻塞百毫秒 | `UNLINK`（异步线程释放）+ lazyfree |
| fork（RDB/AOF 重写） | COW 页表复制，实例越大 fork 越慢 | 拆实例/低峰执行 |
| AOF fsync 磁盘抖动 | 后台 fsync 卡住时主线程写 AOF 也会等待 | 慢盘监控 + `aof-load-truncated` 预案 |

对照 [01-Redis缓存实战](../04-中间件/01-Redis缓存实战.md) 的业务用法：本项目 Redis 承担会话热缓存与限流计数，命令都是 O(1)，单线程模型风险主要在"运维误操作大命令"——`slowlog-log-slower-than 10000`（`redis.conf:36`）留了 10ms 慢查询日志兜底。

---

## 七、面试高频问答

**Q1：write() 返回成功，数据在磁盘上吗？**
A：不在。write 只把数据拷进内核页缓存并标记脏页，真正落盘靠后台 writeback 或显式 fsync。断电丢的是页缓存里未 fsync 的部分；所以数据库用 WAL：先 fsync 日志再异步刷脏页。

**Q2：fsync 和 fdatasync 的区别？**
A：fsync 刷数据 + 元数据（如文件长度变化）；fdatasync 只保证数据持久，元数据仅在影响读取时刷。日志型追加写用 fdatasync 可省一次元数据 I/O，是高性能存储的常规优化。

**Q3：InnoDB 的 innodb_flush_log_at_trx_commit=1/2/0 的取舍？**
A：1 = 每次提交 fsync redo，不丢已提交事务，最慢；2 = 提交只写页缓存、每秒 fsync，MySQL 挂不丢、OS 挂丢 1 秒；0 = 每秒才写+fsync，连 MySQL 进程崩溃也丢 1 秒。本项目 master.cnf:16 用 1，因为主库承担订单/支付相关链路，宁可慢不可丢。

**Q4：epoll 为什么比 select 快？内核用了什么数据结构？**
A：select/poll 每次调用都全量拷贝 fd 集合进内核并 O(n) 遍历；epoll 用 epoll_ctl 把 fd 注册进内核红黑树（一次），fd 就绪时由等待队列回调把节点挂入就绪链表，epoll_wait 只取就绪链表——O(活跃数) 而非 O(总数)，且无重复拷贝。

**Q5：水平触发和边沿触发的区别？写 ET 要注意什么？**
A：LT 只要缓冲区还有数据每次 wait 都报；ET 只在状态变化时报一次。ET 必须配非阻塞 fd 并循环读直到 EAGAIN，否则漏读后永不再通知、连接卡死。JDK NIO 与 Redis 用 LT；Netty native epoll、Nginx 用 ET。

**Q6：Redis 单线程为什么能扛高并发？**
A：命令是纯内存操作（亚微秒），瓶颈在网络 RTT；epoll 单线程即可监听上万连接且无锁无切换开销，所有命令天然原子。代价是一个慢命令（KEYS、大 key）阻塞所有人——生产禁用慢命令、拆大 key。

**Q7：Redis AOF appendfsync everysec 最坏丢多少数据？为什么？**
A：约 1~2 秒。主线程只 write 进页缓存，fsync 由后台线程每秒一次；崩溃时未 fsync 的部分丢失。若磁盘繁忙导致后台 fsync 积压（Redis 会等它，最坏形成约 2 秒窗口）。比 always 快一个数量级，比 no 安全得多。

**Q8：主从复制的 I/O 链路是怎样的？延迟从哪来？**
A：主库事务提交（redo fsync）→ 事务写 binlog → dump 线程推送；从库 IO 线程写 relay-log（slave.cnf:6）→ SQL 线程重放。延迟 = 主库刷盘节奏 + 网络 + 从库单线程重放速度；大事务/大表 DDL/从库单点读压力都是常见放大器。

**Q9：什么是 WAL？为什么 MySQL/Redis/RocketMQ 都用它？**
A：Write-Ahead Logging：先顺序写日志并 fsync，再异步刷数据页。一次顺序 fsync 的成本远低于多次随机写，且崩溃后重放日志即可恢复。Redo log（MySQL）、AOF（Redis）、CommitLog（RocketMQ）都是同一模式的实现。

**Q10：页缓存对应用是"纯福利"吗？什么时候要绕开它？**
A：读多写少时是福利（热数据免磁盘）；但当应用自建了精密缓存（InnoDB buffer pool）时，页缓存就是重复占内存的双缓冲，且写多读少场景 writeback 时机不可控。绕开方式是 `O_DIRECT`（`innodb_flush_method`），把内存预算还给应用层缓存——代价是失去内核预读，适合"自带缓存"的数据库，不适合普通应用。

---

## 八、动手练习

1. 用 `dd if=/dev/zero of=/tmp/t bs=1M count=512 conv=fsync` 与不加 `conv=fsync` 各跑一次，对比耗时，量化"页缓存 writeback"与"真落盘"的差距（差值就是 fsync 的价钱）。
2. 把测试库 `innodb_flush_log_at_trx_commit` 临时改为 2（`SET GLOBAL`），用 sysbench/压测脚本对比写事务 TPS，解释变化与 §5.1 表格的对应关系，测完改回 1。
3. `strace -f -e trace=epoll_wait,read,write -p $(pidof redis-server)` 30 秒，观察单线程事件循环的真实系统调用序列，验证"epoll_wait → read → write"的循环结构。
4. 读 `deploy/mysql/slave.cnf`，用 `SHOW SLAVE STATUS\G`（真实从库）对照 `relay-log`、`replicate-do-db` 四个库，计算当前复制延迟并定位它属于"IO 线程"还是"SQL 线程"阶段。
5. 改造练习：为 chat 服务的会话历史落库路径评估"trx_commit=2 + 每秒批量"方案，列出丢数据窗口内的业务影响（哪些消息可能丢），写一份 200 字的取舍说明。

---

> 上一篇：[02-内存管理与虚拟内存](./02-内存管理与虚拟内存.md) ｜ 下一篇：[04-TCP协议实战](./04-TCP协议实战.md)
