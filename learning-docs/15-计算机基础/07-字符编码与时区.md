# 字符编码与时区

> 对应项目：`deploy/mysql/master.cnf:20-21`（utf8mb4 服务端字符集）、`deploy/mysql/init.sql:283,305`（chat_db 与 chat_message 建表）、
> `tools/nacos-config/ai-cs-message.yml:3`（JDBC URL 真实参数）、`deploy/docker-compose/docker-compose-all.yml:17`（MySQL 容器 TZ）。
> 相关：[08-语言级细节与序列化](../01-Java基础/08-语言级细节与序列化.md)（`DATETIME + serverTimezone=Asia/Shanghai` 的团队约定）、
> [01-MySQL核心知识](../03-数据库与ORM/01-MySQL核心知识.md)（MySQL 用法层，utf8mb4 基础不重复展开）、
> [05-HTTP与TLS](./05-HTTP与TLS.md)（SSE JSON 里的多字节字符流式解码）。

---

## 一、从一条对话消息说起：emoji 在项目里走一遭

用户在对话框输入：**`订单😊什么时候发货`**。这串字符要在项目里穿过 6 个环节，每一环都有自己的"编码约定"：

```
浏览器输入框（UTF-16 DOM string）
  → fetch POST body（UTF-8 字节）          ChatView.vue:364 URLSearchParams
  → 网关 8080 转发（字节流，不解释内容）
  → ai-cs-chat 处理（Java String = UTF-16）
  → Redis/MQ 传递（UTF-8 字节）
  → MySQL chat_message.content 落库        init.sql:305（TEXT，表 utf8mb4）
  → 历史查询原路返回前端
```

任何一环约定不一致（比如表是 3 字节的 `utf8`），emoji 就变成 `?` 或直接报错。所以本篇的口径是：**编码问题不是"懂 UTF-8"，而是"全链路每层各用什么、边界上谁负责转换"**。

---

## 二、UTF-8 变长编码：规则与"为什么这样设计"

UTF-8 是**变长**编码：1~4 字节表示一个 Unicode 码点（code point）。

| 码点范围 | 字节数 | 首字节模式 | 例子 |
|---|---|---|---|
| U+0000 ~ U+007F | 1 | `0xxxxxxx` | `A` = 0x41；英文/数字与 ASCII 完全兼容 |
| U+0080 ~ U+07FF | 2 | `110xxxxx 10xxxxxx` | 拉丁扩展、希腊字母 |
| U+0800 ~ U+FFFF | 3 | `1110xxxx 10xxxxxx 10xxxxxx` | **常用中文**（`订` = E8 AE A2） |
| U+10000 ~ U+10FFFF | 4 | `11110xxx 10xxxxxx×3` | **emoji**（😊 U+1F60A = F0 9F 98 8A） |

### 2.1 字符 → 字节演示表（自己编码一遍就懂了）

| 字符 | Unicode 码点 | UTF-8 字节序列 | 字节数 |
|---|---|---|---|
| `A` | U+0041 | `41` | 1 |
| `订` | U+8BA2 | `E8 AE A2` | 3 |
| `😊` | U+1F60A | `F0 9F 98 8A` | 4 |
| `"订单😊"` 全串 | — | `E8AEA2 E58D95 F09F988A` | 10 字节 |

推论：这条消息 `length()`=4（UTF-16 码元，见 §三），**UTF-8 字节数是 10**——"字符数"和"字节数"是两个都要心里有数的量：Java 侧校验用码点数，DB/网络容量按字节数算。

### 2.2 模拟一个变长编码的"自同步"验证

```
假设传输中第 2 个字节丢了：E8[A2 丢失]...（示例）
  E8 → 1110 开头 = "我是一个 3 字节字符的首字节"
  下一个字节不是 10xxxxxx 续字节 → 解码器立即知道"字符不完整"
  → 替换为 U+FFFD（�）并从下一个合法边界重新开始——坏 1 字节只坏 1 个字符
```

三个设计要点（也是面试考点）：

1. **ASCII 兼容**：首字节高位 0，纯英文文本按 UTF-8 存与 ASCII 逐字节相同——旧系统无痛迁移；
2. **自同步（前缀码）**：续字节一律 `10xxxxxx`，从任意字节位置都能判断"这是字符开头还是中间"，坏一个字节不毁整段；
3. **无字节序问题**：不像 UTF-16/32 需要 BOM，网络传输天然安全——这是它成为互联网事实标准的原因。

---

## 三、Java String 是 UTF-16：emoji 长度与截断的坑

### 3.1 三个概念对齐

| 术语 | 含义 | Java 里的体现 |
|---|---|---|
| 码点（code point） | 一个 Unicode 字符的编号（0~0x10FFFF） | `"😊".codePointCount(0, 2)` = 1 |
| 码元（code unit） | UTF-16 的最小单元，16 位 | `char` 类型；常用字 1 个，**emoji 用 2 个（代理对）** |
| 代理对（surrogate pair） | 高代理 0xD800-0xDBFF + 低代理 0xDC00-0xDFFF 组合出 4 字节码点 | `"😊".length()` = **2** |

```java
String msg = "订单😊";
msg.length();              // 4 —— 用户眼里的 4 个字，程序里的 4 个 char？错："😊"占 2 个，返回 4
msg.codePointCount(0, msg.length());  // 3 —— 真实"字符数"（校验长度应该用它）
msg.substring(0, 2);       // "订单"
msg.substring(2, 3);       // ❌ 只切了代理对的一半 → 乱码字符（非法码点）
new String(msg.substring(2, 3).getBytes(UTF_8), UTF_8).contains("�") // 替换符出现
```

### 3.2 三个真实会踩的场景（都可对到本项目）

| 场景 | 错误写法 | 后果 | 正确做法 |
|---|---|---|---|
| 输入长度校验（如昵称 ≤ 20） | `msg.length() <= 20` | emoji 用户被"半价"计数 | `msg.codePointCount(0, msg.length())` |
| 摘要截断（历史压缩截 N 字） | `text.substring(0, n)` | 切断代理对 → 存库/展示乱码 | `text.offsetByCodePoints(0, n)` 再截 |
| 字节数限制（如 JWT/字段 255 字节） | 按 `length()` 估算 | UTF-8 下中文 3B/emoji 4B，实际字节超限 | `msg.getBytes(UTF_8).length` |

> 诚实说明：本项目 `ai-cs-chat` 的历史压缩（`compressHistory`）与校验逻辑目前按 char 处理、未出现截断 bug，是因为 LLM 历史消息不做定长截断。**练习 4 会让你真的踩一次这个坑**，踩过才记得住。

---

## 四、MySQL：utf8 ≠ utf8mb4 与 collation

### 4.1 历史包袱

| | MySQL `utf8`（实为 utf8mb3） | `utf8mb4` |
|---|---|---|
| 最长字节 | **3 字节**（历史实现限制） | **4 字节 = 真·UTF-8** |
| emoji（4 字节码点） | ❌ 存不了 | ✅ |
| 现状 | 兼容别名，新项目禁用 | **MySQL 8 默认字符集** |

**项目现状（真实配置，全部可查）**：全链路统一 `utf8mb4`——

```ini
# deploy/mysql/master.cnf:20-21（服务端默认）
character-set-server=utf8mb4
collation-server=utf8mb4_unicode_ci
```

```sql
-- deploy/mysql/init.sql:283（建库）+ :305-318（chat_message 表，content TEXT）
CREATE DATABASE IF NOT EXISTS chat_db DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE TABLE IF NOT EXISTS chat_message (...) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 4.2 collation 是什么、为什么重要

collation = 同一字符集下的**比较/排序规则**。`utf8mb4` 下常见三种：

| collation | 规则 | 影响 |
|---|---|---|
| `utf8mb4_unicode_ci`（**项目现值**） | UCA 4.0，大小写不敏感（ci） | `WHERE username='Tom'` 能匹配 `tom`；唯一索引把 `Tom/tom` 视为冲突 |
| `utf8mb4_0900_ai_ci`（MySQL 8 默认） | UCA 9.0，重音也不敏感（ai） | 排序更符合新版 Unicode；与 unicode_ci 个别字符归类不同（如 `ß` 与 `ss`） |
| `utf8mb4_bin` | 按二进制逐字节比 | 密码类/二进制语义字段才用它；大小写敏感 |

**工程要点**：collation 决定**索引的比较行为**，混用会让 `UNIQUE KEY` 失去约束意义（`uk_username` 在 ci 下拦不住 `Tom` 与 `TOM` 双注册）。项目在服务端默认、建库、每张表三层都显式声明同一 collation，避免"表与库不一致"的经典翻车。

### 4.3 写入 emoji 报错的现场还原

```sql
-- 若表是 utf8mb3（反面教材），严格模式下：
INSERT INTO chat_message(content) VALUES ('订单😊');
-- ERROR 1366 (HY000): Incorrect string value: '\xF0\x9F\x98\x8A'
-- \xF0 开头 = 4 字节 UTF-8 → mb3 装不下
```

项目 `sql-mode=STRICT_TRANS_TABLES`（`master.cnf:35`）：非法字符**报错回滚**而非静默写成 `?`——比非严格模式"悄悄吞数据"好排查得多。

---

## 五、连接层编码：JDBC URL 逐参数解读

项目各服务 Nacos 配置里的真实 URL（`tools/nacos-config/ai-cs-message.yml:3`，chat 服务多数据源同款在 `ai-cs-chat.yml:33-37`）：

```
jdbc:mysql://host:3306/chat_db?useUnicode=true&characterEncoding=utf-8
    &useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
```

| 参数 | 作用 | 本项目的理由 |
|---|---|---|
| `useUnicode=true` | 声明使用 Unicode 字符集（老参数，保留兼容） | 沿用模板 |
| `characterEncoding=utf-8` | 应用 ↔ 驱动的字符集 | Connector/J 8 在服务端支持时会协商到 **utf8mb4**，与服务端 `character-set-server` 对齐 |
| `serverTimezone=Asia/Shanghai` | 驱动做时间类型换算的"服务器时区"（8.0.23 起新名 `connectionTimeZone`，旧名兼容） | MySQL 容器 `TZ=Asia/Shanghai`（`docker-compose-all.yml:17`），两者必须一致，见 §六 |
| `useSSL=false` | 内网明文连接 | 学习/单机环境关闭握手开销 |
| `allowPublicKeyRetrieval=true` | 允许客户端向服务器请求 RSA 公钥（caching_sha2_password 认证） | MySQL 8 默认认证插件需要；仅内网可开 |

**记忆法**：编码参数决定"字符串怎么变字节"，时区参数决定"时间怎么换算"——前者防 `?`，后者防 8 小时。

---

## 六、时区：LocalDateTime、Instant、serverTimezone 与容器 TZ

### 6.1 Java 时间类型选型

| 类型 | 有无时区 | 适用 | 本项目对应 |
|---|---|---|---|
| `LocalDateTime` | 无 | "墙上时间"：营业时间、账单日 | 业务实体普遍用它（MyBatis-Plus 默认映射 `DATETIME`） |
| `Instant` | UTC 时间线 | 计算时长、跨服务传时间戳 | 日志/监控埋点 |
| `ZonedDateTime` | 带时区 | 展示转换 | 前端展示层转换 |

### 6.2 三个"时区"其实是三个不同的钟

```
① JVM 默认时区：容器未设 TZ 时 = UTC（eclipse-temurin 镜像无本地化配置）
② JDBC serverTimezone：驱动换算 java.sql.Timestamp ↔ DB DATETIME 用的参照
③ MySQL 服务器时区：容器 env TZ=Asia/Shanghai（docker-compose-all.yml:17）→ @@time_zone = SYSTEM 跟随
```

**三者组合的行为矩阵（为什么约定很重要）**：

| JVM 时区 | serverTimezone | `Timestamp.now()` 落库 | 结果 |
|---|---|---|---|
| Asia/Shanghai | Asia/Shanghai | 直接写 | ✅ 一致 |
| **UTC（容器现状）** | Asia/Shanghai | 驱动换算 +8h 后写 | ✅ 兼然一致（换算兜底） |
| UTC | （不配）8.0 驱动报错或按服务器时区 | 视驱动版本 | ⚠️ 版本差异雷区 |

**项目现状（诚实盘点）**：①JDBC URL 统一带 `serverTimezone=Asia/Shanghai`，与 MySQL 容器 TZ 一致——主链路自洽；②**Java 服务容器没有设置 `TZ` 环境变量**（compose 里只有 MySQL/Nacos 相关配置有 TZ），JVM 默认时区是 UTC——依靠 serverTimezone 换算兜住落库，但**日志时间戳是 UTC**（比北京时间早 8 小时），排查问题时先对表。团队约定见 [08-语言级细节与序列化](../01-Java基础/08-语言级细节与序列化.md)（DB 用 `DATETIME`（无时区）+ `serverTimezone=Asia/Shanghai`）。

### 6.3 DATETIME vs TIMESTAMP：时区敏感度的分水岭

| | DATETIME（项目在用） | TIMESTAMP |
|---|---|---|
| 存的内容 | 字面时间，**无时区语义** | UTC 秒数，**读取时按会话时区转换** |
| 范围 | 1000-9999 年 | 1970~2038（32 位限制，MySQL 8.0.28+ 可到 2138） |
| 时区变更影响 | 值不变（"墙上时间"稳定） | 值随会话 time_zone 漂移 |
| 空间 | 5~8 字节 | 4 字节 |
| 本项目 | `create_time DATETIME DEFAULT CURRENT_TIMESTAMP`（init.sql:313，chat_message 表等全库统一） | 未使用 |

**选型逻辑**：项目全库 DATETIME + `serverTimezone=Asia/Shanghai` 的组合，把"时区"责任收敛在 JDBC 驱动一层——Java 侧 UTC 与 DB 侧 Asia/Shanghai 之间的换算由 `connectionTimeZone` 参数一次完成，读写对称，不会二次漂移。TIMESTAMP 的自动 UTC 换算看似省心，但 2038 上限 + 会话时区依赖使它在多语言接入时反而多一个变量。

### 6.4 编码与时区的"隐藏交汇点"：JSON

Python 侧 `ai-cs-py-chat/app/api/chat.py:41,45` 推流时用 `json.dumps({'delta': delta}, ensure_ascii=False)`——`ensure_ascii=False` 让中文/emoji 以 UTF-8 原文出流，而不是被转义成 `\u8ba2\u5355`（前者省流量且前端日志可读）。Java 侧 Jackson 默认即非 ASCII 转义。**两端一致才不会出现"一端转义一端原文"的双重解码问题**。

### 6.5 排查命令速查

```bash
# 三口钟对表
docker exec aics-mysql mysql -uroot -proot -e "select @@global.time_zone, @@session.time_zone, now();"
docker exec aics-chat-service date                      # 容器 OS 时区（UTC 即未设 TZ）
docker exec aics-chat-service jcmd 1 VM.uptime          # 配合日志时间戳判断 JVM 时区
show variables like '%char%'; show variables like 'collation%';
```

---

## 七、面试高频问答

**Q1：UTF-8 为什么能成为互联网主流编码？**
A：①1~4 字节变长，英文 1 字节省空间、中文 3 字节、emoji 4 字节；②完全兼容 ASCII；③前缀码自同步，坏字节不扩散；④无字节序/BOM 问题，网络传输安全。

**Q2：Java String 用什么编码？"😊".length() 是多少？怎么正确数"字数"？**
A：UTF-16。char 是 16 位码元，emoji 是两个码元的代理对，所以 length()=2。数"用户字符数"用 `codePointCount(0, length())`；截断用 `offsetByCodePoints` 定位再切，避免切断代理对产生乱码。

**Q3：MySQL 的 utf8 和 utf8mb4 区别？项目用的哪个？**
A：MySQL 的 utf8 实为 utf8mb3，最多 3 字节，存不了 emoji；utf8mb4 才是完整 UTF-8。项目服务端默认（master.cnf:20）、建库（init.sql:283）、每张表三层统一 utf8mb4 + utf8mb4_unicode_ci。

**Q4：collation 是什么？unicode_ci 对唯一索引意味着什么？**
A：同一字符集下的比较排序规则。`utf8mb4_unicode_ci` 大小写不敏感：唯一索引把 'Tom' 与 'tom' 视为重复（防重同时也拦不住更细的差异）；要二进制级区分用 utf8mb4_bin（密码/令牌类字段）。collation 混用会让跨表 join 与唯一约束行为不一致。

**Q5：往 utf8mb3 表插 emoji 报什么错？严格模式的价值？**
A：`ERROR 1366 Incorrect string value '\xF0\x9F...'`——4 字节 UTF-8 进不了 3 字节字符集。项目 `STRICT_TRANS_TABLES`（master.cnf:35）下报错回滚，好过非严格模式静默截断成 `?`——数据出问题要"响"，不要"哑"。

**Q6：JDBC URL 里 characterEncoding 和 serverTimezone 各管什么？**
A：characterEncoding 管字符串字节转换（Connector/J 8 会协商到 utf8mb4，防 emoji 变 ?）；serverTimezone 是驱动换算 Timestamp ↔ DATETIME 的参照时区，必须与 MySQL 容器 TZ（Asia/Shanghai）一致，否则时间漂移 8 小时。

**Q7：LocalDateTime 和 Instant 怎么选？落库哪个？**
A：LocalDateTime 是无时区"墙上时间"，适合营业时间/业务字段，配 DATETIME 列；Instant 是 UTC 时间线，适合时长计算与跨服务时间戳。本项目业务实体用 LocalDateTime + DATETIME，团队约定 `serverTimezone=Asia/Shanghai` 统一换算（见 01-Java基础/08 文档）。

**Q8：容器里 JVM 的默认时区是什么？会出什么问题？**
A：未设 TZ 环境变量时 eclipse-temurin 容器默认 UTC。本项目 Java 容器未设 TZ（compose 只有 MySQL 配了 TZ=Asia/Shanghai），后果是日志时间戳为 UTC——落库靠 serverTimezone 换算兜底没漂移，但看日志要用 UTC+8 换算，或给容器补 `TZ: Asia/Shanghai` 统一。

**Q9：前端流式解析 SSE 时 TextDecoder 为什么要 `stream: true`？**
A：TCP 分包可能把一个多字节 UTF-8 字符（中文 3B/emoji 4B）从中间截断，不带 stream 参数会把残缺字节解码成替换符 �。`stream: true` 让解码器保留不完整序列等下一包拼上——编码与传输在流式场景的交叉点（见 05-HTTP与TLS §4.2）。

---

## 八、动手练习

1. 在测试库建一张 `t_enc(content varchar(20) charset utf8)`（故意 mb3）表，插入 `订单😊` 观察报错；把列改成 `utf8mb4` 再插，全程截图，写下两行结论。
2. 用 jshell 验证 §3.1：`"订单😊".length()`、`codePointCount`、`substring(2,3)` 的输出；再写一个"按码点安全截断到 n"的工具方法，用它替换你项目里某处 `substring` 截断。
3. 执行 §6.3 对表命令，记录项目"三口钟"当前值；给 `ai-cs-chat` 容器加 `TZ: Asia/Shanghai` 重启后，对比日志时间戳前后差异，确认落库值不变（为什么？serverTimezone 换算的位置变了）。
4. 制造一次真实截断事故：对含 emoji 的字符串 `substring(0, 3)` 后经 JDBC 写入 `chat_message.content`，查询展示乱码；再用 `offsetByCodePoints` 版本修复对比。
5. 审计练习：grep 全部 `application*.yml` 与 `tools/nacos-config/*.yml` 的 JDBC URL，列表核对每个数据源的 `characterEncoding/serverTimezone` 是否统一；对不一致的条目评估风险并写 5 行整改建议。

---

> 上一篇：[06-容器隔离原理-namespace与cgroup](./06-容器隔离原理-namespace与cgroup.md) ｜ 下一篇：[08-CPU缓存与内存序](./08-CPU缓存与内存序.md)
