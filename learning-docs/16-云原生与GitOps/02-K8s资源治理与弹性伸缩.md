# K8s 资源治理与弹性伸缩

> 对应项目：`deploy/k8s/services/ai-chat-service.yaml:80-98`（全项目最大档 resources + tcpSocket 探针）、`deploy/k8s/services/api-gateway.yaml:50-68`（最小档）、`deploy/k8s/services/product-service.yaml:40-58`（唯一的 httpGet 探针先例）。
> 相关：[07-运维部署/02-Kubernetes入门](../07-运维部署/02-Kubernetes入门.md) §5.4（探针三种写法，本篇只写参数陷阱增量）、[07-运维部署/06-优雅停机与零丢失滚动更新](../07-运维部署/06-优雅停机与零丢失滚动更新.md)（Pod 终止时序）、[01-Helm从零到参数化](./01-Helm从零到参数化.md)（差异如何收敛进 values）。
> ⚠️ HPA/VPA/KEDA/PDB 部分**工程未落地**（`deploy/k8s/` 下 grep `HorizontalPodAutoscaler|PodDisruptionBudget` 零命中），为目标态设计；resources/探针部分为**现状如实盘点**。

---

## 一、先盘点：8 份业务清单的真实资源画像

逐份读出的现状（真实值，非虚构）：

| 服务 | requests | limits | 堆（JAVA_OPTS） | 探针 |
|---|---|---|---|---|
| api-gateway | 100m / 256Mi | 500m / 512Mi | 256m/256m | tcpSocket |
| user-service | 100m / 256Mi | 500m / 512Mi | 256m/256m | tcpSocket |
| knowledge-service | 100m / 256Mi | 500m / 512Mi | 256m/256m | tcpSocket |
| ai-chat-service | **200m / 512Mi** | **1000m / 1Gi** | **512m/512m** | tcpSocket |
| search-service | 100m / 256Mi | 500m / 512Mi | 256m/256m | tcpSocket |
| message-service | 100m / 256Mi | 500m / 512Mi | 256m/256m | tcpSocket |
| notify-service | 100m / 256Mi | 500m / 512Mi | 256m/256m | tcpSocket |
| product-service | 250m / 256Mi | 500m / 512Mi | 无 JAVA_OPTS（镜像不展开） | **httpGet** |

第一眼结论：分了两档（普通 1C1G 上限、AI 服务 2C1G 上限），**没有 HPA、没有 PDB、没有 startupProbe，全部 replicas=1（除 product=2）**。单副本 + 无 PDB 意味着：节点 drain 时服务直接断，这是学习环境的合理取舍，但要知道欠的是什么账。

---

## 二、requests/limits 与 QoS 等级

### 2.1 两个字段各管什么

| 字段 | 作用 | 不设的后果 |
|---|---|---|
| `requests` | **调度依据**：node 剩余可分配量 ≥ requests 才放 pod | 超卖，节点过载后互相挤兑 |
| `limits` | **运行时上限**：CPU 超限被节流（可压缩），内存超限被 OOMKill（不可压缩） | 内存失控可拖死整机 |

**CPU 是可压缩资源**（超限只是变慢），**内存是不可压缩资源**（超限直接杀）。所以内存 limits 的意义是"爆炸半径"，CPU requests 的意义是"排队公平"。

### 2.2 QoS 三等级（K8s 按 requests/limits 组合自动定级）

| QoS | 判定条件 | 驱逐顺序 | 本项目 |
|---|---|---|---|
| **Guaranteed** | 每个容器 CPU/内存 requests == limits | 最后驱逐 | 无 |
| **Burstable** | 设了 requests 且至少一项 ≠ limits | 中间 | **8 份全部是 Burstable** |
| **BestEffort** | 全不设 | 最先驱逐 | 业务服务无（中间件清单另说） |

以 `ai-chat-service.yaml:80-86` 为例：requests `200m/512Mi`、limits `1000m/1Gi`，属于典型 Burstable——资源紧张时它是"中间优先级"，不会被先赶走，也不能指望最后被保护。想给关键服务（网关、pay）上 Guaranteed，把 requests==limits 即可，代价是调度更难。

### 2.3 JVM 堆与容器 limit 的对账（本项目高频考点）

现状对账表：

| 服务 | 堆上限 | 内存 limit | 堆/limit 占比 | 判定 |
|---|---|---|---|---|
| ai-chat | 512m | 1Gi | 50% | 稳妥：堆外（元空间/直接内存/线程栈）还有 512Mi 余量 |
| 其余 7 个 | 256m | 512Mi | 50% | 同上，合理 |

规律：堆只占 limit 的一半左右是 JVM 容器化的安全线。**陷阱**是镜像里写死 `-Xmx` 与 limit 脱钩——某服务把 limit 调到 256Mi 却忘了改 JAVA_OPTS，堆 256m + 堆外开销 > 256Mi，必 OOMKill。更稳的做法是不写死 `-Xmx`，改用 `-XX:MaxRAMPercentage=60`（JVM 17 默认感知容器 cgroup limit），让堆跟着 limit 走——本项目 Dockerfile 通过 `JAVA_OPTS` 注入（如 `ai-cs-chat/Dockerfile:21` 的 `sh -c` 展开），具备改造条件；但注意 `ai-cs-order/Dockerfile:21` 是 `["java","-jar","app.jar"]`，**没有 JAVA_OPTS 展开点**，注入会被无视（这是 [06 篇](./06-镜像构建优化与安全扫描.md)的改造点之一，也是 [08 篇](./08-补全order-pay-mq的K8s清单.md)必须先修镜像入口的原因）。

---

## 三、探针三兄弟与参数陷阱

### 3.1 三种探针各管一件事

| 探针 | 失败动作 | 用途 | 何时该有 |
|---|---|---|---|
| **liveness** | 重启容器 | 摆脱死锁/假死 | 慢速、保守 |
| **readiness** | 摘出 Service endpoints | 未就绪不接流量 | 启动期、依赖故障期 |
| **startup** | 在它成功前**屏蔽**另外两个 | 慢启动应用 | JVM 应用强烈建议 |

### 3.2 现状参数与逐条陷阱

现状（7 份同构清单）：

```yaml
livenessProbe:            # ai-chat-service.yaml:87-92
  tcpSocket: { port: 8083 }
  initialDelaySeconds: 60 # 硬编码等启动
  periodSeconds: 15
  timeoutSeconds: 5
readinessProbe:           # :93-98
  tcpSocket: { port: 8083 }
  initialDelaySeconds: 30
  periodSeconds: 10
```

| 陷阱 | 现状体现 | 后果 | 正解 |
|---|---|---|---|
| **用 initialDelay 硬编码启动时间** | 60s | 调快了 CrashLoop 起不来，调慢了滚动更新白等 1 分钟 | 换 `startupProbe`（failureThreshold 30 × period 3 ≈ 90s 窗口），成功后 liveness/readiness 立即接管 |
| **tcpSocket 只测端口不开箱** | 全部 7 份 | 线程池打满/假死时端口照通，liveness 永远绿 | JVM 服务换 `httpGet /actuator/health`（`aics-shared.yml:32` 已统一暴露 health；`product-service.yaml:47-58` 有现成写法） |
| **readiness 与 liveness 同源同参** | 端口一致、节奏近似 | 一旦误报，两个同时红——重启与摘流同时发生，失去层次 | liveness 只测"活着"（最简单指标），readiness 可测"能服务"（更细） |
| **failureThreshold 忘了默认 3** | 未显式设置 | liveness 3×15s=45s 才杀，假死期间还在接流量；readiness 3×10s=30s 才摘流 | 显式写，按业务容忍度调 |
| **readiness 失败被当 liveness 处理** | 概念混淆 | 依赖（DB/Redis）抖一下就重启实例，雪崩放大 | 依赖抖动应只摘流（readiness），不该重启（liveness） |

`07-运维部署/02-Kubernetes入门.md` §5.4 的思考题（"tcpSocket 的缺点"）本篇给出完整答案：升级路径 = startupProbe 引入 + readiness 换 httpGet + liveness 保持最简。

---

## 四、HPA：弹性伸缩（⚠️ 工程未落地，目标态）

`deploy/k8s/` 下无任何 HPA（grep 实证），且 replicas=1 时 HPA 无意义——**先有 ≥2 副本，再谈伸缩**。

### 4.1 指标怎么选

| 指标 | 适用 | 坑 |
|---|---|---|
| CPU 利用率 | 通用默认 | AI 对话服务流量突刺大，CPU 反应滞后；需先有 metrics-server |
| 内存 | 堆内缓存型服务 | GC 让内存曲线呈锯齿，目标值要避开锯齿谷 |
| 自定义 QPS（Prometheus Adapter） | 网关限流上限明确的服务 | 多一跳依赖：adapter + 指标口径治理 |

本项目可落地的第一版：对 `api-gateway` 用 CPU 目标 60%（限流参数 replenish-rate=5/burst=10 的真实拐点应有压测数据背书，见缺口文档"性能压测体系"行）。

### 4.2 目标态示例（基于真实 deployment 名）

```yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: api-gateway
  namespace: ai-customer-service      # 与 api-gateway.yaml:6 一致
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: api-gateway
  minReplicas: 2                      # 单副本 + PDB 会互相锁死（见 §六）
  maxReplicas: 4
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 60 }
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300 # 缩容防抖 5 分钟
```

依赖前置：集群需装 metrics-server（k3s 自带可开，Minikube 需 `minikube addons enable metrics-server`）；验证 `kubectl top pods -n ai-customer-service` 出数。本项目本地 Registry/学习集群若装不上 metrics-server，`kubectl top` 无输出时 HPA 会一直 `<unknown>`——先用压测脚本（缺口文档"性能压测体系"行的 k6 脚本）标定容量，再补 HPA 不迟。

---

## 五、VPA 与 KEDA：一句话定位（⚠️ 未落地，概念层）

| 组件 | 干什么 | 什么时候值得引入 |
|---|---|---|
| **VPA** | 按历史用量自动调 requests/limits（推荐 Off 模式只出建议） | HPA 稳定运行后，用 VPA 建议值修正"拍脑袋"的资源档（比如上面那张表的 1C1G） |
| **KEDA** | 事件驱动伸缩（MQ 积压深度、Redis 队列长度等任意指标） | message/notify 这类消费者服务——CPU 平坦但 RocketMQ 积压波动大，按积压条数伸缩比 CPU 准 |

记忆口径：**HPA 管横向（副本数）、VPA 管纵向（资源档）、KEDA 管事件指标**；KEDA 与 HPA 并不冲突——KEDA 底层就是生成 HPA。

对本项目的服务逐个判断（哪个组件、哪个指标）：

| 服务 | 建议起点 | 理由 |
|---|---|---|
| api-gateway | HPA + CPU 60% | 流量驱动型，限流参数有明确上限，CPU 拐点可压测标定 |
| message / notify | KEDA + RocketMQ 积压深度 | 消费者 CPU 曲线平坦，积压条数才是真实负载信号 |
| ai-chat-service | 先不扩（LLM 出口是瓶颈） | 伸缩瓶颈在模型 API 的吞吐与费用，Pod 数不是第一变量 |
| user / product | HPA + CPU，且先 replicas 2 | 读写型服务，扩容前先解决单副本 + PDB 的锁死问题（§六） |

---

## 六、PDB：驱逐预算（⚠️ 未落地，目标态）

**场景**：`kubectl drain <node>`（节点维护）时，K8s 要驱逐该节点上的 pod。PDB（PodDisruptionBudget）限定"这类应用同时至少要有几个存活"，驱逐器会尊重它（自愿驱逐才受管，liveness 杀容器不受管）。

```yaml
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: ai-chat-service
  namespace: ai-customer-service
spec:
  minAvailable: 1               # 至少留 1 个在服务
  selector:
    matchLabels: { app: ai-chat-service }
```

**本项目最大的坑**：8 份清单里 7 份 `replicas: 1`。单副本 + `minAvailable: 1` = **drain 被永久阻塞**（那个唯一 pod 谁也不敢动）。所以正确顺序是：

1. 先把关键服务扩到 ≥2 副本（`kubectl scale deploy/ai-chat-service --replicas=2`）；
2. 再上 PDB（`minAvailable: 1` 或 `maxUnavailable: 1`）；
3. 驱逐预算才真正生效，而不是变成维护作业的锁。

配合 [07-运维部署/06-优雅停机](../07-运维部署/06-优雅停机与零丢失滚动更新.md) 的 terminationGracePeriod 全链路：PDB 管"换节点不断服"，优雅停机管"被杀那一刻不丢请求"。

### 6.1 现状诊断命令（把本篇的账算在集群上）

```bash
# 看 QoS 等级与资源对账（Burstable 判定、OOMKilled 现场）
kubectl get pods -n ai-customer-service -o custom-columns=\
'NAME:.metadata.name,QOS:.status.qosClass,MEM:.spec.containers[*].resources.limits.memory'

# 看 OOMKill 与重启原因
kubectl describe pod ai-chat-service -n ai-customer-service | grep -A3 "Last State"

# 看驱逐事件（资源压力时谁先被赶）
kubectl get events -n ai-customer-service --field-selector reason=Evicted

# 节点可分配量 vs requests 汇总（调度余量）
kubectl describe node <node> | sed -n '/Allocated resources/,/Events/p'
```

四条命令分别对应本篇四个主题：QoS 判定（§2.2）、OOM 对账（§2.3）、驱逐顺序（§2.2 QoS 表）、调度容量（§2.1）。巡检脚本里各放一条，比出事后猜快得多。

---

## 面试高频问答

**Q1：requests 和 limits 分别影响什么？**
A：requests 是调度依据（节点可分配量对账），limits 是运行时上限。CPU 超限被 CFS 节流（可压缩），内存超限被 OOMKill（不可压缩）。

**Q2：QoS 等级怎么划分，驱逐顺序如何？**
A：Guaranteed（每容器 CPU/内存 requests==limits）> Burstable > BestEffort（全不设）。节点资源压力下按 BestEffort → Burstable → Guaranteed 顺序驱逐。本项目 8 份清单全是 Burstable。

**Q3：容器里 JVM 的 -Xmx 应该设成 limit 的多少？**
A：经验值 50%~70%，剩余留给元空间、直接内存、线程栈、JIT。更稳的做法是 `MaxRAMPercentage=60`（JVM 17 默认感知容器 limit），避免改 limit 忘改堆导致 OOMKill。本项目现状堆/limit=50%，合理。

**Q4：liveness 和 readiness 的本质区别？**
A：liveness 失败 → 重启容器（治"死锁/假死"）；readiness 失败 → 从 Service endpoints 摘流（治"暂时不能服务"）。依赖抖动应只摘流不重启，否则故障会被放大成雪崩。

**Q5：startupProbe 解决什么问题？**
A：给慢启动应用一个"屏蔽期"——它成功前 liveness/readiness 都不生效。相比硬编码 initialDelaySeconds: 60，窗口自适应且语义清晰。本项目 7 份清单靠 initialDelay 60 硬等，是典型可升级点。

**Q6：tcpSocket 探针为什么不可靠？**
A：它只证明"端口在 LISTEN"。线程池打满、应用假死、连接堆积时 TCP 三次握手照样成功。JVM 服务的正确姿势是 `httpGet /actuator/health`（本项目 aics-shared.yml 已统一暴露 health 端点，product-service 已有先例）。

**Q7：HPA 为什么在 replicas=1 时没意义？PDB 为什么会锁死 drain？**
A：HPA 需要 ≥2 副本才有伸缩空间。PDB minAvailable:1 + 单副本 = 唯一 pod 受保护，驱逐器无法完成任务，drain 永久挂起——所以扩容必须先于 PDB。

**Q8：CPU 限流（throttling）和 OOMKill 排查上有什么不同？**
A：限流表现为延迟毛刺，`kubectl top` 看 CPU 逼近 limits、指标里 throttled periods 升高；OOMKill 表现为 pod 重启（RestartCount 增加），`kubectl describe pod` 的 Last State 显示 Reason: OOMKilled、Exit Code 137。

**Q9：`kubectl set image` 和直接改 YAML 再 apply 有什么区别？本项目用的哪种？**
A：set image 直接改活集群对象的字段，git 里的 YAML 不知道；改 YAML 再 apply 以文件为准做 diff。本项目脚本两条都用了——先 apply 全量清单、再 set image 打 BUILD_NUMBER tag，这也是 03 篇要讲的漂移根源。

---

## 动手练习

1. 对 `ai-chat-service` 做 OOM 实验：把内存 limits 改成 300Mi（堆 512m 不变）重新 apply，观察 `kubectl describe pod` 的 OOMKilled 与 CrashLoopBackOff——亲手验证 §2.3 的对账规律。
2. 给 `user-service` 补上 startupProbe（failureThreshold 30 × periodSeconds 3），删除原 initialDelaySeconds，观察滚动更新耗时变化。
3. 把 8 份清单的探针从 tcpSocket 批量升级为 `httpGet /actuator/health`（product-service.yaml:47-58 是模板），升级后压测网关验证假死场景能否被摘流。
4. 启用 metrics-server 后 `kubectl top pods` 记录 8 个服务的真实用量，与 §一 的 requests/limits 对账，找出"资源画像最不合理"的一个并给调整建议。
5. 给 replicas=1 的 `ai-chat-service` 先 scale 到 2、再配 PDB minAvailable:1，然后 `kubectl drain <node> --ignore-daemonsets` 验证：先体验"单副本+PDB 锁死"（把 scale 改回 1 再 drain），再验证扩容后 drain 正常轮替。
6. 用 `kubectl get pods -o custom-columns='NAME:.metadata.name,QOS:.status.qosClass'` 列出全部服务的 QoS 等级，对照 §二 判断哪些服务在节点内存紧张时最先被驱逐，并写出调整 requests/limits 的具体方案。

---

> 上一篇：[01-Helm从零到参数化](./01-Helm从零到参数化.md) ｜ 下一篇：[03-GitOps与ArgoCD](./03-GitOps与ArgoCD.md)
