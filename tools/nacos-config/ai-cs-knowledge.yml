spring:
  datasource:
    url: jdbc:mysql://123.60.31.79:3306/knowledge_db?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: ${DB_PASSWORD}

# RocketMQ（增量同步，name-server 引用 aics-shared.yml 中的共享配置）
rocketmq:
  producer:
    group: knowledge-producer-group
