spring:
  datasource:
    url: jdbc:mysql://123.60.31.79:3306/chat_db?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: ${DB_PASSWORD}
    hikari:
      connection-test-query: SELECT 1
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 600000
      max-lifetime: 1500000
      connection-timeout: 30000

rocketmq:
  name-server: 127.0.0.1:9876
  producer:
    group: chat-producer-group
  consumer:
    group: chat-consumer-group
